import "./chunk-LK32TJAX.js";

// node_modules/d3-time/src/interval.js
var t0 = /* @__PURE__ */ new Date();
var t1 = /* @__PURE__ */ new Date();
function timeInterval(floori, offseti, count3, field) {
  function interval2(date2) {
    return floori(date2 = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date2)), date2;
  }
  interval2.floor = (date2) => {
    return floori(date2 = /* @__PURE__ */ new Date(+date2)), date2;
  };
  interval2.ceil = (date2) => {
    return floori(date2 = new Date(date2 - 1)), offseti(date2, 1), floori(date2), date2;
  };
  interval2.round = (date2) => {
    const d0 = interval2(date2), d1 = interval2.ceil(date2);
    return date2 - d0 < d1 - date2 ? d0 : d1;
  };
  interval2.offset = (date2, step2) => {
    return offseti(date2 = /* @__PURE__ */ new Date(+date2), step2 == null ? 1 : Math.floor(step2)), date2;
  };
  interval2.range = (start2, stop, step2) => {
    const range2 = [];
    start2 = interval2.ceil(start2);
    step2 = step2 == null ? 1 : Math.floor(step2);
    if (!(start2 < stop) || !(step2 > 0)) return range2;
    let previous;
    do
      range2.push(previous = /* @__PURE__ */ new Date(+start2)), offseti(start2, step2), floori(start2);
    while (previous < start2 && start2 < stop);
    return range2;
  };
  interval2.filter = (test) => {
    return timeInterval((date2) => {
      if (date2 >= date2) while (floori(date2), !test(date2)) date2.setTime(date2 - 1);
    }, (date2, step2) => {
      if (date2 >= date2) {
        if (step2 < 0) while (++step2 <= 0) {
          while (offseti(date2, -1), !test(date2)) {
          }
        }
        else while (--step2 >= 0) {
          while (offseti(date2, 1), !test(date2)) {
          }
        }
      }
    });
  };
  if (count3) {
    interval2.count = (start2, end) => {
      t0.setTime(+start2), t1.setTime(+end);
      floori(t0), floori(t1);
      return Math.floor(count3(t0, t1));
    };
    interval2.every = (step2) => {
      step2 = Math.floor(step2);
      return !isFinite(step2) || !(step2 > 0) ? null : !(step2 > 1) ? interval2 : interval2.filter(field ? (d) => field(d) % step2 === 0 : (d) => interval2.count(0, d) % step2 === 0);
    };
  }
  return interval2;
}

// node_modules/d3-time/src/millisecond.js
var millisecond = timeInterval(() => {
}, (date2, step2) => {
  date2.setTime(+date2 + step2);
}, (start2, end) => {
  return end - start2;
});
millisecond.every = (k2) => {
  k2 = Math.floor(k2);
  if (!isFinite(k2) || !(k2 > 0)) return null;
  if (!(k2 > 1)) return millisecond;
  return timeInterval((date2) => {
    date2.setTime(Math.floor(date2 / k2) * k2);
  }, (date2, step2) => {
    date2.setTime(+date2 + step2 * k2);
  }, (start2, end) => {
    return (end - start2) / k2;
  });
};
var milliseconds = millisecond.range;

// node_modules/d3-time/src/duration.js
var durationSecond = 1e3;
var durationMinute = durationSecond * 60;
var durationHour = durationMinute * 60;
var durationDay = durationHour * 24;
var durationWeek = durationDay * 7;
var durationMonth = durationDay * 30;
var durationYear = durationDay * 365;

// node_modules/d3-time/src/second.js
var second = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds());
}, (date2, step2) => {
  date2.setTime(+date2 + step2 * durationSecond);
}, (start2, end) => {
  return (end - start2) / durationSecond;
}, (date2) => {
  return date2.getUTCSeconds();
});
var seconds = second.range;

// node_modules/d3-time/src/minute.js
var timeMinute = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond);
}, (date2, step2) => {
  date2.setTime(+date2 + step2 * durationMinute);
}, (start2, end) => {
  return (end - start2) / durationMinute;
}, (date2) => {
  return date2.getMinutes();
});
var timeMinutes = timeMinute.range;
var utcMinute = timeInterval((date2) => {
  date2.setUTCSeconds(0, 0);
}, (date2, step2) => {
  date2.setTime(+date2 + step2 * durationMinute);
}, (start2, end) => {
  return (end - start2) / durationMinute;
}, (date2) => {
  return date2.getUTCMinutes();
});
var utcMinutes = utcMinute.range;

// node_modules/d3-time/src/hour.js
var timeHour = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond - date2.getMinutes() * durationMinute);
}, (date2, step2) => {
  date2.setTime(+date2 + step2 * durationHour);
}, (start2, end) => {
  return (end - start2) / durationHour;
}, (date2) => {
  return date2.getHours();
});
var timeHours = timeHour.range;
var utcHour = timeInterval((date2) => {
  date2.setUTCMinutes(0, 0, 0);
}, (date2, step2) => {
  date2.setTime(+date2 + step2 * durationHour);
}, (start2, end) => {
  return (end - start2) / durationHour;
}, (date2) => {
  return date2.getUTCHours();
});
var utcHours = utcHour.range;

// node_modules/d3-time/src/day.js
var timeDay = timeInterval(
  (date2) => date2.setHours(0, 0, 0, 0),
  (date2, step2) => date2.setDate(date2.getDate() + step2),
  (start2, end) => (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute) / durationDay,
  (date2) => date2.getDate() - 1
);
var timeDays = timeDay.range;
var utcDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setUTCDate(date2.getUTCDate() + step2);
}, (start2, end) => {
  return (end - start2) / durationDay;
}, (date2) => {
  return date2.getUTCDate() - 1;
});
var utcDays = utcDay.range;
var unixDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setUTCDate(date2.getUTCDate() + step2);
}, (start2, end) => {
  return (end - start2) / durationDay;
}, (date2) => {
  return Math.floor(date2 / durationDay);
});
var unixDays = unixDay.range;

// node_modules/d3-time/src/week.js
function timeWeekday(i) {
  return timeInterval((date2) => {
    date2.setDate(date2.getDate() - (date2.getDay() + 7 - i) % 7);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step2) => {
    date2.setDate(date2.getDate() + step2 * 7);
  }, (start2, end) => {
    return (end - start2 - (end.getTimezoneOffset() - start2.getTimezoneOffset()) * durationMinute) / durationWeek;
  });
}
var timeSunday = timeWeekday(0);
var timeMonday = timeWeekday(1);
var timeTuesday = timeWeekday(2);
var timeWednesday = timeWeekday(3);
var timeThursday = timeWeekday(4);
var timeFriday = timeWeekday(5);
var timeSaturday = timeWeekday(6);
var timeSundays = timeSunday.range;
var timeMondays = timeMonday.range;
var timeTuesdays = timeTuesday.range;
var timeWednesdays = timeWednesday.range;
var timeThursdays = timeThursday.range;
var timeFridays = timeFriday.range;
var timeSaturdays = timeSaturday.range;
function utcWeekday(i) {
  return timeInterval((date2) => {
    date2.setUTCDate(date2.getUTCDate() - (date2.getUTCDay() + 7 - i) % 7);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step2) => {
    date2.setUTCDate(date2.getUTCDate() + step2 * 7);
  }, (start2, end) => {
    return (end - start2) / durationWeek;
  });
}
var utcSunday = utcWeekday(0);
var utcMonday = utcWeekday(1);
var utcTuesday = utcWeekday(2);
var utcWednesday = utcWeekday(3);
var utcThursday = utcWeekday(4);
var utcFriday = utcWeekday(5);
var utcSaturday = utcWeekday(6);
var utcSundays = utcSunday.range;
var utcMondays = utcMonday.range;
var utcTuesdays = utcTuesday.range;
var utcWednesdays = utcWednesday.range;
var utcThursdays = utcThursday.range;
var utcFridays = utcFriday.range;
var utcSaturdays = utcSaturday.range;

// node_modules/d3-time/src/month.js
var timeMonth = timeInterval((date2) => {
  date2.setDate(1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setMonth(date2.getMonth() + step2);
}, (start2, end) => {
  return end.getMonth() - start2.getMonth() + (end.getFullYear() - start2.getFullYear()) * 12;
}, (date2) => {
  return date2.getMonth();
});
var timeMonths = timeMonth.range;
var utcMonth = timeInterval((date2) => {
  date2.setUTCDate(1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setUTCMonth(date2.getUTCMonth() + step2);
}, (start2, end) => {
  return end.getUTCMonth() - start2.getUTCMonth() + (end.getUTCFullYear() - start2.getUTCFullYear()) * 12;
}, (date2) => {
  return date2.getUTCMonth();
});
var utcMonths = utcMonth.range;

// node_modules/d3-time/src/year.js
var timeYear = timeInterval((date2) => {
  date2.setMonth(0, 1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setFullYear(date2.getFullYear() + step2);
}, (start2, end) => {
  return end.getFullYear() - start2.getFullYear();
}, (date2) => {
  return date2.getFullYear();
});
timeYear.every = (k2) => {
  return !isFinite(k2 = Math.floor(k2)) || !(k2 > 0) ? null : timeInterval((date2) => {
    date2.setFullYear(Math.floor(date2.getFullYear() / k2) * k2);
    date2.setMonth(0, 1);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step2) => {
    date2.setFullYear(date2.getFullYear() + step2 * k2);
  });
};
var timeYears = timeYear.range;
var utcYear = timeInterval((date2) => {
  date2.setUTCMonth(0, 1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step2) => {
  date2.setUTCFullYear(date2.getUTCFullYear() + step2);
}, (start2, end) => {
  return end.getUTCFullYear() - start2.getUTCFullYear();
}, (date2) => {
  return date2.getUTCFullYear();
});
utcYear.every = (k2) => {
  return !isFinite(k2 = Math.floor(k2)) || !(k2 > 0) ? null : timeInterval((date2) => {
    date2.setUTCFullYear(Math.floor(date2.getUTCFullYear() / k2) * k2);
    date2.setUTCMonth(0, 1);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step2) => {
    date2.setUTCFullYear(date2.getUTCFullYear() + step2 * k2);
  });
};
var utcYears = utcYear.range;

// node_modules/d3-array/src/ascending.js
function ascending(a2, b) {
  return a2 == null || b == null ? NaN : a2 < b ? -1 : a2 > b ? 1 : a2 >= b ? 0 : NaN;
}

// node_modules/d3-array/src/descending.js
function descending(a2, b) {
  return a2 == null || b == null ? NaN : b < a2 ? -1 : b > a2 ? 1 : b >= a2 ? 0 : NaN;
}

// node_modules/d3-array/src/bisector.js
function bisector(f) {
  let compare1, compare2, delta;
  if (f.length !== 2) {
    compare1 = ascending;
    compare2 = (d, x3) => ascending(f(d), x3);
    delta = (d, x3) => f(d) - x3;
  } else {
    compare1 = f === ascending || f === descending ? f : zero;
    compare2 = f;
    delta = f;
  }
  function left2(a2, x3, lo = 0, hi = a2.length) {
    if (lo < hi) {
      if (compare1(x3, x3) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a2[mid], x3) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function right2(a2, x3, lo = 0, hi = a2.length) {
    if (lo < hi) {
      if (compare1(x3, x3) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare2(a2[mid], x3) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function center2(a2, x3, lo = 0, hi = a2.length) {
    const i = left2(a2, x3, lo, hi - 1);
    return i > lo && delta(a2[i - 1], x3) > -delta(a2[i], x3) ? i - 1 : i;
  }
  return { left: left2, center: center2, right: right2 };
}
function zero() {
  return 0;
}

// node_modules/d3-array/src/number.js
function number(x3) {
  return x3 === null ? NaN : +x3;
}

// node_modules/d3-array/src/bisect.js
var ascendingBisect = bisector(ascending);
var bisectRight = ascendingBisect.right;
var bisectLeft = ascendingBisect.left;
var bisectCenter = bisector(number).center;
var bisect_default = bisectRight;

// node_modules/d3-array/src/blur.js
var blur2 = Blur2(blurf);
var blurImage = Blur2(blurfImage);
function Blur2(blur3) {
  return function(data2, rx, ry = rx) {
    if (!((rx = +rx) >= 0)) throw new RangeError("invalid rx");
    if (!((ry = +ry) >= 0)) throw new RangeError("invalid ry");
    let { data: values, width, height } = data2;
    if (!((width = Math.floor(width)) >= 0)) throw new RangeError("invalid width");
    if (!((height = Math.floor(height !== void 0 ? height : values.length / width)) >= 0)) throw new RangeError("invalid height");
    if (!width || !height || !rx && !ry) return data2;
    const blurx = rx && blur3(rx);
    const blury = ry && blur3(ry);
    const temp = values.slice();
    if (blurx && blury) {
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    } else if (blurx) {
      blurh(blurx, values, temp, width, height);
      blurh(blurx, temp, values, width, height);
      blurh(blurx, values, temp, width, height);
    } else if (blury) {
      blurv(blury, values, temp, width, height);
      blurv(blury, temp, values, width, height);
      blurv(blury, values, temp, width, height);
    }
    return data2;
  };
}
function blurh(blur3, T, S, w, h) {
  for (let y3 = 0, n = w * h; y3 < n; ) {
    blur3(T, S, y3, y3 += w, 1);
  }
}
function blurv(blur3, T, S, w, h) {
  for (let x3 = 0, n = w * h; x3 < w; ++x3) {
    blur3(T, S, x3, x3 + n, w);
  }
}
function blurfImage(radius) {
  const blur3 = blurf(radius);
  return (T, S, start2, stop, step2) => {
    start2 <<= 2, stop <<= 2, step2 <<= 2;
    blur3(T, S, start2 + 0, stop + 0, step2);
    blur3(T, S, start2 + 1, stop + 1, step2);
    blur3(T, S, start2 + 2, stop + 2, step2);
    blur3(T, S, start2 + 3, stop + 3, step2);
  };
}
function blurf(radius) {
  const radius0 = Math.floor(radius);
  if (radius0 === radius) return bluri(radius);
  const t = radius - radius0;
  const w = 2 * radius + 1;
  return (T, S, start2, stop, step2) => {
    if (!((stop -= step2) >= start2)) return;
    let sum3 = radius0 * S[start2];
    const s0 = step2 * radius0;
    const s1 = s0 + step2;
    for (let i = start2, j = start2 + s0; i < j; i += step2) {
      sum3 += S[Math.min(stop, i)];
    }
    for (let i = start2, j = stop; i <= j; i += step2) {
      sum3 += S[Math.min(stop, i + s0)];
      T[i] = (sum3 + t * (S[Math.max(start2, i - s1)] + S[Math.min(stop, i + s1)])) / w;
      sum3 -= S[Math.max(start2, i - s0)];
    }
  };
}
function bluri(radius) {
  const w = 2 * radius + 1;
  return (T, S, start2, stop, step2) => {
    if (!((stop -= step2) >= start2)) return;
    let sum3 = radius * S[start2];
    const s2 = step2 * radius;
    for (let i = start2, j = start2 + s2; i < j; i += step2) {
      sum3 += S[Math.min(stop, i)];
    }
    for (let i = start2, j = stop; i <= j; i += step2) {
      sum3 += S[Math.min(stop, i + s2)];
      T[i] = sum3 / w;
      sum3 -= S[Math.max(start2, i - s2)];
    }
  };
}

// node_modules/internmap/src/index.js
var InternMap = class extends Map {
  constructor(entries, key = keyof) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key } });
    if (entries != null) for (const [key2, value] of entries) this.set(key2, value);
  }
  get(key) {
    return super.get(intern_get(this, key));
  }
  has(key) {
    return super.has(intern_get(this, key));
  }
  set(key, value) {
    return super.set(intern_set(this, key), value);
  }
  delete(key) {
    return super.delete(intern_delete(this, key));
  }
};
function intern_get({ _intern, _key }, value) {
  const key = _key(value);
  return _intern.has(key) ? _intern.get(key) : value;
}
function intern_set({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) return _intern.get(key);
  _intern.set(key, value);
  return value;
}
function intern_delete({ _intern, _key }, value) {
  const key = _key(value);
  if (_intern.has(key)) {
    value = _intern.get(key);
    _intern.delete(key);
  }
  return value;
}
function keyof(value) {
  return value !== null && typeof value === "object" ? value.valueOf() : value;
}

// node_modules/d3-array/src/array.js
var array = Array.prototype;
var slice = array.slice;
var map = array.map;

// node_modules/d3-array/src/ticks.js
var e10 = Math.sqrt(50);
var e5 = Math.sqrt(10);
var e2 = Math.sqrt(2);
function tickSpec(start2, stop, count3) {
  const step2 = (stop - start2) / Math.max(0, count3), power = Math.floor(Math.log10(step2)), error = step2 / Math.pow(10, power), factor = error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1;
  let i1, i2, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start2 * inc);
    i2 = Math.round(stop * inc);
    if (i1 / inc < start2) ++i1;
    if (i2 / inc > stop) --i2;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start2 / inc);
    i2 = Math.round(stop / inc);
    if (i1 * inc < start2) ++i1;
    if (i2 * inc > stop) --i2;
  }
  if (i2 < i1 && 0.5 <= count3 && count3 < 2) return tickSpec(start2, stop, count3 * 2);
  return [i1, i2, inc];
}
function ticks(start2, stop, count3) {
  stop = +stop, start2 = +start2, count3 = +count3;
  if (!(count3 > 0)) return [];
  if (start2 === stop) return [start2];
  const reverse2 = stop < start2, [i1, i2, inc] = reverse2 ? tickSpec(stop, start2, count3) : tickSpec(start2, stop, count3);
  if (!(i2 >= i1)) return [];
  const n = i2 - i1 + 1, ticks2 = new Array(n);
  if (reverse2) {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks2[i] = (i2 - i) / -inc;
    else for (let i = 0; i < n; ++i) ticks2[i] = (i2 - i) * inc;
  } else {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks2[i] = (i1 + i) / -inc;
    else for (let i = 0; i < n; ++i) ticks2[i] = (i1 + i) * inc;
  }
  return ticks2;
}
function tickIncrement(start2, stop, count3) {
  stop = +stop, start2 = +start2, count3 = +count3;
  return tickSpec(start2, stop, count3)[2];
}
function tickStep(start2, stop, count3) {
  stop = +stop, start2 = +start2, count3 = +count3;
  const reverse2 = stop < start2, inc = reverse2 ? tickIncrement(stop, start2, count3) : tickIncrement(start2, stop, count3);
  return (reverse2 ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

// node_modules/d3-array/src/shuffle.js
var shuffle_default = shuffler(Math.random);
function shuffler(random) {
  return function shuffle2(array3, i0 = 0, i1 = array3.length) {
    let m = i1 - (i0 = +i0);
    while (m) {
      const i = random() * m-- | 0, t = array3[m + i0];
      array3[m + i0] = array3[i + i0];
      array3[i + i0] = t;
    }
    return array3;
  };
}

// node_modules/d3-time/src/ticks.js
function ticker(year, month, week, day, hour, minute) {
  const tickIntervals = [
    [second, 1, durationSecond],
    [second, 5, 5 * durationSecond],
    [second, 15, 15 * durationSecond],
    [second, 30, 30 * durationSecond],
    [minute, 1, durationMinute],
    [minute, 5, 5 * durationMinute],
    [minute, 15, 15 * durationMinute],
    [minute, 30, 30 * durationMinute],
    [hour, 1, durationHour],
    [hour, 3, 3 * durationHour],
    [hour, 6, 6 * durationHour],
    [hour, 12, 12 * durationHour],
    [day, 1, durationDay],
    [day, 2, 2 * durationDay],
    [week, 1, durationWeek],
    [month, 1, durationMonth],
    [month, 3, 3 * durationMonth],
    [year, 1, durationYear]
  ];
  function ticks2(start2, stop, count3) {
    const reverse2 = stop < start2;
    if (reverse2) [start2, stop] = [stop, start2];
    const interval2 = count3 && typeof count3.range === "function" ? count3 : tickInterval(start2, stop, count3);
    const ticks3 = interval2 ? interval2.range(start2, +stop + 1) : [];
    return reverse2 ? ticks3.reverse() : ticks3;
  }
  function tickInterval(start2, stop, count3) {
    const target = Math.abs(stop - start2) / count3;
    const i = bisector(([, , step3]) => step3).right(tickIntervals, target);
    if (i === tickIntervals.length) return year.every(tickStep(start2 / durationYear, stop / durationYear, count3));
    if (i === 0) return millisecond.every(Math.max(tickStep(start2, stop, count3), 1));
    const [t, step2] = tickIntervals[target / tickIntervals[i - 1][2] < tickIntervals[i][2] / target ? i - 1 : i];
    return t.every(step2);
  }
  return [ticks2, tickInterval];
}
var [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
var [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);

// node_modules/d3-time-format/src/locale.js
function localDate(d) {
  if (0 <= d.y && d.y < 100) {
    var date2 = new Date(-1, d.m, d.d, d.H, d.M, d.S, d.L);
    date2.setFullYear(d.y);
    return date2;
  }
  return new Date(d.y, d.m, d.d, d.H, d.M, d.S, d.L);
}
function utcDate(d) {
  if (0 <= d.y && d.y < 100) {
    var date2 = new Date(Date.UTC(-1, d.m, d.d, d.H, d.M, d.S, d.L));
    date2.setUTCFullYear(d.y);
    return date2;
  }
  return new Date(Date.UTC(d.y, d.m, d.d, d.H, d.M, d.S, d.L));
}
function newDate(y3, m, d) {
  return { y: y3, m, d, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale(locale3) {
  var locale_dateTime = locale3.dateTime, locale_date = locale3.date, locale_time = locale3.time, locale_periods = locale3.periods, locale_weekdays = locale3.days, locale_shortWeekdays = locale3.shortDays, locale_months = locale3.months, locale_shortMonths = locale3.shortMonths;
  var periodRe = formatRe(locale_periods), periodLookup = formatLookup(locale_periods), weekdayRe = formatRe(locale_weekdays), weekdayLookup = formatLookup(locale_weekdays), shortWeekdayRe = formatRe(locale_shortWeekdays), shortWeekdayLookup = formatLookup(locale_shortWeekdays), monthRe = formatRe(locale_months), monthLookup = formatLookup(locale_months), shortMonthRe = formatRe(locale_shortMonths), shortMonthLookup = formatLookup(locale_shortMonths);
  var formats = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth,
    "e": formatDayOfMonth,
    "f": formatMicroseconds,
    "g": formatYearISO,
    "G": formatFullYearISO,
    "H": formatHour24,
    "I": formatHour12,
    "j": formatDayOfYear,
    "L": formatMilliseconds,
    "m": formatMonthNumber,
    "M": formatMinutes,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatSeconds,
    "u": formatWeekdayNumberMonday,
    "U": formatWeekNumberSunday,
    "V": formatWeekNumberISO,
    "w": formatWeekdayNumberSunday,
    "W": formatWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatYear,
    "Y": formatFullYear,
    "Z": formatZone,
    "%": formatLiteralPercent
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth,
    "e": formatUTCDayOfMonth,
    "f": formatUTCMicroseconds,
    "g": formatUTCYearISO,
    "G": formatUTCFullYearISO,
    "H": formatUTCHour24,
    "I": formatUTCHour12,
    "j": formatUTCDayOfYear,
    "L": formatUTCMilliseconds,
    "m": formatUTCMonthNumber,
    "M": formatUTCMinutes,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatUTCSeconds,
    "u": formatUTCWeekdayNumberMonday,
    "U": formatUTCWeekNumberSunday,
    "V": formatUTCWeekNumberISO,
    "w": formatUTCWeekdayNumberSunday,
    "W": formatUTCWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatUTCYear,
    "Y": formatUTCFullYear,
    "Z": formatUTCZone,
    "%": formatLiteralPercent
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth,
    "e": parseDayOfMonth,
    "f": parseMicroseconds,
    "g": parseYear,
    "G": parseFullYear,
    "H": parseHour24,
    "I": parseHour24,
    "j": parseDayOfYear,
    "L": parseMilliseconds,
    "m": parseMonthNumber,
    "M": parseMinutes,
    "p": parsePeriod,
    "q": parseQuarter,
    "Q": parseUnixTimestamp,
    "s": parseUnixTimestampSeconds,
    "S": parseSeconds,
    "u": parseWeekdayNumberMonday,
    "U": parseWeekNumberSunday,
    "V": parseWeekNumberISO,
    "w": parseWeekdayNumberSunday,
    "W": parseWeekNumberMonday,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear,
    "Y": parseFullYear,
    "Z": parseZone,
    "%": parseLiteralPercent
  };
  formats.x = newFormat(locale_date, formats);
  formats.X = newFormat(locale_time, formats);
  formats.c = newFormat(locale_dateTime, formats);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats2) {
    return function(date2) {
      var string = [], i = -1, j = 0, n = specifier.length, c, pad3, format3;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i < n) {
        if (specifier.charCodeAt(i) === 37) {
          string.push(specifier.slice(j, i));
          if ((pad3 = pads[c = specifier.charAt(++i)]) != null) c = specifier.charAt(++i);
          else pad3 = c === "e" ? " " : "0";
          if (format3 = formats2[c]) c = format3(date2, pad3);
          string.push(c);
          j = i + 1;
        }
      }
      string.push(specifier.slice(j, i));
      return string.join("");
    };
  }
  function newParse(specifier, Z) {
    return function(string) {
      var d = newDate(1900, void 0, 1), i = parseSpecifier(d, specifier, string += "", 0), week, day;
      if (i != string.length) return null;
      if ("Q" in d) return new Date(d.Q);
      if ("s" in d) return new Date(d.s * 1e3 + ("L" in d ? d.L : 0));
      if (Z && !("Z" in d)) d.Z = 0;
      if ("p" in d) d.H = d.H % 12 + d.p * 12;
      if (d.m === void 0) d.m = "q" in d ? d.q : 0;
      if ("V" in d) {
        if (d.V < 1 || d.V > 53) return null;
        if (!("w" in d)) d.w = 1;
        if ("Z" in d) {
          week = utcDate(newDate(d.y, 0, 1)), day = week.getUTCDay();
          week = day > 4 || day === 0 ? utcMonday.ceil(week) : utcMonday(week);
          week = utcDay.offset(week, (d.V - 1) * 7);
          d.y = week.getUTCFullYear();
          d.m = week.getUTCMonth();
          d.d = week.getUTCDate() + (d.w + 6) % 7;
        } else {
          week = localDate(newDate(d.y, 0, 1)), day = week.getDay();
          week = day > 4 || day === 0 ? timeMonday.ceil(week) : timeMonday(week);
          week = timeDay.offset(week, (d.V - 1) * 7);
          d.y = week.getFullYear();
          d.m = week.getMonth();
          d.d = week.getDate() + (d.w + 6) % 7;
        }
      } else if ("W" in d || "U" in d) {
        if (!("w" in d)) d.w = "u" in d ? d.u % 7 : "W" in d ? 1 : 0;
        day = "Z" in d ? utcDate(newDate(d.y, 0, 1)).getUTCDay() : localDate(newDate(d.y, 0, 1)).getDay();
        d.m = 0;
        d.d = "W" in d ? (d.w + 6) % 7 + d.W * 7 - (day + 5) % 7 : d.w + d.U * 7 - (day + 6) % 7;
      }
      if ("Z" in d) {
        d.H += d.Z / 100 | 0;
        d.M += d.Z % 100;
        return utcDate(d);
      }
      return localDate(d);
    };
  }
  function parseSpecifier(d, specifier, string, j) {
    var i = 0, n = specifier.length, m = string.length, c, parse;
    while (i < n) {
      if (j >= m) return -1;
      c = specifier.charCodeAt(i++);
      if (c === 37) {
        c = specifier.charAt(i++);
        parse = parses[c in pads ? specifier.charAt(i++) : c];
        if (!parse || (j = parse(d, string, j)) < 0) return -1;
      } else if (c != string.charCodeAt(j++)) {
        return -1;
      }
    }
    return j;
  }
  function parsePeriod(d, string, i) {
    var n = periodRe.exec(string.slice(i));
    return n ? (d.p = periodLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseShortWeekday(d, string, i) {
    var n = shortWeekdayRe.exec(string.slice(i));
    return n ? (d.w = shortWeekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseWeekday(d, string, i) {
    var n = weekdayRe.exec(string.slice(i));
    return n ? (d.w = weekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseShortMonth(d, string, i) {
    var n = shortMonthRe.exec(string.slice(i));
    return n ? (d.m = shortMonthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseMonth(d, string, i) {
    var n = monthRe.exec(string.slice(i));
    return n ? (d.m = monthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseLocaleDateTime(d, string, i) {
    return parseSpecifier(d, locale_dateTime, string, i);
  }
  function parseLocaleDate(d, string, i) {
    return parseSpecifier(d, locale_date, string, i);
  }
  function parseLocaleTime(d, string, i) {
    return parseSpecifier(d, locale_time, string, i);
  }
  function formatShortWeekday(d) {
    return locale_shortWeekdays[d.getDay()];
  }
  function formatWeekday(d) {
    return locale_weekdays[d.getDay()];
  }
  function formatShortMonth(d) {
    return locale_shortMonths[d.getMonth()];
  }
  function formatMonth(d) {
    return locale_months[d.getMonth()];
  }
  function formatPeriod(d) {
    return locale_periods[+(d.getHours() >= 12)];
  }
  function formatQuarter(d) {
    return 1 + ~~(d.getMonth() / 3);
  }
  function formatUTCShortWeekday(d) {
    return locale_shortWeekdays[d.getUTCDay()];
  }
  function formatUTCWeekday(d) {
    return locale_weekdays[d.getUTCDay()];
  }
  function formatUTCShortMonth(d) {
    return locale_shortMonths[d.getUTCMonth()];
  }
  function formatUTCMonth(d) {
    return locale_months[d.getUTCMonth()];
  }
  function formatUTCPeriod(d) {
    return locale_periods[+(d.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d) {
    return 1 + ~~(d.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f = newFormat(specifier += "", formats);
      f.toString = function() {
        return specifier;
      };
      return f;
    },
    parse: function(specifier) {
      var p = newParse(specifier += "", false);
      p.toString = function() {
        return specifier;
      };
      return p;
    },
    utcFormat: function(specifier) {
      var f = newFormat(specifier += "", utcFormats);
      f.toString = function() {
        return specifier;
      };
      return f;
    },
    utcParse: function(specifier) {
      var p = newParse(specifier += "", true);
      p.toString = function() {
        return specifier;
      };
      return p;
    }
  };
}
var pads = { "-": "", "_": " ", "0": "0" };
var numberRe = /^\s*\d+/;
var percentRe = /^%/;
var requoteRe = /[\\^$*+?|[\]().{}]/g;
function pad(value, fill, width) {
  var sign2 = value < 0 ? "-" : "", string = (sign2 ? -value : value) + "", length = string.length;
  return sign2 + (length < width ? new Array(width - length + 1).join(fill) + string : string);
}
function requote(s2) {
  return s2.replace(requoteRe, "\\$&");
}
function formatRe(names) {
  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
}
function formatLookup(names) {
  return new Map(names.map((name, i) => [name.toLowerCase(), i]));
}
function parseWeekdayNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.w = +n[0], i + n[0].length) : -1;
}
function parseWeekdayNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.u = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.U = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberISO(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.V = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.W = +n[0], i + n[0].length) : -1;
}
function parseFullYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 4));
  return n ? (d.y = +n[0], i + n[0].length) : -1;
}
function parseYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.y = +n[0] + (+n[0] > 68 ? 1900 : 2e3), i + n[0].length) : -1;
}
function parseZone(d, string, i) {
  var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i, i + 6));
  return n ? (d.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), i + n[0].length) : -1;
}
function parseQuarter(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.q = n[0] * 3 - 3, i + n[0].length) : -1;
}
function parseMonthNumber(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.m = n[0] - 1, i + n[0].length) : -1;
}
function parseDayOfMonth(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.d = +n[0], i + n[0].length) : -1;
}
function parseDayOfYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.m = 0, d.d = +n[0], i + n[0].length) : -1;
}
function parseHour24(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.H = +n[0], i + n[0].length) : -1;
}
function parseMinutes(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.M = +n[0], i + n[0].length) : -1;
}
function parseSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.S = +n[0], i + n[0].length) : -1;
}
function parseMilliseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.L = +n[0], i + n[0].length) : -1;
}
function parseMicroseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 6));
  return n ? (d.L = Math.floor(n[0] / 1e3), i + n[0].length) : -1;
}
function parseLiteralPercent(d, string, i) {
  var n = percentRe.exec(string.slice(i, i + 1));
  return n ? i + n[0].length : -1;
}
function parseUnixTimestamp(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.Q = +n[0], i + n[0].length) : -1;
}
function parseUnixTimestampSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.s = +n[0], i + n[0].length) : -1;
}
function formatDayOfMonth(d, p) {
  return pad(d.getDate(), p, 2);
}
function formatHour24(d, p) {
  return pad(d.getHours(), p, 2);
}
function formatHour12(d, p) {
  return pad(d.getHours() % 12 || 12, p, 2);
}
function formatDayOfYear(d, p) {
  return pad(1 + timeDay.count(timeYear(d), d), p, 3);
}
function formatMilliseconds(d, p) {
  return pad(d.getMilliseconds(), p, 3);
}
function formatMicroseconds(d, p) {
  return formatMilliseconds(d, p) + "000";
}
function formatMonthNumber(d, p) {
  return pad(d.getMonth() + 1, p, 2);
}
function formatMinutes(d, p) {
  return pad(d.getMinutes(), p, 2);
}
function formatSeconds(d, p) {
  return pad(d.getSeconds(), p, 2);
}
function formatWeekdayNumberMonday(d) {
  var day = d.getDay();
  return day === 0 ? 7 : day;
}
function formatWeekNumberSunday(d, p) {
  return pad(timeSunday.count(timeYear(d) - 1, d), p, 2);
}
function dISO(d) {
  var day = d.getDay();
  return day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
}
function formatWeekNumberISO(d, p) {
  d = dISO(d);
  return pad(timeThursday.count(timeYear(d), d) + (timeYear(d).getDay() === 4), p, 2);
}
function formatWeekdayNumberSunday(d) {
  return d.getDay();
}
function formatWeekNumberMonday(d, p) {
  return pad(timeMonday.count(timeYear(d) - 1, d), p, 2);
}
function formatYear(d, p) {
  return pad(d.getFullYear() % 100, p, 2);
}
function formatYearISO(d, p) {
  d = dISO(d);
  return pad(d.getFullYear() % 100, p, 2);
}
function formatFullYear(d, p) {
  return pad(d.getFullYear() % 1e4, p, 4);
}
function formatFullYearISO(d, p) {
  var day = d.getDay();
  d = day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
  return pad(d.getFullYear() % 1e4, p, 4);
}
function formatZone(d) {
  var z = d.getTimezoneOffset();
  return (z > 0 ? "-" : (z *= -1, "+")) + pad(z / 60 | 0, "0", 2) + pad(z % 60, "0", 2);
}
function formatUTCDayOfMonth(d, p) {
  return pad(d.getUTCDate(), p, 2);
}
function formatUTCHour24(d, p) {
  return pad(d.getUTCHours(), p, 2);
}
function formatUTCHour12(d, p) {
  return pad(d.getUTCHours() % 12 || 12, p, 2);
}
function formatUTCDayOfYear(d, p) {
  return pad(1 + utcDay.count(utcYear(d), d), p, 3);
}
function formatUTCMilliseconds(d, p) {
  return pad(d.getUTCMilliseconds(), p, 3);
}
function formatUTCMicroseconds(d, p) {
  return formatUTCMilliseconds(d, p) + "000";
}
function formatUTCMonthNumber(d, p) {
  return pad(d.getUTCMonth() + 1, p, 2);
}
function formatUTCMinutes(d, p) {
  return pad(d.getUTCMinutes(), p, 2);
}
function formatUTCSeconds(d, p) {
  return pad(d.getUTCSeconds(), p, 2);
}
function formatUTCWeekdayNumberMonday(d) {
  var dow = d.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday(d, p) {
  return pad(utcSunday.count(utcYear(d) - 1, d), p, 2);
}
function UTCdISO(d) {
  var day = d.getUTCDay();
  return day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
}
function formatUTCWeekNumberISO(d, p) {
  d = UTCdISO(d);
  return pad(utcThursday.count(utcYear(d), d) + (utcYear(d).getUTCDay() === 4), p, 2);
}
function formatUTCWeekdayNumberSunday(d) {
  return d.getUTCDay();
}
function formatUTCWeekNumberMonday(d, p) {
  return pad(utcMonday.count(utcYear(d) - 1, d), p, 2);
}
function formatUTCYear(d, p) {
  return pad(d.getUTCFullYear() % 100, p, 2);
}
function formatUTCYearISO(d, p) {
  d = UTCdISO(d);
  return pad(d.getUTCFullYear() % 100, p, 2);
}
function formatUTCFullYear(d, p) {
  return pad(d.getUTCFullYear() % 1e4, p, 4);
}
function formatUTCFullYearISO(d, p) {
  var day = d.getUTCDay();
  d = day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
  return pad(d.getUTCFullYear() % 1e4, p, 4);
}
function formatUTCZone() {
  return "+0000";
}
function formatLiteralPercent() {
  return "%";
}
function formatUnixTimestamp(d) {
  return +d;
}
function formatUnixTimestampSeconds(d) {
  return Math.floor(+d / 1e3);
}

// node_modules/d3-time-format/src/defaultLocale.js
var locale;
var timeFormat;
var timeParse;
var utcFormat;
var utcParse;
defaultLocale({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale(definition) {
  locale = formatLocale(definition);
  timeFormat = locale.format;
  timeParse = locale.parse;
  utcFormat = locale.utcFormat;
  utcParse = locale.utcParse;
  return locale;
}

// node_modules/d3-time-format/src/isoFormat.js
var isoSpecifier = "%Y-%m-%dT%H:%M:%S.%LZ";
function formatIsoNative(date2) {
  return date2.toISOString();
}
var formatIso = Date.prototype.toISOString ? formatIsoNative : utcFormat(isoSpecifier);

// node_modules/d3-time-format/src/isoParse.js
function parseIsoNative(string) {
  var date2 = new Date(string);
  return isNaN(date2) ? null : date2;
}
var parseIso = +/* @__PURE__ */ new Date("2000-01-01T00:00:00.000Z") ? parseIsoNative : utcParse(isoSpecifier);

// node_modules/d3-selection/src/namespaces.js
var xhtml = "http://www.w3.org/1999/xhtml";
var namespaces_default = {
  svg: "http://www.w3.org/2000/svg",
  xhtml,
  xlink: "http://www.w3.org/1999/xlink",
  xml: "http://www.w3.org/XML/1998/namespace",
  xmlns: "http://www.w3.org/2000/xmlns/"
};

// node_modules/d3-selection/src/namespace.js
function namespace_default(name) {
  var prefix = name += "", i = prefix.indexOf(":");
  if (i >= 0 && (prefix = name.slice(0, i)) !== "xmlns") name = name.slice(i + 1);
  return namespaces_default.hasOwnProperty(prefix) ? { space: namespaces_default[prefix], local: name } : name;
}

// node_modules/d3-selection/src/creator.js
function creatorInherit(name) {
  return function() {
    var document2 = this.ownerDocument, uri = this.namespaceURI;
    return uri === xhtml && document2.documentElement.namespaceURI === xhtml ? document2.createElement(name) : document2.createElementNS(uri, name);
  };
}
function creatorFixed(fullname) {
  return function() {
    return this.ownerDocument.createElementNS(fullname.space, fullname.local);
  };
}
function creator_default(name) {
  var fullname = namespace_default(name);
  return (fullname.local ? creatorFixed : creatorInherit)(fullname);
}

// node_modules/d3-selection/src/selector.js
function none() {
}
function selector_default(selector) {
  return selector == null ? none : function() {
    return this.querySelector(selector);
  };
}

// node_modules/d3-selection/src/selection/select.js
function select_default(select) {
  if (typeof select !== "function") select = selector_default(select);
  for (var groups2 = this._groups, m = groups2.length, subgroups = new Array(m), j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, subgroup = subgroups[j] = new Array(n), node, subnode, i = 0; i < n; ++i) {
      if ((node = group2[i]) && (subnode = select.call(node, node.__data__, i, group2))) {
        if ("__data__" in node) subnode.__data__ = node.__data__;
        subgroup[i] = subnode;
      }
    }
  }
  return new Selection(subgroups, this._parents);
}

// node_modules/d3-selection/src/array.js
function array2(x3) {
  return x3 == null ? [] : Array.isArray(x3) ? x3 : Array.from(x3);
}

// node_modules/d3-selection/src/selectorAll.js
function empty() {
  return [];
}
function selectorAll_default(selector) {
  return selector == null ? empty : function() {
    return this.querySelectorAll(selector);
  };
}

// node_modules/d3-selection/src/selection/selectAll.js
function arrayAll(select) {
  return function() {
    return array2(select.apply(this, arguments));
  };
}
function selectAll_default(select) {
  if (typeof select === "function") select = arrayAll(select);
  else select = selectorAll_default(select);
  for (var groups2 = this._groups, m = groups2.length, subgroups = [], parents = [], j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, node, i = 0; i < n; ++i) {
      if (node = group2[i]) {
        subgroups.push(select.call(node, node.__data__, i, group2));
        parents.push(node);
      }
    }
  }
  return new Selection(subgroups, parents);
}

// node_modules/d3-selection/src/matcher.js
function matcher_default(selector) {
  return function() {
    return this.matches(selector);
  };
}
function childMatcher(selector) {
  return function(node) {
    return node.matches(selector);
  };
}

// node_modules/d3-selection/src/selection/selectChild.js
var find = Array.prototype.find;
function childFind(match) {
  return function() {
    return find.call(this.children, match);
  };
}
function childFirst() {
  return this.firstElementChild;
}
function selectChild_default(match) {
  return this.select(match == null ? childFirst : childFind(typeof match === "function" ? match : childMatcher(match)));
}

// node_modules/d3-selection/src/selection/selectChildren.js
var filter2 = Array.prototype.filter;
function children() {
  return Array.from(this.children);
}
function childrenFilter(match) {
  return function() {
    return filter2.call(this.children, match);
  };
}
function selectChildren_default(match) {
  return this.selectAll(match == null ? children : childrenFilter(typeof match === "function" ? match : childMatcher(match)));
}

// node_modules/d3-selection/src/selection/filter.js
function filter_default(match) {
  if (typeof match !== "function") match = matcher_default(match);
  for (var groups2 = this._groups, m = groups2.length, subgroups = new Array(m), j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, subgroup = subgroups[j] = [], node, i = 0; i < n; ++i) {
      if ((node = group2[i]) && match.call(node, node.__data__, i, group2)) {
        subgroup.push(node);
      }
    }
  }
  return new Selection(subgroups, this._parents);
}

// node_modules/d3-selection/src/selection/sparse.js
function sparse_default(update) {
  return new Array(update.length);
}

// node_modules/d3-selection/src/selection/enter.js
function enter_default() {
  return new Selection(this._enter || this._groups.map(sparse_default), this._parents);
}
function EnterNode(parent, datum2) {
  this.ownerDocument = parent.ownerDocument;
  this.namespaceURI = parent.namespaceURI;
  this._next = null;
  this._parent = parent;
  this.__data__ = datum2;
}
EnterNode.prototype = {
  constructor: EnterNode,
  appendChild: function(child) {
    return this._parent.insertBefore(child, this._next);
  },
  insertBefore: function(child, next) {
    return this._parent.insertBefore(child, next);
  },
  querySelector: function(selector) {
    return this._parent.querySelector(selector);
  },
  querySelectorAll: function(selector) {
    return this._parent.querySelectorAll(selector);
  }
};

// node_modules/d3-selection/src/constant.js
function constant_default(x3) {
  return function() {
    return x3;
  };
}

// node_modules/d3-selection/src/selection/data.js
function bindIndex(parent, group2, enter, update, exit, data2) {
  var i = 0, node, groupLength = group2.length, dataLength = data2.length;
  for (; i < dataLength; ++i) {
    if (node = group2[i]) {
      node.__data__ = data2[i];
      update[i] = node;
    } else {
      enter[i] = new EnterNode(parent, data2[i]);
    }
  }
  for (; i < groupLength; ++i) {
    if (node = group2[i]) {
      exit[i] = node;
    }
  }
}
function bindKey(parent, group2, enter, update, exit, data2, key) {
  var i, node, nodeByKeyValue = /* @__PURE__ */ new Map(), groupLength = group2.length, dataLength = data2.length, keyValues = new Array(groupLength), keyValue;
  for (i = 0; i < groupLength; ++i) {
    if (node = group2[i]) {
      keyValues[i] = keyValue = key.call(node, node.__data__, i, group2) + "";
      if (nodeByKeyValue.has(keyValue)) {
        exit[i] = node;
      } else {
        nodeByKeyValue.set(keyValue, node);
      }
    }
  }
  for (i = 0; i < dataLength; ++i) {
    keyValue = key.call(parent, data2[i], i, data2) + "";
    if (node = nodeByKeyValue.get(keyValue)) {
      update[i] = node;
      node.__data__ = data2[i];
      nodeByKeyValue.delete(keyValue);
    } else {
      enter[i] = new EnterNode(parent, data2[i]);
    }
  }
  for (i = 0; i < groupLength; ++i) {
    if ((node = group2[i]) && nodeByKeyValue.get(keyValues[i]) === node) {
      exit[i] = node;
    }
  }
}
function datum(node) {
  return node.__data__;
}
function data_default(value, key) {
  if (!arguments.length) return Array.from(this, datum);
  var bind = key ? bindKey : bindIndex, parents = this._parents, groups2 = this._groups;
  if (typeof value !== "function") value = constant_default(value);
  for (var m = groups2.length, update = new Array(m), enter = new Array(m), exit = new Array(m), j = 0; j < m; ++j) {
    var parent = parents[j], group2 = groups2[j], groupLength = group2.length, data2 = arraylike(value.call(parent, parent && parent.__data__, j, parents)), dataLength = data2.length, enterGroup = enter[j] = new Array(dataLength), updateGroup = update[j] = new Array(dataLength), exitGroup = exit[j] = new Array(groupLength);
    bind(parent, group2, enterGroup, updateGroup, exitGroup, data2, key);
    for (var i0 = 0, i1 = 0, previous, next; i0 < dataLength; ++i0) {
      if (previous = enterGroup[i0]) {
        if (i0 >= i1) i1 = i0 + 1;
        while (!(next = updateGroup[i1]) && ++i1 < dataLength) ;
        previous._next = next || null;
      }
    }
  }
  update = new Selection(update, parents);
  update._enter = enter;
  update._exit = exit;
  return update;
}
function arraylike(data2) {
  return typeof data2 === "object" && "length" in data2 ? data2 : Array.from(data2);
}

// node_modules/d3-selection/src/selection/exit.js
function exit_default() {
  return new Selection(this._exit || this._groups.map(sparse_default), this._parents);
}

// node_modules/d3-selection/src/selection/join.js
function join_default(onenter, onupdate, onexit) {
  var enter = this.enter(), update = this, exit = this.exit();
  if (typeof onenter === "function") {
    enter = onenter(enter);
    if (enter) enter = enter.selection();
  } else {
    enter = enter.append(onenter + "");
  }
  if (onupdate != null) {
    update = onupdate(update);
    if (update) update = update.selection();
  }
  if (onexit == null) exit.remove();
  else onexit(exit);
  return enter && update ? enter.merge(update).order() : update;
}

// node_modules/d3-selection/src/selection/merge.js
function merge_default(context) {
  var selection3 = context.selection ? context.selection() : context;
  for (var groups0 = this._groups, groups1 = selection3._groups, m0 = groups0.length, m1 = groups1.length, m = Math.min(m0, m1), merges = new Array(m0), j = 0; j < m; ++j) {
    for (var group0 = groups0[j], group1 = groups1[j], n = group0.length, merge2 = merges[j] = new Array(n), node, i = 0; i < n; ++i) {
      if (node = group0[i] || group1[i]) {
        merge2[i] = node;
      }
    }
  }
  for (; j < m0; ++j) {
    merges[j] = groups0[j];
  }
  return new Selection(merges, this._parents);
}

// node_modules/d3-selection/src/selection/order.js
function order_default() {
  for (var groups2 = this._groups, j = -1, m = groups2.length; ++j < m; ) {
    for (var group2 = groups2[j], i = group2.length - 1, next = group2[i], node; --i >= 0; ) {
      if (node = group2[i]) {
        if (next && node.compareDocumentPosition(next) ^ 4) next.parentNode.insertBefore(node, next);
        next = node;
      }
    }
  }
  return this;
}

// node_modules/d3-selection/src/selection/sort.js
function sort_default(compare) {
  if (!compare) compare = ascending2;
  function compareNode(a2, b) {
    return a2 && b ? compare(a2.__data__, b.__data__) : !a2 - !b;
  }
  for (var groups2 = this._groups, m = groups2.length, sortgroups = new Array(m), j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, sortgroup = sortgroups[j] = new Array(n), node, i = 0; i < n; ++i) {
      if (node = group2[i]) {
        sortgroup[i] = node;
      }
    }
    sortgroup.sort(compareNode);
  }
  return new Selection(sortgroups, this._parents).order();
}
function ascending2(a2, b) {
  return a2 < b ? -1 : a2 > b ? 1 : a2 >= b ? 0 : NaN;
}

// node_modules/d3-selection/src/selection/call.js
function call_default() {
  var callback = arguments[0];
  arguments[0] = this;
  callback.apply(null, arguments);
  return this;
}

// node_modules/d3-selection/src/selection/nodes.js
function nodes_default() {
  return Array.from(this);
}

// node_modules/d3-selection/src/selection/node.js
function node_default() {
  for (var groups2 = this._groups, j = 0, m = groups2.length; j < m; ++j) {
    for (var group2 = groups2[j], i = 0, n = group2.length; i < n; ++i) {
      var node = group2[i];
      if (node) return node;
    }
  }
  return null;
}

// node_modules/d3-selection/src/selection/size.js
function size_default() {
  let size2 = 0;
  for (const node of this) ++size2;
  return size2;
}

// node_modules/d3-selection/src/selection/empty.js
function empty_default() {
  return !this.node();
}

// node_modules/d3-selection/src/selection/each.js
function each_default(callback) {
  for (var groups2 = this._groups, j = 0, m = groups2.length; j < m; ++j) {
    for (var group2 = groups2[j], i = 0, n = group2.length, node; i < n; ++i) {
      if (node = group2[i]) callback.call(node, node.__data__, i, group2);
    }
  }
  return this;
}

// node_modules/d3-selection/src/selection/attr.js
function attrRemove(name) {
  return function() {
    this.removeAttribute(name);
  };
}
function attrRemoveNS(fullname) {
  return function() {
    this.removeAttributeNS(fullname.space, fullname.local);
  };
}
function attrConstant(name, value) {
  return function() {
    this.setAttribute(name, value);
  };
}
function attrConstantNS(fullname, value) {
  return function() {
    this.setAttributeNS(fullname.space, fullname.local, value);
  };
}
function attrFunction(name, value) {
  return function() {
    var v = value.apply(this, arguments);
    if (v == null) this.removeAttribute(name);
    else this.setAttribute(name, v);
  };
}
function attrFunctionNS(fullname, value) {
  return function() {
    var v = value.apply(this, arguments);
    if (v == null) this.removeAttributeNS(fullname.space, fullname.local);
    else this.setAttributeNS(fullname.space, fullname.local, v);
  };
}
function attr_default(name, value) {
  var fullname = namespace_default(name);
  if (arguments.length < 2) {
    var node = this.node();
    return fullname.local ? node.getAttributeNS(fullname.space, fullname.local) : node.getAttribute(fullname);
  }
  return this.each((value == null ? fullname.local ? attrRemoveNS : attrRemove : typeof value === "function" ? fullname.local ? attrFunctionNS : attrFunction : fullname.local ? attrConstantNS : attrConstant)(fullname, value));
}

// node_modules/d3-selection/src/window.js
function window_default(node) {
  return node.ownerDocument && node.ownerDocument.defaultView || node.document && node || node.defaultView;
}

// node_modules/d3-selection/src/selection/style.js
function styleRemove(name) {
  return function() {
    this.style.removeProperty(name);
  };
}
function styleConstant(name, value, priority) {
  return function() {
    this.style.setProperty(name, value, priority);
  };
}
function styleFunction(name, value, priority) {
  return function() {
    var v = value.apply(this, arguments);
    if (v == null) this.style.removeProperty(name);
    else this.style.setProperty(name, v, priority);
  };
}
function style_default(name, value, priority) {
  return arguments.length > 1 ? this.each((value == null ? styleRemove : typeof value === "function" ? styleFunction : styleConstant)(name, value, priority == null ? "" : priority)) : styleValue(this.node(), name);
}
function styleValue(node, name) {
  return node.style.getPropertyValue(name) || window_default(node).getComputedStyle(node, null).getPropertyValue(name);
}

// node_modules/d3-selection/src/selection/property.js
function propertyRemove(name) {
  return function() {
    delete this[name];
  };
}
function propertyConstant(name, value) {
  return function() {
    this[name] = value;
  };
}
function propertyFunction(name, value) {
  return function() {
    var v = value.apply(this, arguments);
    if (v == null) delete this[name];
    else this[name] = v;
  };
}
function property_default(name, value) {
  return arguments.length > 1 ? this.each((value == null ? propertyRemove : typeof value === "function" ? propertyFunction : propertyConstant)(name, value)) : this.node()[name];
}

// node_modules/d3-selection/src/selection/classed.js
function classArray(string) {
  return string.trim().split(/^|\s+/);
}
function classList(node) {
  return node.classList || new ClassList(node);
}
function ClassList(node) {
  this._node = node;
  this._names = classArray(node.getAttribute("class") || "");
}
ClassList.prototype = {
  add: function(name) {
    var i = this._names.indexOf(name);
    if (i < 0) {
      this._names.push(name);
      this._node.setAttribute("class", this._names.join(" "));
    }
  },
  remove: function(name) {
    var i = this._names.indexOf(name);
    if (i >= 0) {
      this._names.splice(i, 1);
      this._node.setAttribute("class", this._names.join(" "));
    }
  },
  contains: function(name) {
    return this._names.indexOf(name) >= 0;
  }
};
function classedAdd(node, names) {
  var list = classList(node), i = -1, n = names.length;
  while (++i < n) list.add(names[i]);
}
function classedRemove(node, names) {
  var list = classList(node), i = -1, n = names.length;
  while (++i < n) list.remove(names[i]);
}
function classedTrue(names) {
  return function() {
    classedAdd(this, names);
  };
}
function classedFalse(names) {
  return function() {
    classedRemove(this, names);
  };
}
function classedFunction(names, value) {
  return function() {
    (value.apply(this, arguments) ? classedAdd : classedRemove)(this, names);
  };
}
function classed_default(name, value) {
  var names = classArray(name + "");
  if (arguments.length < 2) {
    var list = classList(this.node()), i = -1, n = names.length;
    while (++i < n) if (!list.contains(names[i])) return false;
    return true;
  }
  return this.each((typeof value === "function" ? classedFunction : value ? classedTrue : classedFalse)(names, value));
}

// node_modules/d3-selection/src/selection/text.js
function textRemove() {
  this.textContent = "";
}
function textConstant(value) {
  return function() {
    this.textContent = value;
  };
}
function textFunction(value) {
  return function() {
    var v = value.apply(this, arguments);
    this.textContent = v == null ? "" : v;
  };
}
function text_default(value) {
  return arguments.length ? this.each(value == null ? textRemove : (typeof value === "function" ? textFunction : textConstant)(value)) : this.node().textContent;
}

// node_modules/d3-selection/src/selection/html.js
function htmlRemove() {
  this.innerHTML = "";
}
function htmlConstant(value) {
  return function() {
    this.innerHTML = value;
  };
}
function htmlFunction(value) {
  return function() {
    var v = value.apply(this, arguments);
    this.innerHTML = v == null ? "" : v;
  };
}
function html_default(value) {
  return arguments.length ? this.each(value == null ? htmlRemove : (typeof value === "function" ? htmlFunction : htmlConstant)(value)) : this.node().innerHTML;
}

// node_modules/d3-selection/src/selection/raise.js
function raise() {
  if (this.nextSibling) this.parentNode.appendChild(this);
}
function raise_default() {
  return this.each(raise);
}

// node_modules/d3-selection/src/selection/lower.js
function lower() {
  if (this.previousSibling) this.parentNode.insertBefore(this, this.parentNode.firstChild);
}
function lower_default() {
  return this.each(lower);
}

// node_modules/d3-selection/src/selection/append.js
function append_default(name) {
  var create2 = typeof name === "function" ? name : creator_default(name);
  return this.select(function() {
    return this.appendChild(create2.apply(this, arguments));
  });
}

// node_modules/d3-selection/src/selection/insert.js
function constantNull() {
  return null;
}
function insert_default(name, before) {
  var create2 = typeof name === "function" ? name : creator_default(name), select = before == null ? constantNull : typeof before === "function" ? before : selector_default(before);
  return this.select(function() {
    return this.insertBefore(create2.apply(this, arguments), select.apply(this, arguments) || null);
  });
}

// node_modules/d3-selection/src/selection/remove.js
function remove() {
  var parent = this.parentNode;
  if (parent) parent.removeChild(this);
}
function remove_default() {
  return this.each(remove);
}

// node_modules/d3-selection/src/selection/clone.js
function selection_cloneShallow() {
  var clone = this.cloneNode(false), parent = this.parentNode;
  return parent ? parent.insertBefore(clone, this.nextSibling) : clone;
}
function selection_cloneDeep() {
  var clone = this.cloneNode(true), parent = this.parentNode;
  return parent ? parent.insertBefore(clone, this.nextSibling) : clone;
}
function clone_default(deep) {
  return this.select(deep ? selection_cloneDeep : selection_cloneShallow);
}

// node_modules/d3-selection/src/selection/datum.js
function datum_default(value) {
  return arguments.length ? this.property("__data__", value) : this.node().__data__;
}

// node_modules/d3-selection/src/selection/on.js
function contextListener(listener) {
  return function(event) {
    listener.call(this, event, this.__data__);
  };
}
function parseTypenames(typenames) {
  return typenames.trim().split(/^|\s+/).map(function(t) {
    var name = "", i = t.indexOf(".");
    if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
    return { type: t, name };
  });
}
function onRemove(typename) {
  return function() {
    var on = this.__on;
    if (!on) return;
    for (var j = 0, i = -1, m = on.length, o; j < m; ++j) {
      if (o = on[j], (!typename.type || o.type === typename.type) && o.name === typename.name) {
        this.removeEventListener(o.type, o.listener, o.options);
      } else {
        on[++i] = o;
      }
    }
    if (++i) on.length = i;
    else delete this.__on;
  };
}
function onAdd(typename, value, options2) {
  return function() {
    var on = this.__on, o, listener = contextListener(value);
    if (on) for (var j = 0, m = on.length; j < m; ++j) {
      if ((o = on[j]).type === typename.type && o.name === typename.name) {
        this.removeEventListener(o.type, o.listener, o.options);
        this.addEventListener(o.type, o.listener = listener, o.options = options2);
        o.value = value;
        return;
      }
    }
    this.addEventListener(typename.type, listener, options2);
    o = { type: typename.type, name: typename.name, value, listener, options: options2 };
    if (!on) this.__on = [o];
    else on.push(o);
  };
}
function on_default(typename, value, options2) {
  var typenames = parseTypenames(typename + ""), i, n = typenames.length, t;
  if (arguments.length < 2) {
    var on = this.node().__on;
    if (on) for (var j = 0, m = on.length, o; j < m; ++j) {
      for (i = 0, o = on[j]; i < n; ++i) {
        if ((t = typenames[i]).type === o.type && t.name === o.name) {
          return o.value;
        }
      }
    }
    return;
  }
  on = value ? onAdd : onRemove;
  for (i = 0; i < n; ++i) this.each(on(typenames[i], value, options2));
  return this;
}

// node_modules/d3-selection/src/selection/dispatch.js
function dispatchEvent(node, type2, params) {
  var window2 = window_default(node), event = window2.CustomEvent;
  if (typeof event === "function") {
    event = new event(type2, params);
  } else {
    event = window2.document.createEvent("Event");
    if (params) event.initEvent(type2, params.bubbles, params.cancelable), event.detail = params.detail;
    else event.initEvent(type2, false, false);
  }
  node.dispatchEvent(event);
}
function dispatchConstant(type2, params) {
  return function() {
    return dispatchEvent(this, type2, params);
  };
}
function dispatchFunction(type2, params) {
  return function() {
    return dispatchEvent(this, type2, params.apply(this, arguments));
  };
}
function dispatch_default(type2, params) {
  return this.each((typeof params === "function" ? dispatchFunction : dispatchConstant)(type2, params));
}

// node_modules/d3-selection/src/selection/iterator.js
function* iterator_default() {
  for (var groups2 = this._groups, j = 0, m = groups2.length; j < m; ++j) {
    for (var group2 = groups2[j], i = 0, n = group2.length, node; i < n; ++i) {
      if (node = group2[i]) yield node;
    }
  }
}

// node_modules/d3-selection/src/selection/index.js
var root = [null];
function Selection(groups2, parents) {
  this._groups = groups2;
  this._parents = parents;
}
function selection() {
  return new Selection([[document.documentElement]], root);
}
function selection_selection() {
  return this;
}
Selection.prototype = selection.prototype = {
  constructor: Selection,
  select: select_default,
  selectAll: selectAll_default,
  selectChild: selectChild_default,
  selectChildren: selectChildren_default,
  filter: filter_default,
  data: data_default,
  enter: enter_default,
  exit: exit_default,
  join: join_default,
  merge: merge_default,
  selection: selection_selection,
  order: order_default,
  sort: sort_default,
  call: call_default,
  nodes: nodes_default,
  node: node_default,
  size: size_default,
  empty: empty_default,
  each: each_default,
  attr: attr_default,
  style: style_default,
  property: property_default,
  classed: classed_default,
  text: text_default,
  html: html_default,
  raise: raise_default,
  lower: lower_default,
  append: append_default,
  insert: insert_default,
  remove: remove_default,
  clone: clone_default,
  datum: datum_default,
  on: on_default,
  dispatch: dispatch_default,
  [Symbol.iterator]: iterator_default
};
var selection_default = selection;

// node_modules/d3-selection/src/select.js
function select_default2(selector) {
  return typeof selector === "string" ? new Selection([[document.querySelector(selector)]], [document.documentElement]) : new Selection([[selector]], root);
}

// node_modules/d3-selection/src/local.js
var nextId = 0;
function local() {
  return new Local();
}
function Local() {
  this._ = "@" + (++nextId).toString(36);
}
Local.prototype = local.prototype = {
  constructor: Local,
  get: function(node) {
    var id2 = this._;
    while (!(id2 in node)) if (!(node = node.parentNode)) return;
    return node[id2];
  },
  set: function(node, value) {
    return node[this._] = value;
  },
  remove: function(node) {
    return this._ in node && delete node[this._];
  },
  toString: function() {
    return this._;
  }
};

// node_modules/d3-selection/src/sourceEvent.js
function sourceEvent_default(event) {
  let sourceEvent;
  while (sourceEvent = event.sourceEvent) event = sourceEvent;
  return event;
}

// node_modules/d3-selection/src/pointer.js
function pointer_default(event, node) {
  event = sourceEvent_default(event);
  if (node === void 0) node = event.currentTarget;
  if (node) {
    var svg = node.ownerSVGElement || node;
    if (svg.createSVGPoint) {
      var point6 = svg.createSVGPoint();
      point6.x = event.clientX, point6.y = event.clientY;
      point6 = point6.matrixTransform(node.getScreenCTM().inverse());
      return [point6.x, point6.y];
    }
    if (node.getBoundingClientRect) {
      var rect = node.getBoundingClientRect();
      return [event.clientX - rect.left - node.clientLeft, event.clientY - rect.top - node.clientTop];
    }
  }
  return [event.pageX, event.pageY];
}

// node_modules/d3-selection/src/selectAll.js
function selectAll_default2(selector) {
  return typeof selector === "string" ? new Selection([document.querySelectorAll(selector)], [document.documentElement]) : new Selection([array2(selector)], root);
}

// node_modules/d3-dispatch/src/dispatch.js
var noop = { value: () => {
} };
function dispatch() {
  for (var i = 0, n = arguments.length, _ = {}, t; i < n; ++i) {
    if (!(t = arguments[i] + "") || t in _ || /[\s.]/.test(t)) throw new Error("illegal type: " + t);
    _[t] = [];
  }
  return new Dispatch(_);
}
function Dispatch(_) {
  this._ = _;
}
function parseTypenames2(typenames, types) {
  return typenames.trim().split(/^|\s+/).map(function(t) {
    var name = "", i = t.indexOf(".");
    if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
    if (t && !types.hasOwnProperty(t)) throw new Error("unknown type: " + t);
    return { type: t, name };
  });
}
Dispatch.prototype = dispatch.prototype = {
  constructor: Dispatch,
  on: function(typename, callback) {
    var _ = this._, T = parseTypenames2(typename + "", _), t, i = -1, n = T.length;
    if (arguments.length < 2) {
      while (++i < n) if ((t = (typename = T[i]).type) && (t = get(_[t], typename.name))) return t;
      return;
    }
    if (callback != null && typeof callback !== "function") throw new Error("invalid callback: " + callback);
    while (++i < n) {
      if (t = (typename = T[i]).type) _[t] = set(_[t], typename.name, callback);
      else if (callback == null) for (t in _) _[t] = set(_[t], typename.name, null);
    }
    return this;
  },
  copy: function() {
    var copy3 = {}, _ = this._;
    for (var t in _) copy3[t] = _[t].slice();
    return new Dispatch(copy3);
  },
  call: function(type2, that) {
    if ((n = arguments.length - 2) > 0) for (var args = new Array(n), i = 0, n, t; i < n; ++i) args[i] = arguments[i + 2];
    if (!this._.hasOwnProperty(type2)) throw new Error("unknown type: " + type2);
    for (t = this._[type2], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
  },
  apply: function(type2, that, args) {
    if (!this._.hasOwnProperty(type2)) throw new Error("unknown type: " + type2);
    for (var t = this._[type2], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
  }
};
function get(type2, name) {
  for (var i = 0, n = type2.length, c; i < n; ++i) {
    if ((c = type2[i]).name === name) {
      return c.value;
    }
  }
}
function set(type2, name, callback) {
  for (var i = 0, n = type2.length; i < n; ++i) {
    if (type2[i].name === name) {
      type2[i] = noop, type2 = type2.slice(0, i).concat(type2.slice(i + 1));
      break;
    }
  }
  if (callback != null) type2.push({ name, value: callback });
  return type2;
}
var dispatch_default2 = dispatch;

// node_modules/d3-drag/src/noevent.js
var nonpassive = { passive: false };
var nonpassivecapture = { capture: true, passive: false };
function nopropagation(event) {
  event.stopImmediatePropagation();
}
function noevent_default(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}

// node_modules/d3-drag/src/nodrag.js
function nodrag_default(view) {
  var root2 = view.document.documentElement, selection3 = select_default2(view).on("dragstart.drag", noevent_default, nonpassivecapture);
  if ("onselectstart" in root2) {
    selection3.on("selectstart.drag", noevent_default, nonpassivecapture);
  } else {
    root2.__noselect = root2.style.MozUserSelect;
    root2.style.MozUserSelect = "none";
  }
}
function yesdrag(view, noclick) {
  var root2 = view.document.documentElement, selection3 = select_default2(view).on("dragstart.drag", null);
  if (noclick) {
    selection3.on("click.drag", noevent_default, nonpassivecapture);
    setTimeout(function() {
      selection3.on("click.drag", null);
    }, 0);
  }
  if ("onselectstart" in root2) {
    selection3.on("selectstart.drag", null);
  } else {
    root2.style.MozUserSelect = root2.__noselect;
    delete root2.__noselect;
  }
}

// node_modules/d3-drag/src/constant.js
var constant_default2 = (x3) => () => x3;

// node_modules/d3-drag/src/event.js
function DragEvent(type2, {
  sourceEvent,
  subject,
  target,
  identifier,
  active,
  x: x3,
  y: y3,
  dx,
  dy,
  dispatch: dispatch2
}) {
  Object.defineProperties(this, {
    type: { value: type2, enumerable: true, configurable: true },
    sourceEvent: { value: sourceEvent, enumerable: true, configurable: true },
    subject: { value: subject, enumerable: true, configurable: true },
    target: { value: target, enumerable: true, configurable: true },
    identifier: { value: identifier, enumerable: true, configurable: true },
    active: { value: active, enumerable: true, configurable: true },
    x: { value: x3, enumerable: true, configurable: true },
    y: { value: y3, enumerable: true, configurable: true },
    dx: { value: dx, enumerable: true, configurable: true },
    dy: { value: dy, enumerable: true, configurable: true },
    _: { value: dispatch2 }
  });
}
DragEvent.prototype.on = function() {
  var value = this._.on.apply(this._, arguments);
  return value === this._ ? this : value;
};

// node_modules/d3-drag/src/drag.js
function defaultFilter(event) {
  return !event.ctrlKey && !event.button;
}
function defaultContainer() {
  return this.parentNode;
}
function defaultSubject(event, d) {
  return d == null ? { x: event.x, y: event.y } : d;
}
function defaultTouchable() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function drag_default() {
  var filter3 = defaultFilter, container = defaultContainer, subject = defaultSubject, touchable = defaultTouchable, gestures = {}, listeners = dispatch_default2("start", "drag", "end"), active = 0, mousedownx, mousedowny, mousemoving, touchending, clickDistance2 = 0;
  function drag2(selection3) {
    selection3.on("mousedown.drag", mousedowned).filter(touchable).on("touchstart.drag", touchstarted).on("touchmove.drag", touchmoved, nonpassive).on("touchend.drag touchcancel.drag", touchended).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  function mousedowned(event, d) {
    if (touchending || !filter3.call(this, event, d)) return;
    var gesture = beforestart(this, container.call(this, event, d), event, d, "mouse");
    if (!gesture) return;
    select_default2(event.view).on("mousemove.drag", mousemoved, nonpassivecapture).on("mouseup.drag", mouseupped, nonpassivecapture);
    nodrag_default(event.view);
    nopropagation(event);
    mousemoving = false;
    mousedownx = event.clientX;
    mousedowny = event.clientY;
    gesture("start", event);
  }
  function mousemoved(event) {
    noevent_default(event);
    if (!mousemoving) {
      var dx = event.clientX - mousedownx, dy = event.clientY - mousedowny;
      mousemoving = dx * dx + dy * dy > clickDistance2;
    }
    gestures.mouse("drag", event);
  }
  function mouseupped(event) {
    select_default2(event.view).on("mousemove.drag mouseup.drag", null);
    yesdrag(event.view, mousemoving);
    noevent_default(event);
    gestures.mouse("end", event);
  }
  function touchstarted(event, d) {
    if (!filter3.call(this, event, d)) return;
    var touches = event.changedTouches, c = container.call(this, event, d), n = touches.length, i, gesture;
    for (i = 0; i < n; ++i) {
      if (gesture = beforestart(this, c, event, d, touches[i].identifier, touches[i])) {
        nopropagation(event);
        gesture("start", event, touches[i]);
      }
    }
  }
  function touchmoved(event) {
    var touches = event.changedTouches, n = touches.length, i, gesture;
    for (i = 0; i < n; ++i) {
      if (gesture = gestures[touches[i].identifier]) {
        noevent_default(event);
        gesture("drag", event, touches[i]);
      }
    }
  }
  function touchended(event) {
    var touches = event.changedTouches, n = touches.length, i, gesture;
    if (touchending) clearTimeout(touchending);
    touchending = setTimeout(function() {
      touchending = null;
    }, 500);
    for (i = 0; i < n; ++i) {
      if (gesture = gestures[touches[i].identifier]) {
        nopropagation(event);
        gesture("end", event, touches[i]);
      }
    }
  }
  function beforestart(that, container2, event, d, identifier, touch) {
    var dispatch2 = listeners.copy(), p = pointer_default(touch || event, container2), dx, dy, s2;
    if ((s2 = subject.call(that, new DragEvent("beforestart", {
      sourceEvent: event,
      target: drag2,
      identifier,
      active,
      x: p[0],
      y: p[1],
      dx: 0,
      dy: 0,
      dispatch: dispatch2
    }), d)) == null) return;
    dx = s2.x - p[0] || 0;
    dy = s2.y - p[1] || 0;
    return function gesture(type2, event2, touch2) {
      var p0 = p, n;
      switch (type2) {
        case "start":
          gestures[identifier] = gesture, n = active++;
          break;
        case "end":
          delete gestures[identifier], --active;
        case "drag":
          p = pointer_default(touch2 || event2, container2), n = active;
          break;
      }
      dispatch2.call(
        type2,
        that,
        new DragEvent(type2, {
          sourceEvent: event2,
          subject: s2,
          target: drag2,
          identifier,
          active: n,
          x: p[0] + dx,
          y: p[1] + dy,
          dx: p[0] - p0[0],
          dy: p[1] - p0[1],
          dispatch: dispatch2
        }),
        d
      );
    };
  }
  drag2.filter = function(_) {
    return arguments.length ? (filter3 = typeof _ === "function" ? _ : constant_default2(!!_), drag2) : filter3;
  };
  drag2.container = function(_) {
    return arguments.length ? (container = typeof _ === "function" ? _ : constant_default2(_), drag2) : container;
  };
  drag2.subject = function(_) {
    return arguments.length ? (subject = typeof _ === "function" ? _ : constant_default2(_), drag2) : subject;
  };
  drag2.touchable = function(_) {
    return arguments.length ? (touchable = typeof _ === "function" ? _ : constant_default2(!!_), drag2) : touchable;
  };
  drag2.on = function() {
    var value = listeners.on.apply(listeners, arguments);
    return value === listeners ? drag2 : value;
  };
  drag2.clickDistance = function(_) {
    return arguments.length ? (clickDistance2 = (_ = +_) * _, drag2) : Math.sqrt(clickDistance2);
  };
  return drag2;
}

// node_modules/d3-color/src/define.js
function define_default(constructor, factory, prototype) {
  constructor.prototype = factory.prototype = prototype;
  prototype.constructor = constructor;
}
function extend(parent, definition) {
  var prototype = Object.create(parent.prototype);
  for (var key in definition) prototype[key] = definition[key];
  return prototype;
}

// node_modules/d3-color/src/color.js
function Color() {
}
var darker = 0.7;
var brighter = 1 / darker;
var reI = "\\s*([+-]?\\d+)\\s*";
var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex = /^#([0-9a-f]{3,8})$/;
var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
var named = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});
function color_formatHex() {
  return this.rgb().formatHex();
}
function color_formatHex8() {
  return this.rgb().formatHex8();
}
function color_formatHsl() {
  return hslConvert(this).formatHsl();
}
function color_formatRgb() {
  return this.rgb().formatRgb();
}
function color(format3) {
  var m, l;
  format3 = (format3 + "").trim().toLowerCase();
  return (m = reHex.exec(format3)) ? (l = m[1].length, m = parseInt(m[1], 16), l === 6 ? rgbn(m) : l === 3 ? new Rgb(m >> 8 & 15 | m >> 4 & 240, m >> 4 & 15 | m & 240, (m & 15) << 4 | m & 15, 1) : l === 8 ? rgba(m >> 24 & 255, m >> 16 & 255, m >> 8 & 255, (m & 255) / 255) : l === 4 ? rgba(m >> 12 & 15 | m >> 8 & 240, m >> 8 & 15 | m >> 4 & 240, m >> 4 & 15 | m & 240, ((m & 15) << 4 | m & 15) / 255) : null) : (m = reRgbInteger.exec(format3)) ? new Rgb(m[1], m[2], m[3], 1) : (m = reRgbPercent.exec(format3)) ? new Rgb(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, 1) : (m = reRgbaInteger.exec(format3)) ? rgba(m[1], m[2], m[3], m[4]) : (m = reRgbaPercent.exec(format3)) ? rgba(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, m[4]) : (m = reHslPercent.exec(format3)) ? hsla(m[1], m[2] / 100, m[3] / 100, 1) : (m = reHslaPercent.exec(format3)) ? hsla(m[1], m[2] / 100, m[3] / 100, m[4]) : named.hasOwnProperty(format3) ? rgbn(named[format3]) : format3 === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
}
function rgbn(n) {
  return new Rgb(n >> 16 & 255, n >> 8 & 255, n & 255, 1);
}
function rgba(r, g, b, a2) {
  if (a2 <= 0) r = g = b = NaN;
  return new Rgb(r, g, b, a2);
}
function rgbConvert(o) {
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Rgb();
  o = o.rgb();
  return new Rgb(o.r, o.g, o.b, o.opacity);
}
function rgb(r, g, b, opacity) {
  return arguments.length === 1 ? rgbConvert(r) : new Rgb(r, g, b, opacity == null ? 1 : opacity);
}
function Rgb(r, g, b, opacity) {
  this.r = +r;
  this.g = +g;
  this.b = +b;
  this.opacity = +opacity;
}
define_default(Rgb, rgb, extend(Color, {
  brighter(k2) {
    k2 = k2 == null ? brighter : Math.pow(brighter, k2);
    return new Rgb(this.r * k2, this.g * k2, this.b * k2, this.opacity);
  },
  darker(k2) {
    k2 = k2 == null ? darker : Math.pow(darker, k2);
    return new Rgb(this.r * k2, this.g * k2, this.b * k2, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));
function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}
function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb() {
  const a2 = clampa(this.opacity);
  return `${a2 === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a2 === 1 ? ")" : `, ${a2})`}`;
}
function clampa(opacity) {
  return isNaN(opacity) ? 1 : Math.max(0, Math.min(1, opacity));
}
function clampi(value) {
  return Math.max(0, Math.min(255, Math.round(value) || 0));
}
function hex(value) {
  value = clampi(value);
  return (value < 16 ? "0" : "") + value.toString(16);
}
function hsla(h, s2, l, a2) {
  if (a2 <= 0) h = s2 = l = NaN;
  else if (l <= 0 || l >= 1) h = s2 = NaN;
  else if (s2 <= 0) h = NaN;
  return new Hsl(h, s2, l, a2);
}
function hslConvert(o) {
  if (o instanceof Hsl) return new Hsl(o.h, o.s, o.l, o.opacity);
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Hsl();
  if (o instanceof Hsl) return o;
  o = o.rgb();
  var r = o.r / 255, g = o.g / 255, b = o.b / 255, min4 = Math.min(r, g, b), max4 = Math.max(r, g, b), h = NaN, s2 = max4 - min4, l = (max4 + min4) / 2;
  if (s2) {
    if (r === max4) h = (g - b) / s2 + (g < b) * 6;
    else if (g === max4) h = (b - r) / s2 + 2;
    else h = (r - g) / s2 + 4;
    s2 /= l < 0.5 ? max4 + min4 : 2 - max4 - min4;
    h *= 60;
  } else {
    s2 = l > 0 && l < 1 ? 0 : h;
  }
  return new Hsl(h, s2, l, o.opacity);
}
function hsl(h, s2, l, opacity) {
  return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s2, l, opacity == null ? 1 : opacity);
}
function Hsl(h, s2, l, opacity) {
  this.h = +h;
  this.s = +s2;
  this.l = +l;
  this.opacity = +opacity;
}
define_default(Hsl, hsl, extend(Color, {
  brighter(k2) {
    k2 = k2 == null ? brighter : Math.pow(brighter, k2);
    return new Hsl(this.h, this.s, this.l * k2, this.opacity);
  },
  darker(k2) {
    k2 = k2 == null ? darker : Math.pow(darker, k2);
    return new Hsl(this.h, this.s, this.l * k2, this.opacity);
  },
  rgb() {
    var h = this.h % 360 + (this.h < 0) * 360, s2 = isNaN(h) || isNaN(this.s) ? 0 : this.s, l = this.l, m2 = l + (l < 0.5 ? l : 1 - l) * s2, m1 = 2 * l - m2;
    return new Rgb(
      hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m2),
      hsl2rgb(h, m1, m2),
      hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m2),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a2 = clampa(this.opacity);
    return `${a2 === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a2 === 1 ? ")" : `, ${a2})`}`;
  }
}));
function clamph(value) {
  value = (value || 0) % 360;
  return value < 0 ? value + 360 : value;
}
function clampt(value) {
  return Math.max(0, Math.min(1, value || 0));
}
function hsl2rgb(h, m1, m2) {
  return (h < 60 ? m1 + (m2 - m1) * h / 60 : h < 180 ? m2 : h < 240 ? m1 + (m2 - m1) * (240 - h) / 60 : m1) * 255;
}

// node_modules/d3-color/src/math.js
var radians = Math.PI / 180;
var degrees = 180 / Math.PI;

// node_modules/d3-color/src/lab.js
var K = 18;
var Xn = 0.96422;
var Yn = 1;
var Zn = 0.82521;
var t02 = 4 / 29;
var t12 = 6 / 29;
var t2 = 3 * t12 * t12;
var t3 = t12 * t12 * t12;
function labConvert(o) {
  if (o instanceof Lab) return new Lab(o.l, o.a, o.b, o.opacity);
  if (o instanceof Hcl) return hcl2lab(o);
  if (!(o instanceof Rgb)) o = rgbConvert(o);
  var r = rgb2lrgb(o.r), g = rgb2lrgb(o.g), b = rgb2lrgb(o.b), y3 = xyz2lab((0.2225045 * r + 0.7168786 * g + 0.0606169 * b) / Yn), x3, z;
  if (r === g && g === b) x3 = z = y3;
  else {
    x3 = xyz2lab((0.4360747 * r + 0.3850649 * g + 0.1430804 * b) / Xn);
    z = xyz2lab((0.0139322 * r + 0.0971045 * g + 0.7141733 * b) / Zn);
  }
  return new Lab(116 * y3 - 16, 500 * (x3 - y3), 200 * (y3 - z), o.opacity);
}
function lab(l, a2, b, opacity) {
  return arguments.length === 1 ? labConvert(l) : new Lab(l, a2, b, opacity == null ? 1 : opacity);
}
function Lab(l, a2, b, opacity) {
  this.l = +l;
  this.a = +a2;
  this.b = +b;
  this.opacity = +opacity;
}
define_default(Lab, lab, extend(Color, {
  brighter(k2) {
    return new Lab(this.l + K * (k2 == null ? 1 : k2), this.a, this.b, this.opacity);
  },
  darker(k2) {
    return new Lab(this.l - K * (k2 == null ? 1 : k2), this.a, this.b, this.opacity);
  },
  rgb() {
    var y3 = (this.l + 16) / 116, x3 = isNaN(this.a) ? y3 : y3 + this.a / 500, z = isNaN(this.b) ? y3 : y3 - this.b / 200;
    x3 = Xn * lab2xyz(x3);
    y3 = Yn * lab2xyz(y3);
    z = Zn * lab2xyz(z);
    return new Rgb(
      lrgb2rgb(3.1338561 * x3 - 1.6168667 * y3 - 0.4906146 * z),
      lrgb2rgb(-0.9787684 * x3 + 1.9161415 * y3 + 0.033454 * z),
      lrgb2rgb(0.0719453 * x3 - 0.2289914 * y3 + 1.4052427 * z),
      this.opacity
    );
  }
}));
function xyz2lab(t) {
  return t > t3 ? Math.pow(t, 1 / 3) : t / t2 + t02;
}
function lab2xyz(t) {
  return t > t12 ? t * t * t : t2 * (t - t02);
}
function lrgb2rgb(x3) {
  return 255 * (x3 <= 31308e-7 ? 12.92 * x3 : 1.055 * Math.pow(x3, 1 / 2.4) - 0.055);
}
function rgb2lrgb(x3) {
  return (x3 /= 255) <= 0.04045 ? x3 / 12.92 : Math.pow((x3 + 0.055) / 1.055, 2.4);
}
function hclConvert(o) {
  if (o instanceof Hcl) return new Hcl(o.h, o.c, o.l, o.opacity);
  if (!(o instanceof Lab)) o = labConvert(o);
  if (o.a === 0 && o.b === 0) return new Hcl(NaN, 0 < o.l && o.l < 100 ? 0 : NaN, o.l, o.opacity);
  var h = Math.atan2(o.b, o.a) * degrees;
  return new Hcl(h < 0 ? h + 360 : h, Math.sqrt(o.a * o.a + o.b * o.b), o.l, o.opacity);
}
function hcl(h, c, l, opacity) {
  return arguments.length === 1 ? hclConvert(h) : new Hcl(h, c, l, opacity == null ? 1 : opacity);
}
function Hcl(h, c, l, opacity) {
  this.h = +h;
  this.c = +c;
  this.l = +l;
  this.opacity = +opacity;
}
function hcl2lab(o) {
  if (isNaN(o.h)) return new Lab(o.l, 0, 0, o.opacity);
  var h = o.h * radians;
  return new Lab(o.l, Math.cos(h) * o.c, Math.sin(h) * o.c, o.opacity);
}
define_default(Hcl, hcl, extend(Color, {
  brighter(k2) {
    return new Hcl(this.h, this.c, this.l + K * (k2 == null ? 1 : k2), this.opacity);
  },
  darker(k2) {
    return new Hcl(this.h, this.c, this.l - K * (k2 == null ? 1 : k2), this.opacity);
  },
  rgb() {
    return hcl2lab(this).rgb();
  }
}));

// node_modules/d3-color/src/cubehelix.js
var A = -0.14861;
var B = 1.78277;
var C = -0.29227;
var D = -0.90649;
var E = 1.97294;
var ED = E * D;
var EB = E * B;
var BC_DA = B * C - D * A;
function cubehelixConvert(o) {
  if (o instanceof Cubehelix) return new Cubehelix(o.h, o.s, o.l, o.opacity);
  if (!(o instanceof Rgb)) o = rgbConvert(o);
  var r = o.r / 255, g = o.g / 255, b = o.b / 255, l = (BC_DA * b + ED * r - EB * g) / (BC_DA + ED - EB), bl = b - l, k2 = (E * (g - l) - C * bl) / D, s2 = Math.sqrt(k2 * k2 + bl * bl) / (E * l * (1 - l)), h = s2 ? Math.atan2(k2, bl) * degrees - 120 : NaN;
  return new Cubehelix(h < 0 ? h + 360 : h, s2, l, o.opacity);
}
function cubehelix(h, s2, l, opacity) {
  return arguments.length === 1 ? cubehelixConvert(h) : new Cubehelix(h, s2, l, opacity == null ? 1 : opacity);
}
function Cubehelix(h, s2, l, opacity) {
  this.h = +h;
  this.s = +s2;
  this.l = +l;
  this.opacity = +opacity;
}
define_default(Cubehelix, cubehelix, extend(Color, {
  brighter(k2) {
    k2 = k2 == null ? brighter : Math.pow(brighter, k2);
    return new Cubehelix(this.h, this.s, this.l * k2, this.opacity);
  },
  darker(k2) {
    k2 = k2 == null ? darker : Math.pow(darker, k2);
    return new Cubehelix(this.h, this.s, this.l * k2, this.opacity);
  },
  rgb() {
    var h = isNaN(this.h) ? 0 : (this.h + 120) * radians, l = +this.l, a2 = isNaN(this.s) ? 0 : this.s * l * (1 - l), cosh2 = Math.cos(h), sinh2 = Math.sin(h);
    return new Rgb(
      255 * (l + a2 * (A * cosh2 + B * sinh2)),
      255 * (l + a2 * (C * cosh2 + D * sinh2)),
      255 * (l + a2 * (E * cosh2)),
      this.opacity
    );
  }
}));

// node_modules/d3-interpolate/src/basis.js
function basis(t13, v0, v1, v2, v3) {
  var t22 = t13 * t13, t32 = t22 * t13;
  return ((1 - 3 * t13 + 3 * t22 - t32) * v0 + (4 - 6 * t22 + 3 * t32) * v1 + (1 + 3 * t13 + 3 * t22 - 3 * t32) * v2 + t32 * v3) / 6;
}
function basis_default(values) {
  var n = values.length - 1;
  return function(t) {
    var i = t <= 0 ? t = 0 : t >= 1 ? (t = 1, n - 1) : Math.floor(t * n), v1 = values[i], v2 = values[i + 1], v0 = i > 0 ? values[i - 1] : 2 * v1 - v2, v3 = i < n - 1 ? values[i + 2] : 2 * v2 - v1;
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

// node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default(values) {
  var n = values.length;
  return function(t) {
    var i = Math.floor(((t %= 1) < 0 ? ++t : t) * n), v0 = values[(i + n - 1) % n], v1 = values[i % n], v2 = values[(i + 1) % n], v3 = values[(i + 2) % n];
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

// node_modules/d3-interpolate/src/constant.js
var constant_default3 = (x3) => () => x3;

// node_modules/d3-interpolate/src/color.js
function linear(a2, d) {
  return function(t) {
    return a2 + t * d;
  };
}
function exponential(a2, b, y3) {
  return a2 = Math.pow(a2, y3), b = Math.pow(b, y3) - a2, y3 = 1 / y3, function(t) {
    return Math.pow(a2 + t * b, y3);
  };
}
function hue(a2, b) {
  var d = b - a2;
  return d ? linear(a2, d > 180 || d < -180 ? d - 360 * Math.round(d / 360) : d) : constant_default3(isNaN(a2) ? b : a2);
}
function gamma(y3) {
  return (y3 = +y3) === 1 ? nogamma : function(a2, b) {
    return b - a2 ? exponential(a2, b, y3) : constant_default3(isNaN(a2) ? b : a2);
  };
}
function nogamma(a2, b) {
  var d = b - a2;
  return d ? linear(a2, d) : constant_default3(isNaN(a2) ? b : a2);
}

// node_modules/d3-interpolate/src/rgb.js
var rgb_default = function rgbGamma(y3) {
  var color3 = gamma(y3);
  function rgb2(start2, end) {
    var r = color3((start2 = rgb(start2)).r, (end = rgb(end)).r), g = color3(start2.g, end.g), b = color3(start2.b, end.b), opacity = nogamma(start2.opacity, end.opacity);
    return function(t) {
      start2.r = r(t);
      start2.g = g(t);
      start2.b = b(t);
      start2.opacity = opacity(t);
      return start2 + "";
    };
  }
  rgb2.gamma = rgbGamma;
  return rgb2;
}(1);
function rgbSpline(spline2) {
  return function(colors) {
    var n = colors.length, r = new Array(n), g = new Array(n), b = new Array(n), i, color3;
    for (i = 0; i < n; ++i) {
      color3 = rgb(colors[i]);
      r[i] = color3.r || 0;
      g[i] = color3.g || 0;
      b[i] = color3.b || 0;
    }
    r = spline2(r);
    g = spline2(g);
    b = spline2(b);
    color3.opacity = 1;
    return function(t) {
      color3.r = r(t);
      color3.g = g(t);
      color3.b = b(t);
      return color3 + "";
    };
  };
}
var rgbBasis = rgbSpline(basis_default);
var rgbBasisClosed = rgbSpline(basisClosed_default);

// node_modules/d3-interpolate/src/numberArray.js
function numberArray_default(a2, b) {
  if (!b) b = [];
  var n = a2 ? Math.min(b.length, a2.length) : 0, c = b.slice(), i;
  return function(t) {
    for (i = 0; i < n; ++i) c[i] = a2[i] * (1 - t) + b[i] * t;
    return c;
  };
}
function isNumberArray(x3) {
  return ArrayBuffer.isView(x3) && !(x3 instanceof DataView);
}

// node_modules/d3-interpolate/src/array.js
function genericArray(a2, b) {
  var nb = b ? b.length : 0, na = a2 ? Math.min(nb, a2.length) : 0, x3 = new Array(na), c = new Array(nb), i;
  for (i = 0; i < na; ++i) x3[i] = value_default(a2[i], b[i]);
  for (; i < nb; ++i) c[i] = b[i];
  return function(t) {
    for (i = 0; i < na; ++i) c[i] = x3[i](t);
    return c;
  };
}

// node_modules/d3-interpolate/src/date.js
function date_default(a2, b) {
  var d = /* @__PURE__ */ new Date();
  return a2 = +a2, b = +b, function(t) {
    return d.setTime(a2 * (1 - t) + b * t), d;
  };
}

// node_modules/d3-interpolate/src/number.js
function number_default(a2, b) {
  return a2 = +a2, b = +b, function(t) {
    return a2 * (1 - t) + b * t;
  };
}

// node_modules/d3-interpolate/src/object.js
function object_default(a2, b) {
  var i = {}, c = {}, k2;
  if (a2 === null || typeof a2 !== "object") a2 = {};
  if (b === null || typeof b !== "object") b = {};
  for (k2 in b) {
    if (k2 in a2) {
      i[k2] = value_default(a2[k2], b[k2]);
    } else {
      c[k2] = b[k2];
    }
  }
  return function(t) {
    for (k2 in i) c[k2] = i[k2](t);
    return c;
  };
}

// node_modules/d3-interpolate/src/string.js
var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB = new RegExp(reA.source, "g");
function zero2(b) {
  return function() {
    return b;
  };
}
function one(b) {
  return function(t) {
    return b(t) + "";
  };
}
function string_default(a2, b) {
  var bi = reA.lastIndex = reB.lastIndex = 0, am, bm, bs, i = -1, s2 = [], q = [];
  a2 = a2 + "", b = b + "";
  while ((am = reA.exec(a2)) && (bm = reB.exec(b))) {
    if ((bs = bm.index) > bi) {
      bs = b.slice(bi, bs);
      if (s2[i]) s2[i] += bs;
      else s2[++i] = bs;
    }
    if ((am = am[0]) === (bm = bm[0])) {
      if (s2[i]) s2[i] += bm;
      else s2[++i] = bm;
    } else {
      s2[++i] = null;
      q.push({ i, x: number_default(am, bm) });
    }
    bi = reB.lastIndex;
  }
  if (bi < b.length) {
    bs = b.slice(bi);
    if (s2[i]) s2[i] += bs;
    else s2[++i] = bs;
  }
  return s2.length < 2 ? q[0] ? one(q[0].x) : zero2(b) : (b = q.length, function(t) {
    for (var i2 = 0, o; i2 < b; ++i2) s2[(o = q[i2]).i] = o.x(t);
    return s2.join("");
  });
}

// node_modules/d3-interpolate/src/value.js
function value_default(a2, b) {
  var t = typeof b, c;
  return b == null || t === "boolean" ? constant_default3(b) : (t === "number" ? number_default : t === "string" ? (c = color(b)) ? (b = c, rgb_default) : string_default : b instanceof color ? rgb_default : b instanceof Date ? date_default : isNumberArray(b) ? numberArray_default : Array.isArray(b) ? genericArray : typeof b.valueOf !== "function" && typeof b.toString !== "function" || isNaN(b) ? object_default : number_default)(a2, b);
}

// node_modules/d3-interpolate/src/round.js
function round_default(a2, b) {
  return a2 = +a2, b = +b, function(t) {
    return Math.round(a2 * (1 - t) + b * t);
  };
}

// node_modules/d3-interpolate/src/transform/decompose.js
var degrees2 = 180 / Math.PI;
var identity2 = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default(a2, b, c, d, e, f) {
  var scaleX, scaleY, skewX;
  if (scaleX = Math.sqrt(a2 * a2 + b * b)) a2 /= scaleX, b /= scaleX;
  if (skewX = a2 * c + b * d) c -= a2 * skewX, d -= b * skewX;
  if (scaleY = Math.sqrt(c * c + d * d)) c /= scaleY, d /= scaleY, skewX /= scaleY;
  if (a2 * d < b * c) a2 = -a2, b = -b, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e,
    translateY: f,
    rotate: Math.atan2(b, a2) * degrees2,
    skewX: Math.atan(skewX) * degrees2,
    scaleX,
    scaleY
  };
}

// node_modules/d3-interpolate/src/transform/parse.js
var svgNode;
function parseCss(value) {
  const m = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value + "");
  return m.isIdentity ? identity2 : decompose_default(m.a, m.b, m.c, m.d, m.e, m.f);
}
function parseSvg(value) {
  if (value == null) return identity2;
  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode.setAttribute("transform", value);
  if (!(value = svgNode.transform.baseVal.consolidate())) return identity2;
  value = value.matrix;
  return decompose_default(value.a, value.b, value.c, value.d, value.e, value.f);
}

// node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform(parse, pxComma, pxParen, degParen) {
  function pop(s2) {
    return s2.length ? s2.pop() + " " : "";
  }
  function translate(xa, ya, xb, yb, s2, q) {
    if (xa !== xb || ya !== yb) {
      var i = s2.push("translate(", null, pxComma, null, pxParen);
      q.push({ i: i - 4, x: number_default(xa, xb) }, { i: i - 2, x: number_default(ya, yb) });
    } else if (xb || yb) {
      s2.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate(a2, b, s2, q) {
    if (a2 !== b) {
      if (a2 - b > 180) b += 360;
      else if (b - a2 > 180) a2 += 360;
      q.push({ i: s2.push(pop(s2) + "rotate(", null, degParen) - 2, x: number_default(a2, b) });
    } else if (b) {
      s2.push(pop(s2) + "rotate(" + b + degParen);
    }
  }
  function skewX(a2, b, s2, q) {
    if (a2 !== b) {
      q.push({ i: s2.push(pop(s2) + "skewX(", null, degParen) - 2, x: number_default(a2, b) });
    } else if (b) {
      s2.push(pop(s2) + "skewX(" + b + degParen);
    }
  }
  function scale2(xa, ya, xb, yb, s2, q) {
    if (xa !== xb || ya !== yb) {
      var i = s2.push(pop(s2) + "scale(", null, ",", null, ")");
      q.push({ i: i - 4, x: number_default(xa, xb) }, { i: i - 2, x: number_default(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s2.push(pop(s2) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a2, b) {
    var s2 = [], q = [];
    a2 = parse(a2), b = parse(b);
    translate(a2.translateX, a2.translateY, b.translateX, b.translateY, s2, q);
    rotate(a2.rotate, b.rotate, s2, q);
    skewX(a2.skewX, b.skewX, s2, q);
    scale2(a2.scaleX, a2.scaleY, b.scaleX, b.scaleY, s2, q);
    a2 = b = null;
    return function(t) {
      var i = -1, n = q.length, o;
      while (++i < n) s2[(o = q[i]).i] = o.x(t);
      return s2.join("");
    };
  };
}
var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

// node_modules/d3-interpolate/src/zoom.js
var epsilon2 = 1e-12;
function cosh(x3) {
  return ((x3 = Math.exp(x3)) + 1 / x3) / 2;
}
function sinh(x3) {
  return ((x3 = Math.exp(x3)) - 1 / x3) / 2;
}
function tanh(x3) {
  return ((x3 = Math.exp(2 * x3)) - 1) / (x3 + 1);
}
var zoom_default = function zoomRho(rho, rho2, rho4) {
  function zoom2(p0, p1) {
    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i, S;
    if (d2 < epsilon2) {
      S = Math.log(w1 / w0) / rho;
      i = function(t) {
        return [
          ux0 + t * dx,
          uy0 + t * dy,
          w0 * Math.exp(rho * t * S)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b02 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b12 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b02 * b02 + 1) - b02), r1 = Math.log(Math.sqrt(b12 * b12 + 1) - b12);
      S = (r1 - r0) / rho;
      i = function(t) {
        var s2 = t * S, coshr0 = cosh(r0), u = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s2 + r0) - sinh(r0));
        return [
          ux0 + u * dx,
          uy0 + u * dy,
          w0 * coshr0 / cosh(rho * s2 + r0)
        ];
      };
    }
    i.duration = S * 1e3 * rho / Math.SQRT2;
    return i;
  }
  zoom2.rho = function(_) {
    var _1 = Math.max(1e-3, +_), _2 = _1 * _1, _4 = _2 * _2;
    return zoomRho(_1, _2, _4);
  };
  return zoom2;
}(Math.SQRT2, 2, 4);

// node_modules/d3-interpolate/src/hsl.js
function hsl2(hue2) {
  return function(start2, end) {
    var h = hue2((start2 = hsl(start2)).h, (end = hsl(end)).h), s2 = nogamma(start2.s, end.s), l = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
    return function(t) {
      start2.h = h(t);
      start2.s = s2(t);
      start2.l = l(t);
      start2.opacity = opacity(t);
      return start2 + "";
    };
  };
}
var hsl_default = hsl2(hue);
var hslLong = hsl2(nogamma);

// node_modules/d3-interpolate/src/hcl.js
function hcl2(hue2) {
  return function(start2, end) {
    var h = hue2((start2 = hcl(start2)).h, (end = hcl(end)).h), c = nogamma(start2.c, end.c), l = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
    return function(t) {
      start2.h = h(t);
      start2.c = c(t);
      start2.l = l(t);
      start2.opacity = opacity(t);
      return start2 + "";
    };
  };
}
var hcl_default = hcl2(hue);
var hclLong = hcl2(nogamma);

// node_modules/d3-interpolate/src/cubehelix.js
function cubehelix2(hue2) {
  return function cubehelixGamma(y3) {
    y3 = +y3;
    function cubehelix3(start2, end) {
      var h = hue2((start2 = cubehelix(start2)).h, (end = cubehelix(end)).h), s2 = nogamma(start2.s, end.s), l = nogamma(start2.l, end.l), opacity = nogamma(start2.opacity, end.opacity);
      return function(t) {
        start2.h = h(t);
        start2.s = s2(t);
        start2.l = l(Math.pow(t, y3));
        start2.opacity = opacity(t);
        return start2 + "";
      };
    }
    cubehelix3.gamma = cubehelixGamma;
    return cubehelix3;
  }(1);
}
var cubehelix_default = cubehelix2(hue);
var cubehelixLong = cubehelix2(nogamma);

// node_modules/d3-timer/src/timer.js
var frame = 0;
var timeout = 0;
var interval = 0;
var pokeDelay = 1e3;
var taskHead;
var taskTail;
var clockLast = 0;
var clockNow = 0;
var clockSkew = 0;
var clock = typeof performance === "object" && performance.now ? performance : Date;
var setFrame = typeof window === "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(f) {
  setTimeout(f, 17);
};
function now() {
  return clockNow || (setFrame(clearNow), clockNow = clock.now() + clockSkew);
}
function clearNow() {
  clockNow = 0;
}
function Timer() {
  this._call = this._time = this._next = null;
}
Timer.prototype = timer.prototype = {
  constructor: Timer,
  restart: function(callback, delay, time2) {
    if (typeof callback !== "function") throw new TypeError("callback is not a function");
    time2 = (time2 == null ? now() : +time2) + (delay == null ? 0 : +delay);
    if (!this._next && taskTail !== this) {
      if (taskTail) taskTail._next = this;
      else taskHead = this;
      taskTail = this;
    }
    this._call = callback;
    this._time = time2;
    sleep();
  },
  stop: function() {
    if (this._call) {
      this._call = null;
      this._time = Infinity;
      sleep();
    }
  }
};
function timer(callback, delay, time2) {
  var t = new Timer();
  t.restart(callback, delay, time2);
  return t;
}
function timerFlush() {
  now();
  ++frame;
  var t = taskHead, e;
  while (t) {
    if ((e = clockNow - t._time) >= 0) t._call.call(void 0, e);
    t = t._next;
  }
  --frame;
}
function wake() {
  clockNow = (clockLast = clock.now()) + clockSkew;
  frame = timeout = 0;
  try {
    timerFlush();
  } finally {
    frame = 0;
    nap();
    clockNow = 0;
  }
}
function poke() {
  var now2 = clock.now(), delay = now2 - clockLast;
  if (delay > pokeDelay) clockSkew -= delay, clockLast = now2;
}
function nap() {
  var t03, t13 = taskHead, t22, time2 = Infinity;
  while (t13) {
    if (t13._call) {
      if (time2 > t13._time) time2 = t13._time;
      t03 = t13, t13 = t13._next;
    } else {
      t22 = t13._next, t13._next = null;
      t13 = t03 ? t03._next = t22 : taskHead = t22;
    }
  }
  taskTail = t03;
  sleep(time2);
}
function sleep(time2) {
  if (frame) return;
  if (timeout) timeout = clearTimeout(timeout);
  var delay = time2 - clockNow;
  if (delay > 24) {
    if (time2 < Infinity) timeout = setTimeout(wake, time2 - clock.now() - clockSkew);
    if (interval) interval = clearInterval(interval);
  } else {
    if (!interval) clockLast = clock.now(), interval = setInterval(poke, pokeDelay);
    frame = 1, setFrame(wake);
  }
}

// node_modules/d3-timer/src/timeout.js
function timeout_default(callback, delay, time2) {
  var t = new Timer();
  delay = delay == null ? 0 : +delay;
  t.restart((elapsed) => {
    t.stop();
    callback(elapsed + delay);
  }, delay, time2);
  return t;
}

// node_modules/d3-transition/src/transition/schedule.js
var emptyOn = dispatch_default2("start", "end", "cancel", "interrupt");
var emptyTween = [];
var CREATED = 0;
var SCHEDULED = 1;
var STARTING = 2;
var STARTED = 3;
var RUNNING = 4;
var ENDING = 5;
var ENDED = 6;
function schedule_default(node, name, id2, index2, group2, timing) {
  var schedules = node.__transition;
  if (!schedules) node.__transition = {};
  else if (id2 in schedules) return;
  create(node, id2, {
    name,
    index: index2,
    // For context during callback.
    group: group2,
    // For context during callback.
    on: emptyOn,
    tween: emptyTween,
    time: timing.time,
    delay: timing.delay,
    duration: timing.duration,
    ease: timing.ease,
    timer: null,
    state: CREATED
  });
}
function init(node, id2) {
  var schedule = get2(node, id2);
  if (schedule.state > CREATED) throw new Error("too late; already scheduled");
  return schedule;
}
function set2(node, id2) {
  var schedule = get2(node, id2);
  if (schedule.state > STARTED) throw new Error("too late; already running");
  return schedule;
}
function get2(node, id2) {
  var schedule = node.__transition;
  if (!schedule || !(schedule = schedule[id2])) throw new Error("transition not found");
  return schedule;
}
function create(node, id2, self2) {
  var schedules = node.__transition, tween;
  schedules[id2] = self2;
  self2.timer = timer(schedule, 0, self2.time);
  function schedule(elapsed) {
    self2.state = SCHEDULED;
    self2.timer.restart(start2, self2.delay, self2.time);
    if (self2.delay <= elapsed) start2(elapsed - self2.delay);
  }
  function start2(elapsed) {
    var i, j, n, o;
    if (self2.state !== SCHEDULED) return stop();
    for (i in schedules) {
      o = schedules[i];
      if (o.name !== self2.name) continue;
      if (o.state === STARTED) return timeout_default(start2);
      if (o.state === RUNNING) {
        o.state = ENDED;
        o.timer.stop();
        o.on.call("interrupt", node, node.__data__, o.index, o.group);
        delete schedules[i];
      } else if (+i < id2) {
        o.state = ENDED;
        o.timer.stop();
        o.on.call("cancel", node, node.__data__, o.index, o.group);
        delete schedules[i];
      }
    }
    timeout_default(function() {
      if (self2.state === STARTED) {
        self2.state = RUNNING;
        self2.timer.restart(tick, self2.delay, self2.time);
        tick(elapsed);
      }
    });
    self2.state = STARTING;
    self2.on.call("start", node, node.__data__, self2.index, self2.group);
    if (self2.state !== STARTING) return;
    self2.state = STARTED;
    tween = new Array(n = self2.tween.length);
    for (i = 0, j = -1; i < n; ++i) {
      if (o = self2.tween[i].value.call(node, node.__data__, self2.index, self2.group)) {
        tween[++j] = o;
      }
    }
    tween.length = j + 1;
  }
  function tick(elapsed) {
    var t = elapsed < self2.duration ? self2.ease.call(null, elapsed / self2.duration) : (self2.timer.restart(stop), self2.state = ENDING, 1), i = -1, n = tween.length;
    while (++i < n) {
      tween[i].call(node, t);
    }
    if (self2.state === ENDING) {
      self2.on.call("end", node, node.__data__, self2.index, self2.group);
      stop();
    }
  }
  function stop() {
    self2.state = ENDED;
    self2.timer.stop();
    delete schedules[id2];
    for (var i in schedules) return;
    delete node.__transition;
  }
}

// node_modules/d3-transition/src/interrupt.js
function interrupt_default(node, name) {
  var schedules = node.__transition, schedule, active, empty3 = true, i;
  if (!schedules) return;
  name = name == null ? null : name + "";
  for (i in schedules) {
    if ((schedule = schedules[i]).name !== name) {
      empty3 = false;
      continue;
    }
    active = schedule.state > STARTING && schedule.state < ENDING;
    schedule.state = ENDED;
    schedule.timer.stop();
    schedule.on.call(active ? "interrupt" : "cancel", node, node.__data__, schedule.index, schedule.group);
    delete schedules[i];
  }
  if (empty3) delete node.__transition;
}

// node_modules/d3-transition/src/selection/interrupt.js
function interrupt_default2(name) {
  return this.each(function() {
    interrupt_default(this, name);
  });
}

// node_modules/d3-transition/src/transition/tween.js
function tweenRemove(id2, name) {
  var tween0, tween1;
  return function() {
    var schedule = set2(this, id2), tween = schedule.tween;
    if (tween !== tween0) {
      tween1 = tween0 = tween;
      for (var i = 0, n = tween1.length; i < n; ++i) {
        if (tween1[i].name === name) {
          tween1 = tween1.slice();
          tween1.splice(i, 1);
          break;
        }
      }
    }
    schedule.tween = tween1;
  };
}
function tweenFunction(id2, name, value) {
  var tween0, tween1;
  if (typeof value !== "function") throw new Error();
  return function() {
    var schedule = set2(this, id2), tween = schedule.tween;
    if (tween !== tween0) {
      tween1 = (tween0 = tween).slice();
      for (var t = { name, value }, i = 0, n = tween1.length; i < n; ++i) {
        if (tween1[i].name === name) {
          tween1[i] = t;
          break;
        }
      }
      if (i === n) tween1.push(t);
    }
    schedule.tween = tween1;
  };
}
function tween_default(name, value) {
  var id2 = this._id;
  name += "";
  if (arguments.length < 2) {
    var tween = get2(this.node(), id2).tween;
    for (var i = 0, n = tween.length, t; i < n; ++i) {
      if ((t = tween[i]).name === name) {
        return t.value;
      }
    }
    return null;
  }
  return this.each((value == null ? tweenRemove : tweenFunction)(id2, name, value));
}
function tweenValue(transition2, name, value) {
  var id2 = transition2._id;
  transition2.each(function() {
    var schedule = set2(this, id2);
    (schedule.value || (schedule.value = {}))[name] = value.apply(this, arguments);
  });
  return function(node) {
    return get2(node, id2).value[name];
  };
}

// node_modules/d3-transition/src/transition/interpolate.js
function interpolate_default(a2, b) {
  var c;
  return (typeof b === "number" ? number_default : b instanceof color ? rgb_default : (c = color(b)) ? (b = c, rgb_default) : string_default)(a2, b);
}

// node_modules/d3-transition/src/transition/attr.js
function attrRemove2(name) {
  return function() {
    this.removeAttribute(name);
  };
}
function attrRemoveNS2(fullname) {
  return function() {
    this.removeAttributeNS(fullname.space, fullname.local);
  };
}
function attrConstant2(name, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = this.getAttribute(name);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function attrConstantNS2(fullname, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = this.getAttributeNS(fullname.space, fullname.local);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function attrFunction2(name, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0, value1 = value(this), string1;
    if (value1 == null) return void this.removeAttribute(name);
    string0 = this.getAttribute(name);
    string1 = value1 + "";
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function attrFunctionNS2(fullname, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0, value1 = value(this), string1;
    if (value1 == null) return void this.removeAttributeNS(fullname.space, fullname.local);
    string0 = this.getAttributeNS(fullname.space, fullname.local);
    string1 = value1 + "";
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function attr_default2(name, value) {
  var fullname = namespace_default(name), i = fullname === "transform" ? interpolateTransformSvg : interpolate_default;
  return this.attrTween(name, typeof value === "function" ? (fullname.local ? attrFunctionNS2 : attrFunction2)(fullname, i, tweenValue(this, "attr." + name, value)) : value == null ? (fullname.local ? attrRemoveNS2 : attrRemove2)(fullname) : (fullname.local ? attrConstantNS2 : attrConstant2)(fullname, i, value));
}

// node_modules/d3-transition/src/transition/attrTween.js
function attrInterpolate(name, i) {
  return function(t) {
    this.setAttribute(name, i.call(this, t));
  };
}
function attrInterpolateNS(fullname, i) {
  return function(t) {
    this.setAttributeNS(fullname.space, fullname.local, i.call(this, t));
  };
}
function attrTweenNS(fullname, value) {
  var t03, i0;
  function tween() {
    var i = value.apply(this, arguments);
    if (i !== i0) t03 = (i0 = i) && attrInterpolateNS(fullname, i);
    return t03;
  }
  tween._value = value;
  return tween;
}
function attrTween(name, value) {
  var t03, i0;
  function tween() {
    var i = value.apply(this, arguments);
    if (i !== i0) t03 = (i0 = i) && attrInterpolate(name, i);
    return t03;
  }
  tween._value = value;
  return tween;
}
function attrTween_default(name, value) {
  var key = "attr." + name;
  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  var fullname = namespace_default(name);
  return this.tween(key, (fullname.local ? attrTweenNS : attrTween)(fullname, value));
}

// node_modules/d3-transition/src/transition/delay.js
function delayFunction(id2, value) {
  return function() {
    init(this, id2).delay = +value.apply(this, arguments);
  };
}
function delayConstant(id2, value) {
  return value = +value, function() {
    init(this, id2).delay = value;
  };
}
function delay_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each((typeof value === "function" ? delayFunction : delayConstant)(id2, value)) : get2(this.node(), id2).delay;
}

// node_modules/d3-transition/src/transition/duration.js
function durationFunction(id2, value) {
  return function() {
    set2(this, id2).duration = +value.apply(this, arguments);
  };
}
function durationConstant(id2, value) {
  return value = +value, function() {
    set2(this, id2).duration = value;
  };
}
function duration_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each((typeof value === "function" ? durationFunction : durationConstant)(id2, value)) : get2(this.node(), id2).duration;
}

// node_modules/d3-transition/src/transition/ease.js
function easeConstant(id2, value) {
  if (typeof value !== "function") throw new Error();
  return function() {
    set2(this, id2).ease = value;
  };
}
function ease_default(value) {
  var id2 = this._id;
  return arguments.length ? this.each(easeConstant(id2, value)) : get2(this.node(), id2).ease;
}

// node_modules/d3-transition/src/transition/easeVarying.js
function easeVarying(id2, value) {
  return function() {
    var v = value.apply(this, arguments);
    if (typeof v !== "function") throw new Error();
    set2(this, id2).ease = v;
  };
}
function easeVarying_default(value) {
  if (typeof value !== "function") throw new Error();
  return this.each(easeVarying(this._id, value));
}

// node_modules/d3-transition/src/transition/filter.js
function filter_default2(match) {
  if (typeof match !== "function") match = matcher_default(match);
  for (var groups2 = this._groups, m = groups2.length, subgroups = new Array(m), j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, subgroup = subgroups[j] = [], node, i = 0; i < n; ++i) {
      if ((node = group2[i]) && match.call(node, node.__data__, i, group2)) {
        subgroup.push(node);
      }
    }
  }
  return new Transition(subgroups, this._parents, this._name, this._id);
}

// node_modules/d3-transition/src/transition/merge.js
function merge_default2(transition2) {
  if (transition2._id !== this._id) throw new Error();
  for (var groups0 = this._groups, groups1 = transition2._groups, m0 = groups0.length, m1 = groups1.length, m = Math.min(m0, m1), merges = new Array(m0), j = 0; j < m; ++j) {
    for (var group0 = groups0[j], group1 = groups1[j], n = group0.length, merge2 = merges[j] = new Array(n), node, i = 0; i < n; ++i) {
      if (node = group0[i] || group1[i]) {
        merge2[i] = node;
      }
    }
  }
  for (; j < m0; ++j) {
    merges[j] = groups0[j];
  }
  return new Transition(merges, this._parents, this._name, this._id);
}

// node_modules/d3-transition/src/transition/on.js
function start(name) {
  return (name + "").trim().split(/^|\s+/).every(function(t) {
    var i = t.indexOf(".");
    if (i >= 0) t = t.slice(0, i);
    return !t || t === "start";
  });
}
function onFunction(id2, name, listener) {
  var on0, on1, sit = start(name) ? init : set2;
  return function() {
    var schedule = sit(this, id2), on = schedule.on;
    if (on !== on0) (on1 = (on0 = on).copy()).on(name, listener);
    schedule.on = on1;
  };
}
function on_default2(name, listener) {
  var id2 = this._id;
  return arguments.length < 2 ? get2(this.node(), id2).on.on(name) : this.each(onFunction(id2, name, listener));
}

// node_modules/d3-transition/src/transition/remove.js
function removeFunction(id2) {
  return function() {
    var parent = this.parentNode;
    for (var i in this.__transition) if (+i !== id2) return;
    if (parent) parent.removeChild(this);
  };
}
function remove_default2() {
  return this.on("end.remove", removeFunction(this._id));
}

// node_modules/d3-transition/src/transition/select.js
function select_default3(select) {
  var name = this._name, id2 = this._id;
  if (typeof select !== "function") select = selector_default(select);
  for (var groups2 = this._groups, m = groups2.length, subgroups = new Array(m), j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, subgroup = subgroups[j] = new Array(n), node, subnode, i = 0; i < n; ++i) {
      if ((node = group2[i]) && (subnode = select.call(node, node.__data__, i, group2))) {
        if ("__data__" in node) subnode.__data__ = node.__data__;
        subgroup[i] = subnode;
        schedule_default(subgroup[i], name, id2, i, subgroup, get2(node, id2));
      }
    }
  }
  return new Transition(subgroups, this._parents, name, id2);
}

// node_modules/d3-transition/src/transition/selectAll.js
function selectAll_default3(select) {
  var name = this._name, id2 = this._id;
  if (typeof select !== "function") select = selectorAll_default(select);
  for (var groups2 = this._groups, m = groups2.length, subgroups = [], parents = [], j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, node, i = 0; i < n; ++i) {
      if (node = group2[i]) {
        for (var children2 = select.call(node, node.__data__, i, group2), child, inherit2 = get2(node, id2), k2 = 0, l = children2.length; k2 < l; ++k2) {
          if (child = children2[k2]) {
            schedule_default(child, name, id2, k2, children2, inherit2);
          }
        }
        subgroups.push(children2);
        parents.push(node);
      }
    }
  }
  return new Transition(subgroups, parents, name, id2);
}

// node_modules/d3-transition/src/transition/selection.js
var Selection2 = selection_default.prototype.constructor;
function selection_default2() {
  return new Selection2(this._groups, this._parents);
}

// node_modules/d3-transition/src/transition/style.js
function styleNull(name, interpolate) {
  var string00, string10, interpolate0;
  return function() {
    var string0 = styleValue(this, name), string1 = (this.style.removeProperty(name), styleValue(this, name));
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : interpolate0 = interpolate(string00 = string0, string10 = string1);
  };
}
function styleRemove2(name) {
  return function() {
    this.style.removeProperty(name);
  };
}
function styleConstant2(name, interpolate, value1) {
  var string00, string1 = value1 + "", interpolate0;
  return function() {
    var string0 = styleValue(this, name);
    return string0 === string1 ? null : string0 === string00 ? interpolate0 : interpolate0 = interpolate(string00 = string0, value1);
  };
}
function styleFunction2(name, interpolate, value) {
  var string00, string10, interpolate0;
  return function() {
    var string0 = styleValue(this, name), value1 = value(this), string1 = value1 + "";
    if (value1 == null) string1 = value1 = (this.style.removeProperty(name), styleValue(this, name));
    return string0 === string1 ? null : string0 === string00 && string1 === string10 ? interpolate0 : (string10 = string1, interpolate0 = interpolate(string00 = string0, value1));
  };
}
function styleMaybeRemove(id2, name) {
  var on0, on1, listener0, key = "style." + name, event = "end." + key, remove3;
  return function() {
    var schedule = set2(this, id2), on = schedule.on, listener = schedule.value[key] == null ? remove3 || (remove3 = styleRemove2(name)) : void 0;
    if (on !== on0 || listener0 !== listener) (on1 = (on0 = on).copy()).on(event, listener0 = listener);
    schedule.on = on1;
  };
}
function style_default2(name, value, priority) {
  var i = (name += "") === "transform" ? interpolateTransformCss : interpolate_default;
  return value == null ? this.styleTween(name, styleNull(name, i)).on("end.style." + name, styleRemove2(name)) : typeof value === "function" ? this.styleTween(name, styleFunction2(name, i, tweenValue(this, "style." + name, value))).each(styleMaybeRemove(this._id, name)) : this.styleTween(name, styleConstant2(name, i, value), priority).on("end.style." + name, null);
}

// node_modules/d3-transition/src/transition/styleTween.js
function styleInterpolate(name, i, priority) {
  return function(t) {
    this.style.setProperty(name, i.call(this, t), priority);
  };
}
function styleTween(name, value, priority) {
  var t, i0;
  function tween() {
    var i = value.apply(this, arguments);
    if (i !== i0) t = (i0 = i) && styleInterpolate(name, i, priority);
    return t;
  }
  tween._value = value;
  return tween;
}
function styleTween_default(name, value, priority) {
  var key = "style." + (name += "");
  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  return this.tween(key, styleTween(name, value, priority == null ? "" : priority));
}

// node_modules/d3-transition/src/transition/text.js
function textConstant2(value) {
  return function() {
    this.textContent = value;
  };
}
function textFunction2(value) {
  return function() {
    var value1 = value(this);
    this.textContent = value1 == null ? "" : value1;
  };
}
function text_default2(value) {
  return this.tween("text", typeof value === "function" ? textFunction2(tweenValue(this, "text", value)) : textConstant2(value == null ? "" : value + ""));
}

// node_modules/d3-transition/src/transition/textTween.js
function textInterpolate(i) {
  return function(t) {
    this.textContent = i.call(this, t);
  };
}
function textTween(value) {
  var t03, i0;
  function tween() {
    var i = value.apply(this, arguments);
    if (i !== i0) t03 = (i0 = i) && textInterpolate(i);
    return t03;
  }
  tween._value = value;
  return tween;
}
function textTween_default(value) {
  var key = "text";
  if (arguments.length < 1) return (key = this.tween(key)) && key._value;
  if (value == null) return this.tween(key, null);
  if (typeof value !== "function") throw new Error();
  return this.tween(key, textTween(value));
}

// node_modules/d3-transition/src/transition/transition.js
function transition_default() {
  var name = this._name, id0 = this._id, id1 = newId();
  for (var groups2 = this._groups, m = groups2.length, j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, node, i = 0; i < n; ++i) {
      if (node = group2[i]) {
        var inherit2 = get2(node, id0);
        schedule_default(node, name, id1, i, group2, {
          time: inherit2.time + inherit2.delay + inherit2.duration,
          delay: 0,
          duration: inherit2.duration,
          ease: inherit2.ease
        });
      }
    }
  }
  return new Transition(groups2, this._parents, name, id1);
}

// node_modules/d3-transition/src/transition/end.js
function end_default() {
  var on0, on1, that = this, id2 = that._id, size2 = that.size();
  return new Promise(function(resolve, reject) {
    var cancel = { value: reject }, end = { value: function() {
      if (--size2 === 0) resolve();
    } };
    that.each(function() {
      var schedule = set2(this, id2), on = schedule.on;
      if (on !== on0) {
        on1 = (on0 = on).copy();
        on1._.cancel.push(cancel);
        on1._.interrupt.push(cancel);
        on1._.end.push(end);
      }
      schedule.on = on1;
    });
    if (size2 === 0) resolve();
  });
}

// node_modules/d3-transition/src/transition/index.js
var id = 0;
function Transition(groups2, parents, name, id2) {
  this._groups = groups2;
  this._parents = parents;
  this._name = name;
  this._id = id2;
}
function transition(name) {
  return selection_default().transition(name);
}
function newId() {
  return ++id;
}
var selection_prototype = selection_default.prototype;
Transition.prototype = transition.prototype = {
  constructor: Transition,
  select: select_default3,
  selectAll: selectAll_default3,
  selectChild: selection_prototype.selectChild,
  selectChildren: selection_prototype.selectChildren,
  filter: filter_default2,
  merge: merge_default2,
  selection: selection_default2,
  transition: transition_default,
  call: selection_prototype.call,
  nodes: selection_prototype.nodes,
  node: selection_prototype.node,
  size: selection_prototype.size,
  empty: selection_prototype.empty,
  each: selection_prototype.each,
  on: on_default2,
  attr: attr_default2,
  attrTween: attrTween_default,
  style: style_default2,
  styleTween: styleTween_default,
  text: text_default2,
  textTween: textTween_default,
  remove: remove_default2,
  tween: tween_default,
  delay: delay_default,
  duration: duration_default,
  ease: ease_default,
  easeVarying: easeVarying_default,
  end: end_default,
  [Symbol.iterator]: selection_prototype[Symbol.iterator]
};

// node_modules/d3-ease/src/linear.js
var linear2 = (t) => +t;

// node_modules/d3-ease/src/cubic.js
function cubicInOut(t) {
  return ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
}

// node_modules/d3-ease/src/poly.js
var exponent = 3;
var polyIn = function custom(e) {
  e = +e;
  function polyIn2(t) {
    return Math.pow(t, e);
  }
  polyIn2.exponent = custom;
  return polyIn2;
}(exponent);
var polyOut = function custom2(e) {
  e = +e;
  function polyOut2(t) {
    return 1 - Math.pow(1 - t, e);
  }
  polyOut2.exponent = custom2;
  return polyOut2;
}(exponent);
var polyInOut = function custom3(e) {
  e = +e;
  function polyInOut2(t) {
    return ((t *= 2) <= 1 ? Math.pow(t, e) : 2 - Math.pow(2 - t, e)) / 2;
  }
  polyInOut2.exponent = custom3;
  return polyInOut2;
}(exponent);

// node_modules/d3-ease/src/sin.js
var pi = Math.PI;
var halfPi = pi / 2;

// node_modules/d3-ease/src/math.js
function tpmt(x3) {
  return (Math.pow(2, -10 * x3) - 9765625e-10) * 1.0009775171065494;
}

// node_modules/d3-ease/src/bounce.js
var b1 = 4 / 11;
var b2 = 6 / 11;
var b3 = 8 / 11;
var b4 = 3 / 4;
var b5 = 9 / 11;
var b6 = 10 / 11;
var b7 = 15 / 16;
var b8 = 21 / 22;
var b9 = 63 / 64;
var b0 = 1 / b1 / b1;

// node_modules/d3-ease/src/back.js
var overshoot = 1.70158;
var backIn = function custom4(s2) {
  s2 = +s2;
  function backIn2(t) {
    return (t = +t) * t * (s2 * (t - 1) + t);
  }
  backIn2.overshoot = custom4;
  return backIn2;
}(overshoot);
var backOut = function custom5(s2) {
  s2 = +s2;
  function backOut2(t) {
    return --t * t * ((t + 1) * s2 + t) + 1;
  }
  backOut2.overshoot = custom5;
  return backOut2;
}(overshoot);
var backInOut = function custom6(s2) {
  s2 = +s2;
  function backInOut2(t) {
    return ((t *= 2) < 1 ? t * t * ((s2 + 1) * t - s2) : (t -= 2) * t * ((s2 + 1) * t + s2) + 2) / 2;
  }
  backInOut2.overshoot = custom6;
  return backInOut2;
}(overshoot);

// node_modules/d3-ease/src/elastic.js
var tau = 2 * Math.PI;
var amplitude = 1;
var period = 0.3;
var elasticIn = function custom7(a2, p) {
  var s2 = Math.asin(1 / (a2 = Math.max(1, a2))) * (p /= tau);
  function elasticIn2(t) {
    return a2 * tpmt(- --t) * Math.sin((s2 - t) / p);
  }
  elasticIn2.amplitude = function(a3) {
    return custom7(a3, p * tau);
  };
  elasticIn2.period = function(p2) {
    return custom7(a2, p2);
  };
  return elasticIn2;
}(amplitude, period);
var elasticOut = function custom8(a2, p) {
  var s2 = Math.asin(1 / (a2 = Math.max(1, a2))) * (p /= tau);
  function elasticOut2(t) {
    return 1 - a2 * tpmt(t = +t) * Math.sin((t + s2) / p);
  }
  elasticOut2.amplitude = function(a3) {
    return custom8(a3, p * tau);
  };
  elasticOut2.period = function(p2) {
    return custom8(a2, p2);
  };
  return elasticOut2;
}(amplitude, period);
var elasticInOut = function custom9(a2, p) {
  var s2 = Math.asin(1 / (a2 = Math.max(1, a2))) * (p /= tau);
  function elasticInOut2(t) {
    return ((t = t * 2 - 1) < 0 ? a2 * tpmt(-t) * Math.sin((s2 - t) / p) : 2 - a2 * tpmt(t) * Math.sin((s2 + t) / p)) / 2;
  }
  elasticInOut2.amplitude = function(a3) {
    return custom9(a3, p * tau);
  };
  elasticInOut2.period = function(p2) {
    return custom9(a2, p2);
  };
  return elasticInOut2;
}(amplitude, period);

// node_modules/d3-transition/src/selection/transition.js
var defaultTiming = {
  time: null,
  // Set on use.
  delay: 0,
  duration: 250,
  ease: cubicInOut
};
function inherit(node, id2) {
  var timing;
  while (!(timing = node.__transition) || !(timing = timing[id2])) {
    if (!(node = node.parentNode)) {
      throw new Error(`transition ${id2} not found`);
    }
  }
  return timing;
}
function transition_default2(name) {
  var id2, timing;
  if (name instanceof Transition) {
    id2 = name._id, name = name._name;
  } else {
    id2 = newId(), (timing = defaultTiming).time = now(), name = name == null ? null : name + "";
  }
  for (var groups2 = this._groups, m = groups2.length, j = 0; j < m; ++j) {
    for (var group2 = groups2[j], n = group2.length, node, i = 0; i < n; ++i) {
      if (node = group2[i]) {
        schedule_default(node, name, id2, i, group2, timing || inherit(node, id2));
      }
    }
  }
  return new Transition(groups2, this._parents, name, id2);
}

// node_modules/d3-transition/src/selection/index.js
selection_default.prototype.interrupt = interrupt_default2;
selection_default.prototype.transition = transition_default2;

// node_modules/d3-brush/src/constant.js
var constant_default4 = (x3) => () => x3;

// node_modules/d3-brush/src/event.js
function BrushEvent(type2, {
  sourceEvent,
  target,
  selection: selection3,
  mode: mode2,
  dispatch: dispatch2
}) {
  Object.defineProperties(this, {
    type: { value: type2, enumerable: true, configurable: true },
    sourceEvent: { value: sourceEvent, enumerable: true, configurable: true },
    target: { value: target, enumerable: true, configurable: true },
    selection: { value: selection3, enumerable: true, configurable: true },
    mode: { value: mode2, enumerable: true, configurable: true },
    _: { value: dispatch2 }
  });
}

// node_modules/d3-brush/src/noevent.js
function nopropagation2(event) {
  event.stopImmediatePropagation();
}
function noevent_default2(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}

// node_modules/d3-brush/src/brush.js
var MODE_DRAG = { name: "drag" };
var MODE_SPACE = { name: "space" };
var MODE_HANDLE = { name: "handle" };
var MODE_CENTER = { name: "center" };
var { abs, max: max2, min: min2 } = Math;
function number1(e) {
  return [+e[0], +e[1]];
}
function number2(e) {
  return [number1(e[0]), number1(e[1])];
}
var X = {
  name: "x",
  handles: ["w", "e"].map(type),
  input: function(x3, e) {
    return x3 == null ? null : [[+x3[0], e[0][1]], [+x3[1], e[1][1]]];
  },
  output: function(xy) {
    return xy && [xy[0][0], xy[1][0]];
  }
};
var Y = {
  name: "y",
  handles: ["n", "s"].map(type),
  input: function(y3, e) {
    return y3 == null ? null : [[e[0][0], +y3[0]], [e[1][0], +y3[1]]];
  },
  output: function(xy) {
    return xy && [xy[0][1], xy[1][1]];
  }
};
var XY = {
  name: "xy",
  handles: ["n", "w", "e", "s", "nw", "ne", "sw", "se"].map(type),
  input: function(xy) {
    return xy == null ? null : number2(xy);
  },
  output: function(xy) {
    return xy;
  }
};
var cursors = {
  overlay: "crosshair",
  selection: "move",
  n: "ns-resize",
  e: "ew-resize",
  s: "ns-resize",
  w: "ew-resize",
  nw: "nwse-resize",
  ne: "nesw-resize",
  se: "nwse-resize",
  sw: "nesw-resize"
};
var flipX = {
  e: "w",
  w: "e",
  nw: "ne",
  ne: "nw",
  se: "sw",
  sw: "se"
};
var flipY = {
  n: "s",
  s: "n",
  nw: "sw",
  ne: "se",
  se: "ne",
  sw: "nw"
};
var signsX = {
  overlay: 1,
  selection: 1,
  n: null,
  e: 1,
  s: null,
  w: -1,
  nw: -1,
  ne: 1,
  se: 1,
  sw: -1
};
var signsY = {
  overlay: 1,
  selection: 1,
  n: -1,
  e: null,
  s: 1,
  w: null,
  nw: -1,
  ne: -1,
  se: 1,
  sw: 1
};
function type(t) {
  return { type: t };
}
function defaultFilter2(event) {
  return !event.ctrlKey && !event.button;
}
function defaultExtent() {
  var svg = this.ownerSVGElement || this;
  if (svg.hasAttribute("viewBox")) {
    svg = svg.viewBox.baseVal;
    return [[svg.x, svg.y], [svg.x + svg.width, svg.y + svg.height]];
  }
  return [[0, 0], [svg.width.baseVal.value, svg.height.baseVal.value]];
}
function defaultTouchable2() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function local2(node) {
  while (!node.__brush) if (!(node = node.parentNode)) return;
  return node.__brush;
}
function empty2(extent2) {
  return extent2[0][0] === extent2[1][0] || extent2[0][1] === extent2[1][1];
}
function brushSelection(node) {
  var state = node.__brush;
  return state ? state.dim.output(state.selection) : null;
}
function brushX() {
  return brush(X);
}
function brushY() {
  return brush(Y);
}
function brush(dim) {
  var extent2 = defaultExtent, filter3 = defaultFilter2, touchable = defaultTouchable2, keys = true, listeners = dispatch_default2("start", "brush", "end"), handleSize = 6, touchending;
  function brush2(group2) {
    var overlay = group2.property("__brush", initialize).selectAll(".overlay").data([type("overlay")]);
    overlay.enter().append("rect").attr("class", "overlay").attr("pointer-events", "all").attr("cursor", cursors.overlay).merge(overlay).each(function() {
      var extent3 = local2(this).extent;
      select_default2(this).attr("x", extent3[0][0]).attr("y", extent3[0][1]).attr("width", extent3[1][0] - extent3[0][0]).attr("height", extent3[1][1] - extent3[0][1]);
    });
    group2.selectAll(".selection").data([type("selection")]).enter().append("rect").attr("class", "selection").attr("cursor", cursors.selection).attr("fill", "#777").attr("fill-opacity", 0.3).attr("stroke", "#fff").attr("shape-rendering", "crispEdges");
    var handle = group2.selectAll(".handle").data(dim.handles, function(d) {
      return d.type;
    });
    handle.exit().remove();
    handle.enter().append("rect").attr("class", function(d) {
      return "handle handle--" + d.type;
    }).attr("cursor", function(d) {
      return cursors[d.type];
    });
    group2.each(redraw2).attr("fill", "none").attr("pointer-events", "all").on("mousedown.brush", started).filter(touchable).on("touchstart.brush", started).on("touchmove.brush", touchmoved).on("touchend.brush touchcancel.brush", touchended).style("touch-action", "none").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  brush2.move = function(group2, selection3, event) {
    if (group2.tween) {
      group2.on("start.brush", function(event2) {
        emitter(this, arguments).beforestart().start(event2);
      }).on("interrupt.brush end.brush", function(event2) {
        emitter(this, arguments).end(event2);
      }).tween("brush", function() {
        var that = this, state = that.__brush, emit = emitter(that, arguments), selection0 = state.selection, selection1 = dim.input(typeof selection3 === "function" ? selection3.apply(this, arguments) : selection3, state.extent), i = value_default(selection0, selection1);
        function tween(t) {
          state.selection = t === 1 && selection1 === null ? null : i(t);
          redraw2.call(that);
          emit.brush();
        }
        return selection0 !== null && selection1 !== null ? tween : tween(1);
      });
    } else {
      group2.each(function() {
        var that = this, args = arguments, state = that.__brush, selection1 = dim.input(typeof selection3 === "function" ? selection3.apply(that, args) : selection3, state.extent), emit = emitter(that, args).beforestart();
        interrupt_default(that);
        state.selection = selection1 === null ? null : selection1;
        redraw2.call(that);
        emit.start(event).brush(event).end(event);
      });
    }
  };
  brush2.clear = function(group2, event) {
    brush2.move(group2, null, event);
  };
  function redraw2() {
    var group2 = select_default2(this), selection3 = local2(this).selection;
    if (selection3) {
      group2.selectAll(".selection").style("display", null).attr("x", selection3[0][0]).attr("y", selection3[0][1]).attr("width", selection3[1][0] - selection3[0][0]).attr("height", selection3[1][1] - selection3[0][1]);
      group2.selectAll(".handle").style("display", null).attr("x", function(d) {
        return d.type[d.type.length - 1] === "e" ? selection3[1][0] - handleSize / 2 : selection3[0][0] - handleSize / 2;
      }).attr("y", function(d) {
        return d.type[0] === "s" ? selection3[1][1] - handleSize / 2 : selection3[0][1] - handleSize / 2;
      }).attr("width", function(d) {
        return d.type === "n" || d.type === "s" ? selection3[1][0] - selection3[0][0] + handleSize : handleSize;
      }).attr("height", function(d) {
        return d.type === "e" || d.type === "w" ? selection3[1][1] - selection3[0][1] + handleSize : handleSize;
      });
    } else {
      group2.selectAll(".selection,.handle").style("display", "none").attr("x", null).attr("y", null).attr("width", null).attr("height", null);
    }
  }
  function emitter(that, args, clean) {
    var emit = that.__brush.emitter;
    return emit && (!clean || !emit.clean) ? emit : new Emitter(that, args, clean);
  }
  function Emitter(that, args, clean) {
    this.that = that;
    this.args = args;
    this.state = that.__brush;
    this.active = 0;
    this.clean = clean;
  }
  Emitter.prototype = {
    beforestart: function() {
      if (++this.active === 1) this.state.emitter = this, this.starting = true;
      return this;
    },
    start: function(event, mode2) {
      if (this.starting) this.starting = false, this.emit("start", event, mode2);
      else this.emit("brush", event);
      return this;
    },
    brush: function(event, mode2) {
      this.emit("brush", event, mode2);
      return this;
    },
    end: function(event, mode2) {
      if (--this.active === 0) delete this.state.emitter, this.emit("end", event, mode2);
      return this;
    },
    emit: function(type2, event, mode2) {
      var d = select_default2(this.that).datum();
      listeners.call(
        type2,
        this.that,
        new BrushEvent(type2, {
          sourceEvent: event,
          target: brush2,
          selection: dim.output(this.state.selection),
          mode: mode2,
          dispatch: listeners
        }),
        d
      );
    }
  };
  function started(event) {
    if (touchending && !event.touches) return;
    if (!filter3.apply(this, arguments)) return;
    var that = this, type2 = event.target.__data__.type, mode2 = (keys && event.metaKey ? type2 = "overlay" : type2) === "selection" ? MODE_DRAG : keys && event.altKey ? MODE_CENTER : MODE_HANDLE, signX = dim === Y ? null : signsX[type2], signY = dim === X ? null : signsY[type2], state = local2(that), extent3 = state.extent, selection3 = state.selection, W = extent3[0][0], w0, w1, N = extent3[0][1], n0, n1, E2 = extent3[1][0], e0, e1, S = extent3[1][1], s0, s1, dx = 0, dy = 0, moving, shifting = signX && signY && keys && event.shiftKey, lockX, lockY, points = Array.from(event.touches || [event], (t) => {
      const i = t.identifier;
      t = pointer_default(t, that);
      t.point0 = t.slice();
      t.identifier = i;
      return t;
    });
    interrupt_default(that);
    var emit = emitter(that, arguments, true).beforestart();
    if (type2 === "overlay") {
      if (selection3) moving = true;
      const pts = [points[0], points[1] || points[0]];
      state.selection = selection3 = [[
        w0 = dim === Y ? W : min2(pts[0][0], pts[1][0]),
        n0 = dim === X ? N : min2(pts[0][1], pts[1][1])
      ], [
        e0 = dim === Y ? E2 : max2(pts[0][0], pts[1][0]),
        s0 = dim === X ? S : max2(pts[0][1], pts[1][1])
      ]];
      if (points.length > 1) move(event);
    } else {
      w0 = selection3[0][0];
      n0 = selection3[0][1];
      e0 = selection3[1][0];
      s0 = selection3[1][1];
    }
    w1 = w0;
    n1 = n0;
    e1 = e0;
    s1 = s0;
    var group2 = select_default2(that).attr("pointer-events", "none");
    var overlay = group2.selectAll(".overlay").attr("cursor", cursors[type2]);
    if (event.touches) {
      emit.moved = moved;
      emit.ended = ended;
    } else {
      var view = select_default2(event.view).on("mousemove.brush", moved, true).on("mouseup.brush", ended, true);
      if (keys) view.on("keydown.brush", keydowned, true).on("keyup.brush", keyupped, true);
      nodrag_default(event.view);
    }
    redraw2.call(that);
    emit.start(event, mode2.name);
    function moved(event2) {
      for (const p of event2.changedTouches || [event2]) {
        for (const d of points)
          if (d.identifier === p.identifier) d.cur = pointer_default(p, that);
      }
      if (shifting && !lockX && !lockY && points.length === 1) {
        const point6 = points[0];
        if (abs(point6.cur[0] - point6[0]) > abs(point6.cur[1] - point6[1]))
          lockY = true;
        else
          lockX = true;
      }
      for (const point6 of points)
        if (point6.cur) point6[0] = point6.cur[0], point6[1] = point6.cur[1];
      moving = true;
      noevent_default2(event2);
      move(event2);
    }
    function move(event2) {
      const point6 = points[0], point0 = point6.point0;
      var t;
      dx = point6[0] - point0[0];
      dy = point6[1] - point0[1];
      switch (mode2) {
        case MODE_SPACE:
        case MODE_DRAG: {
          if (signX) dx = max2(W - w0, min2(E2 - e0, dx)), w1 = w0 + dx, e1 = e0 + dx;
          if (signY) dy = max2(N - n0, min2(S - s0, dy)), n1 = n0 + dy, s1 = s0 + dy;
          break;
        }
        case MODE_HANDLE: {
          if (points[1]) {
            if (signX) w1 = max2(W, min2(E2, points[0][0])), e1 = max2(W, min2(E2, points[1][0])), signX = 1;
            if (signY) n1 = max2(N, min2(S, points[0][1])), s1 = max2(N, min2(S, points[1][1])), signY = 1;
          } else {
            if (signX < 0) dx = max2(W - w0, min2(E2 - w0, dx)), w1 = w0 + dx, e1 = e0;
            else if (signX > 0) dx = max2(W - e0, min2(E2 - e0, dx)), w1 = w0, e1 = e0 + dx;
            if (signY < 0) dy = max2(N - n0, min2(S - n0, dy)), n1 = n0 + dy, s1 = s0;
            else if (signY > 0) dy = max2(N - s0, min2(S - s0, dy)), n1 = n0, s1 = s0 + dy;
          }
          break;
        }
        case MODE_CENTER: {
          if (signX) w1 = max2(W, min2(E2, w0 - dx * signX)), e1 = max2(W, min2(E2, e0 + dx * signX));
          if (signY) n1 = max2(N, min2(S, n0 - dy * signY)), s1 = max2(N, min2(S, s0 + dy * signY));
          break;
        }
      }
      if (e1 < w1) {
        signX *= -1;
        t = w0, w0 = e0, e0 = t;
        t = w1, w1 = e1, e1 = t;
        if (type2 in flipX) overlay.attr("cursor", cursors[type2 = flipX[type2]]);
      }
      if (s1 < n1) {
        signY *= -1;
        t = n0, n0 = s0, s0 = t;
        t = n1, n1 = s1, s1 = t;
        if (type2 in flipY) overlay.attr("cursor", cursors[type2 = flipY[type2]]);
      }
      if (state.selection) selection3 = state.selection;
      if (lockX) w1 = selection3[0][0], e1 = selection3[1][0];
      if (lockY) n1 = selection3[0][1], s1 = selection3[1][1];
      if (selection3[0][0] !== w1 || selection3[0][1] !== n1 || selection3[1][0] !== e1 || selection3[1][1] !== s1) {
        state.selection = [[w1, n1], [e1, s1]];
        redraw2.call(that);
        emit.brush(event2, mode2.name);
      }
    }
    function ended(event2) {
      nopropagation2(event2);
      if (event2.touches) {
        if (event2.touches.length) return;
        if (touchending) clearTimeout(touchending);
        touchending = setTimeout(function() {
          touchending = null;
        }, 500);
      } else {
        yesdrag(event2.view, moving);
        view.on("keydown.brush keyup.brush mousemove.brush mouseup.brush", null);
      }
      group2.attr("pointer-events", "all");
      overlay.attr("cursor", cursors.overlay);
      if (state.selection) selection3 = state.selection;
      if (empty2(selection3)) state.selection = null, redraw2.call(that);
      emit.end(event2, mode2.name);
    }
    function keydowned(event2) {
      switch (event2.keyCode) {
        case 16: {
          shifting = signX && signY;
          break;
        }
        case 18: {
          if (mode2 === MODE_HANDLE) {
            if (signX) e0 = e1 - dx * signX, w0 = w1 + dx * signX;
            if (signY) s0 = s1 - dy * signY, n0 = n1 + dy * signY;
            mode2 = MODE_CENTER;
            move(event2);
          }
          break;
        }
        case 32: {
          if (mode2 === MODE_HANDLE || mode2 === MODE_CENTER) {
            if (signX < 0) e0 = e1 - dx;
            else if (signX > 0) w0 = w1 - dx;
            if (signY < 0) s0 = s1 - dy;
            else if (signY > 0) n0 = n1 - dy;
            mode2 = MODE_SPACE;
            overlay.attr("cursor", cursors.selection);
            move(event2);
          }
          break;
        }
        default:
          return;
      }
      noevent_default2(event2);
    }
    function keyupped(event2) {
      switch (event2.keyCode) {
        case 16: {
          if (shifting) {
            lockX = lockY = shifting = false;
            move(event2);
          }
          break;
        }
        case 18: {
          if (mode2 === MODE_CENTER) {
            if (signX < 0) e0 = e1;
            else if (signX > 0) w0 = w1;
            if (signY < 0) s0 = s1;
            else if (signY > 0) n0 = n1;
            mode2 = MODE_HANDLE;
            move(event2);
          }
          break;
        }
        case 32: {
          if (mode2 === MODE_SPACE) {
            if (event2.altKey) {
              if (signX) e0 = e1 - dx * signX, w0 = w1 + dx * signX;
              if (signY) s0 = s1 - dy * signY, n0 = n1 + dy * signY;
              mode2 = MODE_CENTER;
            } else {
              if (signX < 0) e0 = e1;
              else if (signX > 0) w0 = w1;
              if (signY < 0) s0 = s1;
              else if (signY > 0) n0 = n1;
              mode2 = MODE_HANDLE;
            }
            overlay.attr("cursor", cursors[type2]);
            move(event2);
          }
          break;
        }
        default:
          return;
      }
      noevent_default2(event2);
    }
  }
  function touchmoved(event) {
    emitter(this, arguments).moved(event);
  }
  function touchended(event) {
    emitter(this, arguments).ended(event);
  }
  function initialize() {
    var state = this.__brush || { selection: null };
    state.extent = number2(extent2.apply(this, arguments));
    state.dim = dim;
    return state;
  }
  brush2.extent = function(_) {
    return arguments.length ? (extent2 = typeof _ === "function" ? _ : constant_default4(number2(_)), brush2) : extent2;
  };
  brush2.filter = function(_) {
    return arguments.length ? (filter3 = typeof _ === "function" ? _ : constant_default4(!!_), brush2) : filter3;
  };
  brush2.touchable = function(_) {
    return arguments.length ? (touchable = typeof _ === "function" ? _ : constant_default4(!!_), brush2) : touchable;
  };
  brush2.handleSize = function(_) {
    return arguments.length ? (handleSize = +_, brush2) : handleSize;
  };
  brush2.keyModifiers = function(_) {
    return arguments.length ? (keys = !!_, brush2) : keys;
  };
  brush2.on = function() {
    var value = listeners.on.apply(listeners, arguments);
    return value === listeners ? brush2 : value;
  };
  return brush2;
}

// node_modules/d3-dsv/src/dsv.js
var EOL = {};
var EOF = {};
var QUOTE = 34;
var NEWLINE = 10;
var RETURN = 13;
function objectConverter(columns2) {
  return new Function("d", "return {" + columns2.map(function(name, i) {
    return JSON.stringify(name) + ": d[" + i + '] || ""';
  }).join(",") + "}");
}
function customConverter(columns2, f) {
  var object = objectConverter(columns2);
  return function(row, i) {
    return f(object(row), i, columns2);
  };
}
function inferColumns(rows2) {
  var columnSet = /* @__PURE__ */ Object.create(null), columns2 = [];
  rows2.forEach(function(row) {
    for (var column in row) {
      if (!(column in columnSet)) {
        columns2.push(columnSet[column] = column);
      }
    }
  });
  return columns2;
}
function pad2(value, width) {
  var s2 = value + "", length = s2.length;
  return length < width ? new Array(width - length + 1).join(0) + s2 : s2;
}
function formatYear2(year) {
  return year < 0 ? "-" + pad2(-year, 6) : year > 9999 ? "+" + pad2(year, 6) : pad2(year, 4);
}
function formatDate(date2) {
  var hours = date2.getUTCHours(), minutes = date2.getUTCMinutes(), seconds2 = date2.getUTCSeconds(), milliseconds2 = date2.getUTCMilliseconds();
  return isNaN(date2) ? "Invalid Date" : formatYear2(date2.getUTCFullYear(), 4) + "-" + pad2(date2.getUTCMonth() + 1, 2) + "-" + pad2(date2.getUTCDate(), 2) + (milliseconds2 ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + ":" + pad2(seconds2, 2) + "." + pad2(milliseconds2, 3) + "Z" : seconds2 ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + ":" + pad2(seconds2, 2) + "Z" : minutes || hours ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + "Z" : "");
}
function dsv_default(delimiter) {
  var reFormat = new RegExp('["' + delimiter + "\n\r]"), DELIMITER = delimiter.charCodeAt(0);
  function parse(text2, f) {
    var convert, columns2, rows2 = parseRows(text2, function(row, i) {
      if (convert) return convert(row, i - 1);
      columns2 = row, convert = f ? customConverter(row, f) : objectConverter(row);
    });
    rows2.columns = columns2 || [];
    return rows2;
  }
  function parseRows(text2, f) {
    var rows2 = [], N = text2.length, I = 0, n = 0, t, eof = N <= 0, eol = false;
    if (text2.charCodeAt(N - 1) === NEWLINE) --N;
    if (text2.charCodeAt(N - 1) === RETURN) --N;
    function token() {
      if (eof) return EOF;
      if (eol) return eol = false, EOL;
      var i, j = I, c;
      if (text2.charCodeAt(j) === QUOTE) {
        while (I++ < N && text2.charCodeAt(I) !== QUOTE || text2.charCodeAt(++I) === QUOTE) ;
        if ((i = I) >= N) eof = true;
        else if ((c = text2.charCodeAt(I++)) === NEWLINE) eol = true;
        else if (c === RETURN) {
          eol = true;
          if (text2.charCodeAt(I) === NEWLINE) ++I;
        }
        return text2.slice(j + 1, i - 1).replace(/""/g, '"');
      }
      while (I < N) {
        if ((c = text2.charCodeAt(i = I++)) === NEWLINE) eol = true;
        else if (c === RETURN) {
          eol = true;
          if (text2.charCodeAt(I) === NEWLINE) ++I;
        } else if (c !== DELIMITER) continue;
        return text2.slice(j, i);
      }
      return eof = true, text2.slice(j, N);
    }
    while ((t = token()) !== EOF) {
      var row = [];
      while (t !== EOL && t !== EOF) row.push(t), t = token();
      if (f && (row = f(row, n++)) == null) continue;
      rows2.push(row);
    }
    return rows2;
  }
  function preformatBody(rows2, columns2) {
    return rows2.map(function(row) {
      return columns2.map(function(column) {
        return formatValue(row[column]);
      }).join(delimiter);
    });
  }
  function format3(rows2, columns2) {
    if (columns2 == null) columns2 = inferColumns(rows2);
    return [columns2.map(formatValue).join(delimiter)].concat(preformatBody(rows2, columns2)).join("\n");
  }
  function formatBody(rows2, columns2) {
    if (columns2 == null) columns2 = inferColumns(rows2);
    return preformatBody(rows2, columns2).join("\n");
  }
  function formatRows(rows2) {
    return rows2.map(formatRow).join("\n");
  }
  function formatRow(row) {
    return row.map(formatValue).join(delimiter);
  }
  function formatValue(value) {
    return value == null ? "" : value instanceof Date ? formatDate(value) : reFormat.test(value += "") ? '"' + value.replace(/"/g, '""') + '"' : value;
  }
  return {
    parse,
    parseRows,
    format: format3,
    formatBody,
    formatRows,
    formatRow,
    formatValue
  };
}

// node_modules/d3-dsv/src/csv.js
var csv = dsv_default(",");
var csvParse = csv.parse;
var csvParseRows = csv.parseRows;
var csvFormat = csv.format;
var csvFormatBody = csv.formatBody;
var csvFormatRows = csv.formatRows;
var csvFormatRow = csv.formatRow;
var csvFormatValue = csv.formatValue;

// node_modules/d3-dsv/src/tsv.js
var tsv = dsv_default("	");
var tsvParse = tsv.parse;
var tsvParseRows = tsv.parseRows;
var tsvFormat = tsv.format;
var tsvFormatBody = tsv.formatBody;
var tsvFormatRows = tsv.formatRows;
var tsvFormatRow = tsv.formatRow;
var tsvFormatValue = tsv.formatValue;

// node_modules/d3-dsv/src/autoType.js
var fixtz = (/* @__PURE__ */ new Date("2019-01-01T00:00")).getHours() || (/* @__PURE__ */ new Date("2019-07-01T00:00")).getHours();

// node_modules/d3-scale/src/init.js
function initRange(domain2, range2) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(domain2);
      break;
    default:
      this.range(range2).domain(domain2);
      break;
  }
  return this;
}

// node_modules/d3-scale/src/ordinal.js
var implicit = Symbol("implicit");
function ordinal() {
  var index2 = new InternMap(), domain2 = [], range2 = [], unknown = implicit;
  function scale2(d) {
    let i = index2.get(d);
    if (i === void 0) {
      if (unknown !== implicit) return unknown;
      index2.set(d, i = domain2.push(d) - 1);
    }
    return range2[i % range2.length];
  }
  scale2.domain = function(_) {
    if (!arguments.length) return domain2.slice();
    domain2 = [], index2 = new InternMap();
    for (const value of _) {
      if (index2.has(value)) continue;
      index2.set(value, domain2.push(value) - 1);
    }
    return scale2;
  };
  scale2.range = function(_) {
    return arguments.length ? (range2 = Array.from(_), scale2) : range2.slice();
  };
  scale2.unknown = function(_) {
    return arguments.length ? (unknown = _, scale2) : unknown;
  };
  scale2.copy = function() {
    return ordinal(domain2, range2).unknown(unknown);
  };
  initRange.apply(scale2, arguments);
  return scale2;
}

// node_modules/d3-scale/src/constant.js
function constants(x3) {
  return function() {
    return x3;
  };
}

// node_modules/d3-scale/src/number.js
function number3(x3) {
  return +x3;
}

// node_modules/d3-scale/src/continuous.js
var unit = [0, 1];
function identity3(x3) {
  return x3;
}
function normalize(a2, b) {
  return (b -= a2 = +a2) ? function(x3) {
    return (x3 - a2) / b;
  } : constants(isNaN(b) ? NaN : 0.5);
}
function clamper(a2, b) {
  var t;
  if (a2 > b) t = a2, a2 = b, b = t;
  return function(x3) {
    return Math.max(a2, Math.min(b, x3));
  };
}
function bimap(domain2, range2, interpolate) {
  var d0 = domain2[0], d1 = domain2[1], r0 = range2[0], r1 = range2[1];
  if (d1 < d0) d0 = normalize(d1, d0), r0 = interpolate(r1, r0);
  else d0 = normalize(d0, d1), r0 = interpolate(r0, r1);
  return function(x3) {
    return r0(d0(x3));
  };
}
function polymap(domain2, range2, interpolate) {
  var j = Math.min(domain2.length, range2.length) - 1, d = new Array(j), r = new Array(j), i = -1;
  if (domain2[j] < domain2[0]) {
    domain2 = domain2.slice().reverse();
    range2 = range2.slice().reverse();
  }
  while (++i < j) {
    d[i] = normalize(domain2[i], domain2[i + 1]);
    r[i] = interpolate(range2[i], range2[i + 1]);
  }
  return function(x3) {
    var i2 = bisect_default(domain2, x3, 1, j) - 1;
    return r[i2](d[i2](x3));
  };
}
function copy(source, target) {
  return target.domain(source.domain()).range(source.range()).interpolate(source.interpolate()).clamp(source.clamp()).unknown(source.unknown());
}
function transformer() {
  var domain2 = unit, range2 = unit, interpolate = value_default, transform3, untransform, unknown, clamp = identity3, piecewise2, output, input;
  function rescale() {
    var n = Math.min(domain2.length, range2.length);
    if (clamp !== identity3) clamp = clamper(domain2[0], domain2[n - 1]);
    piecewise2 = n > 2 ? polymap : bimap;
    output = input = null;
    return scale2;
  }
  function scale2(x3) {
    return x3 == null || isNaN(x3 = +x3) ? unknown : (output || (output = piecewise2(domain2.map(transform3), range2, interpolate)))(transform3(clamp(x3)));
  }
  scale2.invert = function(y3) {
    return clamp(untransform((input || (input = piecewise2(range2, domain2.map(transform3), number_default)))(y3)));
  };
  scale2.domain = function(_) {
    return arguments.length ? (domain2 = Array.from(_, number3), rescale()) : domain2.slice();
  };
  scale2.range = function(_) {
    return arguments.length ? (range2 = Array.from(_), rescale()) : range2.slice();
  };
  scale2.rangeRound = function(_) {
    return range2 = Array.from(_), interpolate = round_default, rescale();
  };
  scale2.clamp = function(_) {
    return arguments.length ? (clamp = _ ? true : identity3, rescale()) : clamp !== identity3;
  };
  scale2.interpolate = function(_) {
    return arguments.length ? (interpolate = _, rescale()) : interpolate;
  };
  scale2.unknown = function(_) {
    return arguments.length ? (unknown = _, scale2) : unknown;
  };
  return function(t, u) {
    transform3 = t, untransform = u;
    return rescale();
  };
}
function continuous() {
  return transformer()(identity3, identity3);
}

// node_modules/d3-format/src/formatDecimal.js
function formatDecimal_default(x3) {
  return Math.abs(x3 = Math.round(x3)) >= 1e21 ? x3.toLocaleString("en").replace(/,/g, "") : x3.toString(10);
}
function formatDecimalParts(x3, p) {
  if ((i = (x3 = p ? x3.toExponential(p - 1) : x3.toExponential()).indexOf("e")) < 0) return null;
  var i, coefficient = x3.slice(0, i);
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x3.slice(i + 1)
  ];
}

// node_modules/d3-format/src/exponent.js
function exponent_default(x3) {
  return x3 = formatDecimalParts(Math.abs(x3)), x3 ? x3[1] : NaN;
}

// node_modules/d3-format/src/formatGroup.js
function formatGroup_default(grouping, thousands) {
  return function(value, width) {
    var i = value.length, t = [], j = 0, g = grouping[0], length = 0;
    while (i > 0 && g > 0) {
      if (length + g + 1 > width) g = Math.max(1, width - length);
      t.push(value.substring(i -= g, i + g));
      if ((length += g + 1) > width) break;
      g = grouping[j = (j + 1) % grouping.length];
    }
    return t.reverse().join(thousands);
  };
}

// node_modules/d3-format/src/formatNumerals.js
function formatNumerals_default(numerals) {
  return function(value) {
    return value.replace(/[0-9]/g, function(i) {
      return numerals[+i];
    });
  };
}

// node_modules/d3-format/src/formatSpecifier.js
var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function formatSpecifier(specifier) {
  if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match;
  return new FormatSpecifier({
    fill: match[1],
    align: match[2],
    sign: match[3],
    symbol: match[4],
    zero: match[5],
    width: match[6],
    comma: match[7],
    precision: match[8] && match[8].slice(1),
    trim: match[9],
    type: match[10]
  });
}
formatSpecifier.prototype = FormatSpecifier.prototype;
function FormatSpecifier(specifier) {
  this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
  this.align = specifier.align === void 0 ? ">" : specifier.align + "";
  this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === void 0 ? void 0 : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === void 0 ? "" : specifier.type + "";
}
FormatSpecifier.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};

// node_modules/d3-format/src/formatTrim.js
function formatTrim_default(s2) {
  out: for (var n = s2.length, i = 1, i0 = -1, i1; i < n; ++i) {
    switch (s2[i]) {
      case ".":
        i0 = i1 = i;
        break;
      case "0":
        if (i0 === 0) i0 = i;
        i1 = i;
        break;
      default:
        if (!+s2[i]) break out;
        if (i0 > 0) i0 = 0;
        break;
    }
  }
  return i0 > 0 ? s2.slice(0, i0) + s2.slice(i1 + 1) : s2;
}

// node_modules/d3-format/src/formatPrefixAuto.js
var prefixExponent;
function formatPrefixAuto_default(x3, p) {
  var d = formatDecimalParts(x3, p);
  if (!d) return x3 + "";
  var coefficient = d[0], exponent2 = d[1], i = exponent2 - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent2 / 3))) * 3) + 1, n = coefficient.length;
  return i === n ? coefficient : i > n ? coefficient + new Array(i - n + 1).join("0") : i > 0 ? coefficient.slice(0, i) + "." + coefficient.slice(i) : "0." + new Array(1 - i).join("0") + formatDecimalParts(x3, Math.max(0, p + i - 1))[0];
}

// node_modules/d3-format/src/formatRounded.js
function formatRounded_default(x3, p) {
  var d = formatDecimalParts(x3, p);
  if (!d) return x3 + "";
  var coefficient = d[0], exponent2 = d[1];
  return exponent2 < 0 ? "0." + new Array(-exponent2).join("0") + coefficient : coefficient.length > exponent2 + 1 ? coefficient.slice(0, exponent2 + 1) + "." + coefficient.slice(exponent2 + 1) : coefficient + new Array(exponent2 - coefficient.length + 2).join("0");
}

// node_modules/d3-format/src/formatTypes.js
var formatTypes_default = {
  "%": (x3, p) => (x3 * 100).toFixed(p),
  "b": (x3) => Math.round(x3).toString(2),
  "c": (x3) => x3 + "",
  "d": formatDecimal_default,
  "e": (x3, p) => x3.toExponential(p),
  "f": (x3, p) => x3.toFixed(p),
  "g": (x3, p) => x3.toPrecision(p),
  "o": (x3) => Math.round(x3).toString(8),
  "p": (x3, p) => formatRounded_default(x3 * 100, p),
  "r": formatRounded_default,
  "s": formatPrefixAuto_default,
  "X": (x3) => Math.round(x3).toString(16).toUpperCase(),
  "x": (x3) => Math.round(x3).toString(16)
};

// node_modules/d3-format/src/identity.js
function identity_default(x3) {
  return x3;
}

// node_modules/d3-format/src/locale.js
var map3 = Array.prototype.map;
var prefixes = ["y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function locale_default(locale3) {
  var group2 = locale3.grouping === void 0 || locale3.thousands === void 0 ? identity_default : formatGroup_default(map3.call(locale3.grouping, Number), locale3.thousands + ""), currencyPrefix = locale3.currency === void 0 ? "" : locale3.currency[0] + "", currencySuffix = locale3.currency === void 0 ? "" : locale3.currency[1] + "", decimal = locale3.decimal === void 0 ? "." : locale3.decimal + "", numerals = locale3.numerals === void 0 ? identity_default : formatNumerals_default(map3.call(locale3.numerals, String)), percent = locale3.percent === void 0 ? "%" : locale3.percent + "", minus = locale3.minus === void 0 ? "−" : locale3.minus + "", nan = locale3.nan === void 0 ? "NaN" : locale3.nan + "";
  function newFormat(specifier) {
    specifier = formatSpecifier(specifier);
    var fill = specifier.fill, align = specifier.align, sign2 = specifier.sign, symbol = specifier.symbol, zero3 = specifier.zero, width = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type2 = specifier.type;
    if (type2 === "n") comma = true, type2 = "g";
    else if (!formatTypes_default[type2]) precision === void 0 && (precision = 12), trim = true, type2 = "g";
    if (zero3 || fill === "0" && align === "=") zero3 = true, fill = "0", align = "=";
    var prefix = symbol === "$" ? currencyPrefix : symbol === "#" && /[boxX]/.test(type2) ? "0" + type2.toLowerCase() : "", suffix = symbol === "$" ? currencySuffix : /[%p]/.test(type2) ? percent : "";
    var formatType = formatTypes_default[type2], maybeSuffix = /[defgprs%]/.test(type2);
    precision = precision === void 0 ? 6 : /[gprs]/.test(type2) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
    function format3(value) {
      var valuePrefix = prefix, valueSuffix = suffix, i, n, c;
      if (type2 === "c") {
        valueSuffix = formatType(value) + valueSuffix;
        value = "";
      } else {
        value = +value;
        var valueNegative = value < 0 || 1 / value < 0;
        value = isNaN(value) ? nan : formatType(Math.abs(value), precision);
        if (trim) value = formatTrim_default(value);
        if (valueNegative && +value === 0 && sign2 !== "+") valueNegative = false;
        valuePrefix = (valueNegative ? sign2 === "(" ? sign2 : minus : sign2 === "-" || sign2 === "(" ? "" : sign2) + valuePrefix;
        valueSuffix = (type2 === "s" ? prefixes[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign2 === "(" ? ")" : "");
        if (maybeSuffix) {
          i = -1, n = value.length;
          while (++i < n) {
            if (c = value.charCodeAt(i), 48 > c || c > 57) {
              valueSuffix = (c === 46 ? decimal + value.slice(i + 1) : value.slice(i)) + valueSuffix;
              value = value.slice(0, i);
              break;
            }
          }
        }
      }
      if (comma && !zero3) value = group2(value, Infinity);
      var length = valuePrefix.length + value.length + valueSuffix.length, padding = length < width ? new Array(width - length + 1).join(fill) : "";
      if (comma && zero3) value = group2(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";
      switch (align) {
        case "<":
          value = valuePrefix + value + valueSuffix + padding;
          break;
        case "=":
          value = valuePrefix + padding + value + valueSuffix;
          break;
        case "^":
          value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length);
          break;
        default:
          value = padding + valuePrefix + value + valueSuffix;
          break;
      }
      return numerals(value);
    }
    format3.toString = function() {
      return specifier + "";
    };
    return format3;
  }
  function formatPrefix2(specifier, value) {
    var f = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier)), e = Math.max(-8, Math.min(8, Math.floor(exponent_default(value) / 3))) * 3, k2 = Math.pow(10, -e), prefix = prefixes[8 + e / 3];
    return function(value2) {
      return f(k2 * value2) + prefix;
    };
  }
  return {
    format: newFormat,
    formatPrefix: formatPrefix2
  };
}

// node_modules/d3-format/src/defaultLocale.js
var locale2;
var format;
var formatPrefix;
defaultLocale2({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function defaultLocale2(definition) {
  locale2 = locale_default(definition);
  format = locale2.format;
  formatPrefix = locale2.formatPrefix;
  return locale2;
}

// node_modules/d3-format/src/precisionFixed.js
function precisionFixed_default(step2) {
  return Math.max(0, -exponent_default(Math.abs(step2)));
}

// node_modules/d3-format/src/precisionPrefix.js
function precisionPrefix_default(step2, value) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent_default(value) / 3))) * 3 - exponent_default(Math.abs(step2)));
}

// node_modules/d3-format/src/precisionRound.js
function precisionRound_default(step2, max4) {
  step2 = Math.abs(step2), max4 = Math.abs(max4) - step2;
  return Math.max(0, exponent_default(max4) - exponent_default(step2)) + 1;
}

// node_modules/d3-scale/src/tickFormat.js
function tickFormat(start2, stop, count3, specifier) {
  var step2 = tickStep(start2, stop, count3), precision;
  specifier = formatSpecifier(specifier == null ? ",f" : specifier);
  switch (specifier.type) {
    case "s": {
      var value = Math.max(Math.abs(start2), Math.abs(stop));
      if (specifier.precision == null && !isNaN(precision = precisionPrefix_default(step2, value))) specifier.precision = precision;
      return formatPrefix(specifier, value);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      if (specifier.precision == null && !isNaN(precision = precisionRound_default(step2, Math.max(Math.abs(start2), Math.abs(stop))))) specifier.precision = precision - (specifier.type === "e");
      break;
    }
    case "f":
    case "%": {
      if (specifier.precision == null && !isNaN(precision = precisionFixed_default(step2))) specifier.precision = precision - (specifier.type === "%") * 2;
      break;
    }
  }
  return format(specifier);
}

// node_modules/d3-scale/src/linear.js
function linearish(scale2) {
  var domain2 = scale2.domain;
  scale2.ticks = function(count3) {
    var d = domain2();
    return ticks(d[0], d[d.length - 1], count3 == null ? 10 : count3);
  };
  scale2.tickFormat = function(count3, specifier) {
    var d = domain2();
    return tickFormat(d[0], d[d.length - 1], count3 == null ? 10 : count3, specifier);
  };
  scale2.nice = function(count3) {
    if (count3 == null) count3 = 10;
    var d = domain2();
    var i0 = 0;
    var i1 = d.length - 1;
    var start2 = d[i0];
    var stop = d[i1];
    var prestep;
    var step2;
    var maxIter = 10;
    if (stop < start2) {
      step2 = start2, start2 = stop, stop = step2;
      step2 = i0, i0 = i1, i1 = step2;
    }
    while (maxIter-- > 0) {
      step2 = tickIncrement(start2, stop, count3);
      if (step2 === prestep) {
        d[i0] = start2;
        d[i1] = stop;
        return domain2(d);
      } else if (step2 > 0) {
        start2 = Math.floor(start2 / step2) * step2;
        stop = Math.ceil(stop / step2) * step2;
      } else if (step2 < 0) {
        start2 = Math.ceil(start2 * step2) / step2;
        stop = Math.floor(stop * step2) / step2;
      } else {
        break;
      }
      prestep = step2;
    }
    return scale2;
  };
  return scale2;
}
function linear3() {
  var scale2 = continuous();
  scale2.copy = function() {
    return copy(scale2, linear3());
  };
  initRange.apply(scale2, arguments);
  return linearish(scale2);
}

// node_modules/d3-scale/src/nice.js
function nice2(domain2, interval2) {
  domain2 = domain2.slice();
  var i0 = 0, i1 = domain2.length - 1, x0 = domain2[i0], x1 = domain2[i1], t;
  if (x1 < x0) {
    t = i0, i0 = i1, i1 = t;
    t = x0, x0 = x1, x1 = t;
  }
  domain2[i0] = interval2.floor(x0);
  domain2[i1] = interval2.ceil(x1);
  return domain2;
}

// node_modules/d3-scale/src/log.js
function transformLog(x3) {
  return Math.log(x3);
}
function transformExp(x3) {
  return Math.exp(x3);
}
function transformLogn(x3) {
  return -Math.log(-x3);
}
function transformExpn(x3) {
  return -Math.exp(-x3);
}
function pow10(x3) {
  return isFinite(x3) ? +("1e" + x3) : x3 < 0 ? 0 : x3;
}
function powp(base) {
  return base === 10 ? pow10 : base === Math.E ? Math.exp : (x3) => Math.pow(base, x3);
}
function logp(base) {
  return base === Math.E ? Math.log : base === 10 && Math.log10 || base === 2 && Math.log2 || (base = Math.log(base), (x3) => Math.log(x3) / base);
}
function reflect(f) {
  return (x3, k2) => -f(-x3, k2);
}
function loggish(transform3) {
  const scale2 = transform3(transformLog, transformExp);
  const domain2 = scale2.domain;
  let base = 10;
  let logs;
  let pows;
  function rescale() {
    logs = logp(base), pows = powp(base);
    if (domain2()[0] < 0) {
      logs = reflect(logs), pows = reflect(pows);
      transform3(transformLogn, transformExpn);
    } else {
      transform3(transformLog, transformExp);
    }
    return scale2;
  }
  scale2.base = function(_) {
    return arguments.length ? (base = +_, rescale()) : base;
  };
  scale2.domain = function(_) {
    return arguments.length ? (domain2(_), rescale()) : domain2();
  };
  scale2.ticks = (count3) => {
    const d = domain2();
    let u = d[0];
    let v = d[d.length - 1];
    const r = v < u;
    if (r) [u, v] = [v, u];
    let i = logs(u);
    let j = logs(v);
    let k2;
    let t;
    const n = count3 == null ? 10 : +count3;
    let z = [];
    if (!(base % 1) && j - i < n) {
      i = Math.floor(i), j = Math.ceil(j);
      if (u > 0) for (; i <= j; ++i) {
        for (k2 = 1; k2 < base; ++k2) {
          t = i < 0 ? k2 / pows(-i) : k2 * pows(i);
          if (t < u) continue;
          if (t > v) break;
          z.push(t);
        }
      }
      else for (; i <= j; ++i) {
        for (k2 = base - 1; k2 >= 1; --k2) {
          t = i > 0 ? k2 / pows(-i) : k2 * pows(i);
          if (t < u) continue;
          if (t > v) break;
          z.push(t);
        }
      }
      if (z.length * 2 < n) z = ticks(u, v, n);
    } else {
      z = ticks(i, j, Math.min(j - i, n)).map(pows);
    }
    return r ? z.reverse() : z;
  };
  scale2.tickFormat = (count3, specifier) => {
    if (count3 == null) count3 = 10;
    if (specifier == null) specifier = base === 10 ? "s" : ",";
    if (typeof specifier !== "function") {
      if (!(base % 1) && (specifier = formatSpecifier(specifier)).precision == null) specifier.trim = true;
      specifier = format(specifier);
    }
    if (count3 === Infinity) return specifier;
    const k2 = Math.max(1, base * count3 / scale2.ticks().length);
    return (d) => {
      let i = d / pows(Math.round(logs(d)));
      if (i * base < base - 0.5) i *= base;
      return i <= k2 ? specifier(d) : "";
    };
  };
  scale2.nice = () => {
    return domain2(nice2(domain2(), {
      floor: (x3) => pows(Math.floor(logs(x3))),
      ceil: (x3) => pows(Math.ceil(logs(x3)))
    }));
  };
  return scale2;
}
function log() {
  const scale2 = loggish(transformer()).domain([1, 10]);
  scale2.copy = () => copy(scale2, log()).base(scale2.base());
  initRange.apply(scale2, arguments);
  return scale2;
}

// node_modules/d3-scale/src/symlog.js
function transformSymlog(c) {
  return function(x3) {
    return Math.sign(x3) * Math.log1p(Math.abs(x3 / c));
  };
}
function transformSymexp(c) {
  return function(x3) {
    return Math.sign(x3) * Math.expm1(Math.abs(x3)) * c;
  };
}
function symlogish(transform3) {
  var c = 1, scale2 = transform3(transformSymlog(c), transformSymexp(c));
  scale2.constant = function(_) {
    return arguments.length ? transform3(transformSymlog(c = +_), transformSymexp(c)) : c;
  };
  return linearish(scale2);
}
function symlog() {
  var scale2 = symlogish(transformer());
  scale2.copy = function() {
    return copy(scale2, symlog()).constant(scale2.constant());
  };
  return initRange.apply(scale2, arguments);
}

// node_modules/d3-scale/src/time.js
function date(t) {
  return new Date(t);
}
function number4(t) {
  return t instanceof Date ? +t : +/* @__PURE__ */ new Date(+t);
}
function calendar(ticks2, tickInterval, year, month, week, day, hour, minute, second2, format3) {
  var scale2 = continuous(), invert = scale2.invert, domain2 = scale2.domain;
  var formatMillisecond = format3(".%L"), formatSecond = format3(":%S"), formatMinute = format3("%I:%M"), formatHour = format3("%I %p"), formatDay = format3("%a %d"), formatWeek = format3("%b %d"), formatMonth = format3("%B"), formatYear3 = format3("%Y");
  function tickFormat2(date2) {
    return (second2(date2) < date2 ? formatMillisecond : minute(date2) < date2 ? formatSecond : hour(date2) < date2 ? formatMinute : day(date2) < date2 ? formatHour : month(date2) < date2 ? week(date2) < date2 ? formatDay : formatWeek : year(date2) < date2 ? formatMonth : formatYear3)(date2);
  }
  scale2.invert = function(y3) {
    return new Date(invert(y3));
  };
  scale2.domain = function(_) {
    return arguments.length ? domain2(Array.from(_, number4)) : domain2().map(date);
  };
  scale2.ticks = function(interval2) {
    var d = domain2();
    return ticks2(d[0], d[d.length - 1], interval2 == null ? 10 : interval2);
  };
  scale2.tickFormat = function(count3, specifier) {
    return specifier == null ? tickFormat2 : format3(specifier);
  };
  scale2.nice = function(interval2) {
    var d = domain2();
    if (!interval2 || typeof interval2.range !== "function") interval2 = tickInterval(d[0], d[d.length - 1], interval2 == null ? 10 : interval2);
    return interval2 ? domain2(nice2(d, interval2)) : scale2;
  };
  scale2.copy = function() {
    return copy(scale2, calendar(ticks2, tickInterval, year, month, week, day, hour, minute, second2, format3));
  };
  return scale2;
}
function time() {
  return initRange.apply(calendar(timeTicks, timeTickInterval, timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute, second, timeFormat).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
}

// node_modules/d3-scale/src/utcTime.js
function utcTime() {
  return initRange.apply(calendar(utcTicks, utcTickInterval, utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, utcFormat).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
}

// node_modules/d3-shape/src/constant.js
function constant_default5(x3) {
  return function constant2() {
    return x3;
  };
}

// node_modules/d3-shape/src/math.js
var abs2 = Math.abs;
var atan2 = Math.atan2;
var cos = Math.cos;
var max3 = Math.max;
var min3 = Math.min;
var sin = Math.sin;
var sqrt2 = Math.sqrt;
var epsilon = 1e-12;
var pi2 = Math.PI;
var halfPi2 = pi2 / 2;
var tau2 = 2 * pi2;
function acos(x3) {
  return x3 > 1 ? 0 : x3 < -1 ? pi2 : Math.acos(x3);
}
function asin(x3) {
  return x3 >= 1 ? halfPi2 : x3 <= -1 ? -halfPi2 : Math.asin(x3);
}

// node_modules/d3-path/src/path.js
var pi3 = Math.PI;
var tau3 = 2 * pi3;
var epsilon3 = 1e-6;
var tauEpsilon = tau3 - epsilon3;
function append(strings) {
  this._ += strings[0];
  for (let i = 1, n = strings.length; i < n; ++i) {
    this._ += arguments[i] + strings[i];
  }
}
function appendRound(digits) {
  let d = Math.floor(digits);
  if (!(d >= 0)) throw new Error(`invalid digits: ${digits}`);
  if (d > 15) return append;
  const k2 = 10 ** d;
  return function(strings) {
    this._ += strings[0];
    for (let i = 1, n = strings.length; i < n; ++i) {
      this._ += Math.round(arguments[i] * k2) / k2 + strings[i];
    }
  };
}
var Path = class {
  constructor(digits) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
    this._append = digits == null ? append : appendRound(digits);
  }
  moveTo(x3, y3) {
    this._append`M${this._x0 = this._x1 = +x3},${this._y0 = this._y1 = +y3}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._append`Z`;
    }
  }
  lineTo(x3, y3) {
    this._append`L${this._x1 = +x3},${this._y1 = +y3}`;
  }
  quadraticCurveTo(x1, y1, x3, y3) {
    this._append`Q${+x1},${+y1},${this._x1 = +x3},${this._y1 = +y3}`;
  }
  bezierCurveTo(x1, y1, x22, y23, x3, y3) {
    this._append`C${+x1},${+y1},${+x22},${+y23},${this._x1 = +x3},${this._y1 = +y3}`;
  }
  arcTo(x1, y1, x22, y23, r) {
    x1 = +x1, y1 = +y1, x22 = +x22, y23 = +y23, r = +r;
    if (r < 0) throw new Error(`negative radius: ${r}`);
    let x0 = this._x1, y0 = this._y1, x21 = x22 - x1, y21 = y23 - y1, x01 = x0 - x1, y01 = y0 - y1, l01_2 = x01 * x01 + y01 * y01;
    if (this._x1 === null) {
      this._append`M${this._x1 = x1},${this._y1 = y1}`;
    } else if (!(l01_2 > epsilon3)) ;
    else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon3) || !r) {
      this._append`L${this._x1 = x1},${this._y1 = y1}`;
    } else {
      let x20 = x22 - x0, y20 = y23 - y0, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l = r * Math.tan((pi3 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l / l01, t21 = l / l21;
      if (Math.abs(t01 - 1) > epsilon3) {
        this._append`L${x1 + t01 * x01},${y1 + t01 * y01}`;
      }
      this._append`A${r},${r},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x1 + t21 * x21},${this._y1 = y1 + t21 * y21}`;
    }
  }
  arc(x3, y3, r, a0, a1, ccw) {
    x3 = +x3, y3 = +y3, r = +r, ccw = !!ccw;
    if (r < 0) throw new Error(`negative radius: ${r}`);
    let dx = r * Math.cos(a0), dy = r * Math.sin(a0), x0 = x3 + dx, y0 = y3 + dy, cw = 1 ^ ccw, da = ccw ? a0 - a1 : a1 - a0;
    if (this._x1 === null) {
      this._append`M${x0},${y0}`;
    } else if (Math.abs(this._x1 - x0) > epsilon3 || Math.abs(this._y1 - y0) > epsilon3) {
      this._append`L${x0},${y0}`;
    }
    if (!r) return;
    if (da < 0) da = da % tau3 + tau3;
    if (da > tauEpsilon) {
      this._append`A${r},${r},0,1,${cw},${x3 - dx},${y3 - dy}A${r},${r},0,1,${cw},${this._x1 = x0},${this._y1 = y0}`;
    } else if (da > epsilon3) {
      this._append`A${r},${r},0,${+(da >= pi3)},${cw},${this._x1 = x3 + r * Math.cos(a1)},${this._y1 = y3 + r * Math.sin(a1)}`;
    }
  }
  rect(x3, y3, w, h) {
    this._append`M${this._x0 = this._x1 = +x3},${this._y0 = this._y1 = +y3}h${w = +w}v${+h}h${-w}Z`;
  }
  toString() {
    return this._;
  }
};
function path() {
  return new Path();
}
path.prototype = Path.prototype;

// node_modules/d3-shape/src/path.js
function withPath(shape2) {
  let digits = 3;
  shape2.digits = function(_) {
    if (!arguments.length) return digits;
    if (_ == null) {
      digits = null;
    } else {
      const d = Math.floor(_);
      if (!(d >= 0)) throw new RangeError(`invalid digits: ${_}`);
      digits = d;
    }
    return shape2;
  };
  return () => new Path(digits);
}

// node_modules/d3-shape/src/arc.js
function arcInnerRadius(d) {
  return d.innerRadius;
}
function arcOuterRadius(d) {
  return d.outerRadius;
}
function arcStartAngle(d) {
  return d.startAngle;
}
function arcEndAngle(d) {
  return d.endAngle;
}
function arcPadAngle(d) {
  return d && d.padAngle;
}
function intersect(x0, y0, x1, y1, x22, y23, x3, y3) {
  var x10 = x1 - x0, y10 = y1 - y0, x32 = x3 - x22, y32 = y3 - y23, t = y32 * x10 - x32 * y10;
  if (t * t < epsilon) return;
  t = (x32 * (y0 - y23) - y32 * (x0 - x22)) / t;
  return [x0 + t * x10, y0 + t * y10];
}
function cornerTangents(x0, y0, x1, y1, r1, rc, cw) {
  var x01 = x0 - x1, y01 = y0 - y1, lo = (cw ? rc : -rc) / sqrt2(x01 * x01 + y01 * y01), ox = lo * y01, oy = -lo * x01, x11 = x0 + ox, y11 = y0 + oy, x10 = x1 + ox, y10 = y1 + oy, x00 = (x11 + x10) / 2, y00 = (y11 + y10) / 2, dx = x10 - x11, dy = y10 - y11, d2 = dx * dx + dy * dy, r = r1 - rc, D2 = x11 * y10 - x10 * y11, d = (dy < 0 ? -1 : 1) * sqrt2(max3(0, r * r * d2 - D2 * D2)), cx0 = (D2 * dy - dx * d) / d2, cy0 = (-D2 * dx - dy * d) / d2, cx1 = (D2 * dy + dx * d) / d2, cy1 = (-D2 * dx + dy * d) / d2, dx0 = cx0 - x00, dy0 = cy0 - y00, dx1 = cx1 - x00, dy1 = cy1 - y00;
  if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;
  return {
    cx: cx0,
    cy: cy0,
    x01: -ox,
    y01: -oy,
    x11: cx0 * (r1 / r - 1),
    y11: cy0 * (r1 / r - 1)
  };
}
function arc_default() {
  var innerRadius = arcInnerRadius, outerRadius = arcOuterRadius, cornerRadius = constant_default5(0), padRadius = null, startAngle = arcStartAngle, endAngle = arcEndAngle, padAngle = arcPadAngle, context = null, path2 = withPath(arc);
  function arc() {
    var buffer, r, r0 = +innerRadius.apply(this, arguments), r1 = +outerRadius.apply(this, arguments), a0 = startAngle.apply(this, arguments) - halfPi2, a1 = endAngle.apply(this, arguments) - halfPi2, da = abs2(a1 - a0), cw = a1 > a0;
    if (!context) context = buffer = path2();
    if (r1 < r0) r = r1, r1 = r0, r0 = r;
    if (!(r1 > epsilon)) context.moveTo(0, 0);
    else if (da > tau2 - epsilon) {
      context.moveTo(r1 * cos(a0), r1 * sin(a0));
      context.arc(0, 0, r1, a0, a1, !cw);
      if (r0 > epsilon) {
        context.moveTo(r0 * cos(a1), r0 * sin(a1));
        context.arc(0, 0, r0, a1, a0, cw);
      }
    } else {
      var a01 = a0, a11 = a1, a00 = a0, a10 = a1, da0 = da, da1 = da, ap = padAngle.apply(this, arguments) / 2, rp = ap > epsilon && (padRadius ? +padRadius.apply(this, arguments) : sqrt2(r0 * r0 + r1 * r1)), rc = min3(abs2(r1 - r0) / 2, +cornerRadius.apply(this, arguments)), rc0 = rc, rc1 = rc, t03, t13;
      if (rp > epsilon) {
        var p0 = asin(rp / r0 * sin(ap)), p1 = asin(rp / r1 * sin(ap));
        if ((da0 -= p0 * 2) > epsilon) p0 *= cw ? 1 : -1, a00 += p0, a10 -= p0;
        else da0 = 0, a00 = a10 = (a0 + a1) / 2;
        if ((da1 -= p1 * 2) > epsilon) p1 *= cw ? 1 : -1, a01 += p1, a11 -= p1;
        else da1 = 0, a01 = a11 = (a0 + a1) / 2;
      }
      var x01 = r1 * cos(a01), y01 = r1 * sin(a01), x10 = r0 * cos(a10), y10 = r0 * sin(a10);
      if (rc > epsilon) {
        var x11 = r1 * cos(a11), y11 = r1 * sin(a11), x00 = r0 * cos(a00), y00 = r0 * sin(a00), oc;
        if (da < pi2) {
          if (oc = intersect(x01, y01, x00, y00, x11, y11, x10, y10)) {
            var ax = x01 - oc[0], ay = y01 - oc[1], bx = x11 - oc[0], by = y11 - oc[1], kc = 1 / sin(acos((ax * bx + ay * by) / (sqrt2(ax * ax + ay * ay) * sqrt2(bx * bx + by * by))) / 2), lc = sqrt2(oc[0] * oc[0] + oc[1] * oc[1]);
            rc0 = min3(rc, (r0 - lc) / (kc - 1));
            rc1 = min3(rc, (r1 - lc) / (kc + 1));
          } else {
            rc0 = rc1 = 0;
          }
        }
      }
      if (!(da1 > epsilon)) context.moveTo(x01, y01);
      else if (rc1 > epsilon) {
        t03 = cornerTangents(x00, y00, x01, y01, r1, rc1, cw);
        t13 = cornerTangents(x11, y11, x10, y10, r1, rc1, cw);
        context.moveTo(t03.cx + t03.x01, t03.cy + t03.y01);
        if (rc1 < rc) context.arc(t03.cx, t03.cy, rc1, atan2(t03.y01, t03.x01), atan2(t13.y01, t13.x01), !cw);
        else {
          context.arc(t03.cx, t03.cy, rc1, atan2(t03.y01, t03.x01), atan2(t03.y11, t03.x11), !cw);
          context.arc(0, 0, r1, atan2(t03.cy + t03.y11, t03.cx + t03.x11), atan2(t13.cy + t13.y11, t13.cx + t13.x11), !cw);
          context.arc(t13.cx, t13.cy, rc1, atan2(t13.y11, t13.x11), atan2(t13.y01, t13.x01), !cw);
        }
      } else context.moveTo(x01, y01), context.arc(0, 0, r1, a01, a11, !cw);
      if (!(r0 > epsilon) || !(da0 > epsilon)) context.lineTo(x10, y10);
      else if (rc0 > epsilon) {
        t03 = cornerTangents(x10, y10, x11, y11, r0, -rc0, cw);
        t13 = cornerTangents(x01, y01, x00, y00, r0, -rc0, cw);
        context.lineTo(t03.cx + t03.x01, t03.cy + t03.y01);
        if (rc0 < rc) context.arc(t03.cx, t03.cy, rc0, atan2(t03.y01, t03.x01), atan2(t13.y01, t13.x01), !cw);
        else {
          context.arc(t03.cx, t03.cy, rc0, atan2(t03.y01, t03.x01), atan2(t03.y11, t03.x11), !cw);
          context.arc(0, 0, r0, atan2(t03.cy + t03.y11, t03.cx + t03.x11), atan2(t13.cy + t13.y11, t13.cx + t13.x11), cw);
          context.arc(t13.cx, t13.cy, rc0, atan2(t13.y11, t13.x11), atan2(t13.y01, t13.x01), !cw);
        }
      } else context.arc(0, 0, r0, a10, a00, cw);
    }
    context.closePath();
    if (buffer) return context = null, buffer + "" || null;
  }
  arc.centroid = function() {
    var r = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2, a2 = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - pi2 / 2;
    return [cos(a2) * r, sin(a2) * r];
  };
  arc.innerRadius = function(_) {
    return arguments.length ? (innerRadius = typeof _ === "function" ? _ : constant_default5(+_), arc) : innerRadius;
  };
  arc.outerRadius = function(_) {
    return arguments.length ? (outerRadius = typeof _ === "function" ? _ : constant_default5(+_), arc) : outerRadius;
  };
  arc.cornerRadius = function(_) {
    return arguments.length ? (cornerRadius = typeof _ === "function" ? _ : constant_default5(+_), arc) : cornerRadius;
  };
  arc.padRadius = function(_) {
    return arguments.length ? (padRadius = _ == null ? null : typeof _ === "function" ? _ : constant_default5(+_), arc) : padRadius;
  };
  arc.startAngle = function(_) {
    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default5(+_), arc) : startAngle;
  };
  arc.endAngle = function(_) {
    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default5(+_), arc) : endAngle;
  };
  arc.padAngle = function(_) {
    return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default5(+_), arc) : padAngle;
  };
  arc.context = function(_) {
    return arguments.length ? (context = _ == null ? null : _, arc) : context;
  };
  return arc;
}

// node_modules/d3-shape/src/array.js
var slice2 = Array.prototype.slice;
function array_default2(x3) {
  return typeof x3 === "object" && "length" in x3 ? x3 : Array.from(x3);
}

// node_modules/d3-shape/src/curve/linear.js
function Linear(context) {
  this._context = context;
}
Linear.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
      default:
        this._context.lineTo(x3, y3);
        break;
    }
  }
};
function linear_default(context) {
  return new Linear(context);
}

// node_modules/d3-shape/src/point.js
function x(p) {
  return p[0];
}
function y(p) {
  return p[1];
}

// node_modules/d3-shape/src/line.js
function line_default(x3, y3) {
  var defined = constant_default5(true), context = null, curve = linear_default, output = null, path2 = withPath(line2);
  x3 = typeof x3 === "function" ? x3 : x3 === void 0 ? x : constant_default5(x3);
  y3 = typeof y3 === "function" ? y3 : y3 === void 0 ? y : constant_default5(y3);
  function line2(data2) {
    var i, n = (data2 = array_default2(data2)).length, d, defined0 = false, buffer;
    if (context == null) output = curve(buffer = path2());
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined(d = data2[i], i, data2)) === defined0) {
        if (defined0 = !defined0) output.lineStart();
        else output.lineEnd();
      }
      if (defined0) output.point(+x3(d, i, data2), +y3(d, i, data2));
    }
    if (buffer) return output = null, buffer + "" || null;
  }
  line2.x = function(_) {
    return arguments.length ? (x3 = typeof _ === "function" ? _ : constant_default5(+_), line2) : x3;
  };
  line2.y = function(_) {
    return arguments.length ? (y3 = typeof _ === "function" ? _ : constant_default5(+_), line2) : y3;
  };
  line2.defined = function(_) {
    return arguments.length ? (defined = typeof _ === "function" ? _ : constant_default5(!!_), line2) : defined;
  };
  line2.curve = function(_) {
    return arguments.length ? (curve = _, context != null && (output = curve(context)), line2) : curve;
  };
  line2.context = function(_) {
    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), line2) : context;
  };
  return line2;
}

// node_modules/d3-shape/src/area.js
function area_default(x0, y0, y1) {
  var x1 = null, defined = constant_default5(true), context = null, curve = linear_default, output = null, path2 = withPath(area2);
  x0 = typeof x0 === "function" ? x0 : x0 === void 0 ? x : constant_default5(+x0);
  y0 = typeof y0 === "function" ? y0 : y0 === void 0 ? constant_default5(0) : constant_default5(+y0);
  y1 = typeof y1 === "function" ? y1 : y1 === void 0 ? y : constant_default5(+y1);
  function area2(data2) {
    var i, j, k2, n = (data2 = array_default2(data2)).length, d, defined0 = false, buffer, x0z = new Array(n), y0z = new Array(n);
    if (context == null) output = curve(buffer = path2());
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined(d = data2[i], i, data2)) === defined0) {
        if (defined0 = !defined0) {
          j = i;
          output.areaStart();
          output.lineStart();
        } else {
          output.lineEnd();
          output.lineStart();
          for (k2 = i - 1; k2 >= j; --k2) {
            output.point(x0z[k2], y0z[k2]);
          }
          output.lineEnd();
          output.areaEnd();
        }
      }
      if (defined0) {
        x0z[i] = +x0(d, i, data2), y0z[i] = +y0(d, i, data2);
        output.point(x1 ? +x1(d, i, data2) : x0z[i], y1 ? +y1(d, i, data2) : y0z[i]);
      }
    }
    if (buffer) return output = null, buffer + "" || null;
  }
  function arealine() {
    return line_default().defined(defined).curve(curve).context(context);
  }
  area2.x = function(_) {
    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant_default5(+_), x1 = null, area2) : x0;
  };
  area2.x0 = function(_) {
    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant_default5(+_), area2) : x0;
  };
  area2.x1 = function(_) {
    return arguments.length ? (x1 = _ == null ? null : typeof _ === "function" ? _ : constant_default5(+_), area2) : x1;
  };
  area2.y = function(_) {
    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant_default5(+_), y1 = null, area2) : y0;
  };
  area2.y0 = function(_) {
    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant_default5(+_), area2) : y0;
  };
  area2.y1 = function(_) {
    return arguments.length ? (y1 = _ == null ? null : typeof _ === "function" ? _ : constant_default5(+_), area2) : y1;
  };
  area2.lineX0 = area2.lineY0 = function() {
    return arealine().x(x0).y(y0);
  };
  area2.lineY1 = function() {
    return arealine().x(x0).y(y1);
  };
  area2.lineX1 = function() {
    return arealine().x(x1).y(y0);
  };
  area2.defined = function(_) {
    return arguments.length ? (defined = typeof _ === "function" ? _ : constant_default5(!!_), area2) : defined;
  };
  area2.curve = function(_) {
    return arguments.length ? (curve = _, context != null && (output = curve(context)), area2) : curve;
  };
  area2.context = function(_) {
    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), area2) : context;
  };
  return area2;
}

// node_modules/d3-shape/src/descending.js
function descending_default(a2, b) {
  return b < a2 ? -1 : b > a2 ? 1 : b >= a2 ? 0 : NaN;
}

// node_modules/d3-shape/src/identity.js
function identity_default2(d) {
  return d;
}

// node_modules/d3-shape/src/pie.js
function pie_default() {
  var value = identity_default2, sortValues = descending_default, sort2 = null, startAngle = constant_default5(0), endAngle = constant_default5(tau2), padAngle = constant_default5(0);
  function pie2(data2) {
    var i, n = (data2 = array_default2(data2)).length, j, k2, sum3 = 0, index2 = new Array(n), arcs = new Array(n), a0 = +startAngle.apply(this, arguments), da = Math.min(tau2, Math.max(-tau2, endAngle.apply(this, arguments) - a0)), a1, p = Math.min(Math.abs(da) / n, padAngle.apply(this, arguments)), pa = p * (da < 0 ? -1 : 1), v;
    for (i = 0; i < n; ++i) {
      if ((v = arcs[index2[i] = i] = +value(data2[i], i, data2)) > 0) {
        sum3 += v;
      }
    }
    if (sortValues != null) index2.sort(function(i2, j2) {
      return sortValues(arcs[i2], arcs[j2]);
    });
    else if (sort2 != null) index2.sort(function(i2, j2) {
      return sort2(data2[i2], data2[j2]);
    });
    for (i = 0, k2 = sum3 ? (da - n * pa) / sum3 : 0; i < n; ++i, a0 = a1) {
      j = index2[i], v = arcs[j], a1 = a0 + (v > 0 ? v * k2 : 0) + pa, arcs[j] = {
        data: data2[j],
        index: i,
        value: v,
        startAngle: a0,
        endAngle: a1,
        padAngle: p
      };
    }
    return arcs;
  }
  pie2.value = function(_) {
    return arguments.length ? (value = typeof _ === "function" ? _ : constant_default5(+_), pie2) : value;
  };
  pie2.sortValues = function(_) {
    return arguments.length ? (sortValues = _, sort2 = null, pie2) : sortValues;
  };
  pie2.sort = function(_) {
    return arguments.length ? (sort2 = _, sortValues = null, pie2) : sort2;
  };
  pie2.startAngle = function(_) {
    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default5(+_), pie2) : startAngle;
  };
  pie2.endAngle = function(_) {
    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default5(+_), pie2) : endAngle;
  };
  pie2.padAngle = function(_) {
    return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default5(+_), pie2) : padAngle;
  };
  return pie2;
}

// node_modules/d3-shape/src/curve/radial.js
var curveRadialLinear = curveRadial(linear_default);
function Radial(curve) {
  this._curve = curve;
}
Radial.prototype = {
  areaStart: function() {
    this._curve.areaStart();
  },
  areaEnd: function() {
    this._curve.areaEnd();
  },
  lineStart: function() {
    this._curve.lineStart();
  },
  lineEnd: function() {
    this._curve.lineEnd();
  },
  point: function(a2, r) {
    this._curve.point(r * Math.sin(a2), r * -Math.cos(a2));
  }
};
function curveRadial(curve) {
  function radial2(context) {
    return new Radial(curve(context));
  }
  radial2._curve = curve;
  return radial2;
}

// node_modules/d3-shape/src/symbol/asterisk.js
var sqrt3 = sqrt2(3);

// node_modules/d3-shape/src/symbol/diamond.js
var tan30 = sqrt2(1 / 3);
var tan30_2 = tan30 * 2;

// node_modules/d3-shape/src/symbol/star.js
var kr = sin(pi2 / 10) / sin(7 * pi2 / 10);
var kx = sin(tau2 / 10) * kr;
var ky = -cos(tau2 / 10) * kr;

// node_modules/d3-shape/src/symbol/triangle.js
var sqrt32 = sqrt2(3);

// node_modules/d3-shape/src/symbol/triangle2.js
var sqrt33 = sqrt2(3);

// node_modules/d3-shape/src/symbol/wye.js
var s = sqrt2(3) / 2;
var k = 1 / sqrt2(12);
var a = (k / 2 + 1) * 3;

// node_modules/d3-shape/src/noop.js
function noop_default() {
}

// node_modules/d3-shape/src/curve/basis.js
function point2(that, x3, y3) {
  that._context.bezierCurveTo(
    (2 * that._x0 + that._x1) / 3,
    (2 * that._y0 + that._y1) / 3,
    (that._x0 + 2 * that._x1) / 3,
    (that._y0 + 2 * that._y1) / 3,
    (that._x0 + 4 * that._x1 + x3) / 6,
    (that._y0 + 4 * that._y1 + y3) / 6
  );
}
function Basis(context) {
  this._context = context;
}
Basis.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        point2(this, this._x1, this._y1);
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      default:
        point2(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = x3;
    this._y0 = this._y1, this._y1 = y3;
  }
};
function basis_default2(context) {
  return new Basis(context);
}

// node_modules/d3-shape/src/curve/basisClosed.js
function BasisClosed(context) {
  this._context = context;
}
BasisClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2);
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x2 = x3, this._y2 = y3;
        break;
      case 1:
        this._point = 2;
        this._x3 = x3, this._y3 = y3;
        break;
      case 2:
        this._point = 3;
        this._x4 = x3, this._y4 = y3;
        this._context.moveTo((this._x0 + 4 * this._x1 + x3) / 6, (this._y0 + 4 * this._y1 + y3) / 6);
        break;
      default:
        point2(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = x3;
    this._y0 = this._y1, this._y1 = y3;
  }
};
function basisClosed_default2(context) {
  return new BasisClosed(context);
}

// node_modules/d3-shape/src/curve/basisOpen.js
function BasisOpen(context) {
  this._context = context;
}
BasisOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var x0 = (this._x0 + 4 * this._x1 + x3) / 6, y0 = (this._y0 + 4 * this._y1 + y3) / 6;
        this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0);
        break;
      case 3:
        this._point = 4;
      default:
        point2(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = x3;
    this._y0 = this._y1, this._y1 = y3;
  }
};
function basisOpen_default(context) {
  return new BasisOpen(context);
}

// node_modules/d3-shape/src/curve/bundle.js
function Bundle(context, beta) {
  this._basis = new Basis(context);
  this._beta = beta;
}
Bundle.prototype = {
  lineStart: function() {
    this._x = [];
    this._y = [];
    this._basis.lineStart();
  },
  lineEnd: function() {
    var x3 = this._x, y3 = this._y, j = x3.length - 1;
    if (j > 0) {
      var x0 = x3[0], y0 = y3[0], dx = x3[j] - x0, dy = y3[j] - y0, i = -1, t;
      while (++i <= j) {
        t = i / j;
        this._basis.point(
          this._beta * x3[i] + (1 - this._beta) * (x0 + t * dx),
          this._beta * y3[i] + (1 - this._beta) * (y0 + t * dy)
        );
      }
    }
    this._x = this._y = null;
    this._basis.lineEnd();
  },
  point: function(x3, y3) {
    this._x.push(+x3);
    this._y.push(+y3);
  }
};
var bundle_default = function custom10(beta) {
  function bundle(context) {
    return beta === 1 ? new Basis(context) : new Bundle(context, beta);
  }
  bundle.beta = function(beta2) {
    return custom10(+beta2);
  };
  return bundle;
}(0.85);

// node_modules/d3-shape/src/curve/cardinal.js
function point3(that, x3, y3) {
  that._context.bezierCurveTo(
    that._x1 + that._k * (that._x2 - that._x0),
    that._y1 + that._k * (that._y2 - that._y0),
    that._x2 + that._k * (that._x1 - x3),
    that._y2 + that._k * (that._y1 - y3),
    that._x2,
    that._y2
  );
}
function Cardinal(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
Cardinal.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        point3(this, this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
        this._x1 = x3, this._y1 = y3;
        break;
      case 2:
        this._point = 3;
      default:
        point3(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var cardinal_default = function custom11(tension) {
  function cardinal(context) {
    return new Cardinal(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom11(+tension2);
  };
  return cardinal;
}(0);

// node_modules/d3-shape/src/curve/cardinalClosed.js
function CardinalClosed(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x3, this._y3 = y3;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x3, this._y4 = y3);
        break;
      case 2:
        this._point = 3;
        this._x5 = x3, this._y5 = y3;
        break;
      default:
        point3(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var cardinalClosed_default = function custom12(tension) {
  function cardinal(context) {
    return new CardinalClosed(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom12(+tension2);
  };
  return cardinal;
}(0);

// node_modules/d3-shape/src/curve/cardinalOpen.js
function CardinalOpen(context, tension) {
  this._context = context;
  this._k = (1 - tension) / 6;
}
CardinalOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        point3(this, x3, y3);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var cardinalOpen_default = function custom13(tension) {
  function cardinal(context) {
    return new CardinalOpen(context, tension);
  }
  cardinal.tension = function(tension2) {
    return custom13(+tension2);
  };
  return cardinal;
}(0);

// node_modules/d3-shape/src/curve/catmullRom.js
function point4(that, x3, y3) {
  var x1 = that._x1, y1 = that._y1, x22 = that._x2, y23 = that._y2;
  if (that._l01_a > epsilon) {
    var a2 = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a, n = 3 * that._l01_a * (that._l01_a + that._l12_a);
    x1 = (x1 * a2 - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n;
    y1 = (y1 * a2 - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n;
  }
  if (that._l23_a > epsilon) {
    var b = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a, m = 3 * that._l23_a * (that._l23_a + that._l12_a);
    x22 = (x22 * b + that._x1 * that._l23_2a - x3 * that._l12_2a) / m;
    y23 = (y23 * b + that._y1 * that._l23_2a - y3 * that._l12_2a) / m;
  }
  that._context.bezierCurveTo(x1, y1, x22, y23, that._x2, that._y2);
}
function CatmullRom(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRom.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        this.point(this._x2, this._y2);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    if (this._point) {
      var x23 = this._x2 - x3, y23 = this._y2 - y3;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
      default:
        point4(this, x3, y3);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var catmullRom_default = function custom14(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRom(context, alpha) : new Cardinal(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom14(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/d3-shape/src/curve/catmullRomClosed.js
function CatmullRomClosed(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    if (this._point) {
      var x23 = this._x2 - x3, y23 = this._y2 - y3;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x3, this._y3 = y3;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x3, this._y4 = y3);
        break;
      case 2:
        this._point = 3;
        this._x5 = x3, this._y5 = y3;
        break;
      default:
        point4(this, x3, y3);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var catmullRomClosed_default = function custom15(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomClosed(context, alpha) : new CardinalClosed(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom15(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/d3-shape/src/curve/catmullRomOpen.js
function CatmullRomOpen(context, alpha) {
  this._context = context;
  this._alpha = alpha;
}
CatmullRomOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    if (this._point) {
      var x23 = this._x2 - x3, y23 = this._y2 - y3;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      default:
        point4(this, x3, y3);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x3;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y3;
  }
};
var catmullRomOpen_default = function custom16(alpha) {
  function catmullRom(context) {
    return alpha ? new CatmullRomOpen(context, alpha) : new CardinalOpen(context, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom16(+alpha2);
  };
  return catmullRom;
}(0.5);

// node_modules/d3-shape/src/curve/linearClosed.js
function LinearClosed(context) {
  this._context = context;
}
LinearClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._point) this._context.closePath();
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    if (this._point) this._context.lineTo(x3, y3);
    else this._point = 1, this._context.moveTo(x3, y3);
  }
};
function linearClosed_default(context) {
  return new LinearClosed(context);
}

// node_modules/d3-shape/src/curve/monotone.js
function sign(x3) {
  return x3 < 0 ? -1 : 1;
}
function slope3(that, x22, y23) {
  var h0 = that._x1 - that._x0, h1 = x22 - that._x1, s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0), s1 = (y23 - that._y1) / (h1 || h0 < 0 && -0), p = (s0 * h1 + s1 * h0) / (h0 + h1);
  return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p)) || 0;
}
function slope2(that, t) {
  var h = that._x1 - that._x0;
  return h ? (3 * (that._y1 - that._y0) / h - t) / 2 : t;
}
function point5(that, t03, t13) {
  var x0 = that._x0, y0 = that._y0, x1 = that._x1, y1 = that._y1, dx = (x1 - x0) / 3;
  that._context.bezierCurveTo(x0 + dx, y0 + dx * t03, x1 - dx, y1 - dx * t13, x1, y1);
}
function MonotoneX(context) {
  this._context = context;
}
MonotoneX.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        point5(this, this._t0, slope2(this, this._t0));
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    var t13 = NaN;
    x3 = +x3, y3 = +y3;
    if (x3 === this._x1 && y3 === this._y1) return;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        point5(this, slope2(this, t13 = slope3(this, x3, y3)), t13);
        break;
      default:
        point5(this, this._t0, t13 = slope3(this, x3, y3));
        break;
    }
    this._x0 = this._x1, this._x1 = x3;
    this._y0 = this._y1, this._y1 = y3;
    this._t0 = t13;
  }
};
function MonotoneY(context) {
  this._context = new ReflectContext(context);
}
(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x3, y3) {
  MonotoneX.prototype.point.call(this, y3, x3);
};
function ReflectContext(context) {
  this._context = context;
}
ReflectContext.prototype = {
  moveTo: function(x3, y3) {
    this._context.moveTo(y3, x3);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(x3, y3) {
    this._context.lineTo(y3, x3);
  },
  bezierCurveTo: function(x1, y1, x22, y23, x3, y3) {
    this._context.bezierCurveTo(y1, x1, y23, x22, y3, x3);
  }
};
function monotoneX(context) {
  return new MonotoneX(context);
}
function monotoneY(context) {
  return new MonotoneY(context);
}

// node_modules/d3-shape/src/curve/natural.js
function Natural(context) {
  this._context = context;
}
Natural.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [];
    this._y = [];
  },
  lineEnd: function() {
    var x3 = this._x, y3 = this._y, n = x3.length;
    if (n) {
      this._line ? this._context.lineTo(x3[0], y3[0]) : this._context.moveTo(x3[0], y3[0]);
      if (n === 2) {
        this._context.lineTo(x3[1], y3[1]);
      } else {
        var px = controlPoints(x3), py = controlPoints(y3);
        for (var i0 = 0, i1 = 1; i1 < n; ++i0, ++i1) {
          this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x3[i1], y3[i1]);
        }
      }
    }
    if (this._line || this._line !== 0 && n === 1) this._context.closePath();
    this._line = 1 - this._line;
    this._x = this._y = null;
  },
  point: function(x3, y3) {
    this._x.push(+x3);
    this._y.push(+y3);
  }
};
function controlPoints(x3) {
  var i, n = x3.length - 1, m, a2 = new Array(n), b = new Array(n), r = new Array(n);
  a2[0] = 0, b[0] = 2, r[0] = x3[0] + 2 * x3[1];
  for (i = 1; i < n - 1; ++i) a2[i] = 1, b[i] = 4, r[i] = 4 * x3[i] + 2 * x3[i + 1];
  a2[n - 1] = 2, b[n - 1] = 7, r[n - 1] = 8 * x3[n - 1] + x3[n];
  for (i = 1; i < n; ++i) m = a2[i] / b[i - 1], b[i] -= m, r[i] -= m * r[i - 1];
  a2[n - 1] = r[n - 1] / b[n - 1];
  for (i = n - 2; i >= 0; --i) a2[i] = (r[i] - a2[i + 1]) / b[i];
  b[n - 1] = (x3[n] + a2[n - 1]) / 2;
  for (i = 0; i < n - 1; ++i) b[i] = 2 * x3[i + 1] - a2[i + 1];
  return [a2, b];
}
function natural_default(context) {
  return new Natural(context);
}

// node_modules/d3-shape/src/curve/step.js
function Step(context, t) {
  this._context = context;
  this._t = t;
}
Step.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
  },
  point: function(x3, y3) {
    x3 = +x3, y3 = +y3;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x3, y3) : this._context.moveTo(x3, y3);
        break;
      case 1:
        this._point = 2;
      default: {
        if (this._t <= 0) {
          this._context.lineTo(this._x, y3);
          this._context.lineTo(x3, y3);
        } else {
          var x1 = this._x * (1 - this._t) + x3 * this._t;
          this._context.lineTo(x1, this._y);
          this._context.lineTo(x1, y3);
        }
        break;
      }
    }
    this._x = x3, this._y = y3;
  }
};
function step_default(context) {
  return new Step(context, 0.5);
}
function stepBefore(context) {
  return new Step(context, 0);
}
function stepAfter(context) {
  return new Step(context, 1);
}

// node_modules/d3-axis/src/identity.js
function identity_default3(x3) {
  return x3;
}

// node_modules/d3-axis/src/axis.js
var top = 1;
var right = 2;
var bottom = 3;
var left = 4;
var epsilon4 = 1e-6;
function translateX(x3) {
  return "translate(" + x3 + ",0)";
}
function translateY(y3) {
  return "translate(0," + y3 + ")";
}
function number5(scale2) {
  return (d) => +scale2(d);
}
function center(scale2, offset) {
  offset = Math.max(0, scale2.bandwidth() - offset * 2) / 2;
  if (scale2.round()) offset = Math.round(offset);
  return (d) => +scale2(d) + offset;
}
function entering() {
  return !this.__axis;
}
function axis(orient, scale2) {
  var tickArguments = [], tickValues = null, tickFormat2 = null, tickSizeInner = 6, tickSizeOuter = 6, tickPadding = 3, offset = typeof window !== "undefined" && window.devicePixelRatio > 1 ? 0 : 0.5, k2 = orient === top || orient === left ? -1 : 1, x3 = orient === left || orient === right ? "x" : "y", transform3 = orient === top || orient === bottom ? translateX : translateY;
  function axis3(context) {
    var values = tickValues == null ? scale2.ticks ? scale2.ticks.apply(scale2, tickArguments) : scale2.domain() : tickValues, format3 = tickFormat2 == null ? scale2.tickFormat ? scale2.tickFormat.apply(scale2, tickArguments) : identity_default3 : tickFormat2, spacing = Math.max(tickSizeInner, 0) + tickPadding, range2 = scale2.range(), range0 = +range2[0] + offset, range1 = +range2[range2.length - 1] + offset, position2 = (scale2.bandwidth ? center : number5)(scale2.copy(), offset), selection3 = context.selection ? context.selection() : context, path2 = selection3.selectAll(".domain").data([null]), tick = selection3.selectAll(".tick").data(values, scale2).order(), tickExit = tick.exit(), tickEnter = tick.enter().append("g").attr("class", "tick"), line2 = tick.select("line"), text2 = tick.select("text");
    path2 = path2.merge(path2.enter().insert("path", ".tick").attr("class", "domain").attr("stroke", "currentColor"));
    tick = tick.merge(tickEnter);
    line2 = line2.merge(tickEnter.append("line").attr("stroke", "currentColor").attr(x3 + "2", k2 * tickSizeInner));
    text2 = text2.merge(tickEnter.append("text").attr("fill", "currentColor").attr(x3, k2 * spacing).attr("dy", orient === top ? "0em" : orient === bottom ? "0.71em" : "0.32em"));
    if (context !== selection3) {
      path2 = path2.transition(context);
      tick = tick.transition(context);
      line2 = line2.transition(context);
      text2 = text2.transition(context);
      tickExit = tickExit.transition(context).attr("opacity", epsilon4).attr("transform", function(d) {
        return isFinite(d = position2(d)) ? transform3(d + offset) : this.getAttribute("transform");
      });
      tickEnter.attr("opacity", epsilon4).attr("transform", function(d) {
        var p = this.parentNode.__axis;
        return transform3((p && isFinite(p = p(d)) ? p : position2(d)) + offset);
      });
    }
    tickExit.remove();
    path2.attr("d", orient === left || orient === right ? tickSizeOuter ? "M" + k2 * tickSizeOuter + "," + range0 + "H" + offset + "V" + range1 + "H" + k2 * tickSizeOuter : "M" + offset + "," + range0 + "V" + range1 : tickSizeOuter ? "M" + range0 + "," + k2 * tickSizeOuter + "V" + offset + "H" + range1 + "V" + k2 * tickSizeOuter : "M" + range0 + "," + offset + "H" + range1);
    tick.attr("opacity", 1).attr("transform", function(d) {
      return transform3(position2(d) + offset);
    });
    line2.attr(x3 + "2", k2 * tickSizeInner);
    text2.attr(x3, k2 * spacing).text(format3);
    selection3.filter(entering).attr("fill", "none").attr("font-size", 10).attr("font-family", "sans-serif").attr("text-anchor", orient === right ? "start" : orient === left ? "end" : "middle");
    selection3.each(function() {
      this.__axis = position2;
    });
  }
  axis3.scale = function(_) {
    return arguments.length ? (scale2 = _, axis3) : scale2;
  };
  axis3.ticks = function() {
    return tickArguments = Array.from(arguments), axis3;
  };
  axis3.tickArguments = function(_) {
    return arguments.length ? (tickArguments = _ == null ? [] : Array.from(_), axis3) : tickArguments.slice();
  };
  axis3.tickValues = function(_) {
    return arguments.length ? (tickValues = _ == null ? null : Array.from(_), axis3) : tickValues && tickValues.slice();
  };
  axis3.tickFormat = function(_) {
    return arguments.length ? (tickFormat2 = _, axis3) : tickFormat2;
  };
  axis3.tickSize = function(_) {
    return arguments.length ? (tickSizeInner = tickSizeOuter = +_, axis3) : tickSizeInner;
  };
  axis3.tickSizeInner = function(_) {
    return arguments.length ? (tickSizeInner = +_, axis3) : tickSizeInner;
  };
  axis3.tickSizeOuter = function(_) {
    return arguments.length ? (tickSizeOuter = +_, axis3) : tickSizeOuter;
  };
  axis3.tickPadding = function(_) {
    return arguments.length ? (tickPadding = +_, axis3) : tickPadding;
  };
  axis3.offset = function(_) {
    return arguments.length ? (offset = +_, axis3) : offset;
  };
  return axis3;
}
function axisTop(scale2) {
  return axis(top, scale2);
}
function axisRight(scale2) {
  return axis(right, scale2);
}
function axisBottom(scale2) {
  return axis(bottom, scale2);
}
function axisLeft(scale2) {
  return axis(left, scale2);
}

// node_modules/d3-hierarchy/src/hierarchy/count.js
function count2(node) {
  var sum3 = 0, children2 = node.children, i = children2 && children2.length;
  if (!i) sum3 = 1;
  else while (--i >= 0) sum3 += children2[i].value;
  node.value = sum3;
}
function count_default() {
  return this.eachAfter(count2);
}

// node_modules/d3-hierarchy/src/hierarchy/each.js
function each_default2(callback, that) {
  let index2 = -1;
  for (const node of this) {
    callback.call(that, node, ++index2, this);
  }
  return this;
}

// node_modules/d3-hierarchy/src/hierarchy/eachBefore.js
function eachBefore_default(callback, that) {
  var node = this, nodes = [node], children2, i, index2 = -1;
  while (node = nodes.pop()) {
    callback.call(that, node, ++index2, this);
    if (children2 = node.children) {
      for (i = children2.length - 1; i >= 0; --i) {
        nodes.push(children2[i]);
      }
    }
  }
  return this;
}

// node_modules/d3-hierarchy/src/hierarchy/eachAfter.js
function eachAfter_default(callback, that) {
  var node = this, nodes = [node], next = [], children2, i, n, index2 = -1;
  while (node = nodes.pop()) {
    next.push(node);
    if (children2 = node.children) {
      for (i = 0, n = children2.length; i < n; ++i) {
        nodes.push(children2[i]);
      }
    }
  }
  while (node = next.pop()) {
    callback.call(that, node, ++index2, this);
  }
  return this;
}

// node_modules/d3-hierarchy/src/hierarchy/find.js
function find_default(callback, that) {
  let index2 = -1;
  for (const node of this) {
    if (callback.call(that, node, ++index2, this)) {
      return node;
    }
  }
}

// node_modules/d3-hierarchy/src/hierarchy/sum.js
function sum_default(value) {
  return this.eachAfter(function(node) {
    var sum3 = +value(node.data) || 0, children2 = node.children, i = children2 && children2.length;
    while (--i >= 0) sum3 += children2[i].value;
    node.value = sum3;
  });
}

// node_modules/d3-hierarchy/src/hierarchy/sort.js
function sort_default2(compare) {
  return this.eachBefore(function(node) {
    if (node.children) {
      node.children.sort(compare);
    }
  });
}

// node_modules/d3-hierarchy/src/hierarchy/path.js
function path_default(end) {
  var start2 = this, ancestor = leastCommonAncestor(start2, end), nodes = [start2];
  while (start2 !== ancestor) {
    start2 = start2.parent;
    nodes.push(start2);
  }
  var k2 = nodes.length;
  while (end !== ancestor) {
    nodes.splice(k2, 0, end);
    end = end.parent;
  }
  return nodes;
}
function leastCommonAncestor(a2, b) {
  if (a2 === b) return a2;
  var aNodes = a2.ancestors(), bNodes = b.ancestors(), c = null;
  a2 = aNodes.pop();
  b = bNodes.pop();
  while (a2 === b) {
    c = a2;
    a2 = aNodes.pop();
    b = bNodes.pop();
  }
  return c;
}

// node_modules/d3-hierarchy/src/hierarchy/ancestors.js
function ancestors_default() {
  var node = this, nodes = [node];
  while (node = node.parent) {
    nodes.push(node);
  }
  return nodes;
}

// node_modules/d3-hierarchy/src/hierarchy/descendants.js
function descendants_default() {
  return Array.from(this);
}

// node_modules/d3-hierarchy/src/hierarchy/leaves.js
function leaves_default() {
  var leaves = [];
  this.eachBefore(function(node) {
    if (!node.children) {
      leaves.push(node);
    }
  });
  return leaves;
}

// node_modules/d3-hierarchy/src/hierarchy/links.js
function links_default() {
  var root2 = this, links = [];
  root2.each(function(node) {
    if (node !== root2) {
      links.push({ source: node.parent, target: node });
    }
  });
  return links;
}

// node_modules/d3-hierarchy/src/hierarchy/iterator.js
function* iterator_default2() {
  var node = this, current, next = [node], children2, i, n;
  do {
    current = next.reverse(), next = [];
    while (node = current.pop()) {
      yield node;
      if (children2 = node.children) {
        for (i = 0, n = children2.length; i < n; ++i) {
          next.push(children2[i]);
        }
      }
    }
  } while (next.length);
}

// node_modules/d3-hierarchy/src/hierarchy/index.js
function hierarchy(data2, children2) {
  if (data2 instanceof Map) {
    data2 = [void 0, data2];
    if (children2 === void 0) children2 = mapChildren;
  } else if (children2 === void 0) {
    children2 = objectChildren;
  }
  var root2 = new Node(data2), node, nodes = [root2], child, childs, i, n;
  while (node = nodes.pop()) {
    if ((childs = children2(node.data)) && (n = (childs = Array.from(childs)).length)) {
      node.children = childs;
      for (i = n - 1; i >= 0; --i) {
        nodes.push(child = childs[i] = new Node(childs[i]));
        child.parent = node;
        child.depth = node.depth + 1;
      }
    }
  }
  return root2.eachBefore(computeHeight);
}
function node_copy() {
  return hierarchy(this).eachBefore(copyData);
}
function objectChildren(d) {
  return d.children;
}
function mapChildren(d) {
  return Array.isArray(d) ? d[1] : null;
}
function copyData(node) {
  if (node.data.value !== void 0) node.value = node.data.value;
  node.data = node.data.data;
}
function computeHeight(node) {
  var height = 0;
  do
    node.height = height;
  while ((node = node.parent) && node.height < ++height);
}
function Node(data2) {
  this.data = data2;
  this.depth = this.height = 0;
  this.parent = null;
}
Node.prototype = hierarchy.prototype = {
  constructor: Node,
  count: count_default,
  each: each_default2,
  eachAfter: eachAfter_default,
  eachBefore: eachBefore_default,
  find: find_default,
  sum: sum_default,
  sort: sort_default2,
  path: path_default,
  ancestors: ancestors_default,
  descendants: descendants_default,
  leaves: leaves_default,
  links: links_default,
  copy: node_copy,
  [Symbol.iterator]: iterator_default2
};

// node_modules/d3-hierarchy/src/accessors.js
function required(f) {
  if (typeof f !== "function") throw new Error();
  return f;
}

// node_modules/d3-hierarchy/src/constant.js
function constantZero() {
  return 0;
}
function constant_default6(x3) {
  return function() {
    return x3;
  };
}

// node_modules/d3-hierarchy/src/treemap/round.js
function round_default2(node) {
  node.x0 = Math.round(node.x0);
  node.y0 = Math.round(node.y0);
  node.x1 = Math.round(node.x1);
  node.y1 = Math.round(node.y1);
}

// node_modules/d3-hierarchy/src/treemap/dice.js
function dice_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, node, i = -1, n = nodes.length, k2 = parent.value && (x1 - x0) / parent.value;
  while (++i < n) {
    node = nodes[i], node.y0 = y0, node.y1 = y1;
    node.x0 = x0, node.x1 = x0 += node.value * k2;
  }
}

// node_modules/d3-hierarchy/src/tree.js
function TreeNode(node, i) {
  this._ = node;
  this.parent = null;
  this.children = null;
  this.A = null;
  this.a = this;
  this.z = 0;
  this.m = 0;
  this.c = 0;
  this.s = 0;
  this.t = null;
  this.i = i;
}
TreeNode.prototype = Object.create(Node.prototype);

// node_modules/d3-hierarchy/src/treemap/slice.js
function slice_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, node, i = -1, n = nodes.length, k2 = parent.value && (y1 - y0) / parent.value;
  while (++i < n) {
    node = nodes[i], node.x0 = x0, node.x1 = x1;
    node.y0 = y0, node.y1 = y0 += node.value * k2;
  }
}

// node_modules/d3-hierarchy/src/treemap/squarify.js
var phi = (1 + Math.sqrt(5)) / 2;
function squarifyRatio(ratio, parent, x0, y0, x1, y1) {
  var rows2 = [], nodes = parent.children, row, nodeValue, i0 = 0, i1 = 0, n = nodes.length, dx, dy, value = parent.value, sumValue, minValue, maxValue, newRatio, minRatio, alpha, beta;
  while (i0 < n) {
    dx = x1 - x0, dy = y1 - y0;
    do
      sumValue = nodes[i1++].value;
    while (!sumValue && i1 < n);
    minValue = maxValue = sumValue;
    alpha = Math.max(dy / dx, dx / dy) / (value * ratio);
    beta = sumValue * sumValue * alpha;
    minRatio = Math.max(maxValue / beta, beta / minValue);
    for (; i1 < n; ++i1) {
      sumValue += nodeValue = nodes[i1].value;
      if (nodeValue < minValue) minValue = nodeValue;
      if (nodeValue > maxValue) maxValue = nodeValue;
      beta = sumValue * sumValue * alpha;
      newRatio = Math.max(maxValue / beta, beta / minValue);
      if (newRatio > minRatio) {
        sumValue -= nodeValue;
        break;
      }
      minRatio = newRatio;
    }
    rows2.push(row = { value: sumValue, dice: dx < dy, children: nodes.slice(i0, i1) });
    if (row.dice) dice_default(row, x0, y0, x1, value ? y0 += dy * sumValue / value : y1);
    else slice_default(row, x0, y0, value ? x0 += dx * sumValue / value : x1, y1);
    value -= sumValue, i0 = i1;
  }
  return rows2;
}
var squarify_default = function custom17(ratio) {
  function squarify(parent, x0, y0, x1, y1) {
    squarifyRatio(ratio, parent, x0, y0, x1, y1);
  }
  squarify.ratio = function(x3) {
    return custom17((x3 = +x3) > 1 ? x3 : 1);
  };
  return squarify;
}(phi);

// node_modules/d3-hierarchy/src/treemap/index.js
function treemap_default() {
  var tile = squarify_default, round = false, dx = 1, dy = 1, paddingStack = [0], paddingInner = constantZero, paddingTop = constantZero, paddingRight = constantZero, paddingBottom = constantZero, paddingLeft = constantZero;
  function treemap2(root2) {
    root2.x0 = root2.y0 = 0;
    root2.x1 = dx;
    root2.y1 = dy;
    root2.eachBefore(positionNode);
    paddingStack = [0];
    if (round) root2.eachBefore(round_default2);
    return root2;
  }
  function positionNode(node) {
    var p = paddingStack[node.depth], x0 = node.x0 + p, y0 = node.y0 + p, x1 = node.x1 - p, y1 = node.y1 - p;
    if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
    if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
    node.x0 = x0;
    node.y0 = y0;
    node.x1 = x1;
    node.y1 = y1;
    if (node.children) {
      p = paddingStack[node.depth + 1] = paddingInner(node) / 2;
      x0 += paddingLeft(node) - p;
      y0 += paddingTop(node) - p;
      x1 -= paddingRight(node) - p;
      y1 -= paddingBottom(node) - p;
      if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
      if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
      tile(node, x0, y0, x1, y1);
    }
  }
  treemap2.round = function(x3) {
    return arguments.length ? (round = !!x3, treemap2) : round;
  };
  treemap2.size = function(x3) {
    return arguments.length ? (dx = +x3[0], dy = +x3[1], treemap2) : [dx, dy];
  };
  treemap2.tile = function(x3) {
    return arguments.length ? (tile = required(x3), treemap2) : tile;
  };
  treemap2.padding = function(x3) {
    return arguments.length ? treemap2.paddingInner(x3).paddingOuter(x3) : treemap2.paddingInner();
  };
  treemap2.paddingInner = function(x3) {
    return arguments.length ? (paddingInner = typeof x3 === "function" ? x3 : constant_default6(+x3), treemap2) : paddingInner;
  };
  treemap2.paddingOuter = function(x3) {
    return arguments.length ? treemap2.paddingTop(x3).paddingRight(x3).paddingBottom(x3).paddingLeft(x3) : treemap2.paddingTop();
  };
  treemap2.paddingTop = function(x3) {
    return arguments.length ? (paddingTop = typeof x3 === "function" ? x3 : constant_default6(+x3), treemap2) : paddingTop;
  };
  treemap2.paddingRight = function(x3) {
    return arguments.length ? (paddingRight = typeof x3 === "function" ? x3 : constant_default6(+x3), treemap2) : paddingRight;
  };
  treemap2.paddingBottom = function(x3) {
    return arguments.length ? (paddingBottom = typeof x3 === "function" ? x3 : constant_default6(+x3), treemap2) : paddingBottom;
  };
  treemap2.paddingLeft = function(x3) {
    return arguments.length ? (paddingLeft = typeof x3 === "function" ? x3 : constant_default6(+x3), treemap2) : paddingLeft;
  };
  return treemap2;
}

// node_modules/d3-hierarchy/src/treemap/binary.js
function binary_default(parent, x0, y0, x1, y1) {
  var nodes = parent.children, i, n = nodes.length, sum3, sums = new Array(n + 1);
  for (sums[0] = sum3 = i = 0; i < n; ++i) {
    sums[i + 1] = sum3 += nodes[i].value;
  }
  partition(0, n, parent.value, x0, y0, x1, y1);
  function partition(i2, j, value, x02, y02, x12, y12) {
    if (i2 >= j - 1) {
      var node = nodes[i2];
      node.x0 = x02, node.y0 = y02;
      node.x1 = x12, node.y1 = y12;
      return;
    }
    var valueOffset = sums[i2], valueTarget = value / 2 + valueOffset, k2 = i2 + 1, hi = j - 1;
    while (k2 < hi) {
      var mid = k2 + hi >>> 1;
      if (sums[mid] < valueTarget) k2 = mid + 1;
      else hi = mid;
    }
    if (valueTarget - sums[k2 - 1] < sums[k2] - valueTarget && i2 + 1 < k2) --k2;
    var valueLeft = sums[k2] - valueOffset, valueRight = value - valueLeft;
    if (x12 - x02 > y12 - y02) {
      var xk = value ? (x02 * valueRight + x12 * valueLeft) / value : x12;
      partition(i2, k2, valueLeft, x02, y02, xk, y12);
      partition(k2, j, valueRight, xk, y02, x12, y12);
    } else {
      var yk = value ? (y02 * valueRight + y12 * valueLeft) / value : y12;
      partition(i2, k2, valueLeft, x02, y02, x12, yk);
      partition(k2, j, valueRight, x02, yk, x12, y12);
    }
  }
}

// node_modules/d3-hierarchy/src/treemap/sliceDice.js
function sliceDice_default(parent, x0, y0, x1, y1) {
  (parent.depth & 1 ? slice_default : dice_default)(parent, x0, y0, x1, y1);
}

// node_modules/d3-hierarchy/src/treemap/resquarify.js
var resquarify_default = function custom18(ratio) {
  function resquarify(parent, x0, y0, x1, y1) {
    if ((rows2 = parent._squarify) && rows2.ratio === ratio) {
      var rows2, row, nodes, i, j = -1, n, m = rows2.length, value = parent.value;
      while (++j < m) {
        row = rows2[j], nodes = row.children;
        for (i = row.value = 0, n = nodes.length; i < n; ++i) row.value += nodes[i].value;
        if (row.dice) dice_default(row, x0, y0, x1, value ? y0 += (y1 - y0) * row.value / value : y1);
        else slice_default(row, x0, y0, value ? x0 += (x1 - x0) * row.value / value : x1, y1);
        value -= row.value;
      }
    } else {
      parent._squarify = rows2 = squarifyRatio(ratio, parent, x0, y0, x1, y1);
      rows2.ratio = ratio;
    }
  }
  resquarify.ratio = function(x3) {
    return custom18((x3 = +x3) > 1 ? x3 : 1);
  };
  return resquarify;
}(phi);

// node_modules/d3-zoom/src/constant.js
var constant_default7 = (x3) => () => x3;

// node_modules/d3-zoom/src/event.js
function ZoomEvent(type2, {
  sourceEvent,
  target,
  transform: transform3,
  dispatch: dispatch2
}) {
  Object.defineProperties(this, {
    type: { value: type2, enumerable: true, configurable: true },
    sourceEvent: { value: sourceEvent, enumerable: true, configurable: true },
    target: { value: target, enumerable: true, configurable: true },
    transform: { value: transform3, enumerable: true, configurable: true },
    _: { value: dispatch2 }
  });
}

// node_modules/d3-zoom/src/transform.js
function Transform(k2, x3, y3) {
  this.k = k2;
  this.x = x3;
  this.y = y3;
}
Transform.prototype = {
  constructor: Transform,
  scale: function(k2) {
    return k2 === 1 ? this : new Transform(this.k * k2, this.x, this.y);
  },
  translate: function(x3, y3) {
    return x3 === 0 & y3 === 0 ? this : new Transform(this.k, this.x + this.k * x3, this.y + this.k * y3);
  },
  apply: function(point6) {
    return [point6[0] * this.k + this.x, point6[1] * this.k + this.y];
  },
  applyX: function(x3) {
    return x3 * this.k + this.x;
  },
  applyY: function(y3) {
    return y3 * this.k + this.y;
  },
  invert: function(location) {
    return [(location[0] - this.x) / this.k, (location[1] - this.y) / this.k];
  },
  invertX: function(x3) {
    return (x3 - this.x) / this.k;
  },
  invertY: function(y3) {
    return (y3 - this.y) / this.k;
  },
  rescaleX: function(x3) {
    return x3.copy().domain(x3.range().map(this.invertX, this).map(x3.invert, x3));
  },
  rescaleY: function(y3) {
    return y3.copy().domain(y3.range().map(this.invertY, this).map(y3.invert, y3));
  },
  toString: function() {
    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
  }
};
var identity5 = new Transform(1, 0, 0);
transform.prototype = Transform.prototype;
function transform(node) {
  while (!node.__zoom) if (!(node = node.parentNode)) return identity5;
  return node.__zoom;
}

// node_modules/d3-zoom/src/noevent.js
function nopropagation3(event) {
  event.stopImmediatePropagation();
}
function noevent_default3(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}

// node_modules/d3-zoom/src/zoom.js
function defaultFilter3(event) {
  return (!event.ctrlKey || event.type === "wheel") && !event.button;
}
function defaultExtent2() {
  var e = this;
  if (e instanceof SVGElement) {
    e = e.ownerSVGElement || e;
    if (e.hasAttribute("viewBox")) {
      e = e.viewBox.baseVal;
      return [[e.x, e.y], [e.x + e.width, e.y + e.height]];
    }
    return [[0, 0], [e.width.baseVal.value, e.height.baseVal.value]];
  }
  return [[0, 0], [e.clientWidth, e.clientHeight]];
}
function defaultTransform() {
  return this.__zoom || identity5;
}
function defaultWheelDelta(event) {
  return -event.deltaY * (event.deltaMode === 1 ? 0.05 : event.deltaMode ? 1 : 2e-3) * (event.ctrlKey ? 10 : 1);
}
function defaultTouchable3() {
  return navigator.maxTouchPoints || "ontouchstart" in this;
}
function defaultConstrain(transform3, extent2, translateExtent) {
  var dx0 = transform3.invertX(extent2[0][0]) - translateExtent[0][0], dx1 = transform3.invertX(extent2[1][0]) - translateExtent[1][0], dy0 = transform3.invertY(extent2[0][1]) - translateExtent[0][1], dy1 = transform3.invertY(extent2[1][1]) - translateExtent[1][1];
  return transform3.translate(
    dx1 > dx0 ? (dx0 + dx1) / 2 : Math.min(0, dx0) || Math.max(0, dx1),
    dy1 > dy0 ? (dy0 + dy1) / 2 : Math.min(0, dy0) || Math.max(0, dy1)
  );
}
function zoom_default2() {
  var filter3 = defaultFilter3, extent2 = defaultExtent2, constrain = defaultConstrain, wheelDelta = defaultWheelDelta, touchable = defaultTouchable3, scaleExtent = [0, Infinity], translateExtent = [[-Infinity, -Infinity], [Infinity, Infinity]], duration = 250, interpolate = zoom_default, listeners = dispatch_default2("start", "zoom", "end"), touchstarting, touchfirst, touchending, touchDelay = 500, wheelDelay = 150, clickDistance2 = 0, tapDistance = 10;
  function zoom2(selection3) {
    selection3.property("__zoom", defaultTransform).on("wheel.zoom", wheeled, { passive: false }).on("mousedown.zoom", mousedowned).on("dblclick.zoom", dblclicked).filter(touchable).on("touchstart.zoom", touchstarted).on("touchmove.zoom", touchmoved).on("touchend.zoom touchcancel.zoom", touchended).style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
  }
  zoom2.transform = function(collection, transform3, point6, event) {
    var selection3 = collection.selection ? collection.selection() : collection;
    selection3.property("__zoom", defaultTransform);
    if (collection !== selection3) {
      schedule(collection, transform3, point6, event);
    } else {
      selection3.interrupt().each(function() {
        gesture(this, arguments).event(event).start().zoom(null, typeof transform3 === "function" ? transform3.apply(this, arguments) : transform3).end();
      });
    }
  };
  zoom2.scaleBy = function(selection3, k2, p, event) {
    zoom2.scaleTo(selection3, function() {
      var k0 = this.__zoom.k, k1 = typeof k2 === "function" ? k2.apply(this, arguments) : k2;
      return k0 * k1;
    }, p, event);
  };
  zoom2.scaleTo = function(selection3, k2, p, event) {
    zoom2.transform(selection3, function() {
      var e = extent2.apply(this, arguments), t03 = this.__zoom, p0 = p == null ? centroid(e) : typeof p === "function" ? p.apply(this, arguments) : p, p1 = t03.invert(p0), k1 = typeof k2 === "function" ? k2.apply(this, arguments) : k2;
      return constrain(translate(scale2(t03, k1), p0, p1), e, translateExtent);
    }, p, event);
  };
  zoom2.translateBy = function(selection3, x3, y3, event) {
    zoom2.transform(selection3, function() {
      return constrain(this.__zoom.translate(
        typeof x3 === "function" ? x3.apply(this, arguments) : x3,
        typeof y3 === "function" ? y3.apply(this, arguments) : y3
      ), extent2.apply(this, arguments), translateExtent);
    }, null, event);
  };
  zoom2.translateTo = function(selection3, x3, y3, p, event) {
    zoom2.transform(selection3, function() {
      var e = extent2.apply(this, arguments), t = this.__zoom, p0 = p == null ? centroid(e) : typeof p === "function" ? p.apply(this, arguments) : p;
      return constrain(identity5.translate(p0[0], p0[1]).scale(t.k).translate(
        typeof x3 === "function" ? -x3.apply(this, arguments) : -x3,
        typeof y3 === "function" ? -y3.apply(this, arguments) : -y3
      ), e, translateExtent);
    }, p, event);
  };
  function scale2(transform3, k2) {
    k2 = Math.max(scaleExtent[0], Math.min(scaleExtent[1], k2));
    return k2 === transform3.k ? transform3 : new Transform(k2, transform3.x, transform3.y);
  }
  function translate(transform3, p0, p1) {
    var x3 = p0[0] - p1[0] * transform3.k, y3 = p0[1] - p1[1] * transform3.k;
    return x3 === transform3.x && y3 === transform3.y ? transform3 : new Transform(transform3.k, x3, y3);
  }
  function centroid(extent3) {
    return [(+extent3[0][0] + +extent3[1][0]) / 2, (+extent3[0][1] + +extent3[1][1]) / 2];
  }
  function schedule(transition2, transform3, point6, event) {
    transition2.on("start.zoom", function() {
      gesture(this, arguments).event(event).start();
    }).on("interrupt.zoom end.zoom", function() {
      gesture(this, arguments).event(event).end();
    }).tween("zoom", function() {
      var that = this, args = arguments, g = gesture(that, args).event(event), e = extent2.apply(that, args), p = point6 == null ? centroid(e) : typeof point6 === "function" ? point6.apply(that, args) : point6, w = Math.max(e[1][0] - e[0][0], e[1][1] - e[0][1]), a2 = that.__zoom, b = typeof transform3 === "function" ? transform3.apply(that, args) : transform3, i = interpolate(a2.invert(p).concat(w / a2.k), b.invert(p).concat(w / b.k));
      return function(t) {
        if (t === 1) t = b;
        else {
          var l = i(t), k2 = w / l[2];
          t = new Transform(k2, p[0] - l[0] * k2, p[1] - l[1] * k2);
        }
        g.zoom(null, t);
      };
    });
  }
  function gesture(that, args, clean) {
    return !clean && that.__zooming || new Gesture(that, args);
  }
  function Gesture(that, args) {
    this.that = that;
    this.args = args;
    this.active = 0;
    this.sourceEvent = null;
    this.extent = extent2.apply(that, args);
    this.taps = 0;
  }
  Gesture.prototype = {
    event: function(event) {
      if (event) this.sourceEvent = event;
      return this;
    },
    start: function() {
      if (++this.active === 1) {
        this.that.__zooming = this;
        this.emit("start");
      }
      return this;
    },
    zoom: function(key, transform3) {
      if (this.mouse && key !== "mouse") this.mouse[1] = transform3.invert(this.mouse[0]);
      if (this.touch0 && key !== "touch") this.touch0[1] = transform3.invert(this.touch0[0]);
      if (this.touch1 && key !== "touch") this.touch1[1] = transform3.invert(this.touch1[0]);
      this.that.__zoom = transform3;
      this.emit("zoom");
      return this;
    },
    end: function() {
      if (--this.active === 0) {
        delete this.that.__zooming;
        this.emit("end");
      }
      return this;
    },
    emit: function(type2) {
      var d = select_default2(this.that).datum();
      listeners.call(
        type2,
        this.that,
        new ZoomEvent(type2, {
          sourceEvent: this.sourceEvent,
          target: zoom2,
          type: type2,
          transform: this.that.__zoom,
          dispatch: listeners
        }),
        d
      );
    }
  };
  function wheeled(event, ...args) {
    if (!filter3.apply(this, arguments)) return;
    var g = gesture(this, args).event(event), t = this.__zoom, k2 = Math.max(scaleExtent[0], Math.min(scaleExtent[1], t.k * Math.pow(2, wheelDelta.apply(this, arguments)))), p = pointer_default(event);
    if (g.wheel) {
      if (g.mouse[0][0] !== p[0] || g.mouse[0][1] !== p[1]) {
        g.mouse[1] = t.invert(g.mouse[0] = p);
      }
      clearTimeout(g.wheel);
    } else if (t.k === k2) return;
    else {
      g.mouse = [p, t.invert(p)];
      interrupt_default(this);
      g.start();
    }
    noevent_default3(event);
    g.wheel = setTimeout(wheelidled, wheelDelay);
    g.zoom("mouse", constrain(translate(scale2(t, k2), g.mouse[0], g.mouse[1]), g.extent, translateExtent));
    function wheelidled() {
      g.wheel = null;
      g.end();
    }
  }
  function mousedowned(event, ...args) {
    if (touchending || !filter3.apply(this, arguments)) return;
    var currentTarget = event.currentTarget, g = gesture(this, args, true).event(event), v = select_default2(event.view).on("mousemove.zoom", mousemoved, true).on("mouseup.zoom", mouseupped, true), p = pointer_default(event, currentTarget), x0 = event.clientX, y0 = event.clientY;
    nodrag_default(event.view);
    nopropagation3(event);
    g.mouse = [p, this.__zoom.invert(p)];
    interrupt_default(this);
    g.start();
    function mousemoved(event2) {
      noevent_default3(event2);
      if (!g.moved) {
        var dx = event2.clientX - x0, dy = event2.clientY - y0;
        g.moved = dx * dx + dy * dy > clickDistance2;
      }
      g.event(event2).zoom("mouse", constrain(translate(g.that.__zoom, g.mouse[0] = pointer_default(event2, currentTarget), g.mouse[1]), g.extent, translateExtent));
    }
    function mouseupped(event2) {
      v.on("mousemove.zoom mouseup.zoom", null);
      yesdrag(event2.view, g.moved);
      noevent_default3(event2);
      g.event(event2).end();
    }
  }
  function dblclicked(event, ...args) {
    if (!filter3.apply(this, arguments)) return;
    var t03 = this.__zoom, p0 = pointer_default(event.changedTouches ? event.changedTouches[0] : event, this), p1 = t03.invert(p0), k1 = t03.k * (event.shiftKey ? 0.5 : 2), t13 = constrain(translate(scale2(t03, k1), p0, p1), extent2.apply(this, args), translateExtent);
    noevent_default3(event);
    if (duration > 0) select_default2(this).transition().duration(duration).call(schedule, t13, p0, event);
    else select_default2(this).call(zoom2.transform, t13, p0, event);
  }
  function touchstarted(event, ...args) {
    if (!filter3.apply(this, arguments)) return;
    var touches = event.touches, n = touches.length, g = gesture(this, args, event.changedTouches.length === n).event(event), started, i, t, p;
    nopropagation3(event);
    for (i = 0; i < n; ++i) {
      t = touches[i], p = pointer_default(t, this);
      p = [p, this.__zoom.invert(p), t.identifier];
      if (!g.touch0) g.touch0 = p, started = true, g.taps = 1 + !!touchstarting;
      else if (!g.touch1 && g.touch0[2] !== p[2]) g.touch1 = p, g.taps = 0;
    }
    if (touchstarting) touchstarting = clearTimeout(touchstarting);
    if (started) {
      if (g.taps < 2) touchfirst = p[0], touchstarting = setTimeout(function() {
        touchstarting = null;
      }, touchDelay);
      interrupt_default(this);
      g.start();
    }
  }
  function touchmoved(event, ...args) {
    if (!this.__zooming) return;
    var g = gesture(this, args).event(event), touches = event.changedTouches, n = touches.length, i, t, p, l;
    noevent_default3(event);
    for (i = 0; i < n; ++i) {
      t = touches[i], p = pointer_default(t, this);
      if (g.touch0 && g.touch0[2] === t.identifier) g.touch0[0] = p;
      else if (g.touch1 && g.touch1[2] === t.identifier) g.touch1[0] = p;
    }
    t = g.that.__zoom;
    if (g.touch1) {
      var p0 = g.touch0[0], l0 = g.touch0[1], p1 = g.touch1[0], l1 = g.touch1[1], dp = (dp = p1[0] - p0[0]) * dp + (dp = p1[1] - p0[1]) * dp, dl = (dl = l1[0] - l0[0]) * dl + (dl = l1[1] - l0[1]) * dl;
      t = scale2(t, Math.sqrt(dp / dl));
      p = [(p0[0] + p1[0]) / 2, (p0[1] + p1[1]) / 2];
      l = [(l0[0] + l1[0]) / 2, (l0[1] + l1[1]) / 2];
    } else if (g.touch0) p = g.touch0[0], l = g.touch0[1];
    else return;
    g.zoom("touch", constrain(translate(t, p, l), g.extent, translateExtent));
  }
  function touchended(event, ...args) {
    if (!this.__zooming) return;
    var g = gesture(this, args).event(event), touches = event.changedTouches, n = touches.length, i, t;
    nopropagation3(event);
    if (touchending) clearTimeout(touchending);
    touchending = setTimeout(function() {
      touchending = null;
    }, touchDelay);
    for (i = 0; i < n; ++i) {
      t = touches[i];
      if (g.touch0 && g.touch0[2] === t.identifier) delete g.touch0;
      else if (g.touch1 && g.touch1[2] === t.identifier) delete g.touch1;
    }
    if (g.touch1 && !g.touch0) g.touch0 = g.touch1, delete g.touch1;
    if (g.touch0) g.touch0[1] = this.__zoom.invert(g.touch0[0]);
    else {
      g.end();
      if (g.taps === 2) {
        t = pointer_default(t, this);
        if (Math.hypot(touchfirst[0] - t[0], touchfirst[1] - t[1]) < tapDistance) {
          var p = select_default2(this).on("dblclick.zoom");
          if (p) p.apply(this, arguments);
        }
      }
    }
  }
  zoom2.wheelDelta = function(_) {
    return arguments.length ? (wheelDelta = typeof _ === "function" ? _ : constant_default7(+_), zoom2) : wheelDelta;
  };
  zoom2.filter = function(_) {
    return arguments.length ? (filter3 = typeof _ === "function" ? _ : constant_default7(!!_), zoom2) : filter3;
  };
  zoom2.touchable = function(_) {
    return arguments.length ? (touchable = typeof _ === "function" ? _ : constant_default7(!!_), zoom2) : touchable;
  };
  zoom2.extent = function(_) {
    return arguments.length ? (extent2 = typeof _ === "function" ? _ : constant_default7([[+_[0][0], +_[0][1]], [+_[1][0], +_[1][1]]]), zoom2) : extent2;
  };
  zoom2.scaleExtent = function(_) {
    return arguments.length ? (scaleExtent[0] = +_[0], scaleExtent[1] = +_[1], zoom2) : [scaleExtent[0], scaleExtent[1]];
  };
  zoom2.translateExtent = function(_) {
    return arguments.length ? (translateExtent[0][0] = +_[0][0], translateExtent[1][0] = +_[1][0], translateExtent[0][1] = +_[0][1], translateExtent[1][1] = +_[1][1], zoom2) : [[translateExtent[0][0], translateExtent[0][1]], [translateExtent[1][0], translateExtent[1][1]]];
  };
  zoom2.constrain = function(_) {
    return arguments.length ? (constrain = _, zoom2) : constrain;
  };
  zoom2.duration = function(_) {
    return arguments.length ? (duration = +_, zoom2) : duration;
  };
  zoom2.interpolate = function(_) {
    return arguments.length ? (interpolate = _, zoom2) : interpolate;
  };
  zoom2.on = function() {
    var value = listeners.on.apply(listeners, arguments);
    return value === listeners ? zoom2 : value;
  };
  zoom2.clickDistance = function(_) {
    return arguments.length ? (clickDistance2 = (_ = +_) * _, zoom2) : Math.sqrt(clickDistance2);
  };
  zoom2.tapDistance = function(_) {
    return arguments.length ? (tapDistance = +_, zoom2) : tapDistance;
  };
  return zoom2;
}

// node_modules/billboard.js/dist-esm/billboard.js
function getGlobal() {
  return typeof globalThis === "object" && globalThis !== null && globalThis.Object === Object && globalThis || typeof global === "object" && global !== null && global.Object === Object && global || typeof self === "object" && self !== null && self.Object === Object && self || Function("return this")();
}
function getFallback(w) {
  var hasRAF = typeof (w === null || w === void 0 ? void 0 : w.requestAnimationFrame) === "function" && typeof (w === null || w === void 0 ? void 0 : w.cancelAnimationFrame) === "function";
  var hasRIC = typeof (w === null || w === void 0 ? void 0 : w.requestIdleCallback) === "function" && typeof (w === null || w === void 0 ? void 0 : w.cancelIdleCallback) === "function";
  var request = function(cb) {
    return setTimeout(cb, 1);
  };
  var cancel = function(id2) {
    return clearTimeout(id2);
  };
  return [
    hasRAF ? w.requestAnimationFrame : request,
    hasRAF ? w.cancelAnimationFrame : cancel,
    hasRIC ? w.requestIdleCallback : request,
    hasRIC ? w.cancelIdleCallback : cancel
  ];
}
var win = getGlobal();
var doc = win === null || win === void 0 ? void 0 : win.document;
var _a = getFallback(win);
var requestAnimationFrame = _a[0];
var requestIdleCallback = _a[2];
var TYPE = {
  AREA: "area",
  AREA_LINE_RANGE: "area-line-range",
  AREA_SPLINE: "area-spline",
  AREA_SPLINE_RANGE: "area-spline-range",
  AREA_STEP: "area-step",
  BAR: "bar",
  BUBBLE: "bubble",
  CANDLESTICK: "candlestick",
  DONUT: "donut",
  GAUGE: "gauge",
  LINE: "line",
  PIE: "pie",
  POLAR: "polar",
  RADAR: "radar",
  SCATTER: "scatter",
  SPLINE: "spline",
  STEP: "step",
  TREEMAP: "treemap"
};
var TYPE_METHOD_NEEDED = {
  AREA: "initArea",
  AREA_LINE_RANGE: "initArea",
  AREA_SPLINE: "initArea",
  AREA_SPLINE_RANGE: "initArea",
  AREA_STEP: "initArea",
  BAR: "initBar",
  BUBBLE: "initCircle",
  CANDLESTICK: "initCandlestick",
  DONUT: "initArc",
  GAUGE: "initArc",
  LINE: "initLine",
  PIE: "initArc",
  POLAR: "initPolar",
  RADAR: "initCircle",
  SCATTER: "initCircle",
  SPLINE: "initLine",
  STEP: "initLine",
  TREEMAP: "initTreemap"
};
var TYPE_BY_CATEGORY = {
  Area: [
    TYPE.AREA,
    TYPE.AREA_SPLINE,
    TYPE.AREA_SPLINE_RANGE,
    TYPE.AREA_LINE_RANGE,
    TYPE.AREA_STEP
  ],
  AreaRange: [
    TYPE.AREA_SPLINE_RANGE,
    TYPE.AREA_LINE_RANGE
  ],
  Arc: [
    TYPE.PIE,
    TYPE.DONUT,
    TYPE.GAUGE,
    TYPE.POLAR,
    TYPE.RADAR
  ],
  Line: [
    TYPE.LINE,
    TYPE.SPLINE,
    TYPE.AREA,
    TYPE.AREA_SPLINE,
    TYPE.AREA_SPLINE_RANGE,
    TYPE.AREA_LINE_RANGE,
    TYPE.STEP,
    TYPE.AREA_STEP
  ],
  Step: [
    TYPE.STEP,
    TYPE.AREA_STEP
  ],
  Spline: [
    TYPE.SPLINE,
    TYPE.AREA_SPLINE,
    TYPE.AREA_SPLINE_RANGE
  ]
};
var _assign = function __assign() {
  _assign = Object.assign || function(t) {
    for (var s2, i = 1, n = arguments.length; i < n; i++) {
      s2 = arguments[i];
      for (var p in s2) if (Object.prototype.hasOwnProperty.call(s2, p)) t[p] = s2[p];
    }
    return t;
  };
  return _assign.apply(this, arguments);
};
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
}
var isValue = function(v) {
  return v || v === 0;
};
var isFunction = function(v) {
  return typeof v === "function";
};
var isString = function(v) {
  return typeof v === "string";
};
var isNumber = function(v) {
  return typeof v === "number";
};
var isUndefined = function(v) {
  return typeof v === "undefined";
};
var isDefined = function(v) {
  return typeof v !== "undefined";
};
var isboolean = function(v) {
  return typeof v === "boolean";
};
var ceil10 = function(v) {
  return Math.ceil(v / 10) * 10;
};
var asHalfPixel = function(n) {
  return Math.ceil(n) + 0.5;
};
var diffDomain = function(d) {
  return d[1] - d[0];
};
var isObjectType = function(v) {
  return typeof v === "object";
};
var isEmpty = function(o) {
  return isUndefined(o) || o === null || isString(o) && o.length === 0 || isObjectType(o) && !(o instanceof Date) && Object.keys(o).length === 0 || isNumber(o) && isNaN(o);
};
var notEmpty = function(o) {
  return !isEmpty(o);
};
var isArray = function(arr) {
  return Array.isArray(arr);
};
var isObject = function(obj) {
  return obj && !(obj === null || obj === void 0 ? void 0 : obj.nodeType) && isObjectType(obj) && !isArray(obj);
};
function getOption(options2, key, defaultValue) {
  return isDefined(options2[key]) ? options2[key] : defaultValue;
}
function hasValue(dict, value) {
  var found = false;
  Object.keys(dict).forEach(function(key) {
    return dict[key] === value && (found = true);
  });
  return found;
}
function callFn(fn, thisArg) {
  var args = [];
  for (var _i = 2; _i < arguments.length; _i++) {
    args[_i - 2] = arguments[_i];
  }
  var isFn = isFunction(fn);
  isFn && fn.call.apply(fn, __spreadArray([thisArg], args, false));
  return isFn;
}
function endall(transition2, cb) {
  var n = 0;
  var end = function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    !--n && cb.apply.apply(cb, __spreadArray([this], args, false));
  };
  if ("duration" in transition2) {
    transition2.each(function() {
      return ++n;
    }).on("end", end);
  } else {
    ++n;
    transition2.call(end);
  }
}
function sanitize(str) {
  return isString(str) ? str.replace(/<(script|img)?/ig, "&lt;").replace(/(script)?>/ig, "&gt;") : str;
}
function setTextValue(node, text2, dy, toMiddle) {
  if (dy === void 0) {
    dy = [-1, 1];
  }
  if (toMiddle === void 0) {
    toMiddle = false;
  }
  if (!node || !isString(text2)) {
    return;
  }
  if (text2.indexOf("\n") === -1) {
    node.text(text2);
  } else {
    var diff = [node.text(), text2].map(function(v) {
      return v.replace(/[\s\n]/g, "");
    });
    if (diff[0] !== diff[1]) {
      var multiline = text2.split("\n");
      var len_1 = toMiddle ? multiline.length - 1 : 1;
      node.html("");
      multiline.forEach(function(v, i) {
        node.append("tspan").attr("x", 0).attr("dy", "".concat(i === 0 ? dy[0] * len_1 : dy[1], "em")).text(v);
      });
    }
  }
}
function getRectSegList(path2) {
  var _a2 = path2.getBBox(), x3 = _a2.x, y3 = _a2.y, width = _a2.width, height = _a2.height;
  return [
    { x: x3, y: y3 + height },
    // seg0
    { x: x3, y: y3 },
    // seg1
    { x: x3 + width, y: y3 },
    // seg2
    { x: x3 + width, y: y3 + height }
    // seg3
  ];
}
function getPathBox(path2) {
  var _a2 = path2.getBoundingClientRect(), width = _a2.width, height = _a2.height;
  var items = getRectSegList(path2);
  var x3 = items[0].x;
  var y3 = Math.min(items[0].y, items[1].y);
  return {
    x: x3,
    y: y3,
    width,
    height
  };
}
function getPointer(event, element) {
  var _a2;
  var touches = event && ((_a2 = event.touches || event.sourceEvent && event.sourceEvent.touches) === null || _a2 === void 0 ? void 0 : _a2[0]);
  var pointer$1 = [0, 0];
  try {
    pointer$1 = pointer_default(touches || event, element);
  } catch (e) {
  }
  return pointer$1.map(function(v) {
    return isNaN(v) ? 0 : v;
  });
}
function getBrushSelection(ctx) {
  var event = ctx.event, $el = ctx.$el;
  var main2 = $el.subchart.main || $el.main;
  var selection3;
  if (event && event.type === "brush") {
    selection3 = event.selection;
  } else if (main2 && (selection3 = main2.select(".bb-brush").node())) {
    selection3 = brushSelection(selection3);
  }
  return selection3;
}
function getBoundingRect(node) {
  var needEvaluate = !("rect" in node) || "rect" in node && node.hasAttribute("width") && node.rect.width !== +node.getAttribute("width");
  return needEvaluate ? node.rect = node.getBoundingClientRect() : node.rect;
}
function getRandom(asStr, min4, max4) {
  if (asStr === void 0) {
    asStr = true;
  }
  if (min4 === void 0) {
    min4 = 0;
  }
  if (max4 === void 0) {
    max4 = 1e4;
  }
  var crpt = win.crypto || win.msCrypto;
  var rand = crpt ? min4 + crpt.getRandomValues(new Uint32Array(1))[0] % (max4 - min4 + 1) : Math.floor(Math.random() * (max4 - min4) + min4);
  return asStr ? String(rand) : rand;
}
function findIndex(arr, v, start2, end, isRotated) {
  if (start2 > end) {
    return -1;
  }
  var mid = Math.floor((start2 + end) / 2);
  var _a2 = arr[mid], x3 = _a2.x, _b = _a2.w, w = _b === void 0 ? 0 : _b;
  if (isRotated) {
    x3 = arr[mid].y;
    w = arr[mid].h;
  }
  if (v >= x3 && v <= x3 + w) {
    return mid;
  }
  return v < x3 ? findIndex(arr, v, start2, mid - 1, isRotated) : findIndex(arr, v, mid + 1, end, isRotated);
}
function brushEmpty(ctx) {
  var selection3 = getBrushSelection(ctx);
  if (selection3) {
    return selection3[0] === selection3[1];
  }
  return true;
}
function deepClone() {
  var objectN = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    objectN[_i] = arguments[_i];
  }
  var clone = function(v) {
    if (isObject(v) && v.constructor) {
      var r = new v.constructor();
      for (var k2 in v) {
        r[k2] = clone(v[k2]);
      }
      return r;
    }
    return v;
  };
  return objectN.map(function(v) {
    return clone(v);
  }).reduce(function(a2, c) {
    return _assign(_assign({}, a2), c);
  });
}
function extend2(target, source) {
  if (target === void 0) {
    target = {};
  }
  if (isArray(source)) {
    source.forEach(function(v) {
      return extend2(target, v);
    });
  }
  for (var p in source) {
    if (/^\d+$/.test(p) || p in target) {
      continue;
    }
    target[p] = source[p];
  }
  return target;
}
var capitalize = function(str) {
  return str.charAt(0).toUpperCase() + str.slice(1);
};
function camelize(str, separator) {
  if (separator === void 0) {
    separator = "-";
  }
  return str.split(separator).map(function(v, i) {
    return i ? v.charAt(0).toUpperCase() + v.slice(1).toLowerCase() : v.toLowerCase();
  }).join("");
}
var toArray = function(v) {
  return [].slice.call(v);
};
function addCssRules(style2, selector, prop) {
  var _a2 = style2.rootSelector, rootSelector = _a2 === void 0 ? "" : _a2, sheet = style2.sheet;
  var getSelector = function(s2) {
    return s2.replace(/\s?(bb-)/g, ".$1").replace(/\.+/g, ".");
  };
  var rule = "".concat(rootSelector, " ").concat(getSelector(selector), " {").concat(prop.join(";"), "}");
  return sheet[sheet.insertRule ? "insertRule" : "addRule"](rule, sheet.cssRules.length);
}
function getCssRules(styleSheets) {
  var rules = [];
  styleSheets.forEach(function(sheet) {
    var _a2;
    try {
      if (sheet.cssRules && sheet.cssRules.length) {
        rules = rules.concat(toArray(sheet.cssRules));
      }
    } catch (e) {
      (_a2 = win.console) === null || _a2 === void 0 ? void 0 : _a2.warn("Error while reading rules from ".concat(sheet.href, ": ").concat(e.toString()));
    }
  });
  return rules;
}
function getScrollPosition(node) {
  var _a2, _b, _c, _d, _e, _f;
  return {
    x: (_c = ((_b = (_a2 = win.pageXOffset) !== null && _a2 !== void 0 ? _a2 : win.scrollX) !== null && _b !== void 0 ? _b : 0) + node.scrollLeft) !== null && _c !== void 0 ? _c : 0,
    y: (_f = ((_e = (_d = win.pageYOffset) !== null && _d !== void 0 ? _d : win.scrollY) !== null && _e !== void 0 ? _e : 0) + node.scrollTop) !== null && _f !== void 0 ? _f : 0
  };
}
function getTranslation(node) {
  var transform3 = node ? node.transform : null;
  var baseVal = transform3 && transform3.baseVal;
  return baseVal && baseVal.numberOfItems ? baseVal.getItem(0).matrix : { a: 0, b: 0, c: 0, d: 0, e: 0, f: 0 };
}
function getUnique(data2) {
  var isDate = data2[0] instanceof Date;
  var d = (isDate ? data2.map(Number) : data2).filter(function(v, i, self2) {
    return self2.indexOf(v) === i;
  });
  return isDate ? d.map(function(v) {
    return new Date(v);
  }) : d;
}
function mergeArray(arr) {
  return arr && arr.length ? arr.reduce(function(p, c) {
    return p.concat(c);
  }) : [];
}
function mergeObj(target) {
  var objectN = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    objectN[_i - 1] = arguments[_i];
  }
  if (!objectN.length || objectN.length === 1 && !objectN[0]) {
    return target;
  }
  var source = objectN.shift();
  if (isObject(target) && isObject(source)) {
    Object.keys(source).forEach(function(key) {
      var value = source[key];
      if (isObject(value)) {
        !target[key] && (target[key] = {});
        target[key] = mergeObj(target[key], value);
      } else {
        target[key] = isArray(value) ? value.concat() : value;
      }
    });
  }
  return mergeObj.apply(void 0, __spreadArray([target], objectN, false));
}
function sortValue(data2, isAsc) {
  if (isAsc === void 0) {
    isAsc = true;
  }
  var fn;
  if (data2[0] instanceof Date) {
    fn = isAsc ? function(a2, b) {
      return a2 - b;
    } : function(a2, b) {
      return b - a2;
    };
  } else {
    if (isAsc && !data2.every(isNaN)) {
      fn = function(a2, b) {
        return a2 - b;
      };
    } else if (!isAsc) {
      fn = function(a2, b) {
        return a2 > b && -1 || a2 < b && 1 || a2 === b && 0;
      };
    }
  }
  return data2.concat().sort(fn);
}
function getMinMax$1(type2, data2) {
  var res = data2.filter(function(v) {
    return notEmpty(v);
  });
  if (res.length) {
    if (isNumber(res[0])) {
      res = Math[type2].apply(Math, res);
    } else if (res[0] instanceof Date) {
      res = sortValue(res, type2 === "min")[0];
    }
  } else {
    res = void 0;
  }
  return res;
}
var getRange = function(start2, end, step2) {
  if (step2 === void 0) {
    step2 = 1;
  }
  var res = [];
  var n = Math.max(0, Math.ceil((end - start2) / step2)) | 0;
  for (var i = start2; i < n; i++) {
    res.push(start2 + i * step2);
  }
  return res;
};
var emulateEvent = {
  mouse: function() {
    var getParams = function() {
      return {
        bubbles: false,
        cancelable: false,
        screenX: 0,
        screenY: 0,
        clientX: 0,
        clientY: 0
      };
    };
    try {
      new MouseEvent("t");
      return function(el, eventType, params) {
        if (params === void 0) {
          params = getParams();
        }
        el.dispatchEvent(new MouseEvent(eventType, params));
      };
    } catch (e) {
      return function(el, eventType, params) {
        if (params === void 0) {
          params = getParams();
        }
        var mouseEvent = doc.createEvent("MouseEvent");
        mouseEvent.initMouseEvent(
          eventType,
          params.bubbles,
          params.cancelable,
          win,
          0,
          // the event's mouse click count
          params.screenX,
          params.screenY,
          params.clientX,
          params.clientY,
          false,
          false,
          false,
          false,
          0,
          null
        );
        el.dispatchEvent(mouseEvent);
      };
    }
  }(),
  touch: function(el, eventType, params) {
    var touchObj = new Touch(mergeObj({
      identifier: Date.now(),
      target: el,
      radiusX: 2.5,
      radiusY: 2.5,
      rotationAngle: 10,
      force: 0.5
    }, params));
    el.dispatchEvent(new TouchEvent(eventType, {
      cancelable: true,
      bubbles: true,
      shiftKey: true,
      touches: [touchObj],
      targetTouches: [],
      changedTouches: [touchObj]
    }));
  }
};
function tplProcess(tpl, data2) {
  var res = tpl;
  for (var x3 in data2) {
    res = res.replace(new RegExp("{=".concat(x3, "}"), "g"), data2[x3]);
  }
  return res;
}
function parseDate(date2) {
  var _a2;
  var parsedDate;
  if (date2 instanceof Date) {
    parsedDate = date2;
  } else if (isString(date2)) {
    var _b = this, config = _b.config, format3 = _b.format;
    parsedDate = (_a2 = format3.dataTime(config.data_xFormat)(date2)) !== null && _a2 !== void 0 ? _a2 : new Date(date2);
  } else if (isNumber(date2) && !isNaN(date2)) {
    parsedDate = /* @__PURE__ */ new Date(+date2);
  }
  if (!parsedDate || isNaN(+parsedDate)) {
    console && console.error && console.error("Failed to parse x '".concat(date2, "' to Date object"));
  }
  return parsedDate;
}
function isTabVisible() {
  return (doc === null || doc === void 0 ? void 0 : doc.hidden) === false || (doc === null || doc === void 0 ? void 0 : doc.visibilityState) === "visible";
}
function convertInputType(mouse, touch) {
  var DocumentTouch = win.DocumentTouch, matchMedia = win.matchMedia, navigator2 = win.navigator;
  var hasTouch = false;
  if (touch) {
    if (navigator2 && "maxTouchPoints" in navigator2) {
      hasTouch = navigator2.maxTouchPoints > 0;
    } else if ("ontouchmove" in win || DocumentTouch && doc instanceof DocumentTouch) {
      hasTouch = true;
    } else {
      if (matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia("(pointer:coarse)").matches) {
        hasTouch = true;
      } else {
        var UA = navigator2.userAgent;
        hasTouch = /\b(BlackBerry|webOS|iPhone|IEMobile)\b/i.test(UA) || /\b(Android|Windows Phone|iPad|iPod)\b/i.test(UA);
      }
    }
  }
  var hasMouse = mouse && ["any-hover:hover", "any-pointer:fine"].some(function(v) {
    return matchMedia === null || matchMedia === void 0 ? void 0 : matchMedia("(".concat(v, ")")).matches;
  });
  return hasMouse && "mouse" || hasTouch && "touch" || "mouse";
}
function runUntil(fn, conditionFn) {
  if (conditionFn() === false) {
    requestAnimationFrame(function() {
      return runUntil(fn, conditionFn);
    });
  } else {
    fn();
  }
}
function checkModuleImport(ctx) {
  var $$ = ctx;
  var config = $$.config;
  var type2 = "";
  if (isEmpty(config.data_type || config.data_types) && !$$[TYPE_METHOD_NEEDED.LINE]) {
    type2 = "line";
  } else {
    for (var x3 in TYPE_METHOD_NEEDED) {
      var t = TYPE[x3];
      if ($$.hasType(t) && !$$[TYPE_METHOD_NEEDED[x3]]) {
        type2 = t;
        break;
      }
    }
  }
  type2 && logError("Please, make sure if %c".concat(camelize(type2)), "module has been imported and specified correctly.");
}
function logError(head, tail) {
  var _a2;
  var prefix = "[billboard.js]";
  var info = "https://github.com/naver/billboard.js/wiki/CHANGELOG-v2#modularization-by-its-functionality";
  var hasConsole = (_a2 = win.console) === null || _a2 === void 0 ? void 0 : _a2.error;
  if (hasConsole) {
    console.error("❌ ".concat(prefix, " ").concat(head), "background:red;color:white;display:block;font-size:15px", tail);
    console.info("%cℹ️", "font-size:15px", info);
  }
  throw Error("".concat(prefix, " ").concat(head.replace(/\%c([a-z-]+)/i, "'$1' "), " ").concat(tail));
}
var $COMMON = {
  button: "bb-button",
  chart: "bb-chart",
  empty: "bb-empty",
  main: "bb-main",
  target: "bb-target",
  EXPANDED: "_expanded_"
};
var $ARC = {
  arc: "bb-arc",
  arcLabelLine: "bb-arc-label-line",
  arcRange: "bb-arc-range",
  arcs: "bb-arcs",
  chartArc: "bb-chart-arc",
  chartArcs: "bb-chart-arcs",
  chartArcsBackground: "bb-chart-arcs-background",
  chartArcsTitle: "bb-chart-arcs-title",
  needle: "bb-needle"
};
var $AREA = {
  area: "bb-area",
  areas: "bb-areas"
};
var $AXIS = {
  axis: "bb-axis",
  axisX: "bb-axis-x",
  axisXLabel: "bb-axis-x-label",
  axisY: "bb-axis-y",
  axisY2: "bb-axis-y2",
  axisY2Label: "bb-axis-y2-label",
  axisYLabel: "bb-axis-y-label"
};
var $BAR = {
  bar: "bb-bar",
  bars: "bb-bars",
  chartBar: "bb-chart-bar",
  chartBars: "bb-chart-bars"
};
var $CANDLESTICK = {
  candlestick: "bb-candlestick",
  candlesticks: "bb-candlesticks",
  chartCandlestick: "bb-chart-candlestick",
  chartCandlesticks: "bb-chart-candlesticks",
  valueDown: "bb-value-down",
  valueUp: "bb-value-up"
};
var $CIRCLE = {
  chartCircles: "bb-chart-circles",
  circle: "bb-circle",
  circles: "bb-circles"
};
var $COLOR = {
  colorPattern: "bb-color-pattern",
  colorScale: "bb-colorscale"
};
var $DRAG = {
  dragarea: "bb-dragarea",
  INCLUDED: "_included_"
};
var $GAUGE = {
  chartArcsGaugeMax: "bb-chart-arcs-gauge-max",
  chartArcsGaugeMin: "bb-chart-arcs-gauge-min",
  chartArcsGaugeUnit: "bb-chart-arcs-gauge-unit",
  chartArcsGaugeTitle: "bb-chart-arcs-gauge-title",
  gaugeValue: "bb-gauge-value"
};
var $LEGEND = {
  legend: "bb-legend",
  legendBackground: "bb-legend-background",
  legendItem: "bb-legend-item",
  legendItemEvent: "bb-legend-item-event",
  legendItemHidden: "bb-legend-item-hidden",
  legendItemPoint: "bb-legend-item-point",
  legendItemTile: "bb-legend-item-tile"
};
var $LINE = {
  chartLine: "bb-chart-line",
  chartLines: "bb-chart-lines",
  line: "bb-line",
  lines: "bb-lines"
};
var $EVENT = {
  eventRect: "bb-event-rect",
  eventRects: "bb-event-rects",
  eventRectsMultiple: "bb-event-rects-multiple",
  eventRectsSingle: "bb-event-rects-single"
};
var $FOCUS = {
  focused: "bb-focused",
  defocused: "bb-defocused",
  legendItemFocused: "bb-legend-item-focused",
  xgridFocus: "bb-xgrid-focus",
  ygridFocus: "bb-ygrid-focus"
};
var $GRID = {
  grid: "bb-grid",
  gridLines: "bb-grid-lines",
  xgrid: "bb-xgrid",
  xgridLine: "bb-xgrid-line",
  xgridLines: "bb-xgrid-lines",
  xgrids: "bb-xgrids",
  ygrid: "bb-ygrid",
  ygridLine: "bb-ygrid-line",
  ygridLines: "bb-ygrid-lines",
  ygrids: "bb-ygrids"
};
var $LEVEL = {
  level: "bb-level",
  levels: "bb-levels"
};
var $RADAR = {
  chartRadar: "bb-chart-radar",
  chartRadars: "bb-chart-radars"
};
var $REGION = {
  region: "bb-region",
  regions: "bb-regions"
};
var $SELECT = {
  selectedCircle: "bb-selected-circle",
  selectedCircles: "bb-selected-circles",
  SELECTED: "_selected_"
};
var $SHAPE = {
  shape: "bb-shape",
  shapes: "bb-shapes"
};
var $SUBCHART = {
  brush: "bb-brush",
  subchart: "bb-subchart"
};
var $TEXT = {
  chartText: "bb-chart-text",
  chartTexts: "bb-chart-texts",
  text: "bb-text",
  texts: "bb-texts",
  title: "bb-title",
  TextOverlapping: "text-overlapping"
};
var $TOOLTIP = {
  tooltip: "bb-tooltip",
  tooltipContainer: "bb-tooltip-container",
  tooltipName: "bb-tooltip-name"
};
var $TREEMAP = {
  treemap: "bb-treemap",
  chartTreemap: "bb-chart-treemap",
  chartTreemaps: "bb-chart-treemaps"
};
var $ZOOM = {
  buttonZoomReset: "bb-zoom-reset",
  zoomBrush: "bb-zoom-brush"
};
var CLASS = _assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign(_assign({}, $COMMON), $ARC), $AREA), $AXIS), $BAR), $CANDLESTICK), $CIRCLE), $COLOR), $DRAG), $GAUGE), $LEGEND), $LINE), $EVENT), $FOCUS), $GRID), $RADAR), $REGION), $SELECT), $SHAPE), $SUBCHART), $TEXT), $TOOLTIP), $TREEMAP), $ZOOM);
var Element = (
  /** @class */
  /* @__PURE__ */ function() {
    function Element2() {
      var element = {
        chart: null,
        main: null,
        svg: null,
        axis: {
          x: null,
          y: null,
          y2: null,
          subX: null
        },
        defs: null,
        tooltip: null,
        legend: null,
        title: null,
        subchart: {
          main: null,
          // $$.context
          bar: null,
          // $$.contextBar
          line: null,
          // $$.contextLine
          area: null
          // $$.contextArea
        },
        arcs: null,
        bar: null,
        // mainBar,
        candlestick: null,
        line: null,
        // mainLine,
        area: null,
        // mainArea,
        circle: null,
        // mainCircle,
        radar: null,
        text: null,
        // mainText,
        grid: {
          main: null,
          // grid (also focus)
          x: null,
          // xgrid,
          y: null
          // ygrid,
        },
        gridLines: {
          main: null,
          // gridLines
          x: null,
          // xgridLines,
          y: null
          // ygridLines
        },
        region: {
          main: null,
          // region
          list: null
          // mainRegion
        },
        eventRect: null,
        zoomResetBtn: null
        // drag zoom reset button
      };
      return element;
    }
    return Element2;
  }()
);
var State = (
  /** @class */
  /* @__PURE__ */ function() {
    function State2() {
      return {
        // chart drawn area dimension, excluding axes
        width: 0,
        width2: 0,
        height: 0,
        height2: 0,
        margin: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        margin2: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        margin3: {
          top: 0,
          bottom: 0,
          left: 0,
          right: 0
        },
        arcWidth: 0,
        arcHeight: 0,
        xAxisHeight: 0,
        hasAxis: false,
        hasRadar: false,
        hasTreemap: false,
        // for data CSS rule index (used when boost.useCssRule is true)
        cssRule: {},
        current: {
          // current domain value. Assigned when is zoom is called
          domain: void 0,
          // chart whole dimension
          width: 0,
          height: 0,
          dataMax: 0,
          maxTickSize: {
            x: { width: 0, height: 0, ticks: [], clipPath: 0, domain: "" },
            y: { width: 0, height: 0, domain: "" },
            y2: { width: 0, height: 0, domain: "" }
          },
          // current used chart type list
          types: [],
          needle: void 0
          // arc needle current value
        },
        // legend
        isLegendRight: false,
        isLegendInset: false,
        isLegendTop: false,
        isLegendLeft: false,
        legendStep: 0,
        legendItemWidth: 0,
        legendItemHeight: 0,
        legendHasRendered: false,
        eventReceiver: {
          currentIdx: -1,
          // current event interaction index
          rect: {},
          // event rect's clientBoundingRect
          data: [],
          // event data bound of previoous eventRect
          coords: []
          // coordination value of previous eventRect
        },
        axis: {
          x: {
            padding: { left: 0, right: 0 },
            tickCount: 0
          }
        },
        rotatedPadding: {
          left: 30,
          right: 0,
          top: 5
        },
        withoutFadeIn: {},
        inputType: "",
        datetimeId: "",
        // clip id string
        clip: {
          id: "",
          idXAxis: "",
          idYAxis: "",
          idXAxisTickTexts: "",
          idGrid: "",
          idSubchart: "",
          // clipIdForSubchart
          path: "",
          pathXAxis: "",
          pathYAxis: "",
          pathXAxisTickTexts: "",
          pathGrid: ""
        },
        // state
        event: null,
        // event object
        dragStart: null,
        dragging: false,
        flowing: false,
        cancelClick: false,
        mouseover: false,
        rendered: false,
        transiting: false,
        redrawing: false,
        // if redraw() is on process
        resizing: false,
        // resize event called
        toggling: false,
        // legend toggle
        zooming: false,
        hasNegativeValue: false,
        hasPositiveValue: true,
        orgAreaOpacity: "0.2",
        orgConfig: {},
        // user original genration config
        // ID strings
        hiddenTargetIds: [],
        hiddenLegendIds: [],
        focusedTargetIds: [],
        defocusedTargetIds: [],
        // value for Arc
        radius: 0,
        innerRadius: 0,
        outerRadius: void 0,
        innerRadiusRatio: 0,
        gaugeArcWidth: 0,
        radiusExpanded: 0,
        // xgrid attribute
        xgridAttr: {
          x1: null,
          x2: null,
          y1: null,
          y2: null
        }
      };
    }
    return State2;
  }()
);
var classes = {
  element: Element,
  state: State
};
var Store = (
  /** @class */
  function() {
    function Store2() {
      var _this = this;
      Object.keys(classes).forEach(function(v) {
        _this[v] = new classes[v]();
      });
    }
    Store2.prototype.getStore = function(name) {
      return this[name];
    };
    return Store2;
  }()
);
var main = {
  /**
   * Specify the CSS selector or the element which the chart will be set to. D3 selection object can be specified also.<br>
   * If other chart is set already, it will be replaced with the new one (only one chart can be set in one element).
   * - **NOTE:** In case of element doesn't exist or not specified, will add a `<div>` element to the body.
   * @name bindto
   * @memberof Options
   * @property {string|HTMLElement|d3.selection|object} [bindto="#chart"] Specify the element where chart will be drawn.
   * @property {string|HTMLElement|d3.selection} bindto.element="#chart" Specify the element where chart will be drawn.
   * @property {string} [bindto.classname=bb] Specify the class name of bind element.<br>
   *     **NOTE:** When class name isn't `bb`, then you also need to update the default CSS to be rendered correctly.
   * @default #chart
   * @example
   * bindto: "#myContainer"
   *
   * // or HTMLElement
   * bindto: document.getElementById("myContainer")
   *
   * // or D3 selection object
   * bindto: d3.select("#myContainer")
   *
   * // or to change default classname
   * bindto: {
   *    element: "#chart",
   *    classname: "bill-board"  // ex) <div id='chart' class='bill-board'>
   * }
   */
  bindto: "#chart",
  /**
   * Set chart background.
   * @name background
   * @memberof Options
   * @property {object} background background object
   * @property {string} background.class Specify the class name for background element.
   * @property {string} background.color Specify the fill color for background element.<br>**NOTE:** Will be ignored if `imgUrl` option is set.
   * @property {string} background.imgUrl Specify the image url string for background.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.Background)
   * @example
   * background: {
   *    class: "myClass",
   *    color: "red",
   *
   *    // Set image url for background.
   *    // If specified, 'color' option will be ignored.
   *    imgUrl: "https://naver.github.io/billboard.js/img/logo/billboard.js.svg",
   * }
   */
  background: {},
  /**
   * Set 'clip-path' attribute for chart element
   * - **NOTE:**
   *  > When is false, chart node element is positioned after the axis node in DOM tree hierarchy.
   *  > Is to make chart element positioned over axis element.
   * @name clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.clipPath)
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  clipPath: true,
  /**
   * Set svg element's class name
   * @name svg
   * @memberof Options
   * @type {object}
   * @property {object} [svg] svg object
   * @property {string} [svg.classname] class name for svg element
   * @example
   * svg: {
   *   classname: "test_class"
   * }
   */
  svg_classname: void 0,
  /**
   * The desired size of the chart element.
   * If value is not specified, the width of the chart will be calculated by the size of the parent element it's appended to.
   * @name size
   * @memberof Options
   * @type {object}
   * @property {object} [size] size object
   * @property {number} [size.width] width of the chart element
   * @property {number} [size.height] height of the chart element
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.ChartSize)
   * @example
   * size: {
   *   width: 640,
   *   height: 480
   * }
   */
  size_width: void 0,
  size_height: void 0,
  /**
   * The padding of the chart element.
   * - **NOTE:** for more information, see the "[`Understanding padding`](https://github.com/naver/billboard.js/wiki/Understanding-padding)"" wiki documentaion.
   * @name padding
   * @memberof Options
   * @type {object}
   * @property {object|boolean} [padding=true] Set padding of chart, and accepts object or boolean type.
   * - `Object`: Specify each side's padding.
   * - `false`: Remove padding completely and make shape to fully occupy the container element.
   *   - In this case, axes and subchart will be hidden.
   *   - To adjust some padding from this state, use `axis.[x|y].padding` option.
   * @property {string} [padding.mode] padding mode
   * - `"fit"`: Reduce padding as much as possible to make chart fit to the container element for chart types w/axis.<br>When specified, all padding values will be relative from fitted value.
   * @property {number} [padding.top] padding on the top of chart
   * @property {number} [padding.right] padding on the right of chart
   * @property {number} [padding.bottom] padding on the bottom of chart
   * @property {number} [padding.left] padding on the left of chart
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.Padding)
   * @see [Demo: Fit padding](https://naver.github.io/billboard.js/demo/#ChartOptions.FitPadding)
   * @example
   * // remove padding completely.
   * padding: false,
   *
   * padding: {
   *   // specifying mode value, will reduce padding and make fit to the container element.
   *   mode: "fit"
   *
   *   // when mode is "fit", all padding values will be relative from fitted value.
   *   // so, 0 will be initial fitted value.
   *   top: 20,
   *   right: 20,
   *   bottom: 20,
   *   left: 20
   * }
   *
   * // or specify padding value for each side
   * padding: {
   *   top: 20,
   *   right: 20,
   *   bottom: 20,
   *   left: 20
   * }
   */
  padding: true,
  padding_mode: void 0,
  padding_left: void 0,
  padding_right: void 0,
  padding_top: void 0,
  padding_bottom: void 0,
  /**
   * Set chart resize options
   * @name resize
   * @memberof Options
   * @type {object}
   * @property {object} [resize] resize object
   * @property {boolean} [resize.auto=true] Set chart resize automatically on viewport changes.
   * @property {boolean|number} [resize.timer=true] Set resize timer option.
   * - **NOTE:**
   *   - The resize function will be called using:
   *     - true: `setTimeout()`
   *     - false: `requestIdleCallback()`
   *   - Given number(delay in ms) value, resize function will be triggered using `setTimer()` with given delay.
   * @example
   *  resize: {
   *      auto: false,
   *
   *      // set resize function will be triggered using `setTimer()`
   *      timer: true,
   *
   *      // set resize function will be triggered using `requestIdleCallback()`
   *      timer: false,
   *
   *      // set resize function will be triggered using `setTimer()` with a delay of `100ms`.
   *      timer: 100
   *  }
   */
  resize_auto: true,
  resize_timer: true,
  /**
   * Set a callback to execute when the chart is clicked.
   * @name onclick
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onclick: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onclick: void 0,
  /**
   * Set a callback to execute when mouse/touch enters the chart.
   * @name onover
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onover: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onover: void 0,
  /**
   * Set a callback to execute when mouse/touch leaves the chart.
   * @name onout
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onout: function(event) {
   *   this; // chart instance itself
   *   event; // native event object
   *   ...
   * }
   */
  onout: void 0,
  /**
   * Set a callback to execute when user resizes the screen.
   * @name onresize
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onresize: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onresize: void 0,
  /**
   * Set a callback to execute when screen resize finished.
   * @name onresized
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onresized: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onresized: void 0,
  /**
   * Set a callback to execute before the chart is initialized
   * @name onbeforeinit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onbeforeinit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onbeforeinit: void 0,
  /**
   * Set a callback to execute when the chart is initialized.
   * @name oninit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * oninit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  oninit: void 0,
  /**
   * Set a callback to execute after the chart is initialized
   * @name onafterinit
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onafterinit: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onafterinit: void 0,
  /**
   * Set a callback which is executed when the chart is rendered. Basically, this callback will be called in each time when the chart is redrawed.
   * @name onrendered
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * onrendered: function() {
   *   this; // chart instance itself
   *   ...
   * }
   */
  onrendered: void 0,
  /**
   * Set duration of transition (in milliseconds) for chart animation.<br><br>
   * - **NOTE:** If `0 `or `null` set, transition will be skipped. So, this makes initial rendering faster especially in case you have a lot of data.
   * @name transition
   * @memberof Options
   * @type {object}
   * @property {object} [transition] transition object
   * @property {number} [transition.duration=350] duration in milliseconds
   * @example
   * transition: {
   *    duration: 500
   * }
   */
  transition_duration: 250,
  /**
   * Set plugins
   * @name plugins
   * @memberof Options
   * @type {Array}
   * @example
   *  plugins: [
   *    new bb.plugin.stanford({ ... }),
   *    new PluginA(),
   *    ...
   * ]
   */
  plugins: [],
  /**
   * Control the render timing
   * @name render
   * @memberof Options
   * @type {object}
   * @property {object} [render] render object
   * @property {boolean} [render.lazy=true] Make to not render at initialization (enabled by default when bind element's visibility is hidden).
   * @property {boolean} [render.observe=true] Observe bind element's visibility(`display` or `visiblity` inline css property or class value) & render when is visible automatically (for IEs, only works IE11+). When set to **false**, call [`.flush()`](./Chart.html#flush) to render.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#ChartOptions.LazyRender)
   * @example
   *  render: {
   *    lazy: true,
   *    observe: true
   * }
   *
   * @example
   *	// <!-- render.lazy will detect visibility defined -->
   *  // (a) <div id='chart' class='hide'></div>
   *  // (b) <div id='chart' style='display:none'></div>
   *
   *  // render.lazy enabled by default when element is hidden
   *  var chart = bb.generate({ ... });
   *
   *  // chart will be rendered automatically when element's visibility changes
   *  // Note: works only for inlined css property or class attribute changes
   *  document.getElementById('chart').classList.remove('hide')  // (a)
   *  document.getElementById('chart').style.display = 'block';  // (b)
   *
   * @example
   *	// chart won't be rendered and not observing bind element's visiblity changes
   *  var chart = bb.generate({
   *     render: {
   *          lazy: true,
   *          observe: false
   *     }
   *  });
   *
   *  // call at any point when you want to render
   *  chart.flush();
   */
  render: {},
  /**
   * Show rectangles inside the chart.<br><br>
   * This option accepts array including object that has axis, start, end and class.
   * The keys start, end and class are optional.
   * axis must be x, y or y2. start and end should be the value where regions start and end.
   * If not specified, the edge values will be used.
   * If timeseries x axis, date string, Date object and unixtime integer can be used.
   * If class is set, the region element will have it as class.
   * @name regions
   * @memberof Options
   * @type {Array}
   * @default []
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Region.RegionLabel)
   * @example
   *  regions: [
   *    {
   *      axis: "x",
   *      start: 1,
   *      end: 4,
   *      class: "region-1-4",
   *      label: {
   *      	text: "Region Text",
   *      	x: 5,  // position relative of the initial x coordinate
   *      	y: 5,  // position relative of the initial y coordinate
   *      	color: "red",  // color string
   *      	rotated: true  // make text to show in vertical or horizontal
   *      }
   *    }
   *  ]
   */
  regions: []
};
var boost = {
  /**
   * Set boost options
   * @name boost
   * @memberof Options
   * @type {object}
   * @property {object} boost boost object
   * @property {boolean} [boost.useCssRule=false] Avoid setting inline styles for each shape elements.
   * - **NOTE:**
   *   - Will append &lt;style> to the head tag and will add shpes' CSS rules dynamically.
   *   - For now, covers colors related properties (fill, stroke, etc.) only.
   * @property {boolean} [boost.useWorker=false] Use Web Worker as possible for processing.
   * - **NOTE:**
   *   - For now, only applies for data conversion at the initial time.
   *   - As of Web Worker's async nature, handling chart instance synchrously is not recommended.
   * @example
   *  boost: {
   *      useCssRule: true,
   *      useWorker: false
   *  }
   */
  boost_useCssRule: false,
  boost_useWorker: false
};
var data$2 = {
  /**
   * Specify the key of x values in the data.<br><br>
   * We can show the data with non-index x values by this option. This option is required when the type of x axis is timeseries. If this option is set on category axis, the values of the data on the key will be used for category names.
   * @name data․x
   * @memberof Options
   * @type {string}
   * @default undefined
   * @example
   * data: {
   *   x: "date"
   * }
   */
  data_x: void 0,
  /**
   * Converts data id value
   * @name data․idConverter
   * @memberof Options
   * @type {Function}
   * @default function(id) { return id; }
   * @example
   * data: {
   *    idConverter: function(id) {
   *       // when id is 'data1', converts to be 'data2'
   *       // 'data2' should be given as the initial data value
   *       if (id === "data1") {
   *          return "data2";
   *       } else {
   *          return id;
   *       }
   *    }
   * }
   */
  data_idConverter: function(id2) {
    return id2;
  },
  /**
   * Set custom data name.
   * If a name is set to `null`, the series is omitted from the legend.
   * @name data․names
   * @memberof Options
   * @type {object}
   * @default {}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataName)
   * @example
   * data: {
   *   names: {
   *     data1: "Data Name 1",
   *     data2: "Data Name 2"
   *   }
   * }
   */
  data_names: {},
  /**
   * Set custom data class.<br><br>
   * If this option is specified, the element g for the data has an additional class that has the prefix 'bb-target-' (eg. bb-target-additional-data1-class).
   * @name data․classes
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   classes: {
   *     data1: "additional-data1-class",
   *     data2: "additional-data2-class"
   *   }
   * }
   */
  data_classes: {},
  /**
   * Set chart type at once.<br><br>
   * If this option is specified, the type will be applied to every data. This setting can be overwritten by data.types.<br><br>
   * **Available Values:**
   * - area
   * - area-line-range
   * - area-spline
   * - area-spline-range
   * - area-step
   * - bar
   * - bubble
   * - candlestick
   * - donut
   * - gauge
   * - line
   * - pie
   * - polar
   * - radar
   * - scatter
   * - spline
   * - step
   * - treemap
   * @name data․type
   * @memberof Options
   * @type {string}
   * @default "line"<br>NOTE: When importing shapes by ESM, `line()` should be specified for type.
   * @example
   * data: {
   *    type: "bar"
   * }
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   bar,
   *   bubble,
   *   candlestick,
   *   donut,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step,
   *   treemap
   * }
   *
   * bb.generate({
   *   ...,
   *   data: {
   *     type: bar()
   *   }
   * });
   */
  data_type: void 0,
  /**
   * Set chart type for each data.<br>
   * This setting overwrites data.type setting.
   * - **NOTE:** `radar` and `treemap` type can't be combined with other types.
   * @name data․types
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   types: {
   *     data1: "bar",
   *     data2: "spline"
   *   }
   * }
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   bar,
   *   bubble,
   *   candlestick,
   *   donut,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step,
   *   treemap
   * }
   *
   * bb.generate({
   *   ...,
   *   data: {
   *     types: {
   *       data1: bar(),
   *       data1: spline()
   *     }
   *   }
   * });
   */
  data_types: {},
  /**
   *  This option changes the order of stacking data and pieces of pie/donut.
   *  - If `null` specified, it will be the order the data loaded.
   *  - If function specified, it will be used as [Array.sort compareFunction](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort#Parameters)<br><br>
   *
   *  **Available Values:**
   *  - `desc`: In descending order
   *  - `asc`: In ascending order
   *  - `null`: It keeps the data load order
   *  - `function(data1, data2) { ... }`: Array.sort compareFunction
   *
   *  **NOTE**: order function, only works for Axis based types & Arc types, except `Radar` type.
   * @name data․order
   * @memberof Options
   * @type {string|Function|null}
   * @default desc
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataOrder)
   * @example
   * data: {
   *   // in descending order (default)
   *   order: "desc"
   *
   *   // in ascending order
   *   order: "asc"
   *
   *   // keeps data input order
   *   order: null
   *
   *   // specifying sort function
   *   order: function(a, b) {
   *       // param data passed format
   *       // {
   *       //   id: "data1", id_org: "data1", values: [
   *       //      {x: 5, value: 250, id: "data1", index: 5, name: "data1"},
   *       //       ...
   *       //   ]
   *       // }
   *
   *       const reducer = (p, c) => p + Math.abs(c.value);
   *       const aSum = a.values.reduce(reducer, 0);
   *       const bSum = b.values.reduce(reducer, 0);
   *
   *       // ascending order
   *       return aSum - bSum;
   *
   *       // descending order
   *       // return bSum - aSum;
   *   }
   * }
   */
  data_order: "desc",
  /**
   * Set groups for the data for stacking.
   * @name data․groups
   * @memberof Options
   * @type {Array}
   * @default []
   * @example
   * data: {
   *   groups: [
   *     ["data1", "data2"],
   *     ["data3"]
   *   ]
   * }
   */
  data_groups: [],
  /**
   * Set how zero value will be treated on groups.<br>
   * Possible values:
   * - `zero`: 0 will be positioned at absolute axis zero point.
   * - `positive`: 0 will be positioned at the top of a stack.
   * - `negative`: 0 will be positioned at the bottom of a stack.
   * @name data․groupsZeroAs
   * @memberof Options
   * @type {string}
   * @default "positive"
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.Groups)
   * @example
   * data: {
   *   groupsZeroAs: "zero" // "positive" or "negative"
   * }
   */
  data_groupsZeroAs: "positive",
  /**
   * Set color converter function.<br><br>
   * This option should a function and the specified function receives color (e.g. '#ff0000') and d that has data parameters like id, value, index, etc. And it must return a string that represents color (e.g. '#00ff00').
   * @name data․color
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataColor)
   * @example
   * data: {
   *   color: function(color, d) { ... }
   * }
   */
  data_color: void 0,
  /**
   * Set color for each data.
   * @name data․colors
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   colors: {
   *     data1: "#ff0000",
   *     data2: function(d) {
   *        return "#000";
   *     }
   *     ...
   *   }
   * }
   */
  data_colors: {},
  /**
   * Set labels options
   * @name data․labels
   * @memberof Options
   * @type {object}
   * @property {object} data Data object
   * @property {boolean} [data.labels=false] Show or hide labels on each data points
   * @property {boolean} [data.labels.centered=false] Centerize labels on `bar` shape. (**NOTE:** works only for 'bar' type)
   * @property {Function} [data.labels.format] Set formatter function for data labels.<br>
   * The formatter function receives 4 arguments such as `v, id, i, texts` and it **must return a string** (`\n` character will be used as line break) that will be shown as the label.<br><br>
   * The arguments are:<br>
   *  - `v` is the value of the data point where the label is shown.
   *  - `id` is the id of the data where the label is shown.
   *  - `i` is the index of the data series point where the label is shown.
   *  - `texts` is the array of whole corresponding data series' text labels.<br><br>
   * Formatter function can be defined for each data by specifying as an object and D3 formatter function can be set (ex. d3.format('$'))
   * @property {string|object} [data.labels.backgroundColors] Set label text background colors.
   * @property {string|object|Function} [data.labels.colors] Set label text colors.
   * @property {object|Function} [data.labels.position] Set each dataset position, relative the original.<br><br>
   * When function is specified, will receives 5 arguments such as `type, v, id, i, texts` and it must return a position number.<br><br>
   * The arguments are:<br>
   *  - `type` coordinate type string, which will be 'x' or 'y'.
   *  - `v` is the value of the data point where the label is shown.
   *  - `id` is the id of the data where the label is shown.
   *  - `i` is the index of the data series point where the label is shown.
   *  - `texts` is the array of whole corresponding data series' text labels.<br><br>
   * @property {number} [data.labels.position.x=0] x coordinate position, relative the original.
   * @property {number} [data.labels.position.y=0] y coordinate position, relative the original.
   * @property {object} [data.labels.rotate] Rotate label text. Specify degree value in a range of `0 ~ 360`.
   * - **NOTE:** Depend on rotate value, text position need to be adjusted manually(using `data.labels.position` option) to be shown nicely.
   * @memberof Options
   * @type {object}
   * @default {}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataLabel)
   * @see [Demo: label colors](https://naver.github.io/billboard.js/demo/#Data.DataLabelColors)
   * @see [Demo: label format](https://naver.github.io/billboard.js/demo/#Data.DataLabelFormat)
   * @see [Demo: label multiline](https://naver.github.io/billboard.js/demo/#Data.DataLabelMultiline)
   * @see [Demo: label overlap](https://naver.github.io/billboard.js/demo/#Data.DataLabelOverlap)
   * @see [Demo: label position](https://naver.github.io/billboard.js/demo/#Data.DataLabelPosition)
   * @see [Demo: label rotate](https://naver.github.io/billboard.js/demo/#Data.DataLabelRotate)
   * @example
   * data: {
   *   labels: true,
   *
   *   // or set specific options
   *   labels: {
   *     format: function(v, id, i, texts) {
   *         ...
   *         // to multiline, return with '\n' character
   *         return "Line1\nLine2";
   *     },
   *
   *     // it's possible to set for each data
   *     format: {
   *         data1: function(v, id, i, texts) { ... },
   *         ...
   *     },
   *
   *     // align text to center of the 'bar' shape (works only for 'bar' type)
   *     centered: true,
   *
   *     // apply backgound color for label texts
   *     backgroundColors: "red",
   *
   *     // set differenct backround colors per dataset
   *     backgroundColors: {
   *          data1: "green",
   *          data2: "yellow"
   *     }
   *
   *     // apply for all label texts
   *     colors: "red",
   *
   *     // set different colors per dataset
   *     // for not specified dataset, will have the default color value
   *     colors: {
   *        data1: "yellow",
   *        data3: "green"
   *     },
   *
   *     // call back for label text color
   *     colors: function(color, d) {
   *         // color: the default data label color string
   *         // data: ex) {x: 0, value: 200, id: "data3", index: 0}
   *         ....
   *         return d.value > 200 ? "cyan" : color;
   *     },
   *
   *     // return x, y coordinate position
   *     // apt to handle each text position manually
   *     position: function(type, v, id, i, texts) {
   *         ...
   *         return type == "x" ? 10 : 20;
   *     },
   *
   *     // set x, y coordinate position
   *     position: {
   *        x: -10,
   *        y: 10
   *     },
   *
   *     // or set x, y coordinate position by each dataset
   *     position: {
   *        data1: {x: 5, y: 5},
   *        data2: {x: 10, y: -20}
   *     },
   *
   *	   // rotate degree for label text
   *     rotate: 90
   *   }
   * }
   */
  data_labels: {},
  data_labels_backgroundColors: void 0,
  data_labels_colors: void 0,
  data_labels_position: {},
  /**
   * Hide each data when the chart appears.<br><br>
   * If true specified, all of data will be hidden. If multiple ids specified as an array, those will be hidden.
   * @name data․hide
   * @memberof Options
   * @type {boolean|Array}
   * @default false
   * @example
   * data: {
   *   // all of data will be hidden
   *   hide: true
   *
   *   // specified data will be hidden
   *   hide: ["data1", ...]
   * }
   */
  data_hide: false,
  /**
   * Filter values to be shown
   * The data value is the same as the returned by `.data()`.
   * @see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/filter
   * @name data․filter
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * data: {
   *   // filter for id value
   *   filter: function(v) {
   *      // v: [{id: "data1", id_org: "data1", values: [
   *      //      {x: 0, value: 130, id: "data2", index: 0}, ...]
   *      //    }, ...]
   *      return v.id !== "data1";
   *   }
   */
  data_filter: void 0,
  /**
   * Set a callback for click event on each data point.<br><br>
   * This callback will be called when each data point clicked and will receive `d` and element as the arguments.
   * - `d` is the data clicked and element is the element clicked.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onclick
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onclick: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4, name: "data1"}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onclick: function() {
  },
  /**
   * Set a callback for mouse/touch over event on each data point.<br><br>
   * This callback will be called when mouse cursor or via touch moves onto each data point and will receive `d` and `element` as the argument.
   * - `d` is the data where mouse cursor moves onto.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onover
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onover: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onover: function() {
  },
  /**
   * Set a callback for mouse/touch out event on each data point.<br><br>
   * This callback will be called when mouse cursor or via touch moves out each data point and will receive `d` as the argument.
   * - `d` is the data where mouse cursor moves out.
   * - `element` is the current interacting svg element.
   * - In this callback, `this` will be the Chart object.
   * @name data․onout
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onout: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4}
   *        // element - <circle>
   *        ...
   *     }
   * }
   */
  data_onout: function() {
  },
  /**
   * Set a callback for when data is shown.<br>
   * The callback will receive shown data ids in array.
   * @name data․onshown
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   *  data: {
   *    onshown: function(ids) {
   *      // ids - ["data1", "data2", ...]
   *      ...
   *    }
   *  }
   */
  data_onshown: void 0,
  /**
   * Set a callback for when data is hidden.<br>
   * The callback will receive hidden data ids in array.
   * @name data․onhidden
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   *  data: {
   *    onhidden: function(ids) {
   *      // ids - ["data1", "data2", ...]
   *      ...
   *    }
   *  }
   */
  data_onhidden: void 0,
  /**
   * Set a callback for minimum data
   * - **NOTE:** For 'area-line-range' and 'area-spline-range', `mid` data will be taken for the comparison
   * @name data․onmin
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.OnMinMaxCallback)
   * @example
   *  onmin: function(data) {
   *    // data - ex) [{x: 3, value: 400, id: "data1", index: 3}, ... ]
   *    ...
   *  }
   */
  data_onmin: void 0,
  /**
   * Set a callback for maximum data
   * - **NOTE:** For 'area-line-range' and 'area-spline-range', `mid` data will be taken for the comparison
   * @name data․onmax
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.OnMinMaxCallback)
   * @example
   *  onmax: function(data) {
   *    // data - ex) [{x: 3, value: 400, id: "data1", index: 3}, ... ]
   *    ...
   *  }
   */
  data_onmax: void 0,
  /**
   * Load a CSV or JSON file from a URL. NOTE that this will not work if loading via the "file://" protocol as the most browsers will block XMLHTTPRequests.
   * @name data․url
   * @memberof Options
   * @type {string}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.LoadData)
   * @example
   * data: {
   *     url: "/data/test.csv"
   * }
   */
  data_url: void 0,
  /**
   * XHR header value
   * - **NOTE:** Should be used with `data.url` option
   * @name data․headers
   * @memberof Options
   * @type {string}
   * @default undefined
   * @see https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest/setRequestHeader
   * @example
   * data: {
   *     url: "/data/test.csv",
   *     headers: {
   *        "Content-Type": "text/xml",
   *        ...
   *     }
   * }
   */
  data_headers: void 0,
  /**
   * Parse a JSON object for data. See also data.keys.
   * @name data․json
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [data․keys](#.data%25E2%2580%25A4keys)
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.JSONData)
   * @example
   * data: {
   *     json: [
   *       {name: "www.site1.com", upload: 200, download: 200, total: 400},
   *       {name: "www.site2.com", upload: 100, download: 300, total: 400},
   *       {name: "www.site3.com", upload: 300, download: 200, total: 500},
   *       {name: "www.site4.com", upload: 400, download: 100, total: 500}
   *     ],
   *     keys: {
   *       // case 1: specify 'x' key for category axis
   *       x: "name", // 'name' key will be used as category x axis values
   *       value: ["upload", "download"]
   *
   *       // case 2: without 'x' key for non-category axis
   *       value: ["upload", "download"]
   *     }
   * }
   */
  data_json: void 0,
  /**
   * Load data from a multidimensional array, with the first element containing the data names, the following containing related data in that order.
   * @name data․rows
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.RowOrientedData)
   * @example
   * data: {
   *   rows: [
   *     ["A", "B", "C"],
   *     [90, 120, 300],
   *     [40, 160, 240],
   *     [50, 200, 290],
   *     [120, 160, 230],
   *     [80, 130, 300],
   *     [90, 220, 320]
   *   ]
   * }
   *
   * // for 'bar' type, data can contain:
   * // - an array of [start, end] data following the order
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [[100, 150], 120],
   *      [[200, 300], 55],
   *      [[-400, 500], 60]
   *   ],
   *   type: "bar"
   * }
   *
   * // for 'range' types('area-line-range' or 'area-spline-range'), data should contain:
   * // - an array of [high, mid, low] data following the order
   * // - or an object with 'high', 'mid' and 'low' key value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [
   *        // or {high:150, mid: 140, low: 110}, 120
   *        [150, 140, 110], 120
   *      ],
   *      [[155, 130, 115], 55],
   *      [[160, 135, 120], 60]
   *   ],
   *   types: {
   *       data1: "area-line-range",
   *       data2: "line"
   *   }
   * }
   *
   * // for 'bubble' type, data can contain dimension value:
   * // - an array of [y, z] data following the order
   * // - or an object with 'y' and 'z' key value
   * // 'y' is for y axis coordination and 'z' is the bubble radius value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *      [
   *        // or {y:10, z: 140}, 120
   *        [10, 140], 120
   *      ],
   *      [[100, 30], 55],
   *      [[50, 100], 60]
   *   ],
   *   types: {
   *       data1: "bubble",
   *       data2: "line"
   *   }
   * }
   *
   * // for 'canlestick' type, data should contain:
   * // - an array of [open, high, low, close, volume(optional)] data following the order
   * // - or an object with 'open', 'high', 'low', 'close' and 'value'(optional) key value
   * data: {
   *   rows: [
   *      ["data1", "data2"],
   *		[
   *			// open, high, low, close, volume (optional)
   *			{open: 1300, high: 1369, low: 1200, close: 1339, volume: 100},
   *			[1000, 1100, 850, 870]
   *		],
   *		[
   *			{open: 1348, high: 1371, low: 1271, close: 1320},
   *			[870, 1250, 830, 1200, 50]
   *		]
   *   ],
   *   type: "candlestick"
   * }
   */
  data_rows: void 0,
  /**
   * Load data from a multidimensional array, with each element containing an array consisting of a datum name and associated data values.
   * @name data․columns
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.ColumnOrientedData)
   * @example
   * data: {
   *   columns: [
   *      ["data1", 30, 20, 50, 40, 60, 50],
   *      ["data2", 200, 130, 90, 240, 130, 220],
   *      ["data3", 300, 200, 160, 400, 250, 250]
   *   ]
   * }
   *
   * // for 'bar' type, data can contain:
   * // - an array of [start, end] data following the order
   * data: {
   *   columns: [
   *     ["data1", -100, 50, [100, 200], [200, 300]],
   *     ["data2", -200, 300, [-100, 100], [-50, -30]],
   *   ],
   *   type: "bar"
   * }
   *
   * // for 'range' types('area-line-range' or 'area-spline-range'), data should contain:
   * // - an array of [high, mid, low] data following the order
   * // - or an object with 'high', 'mid' and 'low' key value
   * data: {
   *   columns: [
   *      ["data1",
   *          [150, 140, 110],  // or {high:150, mid: 140, low: 110}
   *          [150, 140, 110],
   *          [150, 140, 110]
   *      ]
   *   ],
   *   type: "area-line-range"
   * }
   *
   * // for 'bubble' type, data can contain dimension value:
   * // - an array of [y, z] data following the order
   * // - or an object with 'y' and 'z' key value
   * // 'y' is for y axis coordination and 'z' is the bubble radius value
   * data: {
   *   columns: [
   *      ["data1",
   *          [10, 140],  // or {y:10, z: 140}
   *          [100, 30],
   *          [50, 100]
   *      ]
   *   ],
   *   type: "bubble"
   * }
   *
   * // for 'canlestick' type, data should contain:
   * // - an array of [open, high, low, close, volume(optional)] data following the order
   * // - or an object with 'open', 'high', 'low', 'close' and 'value'(optional) key value
   * data: {
   *   columns: [
   *      ["data1",
   *          [1000, 1100, 850, 870, 100],  // or {open:1000, high: 1100, low: 870, volume: 100}
   *          [870, 1250, 830, 1200]  // 'volume' can be omitted
   *      ]
   *   ],
   *   type: "candlestick"
   * }
   */
  data_columns: void 0,
  /**
   * Used if loading JSON via data.url.
   * - **Available Values:**
   *   - json
   *   - csv
   *   - tsv
   * @name data․mimeType
   * @memberof Options
   * @type {string}
   * @default csv
   * @example
   * data: {
   *     mimeType: "json"
   * }
   */
  data_mimeType: "csv",
  /**
   * Choose which JSON object keys correspond to desired data.
   * - **NOTE:** Only for JSON object given as array.
   * @name data․keys
   * @memberof Options
   * @type {string}
   * @default undefined
   * @example
   * data: {
   *     json: [
   *       {name: "www.site1.com", upload: 200, download: 200, total: 400},
   *       {name: "www.site2.com", upload: 100, download: 300, total: 400},
   *       {name: "www.site3.com", upload: 300, download: 200, total: 500},
   *       {name: "www.site4.com", upload: 400, download: 100, total: 500}
   *     ],
   *     keys: {
   *       // case 1: specify 'x' key for category axis
   *       x: "name", // 'name' key will be used as category x axis values
   *       value: ["upload", "download"]
   *
   *       // case 2: without 'x' key for non-category axis
   *       value: ["upload", "download"]
   *     }
   * }
   */
  data_keys: void 0,
  /**
   * Set text label to be displayed when there's no data to show.
   * - ex. Toggling all visible data to not be shown, unloading all current data, etc.
   * @name data․empty․label․text
   * @memberof Options
   * @type {string}
   * @default ""
   * @example
   * data: {
   *   empty: {
   *     label: {
   *       text: "No Data"
   *     }
   *   }
   * }
   */
  data_empty_label_text: ""
};
var color$1 = {
  /**
   * Set color of the data values
   * @name color
   * @memberof Options
   * @type {object}
   * @property {object} color color object
   * @property {string|object|Function} [color.onover] Set the color value for each data point when mouse/touch onover event occurs.
   * @property {Array|null} [color.pattern=[]] Set custom color pattern. Passing `null` will not set a color for these elements, which requires the usage of custom CSS-based theming to work.
   * @property {Function} [color.tiles] if defined, allows use svg's patterns to fill data area. It should return an array of [SVGPatternElement](https://developer.mozilla.org/en-US/docs/Web/API/SVGPatternElement).
   *  - **NOTE:** The pattern element's id will be defined as `bb-colorize-pattern-$COLOR-VALUE`.<br>
   *    ex. When color pattern value is `['red', '#fff']` and defined 2 patterns,then ids for pattern elements are:<br>
   *    - `bb-colorize-pattern-red`
   *    - `bb-colorize-pattern-fff`
   * @property {object} [color.threshold] color threshold for gauge and tooltip color
   * @property {string} [color.threshold.unit] If set to `value`, the threshold will be based on the data value. Otherwise it'll be based on equation of the `threshold.max` option value.
   * @property {Array} [color.threshold.values] Threshold values for each steps
   * @property {number} [color.threshold.max=100] The base value to determine threshold step value condition. When the given value is 15 and max 10, then the value for threshold is `15*100/10`.
   * @example
   *  color: {
   *      pattern: ["#1f77b4", "#aec7e8", ...],
   *
   *      // Set colors' patterns
   *      // it should return an array of SVGPatternElement
   *      tiles: function() {
   *         var pattern = document.createElementNS("http://www.w3.org/2000/svg", "pattern");
   *         var g = document.createElementNS("http://www.w3.org/2000/svg", "g");
   *         var circle1 = document.createElementNS("http://www.w3.org/2000/svg", "circle");
   *
   *         pattern.setAttribute("patternUnits", "userSpaceOnUse");
   *         pattern.setAttribute("width", "32");
   *         pattern.setAttribute("height", "32");
   *
   *         g.style.fill = "#000";
   *         g.style.opacity = "0.2";
   *
   *         circle1.setAttribute("cx", "3");
   *         circle1.setAttribute("cy", "3");
   *         circle1.setAttribute("r", "3");
   *
   *         g.appendChild(circle1);
   *         pattern.appendChild(g);
   *
   *         return [pattern];
   *      },
   *
   *      // for threshold usage, pattern values should be set for each steps
   *      pattern: ["grey", "green", "yellow", "orange", "red"],
   *      threshold: {
   *          unit: "value",
   *
   *          // when value is 20 => 'green', value is 40 => 'orange' will be set.
   *          values: [10, 20, 30, 40, 50],
   *
   *          // the equation for max:
   *          // - unit == 'value': max => 30
   *          // - unit != 'value': max => value*100/30
   *          max: 30
   *      },
   *
   *      // set all data to 'red'
   *      onover: "red",
   *
   *      // set different color for data
   *      onover: {
   *          data1: "red",
   *          data2: "yellow"
   *      },
   *
   *      // will pass data object to the callback
   *      onover: function(d) {
   *          return d.id === "data1" ? "red" : "green";
   *      }
   *  }
   */
  color_pattern: [],
  color_tiles: void 0,
  color_threshold: {},
  color_onover: void 0
};
var interaction$1 = {
  /**
   * Interaction options
   * @name interaction
   * @memberof Options
   * @type {object}
   * @property {object} interaction Intersection object
   * @property {boolean} [interaction.enabled=true] Indicate if the chart should have interactions.<br>
   *     If `false` is set, all of interactions (showing/hiding tooltip, selection, mouse events, etc) will be disabled.
   * @property {boolean} [interaction.brighten=true] Make brighter for the selected area (ex. 'pie' type data selected area)
   * @property {boolean} [interaction.inputType.mouse=true] enable or disable mouse interaction
   * @property {boolean} [interaction.inputType.touch=true] enable or disable  touch interaction
   * @property {boolean|number} [interaction.inputType.touch.preventDefault=false] enable or disable to call event.preventDefault on touchstart & touchmove event. It's usually used to prevent document scrolling.
   * @see [Demo: touch.preventDefault](https://naver.github.io/billboard.js/demo/#Interaction.PreventScrollOnTouch)
   * @example
   * interaction: {
   *    enabled: false,
   *    brighten: false,
   *    inputType: {
   *        mouse: true,
   *        touch: false
   *
   *        // or declare preventDefault explicitly.
   *        // In this case touch inputType is enabled by default
   *        touch: {
   *            preventDefault: true
   *
   *            // or threshold pixel value (pixel moved from touchstart to touchmove)
   *            preventDefault: 5
   *        }
   *    }
   * }
   */
  interaction_enabled: true,
  interaction_brighten: true,
  interaction_inputType_mouse: true,
  interaction_inputType_touch: {}
};
var legend$2 = {
  /**
   * Legend options
   * @name legend
   * @memberof Options
   * @type {object}
   * @property {object} legend Legend object
   * @property {boolean} [legend.show=true] Show or hide legend.
   * @property {boolean} [legend.hide=false] Hide legend
   *  If true given, all legend will be hidden. If string or array given, only the legend that has the id will be hidden.
   * @property {string|HTMLElement} [legend.contents.bindto=undefined] Set CSS selector or element reference to bind legend items.
   * - **NOTE:** Should be used along with `legend.contents.template`.
   * @property {string|Function} [legend.contents.template="<span style='color:#fff;padding:5px;background-color:{=COLOR}'>{=TITLE}</span>"] Set item's template.<br>
   *  - If set `string` value, within template the 'color' and 'title' can be replaced using template-like syntax string:
   *    - {=COLOR}: data color value
   *    - {=TITLE}: data title value
   *  - If set `function` value, will pass following arguments to the given function:
   *   - title {string}: data's id value
   *   - color {string}: color string
   *   - data {Array}: data array
   * @property {string} [legend.position=bottom] Change the position of legend.<br>
   *  Available values are: `bottom`, `right` and `inset` are supported.
   * @property {object} [legend.inset={anchor: 'top-left',x: 10,y: 0,step: undefined}] Change inset legend attributes.<br>
   *  This option accepts object that has the keys `anchor`, `x`, `y` and `step`.
   *  - **anchor** decides the position of the legend:
   *   - top-left
   *   - top-right
   *   - bottom-left
   *   - bottom-right
   *  - **x** and **y**:
   *   - set the position of the legend based on the anchor.
   *  - **step**:
   *   - defines the max step the legend has (e.g. If 2 set and legend has 3 legend item, the legend 2 columns).
   * @property {boolean} [legend.equally=false] Set to all items have same width size.
   * @property {number} [legend.padding=0] Set padding value
   * @property {boolean} [legend.item.interaction=true] Set legend item interaction.
   *  - **NOTE:**
   *    - This setting will not have effect on `.toggle()` method.
   *    - `legend.item.onXXX` listener options will work if set, regardless of this option value.
   * @property {boolean} [legend.item.interaction.dblclick=false] Set legend item to interact on double click.
   *  - **NOTE:**
   *    - Double clicking will make focused clicked dataseries only, hiding all others.
   *      - for single click case, `click + altKey(Win)/optionKey(Mac OS)` to have same effect.
   *    - To return initial state(which all dataseries are showing), double click current focused legend item again.
   *      - for single click case, `click + altKey(Win)/optionKey(Mac OS)` to have same effect.
   *    - In this case, default `click` interaction will be disabled.
   * @property {Function} [legend.item.onclick=undefined] Set click event handler to the legend item.
   *  - **NOTE:**
   *    - When set, default `click` interaction will be disabled.
   *    - When `interaction.dblclick=true` is set, will be called on double click.
   * @property {Function} [legend.item.onover=undefined] Set mouse/touch over event handler to the legend item.
   *  - **NOTE:** When set, default `mouseover` interaction will be disabled.
   * @property {Function} [legend.item.onout=undefined] Set mouse/touch out event handler to the legend item.
   *  - **NOTE:** When set, default `mouseout` interaction will be disabled.
   * @property {number} [legend.item.tile.width=10] Set width for 'rectangle' legend item tile element.
   * @property {number} [legend.item.tile.height=10] Set height for 'rectangle' legend item tile element.
   * @property {number} [legend.item.tile.r=5] Set the radius for 'circle' legend item tile type.
   * @property {string} [legend.item.tile.type="rectangle"] Set legend item shape type.<br>
   * - **Available Values:**
   *   - circle
   *   - rectangle
   * @property {boolean} [legend.format] Set formatter function for legend text.
   * The argument:<br>
   *  - `id`: legend text value. When `data.names` is specified, will pass from it, otherwise will pass data id.
   * @property {boolean} [legend.tooltip=false] Show full legend text value using system tooltip(via `<title>` element).
   * @property {boolean} [legend.usePoint=false] Whether to use custom points in legend.
   * @see [Demo: format](https://naver.github.io/billboard.js/demo/#Legend.LegendFormat)
   * @see [Demo: item.interaction](https://naver.github.io/billboard.js/demo/#Legend.LegendItemInteraction)
   * @see [Demo: item.tile.type](https://naver.github.io/billboard.js/demo/#Legend.LegendItemTileType)
   * @see [Demo: position](https://naver.github.io/billboard.js/demo/#Legend.LegendPosition)
   * @see [Demo: contents.template](https://naver.github.io/billboard.js/demo/#Legend.LegendTemplate1)
   * @see [Demo: usePoint](https://naver.github.io/billboard.js/demo/#Legend.usePoint)
   * @example
   *  legend: {
   *      show: true,
   *      hide: true,
   *      //or hide: "data1"
   *      //or hide: ["data1", "data2"]
   *      contents: {
   *          bindto: "#legend",   // <ul id='legend'></ul>
   *
   *          // will be as: <li style='background-color:#1f77b4'>data1</li>
   *          template: "<li style='background-color:{=COLOR}'>{=TITLE}</li>"
   *
   *          // or using function
   *          template: function(id, color, data) {
   *               // if you want omit some legend, return falsy value
   *               if (id !== "data1") {
   *                    return "<li style='background-color:"+ color +">"+ id +"</li>";
   *               }
   *          }
   *      },
   *      position: "bottom",  // bottom, right, inset
   *      inset: {
   *          anchor: "top-right"  // top-left, top-right, bottom-left, bottom-right
   *          x: 20,
   *          y: 10,
   *          step: 2
   *      },
   *      equally: false,
   *      padding: 10,
   *      item: {
   *          // will disable default interaction
   *          interaction: false,
   *
   *          // set legend interact on double click
   *          // by double clicking, will make focused clicked dataseries only, hiding all others.
   *          interaction: {
   *            dblclick: true
   *          }
   *
   *          // when set below callback, will disable corresponding default interactions
   *          onclick: function(id) { ... },
   *          onover: function(id) { ... },
   *          onout: function(id) { ... },
   *
   *          // set tile's size
   *          tile: {
   *              // set tile type
   *              type: "circle"  // or "rectangle" (default)
   *
   *              // width & height, are only applicable for 'rectangle' legend type
   *              width: 15,
   *              height: 15
   *
   *              // radis is only applicable for 'circle' legend type
   *              r: 10
   *          }
   *      },
   *      format: function(id) {
   *          // set ellipsis string when length is > 5
   *          // to get full legend value, combine with 'legend.tooltip=true'
   *          if (id.length > 5) {
   *            	id = id.substr(0, 5) + "...";
   *          }
   *
   *          return id;
   *      },
   *      tooltip: true,
   *      usePoint: true
   *  }
   */
  legend_contents_bindto: void 0,
  legend_contents_template: "<span style='color:#fff;padding:5px;background-color:{=COLOR}'>{=TITLE}</span>",
  legend_equally: false,
  legend_hide: false,
  legend_inset_anchor: "top-left",
  legend_inset_x: 10,
  legend_inset_y: 0,
  legend_inset_step: void 0,
  legend_item_interaction: true,
  legend_item_dblclick: false,
  legend_item_onclick: void 0,
  legend_item_onover: void 0,
  legend_item_onout: void 0,
  legend_item_tile_width: 10,
  legend_item_tile_height: 10,
  legend_item_tile_r: 5,
  legend_item_tile_type: "rectangle",
  legend_format: void 0,
  legend_padding: 0,
  legend_position: "bottom",
  legend_show: true,
  legend_tooltip: false,
  legend_usePoint: false
};
var title$1 = {
  /**
   * Set title options
   * @name title
   * @memberof Options
   * @type {object}
   * @property {object} title Title object
   * @property {string} [title.text] Title text. If contains `\n`, it's used as line break allowing multiline title.
   * @property {number} [title.padding.top=0] Top padding value.
   * @property {number} [title.padding.right=0] Right padding value.
   * @property {number} [title.padding.bottom=0] Bottom padding value.
   * @property {number} [title.padding.left=0] Left padding value.
   * @property {string} [title.position=center] Available values are: 'center', 'right' and 'left'.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Title.MultilinedTitle)
   * @example
   *  title: {
   *      text: "Title Text",
   *
   *      // or Multiline title text
   *      text: "Main title text\nSub title text",
   *
   *      padding: {
   *          top: 10,
   *          right: 10,
   *          bottom: 10,
   *          left: 10
   *      },
   *      position: "center"
   *  }
   */
  title_text: void 0,
  title_padding: {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  },
  title_position: "center"
};
var tooltip$2 = {
  /**
   * Tooltip options
   * @name tooltip
   * @memberof Options
   * @type {object}
   * @property {object} tooltip Tooltip object
   * @property {boolean} [tooltip.show=true] Show or hide tooltip.
   * @property {boolean} [tooltip.doNotHide=false] Make tooltip keep showing not hiding on interaction.
   * @property {boolean} [tooltip.grouped=true] Set if tooltip is grouped or not for the data points.
   *   - **NOTE:** The overlapped data points will be displayed as grouped even if set false.
   * @property {boolean} [tooltip.linked=false] Set if tooltips on all visible charts with like x points are shown together when one is shown.
   * @property {string} [tooltip.linked.name=""] Groping name for linked tooltip.<br>If specified, linked tooltip will be groped interacting to be worked only with the same name.
   * @property {Function} [tooltip.format.title] Set format for the title of tooltip.<br>
   *  Specified function receives x of the data point to show.
   * @property {Function} [tooltip.format.name] Set format for the name of each data in tooltip.<br>
   *  Specified function receives name, ratio, id and index of the data point to show. ratio will be undefined if the chart is not donut/pie/gauge.
   * @property {Function} [tooltip.format.value] Set format for the value of each data value in tooltip. If undefined returned, the row of that value will be skipped to be called.
   *  - Will pass following arguments to the given function:
   *    - `value {string}`: Value of the data point. If data row contains multiple or ranged(ex. candlestick, area range, etc.) value, formatter will be called as value length.
   *    - `ratio {number}`: Ratio of the data point in the `pie/donut/gauge` and `area/bar` when contains grouped data. Otherwise is `undefined`.
   *    - `id {string}`: id of the data point
   *    - `index {number}`: Index of the data point
   * @property {Function} [tooltip.position] Set custom position function for the tooltip.<br>
   *  This option can be used to modify the tooltip position by returning object that has top and left.
   *  - Will pass following arguments to the given function:
   *    - `data {Array}`: Current selected data array object.
   *    - `width {number}`: Width of tooltip.
   *    - `height {number}`: Height of tooltip.
   *    - `element {SVGElement}`: Tooltip event bound element
   *    - `pos {object}`: Current position of the tooltip.
   * @property {Function|object} [tooltip.contents] Set custom HTML for the tooltip.<br>
   *  If tooltip.grouped is true, data includes multiple data points.<br><br>
   *  Specified function receives `data` array and `defaultTitleFormat`, `defaultValueFormat` and `color` functions of the data point to show.
   *  - **Note:**
   *    - defaultTitleFormat:
   *      - if `axis.x.tick.format` option will be used if set.
   *      - otherwise, will return function based on tick format type(category, timeseries).
   *    - defaultValueFormat:
   *	    - for Arc type (except gauge, radar), the function will return value from `(ratio * 100).toFixed(1)`.
   *	    - for Axis based types, will be used `axis.[y|y2].tick.format` option value if is set.
   *	    - otherwise, will parse value and return as number.
   * @property {string|HTMLElement} [tooltip.contents.bindto=undefined] Set CSS selector or element reference to bind tooltip.
   *  - **NOTE:** When is specified, will not be updating tooltip's position.
   * @property {string} [tooltip.contents.template=undefined] Set tooltip's template.<br><br>
   *  Within template, below syntax will be replaced using template-like syntax string:
   *    - **{{ ... }}**: the doubly curly brackets indicate loop block for data rows.
   *    - **{=CLASS_TOOLTIP}**: default tooltip class name `bb-tooltip`.
   *    - **{=CLASS_TOOLTIP_NAME}**: default tooltip data class name (ex. `bb-tooltip-name-data1`)
   *    - **{=TITLE}**: title value.
   *    - **{=COLOR}**: data color.
   *    - **{=NAME}**: data id value.
   *    - **{=VALUE}**: data value.
   * @property {object} [tooltip.contents.text=undefined] Set additional text content within data loop, using template syntax.
   *  - **NOTE:** It should contain `{ key: Array, ... }` value
   *    - 'key' name is used as substitution within template as '{=KEY}'
   *    - The value array length should match with the data length
   * @property {boolean} [tooltip.init.show=false] Show tooltip at the initialization.
   * @property {number} [tooltip.init.x=0] Set x Axis index(or index for Arc(donut, gauge, pie) types) to be shown at the initialization.
   * @property {object} [tooltip.init.position] Set the position of tooltip at the initialization.
   * @property {Function} [tooltip.onshow] Set a callback that will be invoked before the tooltip is shown.
   * @property {Function} [tooltip.onhide] Set a callback that will be invoked before the tooltip is hidden.
   * @property {Function} [tooltip.onshown] Set a callback that will be invoked after the tooltip is shown
   * @property {Function} [tooltip.onhidden] Set a callback that will be invoked after the tooltip is hidden.
   * @property {string|Function|null} [tooltip.order=null] Set tooltip data display order.<br><br>
   *  **Available Values:**
   *  - `desc`: In descending data value order
   *  - `asc`: In ascending data value order
   *  - `null`: It keeps the data display order<br>
   *     **NOTE:** When `data.groups` is set, the order will follow as the stacked graph order.<br>
   *      If want to order as data bound, set any value rather than asc, desc or null. (ex. empty string "")
   *  - `function(data1, data2) { ... }`: [Array.sort compareFunction](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort#Parameters)
   * @see [Demo: Hide Tooltip](https://naver.github.io/billboard.js/demo/#Tooltip.HideTooltip)
   * @see [Demo: Tooltip Grouping](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipGrouping)
   * @see [Demo: Tooltip Format](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipFormat)
   * @see [Demo: Linked Tooltip](https://naver.github.io/billboard.js/demo/#Tooltip.LinkedTooltips)
   * @see [Demo: Tooltip Position](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipPosition)
   * @see [Demo: Tooltip Template](https://naver.github.io/billboard.js/demo/#Tooltip.TooltipTemplate)
   * @example
   *  tooltip: {
   *      show: true,
   *      doNotHide: true,
   *      grouped: false,
   *      format: {
   *          title: function(x) { return "Data " + x; },
   *          name: function(name, ratio, id, index) { return name; },
   *
   *          // If data row contains multiple or ranged(ex. candlestick, area range, etc.) value,
   *          // formatter will be called as value length times.
   *          value: function(value, ratio, id, index) { return ratio; }
   *      },
   *      position: function(data, width, height, element, pos) {
   *          // data: [{x, index, id, name, value}, ...]
   *          // width: Tooltip width
   *          // height: Tooltip height
   *          // element: Tooltip event bound element
   *          // pos: {
   *          //   x: Current mouse event x position,
   *          //   y: Current mouse event y position,
   *          //   xAxis: Current x Axis position (the value is given for axis based chart type only)
   *          //   yAxis: Current y Axis position value or function(the value is given for axis based chart type only)
   *          // }
   *
   *          // yAxis will work differently per data lenghts
   *          // - a) Single data: `yAxis` will return `number` value
   *          // - b) Multiple data: `yAxis` will return a function with property value
   *
   *          // a) Single data:
   *          // Get y coordinate
   *          pos.yAxis; // y axis coordinate value of current data point
   *
   *          // b) Multiple data:
   *          // Get y coordinate of value 500, where 'data1' scales(y or y2).
   *          // When 'data.axes' option is used, data can bound to different axes.
   *          // - when "data.axes={data1: 'y'}", wil return y value from y axis scale.
   *          // - when "data.axes={data1: 'y2'}", wil return y value from y2 axis scale.
   *          pos.yAxis(500, "data1"); // will return y coordinate value of data1
   *
   *          pos.yAxis(500); // get y coordinate with value of 500, using y axis scale
   *          pos.yAxis(500, null, "y2"); // get y coordinate with value of 500, using y2 axis scale
   *
   *          return {
   *            top: 0,
   *            left: 0
   *          }
   *      },
   *
   *      contents: function(d, defaultTitleFormat, defaultValueFormat, color) {
   *          return ... // formatted html as you want
   *      },
   *
   *       // specify tooltip contents using template
   *       // - example of HTML returned:
   *       // <ul class="bb-tooltip">
   *       //   <li class="bb-tooltip-name-data1"><span>250</span><br><span style="color:#00c73c">data1</span></li>
   *       //   <li class="bb-tooltip-name-data2"><span>50</span><br><span style="color:#fa7171">data2</span></li>
   *       // </ul>
   *       contents: {
   *      	bindto: "#tooltip",
   *      	template: '<ul class={=CLASS_TOOLTIP}>{{' +
   *      			'<li class="{=CLASS_TOOLTIP_NAME}"><span>{=VALUE}</span><br>' +
   *      			'<span style=color:{=COLOR}>{=NAME}</span></li>' +
   *      		'}}</ul>'
   *      }
   *
   *       // with additional text value
   *       // - example of HTML returned:
   *       // <ul class="bb-tooltip">
   *       //   <li class="bb-tooltip-name-data1"><span>250</span><br>comment1<span style="color:#00c73c">data1</span>text1</li>
   *       //   <li class="bb-tooltip-name-data2"><span>50</span><br>comment2<span style="color:#fa7171">data2</span>text2</li>
   *       // </ul>
   *       contents: {
   *      	bindto: "#tooltip",
   *      	text: {
   *      		// a) 'key' name is used as substitution within template as '{=KEY}'
   *      		// b) the length should match with the data length
   *      		VAR1: ["text1", "text2"],
   *      		VAR2: ["comment1", "comment2"],
   *      	},
   *      	template: '<ul class={=CLASS_TOOLTIP}>{{' +
   *      			'<li class="{=CLASS_TOOLTIP_NAME}"><span>{=VALUE}</span>{=VAR2}<br>' +
   *      			'<span style=color:{=COLOR}>{=NAME}</span>{=VAR1}</li>' +
   *      		'}}</ul>'
   *      }
   *
   *      // sort tooltip data value display in ascending order
   *      order: "asc",
   *
   *      // specifying sort function
   *      order: function(a, b) {
   *         // param data passed format
   *         {x: 5, value: 250, id: "data1", index: 5, name: "data1"}
   *           ...
   *      },
   *
   *      // show at the initialization
   *      init: {
   *          show: true,
   *          x: 2, // x Axis index (or index for Arc(donut, gauge, pie) types)
   *          position: {
   *              top: "150px",  // specify as number or as string with 'px' unit string
   *              left: 250  // specify as number or as string with 'px' unit string
   *          }
   *      },
   *
   *      // fires prior tooltip is shown
   *      onshow: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires prior tooltip is hidden
   *      onhide: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires after tooltip is shown
   *      onshown: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // fires after tooltip is hidden
   *      onhidden: function(selectedData) {
   *      	// current dataset selected
   *      	// ==> [{x: 4, value: 150, id: "data2", index: 4, name: "data2"}, ...]
   *      	selectedData;
   *      },
   *
   *      // Link any tooltips when multiple charts are on the screen where same x coordinates are available
   *      // Useful for timeseries correlation
   *      linked: true,
   *
   *      // Specify name to interact those with the same name only.
   *      linked: {
   *          name: "some-group"
   *      }
   *  }
   */
  tooltip_show: true,
  tooltip_doNotHide: false,
  tooltip_grouped: true,
  tooltip_format_title: void 0,
  tooltip_format_name: void 0,
  tooltip_format_value: void 0,
  tooltip_position: void 0,
  tooltip_contents: {},
  tooltip_init_show: false,
  tooltip_init_x: 0,
  tooltip_init_position: void 0,
  tooltip_linked: false,
  tooltip_linked_name: "",
  tooltip_onshow: function() {
  },
  tooltip_onhide: function() {
  },
  tooltip_onshown: function() {
  },
  tooltip_onhidden: function() {
  },
  tooltip_order: null
};
var Options = (
  /** @class */
  function() {
    function Options2() {
      return deepClone(main, boost, data$2, color$1, interaction$1, legend$2, title$1, tooltip$2, Options2.data);
    }
    Options2.setOptions = function(options2) {
      this.data = options2.reduce(function(a2, c) {
        return _assign(_assign({}, a2), c);
      }, this.data);
    };
    Options2.data = {};
    return Options2;
  }()
);
var KEY = {
  bubbleBaseLength: "$baseLength",
  colorPattern: "__colorPattern__",
  dataMinMax: "$dataMinMax",
  dataTotalSum: "$dataTotalSum",
  dataTotalPerIndex: "$totalPerIndex",
  legendItemTextBox: "legendItemTextBox",
  radarPoints: "$radarPoints",
  radarTextWidth: "$radarTextWidth",
  setOverOut: "setOverOut",
  callOverOutForTouch: "callOverOutForTouch",
  textRect: "textRect"
};
var Cache = (
  /** @class */
  function() {
    function Cache2() {
      this.cache = {};
    }
    Cache2.prototype.add = function(key, value, isDataType) {
      if (isDataType === void 0) {
        isDataType = false;
      }
      this.cache[key] = isDataType ? this.cloneTarget(value) : value;
      return this.cache[key];
    };
    Cache2.prototype.remove = function(key) {
      var _this = this;
      (isString(key) ? [key] : key).forEach(function(v) {
        return delete _this.cache[v];
      });
    };
    Cache2.prototype.get = function(key, isDataType) {
      if (isDataType === void 0) {
        isDataType = false;
      }
      if (isDataType && Array.isArray(key)) {
        var targets = [];
        for (var i = 0, id2 = void 0; id2 = key[i]; i++) {
          if (id2 in this.cache) {
            targets.push(this.cloneTarget(this.cache[id2]));
          }
        }
        return targets;
      } else {
        var value = this.cache[key];
        return isValue(value) ? value : null;
      }
    };
    Cache2.prototype.reset = function(all) {
      var $$ = this;
      for (var x3 in $$.cache) {
        if (all || /^\$/.test(x3)) {
          $$.cache[x3] = null;
        }
      }
    };
    Cache2.prototype.cloneTarget = function(target) {
      return {
        id: target.id,
        id_org: target.id_org,
        values: target.values.map(function(d) {
          return { x: d.x, value: d.value, id: d.id };
        })
      };
    };
    return Cache2;
  }()
);
var setTimeout$1 = win.setTimeout;
var clearTimeout$1 = win.clearTimeout;
function generateResize(option) {
  var fn = [];
  var timeout2;
  var callResizeFn = function() {
    callResizeFn.clear();
    if (option === false) {
      requestIdleCallback(function() {
        fn.forEach(function(f) {
          return f();
        });
      }, { timeout: 200 });
    } else {
      timeout2 = setTimeout$1(function() {
        fn.forEach(function(f) {
          return f();
        });
      }, isNumber(option) ? option : 200);
    }
  };
  callResizeFn.clear = function() {
    if (timeout2) {
      clearTimeout$1(timeout2);
      timeout2 = null;
    }
  };
  callResizeFn.add = function(f) {
    return fn.push(f);
  };
  callResizeFn.remove = function(f) {
    return fn.splice(fn.indexOf(f), 1);
  };
  return callResizeFn;
}
function generateWait() {
  var transitionsToWait = [];
  var f = function(selection3, callback) {
    function loop() {
      var _a2;
      var done = 0;
      for (var i = 0, t = void 0; t = transitionsToWait[i]; i++) {
        if (t === true || ((_a2 = t.empty) === null || _a2 === void 0 ? void 0 : _a2.call(t))) {
          done++;
          continue;
        }
        if (isTabVisible() === false) {
          done = transitionsToWait.length;
          break;
        }
        try {
          t.transition();
        } catch (e) {
          done++;
        }
      }
      return done === transitionsToWait.length;
    }
    runUntil(function() {
      callback === null || callback === void 0 ? void 0 : callback();
    }, loop);
  };
  f.add = function(t) {
    isArray(t) ? transitionsToWait = transitionsToWait.concat(t) : transitionsToWait.push(t);
  };
  return f;
}
var blob = {};
function getObjectURL(fn, depsFn) {
  var _a2;
  var fnString = fn.toString();
  var key = fnString.replace(/(function|[\s\W\n])/g, "").substring(0, 15);
  if (!(key in blob)) {
    blob[key] = new win.Blob([
      "".concat((_a2 = depsFn === null || depsFn === void 0 ? void 0 : depsFn.map(String).join(";")) !== null && _a2 !== void 0 ? _a2 : "", "\n\n			self.onmessage=function({data}) {\n				const result = (").concat(fnString, ").apply(null, data);\n				self.postMessage(result);\n			};")
    ], {
      type: "text/javascript"
    });
  }
  return win.URL.createObjectURL(blob[key]);
}
function getWorker(src) {
  var worker = new win.Worker(src);
  worker.onerror = function(e) {
    console.error ? console.error(e) : console.log(e);
  };
  return worker;
}
function runWorker(useWorker, fn, callback, depsFn) {
  if (useWorker === void 0) {
    useWorker = true;
  }
  var runFn = function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    var res = fn.apply(void 0, args);
    callback(res);
  };
  if (win.Worker && useWorker) {
    var src_1 = getObjectURL(fn, depsFn);
    var worker_1 = getWorker(src_1);
    runFn = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      worker_1.postMessage(args);
      worker_1.onmessage = function(e) {
        win.URL.revokeObjectURL(src_1);
        return callback(e.data);
      };
    };
  }
  return runFn;
}
function columns(columns2) {
  var newRows = [];
  columns2.forEach(function(col, i) {
    var key = col[0];
    col.forEach(function(v, j) {
      if (j > 0) {
        if (typeof newRows[j - 1] === "undefined") {
          newRows[j - 1] = {};
        }
        if (typeof v === "undefined") {
          throw new Error("Source data is missing a component at (".concat(i, ", ").concat(j, ")!"));
        }
        newRows[j - 1][key] = v;
      }
    });
  });
  return newRows;
}
function rows(rows2) {
  var keys = rows2[0];
  var newRows = [];
  rows2.forEach(function(row, i) {
    if (i > 0) {
      var newRow_1 = {};
      row.forEach(function(v, j) {
        if (typeof v === "undefined") {
          throw new Error("Source data is missing a component at (".concat(i, ", ").concat(j, ")!"));
        }
        newRow_1[keys[j]] = v;
      });
      newRows.push(newRow_1);
    }
  });
  return newRows;
}
function json(json2, keysParam) {
  var newRows = [];
  var targetKeys;
  var data2;
  if (Array.isArray(json2)) {
    var findValueInJson_1 = function(object, path2) {
      if (object[path2] !== void 0) {
        return object[path2];
      }
      var convertedPath = path2.replace(/\[(\w+)\]/g, ".$1");
      var pathArray = convertedPath.replace(/^\./, "").split(".");
      var target = object;
      pathArray.some(function(k2) {
        return !(target = target && k2 in target ? target[k2] : void 0);
      });
      return target;
    };
    if (keysParam.x) {
      targetKeys = keysParam.value.concat(keysParam.x);
    } else {
      targetKeys = keysParam.value;
    }
    newRows.push(targetKeys);
    json2.forEach(function(o) {
      var newRow = targetKeys.map(function(key) {
        var v = findValueInJson_1(o, key);
        if (typeof v === "undefined") {
          v = null;
        }
        return v;
      });
      newRows.push(newRow);
    });
    data2 = rows(newRows);
  } else {
    Object.keys(json2).forEach(function(key) {
      var _a2;
      var tmp = json2[key].concat();
      (_a2 = tmp.unshift) === null || _a2 === void 0 ? void 0 : _a2.call(tmp, key);
      newRows.push(tmp);
    });
    data2 = columns(newRows);
  }
  return data2;
}
function url(url2, mimeType, headers, keys, done) {
  if (mimeType === void 0) {
    mimeType = "csv";
  }
  var req = new XMLHttpRequest();
  var converter = { csv: csv2, tsv: tsv2, json };
  req.open("GET", url2);
  if (headers) {
    Object.keys(headers).forEach(function(key) {
      req.setRequestHeader(key, headers[key]);
    });
  }
  req.onreadystatechange = function() {
    if (req.readyState === 4) {
      if (req.status === 200) {
        var response = req.responseText;
        response && done.call(this, converter[mimeType](mimeType === "json" ? JSON.parse(response) : response, keys));
      } else {
        throw new Error("".concat(url2, ": Something went wrong loading!"));
      }
    }
  };
  req.send();
}
function convertCsvTsvToData(parser, xsv) {
  var rows2 = parser.rows(xsv);
  var d;
  if (rows2.length === 1) {
    d = [{}];
    rows2[0].forEach(function(id2) {
      d[0][id2] = null;
    });
  } else {
    d = parser.parse(xsv);
  }
  return d;
}
function csv2(xsv) {
  return convertCsvTsvToData({
    rows: csvParseRows,
    parse: csvParse
  }, xsv);
}
function tsv2(tsv3) {
  return convertCsvTsvToData({
    rows: tsvParseRows,
    parse: tsvParse
  }, tsv3);
}
function getDataKeyForJson(keysParam, config) {
  var keys = keysParam || (config === null || config === void 0 ? void 0 : config.data_keys);
  if (keys === null || keys === void 0 ? void 0 : keys.x) {
    config.data_x = keys.x;
  }
  return keys;
}
var dataConvert = {
  /**
   * Convert data according its type
   * @param {object} args data object
   * @param {Function} [callback] callback for url(XHR) type loading
   * @private
   */
  convertData: function(args, callback) {
    var config = this.config;
    var useWorker = config.boost_useWorker;
    var data2 = args;
    if (args.bindto) {
      data2 = {};
      ["url", "mimeType", "headers", "keys", "json", "keys", "rows", "columns"].forEach(function(v) {
        var key = "data_".concat(v);
        if (key in args) {
          data2[v] = args[key];
        }
      });
    }
    if (data2.url && callback) {
      url(data2.url, data2.mimeType, data2.headers, getDataKeyForJson(data2.keys, config), callback);
    } else if (data2.json) {
      runWorker(useWorker, json, callback, [columns, rows])(data2.json, getDataKeyForJson(data2.keys, config));
    } else if (data2.rows) {
      runWorker(useWorker, rows, callback)(data2.rows);
    } else if (data2.columns) {
      runWorker(useWorker, columns, callback)(data2.columns);
    } else if (args.bindto) {
      throw Error("url or json or rows or columns is required.");
    }
  },
  convertDataToTargets: function(data2, appendXs) {
    var _this = this;
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, state = $$.state;
    var chartType = config.data_type;
    var isCategorized = false;
    var isTimeSeries = false;
    var isCustomX = false;
    if (axis3) {
      isCategorized = axis3.isCategorized();
      isTimeSeries = axis3.isTimeSeries();
      isCustomX = axis3.isCustomX();
    }
    var dataKeys = Object.keys(data2[0] || {});
    var ids = dataKeys.length ? dataKeys.filter($$.isNotX, $$) : [];
    var xs = dataKeys.length ? dataKeys.filter($$.isX, $$) : [];
    var xsData;
    ids.forEach(function(id2) {
      var xKey = _this.getXKey(id2);
      if (isCustomX || isTimeSeries) {
        if (xs.indexOf(xKey) >= 0) {
          xsData = (appendXs && $$.data.xs[id2] || []).concat(data2.map(function(d) {
            return d[xKey];
          }).filter(isValue).map(function(rawX, i) {
            return $$.generateTargetX(rawX, id2, i);
          }));
        } else if (config.data_x) {
          xsData = _this.getOtherTargetXs();
        } else if (notEmpty(config.data_xs)) {
          xsData = $$.getXValuesOfXKey(xKey, $$.data.targets);
        }
      } else {
        xsData = data2.map(function(d, i) {
          return i;
        });
      }
      xsData && (_this.data.xs[id2] = xsData);
    });
    ids.forEach(function(id2) {
      if (!_this.data.xs[id2]) {
        throw new Error('x is not defined for id = "'.concat(id2, '".'));
      }
    });
    var targets = ids.map(function(id2, index2) {
      var convertedId = config.data_idConverter.bind($$.api)(id2);
      var xKey = $$.getXKey(id2);
      var isCategory = isCustomX && isCategorized;
      var hasCategory = isCategory && data2.map(function(v) {
        return v.x;
      }).every(function(v) {
        return config.axis_x_categories.indexOf(v) > -1;
      });
      var isDataAppend = data2.__append__;
      var xIndex = xKey === null && isDataAppend ? $$.api.data.values(id2).length : 0;
      return {
        id: convertedId,
        id_org: id2,
        values: data2.map(function(d, i) {
          var rawX = d[xKey];
          var value = d[id2];
          var x3;
          value = value !== null && !isNaN(value) && !isObject(value) ? +value : isArray(value) || isObject(value) ? value : null;
          if ((isCategory || state.hasRadar) && index2 === 0 && !isUndefined(rawX)) {
            if (!hasCategory && index2 === 0 && i === 0 && !isDataAppend) {
              config.axis_x_categories = [];
            }
            x3 = config.axis_x_categories.indexOf(rawX);
            if (x3 === -1) {
              x3 = config.axis_x_categories.length;
              config.axis_x_categories.push(rawX);
            }
          } else {
            x3 = $$.generateTargetX(rawX, id2, xIndex + i);
          }
          if (isUndefined(value) || $$.data.xs[id2].length <= i) {
            x3 = void 0;
          }
          return {
            x: x3,
            value,
            id: convertedId,
            index: -1
          };
        }).filter(function(v) {
          return isDefined(v.x);
        })
      };
    });
    targets.forEach(function(t) {
      var _a2;
      if (config.data_xSort) {
        t.values = t.values.sort(function(v1, v2) {
          var x1 = v1.x || v1.x === 0 ? v1.x : Infinity;
          var x22 = v2.x || v2.x === 0 ? v2.x : Infinity;
          return x1 - x22;
        });
      }
      t.values.forEach(function(v, i) {
        return v.index = i;
      });
      (_a2 = $$.data.xs[t.id]) === null || _a2 === void 0 ? void 0 : _a2.sort(function(v1, v2) {
        return v1 - v2;
      });
    });
    state.hasNegativeValue = $$.hasNegativeValueInTargets(targets);
    state.hasPositiveValue = $$.hasPositiveValueInTargets(targets);
    if (chartType && $$.isValidChartType(chartType)) {
      var targetIds = $$.mapToIds(targets).filter(function(id2) {
        return !(id2 in config.data_types) || !$$.isValidChartType(config.data_types[id2]);
      });
      $$.setTargetType(targetIds, chartType);
    }
    targets.forEach(function(d) {
      return $$.cache.add(d.id_org, d, true);
    });
    return targets;
  }
};
var data$1 = {
  isX: function(key) {
    var $$ = this;
    var config = $$.config;
    var dataKey = config.data_x && key === config.data_x;
    var existValue = notEmpty(config.data_xs) && hasValue(config.data_xs, key);
    return dataKey || existValue;
  },
  isNotX: function(key) {
    return !this.isX(key);
  },
  isStackNormalized: function() {
    var config = this.config;
    return !!(config.data_stack_normalize && config.data_groups.length);
  },
  /**
   * Check if given id is grouped data or has grouped data
   * @param {string} id Data id value
   * @returns {boolean} is grouped data or has grouped data
   * @private
   */
  isGrouped: function(id2) {
    var groups2 = this.config.data_groups;
    return id2 ? groups2.some(function(v) {
      return v.indexOf(id2) >= 0 && v.length > 1;
    }) : groups2.length > 0;
  },
  getXKey: function(id2) {
    var $$ = this;
    var config = $$.config;
    return config.data_x ? config.data_x : notEmpty(config.data_xs) ? config.data_xs[id2] : null;
  },
  getXValuesOfXKey: function(key, targets) {
    var $$ = this;
    var ids = targets && notEmpty(targets) ? $$.mapToIds(targets) : [];
    var xValues;
    ids.forEach(function(id2) {
      if ($$.getXKey(id2) === key) {
        xValues = $$.data.xs[id2];
      }
    });
    return xValues;
  },
  /**
   * Get index number based on given x Axis value
   * @param {Date|number|string} x x Axis to be compared
   * @param {Array} basedX x Axis list to be based on
   * @returns {number} index number
   * @private
   */
  getIndexByX: function(x3, basedX) {
    var $$ = this;
    return basedX ? basedX.indexOf(isString(x3) ? x3 : +x3) : ($$.filterByX($$.data.targets, x3)[0] || { index: null }).index;
  },
  getXValue: function(id2, i) {
    var $$ = this;
    return id2 in $$.data.xs && $$.data.xs[id2] && isValue($$.data.xs[id2][i]) ? $$.data.xs[id2][i] : i;
  },
  getOtherTargetXs: function() {
    var $$ = this;
    var idsForX = Object.keys($$.data.xs);
    return idsForX.length ? $$.data.xs[idsForX[0]] : null;
  },
  getOtherTargetX: function(index2) {
    var xs = this.getOtherTargetXs();
    return xs && index2 < xs.length ? xs[index2] : null;
  },
  addXs: function(xs) {
    var $$ = this;
    var config = $$.config;
    Object.keys(xs).forEach(function(id2) {
      config.data_xs[id2] = xs[id2];
    });
  },
  isMultipleX: function() {
    return notEmpty(this.config.data_xs) || this.hasType("bubble") || this.hasType("scatter");
  },
  addName: function(data2) {
    var $$ = this;
    var config = $$.config;
    var name;
    if (data2) {
      name = config.data_names[data2.id];
      data2.name = name !== void 0 ? name : data2.id;
    }
    return data2;
  },
  /**
   * Get all values on given index
   * @param {number} index Index
   * @param {boolean} filterNull Filter nullish value
   * @returns {Array}
   * @private
   */
  getAllValuesOnIndex: function(index2, filterNull) {
    if (filterNull === void 0) {
      filterNull = false;
    }
    var $$ = this;
    var value = $$.filterTargetsToShow($$.data.targets).map(function(t) {
      return $$.addName($$.getValueOnIndex(t.values, index2));
    });
    if (filterNull) {
      value = value.filter(function(v) {
        return v && "value" in v && isValue(v.value);
      });
    }
    return value;
  },
  getValueOnIndex: function(values, index2) {
    var valueOnIndex = values.filter(function(v) {
      return v.index === index2;
    });
    return valueOnIndex.length ? valueOnIndex[0] : null;
  },
  updateTargetX: function(targets, x3) {
    var $$ = this;
    targets.forEach(function(t) {
      t.values.forEach(function(v, i) {
        v.x = $$.generateTargetX(x3[i], t.id, i);
      });
      $$.data.xs[t.id] = x3;
    });
  },
  updateTargetXs: function(targets, xs) {
    var $$ = this;
    targets.forEach(function(t) {
      xs[t.id] && $$.updateTargetX([t], xs[t.id]);
    });
  },
  generateTargetX: function(rawX, id2, index2) {
    var $$ = this;
    var axis3 = $$.axis;
    var x3 = (axis3 === null || axis3 === void 0 ? void 0 : axis3.isCategorized()) ? index2 : rawX || index2;
    if (axis3 === null || axis3 === void 0 ? void 0 : axis3.isTimeSeries()) {
      var fn = parseDate.bind($$);
      x3 = rawX ? fn(rawX) : fn($$.getXValue(id2, index2));
    } else if ((axis3 === null || axis3 === void 0 ? void 0 : axis3.isCustomX()) && !(axis3 === null || axis3 === void 0 ? void 0 : axis3.isCategorized())) {
      x3 = isValue(rawX) ? +rawX : $$.getXValue(id2, index2);
    }
    return x3;
  },
  updateXs: function(values) {
    if (values.length) {
      this.axis.xs = values.map(function(v) {
        return v.x;
      });
    }
  },
  getPrevX: function(i) {
    var x3 = this.axis.xs[i - 1];
    return isDefined(x3) ? x3 : null;
  },
  getNextX: function(i) {
    var x3 = this.axis.xs[i + 1];
    return isDefined(x3) ? x3 : null;
  },
  /**
   * Get base value isAreaRangeType
   * @param {object} data Data object
   * @returns {number}
   * @private
   */
  getBaseValue: function(data2) {
    var $$ = this;
    var hasAxis = $$.state.hasAxis;
    var value = data2.value;
    if (value && hasAxis) {
      if ($$.isAreaRangeType(data2)) {
        value = $$.getRangedData(data2, "mid");
      } else if ($$.isBubbleZType(data2)) {
        value = $$.getBubbleZData(value, "y");
      }
    }
    return value;
  },
  /**
   * Get min/max value from the data
   * @private
   * @param {Array} data array data to be evaluated
   * @returns {{min: {number}, max: {number}}}
   */
  getMinMaxValue: function(data2) {
    var getBaseValue = this.getBaseValue.bind(this);
    var min4;
    var max4;
    (data2 || this.data.targets.map(function(t) {
      return t.values;
    })).forEach(function(v, i) {
      var value = v.map(getBaseValue).filter(isNumber);
      min4 = Math.min.apply(Math, __spreadArray([i ? min4 : Infinity], value, false));
      max4 = Math.max.apply(Math, __spreadArray([i ? max4 : -Infinity], value, false));
    });
    return { min: min4, max: max4 };
  },
  /**
   * Get the min/max data
   * @private
   * @returns {{min: Array, max: Array}}
   */
  getMinMaxData: function() {
    var $$ = this;
    var cacheKey = KEY.dataMinMax;
    var minMaxData = $$.cache.get(cacheKey);
    if (!minMaxData) {
      var data2 = $$.data.targets.map(function(t) {
        return t.values;
      });
      var minMax_1 = $$.getMinMaxValue(data2);
      var min_1 = [];
      var max_1 = [];
      data2.forEach(function(v) {
        var minData = $$.getFilteredDataByValue(v, minMax_1.min);
        var maxData = $$.getFilteredDataByValue(v, minMax_1.max);
        if (minData.length) {
          min_1 = min_1.concat(minData);
        }
        if (maxData.length) {
          max_1 = max_1.concat(maxData);
        }
      });
      $$.cache.add(cacheKey, minMaxData = { min: min_1, max: max_1 });
    }
    return minMaxData;
  },
  /**
   * Get sum of data per index
   * @private
   * @returns {Array}
   */
  getTotalPerIndex: function() {
    var $$ = this;
    var cacheKey = KEY.dataTotalPerIndex;
    var sum3 = $$.cache.get(cacheKey);
    if (($$.config.data_groups.length || $$.isStackNormalized()) && !sum3) {
      sum3 = [];
      $$.data.targets.forEach(function(row) {
        row.values.forEach(function(v, i) {
          if (!sum3[i]) {
            sum3[i] = 0;
          }
          sum3[i] += isNumber(v.value) ? v.value : 0;
        });
      });
    }
    return sum3;
  },
  /**
   * Get total data sum
   * @param {boolean} subtractHidden Subtract hidden data from total
   * @returns {number}
   * @private
   */
  getTotalDataSum: function(subtractHidden) {
    var $$ = this;
    var cacheKey = KEY.dataTotalSum;
    var total = $$.cache.get(cacheKey);
    if (!isNumber(total)) {
      var sum3 = mergeArray($$.data.targets.map(function(t) {
        return t.values;
      })).map(function(v) {
        return v.value;
      });
      total = sum3.length ? sum3.reduce(function(p, c) {
        return p + c;
      }) : 0;
      $$.cache.add(cacheKey, total);
    }
    if (subtractHidden) {
      total -= $$.getHiddenTotalDataSum();
    }
    return total;
  },
  /**
   * Get total hidden data sum
   * @returns {number}
   * @private
   */
  getHiddenTotalDataSum: function() {
    var $$ = this;
    var api2 = $$.api, hiddenTargetIds = $$.state.hiddenTargetIds;
    var total = 0;
    if (hiddenTargetIds.length) {
      total = api2.data.values.bind(api2)(hiddenTargetIds).reduce(function(p, c) {
        return p + c;
      });
    }
    return total;
  },
  /**
   * Get filtered data by value
   * @param {object} data Data
   * @param {number} value Value to be filtered
   * @returns {Array} filtered array data
   * @private
   */
  getFilteredDataByValue: function(data2, value) {
    var _this = this;
    return data2.filter(function(t) {
      return _this.getBaseValue(t) === value;
    });
  },
  /**
   * Return the max length of the data
   * @returns {number} max data length
   * @private
   */
  getMaxDataCount: function() {
    return Math.max.apply(Math, __spreadArray(__spreadArray([], this.data.targets.map(function(t) {
      return t.values.length;
    }), false), [0], false));
  },
  getMaxDataCountTarget: function() {
    var target = this.filterTargetsToShow() || [];
    var length = target.length;
    var isInverted = this.config.axis_x_inverted;
    if (length > 1) {
      target = target.map(function(t) {
        return t.values;
      }).reduce(function(a2, b) {
        return a2.concat(b);
      }).map(function(v) {
        return v.x;
      });
      target = sortValue(getUnique(target)).map(function(x3, index2, array3) {
        return { x: x3, index: isInverted ? array3.length - index2 - 1 : index2 };
      });
    } else if (length) {
      target = target[0].values.concat();
    }
    return target;
  },
  mapToIds: function(targets) {
    return targets.map(function(d) {
      return d.id;
    });
  },
  mapToTargetIds: function(ids) {
    var $$ = this;
    return ids ? isArray(ids) ? ids.concat() : [ids] : $$.mapToIds($$.data.targets);
  },
  hasTarget: function(targets, id2) {
    var ids = this.mapToIds(targets);
    for (var i = 0, val = void 0; val = ids[i]; i++) {
      if (val === id2) {
        return true;
      }
    }
    return false;
  },
  isTargetToShow: function(targetId) {
    return this.state.hiddenTargetIds.indexOf(targetId) < 0;
  },
  isLegendToShow: function(targetId) {
    return this.state.hiddenLegendIds.indexOf(targetId) < 0;
  },
  filterTargetsToShow: function(targets) {
    var $$ = this;
    return (targets || $$.data.targets).filter(function(t) {
      return $$.isTargetToShow(t.id);
    });
  },
  mapTargetsToUniqueXs: function(targets) {
    var $$ = this;
    var axis3 = $$.axis;
    var xs = [];
    if (targets === null || targets === void 0 ? void 0 : targets.length) {
      xs = getUnique(mergeArray(targets.map(function(t) {
        return t.values.map(function(v) {
          return +v.x;
        });
      })));
      xs = (axis3 === null || axis3 === void 0 ? void 0 : axis3.isTimeSeries()) ? xs.map(function(x3) {
        return /* @__PURE__ */ new Date(+x3);
      }) : xs.map(Number);
    }
    return sortValue(xs);
  },
  /**
   * Add to the state target Ids
   * @param {string} type State's prop name
   * @param {Array|string} targetIds Target ids array
   * @private
   */
  addTargetIds: function(type2, targetIds) {
    var state = this.state;
    var ids = isArray(targetIds) ? targetIds : [targetIds];
    ids.forEach(function(v) {
      state[type2].indexOf(v) < 0 && state[type2].push(v);
    });
  },
  /**
   * Remove from the state target Ids
   * @param {string} type State's prop name
   * @param {Array|string} targetIds Target ids array
   * @private
   */
  removeTargetIds: function(type2, targetIds) {
    var state = this.state;
    var ids = isArray(targetIds) ? targetIds : [targetIds];
    ids.forEach(function(v) {
      var index2 = state[type2].indexOf(v);
      index2 >= 0 && state[type2].splice(index2, 1);
    });
  },
  addHiddenTargetIds: function(targetIds) {
    this.addTargetIds("hiddenTargetIds", targetIds);
  },
  removeHiddenTargetIds: function(targetIds) {
    this.removeTargetIds("hiddenTargetIds", targetIds);
  },
  addHiddenLegendIds: function(targetIds) {
    this.addTargetIds("hiddenLegendIds", targetIds);
  },
  removeHiddenLegendIds: function(targetIds) {
    this.removeTargetIds("hiddenLegendIds", targetIds);
  },
  getValuesAsIdKeyed: function(targets) {
    var $$ = this;
    var hasAxis = $$.state.hasAxis;
    var ys = {};
    var isMultipleX = $$.isMultipleX();
    var xs = isMultipleX ? $$.mapTargetsToUniqueXs(targets).map(function(v) {
      return isString(v) ? v : +v;
    }) : null;
    targets.forEach(function(t) {
      var data2 = [];
      t.values.filter(function(_a2) {
        var value = _a2.value;
        return isValue(value) || value === null;
      }).forEach(function(v) {
        var value = v.value;
        if (value !== null && $$.isCandlestickType(v)) {
          value = isArray(value) ? value.slice(0, 4) : [value.open, value.high, value.low, value.close];
        }
        if (isArray(value)) {
          data2.push.apply(data2, value);
        } else if (isObject(value) && "high" in value) {
          data2.push.apply(data2, Object.values(value));
        } else if ($$.isBubbleZType(v)) {
          data2.push(hasAxis && $$.getBubbleZData(value, "y"));
        } else {
          if (isMultipleX) {
            data2[$$.getIndexByX(v.x, xs)] = value;
          } else {
            data2.push(value);
          }
        }
      });
      ys[t.id] = data2;
    });
    return ys;
  },
  checkValueInTargets: function(targets, checker) {
    var ids = Object.keys(targets);
    var values;
    for (var i = 0; i < ids.length; i++) {
      values = targets[ids[i]].values;
      for (var j = 0; j < values.length; j++) {
        if (checker(values[j].value)) {
          return true;
        }
      }
    }
    return false;
  },
  hasMultiTargets: function() {
    return this.filterTargetsToShow().length > 1;
  },
  hasNegativeValueInTargets: function(targets) {
    return this.checkValueInTargets(targets, function(v) {
      return v < 0;
    });
  },
  hasPositiveValueInTargets: function(targets) {
    return this.checkValueInTargets(targets, function(v) {
      return v > 0;
    });
  },
  /**
   * Sort targets data
   * Note: For stacked bar, will sort from the total sum of data series, not for each stacked bar
   * @param {Array} targetsValue Target value
   * @returns {Array}
   * @private
   */
  orderTargets: function(targetsValue) {
    var $$ = this;
    var targets = __spreadArray([], targetsValue, true);
    var fn = $$.getSortCompareFn();
    fn && targets.sort(fn);
    return targets;
  },
  /**
   * Get data.order compare function
   * @param {boolean} isReversed for Arc & Treemap type sort order needs to be reversed
   * @returns {Function} compare function
   * @private
   */
  getSortCompareFn: function(isReversed) {
    if (isReversed === void 0) {
      isReversed = false;
    }
    var $$ = this;
    var config = $$.config;
    var order = config.data_order;
    var orderAsc = /asc/i.test(order);
    var orderDesc = /desc/i.test(order);
    var fn;
    if (orderAsc || orderDesc) {
      var reducer_1 = function(p, c) {
        return p + Math.abs(c.value);
      };
      var sum_1 = function(v) {
        return isNumber(v) ? v : "values" in v ? v.values.reduce(reducer_1, 0) : v.value;
      };
      fn = function(t13, t22) {
        var t1Sum = sum_1(t13);
        var t2Sum = sum_1(t22);
        return isReversed ? orderAsc ? t1Sum - t2Sum : t2Sum - t1Sum : orderAsc ? t2Sum - t1Sum : t1Sum - t2Sum;
      };
    } else if (isFunction(order)) {
      fn = order.bind($$.api);
    }
    return fn || null;
  },
  filterByX: function(targets, x3) {
    return mergeArray(targets.map(function(t) {
      return t.values;
    })).filter(function(v) {
      return v.x - x3 === 0;
    });
  },
  filterRemoveNull: function(data2) {
    var _this = this;
    return data2.filter(function(d) {
      return isValue(_this.getBaseValue(d));
    });
  },
  filterByXDomain: function(targets, xDomain) {
    return targets.map(function(t) {
      return {
        id: t.id,
        id_org: t.id_org,
        values: t.values.filter(function(v) {
          return xDomain[0] <= v.x && v.x <= xDomain[1];
        })
      };
    });
  },
  hasDataLabel: function() {
    var dataLabels = this.config.data_labels;
    return isboolean(dataLabels) && dataLabels || isObjectType(dataLabels) && notEmpty(dataLabels);
  },
  /**
   * Get data index from the event coodinates
   * @param {Event} event Event object
   * @returns {number}
   */
  getDataIndexFromEvent: function(event) {
    var $$ = this;
    var $el = $$.$el, config = $$.config, _a2 = $$.state, hasRadar = _a2.hasRadar, inputType = _a2.inputType, _b = _a2.eventReceiver, coords = _b.coords, rect = _b.rect;
    var index2;
    if (hasRadar) {
      var target = event.target;
      if (/tspan/i.test(target.tagName)) {
        target = target.parentNode;
      }
      var d = select_default2(target).datum();
      index2 = d && Object.keys(d).length === 1 ? d.index : void 0;
    } else {
      var isRotated = config.axis_rotated;
      var scrollPos = getScrollPosition($el.chart.node());
      var e = inputType === "touch" && event.changedTouches ? event.changedTouches[0] : event;
      index2 = findIndex(coords, isRotated ? e.clientY + scrollPos.y - rect.top : e.clientX + scrollPos.x - rect.left, 0, coords.length - 1, isRotated);
    }
    return index2;
  },
  getDataLabelLength: function(min4, max4, key) {
    var $$ = this;
    var lengths = [0, 0];
    var paddingCoef = 1.3;
    $$.$el.chart.select("svg").selectAll(".dummy").data([min4, max4]).enter().append("text").text(function(d) {
      return $$.dataLabelFormat(d.id)(d);
    }).each(function(d, i) {
      lengths[i] = this.getBoundingClientRect()[key] * paddingCoef;
    }).remove();
    return lengths;
  },
  isNoneArc: function(d) {
    return this.hasTarget(this.data.targets, d.id);
  },
  isArc: function(d) {
    return "data" in d && this.hasTarget(this.data.targets, d.data.id);
  },
  findSameXOfValues: function(values, index2) {
    var targetX = values[index2].x;
    var sames = [];
    var i;
    for (i = index2 - 1; i >= 0; i--) {
      if (targetX !== values[i].x) {
        break;
      }
      sames.push(values[i]);
    }
    for (i = index2; i < values.length; i++) {
      if (targetX !== values[i].x) {
        break;
      }
      sames.push(values[i]);
    }
    return sames;
  },
  findClosestFromTargets: function(targets, pos) {
    var $$ = this;
    var candidates = targets.map(function(target) {
      return $$.findClosest(target.values, pos);
    });
    return $$.findClosest(candidates, pos);
  },
  findClosest: function(values, pos) {
    var $$ = this;
    var main2 = $$.$el.main;
    var data2 = values.filter(function(v) {
      return v && isValue(v.value);
    });
    var minDist;
    var closest;
    data2.filter(function(v) {
      return $$.isBarType(v.id) || $$.isCandlestickType(v.id);
    }).forEach(function(v) {
      var selector = $$.isBarType(v.id) ? ".".concat($BAR.chartBar, ".").concat($COMMON.target).concat($$.getTargetSelectorSuffix(v.id), " .").concat($BAR.bar, "-").concat(v.index) : ".".concat($CANDLESTICK.chartCandlestick, ".").concat($COMMON.target).concat($$.getTargetSelectorSuffix(v.id), " .").concat($CANDLESTICK.candlestick, "-").concat(v.index, " path");
      if (!closest && $$.isWithinBar(main2.select(selector).node())) {
        closest = v;
      }
    });
    data2.filter(function(v) {
      return !$$.isBarType(v.id) && !$$.isCandlestickType(v.id);
    }).forEach(function(v) {
      var d = $$.dist(v, pos);
      minDist = $$.getPointSensitivity(v);
      if (d < minDist) {
        minDist = d;
        closest = v;
      }
    });
    return closest;
  },
  dist: function(data2, pos) {
    var $$ = this;
    var isRotated = $$.config.axis_rotated, scale2 = $$.scale;
    var xIndex = +isRotated;
    var yIndex = +!isRotated;
    var y3 = $$.circleY(data2, data2.index);
    var x3 = (scale2.zoom || scale2.x)(data2.x);
    return Math.sqrt(Math.pow(x3 - pos[xIndex], 2) + Math.pow(y3 - pos[yIndex], 2));
  },
  /**
   * Convert data for step type
   * @param {Array} values Object data values
   * @returns {Array}
   * @private
   */
  convertValuesToStep: function(values) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config;
    var stepType = config.line_step_type;
    var isCategorized = axis3 ? axis3.isCategorized() : false;
    var converted = isArray(values) ? values.concat() : [values];
    if (!(isCategorized || /step\-(after|before)/.test(stepType))) {
      return values;
    }
    if (converted.length) {
      var head = converted[0];
      var tail = converted[converted.length - 1];
      var id2 = head.id;
      var x3 = head.x;
      converted.unshift({ x: --x3, value: head.value, id: id2 });
      isCategorized && stepType === "step-after" && converted.unshift({ x: --x3, value: head.value, id: id2 });
      x3 = tail.x;
      converted.push({ x: ++x3, value: tail.value, id: id2 });
      isCategorized && stepType === "step-before" && converted.push({ x: ++x3, value: tail.value, id: id2 });
    }
    return converted;
  },
  convertValuesToRange: function(values) {
    var converted = isArray(values) ? values.concat() : [values];
    var ranges = [];
    converted.forEach(function(range2) {
      var x3 = range2.x, id2 = range2.id;
      ranges.push({
        x: x3,
        id: id2,
        value: range2.value[0]
      });
      ranges.push({
        x: x3,
        id: id2,
        value: range2.value[2]
      });
    });
    return ranges;
  },
  updateDataAttributes: function(name, attrs) {
    var $$ = this;
    var config = $$.config;
    var current = config["data_".concat(name)];
    if (isUndefined(attrs)) {
      return current;
    }
    Object.keys(attrs).forEach(function(id2) {
      current[id2] = attrs[id2];
    });
    $$.redraw({ withLegend: true });
    return current;
  },
  getRangedData: function(d, key, type2) {
    if (key === void 0) {
      key = "";
    }
    if (type2 === void 0) {
      type2 = "areaRange";
    }
    var value = d === null || d === void 0 ? void 0 : d.value;
    if (isArray(value)) {
      if (type2 === "bar") {
        return value.reduce(function(a2, c) {
          return c - a2;
        });
      } else {
        var index2 = {
          areaRange: ["high", "mid", "low"],
          candlestick: ["open", "high", "low", "close", "volume"]
        }[type2].indexOf(key);
        return index2 >= 0 && value ? value[index2] : void 0;
      }
    } else if (value && key) {
      return value[key];
    }
    return value;
  },
  /**
   * Set ratio for grouped data
   * @param {Array} data Data array
   * @private
   */
  setRatioForGroupedData: function(data2) {
    var $$ = this;
    var config = $$.config;
    if (config.data_groups.length && data2.some(function(d) {
      return $$.isGrouped(d.id);
    })) {
      var setter_1 = function(d) {
        return $$.getRatio("index", d, true);
      };
      data2.forEach(function(v) {
        "values" in v ? v.values.forEach(setter_1) : setter_1(v);
      });
    }
  },
  /**
   * Get ratio value
   * @param {string} type Ratio for given type
   * @param {object} d Data value object
   * @param {boolean} asPercent Convert the return as percent or not
   * @returns {number} Ratio value
   * @private
   */
  getRatio: function(type2, d, asPercent) {
    if (asPercent === void 0) {
      asPercent = false;
    }
    var $$ = this;
    var config = $$.config, state = $$.state;
    var api2 = $$.api;
    var ratio = 0;
    if (d && api2.data.shown().length) {
      ratio = d.ratio || d.value;
      if (type2 === "arc") {
        if ($$.pie.padAngle()()) {
          ratio = d.value / $$.getTotalDataSum(true);
        } else {
          var gaugeArcLength = config.gauge_fullCircle ? $$.getArcLength() : $$.getStartingAngle() * -2;
          var arcLength = $$.hasType("gauge") ? gaugeArcLength : Math.PI * 2;
          ratio = (d.endAngle - d.startAngle) / arcLength;
        }
      } else if (type2 === "index") {
        var dataValues = api2.data.values.bind(api2);
        var total = this.getTotalPerIndex();
        if (state.hiddenTargetIds.length) {
          var hiddenSum_1 = dataValues(state.hiddenTargetIds, false);
          if (hiddenSum_1.length) {
            hiddenSum_1 = hiddenSum_1.reduce(function(acc, curr) {
              return acc.map(function(v, i) {
                return (isNumber(v) ? v : 0) + curr[i];
              });
            });
            total = total.map(function(v, i) {
              return v - hiddenSum_1[i];
            });
          }
        }
        var divisor = total[d.index];
        d.ratio = isNumber(d.value) && total && divisor ? d.value / divisor : 0;
        ratio = d.ratio;
      } else if (type2 === "radar") {
        ratio = parseFloat(String(Math.max(d.value, 0))) / state.current.dataMax * config.radar_size_ratio;
      } else if (type2 === "bar") {
        var yScale = $$.getYScaleById.bind($$)(d.id);
        var max4 = yScale.domain().reduce(function(a2, c) {
          return c - a2;
        });
        ratio = max4 === 0 ? 0 : Math.abs($$.getRangedData(d, null, type2) / max4);
      } else if (type2 === "treemap") {
        ratio /= $$.getTotalDataSum(true);
      }
    }
    return asPercent && ratio ? ratio * 100 : ratio;
  },
  /**
   * Sort data index to be aligned with x axis.
   * @param {Array} tickValues Tick array values
   * @private
   */
  updateDataIndexByX: function(tickValues) {
    var $$ = this;
    var tickValueMap = tickValues.reduce(function(out, tick, index2) {
      out[Number(tick.x)] = index2;
      return out;
    }, {});
    $$.data.targets.forEach(function(t) {
      t.values.forEach(function(value, valueIndex) {
        var index2 = tickValueMap[Number(value.x)];
        if (index2 === void 0) {
          index2 = valueIndex;
        }
        value.index = index2;
      });
    });
  },
  /**
   * Determine if bubble has dimension data
   * @param {object|Array} d data value
   * @returns {boolean}
   * @private
   */
  isBubbleZType: function(d) {
    var $$ = this;
    return $$.isBubbleType(d) && (isObject(d.value) && ("z" in d.value || "y" in d.value) || isArray(d.value) && d.value.length >= 2);
  },
  /**
   * Determine if bar has ranged data
   * @param {Array} d data value
   * @returns {boolean}
   * @private
   */
  isBarRangeType: function(d) {
    var $$ = this;
    var value = d.value;
    return $$.isBarType(d) && isArray(value) && value.length >= 2 && value.every(function(v) {
      return isNumber(v);
    });
  },
  /**
   * Get data object by id
   * @param {string} id data id
   * @returns {object}
   * @private
   */
  getDataById: function(id2) {
    var _a2;
    var d = this.cache.get(id2) || this.api.data(id2);
    return (_a2 = d === null || d === void 0 ? void 0 : d[0]) !== null && _a2 !== void 0 ? _a2 : d;
  }
};
function callDone(fn, resizeAfter) {
  if (resizeAfter === void 0) {
    resizeAfter = false;
  }
  var $$ = this;
  var api2 = $$.api;
  resizeAfter && $$.api.flush(true);
  fn === null || fn === void 0 ? void 0 : fn.call(api2);
}
var dataLoad = {
  load: function(rawTargets, args) {
    var $$ = this;
    var axis3 = $$.axis, data2 = $$.data, org = $$.org, scale2 = $$.scale;
    var append2 = args.append;
    var zoomState = {
      domain: null,
      currentDomain: null,
      x: null
    };
    var targets = rawTargets;
    if (targets) {
      if (args.filter) {
        targets = targets.filter(args.filter);
      }
      if (args.type || args.types) {
        targets.forEach(function(t) {
          var _a2;
          var type2 = ((_a2 = args.types) === null || _a2 === void 0 ? void 0 : _a2[t.id]) || args.type;
          $$.setTargetType(t.id, type2);
        });
      }
      data2.targets.forEach(function(d) {
        for (var i = 0; i < targets.length; i++) {
          if (d.id === targets[i].id) {
            d.values = append2 ? d.values.concat(targets[i].values) : targets[i].values;
            targets.splice(i, 1);
            break;
          }
        }
      });
      data2.targets = data2.targets.concat(targets);
    }
    $$.updateTargets(data2.targets);
    if (scale2.zoom) {
      zoomState.x = axis3.isCategorized() ? scale2.x.orgScale() : (org.xScale || scale2.x).copy();
      zoomState.domain = $$.getXDomain(data2.targets);
      zoomState.x.domain(zoomState.domain);
      zoomState.currentDomain = $$.zoom.getDomain();
      if (!$$.withinRange(zoomState.currentDomain, void 0, zoomState.domain)) {
        scale2.x.domain(zoomState.domain);
        scale2.zoom = null;
        $$.$el.eventRect.property("__zoom", null);
      }
    }
    $$.redraw({
      withUpdateOrgXDomain: true,
      withUpdateXDomain: true,
      withLegend: true
    });
    if (scale2.zoom) {
      org.xDomain = zoomState.domain;
      org.xScale = zoomState.x;
      if (axis3.isCategorized()) {
        zoomState.currentDomain = $$.getZoomDomainValue(zoomState.currentDomain);
        org.xDomain = $$.getZoomDomainValue(org.xDomain);
        org.xScale = zoomState.x.domain(org.xDomain);
      }
      $$.updateCurrentZoomTransform(zoomState.x, zoomState.currentDomain);
    }
    $$.updateTypesElements();
    callDone.call($$, args.done, args.resizeAfter);
  },
  loadFromArgs: function(args) {
    var $$ = this;
    if (!$$.config) {
      return;
    }
    $$.cache.reset();
    $$.convertData(args, function(d) {
      var data2 = args.data || d;
      args.append && (data2.__append__ = true);
      data2 && $$.load($$.convertDataToTargets(data2), args);
    });
  },
  unload: function(rawTargetIds, customDoneCb) {
    var _a2;
    var $$ = this;
    var state = $$.state, $el = $$.$el, $T = $$.$T;
    var hasLegendDefsPoint = !!((_a2 = $$.hasLegendDefsPoint) === null || _a2 === void 0 ? void 0 : _a2.call($$));
    var done = customDoneCb;
    var targetIds = rawTargetIds;
    $$.cache.reset();
    if (!done) {
      done = function() {
      };
    }
    targetIds = targetIds.filter(function(id2) {
      return $$.hasTarget($$.data.targets, id2);
    });
    if (!targetIds || targetIds.length === 0) {
      done();
      return;
    }
    var targets = $el.svg.selectAll(targetIds.map(function(id2) {
      return $$.selectorTarget(id2);
    }));
    $T(targets).style("opacity", "0").remove().call(endall, done);
    targetIds.forEach(function(id2) {
      var _a3;
      var suffixId = $$.getTargetSelectorSuffix(id2);
      state.withoutFadeIn[id2] = false;
      if ($el.legend) {
        $el.legend.selectAll(".".concat($LEGEND.legendItem).concat(suffixId)).remove();
      }
      $$.data.targets = $$.data.targets.filter(function(t) {
        return t.id !== id2;
      });
      hasLegendDefsPoint && ((_a3 = $el.defs) === null || _a3 === void 0 ? void 0 : _a3.select("#".concat($$.getDefsPointId(suffixId))).remove());
    });
    state.hasTreemap && $$.updateTargetsForTreemap($$.data.targets);
    $$.updateTypesElements();
  }
};
var interaction = {
  /**
   * Expand data shape/point
   * @param {number} index Index number
   * @param {string} id Data id
   * @param {boolean} reset Reset expand state
   * @private
   */
  setExpand: function(index2, id2, reset) {
    var $$ = this;
    var config = $$.config, circle = $$.$el.circle;
    circle && config.point_focus_expand_enabled && $$.expandCircles(index2, id2, reset);
    $$.expandBarTypeShapes(true, index2, id2, reset);
  },
  /**
   * Expand/Unexpand bar type shapes
   * @param {boolean} expand Expand or unexpand
   * @param {number} i Shape index
   * @param {string} id Data id
   * @param {boolean} reset Reset expand style
   * @private
   */
  expandBarTypeShapes: function(expand, i, id2, reset) {
    if (expand === void 0) {
      expand = true;
    }
    var $$ = this;
    ["bar", "candlestick"].filter(function(v) {
      return $$.$el[v];
    }).forEach(function(v) {
      reset && $$.$el[v].classed($COMMON.EXPANDED, false);
      $$.getShapeByIndex(v, i, id2).classed($COMMON.EXPANDED, expand);
    });
  },
  /**
   * Handle data.onover/out callback options
   * @param {boolean} isOver Over or not
   * @param {number|object} d data object
   * @private
   */
  setOverOut: function(isOver, d) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, hasRadar = _a2.hasRadar, hasTreemap = _a2.hasTreemap, main2 = $$.$el.main;
    var isArcTreemap = isObject(d);
    if (isArcTreemap || d !== -1) {
      var callback_1 = config[isOver ? "data_onover" : "data_onout"].bind($$.api);
      config.color_onover && $$.setOverColor(isOver, d, isArcTreemap);
      if (isArcTreemap && "id") {
        var selector = hasTreemap ? $TREEMAP.treemap : $ARC.arc;
        callback_1(d, main2.select(".".concat(selector).concat($$.getTargetSelectorSuffix(d.id))).node());
      } else if (!config.tooltip_grouped) {
        var last_1 = $$.cache.get(KEY.setOverOut) || [];
        var shapesAtIndex = main2.selectAll(".".concat($SHAPE.shape, "-").concat(d)).filter(function(d2) {
          return $$.isWithinShape(this, d2);
        });
        var shape2 = shapesAtIndex.filter(function() {
          var _this = this;
          return last_1.every(function(v) {
            return v !== _this;
          });
        });
        if (!isOver || shapesAtIndex.empty() || last_1.length === shape2.size() && shape2.nodes().every(function(v, i) {
          return v !== last_1[i];
        })) {
          while (last_1.length) {
            var target = last_1.pop();
            config.data_onout.bind($$.api)(select_default2(target).datum(), target);
          }
        }
        shape2.each(function() {
          if (isOver) {
            callback_1(select_default2(this).datum(), this);
            last_1.push(this);
          }
        });
        $$.cache.add(KEY.setOverOut, last_1);
      } else {
        if (isOver) {
          hasRadar && $$.isPointFocusOnly() ? $$.showCircleFocus($$.getAllValuesOnIndex(d, true)) : $$.setExpand(d, null, true);
        }
        !$$.isMultipleX() && main2.selectAll(".".concat($SHAPE.shape, "-").concat(d)).each(function(d2) {
          callback_1(d2, this);
        });
      }
    }
  },
  /**
   * Call data.onover/out callback for touch event
   * @param {number|object} d target index or data object for Arc type
   * @private
   */
  callOverOutForTouch: function(d) {
    var $$ = this;
    var last = $$.cache.get(KEY.callOverOutForTouch);
    if (isObject(d) && last ? d.id !== last.id : d !== last) {
      (last || isNumber(last)) && $$.setOverOut(false, last);
      (d || isNumber(d)) && $$.setOverOut(true, d);
      $$.cache.add(KEY.callOverOutForTouch, d);
    }
  },
  /**
   * Return draggable selection function
   * @returns {Function}
   * @private
   */
  getDraggableSelection: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    return config.interaction_enabled && config.data_selection_draggable && $$.drag ? drag_default().on("drag", function(event) {
      state.event = event;
      $$.drag(getPointer(event, this));
    }).on("start", function(event) {
      state.event = event;
      $$.dragstart(getPointer(event, this));
    }).on("end", function(event) {
      state.event = event;
      $$.dragend();
    }) : function() {
    };
  },
  /**
   * Dispatch a mouse event.
   * @private
   * @param {string} type event type
   * @param {number} index Index of eventRect
   * @param {Array} mouse x and y coordinate value
   */
  dispatchEvent: function(type2, index2, mouse) {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, _c = $$.state, eventReceiver = _c.eventReceiver, hasAxis = _c.hasAxis, hasRadar = _c.hasRadar, hasTreemap = _c.hasTreemap, _d = $$.$el, eventRect = _d.eventRect, radar2 = _d.radar, treemap2 = _d.treemap;
    var element = (_b = hasTreemap && eventReceiver.rect || hasRadar && radar2.axes.select(".".concat($AXIS.axis, "-").concat(index2, " text")) || (eventRect || ((_a2 = $$.getArcElementByIdOrIndex) === null || _a2 === void 0 ? void 0 : _a2.call($$, index2)))) === null || _b === void 0 ? void 0 : _b.node();
    if (element) {
      var isMultipleX = $$.isMultipleX();
      var isRotated = config.axis_rotated;
      var _e = element.getBoundingClientRect(), width = _e.width, left2 = _e.left, top_1 = _e.top;
      if (hasAxis && !hasRadar && !isMultipleX) {
        var coords = eventReceiver.coords[index2];
        if (coords) {
          width = coords.w;
          left2 += coords.x;
          top_1 += coords.y;
        } else {
          width = 0;
          left2 = 0;
          top_1 = 0;
        }
      }
      var x3 = left2 + (mouse ? mouse[0] : 0) + (isMultipleX || isRotated ? 0 : width / 2);
      var y3 = top_1 + (mouse ? mouse[1] : 0) + (isRotated ? 4 : 0);
      var params = {
        screenX: x3,
        screenY: y3,
        clientX: x3,
        clientY: y3,
        bubbles: hasRadar
        // radar type needs to bubble up event
      };
      emulateEvent[/^(mouse|click)/.test(type2) ? "mouse" : "touch"](hasTreemap ? treemap2.node() : element, type2, params);
    }
  },
  setDragStatus: function(isDragging) {
    this.state.dragging = isDragging;
  },
  /**
   * Unbind zoom events
   * @private
   */
  unbindZoomEvent: function() {
    var $$ = this;
    var _a2 = $$.$el, eventRect = _a2.eventRect, zoomResetBtn = _a2.zoomResetBtn;
    eventRect === null || eventRect === void 0 ? void 0 : eventRect.on(".zoom wheel.zoom .drag", null);
    zoomResetBtn === null || zoomResetBtn === void 0 ? void 0 : zoomResetBtn.on("click", null).style("display", "none");
  },
  /**
   * Unbind all attached events
   * @private
   */
  unbindAllEvents: function() {
    var _a2;
    var $$ = this;
    var _b = $$.$el, arcs = _b.arcs, eventRect = _b.eventRect, legend2 = _b.legend, region2 = _b.region, svg = _b.svg, treemap2 = _b.treemap, brush2 = $$.brush;
    var list = [
      "wheel",
      "click",
      "mouseover",
      "mousemove",
      "mouseout",
      "touchstart",
      "touchmove",
      "touchend",
      "touchstart.eventRect",
      "touchmove.eventRect",
      "touchend.eventRect",
      ".brush",
      ".drag",
      ".zoom",
      "wheel.zoom",
      "dblclick.zoom"
    ].join(" ");
    [svg, eventRect, region2 === null || region2 === void 0 ? void 0 : region2.list, brush2 === null || brush2 === void 0 ? void 0 : brush2.getSelection(), arcs === null || arcs === void 0 ? void 0 : arcs.selectAll("path"), legend2 === null || legend2 === void 0 ? void 0 : legend2.selectAll("g"), treemap2].forEach(function(v) {
      return v === null || v === void 0 ? void 0 : v.on(list, null);
    });
    (_a2 = $$.unbindZoomEvent) === null || _a2 === void 0 ? void 0 : _a2.call($$);
  }
};
var classModule = {
  generateClass: function(prefix, targetId) {
    return " ".concat(prefix, " ").concat(prefix + this.getTargetSelectorSuffix(targetId));
  },
  /**
   * Get class string
   * @param {string} type Shape type
   * @param {boolean} withShape Get with shape prefix
   * @returns {string} Class string
   * @private
   */
  getClass: function(type2, withShape) {
    var _this = this;
    var isPlural = /s$/.test(type2);
    var useIdKey = /^(area|arc|line|treemap)s?$/.test(type2);
    var key = isPlural ? "id" : "index";
    return function(d) {
      var data2 = d.data || d;
      var result = (withShape ? _this.generateClass(CLASS[isPlural ? "shapes" : "shape"], data2[key]) : "") + _this.generateClass(CLASS[type2], data2[useIdKey ? "id" : key]);
      return result.trim();
    };
  },
  /**
   * Get chart class string
   * @param {string} type Shape type
   * @returns {string} Class string
   * @private
   */
  getChartClass: function(type2) {
    var _this = this;
    return function(d) {
      return CLASS["chart".concat(type2)] + _this.classTarget((d.data ? d.data : d).id);
    };
  },
  generateExtraLineClass: function() {
    var $$ = this;
    var classes2 = $$.config.line_classes || [];
    var ids = [];
    return function(d) {
      var _a2;
      var id2 = d.id || ((_a2 = d.data) === null || _a2 === void 0 ? void 0 : _a2.id) || d;
      if (ids.indexOf(id2) < 0) {
        ids.push(id2);
      }
      return classes2[ids.indexOf(id2) % classes2.length];
    };
  },
  classRegion: function(d, i) {
    return "".concat(this.generateClass(CLASS.region, i), " ").concat("class" in d ? d.class : "");
  },
  classTarget: function(id2) {
    var additionalClassSuffix = this.config.data_classes[id2];
    var additionalClass = "";
    if (additionalClassSuffix) {
      additionalClass = " ".concat(CLASS.target, "-").concat(additionalClassSuffix);
    }
    return this.generateClass(CLASS.target, id2) + additionalClass;
  },
  classFocus: function(d) {
    return this.classFocused(d) + this.classDefocused(d);
  },
  classFocused: function(d) {
    return " ".concat(this.state.focusedTargetIds.indexOf(d.id) >= 0 ? CLASS.focused : "");
  },
  classDefocused: function(d) {
    return " ".concat(this.state.defocusedTargetIds.indexOf(d.id) >= 0 ? CLASS.defocused : "");
  },
  getTargetSelectorSuffix: function(targetId) {
    var targetStr = targetId || targetId === 0 ? "-".concat(targetId) : "";
    return targetStr.replace(/([\s?!@#$%^&*()_=+,.<>'":;\[\]\/|~`{}\\])/g, "-");
  },
  selectorTarget: function(id2, prefix, postfix) {
    if (prefix === void 0) {
      prefix = "";
    }
    if (postfix === void 0) {
      postfix = "";
    }
    var target = this.getTargetSelectorSuffix(id2);
    return "".concat(prefix, ".").concat(CLASS.target + target, " ").concat(postfix, ", ").concat(prefix, ".").concat(CLASS.circles + target, " ").concat(postfix);
  },
  selectorTargets: function(idsValue, prefix) {
    var _this = this;
    var ids = idsValue || [];
    return ids.length ? ids.map(function(id2) {
      return _this.selectorTarget(id2, prefix);
    }) : null;
  },
  selectorLegend: function(id2) {
    return ".".concat(CLASS.legendItem + this.getTargetSelectorSuffix(id2));
  },
  selectorLegends: function(ids) {
    var _this = this;
    return (ids === null || ids === void 0 ? void 0 : ids.length) ? ids.map(function(id2) {
      return _this.selectorLegend(id2);
    }) : null;
  }
};
var category = {
  /**
   * Category Name
   * @param {number} i Index number
   * @returns {string} category Name
   * @private
   */
  categoryName: function(i) {
    var _a2;
    var axis_x_categories = this.config.axis_x_categories;
    return (_a2 = axis_x_categories === null || axis_x_categories === void 0 ? void 0 : axis_x_categories[i]) !== null && _a2 !== void 0 ? _a2 : i;
  }
};
var colorizePattern = function(pattern, color3, id2) {
  var node = select_default2(pattern.cloneNode(true));
  node.attr("id", id2).insert("rect", ":first-child").attr("width", node.attr("width")).attr("height", node.attr("height")).style("fill", color3);
  return {
    id: id2,
    node: node.node()
  };
};
function getColorFromCss(element) {
  var cacheKey = KEY.colorPattern;
  var body = doc.body;
  var pattern = body[cacheKey];
  if (!pattern) {
    var delimiter = ";";
    var content = element.classed($COLOR.colorPattern, true).style("background-image");
    element.classed($COLOR.colorPattern, false);
    if (content.indexOf(delimiter) > -1) {
      pattern = content.replace(/url[^#]*|["'()]|(\s|%20)/g, "").split(delimiter).map(function(v) {
        return v.trim().replace(/[\"'\s]/g, "");
      }).filter(Boolean);
      body[cacheKey] = pattern;
    }
  }
  return pattern;
}
var schemeCategory10 = ["#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd", "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf"];
var color2 = {
  generateColor: function() {
    var $$ = this;
    var $el = $$.$el, config = $$.config;
    var colors = config.data_colors;
    var callback = config.data_color;
    var ids = [];
    var pattern = notEmpty(config.color_pattern) ? config.color_pattern : ordinal(getColorFromCss($el.chart) || schemeCategory10).range();
    var originalColorPattern = pattern;
    if (isFunction(config.color_tiles)) {
      var tiles_1 = config.color_tiles.bind($$.api)();
      var colorizedPatterns = pattern.map(function(p, index2) {
        var color3 = p.replace(/[#\(\)\s,]/g, "");
        var id2 = "".concat($$.state.datetimeId, "-pattern-").concat(color3, "-").concat(index2);
        return colorizePattern(tiles_1[index2 % tiles_1.length], p, id2);
      });
      pattern = colorizedPatterns.map(function(p) {
        return "url(#".concat(p.id, ")");
      });
      $$.patterns = colorizedPatterns;
    }
    return function(d) {
      var _a2;
      var id2 = d.id || ((_a2 = d.data) === null || _a2 === void 0 ? void 0 : _a2.id) || d;
      var isLine = $$.isTypeOf(id2, ["line", "spline", "step"]) || !config.data_types[id2];
      var color3;
      if (isFunction(colors[id2])) {
        color3 = colors[id2].bind($$.api)(d);
      } else if (colors[id2]) {
        color3 = colors[id2];
      } else {
        if (ids.indexOf(id2) < 0) {
          ids.push(id2);
        }
        color3 = isLine ? originalColorPattern[ids.indexOf(id2) % originalColorPattern.length] : pattern[ids.indexOf(id2) % pattern.length];
        colors[id2] = color3;
      }
      return isFunction(callback) ? callback.bind($$.api)(color3, d) : color3;
    };
  },
  generateLevelColor: function() {
    var $$ = this;
    var config = $$.config;
    var colors = config.color_pattern;
    var threshold2 = config.color_threshold;
    var asValue = threshold2.unit === "value";
    var max4 = threshold2.max || 100;
    var values = threshold2.values && threshold2.values.length ? threshold2.values : [];
    return notEmpty(threshold2) ? function(value) {
      var v = asValue ? value : value * 100 / max4;
      var color3 = colors[colors.length - 1];
      for (var i = 0, l = values.length; i < l; i++) {
        if (v <= values[i]) {
          color3 = colors[i];
          break;
        }
      }
      return color3;
    } : null;
  },
  /**
   * Append data backgound color filter definition
   * @param {string} color Color string
   * @private
   */
  generateDataLabelBackgroundColorFilter: function(color3) {
    var $$ = this;
    var $el = $$.$el, config = $$.config, state = $$.state;
    var backgroundColors = color3 || config.data_labels_backgroundColors;
    if (backgroundColors) {
      var ids = [];
      if (isString(backgroundColors)) {
        ids.push("");
      } else if (isObject(backgroundColors)) {
        ids = Object.keys(backgroundColors);
      }
      ids.forEach(function(v) {
        var id2 = "".concat(state.datetimeId, "-labels-bg").concat($$.getTargetSelectorSuffix(v)).concat(color3 ? $$.getTargetSelectorSuffix(color3) : "");
        $el.defs.append("filter").attr("x", "0").attr("y", "0").attr("width", "1").attr("height", "1").attr("id", id2).html('<feFlood flood-color="'.concat(v === "" ? backgroundColors : backgroundColors[v], '" /><feComposite in="SourceGraphic"/>'));
      });
    }
  },
  /**
   * Get data gradient color url
   * @param {string} id Data id
   * @returns {string}
   * @private
   */
  getGradienColortUrl: function(id2) {
    return "url(#".concat(this.state.datetimeId, "-gradient").concat(this.getTargetSelectorSuffix(id2), ")");
  },
  /**
   * Update linear/radial gradient definition
   * - linear: area & bar only
   * - radial: type which has data points only
   * @private
   */
  updateLinearGradient: function() {
    var $$ = this;
    var config = $$.config, targets = $$.data.targets, datetimeId = $$.state.datetimeId, defs = $$.$el.defs;
    targets.forEach(function(d) {
      var id2 = "".concat(datetimeId, "-gradient").concat($$.getTargetSelectorSuffix(d.id));
      var radialGradient = $$.hasPointType() && config.point_radialGradient;
      var supportedType = $$.isAreaType(d) && "area" || $$.isBarType(d) && "bar";
      if ((radialGradient || supportedType) && defs.select("#".concat(id2)).empty()) {
        var color_1 = $$.color(d);
        var gradient_1 = {
          defs: null,
          stops: []
        };
        if (radialGradient) {
          var _a2 = radialGradient.cx, cx = _a2 === void 0 ? 0.3 : _a2, _b = radialGradient.cy, cy = _b === void 0 ? 0.3 : _b, _c = radialGradient.r, r = _c === void 0 ? 0.7 : _c, _d = radialGradient.stops, stops = _d === void 0 ? [[0.1, color_1, 0], [0.9, color_1, 1]] : _d;
          gradient_1.stops = stops;
          gradient_1.defs = defs.append("radialGradient").attr("id", "".concat(id2)).attr("cx", cx).attr("cy", cy).attr("r", r);
        } else {
          var isRotated = config.axis_rotated;
          var _e = config["".concat(supportedType, "_linearGradient")], _f = _e.x, x3 = _f === void 0 ? isRotated ? [1, 0] : [0, 0] : _f, _g = _e.y, y3 = _g === void 0 ? isRotated ? [0, 0] : [0, 1] : _g, _h = _e.stops, stops = _h === void 0 ? [[0, color_1, 1], [1, color_1, 0]] : _h;
          gradient_1.stops = stops;
          gradient_1.defs = defs.append("linearGradient").attr("id", "".concat(id2)).attr("x1", x3[0]).attr("x2", x3[1]).attr("y1", y3[0]).attr("y2", y3[1]);
        }
        gradient_1.stops.forEach(function(v) {
          var offset = v[0], stopColor = v[1], stopOpacity = v[2];
          var colorValue = isFunction(stopColor) ? stopColor.bind($$.api)(d.id) : stopColor;
          gradient_1.defs && gradient_1.defs.append("stop").attr("offset", offset).attr("stop-color", colorValue || color_1).attr("stop-opacity", stopOpacity);
        });
      }
    });
  },
  /**
   * Set the data over color.
   * When is out, will restate in its previous color value
   * @param {boolean} isOver true: set overed color, false: restore
   * @param {number|object} d target index or data object for Arc type
   * @private
   */
  setOverColor: function(isOver, d) {
    var $$ = this;
    var config = $$.config, main2 = $$.$el.main;
    var onover = config.color_onover;
    var color3 = isOver ? onover : $$.color;
    if (isObject(color3)) {
      color3 = function(_a2) {
        var id2 = _a2.id;
        return id2 in onover ? onover[id2] : $$.color(id2);
      };
    } else if (isString(color3)) {
      color3 = function() {
        return onover;
      };
    } else if (isFunction(onover)) {
      color3 = color3.bind($$.api);
    }
    main2.selectAll(isObject(d) ? (
      // when is Arc type
      ".".concat($ARC.arc).concat($$.getTargetSelectorSuffix(d.id))
    ) : ".".concat($SHAPE.shape, "-").concat(d)).style("fill", color3);
  }
};
var domain = {
  getYDomainMinMax: function(targets, type2) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config;
    var isMin = type2 === "min";
    var dataGroups = config.data_groups;
    var ids = $$.mapToIds(targets);
    var ys = $$.getValuesAsIdKeyed(targets);
    if (dataGroups.length > 0) {
      var hasValue_1 = $$["has".concat(isMin ? "Negative" : "Positive", "ValueInTargets")](targets);
      dataGroups.forEach(function(groupIds) {
        var idsInGroup = groupIds.filter(function(v) {
          return ids.indexOf(v) >= 0;
        });
        if (idsInGroup.length) {
          var baseId_1 = idsInGroup[0];
          var baseAxisId_1 = axis3.getId(baseId_1);
          if (hasValue_1 && ys[baseId_1]) {
            ys[baseId_1] = ys[baseId_1].map(function(v) {
              return (isMin ? v < 0 : v > 0) ? v : 0;
            });
          }
          idsInGroup.filter(function(v, i) {
            return i > 0;
          }).forEach(function(id2) {
            if (ys[id2]) {
              var axisId_1 = axis3.getId(id2);
              ys[id2].forEach(function(v, i) {
                var val = +v;
                var meetCondition = isMin ? val > 0 : val < 0;
                if (axisId_1 === baseAxisId_1 && !(hasValue_1 && meetCondition)) {
                  ys[baseId_1][i] += val;
                }
              });
            }
          });
        }
      });
    }
    return getMinMax$1(type2, Object.keys(ys).map(function(key) {
      return getMinMax$1(type2, ys[key]);
    }));
  },
  /**
   * Check if hidden targets bound to the given axis id
   * @param {string} id ID to be checked
   * @returns {boolean}
   * @private
   */
  isHiddenTargetWithYDomain: function(id2) {
    var $$ = this;
    return $$.state.hiddenTargetIds.some(function(v) {
      return $$.axis.getId(v) === id2;
    });
  },
  getYDomain: function(targets, axisId, xDomain) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, scale2 = $$.scale;
    var pfx = "axis_".concat(axisId);
    if ($$.isStackNormalized()) {
      return [0, 100];
    }
    var isLog = (scale2 === null || scale2 === void 0 ? void 0 : scale2[axisId]) && scale2[axisId].type === "log";
    var targetsByAxisId = targets.filter(function(t) {
      return axis3.getId(t.id) === axisId;
    });
    var yTargets = xDomain ? $$.filterByXDomain(targetsByAxisId, xDomain) : targetsByAxisId;
    if (yTargets.length === 0) {
      if ($$.isHiddenTargetWithYDomain(axisId)) {
        return scale2[axisId].domain();
      } else {
        return axisId === "y2" ? scale2.y.domain() : (
          // When all data bounds to y2, y Axis domain is called prior y2.
          // So, it needs to call to get y2 domain here
          $$.getYDomain(targets, "y2", xDomain)
        );
      }
    }
    var yMin = config["".concat(pfx, "_min")];
    var yMax = config["".concat(pfx, "_max")];
    var center2 = config["".concat(pfx, "_center")];
    var isInverted = config["".concat(pfx, "_inverted")];
    var showHorizontalDataLabel = $$.hasDataLabel() && config.axis_rotated;
    var showVerticalDataLabel = $$.hasDataLabel() && !config.axis_rotated;
    var yDomainMin = $$.getYDomainMinMax(yTargets, "min");
    var yDomainMax = $$.getYDomainMinMax(yTargets, "max");
    var isZeroBased = __spreadArray([TYPE.BAR, TYPE.BUBBLE, TYPE.SCATTER], TYPE_BY_CATEGORY.Line, true).some(function(v) {
      var type2 = v.indexOf("area") > -1 ? "area" : v;
      return $$.hasType(v, yTargets, true) && config["".concat(type2, "_zerobased")];
    });
    yDomainMin = isValue(yMin) ? yMin : isValue(yMax) ? yDomainMin <= yMax ? yDomainMin : yMax - 10 : yDomainMin;
    yDomainMax = isValue(yMax) ? yMax : isValue(yMin) ? yMin <= yDomainMax ? yDomainMax : yMin + 10 : yDomainMax;
    if (isNaN(yDomainMin)) {
      yDomainMin = 0;
    }
    if (isNaN(yDomainMax)) {
      yDomainMax = yDomainMin;
    }
    if (yDomainMin === yDomainMax) {
      yDomainMin < 0 ? yDomainMax = 0 : yDomainMin = 0;
    }
    var isAllPositive = yDomainMin >= 0 && yDomainMax >= 0;
    var isAllNegative = yDomainMin <= 0 && yDomainMax <= 0;
    if (isValue(yMin) && isAllPositive || isValue(yMax) && isAllNegative) {
      isZeroBased = false;
    }
    if (isZeroBased) {
      isAllPositive && (yDomainMin = 0);
      isAllNegative && (yDomainMax = 0);
    }
    var domainLength = Math.abs(yDomainMax - yDomainMin);
    var padding = { top: domainLength * 0.1, bottom: domainLength * 0.1 };
    if (isDefined(center2)) {
      var yDomainAbs = Math.max(Math.abs(yDomainMin), Math.abs(yDomainMax));
      yDomainMax = center2 + yDomainAbs;
      yDomainMin = center2 - yDomainAbs;
    }
    if (showHorizontalDataLabel) {
      var diff_1 = diffDomain(scale2.y.range());
      var ratio_1 = $$.getDataLabelLength(yDomainMin, yDomainMax, "width").map(function(v) {
        return v / diff_1;
      });
      ["bottom", "top"].forEach(function(v, i) {
        padding[v] += domainLength * (ratio_1[i] / (1 - ratio_1[0] - ratio_1[1]));
      });
    } else if (showVerticalDataLabel) {
      var lengths_1 = $$.getDataLabelLength(yDomainMin, yDomainMax, "height");
      ["bottom", "top"].forEach(function(v, i) {
        padding[v] += $$.convertPixelToScale("y", lengths_1[i], domainLength);
      });
    }
    padding = $$.getResettedPadding(padding);
    var p = config["".concat(pfx, "_padding")];
    if (notEmpty(p)) {
      ["bottom", "top"].forEach(function(v) {
        padding[v] = axis3.getPadding(p, v, padding[v], domainLength);
      });
    }
    if (isZeroBased) {
      isAllPositive && (padding.bottom = yDomainMin);
      isAllNegative && (padding.top = -yDomainMax);
    }
    var domain2 = isLog ? [yDomainMin, yDomainMax].map(function(v) {
      return v < 0 ? 0 : v;
    }) : [yDomainMin - padding.bottom, yDomainMax + padding.top];
    return isInverted ? domain2.reverse() : domain2;
  },
  getXDomainMinMax: function(targets, type2) {
    var _a2;
    var $$ = this;
    var configValue = $$.config["axis_x_".concat(type2)];
    var dataValue = getMinMax$1(type2, targets.map(function(t) {
      return getMinMax$1(type2, t.values.map(function(v) {
        return v.x;
      }));
    }));
    var value = isObject(configValue) ? configValue.value : configValue;
    value = isDefined(value) && ((_a2 = $$.axis) === null || _a2 === void 0 ? void 0 : _a2.isTimeSeries()) ? parseDate.bind(this)(value) : value;
    if (isObject(configValue) && configValue.fit && (type2 === "min" && value < dataValue || type2 === "max" && value > dataValue)) {
      value = void 0;
    }
    return isDefined(value) ? value : dataValue;
  },
  /**
   * Get x Axis padding
   * @param {Array} domain x Axis domain
   * @param {number} tickCount Tick count
   * @returns {object} Padding object values with 'left' & 'right' key
   * @private
   */
  getXDomainPadding: function(domain2, tickCount) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config;
    var padding = config.axis_x_padding;
    var isTimeSeriesTickCount = axis3.isTimeSeries() && tickCount;
    var diff = diffDomain(domain2);
    var defaultValue;
    if (axis3.isCategorized() || isTimeSeriesTickCount) {
      defaultValue = 0;
    } else if ($$.hasType("bar")) {
      var maxDataCount = $$.getMaxDataCount();
      defaultValue = maxDataCount > 1 ? diff / (maxDataCount - 1) / 2 : 0.5;
    } else {
      defaultValue = $$.getResettedPadding(diff * 0.01);
    }
    var _a2 = isNumber(padding) ? { left: padding, right: padding } : padding, _b = _a2.left, left2 = _b === void 0 ? defaultValue : _b, _c = _a2.right, right2 = _c === void 0 ? defaultValue : _c;
    if (padding.unit === "px") {
      var domainLength = Math.abs(diff + diff * 0.2);
      left2 = axis3.getPadding(padding, "left", defaultValue, domainLength);
      right2 = axis3.getPadding(padding, "right", defaultValue, domainLength);
    } else {
      var range2 = diff + left2 + right2;
      if (isTimeSeriesTickCount && range2) {
        var relativeTickWidth = diff / tickCount / range2;
        left2 = left2 / range2 / relativeTickWidth;
        right2 = right2 / range2 / relativeTickWidth;
      }
    }
    return { left: left2, right: right2 };
  },
  /**
   * Get x Axis domain
   * @param {Array} targets targets
   * @returns {Array} x Axis domain
   * @private
   */
  getXDomain: function(targets) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, x3 = $$.scale.x;
    var isInverted = config.axis_x_inverted;
    var domain2 = [
      $$.getXDomainMinMax(targets, "min"),
      $$.getXDomainMinMax(targets, "max")
    ];
    var _a2 = domain2[0], min4 = _a2 === void 0 ? 0 : _a2, _b = domain2[1], max4 = _b === void 0 ? 0 : _b;
    if (x3.type !== "log") {
      var isCategorized = axis3.isCategorized();
      var isTimeSeries = axis3.isTimeSeries();
      var padding = $$.getXDomainPadding(domain2);
      var firstX = domain2[0], lastX = domain2[1];
      if (firstX - lastX === 0 && !isCategorized) {
        if (isTimeSeries) {
          firstX = new Date(firstX.getTime() * 0.5);
          lastX = new Date(lastX.getTime() * 1.5);
        } else {
          firstX = firstX === 0 ? 1 : firstX * 0.5;
          lastX = lastX === 0 ? -1 : lastX * 1.5;
        }
      }
      if (firstX || firstX === 0) {
        min4 = isTimeSeries ? new Date(firstX.getTime() - padding.left) : firstX - padding.left;
      }
      if (lastX || lastX === 0) {
        max4 = isTimeSeries ? new Date(lastX.getTime() + padding.right) : lastX + padding.right;
      }
    }
    return isInverted ? [max4, min4] : [min4, max4];
  },
  updateXDomain: function(targets, withUpdateXDomain, withUpdateOrgXDomain, withTrim, domain2) {
    var _a2;
    var $$ = this;
    var config = $$.config, org = $$.org, _b = $$.scale, x3 = _b.x, subX = _b.subX;
    var zoomEnabled = config.zoom_enabled;
    if (withUpdateOrgXDomain) {
      x3.domain(domain2 || sortValue($$.getXDomain(targets), !config.axis_x_inverted));
      org.xDomain = x3.domain();
      subX.domain(x3.domain());
      (_a2 = $$.brush) === null || _a2 === void 0 ? void 0 : _a2.scale(subX);
    }
    if (withUpdateXDomain) {
      var domainValue = domain2 || (!$$.brush || brushEmpty($$)) ? org.xDomain : getBrushSelection($$).map(subX.invert);
      x3.domain(domainValue);
    }
    if (withUpdateOrgXDomain || withUpdateXDomain) {
      zoomEnabled && $$.zoom.updateScaleExtent();
    }
    withTrim && x3.domain($$.trimXDomain(x3.orgDomain()));
    return x3.domain();
  },
  /**
   * Trim x domain when given domain surpasses the range
   * @param {Array} domain Domain value
   * @returns {Array} Trimed domain if given domain is out of range
   * @private
   */
  trimXDomain: function(domain2) {
    var $$ = this;
    var isInverted = $$.config.axis_x_inverted;
    var zoomDomain = $$.getZoomDomain();
    var min4 = zoomDomain[0], max4 = zoomDomain[1];
    if (isInverted ? domain2[0] >= min4 : domain2[0] <= min4) {
      domain2[1] = +domain2[1] + (min4 - domain2[0]);
      domain2[0] = min4;
    }
    if (isInverted ? domain2[1] <= max4 : domain2[1] >= max4) {
      domain2[0] = +domain2[0] - (domain2[1] - max4);
      domain2[1] = max4;
    }
    return domain2;
  },
  /**
   * Get subchart/zoom domain
   * @param {string} type "subX" or "zoom"
   * @param {boolean} getCurrent Get current domain if true
   * @returns {Array} zoom domain
   * @private
   */
  getZoomDomain: function(type2, getCurrent) {
    if (type2 === void 0) {
      type2 = "zoom";
    }
    if (getCurrent === void 0) {
      getCurrent = false;
    }
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, org = $$.org;
    var _a2 = getCurrent && scale2[type2] ? scale2[type2].domain() : org.xDomain, min4 = _a2[0], max4 = _a2[1];
    if (type2 === "zoom") {
      if (isDefined(config.zoom_x_min)) {
        min4 = getMinMax$1("min", [min4, config.zoom_x_min]);
      }
      if (isDefined(config.zoom_x_max)) {
        max4 = getMinMax$1("max", [max4, config.zoom_x_max]);
      }
    }
    return [min4, max4];
  },
  /**
   * Return zoom domain from given domain
   * - 'category' type need to add offset to original value
   * @param {Array} domainValue domain value
   * @returns {Array} Zoom domain
   * @private
   */
  getZoomDomainValue: function(domainValue) {
    var $$ = this;
    var config = $$.config, axis3 = $$.axis;
    if (axis3.isCategorized() && Array.isArray(domainValue)) {
      var isInverted_1 = config.axis_x_inverted;
      var domain2 = domainValue.map(function(v, i) {
        return Number(v) + (i === 0 ? +isInverted_1 : +!isInverted_1);
      });
      return domain2;
    }
    return domainValue;
  },
  /**
   * Converts pixels to axis' scale values
   * @param {string} type Axis type
   * @param {number} pixels Pixels
   * @param {number} domainLength Domain length
   * @returns {number}
   * @private
   */
  convertPixelToScale: function(type2, pixels, domainLength) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var isRotated = config.axis_rotated;
    var length;
    if (type2 === "x") {
      length = isRotated ? "height" : "width";
    } else {
      length = isRotated ? "width" : "height";
    }
    return domainLength * (pixels / state[length]);
  },
  /**
   * Check if the given domain is within subchart/zoom range
   * @param {Array} domain Target domain value
   * @param {Array} current Current subchart/zoom domain value
   * @param {Array} range subchart/zoom range value
   * @returns {boolean}
   * @private
   */
  withinRange: function(domain2, current, range2) {
    if (current === void 0) {
      current = [0, 0];
    }
    var $$ = this;
    var isInverted = $$.config.axis_x_inverted;
    var _a2 = range2, min4 = _a2[0], max4 = _a2[1];
    if (Array.isArray(domain2)) {
      var target = __spreadArray([], domain2, true);
      isInverted && target.reverse();
      if (target[0] < target[1]) {
        return domain2.every(function(v, i) {
          return (i === 0 ? isInverted ? +v <= min4 : +v >= min4 : isInverted ? +v >= max4 : +v <= max4) && !domain2.every(function(v2, i2) {
            return v2 === current[i2];
          });
        });
      }
    }
    return false;
  }
};
function getFormat($$, typeValue, v) {
  var config = $$.config;
  var type2 = "axis_".concat(typeValue, "_tick_format");
  var format3 = config[type2] ? config[type2] : $$.defaultValueFormat;
  return format3.call($$.api, v);
}
var format2 = {
  yFormat: function(v) {
    return getFormat(this, "y", v);
  },
  y2Format: function(v) {
    return getFormat(this, "y2", v);
  },
  /**
   * Get default value format function
   * @returns {Function} formatter function
   * @private
   */
  getDefaultValueFormat: function() {
    var $$ = this;
    var defaultArcValueFormat = $$.defaultArcValueFormat, yFormat = $$.yFormat, y2Format = $$.y2Format;
    var hasArc = $$.hasArcType(null, ["gauge", "polar", "radar"]);
    return function(v, ratio, id2) {
      var format3 = hasArc ? defaultArcValueFormat : $$.axis && $$.axis.getId(id2) === "y2" ? y2Format : yFormat;
      return format3.call($$, v, ratio);
    };
  },
  defaultValueFormat: function(v) {
    return isArray(v) ? v.join("~") : isValue(v) ? +v : "";
  },
  defaultArcValueFormat: function(v, ratio) {
    return "".concat((ratio * 100).toFixed(1), "%");
  },
  defaultPolarValueFormat: function(v) {
    return "".concat(v);
  },
  dataLabelFormat: function(targetId) {
    var $$ = this;
    var dataLabels = $$.config.data_labels;
    var defaultFormat = function(v) {
      var delimiter = "~";
      var res = v;
      if (isArray(v)) {
        res = v.join(delimiter);
      } else if (isObject(v)) {
        res = Object.values(v).join(delimiter);
      }
      return res;
    };
    var format3 = defaultFormat;
    if (isFunction(dataLabels.format)) {
      format3 = dataLabels.format;
    } else if (isObjectType(dataLabels.format)) {
      if (dataLabels.format[targetId]) {
        format3 = dataLabels.format[targetId] === true ? defaultFormat : dataLabels.format[targetId];
      } else {
        format3 = function() {
          return "";
        };
      }
    }
    return format3.bind($$.api);
  }
};
function getLegendColor(id2) {
  var $$ = this;
  var data2 = $$.getDataById(id2);
  var color3 = $$.levelColor ? $$.levelColor(data2.values[0].value) : $$.color(data2);
  return color3;
}
function getFormattedText(id2, formatted) {
  var _a2;
  if (formatted === void 0) {
    formatted = true;
  }
  var config = this.config;
  var text2 = (_a2 = config.data_names[id2]) !== null && _a2 !== void 0 ? _a2 : id2;
  if (formatted && isFunction(config.legend_format)) {
    text2 = config.legend_format(text2);
  }
  return text2;
}
var legend$1 = {
  /**
   * Initialize the legend.
   * @private
   */
  initLegend: function() {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    $$.legendItemTextBox = {};
    $$.state.legendHasRendered = false;
    if (config.legend_show) {
      if (!config.legend_contents_bindto) {
        $el.legend = $$.$el.svg.append("g").classed($LEGEND.legend, true).attr("transform", $$.getTranslate("legend"));
      }
      $$.updateLegend();
    } else {
      $$.state.hiddenLegendIds = $$.mapToIds($$.data.targets);
    }
  },
  /**
   * Update legend element
   * @param {Array} targetIds ID's of target
   * @param {object} options withTransform : Whether to use the transform property / withTransitionForTransform: Whether transition is used when using the transform property / withTransition : whether or not to transition.
   * @param {object} transitions Return value of the generateTransitions
   * @private
   */
  updateLegend: function(targetIds, options2, transitions) {
    var _a2;
    var $$ = this;
    var config = $$.config, state = $$.state, scale2 = $$.scale, $el = $$.$el;
    var optionz = options2 || {
      withTransform: false,
      withTransitionForTransform: false,
      withTransition: false
    };
    optionz.withTransition = getOption(optionz, "withTransition", true);
    optionz.withTransitionForTransform = getOption(optionz, "withTransitionForTransform", true);
    if (config.legend_contents_bindto && config.legend_contents_template) {
      $$.updateLegendTemplate();
    } else if (!state.hasTreemap) {
      $$.updateLegendElement(targetIds || $$.mapToIds($$.data.targets), optionz, transitions);
    }
    (_a2 = $el.legend) === null || _a2 === void 0 ? void 0 : _a2.selectAll(".".concat($LEGEND.legendItem)).classed($LEGEND.legendItemHidden, function(id2) {
      var hide = !$$.isTargetToShow(id2);
      if (hide) {
        this.style.opacity = null;
      }
      return hide;
    });
    $$.updateScales(false, !scale2.zoom);
    $$.updateSvgSize();
    $$.transformAll(optionz.withTransitionForTransform, transitions);
    state.legendHasRendered = true;
  },
  /**
   * Update legend using template option
   * @private
   */
  updateLegendTemplate: function() {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    var wrapper = select_default2(config.legend_contents_bindto);
    var template = config.legend_contents_template;
    if (!wrapper.empty()) {
      var targets = $$.mapToIds($$.data.targets);
      var ids_1 = [];
      var html_1 = "";
      targets.forEach(function(v) {
        var content = isFunction(template) ? template.bind($$.api)(v, $$.color(v), $$.api.data(v)[0].values) : tplProcess(template, {
          COLOR: $$.color(v),
          TITLE: v
        });
        if (content) {
          ids_1.push(v);
          html_1 += content;
        }
      });
      var legendItem = wrapper.html(html_1).selectAll(function() {
        return this.childNodes;
      }).data(ids_1);
      $$.setLegendItem(legendItem);
      $el.legend = wrapper;
    }
  },
  /**
   * Update the size of the legend.
   * @param {Obejct} size Size object
   * @private
   */
  updateSizeForLegend: function(size2) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, isLegendTop = _a2.isLegendTop, isLegendLeft = _a2.isLegendLeft, isLegendRight = _a2.isLegendRight, isLegendInset = _a2.isLegendInset, current = _a2.current;
    var width = size2.width, height = size2.height;
    var insetLegendPosition = {
      top: isLegendTop ? $$.getCurrentPaddingByDirection("top") + config.legend_inset_y + 5.5 : current.height - height - $$.getCurrentPaddingByDirection("bottom") - config.legend_inset_y,
      left: isLegendLeft ? $$.getCurrentPaddingByDirection("left") + config.legend_inset_x + 0.5 : current.width - width - $$.getCurrentPaddingByDirection("right") - config.legend_inset_x + 0.5
    };
    $$.state.margin3 = {
      top: isLegendRight ? 0 : isLegendInset ? insetLegendPosition.top : current.height - height,
      right: NaN,
      bottom: 0,
      left: isLegendRight ? current.width - width : isLegendInset ? insetLegendPosition.left : 0
    };
  },
  /**
   * Transform Legend
   * @param {boolean} withTransition whether or not to transition.
   * @private
   */
  transformLegend: function(withTransition) {
    var $$ = this;
    var legend2 = $$.$el.legend, $T = $$.$T;
    $T(legend2, withTransition).attr("transform", $$.getTranslate("legend"));
  },
  /**
   * Update the legend step
   * @param {number} step Step value
   * @private
   */
  updateLegendStep: function(step2) {
    this.state.legendStep = step2;
  },
  /**
   * Update legend item width
   * @param {number} width Width value
   * @private
   */
  updateLegendItemWidth: function(width) {
    this.state.legendItemWidth = width;
  },
  /**
   * Update legend item height
   * @param {number} height Height value
   * @private
   */
  updateLegendItemHeight: function(height) {
    this.state.legendItemHeight = height;
  },
  /**
   * Update legend item color
   * @param {string} id Corresponding data ID value
   * @param {string} color Color value
   * @private
   */
  updateLegendItemColor: function(id2, color3) {
    var legend2 = this.$el.legend;
    if (legend2) {
      legend2.select(".".concat($LEGEND.legendItem, "-").concat(id2, " line")).style("stroke", color3);
    }
  },
  /**
   * Get the width of the legend
   * @returns {number} width
   * @private
   */
  getLegendWidth: function() {
    var $$ = this;
    var _a2 = $$.state, width = _a2.current.width, isLegendRight = _a2.isLegendRight, isLegendInset = _a2.isLegendInset, legendItemWidth = _a2.legendItemWidth, legendStep = _a2.legendStep;
    return $$.config.legend_show ? isLegendRight || isLegendInset ? legendItemWidth * (legendStep + 1) : width : 0;
  },
  /**
   * Get the height of the legend
   * @returns {number} height
   * @private
   */
  getLegendHeight: function() {
    var _a2;
    var $$ = this;
    var _b = $$.state, current = _b.current, isLegendRight = _b.isLegendRight, legendItemHeight = _b.legendItemHeight, legendStep = _b.legendStep;
    var isFitPadding = ((_a2 = $$.config.padding) === null || _a2 === void 0 ? void 0 : _a2.mode) === "fit";
    return $$.config.legend_show ? isLegendRight ? current.height : (isFitPadding ? 10 : Math.max(20, legendItemHeight)) * (legendStep + 1) : 0;
  },
  /**
   * Get the opacity of the legend that is unfocused
   * @param {d3.selection} legendItem Legend item node
   * @returns {string|null} opacity
   * @private
   */
  opacityForUnfocusedLegend: function(legendItem) {
    return legendItem.classed($LEGEND.legendItemHidden) ? null : "0.3";
  },
  /**
   * Toggles the focus of the legend
   * @param {Array} targetIds ID's of target
   * @param {boolean} focus whether or not to focus.
   * @private
   */
  toggleFocusLegend: function(targetIds, focus) {
    var $$ = this;
    var legend2 = $$.$el.legend, $T = $$.$T;
    var targetIdz = $$.mapToTargetIds(targetIds);
    legend2 && $T(legend2.selectAll(".".concat($LEGEND.legendItem)).filter(function(id2) {
      return targetIdz.indexOf(id2) >= 0;
    }).classed($FOCUS.legendItemFocused, focus)).style("opacity", function() {
      return focus ? null : $$.opacityForUnfocusedLegend.call($$, select_default2(this));
    });
  },
  /**
   * Revert the legend to its default state
   * @private
   */
  revertLegend: function() {
    var $$ = this;
    var legend2 = $$.$el.legend, $T = $$.$T;
    legend2 && $T(legend2.selectAll(".".concat($LEGEND.legendItem)).classed($FOCUS.legendItemFocused, false)).style("opacity", null);
  },
  /**
   * Shows the legend
   * @param {Array} targetIds ID's of target
   * @private
   */
  showLegend: function(targetIds) {
    var $$ = this;
    var config = $$.config, $el = $$.$el, $T = $$.$T;
    if (!config.legend_show) {
      config.legend_show = true;
      $el.legend ? $el.legend.style("visibility", null) : $$.initLegend();
      !$$.state.legendHasRendered && $$.updateLegend();
    }
    $$.removeHiddenLegendIds(targetIds);
    $T($el.legend.selectAll($$.selectorLegends(targetIds)).style("visibility", null)).style("opacity", null);
  },
  /**
   * Hide the legend
   * @param {Array} targetIds ID's of target
   * @private
   */
  hideLegend: function(targetIds) {
    var $$ = this;
    var config = $$.config, legend2 = $$.$el.legend;
    if (config.legend_show && isEmpty(targetIds)) {
      config.legend_show = false;
      legend2.style("visibility", "hidden");
    }
    $$.addHiddenLegendIds(targetIds);
    legend2.selectAll($$.selectorLegends(targetIds)).style("opacity", "0").style("visibility", "hidden");
  },
  /**
   * Get legend item textbox dimension
   * @param {string} id Data ID
   * @param {HTMLElement|d3.selection} textElement Text node element
   * @returns {object} Bounding rect
   * @private
   */
  getLegendItemTextBox: function(id2, textElement) {
    var $$ = this;
    var cache = $$.cache, state = $$.state;
    var data2;
    var cacheKey = KEY.legendItemTextBox;
    if (id2) {
      data2 = !state.redrawing && cache.get(cacheKey) || {};
      if (!data2[id2]) {
        data2[id2] = $$.getTextRect(textElement, $LEGEND.legendItem);
        cache.add(cacheKey, data2);
      }
      data2 = data2[id2];
    }
    return data2;
  },
  /**
   * Set legend item style & bind events
   * @param {d3.selection} item Item node
   * @private
   */
  setLegendItem: function(item) {
    var $$ = this;
    var $el = $$.$el, api2 = $$.api, config = $$.config, state = $$.state;
    var isTouch = state.inputType === "touch";
    var hasGauge = $$.hasType("gauge");
    var useCssRule = config.boost_useCssRule;
    var interaction2 = config.legend_item_interaction;
    item.attr("class", function(id2) {
      var node = select_default2(this);
      var itemClass = !node.empty() && node.attr("class") || "";
      return itemClass + $$.generateClass($LEGEND.legendItem, id2);
    }).style("visibility", function(id2) {
      return $$.isLegendToShow(id2) ? null : "hidden";
    });
    if (config.interaction_enabled) {
      if (useCssRule) {
        [
          [".".concat($LEGEND.legendItem), "cursor:pointer"],
          [".".concat($LEGEND.legendItem, " text"), "pointer-events:none"],
          [".".concat($LEGEND.legendItemPoint, " text"), "pointer-events:none"],
          [".".concat($LEGEND.legendItemTile), "pointer-events:none"],
          [".".concat($LEGEND.legendItemEvent), "fill-opacity:0"]
        ].forEach(function(v) {
          var selector = v[0], props = v[1];
          $$.setCssRule(false, selector, [props])($el.legend);
        });
      }
      item.on(interaction2.dblclick ? "dblclick" : "click", interaction2 || isFunction(config.legend_item_onclick) ? function(event, id2) {
        if (!callFn(config.legend_item_onclick, api2, id2)) {
          var altKey = event.altKey, target = event.target, type2 = event.type;
          if (type2 === "dblclick" || altKey) {
            if (state.hiddenTargetIds.length && target.parentNode.getAttribute("class").indexOf($LEGEND.legendItemHidden) === -1) {
              api2.show();
            } else {
              api2.hide();
              api2.show(id2);
            }
          } else {
            api2.toggle(id2);
            select_default2(this).classed($FOCUS.legendItemFocused, false);
          }
        }
        isTouch && $$.hideTooltip();
      } : null);
      !isTouch && item.on("mouseout", interaction2 || isFunction(config.legend_item_onout) ? function(event, id2) {
        if (!callFn(config.legend_item_onout, api2, id2)) {
          select_default2(this).classed($FOCUS.legendItemFocused, false);
          if (hasGauge) {
            $$.undoMarkOverlapped($$, ".".concat($GAUGE.gaugeValue));
          }
          $$.api.revert();
        }
      } : null).on("mouseover", interaction2 || isFunction(config.legend_item_onover) ? function(event, id2) {
        if (!callFn(config.legend_item_onover, api2, id2)) {
          select_default2(this).classed($FOCUS.legendItemFocused, true);
          if (hasGauge) {
            $$.markOverlapped(id2, $$, ".".concat($GAUGE.gaugeValue));
          }
          if (!state.transiting && $$.isTargetToShow(id2)) {
            api2.focus(id2);
          }
        }
      } : null);
      !item.empty() && item.on("click mouseout mouseover") && item.style("cursor", $$.getStylePropValue("pointer"));
    }
  },
  /**
   * Update the legend
   * @param {Array} targetIds ID's of target
   * @param {object} options withTransform : Whether to use the transform property / withTransitionForTransform: Whether transition is used when using the transform property / withTransition : whether or not to transition.
   * @private
   */
  updateLegendElement: function(targetIds, options2) {
    var $$ = this;
    var config = $$.config, state = $$.state, legend2 = $$.$el.legend, $T = $$.$T;
    var legendType = config.legend_item_tile_type;
    var isRectangle = legendType !== "circle";
    var legendItemR = config.legend_item_tile_r;
    var itemTileSize = {
      width: isRectangle ? config.legend_item_tile_width : legendItemR * 2,
      height: isRectangle ? config.legend_item_tile_height : legendItemR * 2
    };
    var dimension = {
      padding: {
        top: 4,
        right: 10
      },
      max: {
        width: 0,
        height: 0
      },
      posMin: 10,
      step: 0,
      tileWidth: itemTileSize.width + 5,
      totalLength: 0
    };
    var sizes = {
      offsets: {},
      widths: {},
      heights: {},
      margins: [0],
      steps: {}
    };
    var xForLegend;
    var yForLegend;
    var background;
    var targetIdz = targetIds.filter(function(id2) {
      return !isDefined(config.data_names[id2]) || config.data_names[id2] !== null;
    });
    var withTransition = options2.withTransition;
    var updatePositions = $$.getUpdateLegendPositions(targetIdz, dimension, sizes);
    if (state.isLegendInset) {
      dimension.step = config.legend_inset_step ? config.legend_inset_step : targetIdz.length;
      $$.updateLegendStep(dimension.step);
    }
    if (state.isLegendRight) {
      xForLegend = function(id2) {
        return dimension.max.width * sizes.steps[id2];
      };
      yForLegend = function(id2) {
        return sizes.margins[sizes.steps[id2]] + sizes.offsets[id2];
      };
    } else if (state.isLegendInset) {
      xForLegend = function(id2) {
        return dimension.max.width * sizes.steps[id2] + 10;
      };
      yForLegend = function(id2) {
        return sizes.margins[sizes.steps[id2]] + sizes.offsets[id2];
      };
    } else {
      xForLegend = function(id2) {
        return sizes.margins[sizes.steps[id2]] + sizes.offsets[id2];
      };
      yForLegend = function(id2) {
        return dimension.max.height * sizes.steps[id2];
      };
    }
    var posFn = {
      xText: function(id2, i) {
        return xForLegend(id2, i) + 4 + itemTileSize.width;
      },
      xRect: function(id2, i) {
        return xForLegend(id2, i);
      },
      x1Tile: function(id2, i) {
        return xForLegend(id2, i) - 2;
      },
      x2Tile: function(id2, i) {
        return xForLegend(id2, i) - 2 + itemTileSize.width;
      },
      yText: function(id2, i) {
        return yForLegend(id2, i) + 9;
      },
      yRect: function(id2, i) {
        return yForLegend(id2, i) - 5;
      },
      yTile: function(id2, i) {
        return yForLegend(id2, i) + 4;
      }
    };
    $$.generateLegendItem(targetIdz, itemTileSize, updatePositions, posFn);
    background = legend2.select(".".concat($LEGEND.legendBackground, " rect"));
    if (state.isLegendInset && dimension.max.width > 0 && background.size() === 0) {
      background = legend2.insert("g", ".".concat($LEGEND.legendItem)).attr("class", $LEGEND.legendBackground).append("rect");
    }
    if (config.legend_tooltip) {
      legend2.selectAll("title").data(targetIdz).text(function(id2) {
        return getFormattedText.bind($$)(id2, false);
      });
    }
    var texts = legend2.selectAll("text").data(targetIdz).text(function(id2) {
      return getFormattedText.bind($$)(id2);
    }).each(function(id2, i) {
      updatePositions(this, id2, i);
    });
    $T(texts, withTransition).attr("x", posFn.xText).attr("y", posFn.yText);
    var rects = legend2.selectAll("rect.".concat($LEGEND.legendItemEvent)).data(targetIdz);
    $T(rects, withTransition).attr("width", function(id2) {
      return sizes.widths[id2];
    }).attr("height", function(id2) {
      return sizes.heights[id2];
    }).attr("x", posFn.xRect).attr("y", posFn.yRect);
    $$.updateLegendItemPos(targetIdz, withTransition, posFn);
    if (background) {
      $T(background, withTransition).attr("height", $$.getLegendHeight() - 12).attr("width", dimension.max.width * (dimension.step + 1) + 10);
    }
    $$.updateLegendItemWidth(dimension.max.width);
    $$.updateLegendItemHeight(dimension.max.height);
    $$.updateLegendStep(dimension.step);
  },
  /**
   * Get position update function
   * @param {Array} targetIdz Data ids
   * @param {object} dimension Dimension object
   * @param {object} sizes Size object
   * @returns {Function} Update position function
   * @private
   */
  getUpdateLegendPositions: function(targetIdz, dimension, sizes) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var isLegendRightOrInset = state.isLegendRight || state.isLegendInset;
    return function(textElement, id2, index2) {
      var reset = index2 === 0;
      var isLast = index2 === targetIdz.length - 1;
      var box = $$.getLegendItemTextBox(id2, textElement);
      var itemWidth = box.width + dimension.tileWidth + (isLast && !isLegendRightOrInset ? 0 : dimension.padding.right) + config.legend_padding;
      var itemHeight = box.height + dimension.padding.top;
      var itemLength = isLegendRightOrInset ? itemHeight : itemWidth;
      var areaLength = isLegendRightOrInset ? $$.getLegendHeight() : $$.getLegendWidth();
      var margin;
      var updateValues = function(id22, withoutStep) {
        if (!withoutStep) {
          margin = (areaLength - dimension.totalLength - itemLength) / 2;
          if (margin < dimension.posMin) {
            margin = (areaLength - itemLength) / 2;
            dimension.totalLength = 0;
            dimension.step++;
          }
        }
        sizes.steps[id22] = dimension.step;
        sizes.margins[dimension.step] = state.isLegendInset ? 10 : margin;
        sizes.offsets[id22] = dimension.totalLength;
        dimension.totalLength += itemLength;
      };
      if (reset) {
        dimension.totalLength = 0;
        dimension.step = 0;
        dimension.max.width = 0;
        dimension.max.height = 0;
      }
      if (config.legend_show && !$$.isLegendToShow(id2)) {
        sizes.widths[id2] = 0;
        sizes.heights[id2] = 0;
        sizes.steps[id2] = 0;
        sizes.offsets[id2] = 0;
        return;
      }
      sizes.widths[id2] = itemWidth;
      sizes.heights[id2] = itemHeight;
      if (!dimension.max.width || itemWidth >= dimension.max.width) {
        dimension.max.width = itemWidth;
      }
      if (!dimension.max.height || itemHeight >= dimension.max.height) {
        dimension.max.height = itemHeight;
      }
      var maxLength = isLegendRightOrInset ? dimension.max.height : dimension.max.width;
      if (config.legend_equally) {
        Object.keys(sizes.widths).forEach(function(id22) {
          return sizes.widths[id22] = dimension.max.width;
        });
        Object.keys(sizes.heights).forEach(function(id22) {
          return sizes.heights[id22] = dimension.max.height;
        });
        margin = (areaLength - maxLength * targetIdz.length) / 2;
        if (margin < dimension.posMin) {
          dimension.totalLength = 0;
          dimension.step = 0;
          targetIdz.forEach(function(id22) {
            return updateValues(id22);
          });
        } else {
          updateValues(id2, true);
        }
      } else {
        updateValues(id2);
      }
    };
  },
  /**
   * Generate legend item elements
   * @param {Array} targetIdz Data ids
   * @param {object} itemTileSize Item tile size {width, height}
   * @param {Function} updatePositions Update position function
   * @param {object} posFn Position functions
   * @private
   */
  generateLegendItem: function(targetIdz, itemTileSize, updatePositions, posFn) {
    var $$ = this;
    var config = $$.config, state = $$.state, legend2 = $$.$el.legend;
    var usePoint = config.legend_usePoint;
    var legendItemR = config.legend_item_tile_r;
    var legendType = config.legend_item_tile_type;
    var isRectangle = legendType !== "circle";
    var isLegendRightOrInset = state.isLegendRight || state.isLegendInset;
    var pos = -200;
    var l = legend2.selectAll(".".concat($LEGEND.legendItem)).data(targetIdz).enter().append("g");
    $$.setLegendItem(l);
    if (config.legend_tooltip) {
      l.append("title").text(function(id2) {
        return id2;
      });
    }
    l.append("text").text(function(id2) {
      return getFormattedText.bind($$)(id2);
    }).each(function(id2, i) {
      updatePositions(this, id2, i);
    }).style("pointer-events", $$.getStylePropValue("none")).attr("x", isLegendRightOrInset ? posFn.xText : pos).attr("y", isLegendRightOrInset ? pos : posFn.yText);
    l.append("rect").attr("class", $LEGEND.legendItemEvent).style("fill-opacity", $$.getStylePropValue("0")).attr("x", isLegendRightOrInset ? posFn.xRect : pos).attr("y", isLegendRightOrInset ? pos : posFn.yRect);
    if (usePoint) {
      var ids_2 = [];
      l.append(function(d) {
        var pattern = notEmpty(config.point_pattern) ? config.point_pattern : [config.point_type];
        ids_2.indexOf(d) === -1 && ids_2.push(d);
        var point6 = pattern[ids_2.indexOf(d) % pattern.length];
        if (point6 === "rectangle") {
          point6 = "rect";
        }
        return doc.createElementNS(namespaces_default.svg, "hasValidPointType" in $$ && $$.hasValidPointType(point6) ? point6 : "use");
      }).attr("class", $LEGEND.legendItemPoint).style("fill", getLegendColor.bind($$)).style("pointer-events", $$.getStylePropValue("none")).attr("href", function(data2, idx, selection3) {
        var node = selection3[idx];
        var nodeName = node.nodeName.toLowerCase();
        var id2 = $$.getTargetSelectorSuffix(data2);
        return nodeName === "use" ? "#".concat(state.datetimeId, "-point").concat(id2) : void 0;
      });
    } else {
      l.append(isRectangle ? "line" : legendType).attr("class", $LEGEND.legendItemTile).style("stroke", getLegendColor.bind($$)).style("pointer-events", $$.getStylePropValue("none")).call(function(selection3) {
        if (legendType === "circle") {
          selection3.attr("r", legendItemR).style("fill", getLegendColor.bind($$)).attr("cx", isLegendRightOrInset ? posFn.x2Tile : pos).attr("cy", isLegendRightOrInset ? pos : posFn.yTile);
        } else if (isRectangle) {
          selection3.attr("stroke-width", itemTileSize.height).attr("x1", isLegendRightOrInset ? posFn.x1Tile : pos).attr("y1", isLegendRightOrInset ? pos : posFn.yTile).attr("x2", isLegendRightOrInset ? posFn.x2Tile : pos).attr("y2", isLegendRightOrInset ? pos : posFn.yTile);
        }
      });
    }
  },
  /**
   * Update legend item position
   * @param {Array} targetIdz Data ids
   * @param {boolean} withTransition Whether or not to apply transition
   * @param {object} posFn Position functions
   * @private
   */
  updateLegendItemPos: function(targetIdz, withTransition, posFn) {
    var $$ = this;
    var config = $$.config, legend2 = $$.$el.legend, $T = $$.$T;
    var usePoint = config.legend_usePoint;
    var legendType = config.legend_item_tile_type;
    var isRectangle = legendType !== "circle";
    if (usePoint) {
      var tiles = legend2.selectAll(".".concat($LEGEND.legendItemPoint)).data(targetIdz);
      $T(tiles, withTransition).each(function() {
        var nodeName = this.nodeName.toLowerCase();
        var pointR = config.point_r;
        var x3 = "x";
        var y3 = "y";
        var xOffset = 2;
        var yOffset = 2.5;
        var radius = null;
        var width = null;
        var height = null;
        if (nodeName === "circle") {
          var size2 = pointR * 0.2;
          x3 = "cx";
          y3 = "cy";
          radius = pointR + size2;
          xOffset = pointR * 2;
          yOffset = -size2;
        } else if (nodeName === "rect") {
          var size2 = pointR * 2.5;
          width = size2;
          height = size2;
          yOffset = 3;
        }
        select_default2(this).attr(x3, function(d) {
          return posFn.x1Tile(d) + xOffset;
        }).attr(y3, function(d) {
          return posFn.yTile(d) - yOffset;
        }).attr("r", radius).attr("width", width).attr("height", height);
      });
    } else {
      var tiles = legend2.selectAll(".".concat($LEGEND.legendItemTile)).data(targetIdz);
      $T(tiles, withTransition).style("stroke", getLegendColor.bind($$)).call(function(selection3) {
        if (legendType === "circle") {
          selection3.attr("cx", function(d) {
            var x22 = posFn.x2Tile(d);
            return x22 - (x22 - posFn.x1Tile(d)) / 2;
          }).attr("cy", posFn.yTile);
        } else if (isRectangle) {
          selection3.attr("x1", posFn.x1Tile).attr("y1", posFn.yTile).attr("x2", posFn.x2Tile).attr("y2", posFn.yTile);
        }
      });
    }
  }
};
var redraw = {
  redraw: function(options2) {
    var _a2, _b, _c, _d;
    if (options2 === void 0) {
      options2 = {};
    }
    var $$ = this;
    var config = $$.config, state = $$.state, $el = $$.$el;
    var main2 = $el.main, treemap2 = $el.treemap;
    state.redrawing = true;
    var targetsToShow = $$.filterTargetsToShow($$.data.targets);
    var flow2 = options2.flow, initializing = options2.initializing;
    var wth = $$.getWithOption(options2);
    var duration = wth.Transition ? config.transition_duration : 0;
    var durationForExit = wth.TransitionForExit ? duration : 0;
    var durationForAxis = wth.TransitionForAxis ? duration : 0;
    var transitions = (_a2 = $$.axis) === null || _a2 === void 0 ? void 0 : _a2.generateTransitions(durationForAxis);
    $$.updateSizes(initializing);
    if (wth.Legend && config.legend_show) {
      options2.withTransition = !!duration;
      !treemap2 && $$.updateLegend($$.mapToIds($$.data.targets), options2, transitions);
    } else if (wth.Dimension) {
      $$.updateDimension(true);
    }
    config.data_empty_label_text && main2.select("text.".concat($TEXT.text, ".").concat($COMMON.empty)).attr("x", state.width / 2).attr("y", state.height / 2).text(config.data_empty_label_text).style("display", targetsToShow.length ? "none" : null);
    if (state.hasAxis) {
      $$.axis.redrawAxis(targetsToShow, wth, transitions, flow2, initializing);
      $$.hasGrid() && $$.updateGrid();
      config.regions.length && $$.updateRegion();
      ["bar", "candlestick", "line", "area"].forEach(function(v) {
        var name = capitalize(v);
        if (/^(line|area)$/.test(v) && $$.hasTypeOf(name) || $$.hasType(v)) {
          $$["update".concat(name)](wth.TransitionForExit);
        }
      });
      $el.text && main2.selectAll(".".concat($SELECT.selectedCircles)).filter($$.isBarType.bind($$)).selectAll("circle").remove();
      if (config.interaction_enabled && !flow2 && wth.EventRect) {
        $$.redrawEventRect();
        (_b = $$.bindZoomEvent) === null || _b === void 0 ? void 0 : _b.call($$);
      }
    } else {
      $el.arcs && $$.redrawArc(duration, durationForExit, wth.Transform);
      $el.radar && $$.redrawRadar();
      $el.polar && $$.redrawPolar();
      treemap2 && $$.updateTreemap(durationForExit);
    }
    if (!state.resizing && !treemap2 && ($$.hasPointType() || state.hasRadar)) {
      $$.updateCircle();
    } else if ((_c = $$.hasLegendDefsPoint) === null || _c === void 0 ? void 0 : _c.call($$)) {
      $$.data.targets.forEach($$.point("create", this));
    }
    $$.hasDataLabel() && !$$.hasArcType(null, ["radar"]) && $$.updateText();
    (_d = $$.redrawTitle) === null || _d === void 0 ? void 0 : _d.call($$);
    initializing && $$.updateTypesElements();
    $$.generateRedrawList(targetsToShow, flow2, duration, wth.Subchart);
    $$.updateTooltipOnRedraw();
    $$.callPluginHook("$redraw", options2, duration);
  },
  /**
   * Generate redraw list
   * @param {object} targets targets data to be shown
   * @param {object} flow flow object
   * @param {number} duration duration value
   * @param {boolean} withSubchart whether or not to show subchart
   * @private
   */
  generateRedrawList: function(targets, flow2, duration, withSubchart) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var shape2 = $$.getDrawShape();
    if (state.hasAxis) {
      config.subchart_show && $$.redrawSubchart(withSubchart, duration, shape2);
    }
    var flowFn = flow2 && $$.generateFlow({
      targets,
      flow: flow2,
      duration: flow2.duration,
      shape: shape2,
      xv: $$.xv.bind($$)
    });
    var withTransition = (duration || flowFn) && isTabVisible();
    var redrawList = $$.getRedrawList(shape2, flow2, flowFn, withTransition);
    var afterRedraw = function() {
      flowFn && flowFn();
      state.redrawing = false;
      callFn(config.onrendered, $$.api);
    };
    if (afterRedraw) {
      if (withTransition && redrawList.length) {
        var waitForDraw_1 = generateWait();
        transition().duration(duration).each(function() {
          redrawList.reduce(function(acc, t13) {
            return acc.concat(t13);
          }, []).forEach(function(t) {
            return waitForDraw_1.add(t);
          });
        }).call(waitForDraw_1, afterRedraw);
      } else if (!state.transiting) {
        afterRedraw();
      }
    }
    $$.mapToIds($$.data.targets).forEach(function(id2) {
      state.withoutFadeIn[id2] = true;
    });
  },
  getRedrawList: function(shape2, flow2, flowFn, withTransition) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, hasAxis = _a2.hasAxis, hasRadar = _a2.hasRadar, hasTreemap = _a2.hasTreemap, grid2 = $$.$el.grid;
    var _b = shape2.pos, cx = _b.cx, cy = _b.cy, xForText = _b.xForText, yForText = _b.yForText;
    var list = [];
    if (hasAxis) {
      if (config.grid_x_lines.length || config.grid_y_lines.length) {
        list.push($$.redrawGrid(withTransition));
      }
      if (config.regions.length) {
        list.push($$.redrawRegion(withTransition));
      }
      Object.keys(shape2.type).forEach(function(v) {
        var name = capitalize(v);
        var drawFn = shape2.type[v];
        if (/^(area|line)$/.test(v) && $$.hasTypeOf(name) || $$.hasType(v)) {
          list.push($$["redraw".concat(name)](drawFn, withTransition));
        }
      });
      !flow2 && grid2.main && list.push($$.updateGridFocus());
    }
    if (!$$.hasArcType() || hasRadar) {
      notEmpty(config.data_labels) && config.data_labels !== false && list.push($$.redrawText(xForText, yForText, flow2, withTransition));
    }
    if (($$.hasPointType() || hasRadar) && !$$.isPointFocusOnly()) {
      $$.redrawCircle && list.push($$.redrawCircle(cx, cy, withTransition, flowFn));
    }
    if (hasTreemap) {
      list.push($$.redrawTreemap(withTransition));
    }
    return list;
  },
  updateAndRedraw: function(options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    var $$ = this;
    var config = $$.config, state = $$.state;
    var transitions;
    options2.withTransition = getOption(options2, "withTransition", true);
    options2.withTransform = getOption(options2, "withTransform", false);
    options2.withLegend = getOption(options2, "withLegend", false);
    options2.withUpdateXDomain = true;
    options2.withUpdateOrgXDomain = true;
    options2.withTransitionForExit = false;
    options2.withTransitionForTransform = getOption(options2, "withTransitionForTransform", options2.withTransition);
    if (!(options2.withLegend && config.legend_show)) {
      if (state.hasAxis) {
        transitions = $$.axis.generateTransitions(options2.withTransitionForAxis ? config.transition_duration : 0);
      }
      $$.updateScales();
      $$.updateSvgSize();
      $$.transformAll(options2.withTransitionForTransform, transitions);
    }
    $$.redraw(options2, transitions);
  }
};
function getScale(type2, min4, max4) {
  if (type2 === void 0) {
    type2 = "linear";
  }
  if (min4 === void 0) {
    min4 = 0;
  }
  if (max4 === void 0) {
    max4 = 1;
  }
  var scale2 = {
    linear: linear3,
    log: symlog,
    _log: log,
    time,
    utc: utcTime
  }[type2]();
  scale2.type = type2;
  /_?log/.test(type2) && scale2.clamp(true);
  return scale2.range([min4, max4]);
}
var scale = {
  /**
   * Get x Axis scale function
   * @param {number} min Min range value
   * @param {number} max Max range value
   * @param {Array} domain Domain value
   * @param {Function} offset The offset getter to be sum
   * @returns {Function} scale
   * @private
   */
  getXScale: function(min4, max4, domain2, offset) {
    var $$ = this;
    var scale2 = $$.state.loading !== "append" && $$.scale.zoom || getScale($$.axis.getAxisType("x"), min4, max4);
    return $$.getCustomizedXScale(domain2 ? scale2.domain(domain2) : scale2, offset);
  },
  /**
   * Get y Axis scale function
   * @param {string} id Axis id: 'y' or 'y2'
   * @param {number} min Min value
   * @param {number} max Max value
   * @param {Array} domain Domain value
   * @returns {Function} Scale function
   * @private
   */
  getYScale: function(id2, min4, max4, domain2) {
    var $$ = this;
    var scale2 = getScale($$.axis.getAxisType(id2), min4, max4);
    domain2 && scale2.domain(domain2);
    return scale2;
  },
  /**
   * Get y Axis scale
   * @param {string} id Axis id
   * @param {boolean} isSub Weather is sub Axis
   * @returns {Function} Scale function
   * @private
   */
  getYScaleById: function(id2, isSub) {
    var _a2;
    if (isSub === void 0) {
      isSub = false;
    }
    var isY2 = ((_a2 = this.axis) === null || _a2 === void 0 ? void 0 : _a2.getId(id2)) === "y2";
    var key = isSub ? isY2 ? "subY2" : "subY" : isY2 ? "y2" : "y";
    return this.scale[key];
  },
  /**
   * Get customized x axis scale
   * @param {d3.scaleLinear|d3.scaleTime} scaleValue Scale function
   * @param {Function} offsetValue Offset getter to be sum
   * @returns {Function} Scale function
   * @private
   */
  getCustomizedXScale: function(scaleValue, offsetValue) {
    var $$ = this;
    var offset = offsetValue || function() {
      return $$.axis.x.tickOffset();
    };
    var isInverted = $$.config.axis_x_inverted;
    var scale2 = function(d, raw) {
      var v = scaleValue(d) + offset();
      return raw ? v : Math.ceil(v);
    };
    for (var key in scaleValue) {
      scale2[key] = scaleValue[key];
    }
    scale2.orgDomain = function() {
      return scaleValue.domain();
    };
    scale2.orgScale = function() {
      return scaleValue;
    };
    if ($$.axis.isCategorized()) {
      scale2.domain = function(domainValue) {
        var domain2 = domainValue;
        if (!arguments.length) {
          domain2 = this.orgDomain();
          return isInverted ? [domain2[0] + 1, domain2[1]] : [domain2[0], domain2[1] + 1];
        }
        scaleValue.domain(domain2);
        return scale2;
      };
    }
    return scale2;
  },
  /**
   * Update scale
   * @param {boolean} isInit Param is given at the init rendering
   * @param {boolean} updateXDomain If update x domain
   * @private
   */
  updateScales: function(isInit, updateXDomain) {
    var _a2, _b;
    if (updateXDomain === void 0) {
      updateXDomain = true;
    }
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, format3 = $$.format, org = $$.org, scale2 = $$.scale, _c = $$.state, current = _c.current, width = _c.width, height = _c.height, width2 = _c.width2, height2 = _c.height2, hasAxis = _c.hasAxis, hasTreemap = _c.hasTreemap;
    if (hasAxis) {
      var isRotated = config.axis_rotated;
      var resettedPadding = $$.getResettedPadding(1);
      var min4 = {
        x: isRotated ? resettedPadding : 0,
        y: isRotated ? 0 : height,
        subX: isRotated ? 1 : 0,
        subY: isRotated ? 0 : height2
      };
      var max4 = {
        x: isRotated ? height : width,
        y: isRotated ? width : resettedPadding,
        subX: isRotated ? height : width,
        subY: isRotated ? width2 : 1
      };
      var xDomain = updateXDomain && ((_a2 = scale2.x) === null || _a2 === void 0 ? void 0 : _a2.orgDomain());
      var xSubDomain = updateXDomain && org.xDomain;
      scale2.x = $$.getXScale(min4.x, max4.x, xDomain, function() {
        return axis3.x.tickOffset();
      });
      scale2.subX = $$.getXScale(min4.x, max4.x, xSubDomain, function(d) {
        var _a3;
        return d % 1 ? 0 : ((_a3 = axis3.subX) !== null && _a3 !== void 0 ? _a3 : axis3.x).tickOffset();
      });
      format3.xAxisTick = axis3.getXAxisTickFormat();
      format3.subXAxisTick = axis3.getXAxisTickFormat(true);
      axis3.setAxis("x", scale2.x, config.axis_x_tick_outer, isInit);
      if (config.subchart_show) {
        axis3.setAxis("subX", scale2.subX, config.axis_x_tick_outer, isInit);
      }
      scale2.y = $$.getYScale("y", min4.y, max4.y, scale2.y ? scale2.y.domain() : config.axis_y_default);
      scale2.subY = $$.getYScale("y", min4.subY, max4.subY, scale2.subY ? scale2.subY.domain() : config.axis_y_default);
      axis3.setAxis("y", scale2.y, config.axis_y_tick_outer, isInit);
      if (config.axis_y2_show) {
        scale2.y2 = $$.getYScale("y2", min4.y, max4.y, scale2.y2 ? scale2.y2.domain() : config.axis_y2_default);
        scale2.subY2 = $$.getYScale("y2", min4.subY, max4.subY, scale2.subY2 ? scale2.subY2.domain() : config.axis_y2_default);
        axis3.setAxis("y2", scale2.y2, config.axis_y2_tick_outer, isInit);
      }
    } else if (hasTreemap) {
      var padding = $$.getCurrentPadding();
      scale2.x = linear3().rangeRound([padding.left, current.width - padding.right]);
      scale2.y = linear3().rangeRound([padding.top, current.height - padding.bottom]);
    } else {
      (_b = $$.updateArc) === null || _b === void 0 ? void 0 : _b.call($$);
    }
  },
  /**
   * Get the zoom or unzoomed scaled value
   * @param {Date|number|object} d Data value
   * @returns {number|null}
   * @private
   */
  xx: function(d) {
    var $$ = this;
    var config = $$.config, _a2 = $$.scale, x3 = _a2.x, zoom2 = _a2.zoom;
    var fn = config.zoom_enabled && zoom2 ? zoom2 : x3;
    return d ? fn(isValue(d.x) ? d.x : d) : null;
  },
  xv: function(d) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, _a2 = $$.scale, x3 = _a2.x, zoom2 = _a2.zoom;
    var fn = config.zoom_enabled && zoom2 ? zoom2 : x3;
    var value = $$.getBaseValue(d);
    if (axis3.isTimeSeries()) {
      value = parseDate.call($$, value);
    } else if (axis3.isCategorized() && isString(value)) {
      value = config.axis_x_categories.indexOf(value);
    }
    return Math.ceil(fn(value));
  },
  yv: function(d) {
    var $$ = this;
    var _a2 = $$.scale, y3 = _a2.y, y23 = _a2.y2;
    var yScale = d.axis && d.axis === "y2" ? y23 : y3;
    return Math.ceil(yScale($$.getBaseValue(d)));
  },
  subxx: function(d) {
    return d ? this.scale.subX(d.x) : null;
  }
};
function getGroupedDataPointsFn(d) {
  var $$ = this;
  var fn;
  if ($$.isLineType(d)) {
    fn = $$.generateGetLinePoints($$.getShapeIndices($$.isLineType));
  } else if ($$.isBarType(d)) {
    fn = $$.generateGetBarPoints($$.getShapeIndices($$.isBarType));
  }
  return fn;
}
var shape = {
  /**
   * Get the shape draw function
   * @returns {object}
   * @private
   */
  getDrawShape: function() {
    var $$ = this;
    var isRotated = $$.config.axis_rotated;
    var _a2 = $$.state, hasRadar = _a2.hasRadar, hasTreemap = _a2.hasTreemap;
    var shape2 = { type: {}, indices: {}, pos: {} };
    !hasTreemap && ["bar", "candlestick", "line", "area"].forEach(function(v) {
      var name = capitalize(/^(bubble|scatter)$/.test(v) ? "line" : v);
      if ($$.hasType(v) || $$.hasTypeOf(name) || v === "line" && ($$.hasType("bubble") || $$.hasType("scatter"))) {
        var indices = $$.getShapeIndices($$["is".concat(name, "Type")]);
        var drawFn = $$["generateDraw".concat(name)];
        shape2.indices[v] = indices;
        shape2.type[v] = drawFn ? drawFn.bind($$)(indices, false) : void 0;
      }
    });
    if (!$$.hasArcType() || hasRadar || hasTreemap) {
      var cx = void 0;
      var cy = void 0;
      if (!hasTreemap) {
        cx = hasRadar ? $$.radarCircleX : isRotated ? $$.circleY : $$.circleX;
        cy = hasRadar ? $$.radarCircleY : isRotated ? $$.circleX : $$.circleY;
      }
      shape2.pos = {
        xForText: $$.generateXYForText(shape2.indices, true),
        yForText: $$.generateXYForText(shape2.indices, false),
        cx: (cx || function() {
        }).bind($$),
        cy: (cy || function() {
        }).bind($$)
      };
    }
    return shape2;
  },
  /**
   * Get shape's indices according it's position within each axis tick.
   *
   * From the below example, indices will be:
   * ==> {data1: 0, data2: 0, data3: 1, data4: 1, __max__: 1}
   *
   *	data1 data3   data1 data3
   *	data2 data4   data2 data4
   *	-------------------------
   *		 0             1
   * @param {Function} typeFilter Chart type filter function
   * @returns {object} Indices object with its position
   */
  getShapeIndices: function(typeFilter) {
    var $$ = this;
    var config = $$.config;
    var xs = config.data_xs;
    var hasXs = notEmpty(xs);
    var indices = {};
    var i = hasXs ? {} : 0;
    if (hasXs) {
      getUnique(Object.keys(xs).map(function(v) {
        return xs[v];
      })).forEach(function(v) {
        i[v] = 0;
        indices[v] = {};
      });
    }
    $$.filterTargetsToShow($$.data.targets.filter(typeFilter, $$)).forEach(function(d) {
      var _a2;
      var xKey = d.id in xs ? xs[d.id] : "";
      var ind = xKey ? indices[xKey] : indices;
      for (var j = 0, groups2 = void 0; groups2 = config.data_groups[j]; j++) {
        if (groups2.indexOf(d.id) < 0) {
          continue;
        }
        for (var k2 = 0, key = void 0; key = groups2[k2]; k2++) {
          if (key in ind) {
            ind[d.id] = ind[key];
            break;
          }
          if (d.id !== key && xKey) {
            ind[key] = (_a2 = ind[d.id]) !== null && _a2 !== void 0 ? _a2 : i[xKey];
          }
        }
      }
      if (isUndefined(ind[d.id])) {
        ind[d.id] = xKey ? i[xKey]++ : i++;
        ind.__max__ = (xKey ? i[xKey] : i) - 1;
      }
    });
    return indices;
  },
  /**
   * Get indices value based on data ID value
   * @param {object} indices Indices object
   * @param {object} d Data row
   * @param {string} caller Caller function name (Used only for 'sparkline' plugin)
   * @returns {object} Indices object
   * @private
   */
  getIndices: function(indices, d, caller) {
    var $$ = this;
    var _a2 = $$.config, xs = _a2.data_xs, removeNull = _a2.bar_indices_removeNull;
    var id2 = d.id, index2 = d.index;
    if ($$.isBarType(id2) && removeNull) {
      var ind_1 = {};
      $$.getAllValuesOnIndex(index2, true).forEach(function(v, i) {
        ind_1[v.id] = i;
        ind_1.__max__ = i;
      });
      return ind_1;
    }
    return notEmpty(xs) ? indices[xs[id2]] : indices;
  },
  /**
   * Get indices max number
   * @param {object} indices Indices object
   * @returns {number} Max number
   * @private
   */
  getIndicesMax: function(indices) {
    return notEmpty(this.config.data_xs) ? (
      // if is multiple xs, return total sum of xs' __max__ value
      Object.keys(indices).map(function(v) {
        return indices[v].__max__ || 0;
      }).reduce(function(acc, curr) {
        return acc + curr;
      })
    ) : indices.__max__;
  },
  getShapeX: function(offset, indices, isSub) {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale;
    var currScale = isSub ? scale2.subX : scale2.zoom || scale2.x;
    var barOverlap = config.bar_overlap;
    var barPadding = config.bar_padding;
    var sum3 = function(p, c) {
      return p + c;
    };
    var halfWidth = isObjectType(offset) && (offset._$total.length ? offset._$total.reduce(sum3) / 2 : 0);
    return function(d) {
      var ind = $$.getIndices(indices, d, "getShapeX");
      var index2 = d.id in ind ? ind[d.id] : 0;
      var targetsNum = (ind.__max__ || 0) + 1;
      var x3 = 0;
      if (notEmpty(d.x)) {
        var xPos = currScale(d.x, true);
        if (halfWidth) {
          var offsetWidth = offset[d.id] || offset._$width;
          x3 = barOverlap ? xPos - offsetWidth / 2 : xPos - offsetWidth + offset._$total.slice(0, index2 + 1).reduce(sum3) - halfWidth;
        } else {
          x3 = xPos - (isNumber(offset) ? offset : offset._$width) * (targetsNum / 2 - (barOverlap ? 1 : index2));
        }
      }
      if (offset && x3 && targetsNum > 1 && barPadding) {
        if (index2) {
          x3 += barPadding * index2;
        }
        if (targetsNum > 2) {
          x3 -= (targetsNum - 1) * barPadding / 2;
        } else if (targetsNum === 2) {
          x3 -= barPadding / 2;
        }
      }
      return x3;
    };
  },
  getShapeY: function(isSub) {
    var $$ = this;
    var isStackNormalized = $$.isStackNormalized();
    return function(d) {
      var value = d.value;
      if (isNumber(d)) {
        value = d;
      } else if ($$.isAreaRangeType(d)) {
        value = $$.getBaseValue(d, "mid");
      } else if (isStackNormalized) {
        value = $$.getRatio("index", d, true);
      } else if ($$.isBubbleZType(d)) {
        value = $$.getBubbleZData(d.value, "y");
      } else if ($$.isBarRangeType(d)) {
        value = value[1];
      }
      return $$.getYScaleById(d.id, isSub)(value);
    };
  },
  /**
   * Get shape based y Axis min value
   * @param {string} id Data id
   * @returns {number}
   * @private
   */
  getShapeYMin: function(id2) {
    var $$ = this;
    var axisId = $$.axis.getId(id2);
    var scale2 = $$.scale[axisId];
    var yMin = scale2.domain()[0];
    var inverted = $$.config["axis_".concat(axisId, "_inverted")];
    return !$$.isGrouped(id2) && !inverted && yMin > 0 ? yMin : 0;
  },
  /**
   * Get Shape's offset data
   * @param {Function} typeFilter Type filter function
   * @returns {object}
   * @private
   */
  getShapeOffsetData: function(typeFilter) {
    var $$ = this;
    var targets = $$.orderTargets($$.filterTargetsToShow($$.data.targets.filter(typeFilter, $$)));
    var isStackNormalized = $$.isStackNormalized();
    var shapeOffsetTargets = targets.map(function(target) {
      var rowValues = target.values;
      var values = {};
      if ($$.isStepType(target)) {
        rowValues = $$.convertValuesToStep(rowValues);
      }
      var rowValueMapByXValue = rowValues.reduce(function(out, d) {
        var key = Number(d.x);
        out[key] = d;
        values[key] = isStackNormalized ? $$.getRatio("index", d, true) : d.value;
        return out;
      }, {});
      return {
        id: target.id,
        rowValues,
        rowValueMapByXValue,
        values
      };
    });
    var indexMapByTargetId = targets.reduce(function(out, _a2, index2) {
      var id2 = _a2.id;
      out[id2] = index2;
      return out;
    }, {});
    return { indexMapByTargetId, shapeOffsetTargets };
  },
  getShapeOffset: function(typeFilter, indices, isSub) {
    var $$ = this;
    var _a2 = $$.getShapeOffsetData(typeFilter), shapeOffsetTargets = _a2.shapeOffsetTargets, indexMapByTargetId = _a2.indexMapByTargetId;
    var groupsZeroAs = $$.config.data_groupsZeroAs;
    return function(d, idx) {
      var id2 = d.id, value = d.value, x3 = d.x;
      var ind = $$.getIndices(indices, d);
      var scale2 = $$.getYScaleById(id2, isSub);
      if ($$.isBarRangeType(d)) {
        return scale2(value[0]);
      }
      var dataXAsNumber = Number(x3);
      var y0 = scale2(groupsZeroAs === "zero" ? 0 : $$.getShapeYMin(id2));
      var offset = y0;
      shapeOffsetTargets.filter(function(t) {
        return t.id !== id2 && ind[t.id] === ind[id2];
      }).forEach(function(t) {
        var tid = t.id, rowValueMapByXValue = t.rowValueMapByXValue, rowValues = t.rowValues, tvalues = t.values;
        if (indexMapByTargetId[tid] < indexMapByTargetId[id2]) {
          var rValue = tvalues[dataXAsNumber];
          var row = rowValues[idx];
          if (!row || Number(row.x) !== dataXAsNumber) {
            row = rowValueMapByXValue[dataXAsNumber];
          }
          if ((row === null || row === void 0 ? void 0 : row.value) * value >= 0 && isNumber(rValue)) {
            var addOffset = value === 0 ? groupsZeroAs === "positive" && rValue > 0 || groupsZeroAs === "negative" && rValue < 0 : true;
            if (addOffset) {
              offset += scale2(rValue) - y0;
            }
          }
        }
      });
      return offset;
    };
  },
  /**
   * Get data's y coordinate
   * @param {object} d Target data
   * @param {number} i Index number
   * @returns {number} y coordinate
   * @private
   */
  circleY: function(d, i) {
    var $$ = this;
    var id2 = d.id;
    var points;
    if ($$.isGrouped(id2)) {
      points = getGroupedDataPointsFn.bind($$)(d);
    }
    return points ? points(d, i)[0][1] : $$.getYScaleById(id2)($$.getBaseValue(d));
  },
  getBarW: function(type2, axis3, targetsNum) {
    var _a2, _b, _c, _d, _e;
    var $$ = this;
    var config = $$.config, org = $$.org, scale2 = $$.scale;
    var maxDataCount = $$.getMaxDataCount();
    var isGrouped = type2 === "bar" && ((_a2 = config.data_groups) === null || _a2 === void 0 ? void 0 : _a2.length);
    var configName = "".concat(type2, "_width");
    var k2 = ((_c = (_b = $$.getZoomTransform) === null || _b === void 0 ? void 0 : _b.call($$)) !== null && _c !== void 0 ? _c : { k: 1 }).k;
    var xMinMax = [
      (_d = config.axis_x_min) !== null && _d !== void 0 ? _d : org.xDomain[0],
      (_e = config.axis_x_max) !== null && _e !== void 0 ? _e : org.xDomain[1]
    ].map($$.axis.isTimeSeries() ? parseDate.bind($$) : Number);
    var tickInterval = axis3.tickInterval(maxDataCount);
    if (scale2.zoom && !$$.axis.isCategorized() && k2 > 1) {
      var isSameMinMax_1 = xMinMax.every(function(v, i) {
        return v === org.xDomain[i];
      });
      tickInterval = org.xDomain.map(function(v, i) {
        var value = isSameMinMax_1 ? v : v - Math.abs(xMinMax[i]);
        return scale2.zoom(value);
      }).reduce(function(a2, c) {
        return Math.abs(a2) + c;
      }) / maxDataCount;
    }
    var getWidth = function(id2) {
      var width = id2 ? config[configName][id2] : config[configName];
      var ratio = id2 ? width.ratio : config["".concat(configName, "_ratio")];
      var max4 = id2 ? width.max : config["".concat(configName, "_max")];
      var w = isNumber(width) ? width : targetsNum ? tickInterval * ratio / targetsNum : 0;
      return max4 && w > max4 ? max4 : w;
    };
    var result = getWidth();
    if (!isGrouped && isObjectType(config[configName])) {
      result = { _$width: result, _$total: [] };
      $$.filterTargetsToShow($$.data.targets).forEach(function(v) {
        if (config[configName][v.id]) {
          result[v.id] = getWidth(v.id);
          result._$total.push(result[v.id] || result._$width);
        }
      });
    }
    return result;
  },
  /**
   * Get shape element
   * @param {string} shapeName Shape string
   * @param {number} i Index number
   * @param {string} id Data series id
   * @returns {d3Selection}
   * @private
   */
  getShapeByIndex: function(shapeName, i, id2) {
    var $$ = this;
    var $el = $$.$el;
    var suffix = isValue(i) ? "-".concat(i) : "";
    var shape2 = $el[shapeName];
    if (shape2 && !shape2.empty()) {
      shape2 = shape2.filter(function(d) {
        return id2 ? d.id === id2 : true;
      }).filter(function(d) {
        return isValue(i) ? d.index === i : true;
      });
    } else {
      shape2 = (id2 ? $el.main.selectAll(".".concat(CLASS["".concat(shapeName, "s")]).concat($$.getTargetSelectorSuffix(id2))) : $el.main).selectAll(".".concat(CLASS[shapeName]).concat(suffix));
    }
    return shape2;
  },
  isWithinShape: function(that, d) {
    var _a2;
    var $$ = this;
    var shape2 = select_default2(that);
    var isWithin;
    if (!$$.isTargetToShow(d.id)) {
      isWithin = false;
    } else if ((_a2 = $$.hasValidPointType) === null || _a2 === void 0 ? void 0 : _a2.call($$, that.nodeName)) {
      isWithin = $$.isStepType(d) ? $$.isWithinStep(that, $$.getYScaleById(d.id)(d.value)) : $$.isWithinCircle(that, $$.isBubbleType(d) ? $$.pointSelectR(d) * 1.5 : 0);
    } else if (that.nodeName === "path") {
      isWithin = shape2.classed(CLASS.bar) ? $$.isWithinBar(that) : true;
    }
    return isWithin;
  },
  getInterpolate: function(d) {
    var $$ = this;
    var interpolation = $$.getInterpolateType(d);
    return {
      "basis": basis_default2,
      "basis-closed": basisClosed_default2,
      "basis-open": basisOpen_default,
      "bundle": bundle_default,
      "cardinal": cardinal_default,
      "cardinal-closed": cardinalClosed_default,
      "cardinal-open": cardinalOpen_default,
      "catmull-rom": catmullRom_default,
      "catmull-rom-closed": catmullRomClosed_default,
      "catmull-rom-open": catmullRomOpen_default,
      "monotone-x": monotoneX,
      "monotone-y": monotoneY,
      "natural": natural_default,
      "linear-closed": linearClosed_default,
      "linear": linear_default,
      "step": step_default,
      "step-after": stepAfter,
      "step-before": stepBefore
    }[interpolation];
  },
  getInterpolateType: function(d) {
    var $$ = this;
    var config = $$.config;
    var type2 = config.spline_interpolation_type;
    var interpolation = $$.isInterpolationType(type2) ? type2 : "cardinal";
    return $$.isSplineType(d) ? interpolation : $$.isStepType(d) ? config.line_step_type : "linear";
  },
  isWithinBar: function(that) {
    var mouse = getPointer(this.state.event, that);
    var list = getRectSegList(that);
    var seg0 = list[0], seg1 = list[1];
    var x3 = Math.min(seg0.x, seg1.x);
    var y3 = Math.min(seg0.y, seg1.y);
    var offset = this.config.bar_sensitivity;
    var _a2 = that.getBBox(), width = _a2.width, height = _a2.height;
    var sx = x3 - offset;
    var ex = x3 + width + offset;
    var sy = y3 + height + offset;
    var ey = y3 - offset;
    var isWithin = sx < mouse[0] && mouse[0] < ex && ey < mouse[1] && mouse[1] < sy;
    return isWithin;
  }
};
var size = {
  /**
   * Update container size
   * @private
   */
  setContainerSize: function() {
    var $$ = this;
    var state = $$.state;
    state.current.width = $$.getCurrentWidth();
    state.current.height = $$.getCurrentHeight();
  },
  getCurrentWidth: function() {
    var $$ = this;
    return $$.config.size_width || $$.getParentWidth();
  },
  getCurrentHeight: function() {
    var $$ = this;
    var config = $$.config;
    var h = config.size_height || $$.getParentHeight();
    return h > 0 ? h : 320 / ($$.hasType("gauge") && !config.gauge_fullCircle ? 2 : 1);
  },
  /**
   * Get the parent rect element's size
   * @param {string} key property/attribute name
   * @returns {number}
   * @private
   */
  getParentRectValue: function(key) {
    var offsetName = "offset".concat(capitalize(key));
    var parent = this.$el.chart.node();
    var v = 0;
    while (v < 30 && parent && parent.tagName !== "BODY") {
      try {
        v = parent.getBoundingClientRect()[key];
      } catch (e) {
        if (offsetName in parent) {
          v = parent[offsetName];
        }
      }
      parent = parent.parentNode;
    }
    var bodySize = doc.body[offsetName];
    v > bodySize && (v = bodySize);
    return v;
  },
  getParentWidth: function() {
    return this.getParentRectValue("width");
  },
  getParentHeight: function() {
    var h = this.$el.chart.style("height");
    var height = 0;
    if (h) {
      height = /px$/.test(h) ? parseInt(h, 10) : this.getParentRectValue("height");
    }
    return height;
  },
  getSvgLeft: function(withoutRecompute) {
    var $$ = this;
    var config = $$.config, hasAxis = $$.state.hasAxis, $el = $$.$el;
    var isRotated = config.axis_rotated;
    var hasLeftAxisRect = isRotated || !isRotated && !config.axis_y_inner;
    var leftAxisClass = isRotated ? $AXIS.axisX : $AXIS.axisY;
    var leftAxis = $el.main.select(".".concat(leftAxisClass)).node();
    var leftLabel = hasAxis && config["axis_".concat(isRotated ? "x" : "y", "_label")];
    var labelWidth = 0;
    if (hasAxis && (isString(leftLabel) || isString(leftLabel.text) || /^inner-/.test(leftLabel === null || leftLabel === void 0 ? void 0 : leftLabel.position))) {
      var label = $el.main.select(".".concat(leftAxisClass, "-label"));
      if (!label.empty()) {
        labelWidth = label.node().getBoundingClientRect().left;
      }
    }
    var svgRect = leftAxis && hasLeftAxisRect ? leftAxis.getBoundingClientRect() : { right: 0 };
    var chartRectLeft = $el.chart.node().getBoundingClientRect().left + labelWidth;
    var hasArc = $$.hasArcType();
    var svgLeft = svgRect.right - chartRectLeft - (hasArc ? 0 : $$.getCurrentPaddingByDirection("left", withoutRecompute));
    return svgLeft > 0 ? svgLeft : 0;
  },
  updateDimension: function(withoutAxis) {
    var _a2;
    var $$ = this;
    var config = $$.config, hasAxis = $$.state.hasAxis, $el = $$.$el;
    if (hasAxis && !withoutAxis && $$.axis.x && config.axis_rotated) {
      (_a2 = $$.axis.subX) === null || _a2 === void 0 ? void 0 : _a2.create($el.axis.subX);
    }
    $$.updateScales(withoutAxis);
    $$.updateSvgSize();
    $$.transformAll(false);
  },
  updateSvgSize: function() {
    var $$ = this;
    var _a2 = $$.state, clip2 = _a2.clip, current = _a2.current, hasAxis = _a2.hasAxis, width = _a2.width, height = _a2.height, svg = $$.$el.svg;
    svg.attr("width", current.width).attr("height", current.height);
    if (hasAxis) {
      var brush2 = svg.select(".".concat($SUBCHART.brush, " .overlay"));
      var brushSize = { width: 0, height: 0 };
      if (brush2.size()) {
        brushSize.width = +brush2.attr("width");
        brushSize.height = +brush2.attr("height");
      }
      svg.selectAll(["#".concat(clip2.id), "#".concat(clip2.idGrid)]).select("rect").attr("width", width).attr("height", height);
      svg.select("#".concat(clip2.idXAxis)).select("rect").call($$.setXAxisClipPath.bind($$));
      svg.select("#".concat(clip2.idYAxis)).select("rect").call($$.setYAxisClipPath.bind($$));
      clip2.idSubchart && svg.select("#".concat(clip2.idSubchart)).select("rect").attr("width", width).attr("height", brushSize.height);
    }
  },
  /**
   * Get padding by the direction.
   * @param {string} type "top" | "bottom" | "left" | "right"
   * @param {boolean} [withoutRecompute=false] If set true, do not recompute the padding value.
   * @param {boolean} [withXAxisTickTextOverflow=false] If set true, calculate x axis tick text overflow.
   * @returns {number} padding value
   * @private
   */
  getCurrentPaddingByDirection: function(type2, withoutRecompute, withXAxisTickTextOverflow) {
    var _a2;
    if (withoutRecompute === void 0) {
      withoutRecompute = false;
    }
    if (withXAxisTickTextOverflow === void 0) {
      withXAxisTickTextOverflow = false;
    }
    var $$ = this;
    var config = $$.config, $el = $$.$el, hasAxis = $$.state.hasAxis;
    var isRotated = config.axis_rotated;
    var isFitPadding = ((_a2 = config.padding) === null || _a2 === void 0 ? void 0 : _a2.mode) === "fit";
    var paddingOption = isNumber(config["padding_".concat(type2)]) ? config["padding_".concat(type2)] : void 0;
    var axisId = hasAxis ? {
      top: isRotated ? "y2" : null,
      bottom: isRotated ? "y" : "x",
      left: isRotated ? "x" : "y",
      right: isRotated ? null : "y2"
    }[type2] : null;
    var isLeftRight = /^(left|right)$/.test(type2);
    var isAxisInner = axisId && config["axis_".concat(axisId, "_inner")];
    var isAxisShow = axisId && config["axis_".concat(axisId, "_show")];
    var axesLen = axisId ? config["axis_".concat(axisId, "_axes")].length : 0;
    var axisSize = axisId ? isLeftRight ? $$.getAxisWidthByAxisId(axisId, withoutRecompute) : $$.getHorizontalAxisHeight(axisId) : 0;
    var defaultPadding = 20;
    var gap = 0;
    if (!isFitPadding && isLeftRight) {
      axisSize = ceil10(axisSize);
    }
    var padding = hasAxis && isLeftRight && (isAxisInner || isUndefined(paddingOption) && !isAxisShow) ? 0 : isFitPadding ? (isAxisShow ? axisSize : 0) + (paddingOption !== null && paddingOption !== void 0 ? paddingOption : 0) : isUndefined(paddingOption) ? axisSize : paddingOption;
    if (isLeftRight && hasAxis) {
      if (axisId && (isFitPadding || isAxisInner) && config["axis_".concat(axisId, "_label")].text) {
        padding += $$.axis.getAxisLabelPosition(axisId).isOuter ? defaultPadding : 0;
      }
      if (type2 === "right") {
        padding += isRotated ? !isFitPadding && isUndefined(paddingOption) ? 10 : 2 : !isAxisShow || isAxisInner ? isFitPadding ? 2 : 1 : 0;
        padding += withXAxisTickTextOverflow ? $$.axis.getXAxisTickTextY2Overflow(defaultPadding) : 0;
      } else if (type2 === "left" && isRotated && isUndefined(paddingOption)) {
        padding = !config.axis_x_show ? 1 : isFitPadding ? axisSize : Math.max(axisSize, 40);
      }
    } else {
      if (type2 === "top") {
        if ($el.title && $el.title.node()) {
          padding += $$.getTitlePadding();
        }
        gap = isRotated && !isAxisInner ? axesLen : 0;
      } else if (type2 === "bottom" && hasAxis && isRotated && !isAxisShow) {
        padding += 1;
      }
    }
    return padding + axisSize * axesLen - gap;
  },
  getCurrentPadding: function(withXAxisTickTextOverflow) {
    if (withXAxisTickTextOverflow === void 0) {
      withXAxisTickTextOverflow = false;
    }
    var $$ = this;
    var _a2 = ["top", "bottom", "left", "right"].map(function(v) {
      return $$.getCurrentPaddingByDirection(v, null, withXAxisTickTextOverflow);
    }), top2 = _a2[0], bottom2 = _a2[1], left2 = _a2[2], right2 = _a2[3];
    return { top: top2, bottom: bottom2, left: left2, right: right2 };
  },
  /**
   * Get resetted padding values when 'padding=false' option is set
   * https://github.com/naver/billboard.js/issues/2367
   * @param {number|object} v Padding values to be resetted
   * @returns {number|object} Padding value
   * @private
   */
  getResettedPadding: function(v) {
    var $$ = this;
    var config = $$.config;
    var isNum = isNumber(v);
    var p = isNum ? 0 : {};
    if (config.padding === false) {
      !isNum && Object.keys(v).forEach(function(key) {
        p[key] = !isEmpty(config.data_labels) && config.data_labels !== false && key === "top" ? v[key] : 0;
      });
    } else {
      p = v;
    }
    return p;
  },
  /**
   * Update size values
   * @param {boolean} isInit If is called at initialization
   * @private
   */
  updateSizes: function(isInit) {
    var _a2, _b, _c, _d, _e;
    var $$ = this;
    var config = $$.config, state = $$.state, legend2 = $$.$el.legend;
    var isRotated = config.axis_rotated;
    var isNonAxis = $$.hasArcType() || state.hasTreemap;
    var isFitPadding = ((_a2 = config.padding) === null || _a2 === void 0 ? void 0 : _a2.mode) === "fit";
    !isInit && $$.setContainerSize();
    var currLegend = {
      width: legend2 ? $$.getLegendWidth() : 0,
      height: legend2 ? $$.getLegendHeight() : 0
    };
    if (!isNonAxis && config.axis_x_show && config.axis_x_tick_autorotate) {
      $$.updateXAxisTickClip();
    }
    var legendSize = {
      right: config.legend_show && state.isLegendRight ? $$.getLegendWidth() + (isFitPadding ? 0 : 20) : 0,
      bottom: !config.legend_show || state.isLegendRight || state.isLegendInset ? 0 : currLegend.height
    };
    var xAxisHeight = isRotated || isNonAxis ? 0 : $$.getHorizontalAxisHeight("x");
    var subchartXAxisHeight = config.subchart_axis_x_show && config.subchart_axis_x_tick_text_show ? xAxisHeight : 30;
    var subchartHeight = config.subchart_show && !isNonAxis ? config.subchart_size_height + subchartXAxisHeight : 0;
    var gaugeHeight = $$.hasType("gauge") && config.arc_needle_show && !config.gauge_fullCircle && !config.gauge_label_show ? 10 : 0;
    var padding = $$.getCurrentPadding(true);
    state.margin = !isNonAxis && isRotated ? {
      top: padding.top,
      right: isNonAxis ? 0 : padding.right + legendSize.right,
      bottom: legendSize.bottom + padding.bottom,
      left: subchartHeight + (isNonAxis ? 0 : padding.left)
    } : {
      top: (isFitPadding ? 0 : 4) + padding.top,
      // for top tick text
      right: isNonAxis ? 0 : padding.right + legendSize.right,
      bottom: gaugeHeight + subchartHeight + legendSize.bottom + padding.bottom,
      left: isNonAxis ? 0 : padding.left
    };
    state.margin = $$.getResettedPadding(state.margin);
    state.margin2 = isRotated ? {
      top: state.margin.top,
      right: NaN,
      bottom: 20 + legendSize.bottom,
      left: $$.state.rotatedPadding.left
    } : {
      top: state.current.height - subchartHeight - legendSize.bottom,
      right: NaN,
      bottom: subchartXAxisHeight + legendSize.bottom,
      left: state.margin.left
    };
    state.margin3 = {
      top: 0,
      right: NaN,
      bottom: 0,
      left: 0
    };
    (_b = $$.updateSizeForLegend) === null || _b === void 0 ? void 0 : _b.call($$, currLegend);
    state.width = state.current.width - state.margin.left - state.margin.right;
    state.height = state.current.height - state.margin.top - state.margin.bottom;
    if (state.width < 0) {
      state.width = 0;
    }
    if (state.height < 0) {
      state.height = 0;
    }
    state.width2 = isRotated ? state.margin.left - state.rotatedPadding.left - state.rotatedPadding.right : state.width;
    state.height2 = isRotated ? state.height : state.current.height - state.margin2.top - state.margin2.bottom;
    if (state.width2 < 0) {
      state.width2 = 0;
    }
    if (state.height2 < 0) {
      state.height2 = 0;
    }
    if ($$.hasArcType()) {
      var hasGauge = $$.hasType("gauge");
      var isLegendRight = config.legend_show && state.isLegendRight;
      var textWidth = (_c = state.hasRadar && $$.cache.get(KEY.radarTextWidth)) !== null && _c !== void 0 ? _c : 0;
      state.arcWidth = state.width - (isLegendRight ? currLegend.width + 10 : 0) - textWidth;
      state.arcHeight = state.height - (isLegendRight && !hasGauge ? 0 : 10);
      if ((_d = config.arc_rangeText_values) === null || _d === void 0 ? void 0 : _d.length) {
        if (hasGauge) {
          state.arcWidth -= 25;
          state.arcHeight -= 10;
          state.margin.left += 10;
        } else {
          state.arcHeight -= 20;
          state.margin.top += 10;
        }
      }
      if (hasGauge && !config.gauge_fullCircle) {
        state.arcHeight += state.height - $$.getPaddingBottomForGauge();
      }
      (_e = $$.updateRadius) === null || _e === void 0 ? void 0 : _e.call($$);
    }
    if (state.isLegendRight && isNonAxis) {
      state.margin3.left = state.arcWidth / 2 + state.radiusExpanded * 1.1;
    }
  }
};
var style = {
  /**
   * Add props color css rule to given selector
   * @param {boolean} withShape Set shpes' prefix class
   * @param {string} selector CSS selector
   * @param {Array} props CSS props list
   * @param {Function} propsFn Function to retrieve value or determine for props
   * @returns {Function}
   * @private
   */
  setCssRule: function(withShape, selector, props, propsFn) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, cssRule = _a2.cssRule, style2 = _a2.style;
    return config.boost_useCssRule ? function(selection3) {
      selection3.each(function(d) {
        var res = propsFn && (propsFn === null || propsFn === void 0 ? void 0 : propsFn.call($$, d));
        var shapeSelector = "".concat(withShape ? ".".concat($SHAPE.shapes + $$.getTargetSelectorSuffix(d.id)) : "").concat(selector);
        selector in cssRule && style2.sheet.deleteRule(cssRule[shapeSelector]);
        $$.state.cssRule[shapeSelector] = addCssRules(style2, shapeSelector, props.filter(Boolean).map(function(v) {
          return isString(res) && v.indexOf(":") === -1 ? "".concat(v, ": ").concat(res) : v || "";
        }));
      });
    } : function() {
    };
  },
  /**
   * Get style prop value
   * @param {Function|string} v Value
   * @returns {string|null}
   * @private
   */
  getStylePropValue: function(v) {
    var useCssRule = this.config.boost_useCssRule;
    return useCssRule ? null : isFunction(v) ? v.bind(this) : v;
  }
};
function getRotateAnchor(angle) {
  var anchor = "middle";
  if (angle > 0 && angle <= 170) {
    anchor = "end";
  } else if (angle > 190 && angle <= 360) {
    anchor = "start";
  }
  return anchor;
}
function setRotatePos(d, pos, anchor, isRotated, isInverted) {
  var _a2;
  var $$ = this;
  var value = d.value;
  var isCandlestickType = $$.isCandlestickType(d);
  var isNegative = isNumber(value) && value < 0 || isCandlestickType && !((_a2 = $$.getCandlestickData(d)) === null || _a2 === void 0 ? void 0 : _a2._isUp);
  var x3 = pos.x, y3 = pos.y;
  var gap = 4;
  var doubleGap = gap * 2;
  if (isRotated) {
    if (anchor === "start") {
      x3 += isNegative ? 0 : doubleGap;
      y3 += gap;
    } else if (anchor === "middle") {
      x3 += doubleGap;
      y3 -= doubleGap;
    } else if (anchor === "end") {
      isNegative && (x3 -= doubleGap);
      y3 += gap;
    }
  } else {
    if (anchor === "start") {
      x3 += gap;
      isNegative && (y3 += doubleGap * 2);
    } else if (anchor === "middle") {
      y3 -= doubleGap;
    } else if (anchor === "end") {
      x3 -= gap;
      isNegative && (y3 += doubleGap * 2);
    }
    if (isInverted) {
      y3 += isNegative ? -17 : isCandlestickType ? 13 : 7;
    }
  }
  return { x: x3, y: y3 };
}
function getTextPos(d, type2) {
  var _a2;
  var position2 = this.config.data_labels_position;
  var id2 = d.id, index2 = d.index, value = d.value;
  return (_a2 = isFunction(position2) ? position2.bind(this.api)(type2, value, id2, index2, this.$el.text) : (id2 in position2 ? position2[id2] : position2)[type2]) !== null && _a2 !== void 0 ? _a2 : 0;
}
var text = {
  opacityForText: function(d) {
    var $$ = this;
    return $$.isBarType(d) && !$$.meetsLabelThreshold(Math.abs($$.getRatio("bar", d)), "bar") ? "0" : $$.hasDataLabel ? null : "0";
  },
  /**
   * Initializes the text
   * @private
   */
  initText: function() {
    var $el = this.$el;
    $el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $TEXT.chartTexts).style("pointer-events", $el.treemap ? "none" : null);
  },
  /**
   * Update chartText
   * @param {object} targets $$.data.targets
   * @private
   */
  updateTargetsForText: function(targets) {
    var $$ = this;
    var classChartText = $$.getChartClass("Text");
    var classTexts = $$.getClass("texts", "id");
    var classFocus = $$.classFocus.bind($$);
    var mainTextUpdate = $$.$el.main.select(".".concat($TEXT.chartTexts)).selectAll(".".concat($TEXT.chartText)).data(targets).attr("class", function(d) {
      return "".concat(classChartText(d)).concat(classFocus(d)).trim();
    });
    var mainTextEnter = mainTextUpdate.enter().append("g").style("opacity", "0").attr("class", classChartText).call($$.setCssRule(true, " .".concat($TEXT.text), ["fill", "pointer-events:none"], $$.updateTextColor));
    mainTextEnter.append("g").attr("class", classTexts);
  },
  /**
   * Update text
   * @private
   */
  updateText: function() {
    var $$ = this;
    var $el = $$.$el, $T = $$.$T, config = $$.config, axis3 = $$.axis;
    var classText = $$.getClass("text", "index");
    var labelsCentered = config.data_labels.centered;
    var text2 = $el.main.selectAll(".".concat($TEXT.texts)).selectAll(".".concat($TEXT.text)).data($$.labelishData.bind($$));
    $T(text2.exit()).style("fill-opacity", "0").remove();
    $el.text = text2.enter().append("text").merge(text2).attr("class", classText).attr("text-anchor", function(d) {
      var isInverted = config["axis_".concat(axis3 === null || axis3 === void 0 ? void 0 : axis3.getId(d.id), "_inverted")];
      var isEndAnchor = isInverted ? d.value > 0 : d.value < 0;
      if ($$.isCandlestickType(d)) {
        var data2 = $$.getCandlestickData(d);
        isEndAnchor = !(data2 === null || data2 === void 0 ? void 0 : data2._isUp);
      } else if ($$.isTreemapType(d)) {
        return labelsCentered ? "middle" : "start";
      }
      return config.axis_rotated ? isEndAnchor ? "end" : "start" : "middle";
    }).style("fill", $$.getStylePropValue($$.updateTextColor)).style("fill-opacity", "0").each(function(d, i, texts) {
      var node = select_default2(this);
      var value = d.value;
      if ($$.isBubbleZType(d)) {
        value = $$.getBubbleZData(value, "z");
      } else if ($$.isCandlestickType(d)) {
        var data2 = $$.getCandlestickData(d);
        if (data2) {
          value = data2.close;
        }
      }
      value = $$.isTreemapType(d) ? $$.treemapDataLabelFormat(d)(node) : $$.dataLabelFormat(d.id)(value, d.id, d.index, texts);
      if (isNumber(value)) {
        this.textContent = value;
      } else {
        setTextValue(node, value);
      }
    });
  },
  updateTextColor: function(d) {
    var $$ = this;
    var config = $$.config;
    var labelColors = config.data_labels_colors;
    var defaultColor = $$.isArcType(d) && !$$.isRadarType(d) || $$.isTreemapType(d) ? null : $$.color(d);
    var color3;
    if (isString(labelColors)) {
      color3 = labelColors;
    } else if (isObject(labelColors)) {
      var id2 = (d.data || d).id;
      color3 = labelColors[id2];
    } else if (isFunction(labelColors)) {
      color3 = labelColors.bind($$.api)(defaultColor, d);
    }
    if ($$.isCandlestickType(d) && !isFunction(labelColors)) {
      var value = $$.getCandlestickData(d);
      if (!(value === null || value === void 0 ? void 0 : value._isUp)) {
        var downColor = config.candlestick_color_down;
        color3 = isObject(downColor) ? downColor[d.id] : downColor;
      }
    }
    return color3 || defaultColor;
  },
  /**
   * Update data label text background color
   * @param {object} d Data object
   * @returns {string|null}
   * @private
   */
  updateTextBackgroundColor: function(d) {
    var $$ = this;
    var $el = $$.$el, config = $$.config;
    var backgroundColor = config.data_labels_backgroundColors;
    var color3 = "";
    if (isString(backgroundColor) || isObject(backgroundColor)) {
      var id2 = isString(backgroundColor) ? "" : $$.getTargetSelectorSuffix("id" in d ? d.id : d.data.id);
      var filter3 = $el.defs.select(["filter[id*='labels-bg", "']"].join(id2));
      if (filter3.size()) {
        color3 = "url(#".concat(filter3.attr("id"), ")");
      }
    }
    return color3 || null;
  },
  /**
   * Redraw chartText
   * @param {Function} getX Positioning function for x
   * @param {Function} getY Positioning function for y
   * @param {boolean} forFlow Weather is flow
   * @param {boolean} withTransition transition is enabled
   * @returns {Array}
   * @private
   */
  redrawText: function(getX, getY, forFlow, withTransition) {
    var $$ = this;
    var $T = $$.$T, axis3 = $$.axis, config = $$.config, hasTreemap = $$.state.hasTreemap;
    var t = getRandom(true);
    var isRotated = config.axis_rotated;
    var angle = config.data_labels.rotate;
    var anchorString = getRotateAnchor(angle);
    var rotateString = angle ? "rotate(".concat(angle, ")") : "";
    $$.$el.text.style("fill", $$.getStylePropValue($$.updateTextColor)).attr("filter", $$.updateTextBackgroundColor.bind($$)).style("fill-opacity", forFlow ? 0 : $$.opacityForText.bind($$)).each(function(d, i) {
      var node = $T(hasTreemap && this.childElementCount ? this.parentNode : this, !!(withTransition && this.getAttribute("x")), t);
      var isInverted = config["axis_".concat(axis3 === null || axis3 === void 0 ? void 0 : axis3.getId(d.id), "_inverted")];
      var pos = {
        x: getX.bind(this)(d, i),
        y: getY.bind(this)(d, i)
      };
      if (angle) {
        pos = setRotatePos.bind($$)(d, pos, anchorString, isRotated, isInverted);
        node.attr("text-anchor", anchorString);
      }
      if (this.childElementCount || angle) {
        node.attr("transform", "translate(".concat(pos.x, " ").concat(pos.y, ") ").concat(rotateString));
      } else {
        node.attr("x", pos.x).attr("y", pos.y);
      }
    });
    return true;
  },
  /**
   * Gets the getBoundingClientRect value of the element
   * @param {HTMLElement|d3.selection} element Target element
   * @param {string} className Class name
   * @returns {object} value of element.getBoundingClientRect()
   * @private
   */
  getTextRect: function(element, className) {
    var $$ = this;
    var base = element.node ? element.node() : element;
    if (!/text/i.test(base.tagName)) {
      base = base.querySelector("text");
    }
    var text2 = base.textContent;
    var cacheKey = "".concat(KEY.textRect, "-").concat(text2.replace(/\W/g, "_"));
    var rect = $$.cache.get(cacheKey);
    if (!rect) {
      $$.$el.svg.append("text").style("visibility", "hidden").style("font", select_default2(base).style("font")).classed(className, true).text(text2).call(function(v) {
        rect = getBoundingRect(v.node());
      }).remove();
      $$.cache.add(cacheKey, rect);
    }
    return rect;
  },
  /**
   * Gets the x or y coordinate of the text
   * @param {object} indices Indices values
   * @param {boolean} forX whether or not to x
   * @returns {number} coordinates
   * @private
   */
  generateXYForText: function(indices, forX) {
    var $$ = this;
    var _a2 = $$.state, hasRadar = _a2.hasRadar, hasTreemap = _a2.hasTreemap;
    var types = Object.keys(indices);
    var points = {};
    var getter = forX ? $$.getXForText : $$.getYForText;
    hasRadar && types.push("radar");
    hasTreemap && types.push("treemap");
    types.forEach(function(v) {
      points[v] = $$["generateGet".concat(capitalize(v), "Points")](indices[v], false);
    });
    return function(d, i) {
      var type2 = $$.isAreaType(d) && "area" || $$.isBarType(d) && "bar" || $$.isCandlestickType(d) && "candlestick" || $$.isRadarType(d) && "radar" || $$.isTreemapType(d) && "treemap" || "line";
      return getter.call($$, points[type2](d, i), d, this);
    };
  },
  /**
   * Get centerized text position for bar type data.label.text
   * @param {object} d Data object
   * @param {Array} points Data points position
   * @param {HTMLElement} textElement Data label text element
   * @param {string} type 'x' or 'y'
   * @returns {number} Position value
   * @private
   */
  getCenteredTextPos: function(d, points, textElement, type2) {
    var $$ = this;
    var config = $$.config;
    var isRotated = config.axis_rotated;
    var isBarType = $$.isBarType(d);
    var isTreemapType = $$.isTreemapType(d);
    if (config.data_labels.centered && (isBarType || isTreemapType)) {
      var rect = getBoundingRect(textElement);
      if (isBarType) {
        var isPositive = $$.getRangedData(d, null, "bar") >= 0;
        if (isRotated) {
          var w = (isPositive ? points[1][1] - points[0][1] : points[0][1] - points[1][1]) / 2 + rect.width / 2;
          return isPositive ? -w - 3 : w + 2;
        } else {
          var h = (isPositive ? points[0][1] - points[1][1] : points[1][1] - points[0][1]) / 2 + rect.height / 2;
          return isPositive ? h : -h - 2;
        }
      } else if (isTreemapType) {
        return type2 === "x" ? (points[1][0] - points[0][0]) / 2 : (points[1][1] - points[0][1]) / 2 + rect.height / 2;
      }
    }
    return 0;
  },
  /**
   * Gets the x coordinate of the text
   * @param {object} points Data points position
   * @param {object} d Data object
   * @param {HTMLElement} textElement Data label text element
   * @returns {number} x coordinate
   * @private
   */
  getXForText: function(points, d, textElement) {
    var _a2;
    var $$ = this;
    var config = $$.config;
    var isRotated = config.axis_rotated;
    var isTreemapType = $$.isTreemapType(d);
    var xPos = points[0][0];
    if ($$.isCandlestickType(d)) {
      if (isRotated) {
        xPos = ((_a2 = $$.getCandlestickData(d)) === null || _a2 === void 0 ? void 0 : _a2._isUp) ? points[2][2] + 4 : points[2][1] - 4;
      } else {
        xPos += (points[1][0] - xPos) / 2;
      }
    } else if (isTreemapType) {
      xPos += config.data_labels.centered ? 0 : 5;
    } else {
      if (isRotated) {
        var isInverted = config["axis_".concat($$.axis.getId(d.id), "_inverted")];
        var padding = $$.isBarType(d) ? 4 : 6;
        var value = d.value;
        xPos = points[2][1];
        if (isInverted) {
          xPos -= padding * (value > 0 ? 1 : -1);
        } else {
          xPos += padding * (value < 0 ? -1 : 1);
        }
      } else {
        xPos = $$.hasType("bar") ? (points[2][0] + points[0][0]) / 2 : xPos;
      }
    }
    if (isRotated || isTreemapType) {
      xPos += $$.getCenteredTextPos(d, points, textElement, "x");
    }
    return xPos + getTextPos.call(this, d, "x");
  },
  /**
   * Gets the y coordinate of the text
   * @param {object} points Data points position
   * @param {object} d Data object
   * @param {HTMLElement} textElement Data label text element
   * @returns {number} y coordinate
   * @private
   */
  getYForText: function(points, d, textElement) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, state = $$.state;
    var isRotated = config.axis_rotated;
    var isInverted = config["axis_".concat(axis3 === null || axis3 === void 0 ? void 0 : axis3.getId(d.id), "_inverted")];
    var isBarType = $$.isBarType(d);
    var isTreemapType = $$.isTreemapType(d);
    var r = config.point_r;
    var rect = getBoundingRect(textElement);
    var value = d.value;
    var baseY = 3;
    var yPos;
    if ($$.isCandlestickType(d)) {
      value = $$.getCandlestickData(d);
      if (isRotated) {
        yPos = points[0][0];
        yPos += (points[1][0] - yPos) / 2 + baseY;
      } else {
        yPos = value && value._isUp ? points[2][2] - baseY : points[2][1] + baseY * 4;
        if (isInverted) {
          yPos += 15 * (value._isUp ? 1 : -1);
        }
      }
    } else if (isTreemapType) {
      yPos = points[0][1] + (config.data_labels.centered ? 0 : rect.height + 5);
    } else {
      if (isRotated) {
        yPos = (points[0][0] + points[2][0] + rect.height * 0.6) / 2;
      } else {
        yPos = points[2][1];
        if (isNumber(r) && r > 5 && ($$.isLineType(d) || $$.isScatterType(d))) {
          baseY += config.point_r / 2.3;
        }
        if (value < 0 || value === 0 && !state.hasPositiveValue && state.hasNegativeValue) {
          yPos += isInverted ? isBarType ? -3 : -5 : rect.height + (isBarType ? -baseY : baseY);
        } else {
          var diff = -baseY * 2;
          if (isBarType) {
            diff = -baseY;
          } else if ($$.isBubbleType(d)) {
            diff = baseY;
          }
          if (isInverted) {
            diff = isBarType ? 10 : 15;
          }
          yPos += diff;
        }
      }
    }
    if (!isRotated || isTreemapType) {
      yPos += $$.getCenteredTextPos(d, points, textElement, "y");
    }
    return yPos + getTextPos.call(this, d, "y");
  },
  /**
   * Calculate if two or more text nodes are overlapping
   * Mark overlapping text nodes with "text-overlapping" class
   * @param {string} id Axis id
   * @param {ChartInternal} $$ ChartInternal context
   * @param {string} selector Selector string
   * @private
   */
  markOverlapped: function(id2, $$, selector) {
    var textNodes = $$.$el.arcs.selectAll(selector);
    var filteredTextNodes = textNodes.filter(function(node) {
      return node.data.id !== id2;
    });
    var textNode = textNodes.filter(function(node) {
      return node.data.id === id2;
    });
    var translate = getTranslation(textNode.node());
    var calcHypo = function(x3, y3) {
      return Math.sqrt(Math.pow(x3, 2) + Math.pow(y3, 2));
    };
    textNode.node() && filteredTextNodes.each(function() {
      var coordinate = getTranslation(this);
      var filteredTextNode = select_default2(this);
      var nodeForWidth = calcHypo(translate.e, translate.f) > calcHypo(coordinate.e, coordinate.f) ? textNode : filteredTextNode;
      var overlapsX = Math.ceil(Math.abs(translate.e - coordinate.e)) < Math.ceil(nodeForWidth.node().getComputedTextLength());
      var overlapsY = Math.ceil(Math.abs(translate.f - coordinate.f)) < parseInt(textNode.style("font-size"), 10);
      filteredTextNode.classed($TEXT.TextOverlapping, overlapsX && overlapsY);
    });
  },
  /**
   * Calculate if two or more text nodes are overlapping
   * Remove "text-overlapping" class on selected text nodes
   * @param {ChartInternal} $$ ChartInternal context
   * @param {string} selector Selector string
   * @private
   */
  undoMarkOverlapped: function($$, selector) {
    $$.$el.arcs.selectAll(selector).each(function() {
      selectAll_default2([this, this.previousSibling]).classed($TEXT.TextOverlapping, false);
    });
  },
  /**
   * Check if meets the ratio to show data label text
   * @param {number} ratio ratio to meet
   * @param {string} type chart type
   * @returns {boolean}
   * @private
   */
  meetsLabelThreshold: function(ratio, type2) {
    if (ratio === void 0) {
      ratio = 0;
    }
    var $$ = this;
    var config = $$.config;
    var threshold2 = config["".concat(type2, "_label_threshold")] || 0;
    return ratio >= threshold2;
  }
};
function getTextXPos(pos, width) {
  if (pos === void 0) {
    pos = "left";
  }
  var isNum = isNumber(width);
  var position2;
  if (pos.indexOf("center") > -1) {
    position2 = isNum ? width / 2 : "middle";
  } else if (pos.indexOf("right") > -1) {
    position2 = isNum ? width : "end";
  } else {
    position2 = isNum ? 0 : "start";
  }
  return position2;
}
var title = {
  /**
   * Initializes the title
   * @private
   */
  initTitle: function() {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    if (config.title_text) {
      $el.title = $el.svg.append("g");
      var text2 = $el.title.append("text").style("text-anchor", getTextXPos(config.title_position)).attr("class", $TEXT.title);
      setTextValue(text2, config.title_text, [0.3, 1.5]);
    }
  },
  /**
   * Redraw title
   * @private
   */
  redrawTitle: function() {
    var $$ = this;
    var config = $$.config, current = $$.state.current, title2 = $$.$el.title;
    if (title2) {
      var x3 = getTextXPos(config.title_position, current.width);
      var y3 = (config.title_padding.top || 0) + $$.getTextRect($$.$el.title, $TEXT.title).height;
      title2.attr("transform", "translate(".concat(x3, ", ").concat(y3, ")"));
    }
  },
  /**
   * Get title padding
   * @returns {number} padding value
   * @private
   */
  getTitlePadding: function() {
    var $$ = this;
    var title2 = $$.$el.title, config = $$.config;
    return (config.title_padding.top || 0) + (title2 ? $$.getTextRect(title2, $TEXT.title).height : 0) + (config.title_padding.bottom || 0);
  }
};
var tooltip$1 = {
  /**
   * Initializes the tooltip
   * @private
   */
  initTooltip: function() {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    $el.tooltip = select_default2(config.tooltip_contents.bindto);
    if ($el.tooltip.empty()) {
      $el.tooltip = $el.chart.append("div").attr("class", $TOOLTIP.tooltipContainer).style("position", "absolute").style("pointer-events", "none").style("display", "none");
    }
    $$.bindTooltipResizePos();
  },
  /**
   * Show tooltip at initialization.
   * Is called only when tooltip.init.show=true option is set
   * @private
   */
  initShowTooltip: function() {
    var _a2;
    var _b;
    var $$ = this;
    var config = $$.config, $el = $$.$el, _c = $$.state, hasAxis = _c.hasAxis, hasRadar = _c.hasRadar;
    if (config.tooltip_init_show) {
      var isArc = !(hasAxis || hasRadar);
      if (((_b = $$.axis) === null || _b === void 0 ? void 0 : _b.isTimeSeries()) && isString(config.tooltip_init_x)) {
        config.tooltip_init_x = parseDate.call($$, config.tooltip_init_x);
      }
      $$.api.tooltip.show({
        data: (_a2 = {}, _a2[isArc ? "index" : "x"] = config.tooltip_init_x, _a2)
      });
      var position2 = config.tooltip_init_position;
      if (!config.tooltip_contents.bindto && !isEmpty(position2)) {
        var _d = position2.top, top_1 = _d === void 0 ? 0 : _d, _e = position2.left, left2 = _e === void 0 ? 50 : _e;
        $el.tooltip.style("top", isString(top_1) ? top_1 : "".concat(top_1, "px")).style("left", isString(left2) ? left2 : "".concat(left2, "px")).style("display", null);
      }
    }
  },
  /**
   * Get the tooltip HTML string
   * @param  {Array} args Arguments
   * @returns {string} Formatted HTML string
   * @private
   */
  getTooltipHTML: function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    var $$ = this;
    var api2 = $$.api, config = $$.config;
    return isFunction(config.tooltip_contents) ? config.tooltip_contents.bind(api2).apply(void 0, args) : $$.getTooltipContent.apply($$, args);
  },
  /**
   * Returns the tooltip content(HTML string)
   * @param {object} d data
   * @param {Function} defaultTitleFormat Default title format
   * @param {Function} defaultValueFormat Default format for each data value in the tooltip.
   * @param {Function} color Color function
   * @returns {string} html
   * @private
   */
  getTooltipContent: function(d, defaultTitleFormat, defaultValueFormat, color3) {
    var $$ = this;
    var api2 = $$.api, config = $$.config, state = $$.state, $el = $$.$el;
    var _a2 = ["title", "name", "value"].map(function(v) {
      var fn = config["tooltip_format_".concat(v)];
      return isFunction(fn) ? fn.bind(api2) : fn;
    }), titleFn = _a2[0], nameFn = _a2[1], valueFn = _a2[2];
    var titleFormat = function() {
      var arg = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        arg[_i] = arguments[_i];
      }
      return sanitize((titleFn || defaultTitleFormat).apply(void 0, arg));
    };
    var nameFormat = function() {
      var arg = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        arg[_i] = arguments[_i];
      }
      return sanitize((nameFn || function(name) {
        return name;
      }).apply(void 0, arg));
    };
    var valueFormat = function() {
      var arg = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        arg[_i] = arguments[_i];
      }
      var fn = valueFn || (state.hasTreemap || $$.isStackNormalized() ? function(v, ratio) {
        return "".concat((ratio * 100).toFixed(2), "%");
      } : defaultValueFormat);
      return sanitize(fn.apply(void 0, arg));
    };
    var order = config.tooltip_order;
    var getRowValue = function(row2) {
      return $$.axis && $$.isBubbleZType(row2) ? $$.getBubbleZData(row2.value, "z") : $$.getBaseValue(row2);
    };
    var getBgColor = $$.levelColor ? function(row2) {
      return $$.levelColor(row2.value);
    } : function(row2) {
      return color3(row2);
    };
    var contents = config.tooltip_contents;
    var tplStr = contents.template;
    var targetIds = $$.mapToTargetIds();
    if (order === null && config.data_groups.length) {
      var ids_1 = $$.orderTargets($$.data.targets).map(function(i2) {
        return i2.id;
      }).reverse();
      d.sort(function(a2, b) {
        var v1 = a2 ? a2.value : null;
        var v2 = b ? b.value : null;
        if (v1 > 0 && v2 > 0) {
          v1 = a2.id ? ids_1.indexOf(a2.id) : null;
          v2 = b.id ? ids_1.indexOf(b.id) : null;
        }
        return v1 - v2;
      });
    } else if (/^(asc|desc)$/.test(order)) {
      var isAscending_1 = order === "asc";
      d.sort(function(a2, b) {
        var v1 = a2 ? getRowValue(a2) : null;
        var v2 = b ? getRowValue(b) : null;
        return isAscending_1 ? v1 - v2 : v2 - v1;
      });
    } else if (isFunction(order)) {
      d.sort(order.bind(api2));
    }
    var tpl = $$.getTooltipContentTemplate(tplStr);
    var len = d.length;
    var text2;
    var row;
    var param;
    var value;
    var i;
    var _loop_1 = function() {
      row = d[i];
      if (!row || !(getRowValue(row) || getRowValue(row) === 0)) {
        return "continue";
      }
      if (isUndefined(text2)) {
        var title2 = (state.hasAxis || state.hasRadar) && titleFormat(row.x);
        text2 = tplProcess(tpl[0], {
          CLASS_TOOLTIP: $TOOLTIP.tooltip,
          TITLE: isValue(title2) ? tplStr ? title2 : '<tr><th colspan="2">'.concat(title2, "</th></tr>") : ""
        });
      }
      if (!row.ratio && $el.arcs) {
        param = ["arc", $$.$el.arcs.select("path.".concat($ARC.arc, "-").concat(row.id)).data()[0]];
        row.ratio = $$.getRatio.apply($$, param);
      }
      param = [row.ratio, row.id, row.index];
      if ($$.isAreaRangeType(row)) {
        var _b = ["high", "low"].map(function(v) {
          return valueFormat.apply(void 0, __spreadArray([$$.getRangedData(row, v)], param, false));
        }), high = _b[0], low = _b[1];
        var mid = valueFormat.apply(void 0, __spreadArray([getRowValue(row)], param, false));
        value = "<b>Mid:</b> ".concat(mid, " <b>High:</b> ").concat(high, " <b>Low:</b> ").concat(low);
      } else if ($$.isCandlestickType(row)) {
        var _c = ["open", "high", "low", "close", "volume"].map(function(v) {
          var value2 = $$.getRangedData(row, v, "candlestick");
          return value2 ? valueFormat.apply(void 0, __spreadArray([$$.getRangedData(row, v, "candlestick")], param, false)) : void 0;
        }), open_1 = _c[0], high = _c[1], low = _c[2], close_1 = _c[3], volume = _c[4];
        value = "<b>Open:</b> ".concat(open_1, " <b>High:</b> ").concat(high, " <b>Low:</b> ").concat(low, " <b>Close:</b> ").concat(close_1).concat(volume ? " <b>Volume:</b> ".concat(volume) : "");
      } else if ($$.isBarRangeType(row)) {
        var rangeValue = row.value, id2 = row.id, index2 = row.index;
        value = "".concat(valueFormat(rangeValue, void 0, id2, index2));
      } else {
        value = valueFormat.apply(void 0, __spreadArray([getRowValue(row)], param, false));
      }
      if (value !== void 0) {
        if (row.name === null) {
          return "continue";
        }
        var name_1 = nameFormat.apply(void 0, __spreadArray([row.name], param, false));
        var color_1 = getBgColor(row);
        var contentValue_1 = {
          CLASS_TOOLTIP_NAME: $TOOLTIP.tooltipName + $$.getTargetSelectorSuffix(row.id),
          COLOR: tplStr || !$$.patterns ? color_1 : '<svg><rect style="fill:'.concat(color_1, '" width="10" height="10"></rect></svg>'),
          NAME: name_1,
          VALUE: value
        };
        if (tplStr && isObject(contents.text)) {
          var index_1 = targetIds.indexOf(row.id);
          Object.keys(contents.text).forEach(function(key) {
            contentValue_1[key] = contents.text[key][index_1];
          });
        }
        text2 += tplProcess(tpl[1], contentValue_1);
      }
    };
    for (i = 0; i < len; i++) {
      _loop_1();
    }
    return "".concat(text2, "</table>");
  },
  /**
   * Get the content template string
   * @param {string} tplStr Tempalte string
   * @returns {Array} Template string
   * @private
   */
  getTooltipContentTemplate: function(tplStr) {
    return (tplStr || '<table class="{=CLASS_TOOLTIP}"><tbody>\n				{=TITLE}\n				{{<tr class="{=CLASS_TOOLTIP_NAME}">\n					<td class="name">'.concat(this.patterns ? "{=COLOR}" : '<span style="background-color:{=COLOR}"></span>', '{=NAME}</td>\n					<td class="value">{=VALUE}</td>\n				</tr>}}\n			</tbody></table>')).replace(/(\r?\n|\t)/g, "").split(/{{(.*)}}/);
  },
  /**
   * Update tooltip position coordinate
   * @param {object} dataToShow Data object
   * @param {SVGElement} eventTarget Event element
   * @private
   */
  setTooltipPosition: function(dataToShow, eventTarget) {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state, _c = $$.$el, eventRect = _c.eventRect, tooltip2 = _c.tooltip;
    var bindto = config.tooltip_contents.bindto;
    var isRotated = config.axis_rotated;
    var datum2 = tooltip2 === null || tooltip2 === void 0 ? void 0 : tooltip2.datum();
    if (!bindto && datum2) {
      var data2 = dataToShow !== null && dataToShow !== void 0 ? dataToShow : JSON.parse(datum2.current);
      var _d = getPointer(state.event, eventTarget !== null && eventTarget !== void 0 ? eventTarget : eventRect === null || eventRect === void 0 ? void 0 : eventRect.node()), x3 = _d[0], y3 = _d[1];
      var currPos = { x: x3, y: y3 };
      if (state.hasAxis && scale2.x && datum2 && "x" in datum2) {
        var getYPos = function(value, id2, axisId) {
          var _a3;
          if (value === void 0) {
            value = 0;
          }
          if (axisId === void 0) {
            axisId = "y";
          }
          var scaleFn = scale2[id2 ? (_a3 = $$.axis) === null || _a3 === void 0 ? void 0 : _a3.getId(id2) : axisId];
          return scaleFn ? scaleFn(value) + (isRotated ? state.margin.left : state.margin.top) : 0;
        };
        currPos.xAxis = scale2.x(datum2.x) + // add margin only when user specified tooltip.position function
        (config.tooltip_position ? isRotated ? state.margin.top : state.margin.left : 0);
        if (data2.length === 1) {
          currPos.yAxis = getYPos(data2[0].value, data2[0].id);
        } else {
          currPos.yAxis = getYPos;
        }
      }
      var _e = datum2.width, width = _e === void 0 ? 0 : _e, _f = datum2.height, height = _f === void 0 ? 0 : _f;
      var pos_1 = (_b = (_a2 = config.tooltip_position) === null || _a2 === void 0 ? void 0 : _a2.bind($$.api)(data2, width, height, eventRect === null || eventRect === void 0 ? void 0 : eventRect.node(), currPos)) !== null && _b !== void 0 ? _b : $$.getTooltipPosition.bind($$)(width, height, currPos);
      ["top", "left"].forEach(function(v) {
        var value = pos_1[v];
        tooltip2.style(v, "".concat(value, "px"));
        if (v === "left" && !datum2.xPosInPercent) {
          datum2.xPosInPercent = value / state.current.width * 100;
        }
      });
    }
  },
  /**
   * Returns the position of the tooltip
   * @param {string} tWidth Width value of tooltip element
   * @param {string} tHeight Height value of tooltip element
   * @param {object} currPos Current mouse position
   * @returns {object} top, left value
   * @private
   */
  getTooltipPosition: function(tWidth, tHeight, currPos) {
    var _a2, _b, _c;
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state;
    var width = state.width, height = state.height, current = state.current, hasRadar = state.hasRadar, hasTreemap = state.hasTreemap, isLegendRight = state.isLegendRight, inputType = state.inputType;
    var hasGauge = $$.hasType("gauge") && !config.gauge_fullCircle;
    var isRotated = config.axis_rotated;
    var hasArcType = $$.hasArcType();
    var svgLeft = $$.getSvgLeft(true);
    var chartRight = svgLeft + current.width - $$.getCurrentPaddingByDirection("right");
    var size2 = 20;
    var x3 = currPos.x, y3 = currPos.y;
    if (hasRadar) {
      x3 += x3 >= width / 2 ? 15 : -(tWidth + 15);
      y3 += 15;
    } else if (hasArcType) {
      var notTouch = inputType !== "touch";
      if (notTouch) {
        var titlePadding = (_b = (_a2 = $$.getTitlePadding) === null || _a2 === void 0 ? void 0 : _a2.call($$)) !== null && _b !== void 0 ? _b : 0;
        if (titlePadding && hasGauge && ((_c = config.arc_rangeText_values) === null || _c === void 0 ? void 0 : _c.length)) {
          titlePadding += 10;
        }
        x3 += (width - (isLegendRight ? $$.getLegendWidth() : 0)) / 2;
        y3 += (hasGauge ? height : height / 2 + tHeight) + titlePadding;
      }
    } else if (hasTreemap) {
      y3 += tHeight;
    } else {
      var padding = {
        top: $$.getCurrentPaddingByDirection("top", true),
        left: $$.getCurrentPaddingByDirection("left", true)
      };
      if (isRotated) {
        x3 += svgLeft + padding.left + size2;
        y3 = padding.top + currPos.xAxis + size2;
        chartRight -= svgLeft;
      } else {
        x3 = svgLeft + padding.left + size2 + (scale2.zoom ? x3 : currPos.xAxis);
        y3 += padding.top - 5;
      }
    }
    if (x3 + tWidth + 15 > chartRight) {
      x3 -= tWidth + (hasTreemap || hasArcType ? 0 : isRotated ? size2 * 2 : 38);
    }
    if (y3 + tHeight > current.height) {
      var gap = hasTreemap ? tHeight + 10 : 30;
      y3 -= hasGauge ? tHeight * 1.5 : tHeight + gap;
    }
    var pos = { top: y3, left: x3 };
    Object.keys(pos).forEach(function(v) {
      if (pos[v] < 0) {
        pos[v] = 0;
      }
    });
    return pos;
  },
  /**
   * Show the tooltip
   * @param {object} selectedData Data object
   * @param {SVGElement} eventTarget Event element
   * @private
   */
  showTooltip: function(selectedData, eventTarget) {
    var $$ = this;
    var config = $$.config, tooltip2 = $$.$el.tooltip;
    var dataToShow = selectedData.filter(function(d) {
      return d && isValue($$.getBaseValue(d));
    });
    if (!tooltip2 || dataToShow.length === 0 || !config.tooltip_show) {
      return;
    }
    var datum2 = tooltip2.datum();
    var dataStr = JSON.stringify(selectedData);
    if (!datum2 || datum2.current !== dataStr) {
      var _a2 = selectedData.concat().sort()[0], index2 = _a2.index, x3 = _a2.x;
      callFn(config.tooltip_onshow, $$.api, selectedData);
      tooltip2.html($$.getTooltipHTML(
        selectedData,
        // data
        $$.axis ? $$.axis.getXAxisTickFormat() : $$.categoryName.bind($$),
        // defaultTitleFormat
        $$.getDefaultValueFormat(),
        // defaultValueFormat
        $$.color
        // color
      )).style("display", null).style("visibility", null).datum(datum2 = {
        index: index2,
        x: x3,
        current: dataStr,
        width: tooltip2.property("offsetWidth"),
        height: tooltip2.property("offsetHeight")
      });
      callFn(config.tooltip_onshown, $$.api, selectedData);
      $$._handleLinkedCharts(true, index2);
    }
    $$.setTooltipPosition(dataToShow, eventTarget);
  },
  /**
   * Adjust tooltip position on resize event
   * @private
   */
  bindTooltipResizePos: function() {
    var $$ = this;
    var resizeFunction = $$.resizeFunction, state = $$.state, tooltip2 = $$.$el.tooltip;
    resizeFunction.add(function() {
      if (tooltip2.style("display") === "block") {
        var current = state.current;
        var _a2 = tooltip2.datum(), width = _a2.width, xPosInPercent = _a2.xPosInPercent;
        var value = current.width / 100 * xPosInPercent;
        var diff = current.width - (value + width);
        if (diff < 0) {
          value += diff;
        }
        tooltip2.style("left", "".concat(value, "px"));
      }
    });
  },
  /**
   * Hide the tooltip
   * @param {boolean} force Force to hide
   * @private
   */
  hideTooltip: function(force) {
    var _a2;
    var $$ = this;
    var api2 = $$.api, config = $$.config, tooltip2 = $$.$el.tooltip;
    if (tooltip2 && tooltip2.style("display") !== "none" && (!config.tooltip_doNotHide || force)) {
      var selectedData = JSON.parse((_a2 = tooltip2.datum().current) !== null && _a2 !== void 0 ? _a2 : {});
      callFn(config.tooltip_onhide, api2, selectedData);
      tooltip2.style("display", "none").style("visibility", "hidden").datum(null);
      callFn(config.tooltip_onhidden, api2, selectedData);
    }
  },
  /**
   * Toggle display for linked chart instances
   * @param {boolean} show true: show, false: hide
   * @param {number} index x Axis index
   * @private
   */
  _handleLinkedCharts: function(show, index2) {
    var $$ = this;
    var charts = $$.charts, config = $$.config, event = $$.state.event;
    if ((event === null || event === void 0 ? void 0 : event.isTrusted) && config.tooltip_linked && charts.length > 1) {
      var linkedName_1 = config.tooltip_linked_name;
      charts.filter(function(c) {
        return c !== $$.api;
      }).forEach(function(c) {
        var _a2 = c.internal, config2 = _a2.config, $el = _a2.$el;
        var isLinked = config2.tooltip_linked;
        var name = config2.tooltip_linked_name;
        var isInDom = doc.body.contains($el.chart.node());
        if (isLinked && linkedName_1 === name && isInDom) {
          var data2 = $el.tooltip.data()[0];
          var isNotSameIndex = index2 !== (data2 === null || data2 === void 0 ? void 0 : data2.index);
          try {
            c.tooltip[show && isNotSameIndex ? "show" : "hide"]({ index: index2 });
          } catch (e) {
          }
        }
      });
    }
  },
  /**
   * Update tooltip content on redraw
   * - In a situation where tooltip is displayed and data load happens, it should reflect loaded data to tooltip
   * @param {d3Selection} context Event rect element
   * @param {number} index Data index
   * @private
   */
  updateTooltipOnRedraw: function(context, index2) {
    var _a2;
    var $$ = this;
    var config = $$.config, _b = $$.$el, eventRect = _b.eventRect, svg = _b.svg, tooltip2 = _b.tooltip, _c = $$.state, event = _c.event, hasAxis = _c.hasAxis, hasRadar = _c.hasRadar, hasTreemap = _c.hasTreemap;
    if ((tooltip2 === null || tooltip2 === void 0 ? void 0 : tooltip2.style("display")) === "block" && event) {
      var rect = context !== null && context !== void 0 ? context : (_a2 = hasRadar ? svg : eventRect) === null || _a2 === void 0 ? void 0 : _a2.node();
      if (hasAxis || hasRadar) {
        if ($$.isMultipleX()) {
          $$.selectRectForMultipleXs(rect, false);
        } else {
          var idx = index2 !== null && index2 !== void 0 ? index2 : $$.getDataIndexFromEvent(event);
          if (index2 === -1) {
            $$.api.tooltip.hide();
          } else {
            $$.selectRectForSingle(rect, idx);
            $$.setExpand(idx, null, true);
          }
        }
      } else {
        var clientX_1 = event.clientX, clientY_1 = event.clientY;
        setTimeout(function() {
          var target = doc.elementFromPoint(clientX_1, clientY_1);
          var data2 = select_default2(target).datum();
          if (data2) {
            var d = $$.hasArcType() ? $$.convertToArcData($$.updateAngle(data2)) : data2 === null || data2 === void 0 ? void 0 : data2.data;
            hasTreemap && (target = svg.node());
            d && $$.showTooltip([d], target);
          } else {
            $$.api.tooltip.hide();
          }
        }, config.transition_duration);
      }
    }
  }
};
var transform2 = {
  getTranslate: function(target, index2) {
    var _a2;
    if (index2 === void 0) {
      index2 = 0;
    }
    var $$ = this;
    var config = $$.config, state = $$.state;
    var isRotated = config.axis_rotated;
    var padding = 0;
    var x3;
    var y3;
    if (index2 && /^(x|y2?)$/.test(target)) {
      padding = $$.getAxisSize(target) * index2;
    }
    if (target === "main") {
      x3 = asHalfPixel(state.margin.left);
      y3 = asHalfPixel(state.margin.top);
    } else if (target === "context") {
      x3 = asHalfPixel(state.margin2.left);
      y3 = asHalfPixel(state.margin2.top);
    } else if (target === "legend") {
      x3 = state.margin3.left;
      y3 = state.margin3.top;
    } else if (target === "x") {
      x3 = isRotated ? -padding : 0;
      y3 = isRotated ? 0 : state.height + padding;
    } else if (target === "y") {
      x3 = isRotated ? 0 : -padding;
      y3 = isRotated ? state.height + padding : 0;
    } else if (target === "y2") {
      x3 = isRotated ? 0 : state.width + padding;
      y3 = isRotated ? -padding - 1 : 0;
    } else if (target === "subX") {
      x3 = 0;
      y3 = isRotated ? 0 : state.height2;
    } else if (target === "arc") {
      x3 = state.arcWidth / 2;
      y3 = state.arcHeight / 2;
      if ((_a2 = config.arc_rangeText_values) === null || _a2 === void 0 ? void 0 : _a2.length) {
        y3 += 5 + ($$.hasType("gauge") && config.title_text ? 10 : 0);
      }
    } else if (target === "polar") {
      x3 = state.arcWidth / 2;
      y3 = state.arcHeight / 2;
    } else if (target === "radar") {
      var _b = $$.getRadarSize(), width = _b[0], height = _b[1];
      x3 = state.width / 2 - width;
      y3 = state.height / 2 - height;
    }
    return "translate(".concat(x3, ", ").concat(y3, ")");
  },
  transformMain: function(withTransition, transitions) {
    var $$ = this;
    var main2 = $$.$el.main, $T = $$.$T;
    var xAxis = (transitions === null || transitions === void 0 ? void 0 : transitions.axisX) ? transitions.axisX : $T(main2.select(".".concat($AXIS.axisX)), withTransition);
    var yAxis = (transitions === null || transitions === void 0 ? void 0 : transitions.axisY) ? transitions.axisY : $T(main2.select(".".concat($AXIS.axisY)), withTransition);
    var y2Axis = (transitions === null || transitions === void 0 ? void 0 : transitions.axisY2) ? transitions.axisY2 : $T(main2.select(".".concat($AXIS.axisY2)), withTransition);
    $T(main2, withTransition).attr("transform", $$.getTranslate("main"));
    xAxis.attr("transform", $$.getTranslate("x"));
    yAxis.attr("transform", $$.getTranslate("y"));
    y2Axis.attr("transform", $$.getTranslate("y2"));
    main2.select(".".concat($ARC.chartArcs)).attr("transform", $$.getTranslate("arc"));
  },
  transformAll: function(withTransition, transitions) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, hasAxis = _a2.hasAxis, hasTreemap = _a2.hasTreemap, $el = $$.$el;
    !hasTreemap && $$.transformMain(withTransition, transitions);
    hasAxis && config.subchart_show && $$.transformContext(withTransition, transitions);
    $el.legend && $$.transformLegend(withTransition);
  }
};
var typeInternals = {
  /**
   * Check if the given chart type is valid
   * @param {string} type Chart type string
   * @returns {boolean}
   * @private
   */
  isValidChartType: function(type2) {
    return !!(type2 && Object.values(TYPE).indexOf(type2) > -1);
  },
  setTargetType: function(targetIds, type2) {
    var $$ = this;
    var config = $$.config, withoutFadeIn = $$.state.withoutFadeIn;
    $$.mapToTargetIds(targetIds).forEach(function(id2) {
      withoutFadeIn[id2] = type2 === config.data_types[id2];
      config.data_types[id2] = type2;
    });
    if (!targetIds) {
      config.data_type = type2;
    }
  },
  /**
   * Updte current used chart types
   * @private
   */
  updateTypesElements: function() {
    var $$ = this;
    var current = $$.state.current;
    Object.keys(TYPE).forEach(function(v) {
      var t = TYPE[v];
      var has = $$.hasType(t, null, true);
      var idx = current.types.indexOf(t);
      if (idx === -1 && has) {
        current.types.push(t);
      } else if (idx > -1 && !has) {
        current.types.splice(idx, 1);
      }
    });
    $$.setChartElements();
  },
  /**
   * Check if given chart types exists
   * @param {string} type Chart type
   * @param {Array} targetsValue Data array
   * @param {boolean} checkFromData Force to check type cotains from data targets
   * @returns {boolean}
   * @private
   */
  hasType: function(type2, targetsValue, checkFromData) {
    var _a2;
    if (checkFromData === void 0) {
      checkFromData = false;
    }
    var $$ = this;
    var config = $$.config, current = $$.state.current;
    var types = config.data_types;
    var targets = targetsValue || $$.data.targets;
    var has = false;
    if (!checkFromData && ((_a2 = current.types) === null || _a2 === void 0 ? void 0 : _a2.indexOf(type2)) > -1) {
      has = true;
    } else if (targets === null || targets === void 0 ? void 0 : targets.length) {
      targets.forEach(function(target) {
        var t = types[target.id];
        if (t === type2 || !t && type2 === "line") {
          has = true;
        }
      });
    } else if (Object.keys(types).length) {
      Object.keys(types).forEach(function(id2) {
        if (types[id2] === type2) {
          has = true;
        }
      });
    } else {
      has = config.data_type === type2;
    }
    return has;
  },
  /**
   * Check if contains given chart types
   * @param {string} type Type key
   * @param {object} targets Target data
   * @param {Array} exclude Excluded types
   * @returns {boolean}
   * @private
   */
  hasTypeOf: function(type2, targets, exclude) {
    var _this = this;
    if (exclude === void 0) {
      exclude = [];
    }
    if (type2 in TYPE_BY_CATEGORY) {
      return !TYPE_BY_CATEGORY[type2].filter(function(v) {
        return exclude.indexOf(v) === -1;
      }).every(function(v) {
        return !_this.hasType(v, targets);
      });
    }
    return false;
  },
  /**
   * Check if given data is certain chart type
   * @param {object} d Data object
   * @param {string|Array} type chart type
   * @returns {boolean}
   * @private
   */
  isTypeOf: function(d, type2) {
    var _a2;
    var id2 = isString(d) ? d : d.id;
    var dataType = this.config && (((_a2 = this.config.data_types) === null || _a2 === void 0 ? void 0 : _a2[id2]) || this.config.data_type);
    return isArray(type2) ? type2.indexOf(dataType) >= 0 : dataType === type2;
  },
  hasPointType: function() {
    var $$ = this;
    return $$.hasTypeOf("Line") || $$.hasType("bubble") || $$.hasType("scatter");
  },
  /**
   * Check if contains arc types chart
   * @param {object} targets Target data
   * @param {Array} exclude Excluded types
   * @returns {boolean}
   * @private
   */
  hasArcType: function(targets, exclude) {
    return this.hasTypeOf("Arc", targets, exclude);
  },
  hasMultiArcGauge: function() {
    return this.hasType("gauge") && this.config.gauge_type === "multi";
  },
  isLineType: function(d) {
    var id2 = isString(d) ? d : d.id;
    return !this.config.data_types[id2] || this.isTypeOf(id2, TYPE_BY_CATEGORY.Line);
  },
  isStepType: function(d) {
    return this.isTypeOf(d, TYPE_BY_CATEGORY.Step);
  },
  isSplineType: function(d) {
    return this.isTypeOf(d, TYPE_BY_CATEGORY.Spline);
  },
  isAreaType: function(d) {
    return this.isTypeOf(d, TYPE_BY_CATEGORY.Area);
  },
  isAreaRangeType: function(d) {
    return this.isTypeOf(d, TYPE_BY_CATEGORY.AreaRange);
  },
  isBarType: function(d) {
    return this.isTypeOf(d, "bar");
  },
  isBubbleType: function(d) {
    return this.isTypeOf(d, "bubble");
  },
  isCandlestickType: function(d) {
    return this.isTypeOf(d, "candlestick");
  },
  isScatterType: function(d) {
    return this.isTypeOf(d, "scatter");
  },
  isTreemapType: function(d) {
    return this.isTypeOf(d, "treemap");
  },
  isPieType: function(d) {
    return this.isTypeOf(d, "pie");
  },
  isGaugeType: function(d) {
    return this.isTypeOf(d, "gauge");
  },
  isDonutType: function(d) {
    return this.isTypeOf(d, "donut");
  },
  isPolarType: function(d) {
    return this.isTypeOf(d, "polar");
  },
  isRadarType: function(d) {
    return this.isTypeOf(d, "radar");
  },
  isArcType: function(d) {
    return this.isPieType(d) || this.isDonutType(d) || this.isGaugeType(d) || this.isPolarType(d) || this.isRadarType(d);
  },
  // determine if is 'circle' data point
  isCirclePoint: function(node) {
    var config = this.config;
    var pattern = config.point_pattern;
    var isCircle = false;
    if ((node === null || node === void 0 ? void 0 : node.tagName) === "circle") {
      isCircle = true;
    } else {
      isCircle = config.point_type === "circle" && (!pattern || isArray(pattern) && pattern.length === 0);
    }
    return isCircle;
  },
  lineData: function(d) {
    return this.isLineType(d) ? [d] : [];
  },
  arcData: function(d) {
    return this.isArcType(d.data) ? [d] : [];
  },
  /**
   * Get data adapt for data label showing
   * @param {object} d Data object
   * @returns {Array}
   * @private
   */
  labelishData: function(d) {
    return this.isBarType(d) || this.isLineType(d) || this.isScatterType(d) || this.isBubbleType(d) || this.isCandlestickType(d) || this.isRadarType(d) || this.isTreemapType(d) ? d.values.filter(function(v) {
      return isNumber(v.value) || Boolean(v.value);
    }) : [];
  },
  barLineBubbleData: function(d) {
    return this.isBarType(d) || this.isLineType(d) || this.isBubbleType(d) ? d.values : [];
  },
  // https://github.com/d3/d3-shape#curves
  isInterpolationType: function(type2) {
    return [
      "basis",
      "basis-closed",
      "basis-open",
      "bundle",
      "cardinal",
      "cardinal-closed",
      "cardinal-open",
      "catmull-rom",
      "catmull-rom-closed",
      "catmull-rom-open",
      "linear",
      "linear-closed",
      "monotone-x",
      "monotone-y",
      "natural"
    ].indexOf(type2) >= 0;
  }
};
var ChartInternal = (
  /** @class */
  function() {
    function ChartInternal2(api2) {
      this.data = {
        xs: {},
        targets: []
      };
      this.scale = {
        x: null,
        y: null,
        y2: null,
        subX: null,
        subY: null,
        subY2: null,
        zoom: null
      };
      this.org = {
        xScale: null,
        xDomain: null
      };
      this.format = {
        extraLineClasses: null,
        xAxisTick: null,
        dataTime: null,
        // dataTimeFormat
        defaultAxisTime: null,
        // defaultAxisTimeFormat
        axisTime: null
        // axisTimeFormat
      };
      var $$ = this;
      $$.api = api2;
      $$.config = new Options();
      $$.cache = new Cache();
      var store = new Store();
      $$.$el = store.getStore("element");
      $$.state = store.getStore("state");
      $$.$T = $$.$T.bind($$);
    }
    ChartInternal2.prototype.$T = function(selection3, force, name) {
      var _a2 = this, config = _a2.config, state = _a2.state;
      var duration = config.transition_duration;
      var subchart2 = config.subchart_show;
      var t = selection3;
      if (t) {
        if ("tagName" in t) {
          t = select_default2(t);
        }
        var transit = (force !== false && duration || force) && (!state.zooming || state.dragging) && !state.resizing && state.rendered && !subchart2;
        t = transit ? t.transition(name).duration(duration) : t;
      }
      return t;
    };
    ChartInternal2.prototype.beforeInit = function() {
      var $$ = this;
      $$.callPluginHook("$beforeInit");
      callFn($$.config.onbeforeinit, $$.api);
    };
    ChartInternal2.prototype.afterInit = function() {
      var $$ = this;
      $$.callPluginHook("$afterInit");
      callFn($$.config.onafterinit, $$.api);
    };
    ChartInternal2.prototype.init = function() {
      var $$ = this;
      var config = $$.config, state = $$.state, $el = $$.$el;
      var useCssRule = config.boost_useCssRule;
      checkModuleImport($$);
      state.hasRadar = !state.hasAxis && $$.hasType("radar");
      state.hasTreemap = !state.hasAxis && $$.hasType("treemap");
      state.hasAxis = !$$.hasArcType() && !state.hasTreemap;
      state.datetimeId = "bb-".concat(+/* @__PURE__ */ new Date() * getRandom());
      if (useCssRule) {
        var styleEl = doc.createElement("style");
        styleEl.type = "text/css";
        doc.head.appendChild(styleEl);
        state.style = {
          rootSelctor: ".".concat(state.datetimeId),
          sheet: styleEl.sheet
        };
        $el.style = styleEl;
      }
      var bindto = {
        element: config.bindto,
        classname: "bb"
      };
      if (isObject(config.bindto)) {
        bindto.element = config.bindto.element || "#chart";
        bindto.classname = config.bindto.classname || bindto.classname;
      }
      $el.chart = isFunction(bindto.element.node) ? config.bindto.element : select_default2(bindto.element || []);
      if ($el.chart.empty()) {
        $el.chart = select_default2(doc.body.appendChild(doc.createElement("div")));
      }
      $el.chart.html("").classed(bindto.classname, true).classed(state.datetimeId, useCssRule).style("position", "relative");
      $$.initParams();
      $$.initToRender();
    };
    ChartInternal2.prototype.initToRender = function(forced) {
      var $$ = this;
      var config = $$.config, state = $$.state, chart = $$.$el.chart;
      var isHidden = function() {
        return chart.style("display") === "none" || chart.style("visibility") === "hidden";
      };
      var isLazy = config.render.lazy || isHidden();
      var MutationObserver = win.MutationObserver;
      if (isLazy && MutationObserver && config.render.observe !== false && !forced) {
        new MutationObserver(function(mutation, observer) {
          if (!isHidden()) {
            observer.disconnect();
            !state.rendered && $$.initToRender(true);
          }
        }).observe(chart.node(), {
          attributes: true,
          attributeFilter: ["class", "style"]
        });
      }
      if (!isLazy || forced) {
        $$.convertData(config, function(res) {
          $$.initWithData(res);
          $$.afterInit();
        });
      }
    };
    ChartInternal2.prototype.initParams = function() {
      var _a2;
      var $$ = this;
      var config = $$.config, format3 = $$.format, state = $$.state;
      var isRotated = config.axis_rotated;
      $$.color = $$.generateColor();
      $$.levelColor = $$.generateLevelColor();
      if (config.padding === false) {
        config.axis_x_show = false;
        config.axis_y_show = false;
        config.axis_y2_show = false;
        config.subchart_show = false;
      }
      if ($$.hasPointType() || ((_a2 = $$.hasLegendDefsPoint) === null || _a2 === void 0 ? void 0 : _a2.call($$))) {
        $$.point = $$.generatePoint();
      }
      if (state.hasAxis) {
        $$.initClip();
        format3.extraLineClasses = $$.generateExtraLineClass();
        format3.dataTime = config.data_xLocaltime ? timeParse : utcParse;
        format3.axisTime = config.axis_x_localtime ? timeFormat : utcFormat;
        var isDragZoom_1 = $$.config.zoom_enabled && $$.config.zoom_type === "drag";
        format3.defaultAxisTime = function(d) {
          var _a3 = $$.scale, x3 = _a3.x, zoom2 = _a3.zoom;
          var isZoomed = isDragZoom_1 ? zoom2 : zoom2 && x3.orgDomain().toString() !== zoom2.domain().toString();
          var specifier = d.getMilliseconds() && ".%L" || d.getSeconds() && ".:%S" || d.getMinutes() && "%I:%M" || d.getHours() && "%I %p" || d.getDate() !== 1 && "%b %d" || isZoomed && d.getDate() === 1 && "%b'%y" || d.getMonth() && "%-m/%-d" || "%Y";
          return format3.axisTime(specifier)(d);
        };
      }
      state.isLegendRight = config.legend_position === "right";
      state.isLegendInset = config.legend_position === "inset";
      state.isLegendTop = config.legend_inset_anchor === "top-left" || config.legend_inset_anchor === "top-right";
      state.isLegendLeft = config.legend_inset_anchor === "top-left" || config.legend_inset_anchor === "bottom-left";
      state.rotatedPadding.top = $$.getResettedPadding(state.rotatedPadding.top);
      state.rotatedPadding.right = isRotated && !config.axis_x_show ? 0 : 30;
      state.inputType = convertInputType(config.interaction_inputType_mouse, config.interaction_inputType_touch);
    };
    ChartInternal2.prototype.initWithData = function(data2) {
      var _a2, _b, _c;
      var $$ = this;
      var config = $$.config, scale2 = $$.scale, state = $$.state, $el = $$.$el, org = $$.org;
      var hasAxis = state.hasAxis, hasTreemap = state.hasTreemap;
      var hasInteraction = config.interaction_enabled;
      var hasPolar = $$.hasType("polar");
      if (hasAxis) {
        $$.axis = $$.getAxisInstance();
        config.zoom_enabled && $$.initZoom();
      }
      $$.data.xs = {};
      $$.data.targets = $$.convertDataToTargets(data2);
      if (config.data_filter) {
        $$.data.targets = $$.data.targets.filter(config.data_filter.bind($$.api));
      }
      if (config.data_hide) {
        $$.addHiddenTargetIds(config.data_hide === true ? $$.mapToIds($$.data.targets) : config.data_hide);
      }
      if (config.legend_hide) {
        $$.addHiddenLegendIds(config.legend_hide === true ? $$.mapToIds($$.data.targets) : config.legend_hide);
      }
      $$.updateSizes();
      $$.updateScales(true);
      if (hasAxis) {
        var x3 = scale2.x, y3 = scale2.y, y23 = scale2.y2, subX = scale2.subX, subY = scale2.subY, subY2 = scale2.subY2;
        if (x3) {
          x3.domain(sortValue($$.getXDomain($$.data.targets), !config.axis_x_inverted));
          subX.domain(x3.domain());
          org.xDomain = x3.domain();
        }
        if (y3) {
          y3.domain($$.getYDomain($$.data.targets, "y"));
          subY.domain(y3.domain());
        }
        if (y23) {
          y23.domain($$.getYDomain($$.data.targets, "y2"));
          subY2 && subY2.domain(y23.domain());
        }
      }
      $el.svg = $el.chart.append("svg").style("overflow", "hidden").style("display", "block");
      if (hasInteraction && state.inputType) {
        var isTouch = state.inputType === "touch";
        var onclick_1 = config.onclick, onover = config.onover, onout = config.onout;
        $el.svg.on("click", (onclick_1 === null || onclick_1 === void 0 ? void 0 : onclick_1.bind($$.api)) || null).on(isTouch ? "touchstart" : "mouseenter", (onover === null || onover === void 0 ? void 0 : onover.bind($$.api)) || null).on(isTouch ? "touchend" : "mouseleave", (onout === null || onout === void 0 ? void 0 : onout.bind($$.api)) || null);
      }
      config.svg_classname && $el.svg.attr("class", config.svg_classname);
      var hasColorPatterns = isFunction(config.color_tiles) && $$.patterns;
      if (hasAxis || hasColorPatterns || hasPolar || hasTreemap || config.data_labels_backgroundColors || ((_a2 = $$.hasLegendDefsPoint) === null || _a2 === void 0 ? void 0 : _a2.call($$))) {
        $el.defs = $el.svg.append("defs");
        if (hasAxis) {
          ["id", "idXAxis", "idYAxis", "idGrid"].forEach(function(v) {
            $$.appendClip($el.defs, state.clip[v]);
          });
        }
        $$.generateDataLabelBackgroundColorFilter();
        if (hasColorPatterns) {
          $$.patterns.forEach(function(p) {
            return $el.defs.append(function() {
              return p.node;
            });
          });
        }
      }
      $$.updateSvgSize();
      $$.bindResize();
      var main2 = $el.svg.append("g").classed($COMMON.main, true).attr("transform", hasTreemap ? null : $$.getTranslate("main"));
      $el.main = main2;
      config.subchart_show && $$.initSubchart();
      config.tooltip_show && $$.initTooltip();
      config.title_text && $$.initTitle();
      !hasTreemap && config.legend_show && $$.initLegend();
      if (config.data_empty_label_text) {
        main2.append("text").attr("class", "".concat($TEXT.text, " ").concat($COMMON.empty)).attr("text-anchor", "middle").attr("dominant-baseline", "middle");
      }
      if (hasAxis) {
        config.regions.length && $$.initRegion();
        !config.clipPath && $$.axis.init();
      }
      main2.append("g").classed($COMMON.chart, true).attr("clip-path", hasAxis ? state.clip.path : null);
      $$.callPluginHook("$init");
      $$.initChartElements();
      if (hasAxis) {
        hasInteraction && ((_b = $$.initEventRect) === null || _b === void 0 ? void 0 : _b.call($$));
        $$.initGrid();
        config.clipPath && ((_c = $$.axis) === null || _c === void 0 ? void 0 : _c.init());
      }
      $$.updateTargets($$.data.targets);
      $$.updateDimension();
      callFn(config.oninit, $$.api);
      $$.setBackground();
      $$.redraw({
        withTransition: false,
        withTransform: true,
        withUpdateXDomain: true,
        withUpdateOrgXDomain: true,
        withTransitionForAxis: false,
        initializing: true
      });
      if (config.data_onmin || config.data_onmax) {
        var minMax = $$.getMinMaxData();
        callFn(config.data_onmin, $$.api, minMax.min);
        callFn(config.data_onmax, $$.api, minMax.max);
      }
      config.tooltip_show && $$.initShowTooltip();
      state.rendered = true;
    };
    ChartInternal2.prototype.initChartElements = function() {
      var $$ = this;
      var _a2 = $$.state, hasAxis = _a2.hasAxis, hasRadar = _a2.hasRadar, hasTreemap = _a2.hasTreemap;
      var types = [];
      if (hasAxis) {
        var shapes = ["bar", "bubble", "candlestick", "line"];
        if ($$.config.bar_front) {
          shapes.push(shapes.shift());
        }
        shapes.forEach(function(v) {
          var name = capitalize(v);
          if (v === "line" && $$.hasTypeOf(name) || $$.hasType(v)) {
            types.push(name);
          }
        });
      } else if (hasTreemap) {
        types.push("Treemap");
      } else {
        var hasPolar = $$.hasType("polar");
        if (!hasRadar) {
          types.push("Arc", "Pie");
        }
        if ($$.hasType("gauge")) {
          types.push("Gauge");
        } else if (hasRadar) {
          types.push("Radar");
        } else if (hasPolar) {
          types.push("Polar");
        }
      }
      types.forEach(function(v) {
        $$["init".concat(v)]();
      });
      notEmpty($$.config.data_labels) && !$$.hasArcType(null, ["radar"]) && $$.initText();
    };
    ChartInternal2.prototype.setChartElements = function() {
      var $$ = this;
      var _a2 = $$.$el, chart = _a2.chart, svg = _a2.svg, defs = _a2.defs, main2 = _a2.main, tooltip2 = _a2.tooltip, legend2 = _a2.legend, title2 = _a2.title, grid2 = _a2.grid, needle = _a2.needle, arc = _a2.arcs, circles = _a2.circle, bars = _a2.bar, candlestick2 = _a2.candlestick, lines = _a2.line, areas = _a2.area, texts = _a2.text;
      $$.api.$ = {
        chart,
        svg,
        defs,
        main: main2,
        tooltip: tooltip2,
        legend: legend2,
        title: title2,
        grid: grid2,
        arc,
        circles,
        bar: { bars },
        candlestick: candlestick2,
        line: { lines, areas },
        needle,
        text: { texts }
      };
    };
    ChartInternal2.prototype.setBackground = function() {
      var $$ = this;
      var bg = $$.config.background, state = $$.state, svg = $$.$el.svg;
      if (notEmpty(bg)) {
        var element = svg.select("g").insert(bg.imgUrl ? "image" : "rect", ":first-child");
        if (bg.imgUrl) {
          element.attr("href", bg.imgUrl);
        } else if (bg.color) {
          element.style("fill", bg.color).attr("clip-path", state.clip.path);
        }
        element.attr("class", bg.class || null).attr("width", "100%").attr("height", "100%");
      }
    };
    ChartInternal2.prototype.updateTargets = function(targets) {
      var _a2;
      var $$ = this;
      var _b = $$.state, hasAxis = _b.hasAxis, hasRadar = _b.hasRadar, hasTreemap = _b.hasTreemap;
      var helper = function(type3) {
        return $$["updateTargetsFor".concat(type3)](targets.filter($$["is".concat(type3, "Type")].bind($$)));
      };
      $$.updateTargetsForText(targets);
      if (hasAxis) {
        ["bar", "candlestick", "line"].forEach(function(v) {
          var name = capitalize(v);
          if (v === "line" && $$.hasTypeOf(name) || $$.hasType(v)) {
            helper(name);
          }
        });
        $$.updateTargetsForSubchart && $$.updateTargetsForSubchart(targets);
      } else if ($$.hasArcType(targets)) {
        var type2 = "Arc";
        if (hasRadar) {
          type2 = "Radar";
        } else if ($$.hasType("polar")) {
          type2 = "Polar";
        }
        helper(type2);
      } else if (hasTreemap) {
        helper("Treemap");
      }
      var hasPointType = $$.hasType("bubble") || $$.hasType("scatter");
      if (hasPointType) {
        (_a2 = $$.updateTargetForCircle) === null || _a2 === void 0 ? void 0 : _a2.call($$);
      }
      $$.filterTargetsToShowAtInit(hasPointType);
    };
    ChartInternal2.prototype.filterTargetsToShowAtInit = function(hasPointType) {
      if (hasPointType === void 0) {
        hasPointType = false;
      }
      var $$ = this;
      var svg = $$.$el.svg, $T = $$.$T;
      var selector = ".".concat($COMMON.target);
      if (hasPointType) {
        selector += ", .".concat($CIRCLE.chartCircles, " > .").concat($CIRCLE.circles);
      }
      $T(svg.selectAll(selector).filter(function(d) {
        return $$.isTargetToShow(d.id);
      })).style("opacity", null);
    };
    ChartInternal2.prototype.getWithOption = function(options2) {
      var withOptions = {
        Dimension: true,
        EventRect: true,
        Legend: false,
        Subchart: true,
        Transform: false,
        Transition: true,
        TrimXDomain: true,
        UpdateXAxis: "UpdateXDomain",
        UpdateXDomain: false,
        UpdateOrgXDomain: false,
        TransitionForExit: "Transition",
        TransitionForAxis: "Transition",
        Y: true
      };
      Object.keys(withOptions).forEach(function(key) {
        var defVal = withOptions[key];
        if (isString(defVal)) {
          defVal = withOptions[defVal];
        }
        withOptions[key] = getOption(options2, "with".concat(key), defVal);
      });
      return withOptions;
    };
    ChartInternal2.prototype.initialOpacity = function(d) {
      var $$ = this;
      var withoutFadeIn = $$.state.withoutFadeIn;
      var r = $$.getBaseValue(d) !== null && withoutFadeIn[d.id] ? null : "0";
      return r;
    };
    ChartInternal2.prototype.bindResize = function() {
      var $$ = this;
      var config = $$.config, state = $$.state;
      var resizeFunction = generateResize(config.resize_timer);
      var list = [];
      list.push(function() {
        return callFn(config.onresize, $$.api);
      });
      if (config.resize_auto) {
        list.push(function() {
          state.resizing = true;
          if (config.legend_show) {
            $$.updateSizes();
            $$.updateLegend();
          }
          $$.api.flush(false);
        });
      }
      list.push(function() {
        callFn(config.onresized, $$.api);
        state.resizing = false;
      });
      list.forEach(function(v) {
        return resizeFunction.add(v);
      });
      $$.resizeFunction = resizeFunction;
      win.addEventListener("resize", $$.resizeFunction = resizeFunction);
    };
    ChartInternal2.prototype.callPluginHook = function(phase) {
      var _this = this;
      var args = [];
      for (var _i = 1; _i < arguments.length; _i++) {
        args[_i - 1] = arguments[_i];
      }
      this.config.plugins.forEach(function(v) {
        if (phase === "$beforeInit") {
          v.$$ = _this;
          _this.api.plugins.push(v);
        }
        v[phase].apply(v, args);
      });
    };
    return ChartInternal2;
  }()
);
extend2(ChartInternal.prototype, [
  // common
  dataConvert,
  data$1,
  dataLoad,
  category,
  classModule,
  color2,
  domain,
  interaction,
  format2,
  legend$1,
  redraw,
  scale,
  shape,
  size,
  style,
  text,
  title,
  tooltip$1,
  transform2,
  typeInternals
]);
function loadConfig(config) {
  var thisConfig = this.config;
  var target;
  var keys;
  var read;
  var find2 = function() {
    var key = keys.shift();
    if (key && target && isObjectType(target) && key in target) {
      target = target[key];
      return find2();
    } else if (!key) {
      return target;
    }
    return void 0;
  };
  Object.keys(thisConfig).forEach(function(key) {
    target = config;
    keys = key.split("_");
    read = find2();
    if (isDefined(read)) {
      thisConfig[key] = read;
    }
  });
  if (this.api) {
    this.state.orgConfig = config;
  }
}
var apiChart = {
  /**
   * Resize the chart.
   * @function resize
   * @instance
   * @memberof Chart
   * @param {object} size This argument should include width and height in pixels.
   * @param {number} [size.width] width value
   * @param {number} [size.height] height value
   * @example
   * // Resize to 640x480
   * chart.resize({
   *    width: 640,
   *    height: 480
   * });
   */
  resize: function(size2) {
    var $$ = this.internal;
    var config = $$.config, state = $$.state;
    if (state.rendered) {
      config.size_width = size2 ? size2.width : null;
      config.size_height = size2 ? size2.height : null;
      state.resizing = true;
      this.flush(false);
      $$.resizeFunction();
    }
  },
  /**
   * Force to redraw.
   * - **NOTE:** When zoom/subchart is used, the zoomed state will be resetted.
   * @function flush
   * @instance
   * @memberof Chart
   * @param {boolean} [soft] For soft redraw.
   * @example
   * chart.flush();
   *
   * // for soft redraw
   * chart.flush(true);
   */
  flush: function(soft) {
    var _a2, _b;
    var $$ = this.internal;
    var state = $$.state, zoomResetBtn = $$.$el.zoomResetBtn;
    if (state.rendered) {
      if (state.resizing) {
        (_a2 = $$.brush) === null || _a2 === void 0 ? void 0 : _a2.updateResize();
      } else {
        (_b = $$.axis) === null || _b === void 0 ? void 0 : _b.setOrient();
      }
      zoomResetBtn === null || zoomResetBtn === void 0 ? void 0 : zoomResetBtn.style("display", "none");
      $$.scale.zoom = null;
      soft ? $$.redraw({
        withTransform: true,
        withUpdateXDomain: true,
        withUpdateOrgXDomain: true,
        withLegend: true
      }) : $$.updateAndRedraw({
        withLegend: true,
        withTransition: false,
        withTransitionForTransform: false
      });
      if (!state.resizing && $$.brush) {
        $$.brush.getSelection().call($$.brush.move);
        $$.unselectRect();
      }
    } else {
      $$.initToRender(true);
    }
  },
  /**
   * Reset the chart object and remove element and events completely.
   * @function destroy
   * @instance
   * @memberof Chart
   * @returns {null}
   * @example
   * chart.destroy();
   */
  destroy: function() {
    var _this = this;
    var $$ = this.internal;
    var _a2 = $$.$el, chart = _a2.chart, style2 = _a2.style, svg = _a2.svg;
    if (notEmpty($$)) {
      $$.callPluginHook("$willDestroy");
      $$.charts.splice($$.charts.indexOf(this), 1);
      $$.unbindAllEvents();
      svg.select("*").interrupt();
      $$.resizeFunction.clear();
      win.removeEventListener("resize", $$.resizeFunction);
      chart.classed("bb", false).style("position", null).selectChildren().remove();
      style2 && style2.parentNode.removeChild(style2);
      Object.keys(this).forEach(function(key2) {
        key2 === "internal" && Object.keys($$).forEach(function(k2) {
          $$[k2] = null;
        });
        _this[key2] = null;
        delete _this[key2];
      });
      for (var key in this) {
        this[key] = function() {
        };
      }
    }
    return null;
  },
  /**
   * Get or set config option value.
   * - **NOTE**
   *  - The option key name must be specified as the last level.
   *  - when no argument is given, will return all specified generation options object only. (will exclude any other options not specified at the initialization)
   * @function config
   * @instance
   * @memberof Chart
   * @param {string} name The option key name.
   * @param {*} [value] The value accepted for indicated option.
   * @param {boolean} [redraw] Set to redraw with the new option changes.
   * - **NOTE:** Doesn't guarantee work in all circumstances. It can be applied for limited options only.
   * @returns {*}
   * @example
   *
   * // Getter
   * chart.config("gauge.max");
   *
   * // Getter specified with top level key name will not work.
   * // The option key name must be specified as the last level.
   * // chart.config("gauge"); // will not work
   *
   * // without any arguments, it returns generation config object
   * chart.config();  // {data: { ... }, axis: { ... }, ...}
   *
   * // Setter
   * chart.config("gauge.max", 100);
   *
   * // Setter specified with top level key name will not work.
   * // The option key name must be specified as the last level.
   * // chart.config("gauge", {min: 10, max: 20}); // will not work
   *
   * // Setter & redraw with the new option
   * chart.config("gauge.max", 100, true);
   */
  config: function(name, value, redraw2) {
    var $$ = this.internal;
    var config = $$.config, state = $$.state;
    var key = name === null || name === void 0 ? void 0 : name.replace(/\./g, "_");
    var res;
    if (name && key in config) {
      if (isDefined(value)) {
        config[key] = value;
        res = value;
        redraw2 && this.flush();
      } else {
        res = config[key];
      }
    } else if (arguments.length === 0 || isEmpty(name)) {
      res = state.orgConfig;
    }
    return res;
  }
};
var apiColor = {
  /**
   * Get the color
   * @function color
   * @instance
   * @memberof Chart
   * @param {string} id id to get the color
   * @returns {string}
   * @example
   * chart.color("data1");
   */
  color: function(id2) {
    return this.internal.color(id2);
  }
};
var data = function(targetIds) {
  var targets = this.internal.data.targets;
  if (!isUndefined(targetIds)) {
    var ids_1 = isArray(targetIds) ? targetIds : [targetIds];
    return targets.filter(function(t) {
      return ids_1.some(function(v) {
        return v === t.id;
      });
    });
  }
  return targets;
};
extend2(data, {
  /**
   * Get data shown in the chart.
   * @function data․shown
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds If this argument is given, this API filters the data with specified target ids. If this argument is not given, all shown data will be returned.
   * @returns {Array} Data objects
   * @example
   * // Get shown data by filtering to include only data1 data
   * chart.data.shown("data1");
   * // --> [{id: "data1", id_org: "data1", values: Array(6)}, ...]
   *
   * // Get shown data by filtering to include data1 and data2 data
   * chart.data.shown(["data1", "data2"]);
   *
   * // Get all shown data
   * chart.data.shown();
   */
  shown: function(targetIds) {
    return this.internal.filterTargetsToShow(this.data(targetIds));
  },
  /**
   * Get values of the data loaded in the chart.
   * @function data․values
   * @instance
   * @memberof Chart
   * @param {string|Array|null} targetIds This API returns the values of specified target. If this argument is not given, null will be retruned
   * @param {boolean} [flat=true] Get flatten values
   * @returns {Array} Data values
   * @example
   * // Get data1 values
   * chart.data.values("data1");
   * // --> [10, 20, 30, 40]
   */
  values: function(targetIds, flat) {
    if (flat === void 0) {
      flat = true;
    }
    var values = null;
    if (targetIds) {
      var targets = this.data(targetIds);
      if (isArray(targets)) {
        values = [];
        targets.forEach(function(v) {
          var dataValue = v.values.map(function(d) {
            return d.value;
          });
          flat ? values = values.concat(dataValue) : values.push(dataValue);
        });
      }
    }
    return values;
  },
  /**
   * Get and set names of the data loaded in the chart.
   * @function data․names
   * @instance
   * @memberof Chart
   * @param {object} names If this argument is given, the names of data will be updated. If not given, the current names will be returned. The format of this argument is the same as [data.names](./Options.html#.data%25E2%2580%25A4names).
   * @returns {object} Corresponding names according its key value, if specified names values.
   * @example
   * // Get current names
   * chart.data.names();
   * // --> {data1: "test1", data2: "test2"}
   *
   * // Update names
   * chart.data.names({
   *  data1: "New Name 1",
   *  data2: "New Name 2"
   *});
   */
  names: function(names) {
    var $$ = this.internal;
    return $$.updateDataAttributes("names", names);
  },
  /**
   * Get and set colors of the data loaded in the chart.
   * @function data․colors
   * @instance
   * @memberof Chart
   * @param {object} colors If this argument is given, the colors of data will be updated. If not given, the current colors will be returned. The format of this argument is the same as [data.colors](./Options.html#.data%25E2%2580%25A4colors).
   * @returns {object} Corresponding data color value according its key value.
   * @example
   * // Get current colors
   * chart.data.colors();
   * // --> {data1: "#00c73c", data2: "#fa7171"}
   *
   * // Update colors
   * chart.data.colors({
   *  data1: "#FFFFFF",
   *  data2: "#000000"
   * });
   */
  colors: function(colors) {
    return this.internal.updateDataAttributes("colors", colors);
  },
  /**
   * Get and set axes of the data loaded in the chart.
   * - **NOTE:** If all data is related to one of the axes, the domain of axis without related data will be replaced by the domain from the axis with related data
   * @function data․axes
   * @instance
   * @memberof Chart
   * @param {object} axes If this argument is given, the axes of data will be updated. If not given, the current axes will be returned. The format of this argument is the same as
   * @returns {object} Corresponding axes value for data, if specified axes value.
   * @example
   * // Get current axes
   * chart.data.axes();
   * // --> {data1: "y"}
   *
   * // Update axes
   * chart.data.axes({
   *  data1: "y",
   *  data2: "y2"
   * });
   */
  axes: function(axes) {
    return this.internal.updateDataAttributes("axes", axes);
  },
  /**
   * Get the minimum data value bound to the chart
   * @function data․min
   * @instance
   * @memberof Chart
   * @returns {Array} Data objects
   * @example
   * // Get current axes
   * chart.data.min();
   * // --> [{x: 0, value: 30, id: "data1", index: 0}, ...]
   */
  min: function() {
    return this.internal.getMinMaxData().min;
  },
  /**
   * Get the maximum data value bound to the chart
   * @function data․max
   * @instance
   * @memberof Chart
   * @returns {Array} Data objects
   * @example
   * // Get current axes
   * chart.data.max();
   * // --> [{x: 3, value: 400, id: "data1", index: 3}, ...]
   */
  max: function() {
    return this.internal.getMinMaxData().max;
  }
});
var apiData = { data };
var b64EncodeUnicode = function(str) {
  var _a2;
  return (_a2 = win.btoa) === null || _a2 === void 0 ? void 0 : _a2.call(win, encodeURIComponent(str).replace(/%([0-9A-F]{2})/g, function(match, p) {
    return String.fromCharCode(Number("0x".concat(p)));
  }));
};
function nodeToSvgDataUrl(node, option, orgSize) {
  var _a2 = option || orgSize, width = _a2.width, height = _a2.height;
  var serializer = new XMLSerializer();
  var clone = node.cloneNode(true);
  var cssText = getCssRules(toArray(doc.styleSheets)).filter(function(r) {
    return r.cssText;
  }).map(function(r) {
    return r.cssText;
  });
  clone.setAttribute("xmlns", namespaces_default.xhtml);
  clone.style.margin = "0";
  clone.style.padding = "0";
  if (option.preserveFontStyle) {
    clone.querySelectorAll("text").forEach(function(t) {
      t.innerHTML = "";
    });
  }
  var nodeXml = serializer.serializeToString(clone);
  var style2 = doc.createElement("style");
  style2.appendChild(doc.createTextNode(cssText.join("\n")));
  var styleXml = serializer.serializeToString(style2);
  var dataStr = '<svg xmlns="'.concat(namespaces_default.svg, '" width="').concat(width, '" height="').concat(height, '" \n		viewBox="0 0 ').concat(orgSize.width, " ").concat(orgSize.height, '" \n		preserveAspectRatio="').concat((option === null || option === void 0 ? void 0 : option.preserveAspectRatio) === false ? "none" : "xMinYMid meet", '">\n			<foreignObject width="100%" height="100%">\n				').concat(styleXml, "\n				").concat(nodeXml.replace(/(url\()[^#]+/g, "$1"), "\n			</foreignObject></svg>");
  return "data:image/svg+xml;base64,".concat(b64EncodeUnicode(dataStr));
}
function getCoords(elem, svgOffset) {
  var top2 = svgOffset.top, left2 = svgOffset.left;
  var _a2 = elem.getBBox(), x3 = _a2.x, y3 = _a2.y;
  var _b = elem.getScreenCTM(), a2 = _b.a, b = _b.b, c = _b.c, d = _b.d, e = _b.e, f = _b.f;
  var _c = elem.getBoundingClientRect(), width = _c.width, height = _c.height;
  return {
    x: a2 * x3 + c * y3 + e - left2,
    y: b * x3 + d * y3 + f - top2 + (height - Math.round(height / 4)),
    width,
    height
  };
}
function getGlyph(svg) {
  var _a2 = svg.getBoundingClientRect(), left2 = _a2.left, top2 = _a2.top;
  var filterFn = function(t) {
    return t.textContent || t.childElementCount;
  };
  var glyph = [];
  toArray(svg.querySelectorAll("text")).filter(filterFn).forEach(function(t) {
    var getStyleFn = function(ts) {
      var _a3;
      var _b = win.getComputedStyle(ts), fill = _b.fill, fontFamily = _b.fontFamily, fontSize = _b.fontSize, textAnchor = _b.textAnchor, transform3 = _b.transform;
      var _c = getCoords(ts, { left: left2, top: top2 }), x3 = _c.x, y3 = _c.y, width = _c.width, height = _c.height;
      return _a3 = {}, _a3[ts.textContent] = {
        x: x3,
        y: y3,
        width,
        height,
        fill,
        fontFamily,
        fontSize,
        textAnchor,
        transform: transform3
      }, _a3;
    };
    if (t.childElementCount > 1) {
      var text2 = [];
      toArray(t.querySelectorAll("tspan")).filter(filterFn).forEach(function(ts) {
        glyph.push(getStyleFn(ts));
      });
      return text2;
    } else {
      glyph.push(getStyleFn(t));
    }
  });
  return glyph;
}
function renderText(ctx, glyph) {
  glyph.forEach(function(g) {
    Object.keys(g).forEach(function(key) {
      var _a2 = g[key], x3 = _a2.x, y3 = _a2.y, width = _a2.width, height = _a2.height, fill = _a2.fill, fontFamily = _a2.fontFamily, fontSize = _a2.fontSize, transform3 = _a2.transform;
      ctx.save();
      ctx.font = "".concat(fontSize, " ").concat(fontFamily);
      ctx.fillStyle = fill;
      if (transform3 === "none") {
        ctx.fillText(key, x3, y3);
      } else {
        var args = transform3.replace(/(matrix|\(|\))/g, "").split(",");
        if (args.splice(4).every(function(v) {
          return +v === 0;
        })) {
          args.push(x3 + width - width / 4);
          args.push(y3 - height + height / 3);
        } else {
          args.push(x3);
          args.push(y3);
        }
        ctx.transform.apply(ctx, args);
        ctx.fillText(key, 0, 0);
      }
      ctx.restore();
    });
  });
}
var apiExport = {
  /**
   * Export chart as an image.
   * - **NOTE:**
   *   - IE11 and below not work properly due to the lack of the feature(<a href="https://msdn.microsoft.com/en-us/library/hh834675(v=vs.85).aspx">foreignObject</a>) support
   *   - Every style applied to the chart & the basic CSS file(ex. billboard.css) should be at same domain as API call context to get correct styled export image.
   * @function export
   * @instance
   * @memberof Chart
   * @param {object} option Export option
   * @param {string} [option.mimeType="image/png"] The desired output image format. (ex. 'image/png' for png, 'image/jpeg' for jpeg format)
   * @param {number} [option.width={currentWidth}] width
   * @param {number} [option.height={currentHeigth}] height
   * @param {boolean} [option.preserveAspectRatio=true] Preserve aspect ratio on given size
   * @param {boolean} [option.preserveFontStyle=false] Preserve font style(font-family).<br>
   * **NOTE:**
   *   - This option is useful when outlink web font style's `font-family` are applied to chart's text element.
   *   - Text element's position(especially "transformed") can't be preserved correctly according the page's layout condition.
   *   - If need to preserve accurate text position, embed the web font data within to the page and set `preserveFontStyle=false`.
   *     - Checkout the embed example: <a href="https://stackblitz.com/edit/zfbya9-8nf9nn?file=index.html">https://stackblitz.com/edit/zfbya9-8nf9nn?file=index.html</a>
   * @param {Function} [callback] The callback to be invoked when export is ready.
   * @returns {string} dataURI
   * @example
   *  chart.export();
   *  // --> "data:image/svg+xml;base64,PHN..."
   *
   *  // Initialize the download automatically
   *  chart.export({mimeType: "image/png"}, dataUrl => {
   *     const link = document.createElement("a");
   *
   *     link.download = `${Date.now()}.png`;
   *     link.href = dataUrl;
   *     link.innerHTML = "Download chart as image";
   *
   *     document.body.appendChild(link);
   *  });
   *
   *  // Resize the exported image
   *  chart.export(
   *    {
   *      width: 800,
   *      height: 600,
   *      preserveAspectRatio: false,
   *      preserveFontStyle: false,
   *      mimeType: "image/png"
   *    },
   *    dataUrl => { ... }
   *  );
   */
  export: function(option, callback) {
    var _this = this;
    var $$ = this.internal;
    var state = $$.state, _a2 = $$.$el, chart = _a2.chart, svg = _a2.svg;
    var _b = state.current, width = _b.width, height = _b.height;
    var opt = mergeObj({
      width,
      height,
      preserveAspectRatio: true,
      preserveFontStyle: false,
      mimeType: "image/png"
    }, option);
    var svgDataUrl = nodeToSvgDataUrl(chart.node(), opt, { width, height });
    var glyph = opt.preserveFontStyle ? getGlyph(svg.node()) : [];
    if (callback && isFunction(callback)) {
      var img_1 = new Image();
      img_1.crossOrigin = "Anonymous";
      img_1.onload = function() {
        var canvas = doc.createElement("canvas");
        var ctx = canvas.getContext("2d");
        canvas.width = opt.width || width;
        canvas.height = opt.height || height;
        ctx.drawImage(img_1, 0, 0);
        if (glyph.length) {
          renderText(ctx, glyph);
          glyph.length = 0;
        }
        callback.bind(_this)(canvas.toDataURL(opt.mimeType));
      };
      img_1.src = svgDataUrl;
    }
    return svgDataUrl;
  }
};
var apiFocus = {
  /**
   * This API highlights specified targets and fade out the others.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be highlighted.
   * @function focus
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be highlighted.
   * @example
   *  // data1 will be highlighted and the others will be faded out
   *  chart.focus("data1");
   *
   * // data1 and data2 will be highlighted and the others will be faded out
   * chart.focus(["data1", "data2"]);
   *
   * // all targets will be highlighted
   * chart.focus();
   */
  focus: function(targetIdsValue) {
    var $$ = this.internal;
    var state = $$.state;
    var targetIds = $$.mapToTargetIds(targetIdsValue);
    var candidates = $$.$el.svg.selectAll($$.selectorTargets(targetIds.filter($$.isTargetToShow, $$)));
    this.revert();
    this.defocus();
    candidates.classed($FOCUS.focused, true).classed($FOCUS.defocused, false);
    if ($$.hasArcType() && !state.hasRadar) {
      $$.expandArc(targetIds);
      $$.hasType("gauge") && $$.markOverlapped(targetIdsValue, $$, ".".concat($GAUGE.gaugeValue));
    }
    $$.toggleFocusLegend(targetIds, true);
    state.focusedTargetIds = targetIds;
    state.defocusedTargetIds = state.defocusedTargetIds.filter(function(id2) {
      return targetIds.indexOf(id2) < 0;
    });
  },
  /**
   * This API fades out specified targets and reverts the others.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be faded out.
   * @function defocus
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be faded out.
   * @example
   * // data1 will be faded out and the others will be reverted.
   * chart.defocus("data1");
   *
   * // data1 and data2 will be faded out and the others will be reverted.
   * chart.defocus(["data1", "data2"]);
   *
   * // all targets will be faded out.
   * chart.defocus();
   */
  defocus: function(targetIdsValue) {
    var $$ = this.internal;
    var state = $$.state;
    var targetIds = $$.mapToTargetIds(targetIdsValue);
    var candidates = $$.$el.svg.selectAll($$.selectorTargets(targetIds.filter($$.isTargetToShow, $$)));
    candidates.classed($FOCUS.focused, false).classed($FOCUS.defocused, true);
    if ($$.hasArcType(null, ["polar"])) {
      $$.unexpandArc(targetIds);
      $$.hasType("gauge") && $$.undoMarkOverlapped($$, ".".concat($GAUGE.gaugeValue));
    }
    $$.toggleFocusLegend(targetIds, false);
    state.focusedTargetIds = state.focusedTargetIds.filter(function(id2) {
      return targetIds.indexOf(id2) < 0;
    });
    state.defocusedTargetIds = targetIds;
  },
  /**
   * Revert focused or defocused state to initial state.<br><br>
   * You can specify multiple targets by giving an array that includes id as string. If no argument is given, all of targets will be reverted.
   * @function revert
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIdsValue Target ids to be reverted
   * @example
   * // 'data1' will be reverted.
   * chart.revert("data1");
   *
   * // 'data1' and 'data2' will be reverted.
   * chart.revert(["data1", "data2"]);
   *
   * // all targets will be reverted.
   * chart.revert();
   */
  revert: function(targetIdsValue) {
    var $$ = this.internal;
    var config = $$.config, state = $$.state, $el = $$.$el;
    var targetIds = $$.mapToTargetIds(targetIdsValue);
    var candidates = $el.svg.selectAll($$.selectorTargets(targetIds));
    candidates.classed($FOCUS.focused, false).classed($FOCUS.defocused, false);
    $$.hasArcType(null, ["polar"]) && $$.unexpandArc(targetIds);
    if (config.legend_show) {
      $$.showLegend(targetIds.filter($$.isLegendToShow.bind($$)));
      $el.legend.selectAll($$.selectorLegends(targetIds)).filter(function() {
        return select_default2(this).classed($FOCUS.legendItemFocused);
      }).classed($FOCUS.legendItemFocused, false);
    }
    state.focusedTargetIds = [];
    state.defocusedTargetIds = [];
  }
};
var legend = {
  /**
   * Show legend for each target.
   * - **NOTE:** Legend APIs aren't supported for `treemap` type.
   * @function legend․show
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds
   * - If targetIds is given, specified target's legend will be shown.
   * - If only one target is the candidate, String can be passed.
   * - If no argument is given, all of target's legend will be shown.
   * @example
   * // Show legend for data1.
   * chart.legend.show("data1");
   *
   * // Show legend for data1 and data2.
   * chart.legend.show(["data1", "data2"]);
   *
   * // Show all legend.
   * chart.legend.show();
   */
  show: function(targetIds) {
    var $$ = this.internal;
    $$.showLegend($$.mapToTargetIds(targetIds));
    $$.updateAndRedraw({ withLegend: true });
  },
  /**
   * Hide legend for each target.
   * @function legend․hide
   * @instance
   * @memberof Chart
   * @param {string|Array} targetIds
   * - If targetIds is given, specified target's legend will be hidden.
   * - If only one target is the candidate, String can be passed.
   * - If no argument is given, all of target's legend will be hidden.
   * @example
   * // Hide legend for data1.
   * chart.legend.hide("data1");
   *
   * // Hide legend for data1 and data2.
   * chart.legend.hide(["data1", "data2"]);
   *
   * // Hide all legend.
   * chart.legend.hide();
   */
  hide: function(targetIds) {
    var $$ = this.internal;
    $$.hideLegend($$.mapToTargetIds(targetIds));
    $$.updateAndRedraw({ withLegend: true });
  }
};
var apiLegend = { legend };
var apiLoad = {
  /**
   * Load data to the chart.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be toggles.
   * - <b>Note:</b>
   *   - unload should be used if some data needs to be unloaded simultaneously.
   *     If you call unload API soon after/before load instead of unload param, chart will not be rendered properly because of cancel of animation.<br>
   *   - done will be called after data loaded, but it's not after rendering.
   *     It's because rendering will finish after some transition and there is some time lag between loading and rendering
   * @function load
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | columns | Array | The `columns` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | json | Array | The `json` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | rows | Array | The `rows` data will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | url | string | The data from `url` will be loaded. If data that has the same target id is given, the chart will be updated. Otherwise, new target will be added |
   *    | &nbsp; | | |
   *    | append | boolean | Load data appending it to the current dataseries.<br>If the existing chart has`x` value, should provide with corresponding `x` value for newly loaded data.  |
   *    | axes | Object | The axes specified by data.axes will be updated. axes must be Object that has target id as keys. |
   *    | categories | Array | The categories specified by axis.x.categories or data.x will be updated. categories must be Array. |
   *    | classes | Object | The classes specified by data.classes will be updated. classes must be Object that has target id as keys. |
   *    | colors | Object | The colors specified by data.colors will be updated. colors must be Object that has target id as keys. |
   *    | data | Obejct | Data objects to be loaded. Checkout the example. |
   *    | done | Function | The specified function will be called after data loaded.|
   *    | headers | string |  Set request header if loading via `data.url`.<br>@see [data․headers](Options.html#.data%25E2%2580%25A4headers) |
   *    | keys | Object |  Choose which JSON objects keys correspond to desired data.<br>**NOTE:** Only for JSON object given as array.<br>@see [data․keys](Options.html#.data%25E2%2580%25A4keys) |
   *    | mimeType | string |  Set 'json' if loading JSON via url.<br>@see [data․mimeType](Options.html#.data%25E2%2580%25A4mimeType) |
   *    | names | Object | Same as data.names() |
   *    | resizeAfter | boolean | Resize after the load. Default value is `false`.<br>- This option won't call `onresize` neither `onresized`.<br>- When set to 'true', will call `.flush(true)` at the end of load. |
   *    | type | string | The type of targets will be updated. |
   *    | types | Object | The types of targets will be updated. |
   *    | unload | Array | Specify the data will be unloaded before loading new data. If true given, all of data will be unloaded. If target ids given as String or Array, specified targets will be unloaded. If absent or false given, unload will not occur. |
   *    | xs | string | Same as data.xs option  |
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataFromURL)
   * @example
   * // Load data1 and unload data2 and data3
   * chart.load({
   *     columns: [
   *        ["data1", 100, 200, 150, ...],
   *        ...
   *    ],
   *    unload: ["data2", "data3"],
   *    url: "...",
   *    done: function() { ... }
   *    resizeAfter: true  // will resize after load
   * });
   * @example
   * const chart = bb.generate({
   *   data: {
   *     columns: [
   *       ["data1", 20, 30, 40]
   *     ]
   *   }
   * });
   *
   * chart.load({
   *    columns: [
   *        // with 'append' option, the 'data1' will have `[20,30,40,50,60]`.
   *        ["data1", 50, 60]
   *    ],
   *    append: true
   * });
   * @example
   * const chart = bb.generate({
   *   data: {
   *     x: "x",
   *     xFormat: "%Y-%m-%dT%H:%M:%S",
   *     columns: [
   *       ["x", "2021-01-03T03:00:00", "2021-01-04T12:00:00", "2021-01-05T21:00:00"],
   *       ["data1", 36, 30, 24]
   *     ]
   *   },
   *   axis: {
   *     x: {
   *       type: "timeseries"
   *     }
   *   }
   * };
   *
   * chart.load({
   *   columns: [
   *     // when existing chart has `x` value, should provide correponding 'x' value.
   *     // with 'append' option, the 'data1' will have `[36,30,24,37]`.
   *     ["x", "2021-02-01T08:00:00"],
   *     ["data1", 37]
   *   ],
   *   append: true
   * });
   * @example
   * // myAPI.json
   * // {
   * //   "data1": [220, 240, 270, 250, 280],
   * //   "data2": [180, 150, 300, 70, 120]
   * // }
   *
   * chart.load({
   *     url: './data/myAPI.json',
   *     mimeType: "json",
   *
   *     // set request header if is needed
   *     headers: {
   *       "Content-Type": "text/json"
   *     }
   * });
   * @example
   * chart.load({
   *     data: [
   *       // equivalent as: columns: [["data1", 30, 200, 100]]
   *       {"data1": 30}, {"data1": 200}, {"data1": 100}
   *
   *       // or
   *       // equivalent as: columns: [["data1", 10, 20], ["data2", 13, 30]]
   *       // {"data1": 10, "data2": 13}, {"data1": 20, "data2": 30}}
   *     ]
   * });
   * @example
   * chart.load({
   *     json: [
   *          {name: "www.site1.com", upload: 800, download: 500, total: 400},
   *     ],
   *     keys: {
   *         x: "name",
   *         value: ["upload", "download"]
   *     }
   * });
   * @example
   * chart.load({
   *   json: {
   *       data1:[30, 20, 50, 40, 60, 50],
   *       data2:[200, 130, 90, 240, 130, 220],
   *   }
   * });
   */
  load: function(args) {
    var $$ = this.internal;
    var config = $$.config;
    args.xs && $$.addXs(args.xs);
    "names" in args && this.data.names(args.names);
    "classes" in args && Object.keys(args.classes).forEach(function(id2) {
      config.data_classes[id2] = args.classes[id2];
    });
    if ("categories" in args && $$.axis.isCategorized()) {
      config.axis_x_categories = args.categories;
    }
    "axes" in args && Object.keys(args.axes).forEach(function(id2) {
      config.data_axes[id2] = args.axes[id2];
    });
    "colors" in args && Object.keys(args.colors).forEach(function(id2) {
      config.data_colors[id2] = args.colors[id2];
    });
    if ("unload" in args && args.unload !== false) {
      $$.unload($$.mapToTargetIds(args.unload === true ? null : args.unload), function() {
        requestIdleCallback(function() {
          return $$.loadFromArgs(args);
        });
      });
    } else {
      $$.loadFromArgs(args);
    }
  },
  /**
   * Unload data to the chart.<br><br>
   * You can specify multiple targets by giving an array that includes id as String. If no argument is given, all of targets will be toggles.
   * - <b>Note:</b>
   * If you call load API soon after/before unload, unload param of load should be used. Otherwise chart will not be rendered properly because of cancel of animation.<br>
   * `done` will be called after data loaded, but it's not after rendering. It's because rendering will finish after some transition and there is some time lag between loading and rendering.
   * @function unload
   * @instance
   * @memberof Chart
   * @param {object} argsValue
   *  | key | Type | Description |
   *  | --- | --- | --- |
   *  | ids | String &vert; Array | Target id data to be unloaded. If not given, all data will be unloaded. |
   *  | done | Fuction | Callback after data is unloaded. |
   *  | resizeAfter | boolean | Resize after the unload. Default value is `false`.<br>- This option won't call `onresize` neither `onresized`.<br>- When set to 'true', will call `.flush(true)` at the end of unload. |
   * @example
   *  // Unload data2 and data3
   *  chart.unload({
   *    ids: ["data2", "data3"],
   *    done: function() {
   *       // called after the unloaded
   *    },
   *    resizeAfter: true  // will resize after unload
   *  });
   */
  unload: function(argsValue) {
    var $$ = this.internal;
    var args = argsValue || {};
    isEmpty(args) && this.tooltip.hide();
    if (isArray(args)) {
      args = { ids: args };
    } else if (isString(args)) {
      args = { ids: [args] };
    }
    var ids = $$.mapToTargetIds(args.ids);
    $$.unload(ids, function() {
      $$.redraw({
        withUpdateOrgXDomain: true,
        withUpdateXDomain: true,
        withLegend: true
      });
      $$.cache.remove(ids);
      callDone.call($$, args.done, args.resizeAfter);
    });
  }
};
function showHide(show, targetIdsValue, options2) {
  var _this = this;
  var $$ = this.internal;
  var targetIds = $$.mapToTargetIds(targetIdsValue);
  var hiddenIds = $$.state.hiddenTargetIds.map(function(v) {
    return targetIds.indexOf(v) > -1 && v;
  }).filter(Boolean);
  $$.state.toggling = true;
  $$["".concat(show ? "remove" : "add", "HiddenTargetIds")](targetIds);
  var targets = $$.$el.svg.selectAll($$.selectorTargets(targetIds));
  var opacity = show ? null : "0";
  if (show && hiddenIds.length) {
    targets.style("display", null);
    callFn($$.config.data_onshown, this, hiddenIds);
  }
  $$.$T(targets).style("opacity", opacity, "important").call(endall, function() {
    if (!show && hiddenIds.length === 0) {
      targets.style("display", "none");
      callFn($$.config.data_onhidden, _this, targetIds);
    }
    targets.style("opacity", opacity);
  });
  options2.withLegend && $$["".concat(show ? "show" : "hide", "Legend")](targetIds);
  $$.redraw({
    withUpdateOrgXDomain: true,
    withUpdateXDomain: true,
    withLegend: true
  });
  $$.state.toggling = false;
}
var apiShow = {
  /**
   * Show data series on chart
   * @function show
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIdsValue] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // show 'data1'
   * chart.show("data1");
   *
   * // show 'data1' and 'data3'
   * chart.show(["data1", "data3"]);
   */
  show: function(targetIdsValue, options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    showHide.call(this, true, targetIdsValue, options2);
  },
  /**
   * Hide data series from chart
   * @function hide
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIdsValue] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // hide 'data1'
   * chart.hide("data1");
   *
   * // hide 'data1' and 'data3'
   * chart.hide(["data1", "data3"]);
   */
  hide: function(targetIdsValue, options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    showHide.call(this, false, targetIdsValue, options2);
  },
  /**
   * Toggle data series on chart. When target data is hidden, it will show. If is shown, it will hide in vice versa.
   * @function toggle
   * @instance
   * @memberof Chart
   * @param {string|Array} [targetIds] The target id value.
   * @param {object} [options] The object can consist with following members:<br>
   *
   *    | Key | Type | default | Description |
   *    | --- | --- | --- | --- |
   *    | withLegend | boolean | false | whether or not display legend |
   *
   * @example
   * // toggle 'data1'
   * chart.toggle("data1");
   *
   * // toggle 'data1' and 'data3'
   * chart.toggle(["data1", "data3"]);
   */
  toggle: function(targetIds, options2) {
    var _this = this;
    if (options2 === void 0) {
      options2 = {};
    }
    var $$ = this.internal;
    var targets = { show: [], hide: [] };
    $$.mapToTargetIds(targetIds).forEach(function(id2) {
      return targets[$$.isTargetToShow(id2) ? "hide" : "show"].push(id2);
    });
    targets.show.length && this.show(targets.show, options2);
    targets.hide.length && setTimeout(function() {
      return _this.hide(targets.hide, options2);
    }, 0);
  }
};
var tooltip = {
  /**
   * Show tooltip
   * @function tooltip․show
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | index | Number | Determine focus by index |
   *    | x | Number &vert; Date | Determine focus by x Axis index |
   *    | mouse | Array | Determine x and y coordinate value relative the targeted '.bb-event-rect' x Axis.<br>It should be used along with `data`, `index` or `x` value. The default value is set as `[0,0]` |
   *    | data | Object | When [data.xs](Options.html#.data%25E2%2580%25A4xs) option is used or [tooltip.grouped](Options.html#.tooltip) set to 'false', `should be used giving this param`.<br><br>**Key:**<br>- x {number &verbar; Date}: x Axis value<br>- index {number}: x Axis index (useless for data.xs)<br>- id {string}: data id<br>- value {number}: The corresponding value for tooltip. |
   *
   * @example
   *  // show the 2nd x Axis coordinate tooltip
   *  // for Arc(gauge, donut & pie) and radar type, approch showing tooltip by using "index" number.
   *  chart.tooltip.show({
   *    index: 1
   *  });
   *
   *  // show tooltip for the 3rd x Axis in x:50 and y:100 coordinate of '.bb-event-rect' of the x Axis.
   *  chart.tooltip.show({
   *    x: 2,
   *    mouse: [50, 100]
   *  });
   *
   *  // show tooltip for timeseries x axis
   *  chart.tooltip.show({
   *    x: new Date("2018-01-02 00:00")
   *  });
   *
   *  // treemap type can be shown by using "id" only.
   *  chart.tooltip.show({
   *    data: {
   *        id: "data1"  // data id
   *    }
   *  });
   *
   *  // for Arc types, specify 'id' or 'index'
   *  chart.tooltip.show({ data: { id: "data2" }});
   *  chart.tooltip.show({ data: { index: 2 }});
   *
   *  // when data.xs is used
   *  chart.tooltip.show({
   *    data: {
   *        x: 3,  // x Axis value
   *        id: "data1",  // data id
   *        value: 500  // data value
   *    }
   *  });
   *
   *  // when data.xs isn't used, but tooltip.grouped=false is set
   *  chart.tooltip.show({
   *    data: {
   *        index: 3,  // or 'x' key value
   *        id: "data1",  // data id
   *        value: 500  // data value
   *    }
   *  });
   */
  show: function(args) {
    var _a2, _b, _c;
    var $$ = this.internal;
    var $el = $$.$el, config = $$.config, _d = $$.state, eventReceiver = _d.eventReceiver, hasTreemap = _d.hasTreemap, inputType = _d.inputType;
    var index2;
    var mouse;
    if (args.mouse) {
      mouse = args.mouse;
    }
    if (args.data) {
      var data2 = args.data;
      var y3 = (_a2 = $$.getYScaleById(data2.id)) === null || _a2 === void 0 ? void 0 : _a2(data2.value);
      if (hasTreemap && data2.id) {
        eventReceiver.rect = $el.main.select("".concat($$.selectorTarget(data2.id, void 0, "rect")));
      } else if ($$.isMultipleX()) {
        mouse = [$$.xx(data2), y3];
      } else {
        if (!config.tooltip_grouped) {
          mouse = [0, y3];
        }
        index2 = (_b = data2.index) !== null && _b !== void 0 ? _b : $$.hasArcType() && data2.id ? (_c = $$.getArcElementByIdOrIndex(data2.id)) === null || _c === void 0 ? void 0 : _c.datum().index : $$.getIndexByX(data2.x);
      }
    } else if (isDefined(args.x)) {
      index2 = $$.getIndexByX(args.x);
    } else if (isDefined(args.index)) {
      index2 = args.index;
    }
    (inputType === "mouse" ? ["mouseover", "mousemove"] : ["touchstart"]).forEach(function(eventName) {
      $$.dispatchEvent(eventName, index2, mouse);
    });
  },
  /**
   * Hide tooltip
   * @function tooltip․hide
   * @instance
   * @memberof Chart
   */
  hide: function() {
    var _a2, _b, _c;
    var $$ = this.internal;
    var inputType = $$.state.inputType, tooltip2 = $$.$el.tooltip;
    var data2 = tooltip2 === null || tooltip2 === void 0 ? void 0 : tooltip2.datum();
    if (data2) {
      var index_1 = JSON.parse(data2.current)[0].index;
      (inputType === "mouse" ? ["mouseout"] : ["touchend"]).forEach(function(eventName) {
        $$.dispatchEvent(eventName, index_1);
      });
    }
    inputType === "touch" && $$.callOverOutForTouch();
    $$.hideTooltip(true);
    (_a2 = $$.hideGridFocus) === null || _a2 === void 0 ? void 0 : _a2.call($$);
    (_b = $$.unexpandCircles) === null || _b === void 0 ? void 0 : _b.call($$);
    (_c = $$.expandBarTypeShapes) === null || _c === void 0 ? void 0 : _c.call($$, false);
  }
};
var apiTooltip = { tooltip };
var Chart = (
  /** @class */
  /* @__PURE__ */ function() {
    function Chart2(options2) {
      this.plugins = [];
      var $$ = new ChartInternal(this);
      this.internal = $$;
      (function bindThis(fn, target, argThis) {
        Object.keys(fn).forEach(function(key) {
          var isFunc = isFunction(fn[key]);
          var isChild = target !== argThis;
          var isNotNil = notEmpty(fn[key]);
          var hasChild = isNotNil && Object.keys(fn[key]).length > 0;
          if (isFunc && (!isChild && hasChild || isChild)) {
            target[key] = fn[key].bind(argThis);
          } else if (isNotNil && !isFunc) {
            target[key] = {};
          } else {
            target[key] = fn[key];
          }
          hasChild && bindThis(fn[key], target[key], argThis);
        });
      })(Chart2.prototype, this, this);
      loadConfig.call($$, options2);
      $$.beforeInit();
      $$.init();
    }
    return Chart2;
  }()
);
extend2(Chart.prototype, [
  apiChart,
  apiColor,
  apiData,
  apiExport,
  apiFocus,
  apiLegend,
  apiLoad,
  apiShow,
  apiTooltip
]);
function setMinMax($$, type2, value) {
  var config = $$.config;
  var helper = function(key, value2) {
    var v = isNumber(value2) ? value2 : value2 === false ? void 0 : null;
    if (v !== null) {
      config["axis_".concat(key, "_").concat(type2)] = v;
    }
  };
  if (isDefined(value)) {
    if (isObjectType(value)) {
      Object.keys(value).forEach(function(key) {
        helper(key, value[key]);
      });
    } else if (isNumber(value) || value === false) {
      ["y", "y2"].forEach(function(key) {
        helper(key, value);
      });
    }
    $$.redraw({
      withUpdateOrgXDomain: true,
      withUpdateXDomain: true
    });
  }
}
function getMinMax($$, type2) {
  var config = $$.config;
  return {
    x: config["axis_x_".concat(type2)],
    y: config["axis_y_".concat(type2)],
    y2: config["axis_y2_".concat(type2)]
  };
}
var axis$1 = {
  /**
   * Get and set axis labels.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․labels
   * @instance
   * @memberof Chart
   * @param {object} labels specified axis' label to be updated.
   * @param {string} [labels.x] x Axis string
   * @param {string} [labels.y] y Axis string
   * @param {string} [labels.y2] y2 Axis string
   * @returns {object|undefined} axis labels text object
   * @example
   * // Update axis' label
   * chart.axis.labels({
   *   x: "New X Axis Label",
   *   y: "New Y Axis Label",
   *   y2: "New Y2 Axis Label"
   * });
   *
   * chart.axis.labels();
   * // --> {
   * //  x: "New X Axis Label",
   * //  y: "New Y Axis Label",
   * //  y2: "New Y2 Axis Label"
   * // }
   */
  labels: function(labels) {
    var $$ = this.internal;
    var labelText;
    if (labels) {
      Object.keys(labels).forEach(function(axisId) {
        $$.axis.setLabelText(axisId, labels[axisId]);
      });
      $$.axis.updateLabels();
    }
    ["x", "y", "y2"].forEach(function(v) {
      var text2 = $$.axis.getLabelText(v);
      if (text2) {
        !labelText && (labelText = {});
        labelText[v] = text2;
      }
    });
    return labelText;
  },
  /**
   * Get and set axis min value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․min
   * @instance
   * @memberof Chart
   * @param {object} min If min is given, specified axis' min value will be updated.<br>
   *   If no argument is given, the min values set on generating option for each axis will be returned.
   *   If not set any min values on generation, it will return `undefined`.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' min
   * chart.axis.min({
   *   x: -10,
   *   y: 1000,
   *   y2: 100
   * });
   *
   * // To unset specific axis min, set false to each of them.
   * chart.axis.min({
   *   x: false,
   *   y: false,
   *   y2: false
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.min(-50);
   * chart.axis.min(false);
   */
  min: function(min4) {
    var $$ = this.internal;
    return isValue(min4) || min4 === false ? setMinMax($$, "min", min4) : getMinMax($$, "min");
  },
  /**
   * Get and set axis max value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․max
   * @instance
   * @memberof Chart
   * @param {object} max If max is given, specified axis' max value will be updated.<br>
   *   If no argument is given, the max values set on generating option for each axis will be returned.
   *   If not set any max values on generation, it will return `undefined`.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' label
   * chart.axis.max({
   *    x: 100,
   *    y: 1000,
   *    y2: 10000
   * });
   *
   * // To unset specific axis max, set false to each of them.
   * chart.axis.max({
   *   x: false,
   *   y: false,
   *   y2: false
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.max(10);
   * chart.axis.max(false);
   */
  max: function(max4) {
    var $$ = this.internal;
    return isValue(max4) || max4 === false ? setMinMax($$, "max", max4) : getMinMax($$, "max");
  },
  /**
   * Get and set axis min and max value.
   * - **NOTE:** Only applicable for chart types which has x and y axes.
   * @function axis․range
   * @instance
   * @memberof Chart
   * @param {object} range If range is given, specified axis' min and max value will be updated.
   *   If no argument is given, the current min and max values for each axis will be returned.<br>
   *   To unset specific axis max, set `false` to each of them.
   * @returns {object|undefined}
   * @example
   * // Update axis' label
   * chart.axis.range({
   *   min: {
   *     x: -10,
   *     y: -1000,
   *     y2: -10000
   *   },
   *   max: {
   *     x: 100,
   *     y: 1000,
   *     y2: 10000
   *   },
   * });
   *
   * // To unset specific axis max, set false to each of them.
   * chart.axis.range({
   *   min: {
   *     x: false,
   *     y: false,
   *     y2: false
   *   },
   *   max: {
   *     x: false,
   *     y: false,
   *     y2: false
   *   },
   * });
   *
   * // shorthand (only affects y and y2 axis)
   * chart.axis.range({ min: -50, max: 1000 });
   * chart.axis.range({ min: false, max: false });
   */
  range: function(range2) {
    var axis3 = this.axis;
    if (arguments.length) {
      var min4 = range2.min, max4 = range2.max;
      isDefined(max4) && axis3.max(max4);
      isDefined(min4) && axis3.min(min4);
    } else {
      return {
        max: axis3.max(),
        min: axis3.min()
      };
    }
    return void 0;
  }
};
var apiAxis = { axis: axis$1 };
var apiCategory = {
  /**
   * Set specified category name on category axis.
   * @function category
   * @instance
   * @memberof Chart
   * @param {number} i index of category to be changed
   * @param {string} category category value to be changed
   * @returns {string}
   * @example
   * chart.category(2, "Category 3");
   */
  category: function(i, category2) {
    var $$ = this.internal;
    var config = $$.config;
    if (arguments.length > 1) {
      config.axis_x_categories[i] = category2;
      $$.redraw();
    }
    return config.axis_x_categories[i];
  },
  /**
   * Set or get category names on category axis.
   * @function categories
   * @instance
   * @memberof Chart
   * @param {Array} categories This must be an array that includes category names in string. If category names are included in the date by data.x option, this is not required.
   * @returns {Array}
   * @example
   * chart.categories([
   *      "Category 1", "Category 2", ...
   * ]);
   */
  categories: function(categories) {
    var $$ = this.internal;
    var config = $$.config;
    if (!categories || !Array.isArray(categories)) {
      var cat = config.axis_x_categories;
      return isEmpty(cat) ? Object.values($$.data.xs)[0] : cat;
    }
    config.axis_x_categories = categories;
    $$.redraw();
    return config.axis_x_categories;
  }
};
function grid$1(grids, axisId) {
  var $$ = this.internal;
  var config = $$.config;
  var withTransition = config.transition_duration && isTabVisible();
  var gridPropLines = "grid_".concat(axisId, "_lines");
  if (!grids) {
    return config[gridPropLines];
  }
  config[gridPropLines] = grids;
  $$.updateGrid();
  $$.redrawGrid(withTransition);
  return config[gridPropLines];
}
function add(grids, axisId) {
  var gridPropLines = "grid_".concat(axisId, "_lines");
  return grid$1.bind(this)(this.internal.config[gridPropLines].concat(grids || []), axisId);
}
function remove2(grids, isXAxis) {
  this.internal.removeGridLines(grids, isXAxis);
}
var xgrids = function(grids) {
  return grid$1.bind(this)(grids, "x");
};
extend2(xgrids, {
  /**
   * Add x grid lines.<br>
   * This API adds new x grid lines instead of replacing like xgrids.
   * @function xgrids․add
   * @instance
   * @memberof Chart
   * @param {Array|object} grids New x grid lines will be added. The format of this argument is the same as grid.x.lines and it's possible to give an Object if only one line will be added.
   * @returns {Array}
   * @example
   *  // Add a new x grid line
   * chart.xgrids.add(
   *   {value: 4, text: "Label 4"}
   * );
   *
   * // Add new x grid lines
   * chart.xgrids.add([
   *   {value: 2, text: "Label 2"},
   *   {value: 4, text: "Label 4"}
   * ]);
   */
  add: function(grids) {
    return add.bind(this)(grids, "x");
  },
  /**
   * Remove x grid lines.<br>
   * This API removes x grid lines.
   * @function xgrids․remove
   * @instance
   * @memberof Chart
   * @param {object} grids This argument should include value or class. If value is given, the x grid lines that have specified x value will be removed. If class is given, the x grid lines that have specified class will be removed. If args is not given, all of x grid lines will be removed.
   * @param {number} [grids.value] target value
   * @param {string} [grids.class] target class
   * @returns {void}
   * @example
   * // x grid line on x = 2 will be removed
   * chart.xgrids.remove({value: 2});
   *
   * // x grid lines that have 'grid-A' will be removed
   * chart.xgrids.remove({
   *   class: "grid-A"
   * });
   *
   * // all of x grid lines will be removed
   * chart.xgrids.remove();
   */
  remove: function(grids) {
    return remove2.bind(this)(grids, true);
  }
});
var ygrids = function(grids) {
  return grid$1.bind(this)(grids, "y");
};
extend2(ygrids, {
  /**
   * Add y grid lines.<br>
   * This API adds new y grid lines instead of replacing like ygrids.
   * @function ygrids․add
   * @instance
   * @memberof Chart
   * @param {Array|object} grids New y grid lines will be added. The format of this argument is the same as grid.y.lines and it's possible to give an Object if only one line will be added.
   * @returns {object}
   * @example
   *  // Add a new x grid line
   * chart.ygrids.add(
   *   {value: 400, text: "Label 4"}
   * );
   *
   * // Add new x grid lines
   * chart.ygrids.add([
   *   {value: 200, text: "Label 2"},
   *   {value: 400, text: "Label 4"}
   * ]);
   */
  add: function(grids) {
    return add.bind(this)(grids, "y");
  },
  /**
   * Remove y grid lines.<br>
   * This API removes x grid lines.
   * @function ygrids․remove
   * @instance
   * @memberof Chart
   * @param {object} grids This argument should include value or class. If value is given, the y grid lines that have specified y value will be removed. If class is given, the y grid lines that have specified class will be removed. If args is not given, all of y grid lines will be removed.
   * @param {number} [grids.value] target value
   * @param {string} [grids.class] target class
   * @returns {void}
   * @example
   * // y grid line on y = 200 will be removed
   * chart.ygrids.remove({value: 200});
   *
   * // y grid lines that have 'grid-A' will be removed
   * chart.ygrids.remove({
   *   class: "grid-A"
   * });
   *
   * // all of y grid lines will be removed
   * chart.ygrids.remove();
   */
  remove: function(grids) {
    return remove2.bind(this)(grids, false);
  }
});
var apiGrid = { xgrids, ygrids };
var apiGroup = {
  /**
   * Update groups for the targets.
   * @function groups
   * @instance
   * @memberof Chart
   * @param {Array} groups This argument needs to be an Array that includes one or more Array that includes target ids to be grouped.
   * @returns {Array} Grouped data names array
   * @example
   *  // data1 and data2 will be a new group.
   *  chart.groups([
   *     ["data1", "data2"]
   *  ]);
   */
  groups: function(groups2) {
    var $$ = this.internal;
    var config = $$.config;
    if (isUndefined(groups2)) {
      return config.data_groups;
    }
    config.data_groups = groups2;
    $$.redraw();
    return config.data_groups;
  }
};
function regionsFn(regions2, isAdd) {
  if (isAdd === void 0) {
    isAdd = false;
  }
  var $$ = this.internal;
  var config = $$.config;
  var withTransition = config.transition_duration && isTabVisible();
  if (!regions2) {
    return config.regions;
  }
  config.regions = isAdd ? config.regions.concat(regions2) : regions2;
  $$.updateRegion();
  $$.redrawRegion(withTransition);
  return isAdd ? config.regions : regions2;
}
var regions = function(regions2) {
  return regionsFn.bind(this)(regions2);
};
extend2(regions, {
  /**
   * Add new region.<br><br>
   * This API adds new region instead of replacing like regions.
   * @function regions․add
   * @instance
   * @memberof Chart
   * @param {Array|object} regions New region will be added. The format of this argument is the same as regions and it's possible to give an Object if only one region will be added.
   * @returns {Array} regions
   * @example
   * // Add a new region
   * chart.regions.add(
   *    {axis: "x", start: 5, class: "regionX"}
   * );
   *
   * // Add new regions
   * chart.regions.add([
   *    {axis: "x", start: 5, class: "regionX"},
   *    {axis: "y", end: 50, class: "regionY"}
   *]);
   */
  add: function(regions2) {
    return regionsFn.bind(this)(regions2, true);
  },
  /**
   * Remove regions.<br><br>
   * This API removes regions.
   * @function regions․remove
   * @instance
   * @memberof Chart
   * @param {object} optionsValue This argument should include classes. If classes is given, the regions that have one of the specified classes will be removed. If args is not given, all of regions will be removed.
   * @returns {Array} regions Removed regions
   * @example
   * // regions that have 'region-A' or 'region-B' will be removed.
   * chart.regions.remove({
   *   classes: [
   *     "region-A", "region-B"
   *   ]
   * });
   *
   * // all of regions will be removed.
   * chart.regions.remove();
   */
  remove: function(optionsValue) {
    var $$ = this.internal;
    var config = $$.config, $T = $$.$T;
    var options2 = optionsValue || {};
    var classes2 = getOption(options2, "classes", [$REGION.region]);
    var regions2 = $$.$el.main.select(".".concat($REGION.regions)).selectAll(classes2.map(function(c) {
      return ".".concat(c);
    }));
    $T(regions2).style("opacity", "0").remove();
    regions2 = config.regions;
    if (Object.keys(options2).length) {
      regions2 = regions2.filter(function(region2) {
        var found = false;
        if (!region2.class) {
          return true;
        }
        region2.class.split(" ").forEach(function(c) {
          if (classes2.indexOf(c) >= 0) {
            found = true;
          }
        });
        return !found;
      });
      config.regions = regions2;
    } else {
      config.regions = [];
    }
    return regions2;
  }
});
var apiRegion = { regions };
var apiX = {
  /**
   * Get and set x values for the chart.
   * @function x
   * @instance
   * @memberof Chart
   * @param {Array} x If x is given, x values of every target will be updated. If no argument is given, current x values will be returned as an Object whose keys are the target ids.
   * @returns {object} xs
   * @example
   *  // Get current x values
   *  chart.x();
   *
   *  // Update x values for all targets
   *  chart.x([100, 200, 300, 400, ...]);
   */
  x: function(x3) {
    var $$ = this.internal;
    var axis3 = $$.axis, data2 = $$.data;
    var isCategorized = axis3.isCustomX() && axis3.isCategorized();
    if (isArray(x3)) {
      if (isCategorized) {
        this.categories(x3);
      } else {
        $$.updateTargetX(data2.targets, x3);
        $$.redraw({
          withUpdateOrgXDomain: true,
          withUpdateXDomain: true
        });
      }
    }
    return isCategorized ? this.categories() : data2.xs;
  },
  /**
   * Get and set x values for the chart.
   * @function xs
   * @instance
   * @memberof Chart
   * @param {Array} xs If xs is given, specified target's x values will be updated. If no argument is given, current x values will be returned as an Object whose keys are the target ids.
   * @returns {object} xs
   * @example
   *  // Get current x values
   *  chart.xs();
   *
   *  // Update x values for all targets
   *  chart.xs({
   *    data1: [10, 20, 30, 40, ...],
   *    data2: [100, 200, 300, 400, ...]
   *  });
   */
  xs: function(xs) {
    var $$ = this.internal;
    if (isObject(xs)) {
      $$.updateTargetXs($$.data.targets, xs);
      $$.redraw({
        withUpdateOrgXDomain: true,
        withUpdateXDomain: true
      });
    }
    return $$.data.xs;
  }
};
var apiFlow = {
  /**
   * Flow data to the chart.<br><br>
   * By this API, you can append new data points to the chart.
   * @function flow
   * @instance
   * @memberof Chart
   * @param {object} args The object can consist with following members:<br>
   *
   *    | Key | Type | Description |
   *    | --- | --- | --- |
   *    | json | Object | Data as JSON format (@see [data․json](Options.html#.data%25E2%2580%25A4json)) |
   *    | rows | Array | Data in array as row format (@see [data․rows](Options.html#.data%25E2%2580%25A4json)) |
   *    | columns | Array | Data in array as column format (@see [data․columns](Options.html#.data%25E2%2580%25A4columns)) |
   *    | to | String | The lower x edge will move to that point. If not given, the lower x edge will move by the number of given data points |
   *    | length | Number | The lower x edge will move by the number of this argument |
   *    | duration | Number | The duration of the transition will be specified value. If not given, transition.duration will be used as default |
   *    | done | Function | The specified function will be called when flow ends |
   *
   * - **NOTE:**
   *   - If json, rows and columns given, the data will be loaded.
   *   - If data that has the same target id is given, the chart will be appended.
   *   - Otherwise, new target will be added. One of these is required when calling.
   *   - If json specified, keys is required as well as data.json.
   * 	 - If tab isn't visible(by evaluating `document.hidden`), will not be executed to prevent unnecessary work.
   * @example
   * // 2 data points will be apprended to the tail and popped from the head.
   * // After that, 4 data points will be appended and no data points will be poppoed.
   * chart.flow({
   *  columns: [
   *    ["x", "2018-01-11", "2018-01-21"],
   *    ["data1", 500, 200],
   *    ["data2", 100, 300],
   *    ["data3", 200, 120]
   *  ],
   *  to: "2013-01-11",
   *  done: function () {
   *    chart.flow({
   *      columns: [
   *        ["x", "2018-02-11", "2018-02-12", "2018-02-13", "2018-02-14"],
   *        ["data1", 200, 300, 100, 250],
   *        ["data2", 100, 90, 40, 120],
   *        ["data3", 100, 100, 300, 500]
   *      ],
   *      length: 2,
   *      duration: 1500
   *    });
   *  }
   * });
   */
  flow: function(args) {
    var $$ = this.internal;
    var data2;
    if (args.json || args.rows || args.columns) {
      $$.convertData(args, function(res) {
        data2 = res;
        _();
      });
    }
    function _() {
      var domain2;
      var length = 0;
      var tail = 0;
      var diff;
      var to;
      if ($$.state.redrawing || !data2 || !isTabVisible()) {
        return;
      }
      var notfoundIds = [];
      var orgDataCount = $$.getMaxDataCount();
      var targets = $$.convertDataToTargets(data2, true);
      var isTimeSeries = $$.axis.isTimeSeries();
      $$.data.targets.forEach(function(t) {
        var found = false;
        for (var i = 0; i < targets.length; i++) {
          if (t.id === targets[i].id) {
            found = true;
            if (t.values[t.values.length - 1]) {
              tail = t.values[t.values.length - 1].index + 1;
            }
            length = targets[i].values.length;
            for (var j = 0; j < length; j++) {
              targets[i].values[j].index = tail + j;
              if (!isTimeSeries) {
                targets[i].values[j].x = tail + j;
              }
            }
            t.values = t.values.concat(targets[i].values);
            targets.splice(i, 1);
            break;
          }
        }
        !found && notfoundIds.push(t.id);
      });
      $$.data.targets.forEach(function(t) {
        for (var i = 0; i < notfoundIds.length; i++) {
          if (t.id === notfoundIds[i]) {
            tail = t.values[t.values.length - 1].index + 1;
            for (var j = 0; j < length; j++) {
              t.values.push({
                id: t.id,
                index: tail + j,
                x: isTimeSeries ? $$.getOtherTargetX(tail + j) : tail + j,
                value: null
              });
            }
          }
        }
      });
      if ($$.data.targets.length) {
        targets.forEach(function(t) {
          var missing = [];
          for (var i = $$.data.targets[0].values[0].index; i < tail; i++) {
            missing.push({
              id: t.id,
              index: i,
              x: isTimeSeries ? $$.getOtherTargetX(i) : i,
              value: null
            });
          }
          t.values.forEach(function(v) {
            v.index += tail;
            if (!isTimeSeries) {
              v.x += tail;
            }
          });
          t.values = missing.concat(t.values);
        });
      }
      $$.data.targets = $$.data.targets.concat(targets);
      var baseTarget = $$.data.targets[0];
      var baseValue = baseTarget.values[0];
      if (isDefined(args.to)) {
        length = 0;
        to = isTimeSeries ? parseDate.call($$, args.to) : args.to;
        baseTarget.values.forEach(function(v) {
          v.x < to && length++;
        });
      } else if (isDefined(args.length)) {
        length = args.length;
      }
      if (!orgDataCount) {
        if (isTimeSeries) {
          diff = baseTarget.values.length > 1 ? baseTarget.values[baseTarget.values.length - 1].x - baseValue.x : baseValue.x - $$.getXDomain($$.data.targets)[0];
        } else {
          diff = 1;
        }
        domain2 = [baseValue.x - diff, baseValue.x];
      } else if (orgDataCount === 1 && isTimeSeries) {
        diff = (baseTarget.values[baseTarget.values.length - 1].x - baseValue.x) / 2;
        domain2 = [new Date(+baseValue.x - diff), new Date(+baseValue.x + diff)];
      }
      domain2 && $$.updateXDomain(null, true, true, false, domain2);
      $$.updateTargets($$.data.targets);
      $$.redraw({
        flow: {
          index: baseValue.index,
          length,
          duration: isValue(args.duration) ? args.duration : $$.config.transition_duration,
          done: args.done,
          orgDataCount
        },
        withLegend: true,
        withTransition: orgDataCount > 1,
        withTrimXDomain: false,
        withUpdateXAxis: true
      });
    }
  }
};
var AxisRendererHelper = (
  /** @class */
  function() {
    function AxisRendererHelper2(owner) {
      var scale2 = getScale();
      var config = owner.config, params = owner.params;
      this.owner = owner;
      this.config = config;
      this.scale = scale2;
      if (config.noTransition || !params.config.transition_duration) {
        config.withoutTransition = true;
      }
      config.range = this.scaleExtent((params.orgXScale || scale2).range());
    }
    AxisRendererHelper2.getSizeFor1Char = function(node) {
      var size2 = {
        w: 5.5,
        h: 11.5
      };
      !node.empty() && node.select("text").text("0").call(function(el) {
        try {
          var _a2 = el.node().getBBox(), width = _a2.width, height = _a2.height;
          if (width && height) {
            size2.w = width;
            size2.h = height;
          }
        } catch (e) {
        } finally {
          el.text("");
        }
      });
      this.getSizeFor1Char = function() {
        return size2;
      };
      return size2;
    };
    AxisRendererHelper2.prototype.getTickTransformSetter = function(id2) {
      var config = this.config;
      var fn = id2 === "x" ? function(value) {
        return "translate(".concat(value + config.tickOffset, ",0)");
      } : function(value) {
        return "translate(0,".concat(value, ")");
      };
      return function(selection3, scale2) {
        selection3.attr("transform", function(d) {
          return isValue(d) ? fn(Math.ceil(scale2(d))) : null;
        });
      };
    };
    AxisRendererHelper2.prototype.scaleExtent = function(domain2) {
      var start2 = domain2[0];
      var stop = domain2[domain2.length - 1];
      return start2 < stop ? [start2, stop] : [stop, start2];
    };
    AxisRendererHelper2.prototype.generateTicks = function(scale2, isYAxes) {
      var tickStepSize = this.owner.params.tickStepSize;
      var _a2 = scale2.domain(), start2 = _a2[0], end = _a2[1];
      var ticks2 = [];
      if (isYAxes && tickStepSize) {
        var interval2 = Math.round(start2);
        while (interval2 <= end) {
          ticks2.push(interval2);
          interval2 += tickStepSize;
        }
      } else if (scale2.ticks) {
        var tickArguments = this.config.tickArguments;
        if (scale2.type === "log" && !tickArguments) {
          var s2 = getScale("_log").domain([start2 > 0 ? start2 : 1, end]).range(scale2.range());
          ticks2 = s2.ticks();
          for (var cnt = end.toFixed().length; ticks2.length > 15; cnt--) {
            ticks2 = s2.ticks(cnt);
          }
          ticks2.splice(0, 1, start2);
          ticks2.splice(ticks2.length - 1, 1, end);
        } else {
          ticks2 = scale2.ticks.apply(scale2, this.config.tickArguments || []);
        }
        ticks2 = ticks2.map(function(v) {
          var r = isString(v) && isNumber(v) && !isNaN(v) && Math.round(v * 10) / 10 || v;
          return r;
        });
      }
      return ticks2;
    };
    AxisRendererHelper2.prototype.copyScale = function() {
      var newScale = this.scale.copy();
      if (!newScale.domain().length) {
        newScale.domain(this.scale.domain());
      }
      newScale.type = this.scale.type;
      return newScale;
    };
    AxisRendererHelper2.prototype.textFormatted = function(v) {
      var tickFormat2 = this.config.tickFormat;
      var value = /\d+\.\d+0{5,}\d$/.test(v) ? +String(v).replace(/0+\d$/, "") : v;
      var formatted = tickFormat2 ? tickFormat2(value) : value;
      return isDefined(formatted) ? formatted : "";
    };
    AxisRendererHelper2.prototype.transitionise = function(selection3) {
      var config = this.config;
      var transitionSelection = selection3;
      if (config.withoutTransition) {
        transitionSelection = selection3.interrupt();
      } else if (config.transition || !this.owner.params.noTransition) {
        try {
          transitionSelection = selection3.transition(config.transition);
        } catch (e) {
        }
      }
      return transitionSelection;
    };
    return AxisRendererHelper2;
  }()
);
var AxisRenderer = (
  /** @class */
  function() {
    function AxisRenderer2(params) {
      if (params === void 0) {
        params = {};
      }
      var config = {
        innerTickSize: 6,
        outerTickSize: params.outerTick ? 6 : 0,
        orient: "bottom",
        range: [],
        tickArguments: null,
        tickCentered: null,
        tickCulling: true,
        tickFormat: null,
        tickLength: 9,
        tickOffset: 0,
        tickPadding: 3,
        tickValues: null,
        transition: null,
        noTransition: params.noTransition
      };
      config.tickLength = Math.max(config.innerTickSize, 0) + config.tickPadding;
      this.config = config;
      this.params = params;
      this.helper = new AxisRendererHelper(this);
    }
    AxisRenderer2.prototype.create = function(g) {
      var ctx = this;
      var config = ctx.config, helper = ctx.helper, params = ctx.params;
      var scale2 = helper.scale;
      var orient = config.orient;
      var splitTickText = this.splitTickText.bind(ctx);
      var isLeftRight = /^(left|right)$/.test(orient);
      var isTopBottom = /^(top|bottom)$/.test(orient);
      var tickTransform = helper.getTickTransformSetter(isTopBottom ? "x" : "y");
      var axisPx = tickTransform === helper.axisX ? "y" : "x";
      var sign2 = /^(top|left)$/.test(orient) ? -1 : 1;
      var rotate = params.tickTextRotate;
      this.config.range = scale2.rangeExtent ? scale2.rangeExtent() : helper.scaleExtent((params.orgXScale || scale2).range());
      var innerTickSize = config.innerTickSize, tickLength = config.tickLength, range2 = config.range;
      var id2 = params.id;
      var tickTextPos = id2 && /^(x|y|y2)$/.test(id2) ? params.config["axis_".concat(id2, "_tick_text_position")] : { x: 0, y: 0 };
      var prefix = id2 === "subX" ? "subchart_axis_x" : "axis_".concat(id2);
      var axisShow = params.config["".concat(prefix, "_show")];
      var tickShow = {
        tick: axisShow ? params.config["".concat(prefix, "_tick_show")] : false,
        text: axisShow ? params.config["".concat(prefix, "_tick_text_show")] : false
      };
      var $g;
      g.each(function() {
        var g2 = select_default2(this);
        var scale0 = this.__chart__ || scale2;
        var scale1 = helper.copyScale();
        $g = g2;
        this.__chart__ = scale1;
        config.tickOffset = params.isCategory ? Math.ceil((scale1(1) - scale1(0)) / 2) : 0;
        var path2 = g2.selectAll(".domain").data([0]);
        path2.enter().append("path").attr("class", "domain").merge(path2).attr("d", function() {
          var outerTickSized = config.outerTickSize * sign2;
          return isTopBottom ? "M".concat(range2[0], ",").concat(outerTickSized, "V0H").concat(range2[1], "V").concat(outerTickSized) : "M".concat(outerTickSized, ",").concat(range2[0], "H0V").concat(range2[1], "H").concat(outerTickSized);
        });
        if (tickShow.tick || tickShow.text) {
          var ticks_1 = config.tickValues || helper.generateTicks(scale1, isLeftRight);
          ctx.generatedTicks = ticks_1;
          var tick = g2.selectAll(".tick").data(ticks_1, scale1);
          var tickEnter = tick.enter().insert("g", ".domain").attr("class", "tick");
          var tickExit = tick.exit().remove();
          tick = tickEnter.merge(tick);
          tickShow.tick && tickEnter.append("line");
          tickShow.text && tickEnter.append("text");
          var sizeFor1Char_1 = AxisRendererHelper.getSizeFor1Char(tick);
          var counts_1 = [];
          var tspan = tick.select("text").selectAll("tspan").data(function(d, index2) {
            var split = params.tickMultiline ? splitTickText(d, scale1, ticks_1, isLeftRight, sizeFor1Char_1.w) : isArray(helper.textFormatted(d)) ? helper.textFormatted(d).concat() : [helper.textFormatted(d)];
            counts_1[index2] = split.length;
            return split.map(function(splitted) {
              return { index: index2, splitted };
            });
          });
          tspan.exit().remove();
          tspan = tspan.enter().append("tspan").merge(tspan).text(function(d) {
            return d.splitted;
          });
          tspan.attr("x", isTopBottom ? 0 : tickLength * sign2).attr("dx", function() {
            var dx = 0;
            if (/(top|bottom)/.test(orient) && rotate) {
              dx = 8 * Math.sin(Math.PI * (rotate / 180)) * (orient === "top" ? -1 : 1);
            }
            return dx + (tickTextPos.x || 0);
          }()).attr("dy", function(d, i) {
            var defValue = ".71em";
            var dy = 0;
            if (orient !== "top") {
              dy = sizeFor1Char_1.h;
              if (i === 0) {
                dy = isLeftRight ? -((counts_1[d.index] - 1) * (sizeFor1Char_1.h / 2) - 3) : tickTextPos.y === 0 ? defValue : 0;
              }
            }
            return isNumber(dy) && tickTextPos.y ? dy + tickTextPos.y : dy || defValue;
          });
          var lineUpdate = tick.select("line");
          var textUpdate = tick.select("text");
          tickEnter.select("line").attr("".concat(axisPx, "2"), innerTickSize * sign2);
          tickEnter.select("text").attr(axisPx, tickLength * sign2);
          ctx.setTickLineTextPosition(lineUpdate, textUpdate);
          if (params.tickTitle) {
            var title2 = textUpdate.select("title");
            (title2.empty() ? textUpdate.append("title") : title2).text(function(index2) {
              return params.tickTitle[index2];
            });
          }
          if (scale1.bandwidth) {
            var x_1 = scale1;
            var dx_1 = x_1.bandwidth() / 2;
            scale0 = function(d) {
              return x_1(d) + dx_1;
            };
            scale1 = scale0;
          } else if (scale0.bandwidth) {
            scale0 = scale1;
          } else {
            tickTransform(tickExit, scale1);
          }
          tick = params.owner.state.flowing ? helper.transitionise(tick) : params.owner.$T(tick);
          tickTransform(tickEnter, scale0);
          tickTransform(tick.style("opacity", null), scale1);
        }
      });
      this.g = $g;
    };
    AxisRenderer2.prototype.getGeneratedTicks = function(count3) {
      var _a2;
      var len = ((_a2 = this.generatedTicks) === null || _a2 === void 0 ? void 0 : _a2.length) - 1;
      var res = this.generatedTicks;
      if (len > count3) {
        var interval_1 = Math.round(len / count3 + 0.1);
        res = this.generatedTicks.map(function(v, i) {
          return i % interval_1 === 0 ? v : null;
        }).filter(function(v) {
          return v !== null;
        }).splice(0, count3);
      }
      return res;
    };
    AxisRenderer2.prototype.getTickXY = function() {
      var config = this.config;
      var pos = { x: 0, y: 0 };
      if (this.params.isCategory) {
        pos.x = config.tickCentered ? 0 : config.tickOffset;
        pos.y = config.tickCentered ? config.tickOffset : 0;
      }
      return pos;
    };
    AxisRenderer2.prototype.getTickSize = function(d) {
      var scale2 = this.helper.scale;
      var config = this.config;
      var innerTickSize = config.innerTickSize, range2 = config.range;
      var tickPosition = scale2(d) + (config.tickCentered ? 0 : config.tickOffset);
      return range2[0] < tickPosition && tickPosition < range2[1] ? innerTickSize : 0;
    };
    AxisRenderer2.prototype.setTickLineTextPosition = function(lineUpdate, textUpdate) {
      var tickPos = this.getTickXY();
      var _a2 = this.config, innerTickSize = _a2.innerTickSize, orient = _a2.orient, tickLength = _a2.tickLength, tickOffset = _a2.tickOffset;
      var rotate = this.params.tickTextRotate;
      var textAnchorForText = function(r) {
        var value = ["start", "end"];
        orient === "top" && value.reverse();
        return !r ? "middle" : value[r > 0 ? 0 : 1];
      };
      var textTransform = function(r) {
        return r ? "rotate(".concat(r, ")") : null;
      };
      var yForText = function(r) {
        var r2 = r / (orient === "bottom" ? 15 : 23);
        return r ? 11.5 - 2.5 * r2 * (r > 0 ? 1 : -1) : tickLength;
      };
      var _b = this.params.owner.config, isRotated = _b.axis_rotated, inner = _b.axis_x_tick_text_inner;
      switch (orient) {
        case "bottom":
          lineUpdate.attr("x1", tickPos.x).attr("x2", tickPos.x).attr("y2", this.getTickSize.bind(this));
          textUpdate.attr("x", 0).attr("y", yForText(rotate)).style("text-anchor", textAnchorForText(rotate)).style("text-anchor", function(d, i, _a3) {
            var length = _a3.length;
            if (!isRotated && i === 0 && (inner === true || inner.first)) {
              return "start";
            } else if (!isRotated && i === length - 1 && (inner === true || inner.last)) {
              return "end";
            }
            return textAnchorForText(rotate);
          }).attr("transform", textTransform(rotate));
          break;
        case "top":
          lineUpdate.attr("x2", 0).attr("y2", -innerTickSize);
          textUpdate.attr("x", 0).attr("y", -yForText(rotate) * 2).style("text-anchor", textAnchorForText(rotate)).attr("transform", textTransform(rotate));
          break;
        case "left":
          lineUpdate.attr("x2", -innerTickSize).attr("y1", tickPos.y).attr("y2", tickPos.y);
          textUpdate.attr("x", -tickLength).attr("y", tickOffset).style("text-anchor", "end");
          break;
        case "right":
          lineUpdate.attr("x2", innerTickSize).attr("y2", 0);
          textUpdate.attr("x", tickLength).attr("y", 0).style("text-anchor", "start");
      }
    };
    AxisRenderer2.prototype.splitTickText = function(d, scale2, ticks2, isLeftRight, charWidth) {
      var params = this.params;
      var tickText = this.helper.textFormatted(d);
      var splitted = isString(tickText) && tickText.indexOf("\n") > -1 ? tickText.split("\n") : [];
      if (splitted.length) {
        return splitted;
      }
      if (isArray(tickText)) {
        return tickText;
      }
      var tickWidth = params.tickWidth;
      if (!tickWidth || tickWidth <= 0) {
        tickWidth = isLeftRight ? 95 : params.isCategory ? Math.ceil(params.isInverted ? scale2(ticks2[0]) - scale2(ticks2[1]) : scale2(ticks2[1]) - scale2(ticks2[0])) - 12 : 110;
      }
      function split(splitted2, text2) {
        var subtext;
        var spaceIndex;
        var textWidth;
        for (var i = 1; i < text2.length; i++) {
          if (text2.charAt(i) === " ") {
            spaceIndex = i;
          }
          subtext = text2.substr(0, i + 1);
          textWidth = charWidth * subtext.length;
          if (tickWidth < textWidth) {
            return split(splitted2.concat(text2.substr(0, spaceIndex || i)), text2.slice(spaceIndex ? spaceIndex + 1 : i));
          }
        }
        return splitted2.concat(text2);
      }
      return split(splitted, String(tickText));
    };
    AxisRenderer2.prototype.scale = function(x3) {
      if (!arguments.length) {
        return this.helper.scale;
      }
      this.helper.scale = x3;
      return this;
    };
    AxisRenderer2.prototype.orient = function(x3) {
      if (!arguments.length) {
        return this.config.orient;
      }
      this.config.orient = x3 in {
        top: 1,
        right: 1,
        bottom: 1,
        left: 1
      } ? String(x3) : "bottom";
      return this;
    };
    AxisRenderer2.prototype.tickFormat = function(format3) {
      var config = this.config;
      if (!arguments.length) {
        return config.tickFormat;
      }
      config.tickFormat = format3;
      return this;
    };
    AxisRenderer2.prototype.tickCentered = function(isCentered) {
      var config = this.config;
      if (!arguments.length) {
        return config.tickCentered;
      }
      config.tickCentered = isCentered;
      return this;
    };
    AxisRenderer2.prototype.tickOffset = function() {
      return this.config.tickOffset;
    };
    AxisRenderer2.prototype.tickInterval = function(size2) {
      var _a2;
      var _b = this.config, outerTickSize = _b.outerTickSize, tickOffset = _b.tickOffset, tickValues = _b.tickValues;
      var interval2;
      if (this.params.isCategory) {
        interval2 = tickOffset * 2;
      } else {
        var scale_1 = (_a2 = this.params.owner.scale.zoom) !== null && _a2 !== void 0 ? _a2 : this.helper.scale;
        var length_1 = this.g.select("path.domain").node().getTotalLength() - outerTickSize * 2;
        interval2 = length_1 / (size2 || this.g.selectAll("line").size());
        var intervalByValue = tickValues ? tickValues.map(function(v, i, arr) {
          var next = i + 1;
          return next < arr.length ? scale_1(arr[next]) - scale_1(v) : null;
        }).filter(Boolean) : [];
        interval2 = Math.min.apply(Math, __spreadArray(__spreadArray([], intervalByValue, false), [interval2], false));
      }
      return interval2 === Infinity ? 0 : interval2;
    };
    AxisRenderer2.prototype.ticks = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      var config = this.config;
      if (!args.length) {
        return config.tickArguments;
      }
      config.tickArguments = toArray(args);
      return this;
    };
    AxisRenderer2.prototype.tickCulling = function(culling) {
      var config = this.config;
      if (!arguments.length) {
        return config.tickCulling;
      }
      config.tickCulling = culling;
      return this;
    };
    AxisRenderer2.prototype.tickValues = function(x3) {
      var _this = this;
      var config = this.config;
      if (isFunction(x3)) {
        config.tickValues = function() {
          return x3(_this.helper.scale.domain());
        };
      } else {
        if (!arguments.length) {
          return config.tickValues;
        }
        config.tickValues = x3;
      }
      return this;
    };
    AxisRenderer2.prototype.setTransition = function(t) {
      this.config.transition = t;
      return this;
    };
    return AxisRenderer2;
  }()
);
var axis2 = {
  getAxisInstance: function() {
    return this.axis || new Axis(this);
  }
};
var Axis = (
  /** @class */
  function() {
    function Axis2(owner) {
      this.axesList = {};
      this.tick = {
        x: null,
        y: null,
        y2: null
      };
      this.xs = [];
      this.orient = {
        x: "bottom",
        y: "left",
        y2: "right",
        subX: "bottom"
      };
      this.owner = owner;
      this.setOrient();
    }
    Axis2.prototype.getAxisClassName = function(id2) {
      return "".concat($AXIS.axis, " ").concat($AXIS["axis".concat(capitalize(id2))]);
    };
    Axis2.prototype.isHorizontal = function($$, forHorizontal) {
      var isRotated = $$.config.axis_rotated;
      return forHorizontal ? isRotated : !isRotated;
    };
    Axis2.prototype.isCategorized = function() {
      var _a2 = this.owner, config = _a2.config, state = _a2.state;
      return config.axis_x_type.indexOf("category") >= 0 || state.hasRadar;
    };
    Axis2.prototype.isCustomX = function() {
      var config = this.owner.config;
      return !this.isTimeSeries() && (config.data_x || notEmpty(config.data_xs));
    };
    Axis2.prototype.isTimeSeries = function(id2) {
      if (id2 === void 0) {
        id2 = "x";
      }
      return this.owner.config["axis_".concat(id2, "_type")] === "timeseries";
    };
    Axis2.prototype.isLog = function(id2) {
      if (id2 === void 0) {
        id2 = "x";
      }
      return this.owner.config["axis_".concat(id2, "_type")] === "log";
    };
    Axis2.prototype.isTimeSeriesY = function() {
      return this.isTimeSeries("y");
    };
    Axis2.prototype.getAxisType = function(id2) {
      if (id2 === void 0) {
        id2 = "x";
      }
      var type2 = "linear";
      if (this.isTimeSeries(id2)) {
        type2 = this.owner.config.axis_x_localtime ? "time" : "utc";
      } else if (this.isLog(id2)) {
        type2 = "log";
      }
      return type2;
    };
    Axis2.prototype.init = function() {
      var _this = this;
      var $$ = this.owner;
      var config = $$.config, _a2 = $$.$el, main2 = _a2.main, axis3 = _a2.axis, clip2 = $$.state.clip;
      var isRotated = config.axis_rotated;
      var target = ["x", "y"];
      config.axis_y2_show && target.push("y2");
      target.forEach(function(v) {
        var classAxis = _this.getAxisClassName(v);
        var classLabel = $AXIS["axis".concat(v.toUpperCase(), "Label")];
        axis3[v] = main2.append("g").attr("class", classAxis).attr("clip-path", function() {
          var res = null;
          if (v === "x") {
            res = clip2.pathXAxis;
          } else if (v === "y") {
            res = clip2.pathYAxis;
          }
          return res;
        }).attr("transform", $$.getTranslate(v)).style("visibility", config["axis_".concat(v, "_show")] ? null : "hidden");
        axis3[v].append("text").attr("class", classLabel).attr("transform", ["rotate(-90)", null][v === "x" ? +!isRotated : +isRotated]).style("text-anchor", function() {
          return _this.textAnchorForAxisLabel(v);
        });
        _this.generateAxes(v);
      });
    };
    Axis2.prototype.setOrient = function() {
      var $$ = this.owner;
      var _a2 = $$.config, isRotated = _a2.axis_rotated, yInner = _a2.axis_y_inner, y2Inner = _a2.axis_y2_inner;
      this.orient = {
        x: isRotated ? "left" : "bottom",
        y: isRotated ? yInner ? "top" : "bottom" : yInner ? "right" : "left",
        y2: isRotated ? y2Inner ? "bottom" : "top" : y2Inner ? "left" : "right",
        subX: isRotated ? "left" : "bottom"
      };
    };
    Axis2.prototype.generateAxes = function(id2) {
      var $$ = this.owner;
      var config = $$.config;
      var axes = [];
      var axesConfig = config["axis_".concat(id2, "_axes")];
      var isRotated = config.axis_rotated;
      var d3Axis;
      if (id2 === "x") {
        d3Axis = isRotated ? axisLeft : axisBottom;
      } else if (id2 === "y") {
        d3Axis = isRotated ? axisBottom : axisLeft;
      } else if (id2 === "y2") {
        d3Axis = isRotated ? axisTop : axisRight;
      }
      if (axesConfig.length) {
        axesConfig.forEach(function(v) {
          var tick = v.tick || {};
          var scale2 = $$.scale[id2].copy();
          v.domain && scale2.domain(v.domain);
          axes.push(d3Axis(scale2).ticks(tick.count).tickFormat(isFunction(tick.format) ? tick.format.bind($$.api) : function(x3) {
            return x3;
          }).tickValues(tick.values).tickSizeOuter(tick.outer === false ? 0 : 6));
        });
      }
      this.axesList[id2] = axes;
    };
    Axis2.prototype.updateAxes = function() {
      var _this = this;
      var $$ = this.owner;
      var config = $$.config, main2 = $$.$el.main, $T = $$.$T;
      Object.keys(this.axesList).forEach(function(id2) {
        var axesConfig = config["axis_".concat(id2, "_axes")];
        var scale2 = $$.scale[id2].copy();
        var range2 = scale2.range();
        _this.axesList[id2].forEach(function(v, i) {
          var axisRange = v.scale().range();
          if (!range2.every(function(v2, i2) {
            return v2 === axisRange[i2];
          })) {
            v.scale().range(range2);
          }
          var className = "".concat(_this.getAxisClassName(id2), "-").concat(i + 1);
          var g = main2.select(".".concat(className.replace(/\s/, ".")));
          if (g.empty()) {
            g = main2.append("g").attr("class", className).style("visibility", config["axis_".concat(id2, "_show")] ? null : "hidden").call(v);
          } else {
            axesConfig[i].domain && scale2.domain(axesConfig[i].domain);
            $T(g).call(v.scale(scale2));
          }
          g.attr("transform", $$.getTranslate(id2, i + 1));
        });
      });
    };
    Axis2.prototype.setAxis = function(id2, scale2, outerTick, noTransition) {
      var $$ = this.owner;
      if (id2 !== "subX") {
        this.tick[id2] = this.getTickValues(id2);
      }
      this[id2] = this.getAxis(
        id2,
        scale2,
        outerTick,
        // do not transit x Axis on zoom and resizing
        // https://github.com/naver/billboard.js/issues/1949
        id2 === "x" && ($$.scale.zoom || $$.config.subchart_show || $$.state.resizing) ? true : noTransition
      );
    };
    Axis2.prototype.getAxis = function(id2, scale2, outerTick, noTransition, noTickTextRotate) {
      var $$ = this.owner;
      var config = $$.config;
      var isX = /^(x|subX)$/.test(id2);
      var type2 = isX ? "x" : id2;
      var isCategory = isX && this.isCategorized();
      var orient = this.orient[id2];
      var tickTextRotate = noTickTextRotate ? 0 : $$.getAxisTickRotate(type2);
      var tickFormat2;
      if (isX) {
        tickFormat2 = id2 === "subX" ? $$.format.subXAxisTick : $$.format.xAxisTick;
      } else {
        var fn = config["axis_".concat(id2, "_tick_format")];
        if (isFunction(fn)) {
          tickFormat2 = fn.bind($$.api);
        }
      }
      var tickValues = this.tick[type2];
      var axisParams = mergeObj({
        outerTick,
        noTransition,
        config,
        id: id2,
        tickTextRotate,
        owner: $$
      }, isX && {
        isCategory,
        isInverted: config.axis_x_inverted,
        tickMultiline: config.axis_x_tick_multiline,
        tickWidth: config.axis_x_tick_width,
        tickTitle: isCategory && config.axis_x_tick_tooltip && $$.api.categories(),
        orgXScale: $$.scale.x
      });
      if (!isX) {
        axisParams.tickStepSize = config["axis_".concat(type2, "_tick_stepSize")];
      }
      var axis3 = new AxisRenderer(axisParams).scale(isX && $$.scale.zoom || scale2).orient(orient);
      if (isX && this.isTimeSeries() && tickValues && !isFunction(tickValues)) {
        var fn_1 = parseDate.bind($$);
        tickValues = tickValues.map(function(v) {
          return fn_1(v);
        });
      } else if (!isX && this.isTimeSeriesY()) {
        axis3.ticks(config.axis_y_tick_time_value);
        tickValues = null;
      }
      tickValues && axis3.tickValues(tickValues);
      axis3.tickFormat(tickFormat2 || !isX && ($$.isStackNormalized() && function(x3) {
        return "".concat(x3, "%");
      }));
      if (isCategory) {
        axis3.tickCentered(config.axis_x_tick_centered);
        if (isEmpty(config.axis_x_tick_culling)) {
          config.axis_x_tick_culling = false;
        }
      }
      var tickCount = config["axis_".concat(type2, "_tick_count")];
      tickCount && axis3.ticks(tickCount);
      return axis3;
    };
    Axis2.prototype.updateXAxisTickValues = function(targets, axis3) {
      var _a2;
      var $$ = this.owner;
      var config = $$.config;
      var fit = config.axis_x_tick_fit;
      var count3 = config.axis_x_tick_count;
      var values;
      if (fit || count3 && fit) {
        values = $$.mapTargetsToUniqueXs(targets);
        if (this.isCategorized() && count3 > values.length) {
          count3 = values.length;
        }
        values = this.generateTickValues(values, count3, this.isTimeSeries());
      }
      if (axis3) {
        axis3.tickValues(values);
      } else if (this.x) {
        this.x.tickValues(values);
        (_a2 = this.subX) === null || _a2 === void 0 ? void 0 : _a2.tickValues(values);
      }
      return values;
    };
    Axis2.prototype.getId = function(id2) {
      var _a2 = this.owner, config = _a2.config, scale2 = _a2.scale;
      var axis3 = config.data_axes[id2];
      if (!axis3 || !scale2[axis3]) {
        axis3 = "y";
      }
      return axis3;
    };
    Axis2.prototype.getXAxisTickFormat = function(forSubchart) {
      var $$ = this.owner;
      var config = $$.config, format3 = $$.format;
      var tickFormat2 = forSubchart ? config.subchart_axis_x_tick_format || config.axis_x_tick_format : config.axis_x_tick_format;
      var isTimeSeries = this.isTimeSeries();
      var isCategorized = this.isCategorized();
      var currFormat;
      if (tickFormat2) {
        if (isFunction(tickFormat2)) {
          currFormat = tickFormat2.bind($$.api);
        } else if (isTimeSeries) {
          currFormat = function(date2) {
            return date2 ? format3.axisTime(tickFormat2)(date2) : "";
          };
        }
      } else {
        currFormat = isTimeSeries ? format3.defaultAxisTime : isCategorized ? $$.categoryName : function(v) {
          return v < 0 ? v.toFixed(0) : v;
        };
      }
      return isFunction(currFormat) ? function(v) {
        return currFormat.apply($$, isCategorized ? [v, $$.categoryName(v)] : [v]);
      } : currFormat;
    };
    Axis2.prototype.getTickValues = function(id2) {
      var $$ = this.owner;
      var tickValues = $$.config["axis_".concat(id2, "_tick_values")];
      var axis3 = $$["".concat(id2, "Axis")];
      return (isFunction(tickValues) ? tickValues.call($$.api) : tickValues) || (axis3 ? axis3.tickValues() : void 0);
    };
    Axis2.prototype.getLabelOptionByAxisId = function(id2) {
      return this.owner.config["axis_".concat(id2, "_label")];
    };
    Axis2.prototype.getLabelText = function(id2) {
      var option = this.getLabelOptionByAxisId(id2);
      return isString(option) ? option : option ? option.text : null;
    };
    Axis2.prototype.setLabelText = function(id2, text2) {
      var $$ = this.owner;
      var config = $$.config;
      var option = this.getLabelOptionByAxisId(id2);
      if (isString(option)) {
        config["axis_".concat(id2, "_label")] = text2;
      } else if (option) {
        option.text = text2;
      }
    };
    Axis2.prototype.getLabelPosition = function(id2, defaultPosition) {
      var isRotated = this.owner.config.axis_rotated;
      var option = this.getLabelOptionByAxisId(id2);
      var position2 = isObjectType(option) && option.position ? option.position : defaultPosition[+!isRotated];
      var has = function(v) {
        return !!~position2.indexOf(v);
      };
      return {
        isInner: has("inner"),
        isOuter: has("outer"),
        isLeft: has("left"),
        isCenter: has("center"),
        isRight: has("right"),
        isTop: has("top"),
        isMiddle: has("middle"),
        isBottom: has("bottom")
      };
    };
    Axis2.prototype.getAxisLabelPosition = function(id2) {
      return this.getLabelPosition(id2, id2 === "x" ? ["inner-top", "inner-right"] : ["inner-right", "inner-top"]);
    };
    Axis2.prototype.getLabelPositionById = function(id2) {
      return this.getAxisLabelPosition(id2);
    };
    Axis2.prototype.xForAxisLabel = function(id2) {
      var $$ = this.owner;
      var _a2 = $$.state, width = _a2.width, height = _a2.height;
      var position2 = this.getAxisLabelPosition(id2);
      var x3 = position2.isMiddle ? -height / 2 : 0;
      if (this.isHorizontal($$, id2 !== "x")) {
        x3 = position2.isLeft ? 0 : position2.isCenter ? width / 2 : width;
      } else if (position2.isBottom) {
        x3 = -height;
      }
      return x3;
    };
    Axis2.prototype.dxForAxisLabel = function(id2) {
      var $$ = this.owner;
      var position2 = this.getAxisLabelPosition(id2);
      var dx = position2.isBottom ? "0.5em" : "0";
      if (this.isHorizontal($$, id2 !== "x")) {
        dx = position2.isLeft ? "0.5em" : position2.isRight ? "-0.5em" : "0";
      } else if (position2.isTop) {
        dx = "-0.5em";
      }
      return dx;
    };
    Axis2.prototype.textAnchorForAxisLabel = function(id2) {
      var $$ = this.owner;
      var position2 = this.getAxisLabelPosition(id2);
      var anchor = position2.isMiddle ? "middle" : "end";
      if (this.isHorizontal($$, id2 !== "x")) {
        anchor = position2.isLeft ? "start" : position2.isCenter ? "middle" : "end";
      } else if (position2.isBottom) {
        anchor = "start";
      }
      return anchor;
    };
    Axis2.prototype.dyForAxisLabel = function(id2) {
      var $$ = this.owner;
      var config = $$.config;
      var isRotated = config.axis_rotated;
      var isInner = this.getAxisLabelPosition(id2).isInner;
      var tickRotate = config["axis_".concat(id2, "_tick_rotate")] ? $$.getHorizontalAxisHeight(id2) : 0;
      var maxTickWidth = this.getMaxTickSize(id2).width;
      var dy;
      if (id2 === "x") {
        var xHeight = config.axis_x_height;
        if (isRotated) {
          dy = isInner ? "1.2em" : -25 - maxTickWidth;
        } else if (isInner) {
          dy = "-0.5em";
        } else if (xHeight) {
          dy = xHeight - 10;
        } else if (tickRotate) {
          dy = tickRotate - 10;
        } else {
          dy = "3em";
        }
      } else {
        dy = {
          y: ["-0.5em", 10, "3em", "1.2em", 10],
          y2: ["1.2em", -20, "-2.2em", "-0.5em", 15]
        }[id2];
        if (isRotated) {
          if (isInner) {
            dy = dy[0];
          } else if (tickRotate) {
            dy = tickRotate * (id2 === "y2" ? -1 : 1) - dy[1];
          } else {
            dy = dy[2];
          }
        } else {
          dy = isInner ? dy[3] : (dy[4] + (config["axis_".concat(id2, "_inner")] ? 0 : maxTickWidth + dy[4])) * (id2 === "y" ? -1 : 1);
        }
      }
      return dy;
    };
    Axis2.prototype.getMaxTickSize = function(id2, withoutRecompute) {
      var $$ = this.owner;
      var config = $$.config, current = $$.state.current, _a2 = $$.$el, svg = _a2.svg, chart = _a2.chart;
      var currentTickMax = current.maxTickSize[id2];
      var configPrefix = "axis_".concat(id2);
      var max4 = {
        width: 0,
        height: 0
      };
      if (withoutRecompute || !config["".concat(configPrefix, "_show")] || currentTickMax.width > 0 && $$.filterTargetsToShow().length === 0) {
        return currentTickMax;
      }
      if (svg) {
        var isYAxis_1 = /^y2?$/.test(id2);
        var targetsToShow = $$.filterTargetsToShow($$.data.targets);
        var scale2 = $$.scale[id2].copy().domain($$["get".concat(isYAxis_1 ? "Y" : "X", "Domain")](targetsToShow, id2));
        var domain2 = scale2.domain();
        var isDomainSame = domain2[0] === domain2[1] && domain2.every(function(v) {
          return v > 0;
        });
        var isCurrentMaxTickDomainSame = isArray(currentTickMax.domain) && currentTickMax.domain[0] === currentTickMax.domain[1] && currentTickMax.domain.every(function(v) {
          return v > 0;
        });
        if (isDomainSame || isCurrentMaxTickDomainSame) {
          return currentTickMax.size;
        } else {
          currentTickMax.domain = domain2;
        }
        if (!isYAxis_1) {
          currentTickMax.ticks.splice(0);
        }
        var axis3 = this.getAxis(id2, scale2, false, false, true);
        var tickRotate = config["".concat(configPrefix, "_tick_rotate")];
        var tickCount = config["".concat(configPrefix, "_tick_count")];
        var tickValues = config["".concat(configPrefix, "_tick_values")];
        if (!tickValues && tickCount) {
          axis3.tickValues(this.generateTickValues(domain2, tickCount, isYAxis_1 ? this.isTimeSeriesY() : this.isTimeSeries()));
        }
        !isYAxis_1 && this.updateXAxisTickValues(targetsToShow, axis3);
        var dummy = chart.append("svg").style("visibility", "hidden").style("position", "fixed").style("top", "0").style("left", "0");
        axis3.create(dummy);
        dummy.selectAll("text").attr("transform", isNumber(tickRotate) ? "rotate(".concat(tickRotate, ")") : null).each(function(d, i) {
          var _a3 = this.getBoundingClientRect(), width = _a3.width, height = _a3.height;
          max4.width = Math.max(max4.width, width);
          max4.height = Math.max(max4.height, height);
          if (!isYAxis_1) {
            currentTickMax.ticks[i] = width;
          }
        });
        dummy.remove();
      }
      Object.keys(max4).forEach(function(key) {
        if (max4[key] > 0) {
          currentTickMax[key] = max4[key];
        }
      });
      return currentTickMax;
    };
    Axis2.prototype.getXAxisTickTextY2Overflow = function(defaultPadding) {
      var $$ = this.owner;
      var axis3 = $$.axis, config = $$.config, _a2 = $$.state, current = _a2.current, isLegendRight = _a2.isLegendRight, legendItemWidth = _a2.legendItemWidth;
      var xAxisTickRotate = $$.getAxisTickRotate("x");
      var positiveRotation = xAxisTickRotate > 0 && xAxisTickRotate < 90;
      if ((axis3.isCategorized() || axis3.isTimeSeries()) && config.axis_x_tick_fit && (!config.axis_x_tick_culling || isEmpty(config.axis_x_tick_culling)) && !config.axis_x_tick_multiline && positiveRotation) {
        var y2AxisWidth = config.axis_y2_show && current.maxTickSize.y2.width || 0;
        var legendWidth = isLegendRight && legendItemWidth || 0;
        var widthWithoutCurrentPaddingLeft = current.width - $$.getCurrentPaddingByDirection("left");
        var maxOverflow = this.getXAxisTickMaxOverflow(xAxisTickRotate, widthWithoutCurrentPaddingLeft - defaultPadding) - y2AxisWidth - legendWidth;
        var xAxisTickTextY2Overflow = Math.max(0, maxOverflow) + defaultPadding;
        return Math.min(xAxisTickTextY2Overflow, widthWithoutCurrentPaddingLeft / 2);
      }
      return 0;
    };
    Axis2.prototype.getXAxisTickMaxOverflow = function(xAxisTickRotate, widthWithoutCurrentPaddingLeft) {
      var $$ = this.owner;
      var axis3 = $$.axis, config = $$.config, state = $$.state;
      var isTimeSeries = axis3.isTimeSeries();
      var tickTextWidths = state.current.maxTickSize.x.ticks;
      var tickCount = tickTextWidths.length;
      var _a2 = state.axis.x.padding, left2 = _a2.left, right2 = _a2.right;
      var maxOverflow = 0;
      var remaining = tickCount - (isTimeSeries && config.axis_x_tick_fit ? 0.5 : 0);
      for (var i = 0; i < tickCount; i++) {
        var tickIndex = i + 1;
        var rotatedTickTextWidth = Math.cos(Math.PI * xAxisTickRotate / 180) * tickTextWidths[i];
        var ticksBeforeTickText = tickIndex - (isTimeSeries ? 1 : 0.5) + left2;
        if (ticksBeforeTickText <= 0) {
          continue;
        }
        var xAxisLengthWithoutTickTextWidth = widthWithoutCurrentPaddingLeft - rotatedTickTextWidth;
        var tickLength = xAxisLengthWithoutTickTextWidth / ticksBeforeTickText;
        var remainingTicks = remaining - tickIndex;
        var paddingRightLength = right2 * tickLength;
        var remainingTickWidth = remainingTicks * tickLength + paddingRightLength;
        var overflow = rotatedTickTextWidth - tickLength / 2 - remainingTickWidth;
        maxOverflow = Math.max(maxOverflow, overflow);
      }
      var filteredTargets = $$.filterTargetsToShow($$.data.targets);
      var tickOffset = 0;
      if (!isTimeSeries && config.axis_x_tick_count <= filteredTargets.length && filteredTargets[0].values.length) {
        var scale2 = getScale($$.axis.getAxisType("x"), 0, widthWithoutCurrentPaddingLeft - maxOverflow).domain([
          left2 * -1,
          $$.getXDomainMax($$.data.targets) + 1 + right2
        ]);
        tickOffset = Math.ceil((scale2(1) - scale2(0)) / 2);
      }
      return maxOverflow + tickOffset;
    };
    Axis2.prototype.updateLabels = function(withTransition) {
      var _this = this;
      var $$ = this.owner;
      var main2 = $$.$el.main, $T = $$.$T;
      var labels = {
        x: main2.select(".".concat($AXIS.axisX, " .").concat($AXIS.axisXLabel)),
        y: main2.select(".".concat($AXIS.axisY, " .").concat($AXIS.axisYLabel)),
        y2: main2.select(".".concat($AXIS.axisY2, " .").concat($AXIS.axisY2Label))
      };
      Object.keys(labels).filter(function(id2) {
        return !labels[id2].empty();
      }).forEach(function(v) {
        var node = labels[v];
        $T(node, withTransition).attr("x", function() {
          return _this.xForAxisLabel(v);
        }).attr("dx", function() {
          return _this.dxForAxisLabel(v);
        }).attr("dy", function() {
          return _this.dyForAxisLabel(v);
        }).text(function() {
          return _this.getLabelText(v);
        });
      });
    };
    Axis2.prototype.getPadding = function(padding, key, defaultValue, domainLength) {
      var p = isNumber(padding) ? padding : padding[key];
      if (!isValue(p)) {
        return defaultValue;
      }
      return this.owner.convertPixelToScale(/(bottom|top)/.test(key) ? "y" : "x", p, domainLength);
    };
    Axis2.prototype.generateTickValues = function(values, tickCount, forTimeSeries) {
      var tickValues = values;
      if (tickCount) {
        var targetCount = isFunction(tickCount) ? tickCount() : tickCount;
        if (targetCount === 1) {
          tickValues = [values[0]];
        } else if (targetCount === 2) {
          tickValues = [values[0], values[values.length - 1]];
        } else if (targetCount > 2) {
          var isCategorized = this.isCategorized();
          var count3 = targetCount - 2;
          var start2 = values[0];
          var end = values[values.length - 1];
          var interval2 = (end - start2) / (count3 + 1);
          var tickValue = void 0;
          tickValues = [start2];
          for (var i = 0; i < count3; i++) {
            tickValue = +start2 + interval2 * (i + 1);
            tickValues.push(forTimeSeries ? new Date(tickValue) : isCategorized ? Math.round(tickValue) : tickValue);
          }
          tickValues.push(end);
        }
      }
      if (!forTimeSeries) {
        tickValues = tickValues.sort(function(a2, b) {
          return a2 - b;
        });
      }
      return tickValues;
    };
    Axis2.prototype.generateTransitions = function(withTransition) {
      var $$ = this.owner;
      var axis3 = $$.$el.axis, $T = $$.$T;
      var _a2 = ["x", "y", "y2", "subX"].map(function(v) {
        return $T(axis3[v], withTransition);
      }), axisX = _a2[0], axisY = _a2[1], axisY2 = _a2[2], axisSubX = _a2[3];
      return { axisX, axisY, axisY2, axisSubX };
    };
    Axis2.prototype.redraw = function(transitions, isHidden, isInit) {
      var _this = this;
      var $$ = this.owner;
      var config = $$.config, $el = $$.$el;
      var opacity = isHidden ? "0" : null;
      ["x", "y", "y2", "subX"].forEach(function(id2) {
        var axis3 = _this[id2];
        var $axis = $el.axis[id2];
        if (axis3 && $axis) {
          if (!isInit && !config.transition_duration) {
            axis3.config.withoutTransition = true;
          }
          $axis.style("opacity", opacity);
          axis3.create(transitions["axis".concat(capitalize(id2))]);
        }
      });
      this.updateAxes();
    };
    Axis2.prototype.redrawAxis = function(targetsToShow, wth, transitions, flow2, isInit) {
      var _this = this;
      var _a2, _b, _c;
      var $$ = this.owner;
      var config = $$.config, scale2 = $$.scale, $el = $$.$el;
      var hasZoom = !!scale2.zoom;
      var xDomainForZoom;
      if (!hasZoom && this.isCategorized() && targetsToShow.length === 0) {
        scale2.x.domain([0, $el.axis.x.selectAll(".tick").size()]);
      }
      if (scale2.x && targetsToShow.length) {
        !hasZoom && $$.updateXDomain(targetsToShow, wth.UpdateXDomain, wth.UpdateOrgXDomain, wth.TrimXDomain);
        if (!config.axis_x_tick_values) {
          this.updateXAxisTickValues(targetsToShow);
        }
      } else if (this.x) {
        this.x.tickValues([]);
        (_a2 = this.subX) === null || _a2 === void 0 ? void 0 : _a2.tickValues([]);
      }
      if (config.zoom_rescale && !flow2) {
        xDomainForZoom = scale2.x.orgDomain();
      }
      ["y", "y2"].forEach(function(key) {
        var prefix = "axis_".concat(key, "_");
        var axisScale = scale2[key];
        if (axisScale) {
          var tickValues = config["".concat(prefix, "tick_values")];
          var tickCount = config["".concat(prefix, "tick_count")];
          axisScale.domain($$.getYDomain(targetsToShow, key, xDomainForZoom));
          if (!tickValues && tickCount) {
            var axis3 = $$.axis[key];
            var domain2 = axisScale.domain();
            axis3.tickValues(_this.generateTickValues(domain2, domain2.every(function(v) {
              return v === 0;
            }) ? 1 : tickCount, _this.isTimeSeriesY()));
          }
        }
      });
      this.redraw(transitions, $$.hasArcType(), isInit);
      this.updateLabels(wth.Transition);
      if ((wth.UpdateXDomain || wth.UpdateXAxis || wth.Y) && targetsToShow.length) {
        this.setCulling();
      }
      if (wth.Y) {
        (_b = scale2.subY) === null || _b === void 0 ? void 0 : _b.domain($$.getYDomain(targetsToShow, "y"));
        (_c = scale2.subY2) === null || _c === void 0 ? void 0 : _c.domain($$.getYDomain(targetsToShow, "y2"));
      }
    };
    Axis2.prototype.setCulling = function() {
      var $$ = this.owner;
      var config = $$.config, _a2 = $$.state, clip2 = _a2.clip, current = _a2.current, $el = $$.$el;
      ["subX", "x", "y", "y2"].forEach(function(type2) {
        var axis3 = $el.axis[type2];
        var id2 = type2 === "subX" ? "x" : type2;
        var cullingOptionPrefix = "axis_".concat(id2, "_tick_culling");
        var toCull = config[cullingOptionPrefix];
        if (axis3 && toCull) {
          var tickNodes = axis3.selectAll(".tick");
          var tickValues_1 = sortValue(tickNodes.data());
          var tickSize = tickValues_1.length;
          var cullingMax = config["".concat(cullingOptionPrefix, "_max")];
          var lines_1 = config["".concat(cullingOptionPrefix, "_lines")];
          var intervalForCulling_1;
          if (tickSize) {
            for (var i = 1; i < tickSize; i++) {
              if (tickSize / i < cullingMax) {
                intervalForCulling_1 = i;
                break;
              }
            }
            tickNodes.each(function(d) {
              var node = lines_1 ? this.querySelector("text") : this;
              if (node) {
                node.style.display = tickValues_1.indexOf(d) % intervalForCulling_1 ? "none" : null;
              }
            });
          } else {
            tickNodes.style("display", null);
          }
          if (type2 === "x") {
            var clipPath = current.maxTickSize.x.clipPath ? clip2.pathXAxisTickTexts : null;
            $el.svg.selectAll(".".concat($AXIS.axisX, " .tick text")).attr("clip-path", clipPath);
          }
        }
      });
    };
    return Axis2;
  }()
);
var eventrect = {
  /**
   * Initialize the area that detects the event.
   * Add a container for the zone that detects the event.
   * @private
   */
  initEventRect: function() {
    var $$ = this;
    $$.$el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $EVENT.eventRects).style("fill-opacity", "0");
  },
  /**
   * Redraws the area that detects the event.
   * @private
   */
  redrawEventRect: function() {
    var _a2;
    var $$ = this;
    var config = $$.config, state = $$.state, $el = $$.$el;
    var isMultipleX = $$.isMultipleX();
    var isInverted = config.axis_x_inverted;
    if ($el.eventRect) {
      $$.updateEventRect($el.eventRect, true);
    } else if ($$.data.targets.length) {
      var eventRects = $$.$el.main.select(".".concat($EVENT.eventRects)).style("cursor", config.zoom_enabled && config.zoom_type !== "drag" ? config.axis_rotated ? "ns-resize" : "ew-resize" : null).classed($EVENT.eventRectsMultiple, isMultipleX).classed($EVENT.eventRectsSingle, !isMultipleX);
      var eventRectUpdate = eventRects.selectAll(".".concat($EVENT.eventRect)).data([0]).enter().append("rect");
      $$.updateEventRect(eventRectUpdate);
      isMultipleX ? $$.generateEventRectsForMultipleXs(eventRectUpdate) : $$.generateEventRectsForSingleX(eventRectUpdate);
      eventRectUpdate.call($$.getDraggableSelection());
      $el.eventRect = eventRectUpdate;
      if ($$.state.inputType === "touch" && !$el.svg.on("touchstart.eventRect") && !$$.hasArcType()) {
        $$.bindTouchOnEventRect();
      }
      state.rendered && $$.updateEventRect($el.eventRect, true);
    }
    if (!isMultipleX) {
      var xAxisTickValues = $$.getMaxDataCountTarget();
      if (!config.data_xSort || isInverted) {
        xAxisTickValues.sort(function(a2, b) {
          return isInverted ? b.x - a2.x : a2.x - b.x;
        });
      }
      $$.updateDataIndexByX(xAxisTickValues);
      $$.updateXs(xAxisTickValues);
      (_a2 = $$.updatePointClass) === null || _a2 === void 0 ? void 0 : _a2.call($$, true);
      state.eventReceiver.data = xAxisTickValues;
    }
    $$.updateEventRectData();
  },
  bindTouchOnEventRect: function() {
    var $$ = this;
    var config = $$.config, state = $$.state, _a2 = $$.$el, eventRect = _a2.eventRect, svg = _a2.svg;
    var selectRect = function(context) {
      if ($$.isMultipleX()) {
        $$.selectRectForMultipleXs(context);
      } else {
        var index2 = $$.getDataIndexFromEvent(state.event);
        $$.callOverOutForTouch(index2);
        index2 === -1 ? $$.unselectRect() : $$.selectRectForSingle(context, index2);
      }
    };
    var unselectRect = function() {
      $$.unselectRect();
      $$.callOverOutForTouch();
    };
    var preventDefault = config.interaction_inputType_touch.preventDefault;
    var isPrevented = isboolean(preventDefault) && preventDefault || false;
    var preventThreshold = !isNaN(preventDefault) && preventDefault || null;
    var startPx;
    var preventEvent = function(event) {
      var eventType = event.type;
      var touch = event.changedTouches[0];
      var currentXY = touch["client".concat(config.axis_rotated ? "Y" : "X")];
      if (eventType === "touchstart") {
        if (isPrevented) {
          event.preventDefault();
        } else if (preventThreshold !== null) {
          startPx = currentXY;
        }
      } else if (eventType === "touchmove") {
        if (isPrevented || startPx === true || preventThreshold !== null && Math.abs(startPx - currentXY) >= preventThreshold) {
          startPx = true;
          event.preventDefault();
        }
      }
    };
    eventRect.on("touchstart", function(event) {
      state.event = event;
      $$.updateEventRect();
    }).on("touchstart.eventRect touchmove.eventRect", function(event) {
      state.event = event;
      if (!eventRect.empty() && eventRect.classed($EVENT.eventRect)) {
        if (state.dragging || state.flowing || $$.hasArcType() || event.touches.length > 1) {
          return;
        }
        preventEvent(event);
        selectRect(eventRect.node());
      } else {
        unselectRect();
      }
    }, true).on("touchend.eventRect", function(event) {
      state.event = event;
      if (!eventRect.empty() && eventRect.classed($EVENT.eventRect)) {
        if ($$.hasArcType() || !$$.toggleShape || state.cancelClick) {
          state.cancelClick && (state.cancelClick = false);
        }
      }
    }, true);
    svg.on("touchstart", function(event) {
      state.event = event;
      var target = event.target;
      if (target && target !== eventRect.node()) {
        unselectRect();
      }
    });
  },
  /**
   * Update event rect size
   * @param {d3Selection} eventRect Event <rect> element
   * @param {boolean} force Force to update
   * @private
   */
  updateEventRect: function(eventRect, force) {
    if (force === void 0) {
      force = false;
    }
    var $$ = this;
    var state = $$.state, $el = $$.$el;
    var eventReceiver = state.eventReceiver, width = state.width, height = state.height, rendered = state.rendered, resizing = state.resizing;
    var rectElement = eventRect || $el.eventRect;
    var updateClientRect = function() {
      if (eventReceiver) {
        var scrollPos = getScrollPosition($el.chart.node());
        eventReceiver.rect = rectElement.node().getBoundingClientRect().toJSON();
        eventReceiver.rect.top += scrollPos.y;
        eventReceiver.rect.left += scrollPos.x;
      }
    };
    if (!rendered || resizing || force) {
      rectElement.attr("x", 0).attr("y", 0).attr("width", width).attr("height", height);
      if (!rendered || force) {
        rectElement.classed($EVENT.eventRect, true);
      }
    }
    updateClientRect();
  },
  /**
   * Updates the location and size of the eventRect.
   * @private
   */
  updateEventRectData: function() {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state;
    var xScale = scale2.zoom || scale2.x;
    var isRotated = config.axis_rotated;
    var x3;
    var y3;
    var w;
    var h;
    if ($$.isMultipleX()) {
      x3 = 0;
      y3 = 0;
      w = state.width;
      h = state.height;
    } else {
      var rectW_1;
      var rectX = void 0;
      if ($$.axis.isCategorized()) {
        rectW_1 = $$.getEventRectWidth();
        rectX = function(d) {
          return xScale(d.x) - rectW_1 / 2;
        };
      } else {
        var getPrevNextX_1 = function(_a2) {
          var index2 = _a2.index;
          return {
            prev: $$.getPrevX(index2),
            next: $$.getNextX(index2)
          };
        };
        rectW_1 = function(d) {
          var x4 = getPrevNextX_1(d);
          var xDomain = xScale.domain();
          var val;
          if (x4.prev === null && x4.next === null) {
            val = isRotated ? state.height : state.width;
          } else if (x4.prev === null) {
            val = (xScale(x4.next) + xScale(d.x)) / 2;
          } else if (x4.next === null) {
            val = xScale(xDomain[1]) - (xScale(x4.prev) + xScale(d.x)) / 2;
          } else {
            Object.keys(x4).forEach(function(key, i) {
              var _a2;
              x4[key] = (_a2 = x4[key]) !== null && _a2 !== void 0 ? _a2 : xDomain[i];
            });
            val = Math.max(0, (xScale(x4.next) - xScale(x4.prev)) / 2);
          }
          return val;
        };
        rectX = function(d) {
          var x4 = getPrevNextX_1(d);
          var val;
          if (x4.prev === null && x4.next === null) {
            val = 0;
          } else if (x4.prev === null) {
            val = xScale(xScale.domain()[0]);
          } else {
            val = (xScale(d.x) + xScale(x4.prev)) / 2;
          }
          return val;
        };
      }
      x3 = isRotated ? 0 : rectX;
      y3 = isRotated ? rectX : 0;
      w = isRotated ? state.width : rectW_1;
      h = isRotated ? rectW_1 : state.height;
    }
    var eventReceiver = state.eventReceiver;
    var call = function(fn, v) {
      return isFunction(fn) ? fn(v) : fn;
    };
    eventReceiver.coords.splice(eventReceiver.data.length);
    eventReceiver.data.forEach(function(d, i) {
      eventReceiver.coords[i] = {
        x: call(x3, d),
        y: call(y3, d),
        w: call(w, d),
        h: call(h, d)
      };
    });
  },
  /**
   * Seletct rect for single x value
   * @param {d3Selection} context Event rect element
   * @param {number} index x Axis index
   * @private
   */
  selectRectForSingle: function(context, index2) {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, _c = $$.$el, main2 = _c.main, circle = _c.circle;
    var isSelectionEnabled = config.data_selection_enabled;
    var isSelectionGrouped = config.data_selection_grouped;
    var isSelectable = config.data_selection_isselectable;
    var isTooltipGrouped = config.tooltip_grouped;
    var selectedData = $$.getAllValuesOnIndex(index2);
    if (isTooltipGrouped) {
      $$.showTooltip(selectedData, context);
      (_a2 = $$.showGridFocus) === null || _a2 === void 0 ? void 0 : _a2.call($$, selectedData);
      if (!isSelectionEnabled || isSelectionGrouped) {
        return;
      }
    }
    !circle && main2.selectAll(".".concat($COMMON.EXPANDED, ":not(.").concat($SHAPE.shape, "-").concat(index2, ")")).classed($COMMON.EXPANDED, false);
    var shapeAtIndex = main2.selectAll(".".concat($SHAPE.shape, "-").concat(index2)).classed($COMMON.EXPANDED, true).style("cursor", isSelectable ? "pointer" : null).filter(function(d) {
      return $$.isWithinShape(this, d);
    });
    if (shapeAtIndex.empty() && !isTooltipGrouped) {
      (_b = $$.hideGridFocus) === null || _b === void 0 ? void 0 : _b.call($$);
      $$.hideTooltip();
      !isSelectionGrouped && $$.setExpand(index2);
    }
    shapeAtIndex.call(function(selected) {
      var _a3, _b2;
      var d = selected.data();
      if (isSelectionEnabled && (isSelectionGrouped || (isSelectable === null || isSelectable === void 0 ? void 0 : isSelectable.bind($$.api)(d)))) {
        context.style.cursor = "pointer";
      }
      if (!isTooltipGrouped) {
        $$.showTooltip(d, context);
        (_a3 = $$.showGridFocus) === null || _a3 === void 0 ? void 0 : _a3.call($$, d);
        (_b2 = $$.unexpandCircles) === null || _b2 === void 0 ? void 0 : _b2.call($$);
        selected.each(function(d2) {
          return $$.setExpand(index2, d2.id);
        });
      }
    });
  },
  /**
   * Select rect for multiple x values
   * @param {d3Selection} context Event rect element
   * @param {boolean} [triggerEvent=true] Whether trigger event or not
   * @private
   */
  selectRectForMultipleXs: function(context, triggerEvent) {
    if (triggerEvent === void 0) {
      triggerEvent = true;
    }
    var $$ = this;
    var config = $$.config, state = $$.state;
    var targetsToShow = $$.filterTargetsToShow($$.data.targets);
    if (state.dragging || $$.hasArcType(targetsToShow)) {
      return;
    }
    var mouse = getPointer(state.event, context);
    var closest = $$.findClosestFromTargets(targetsToShow, mouse);
    if (triggerEvent && state.mouseover && (!closest || closest.id !== state.mouseover.id)) {
      config.data_onout.call($$.api, state.mouseover);
      state.mouseover = void 0;
    }
    if (!closest) {
      $$.unselectRect();
      return;
    }
    var sameXData = $$.isBubbleType(closest) || $$.isScatterType(closest) || !config.tooltip_grouped ? [closest] : $$.filterByX(targetsToShow, closest.x);
    var selectedData = sameXData.map(function(d) {
      return $$.addName(d);
    });
    $$.showTooltip(selectedData, context);
    $$.setExpand(closest.index, closest.id, true);
    $$.showGridFocus(selectedData);
    var dist = $$.dist(closest, mouse);
    if ($$.isBarType(closest.id) || dist < $$.getPointSensitivity(closest)) {
      $$.$el.svg.select(".".concat($EVENT.eventRect)).style("cursor", "pointer");
      if (triggerEvent && !state.mouseover) {
        config.data_onover.call($$.api, closest);
        state.mouseover = closest;
      }
    }
  },
  /**
   * Unselect EventRect.
   * @private
   */
  unselectRect: function() {
    var $$ = this;
    var _a2 = $$.$el, circle = _a2.circle, tooltip2 = _a2.tooltip;
    $$.$el.svg.select(".".concat($EVENT.eventRect)).style("cursor", null);
    $$.hideGridFocus();
    if (tooltip2) {
      $$.hideTooltip();
      $$._handleLinkedCharts(false);
    }
    circle && !$$.isPointFocusOnly() && $$.unexpandCircles();
    $$.expandBarTypeShapes(false);
  },
  /**
   * Create eventRect for each data on the x-axis.
   * Register touch and drag events.
   * @param {object} eventRectEnter d3.select($EVENT.eventRects) object.
   * @returns {object} d3.select($EVENT.eventRects) object.
   * @private
   */
  generateEventRectsForSingleX: function(eventRectEnter) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var eventReceiver = state.eventReceiver;
    var rect = eventRectEnter.style("cursor", config.data_selection_enabled && config.data_selection_grouped ? "pointer" : null).on("click", function(event) {
      state.event = event;
      var currentIdx = eventReceiver.currentIdx, data2 = eventReceiver.data;
      var d = data2[currentIdx === -1 ? $$.getDataIndexFromEvent(event) : currentIdx];
      $$.clickHandlerForSingleX.bind(this)(d, $$);
    });
    if (state.inputType === "mouse") {
      var getData_1 = function(event) {
        var index2 = event ? $$.getDataIndexFromEvent(event) : eventReceiver.currentIdx;
        return index2 > -1 ? eventReceiver.data[index2] : null;
      };
      rect.on("mouseover", function(event) {
        state.event = event;
        $$.updateEventRect();
      }).on("mousemove", function(event) {
        var d = getData_1(event);
        state.event = event;
        if (!d) {
          return;
        }
        var index2 = d.index;
        var stepType = config.line_step_type;
        if (config.line_step_tooltipMatch && $$.hasType("step") && /^step\-(before|after)$/.test(stepType)) {
          var scale2 = $$.scale.zoom || $$.scale.x;
          var xs = $$.axis.xs[index2];
          var inverted = scale2.invert(getPointer(event, this)[0]);
          if (stepType === "step-after" && inverted < xs) {
            index2 -= 1;
          } else if (stepType === "step-before" && inverted > xs) {
            index2 += 1;
          }
        }
        var eventOnSameIdx = config.tooltip_grouped && index2 === eventReceiver.currentIdx;
        if (state.dragging || state.flowing || $$.hasArcType() || eventOnSameIdx) {
          config.tooltip_show && eventOnSameIdx && $$.setTooltipPosition();
          return;
        }
        if (index2 !== eventReceiver.currentIdx) {
          $$.setOverOut(false, eventReceiver.currentIdx);
          eventReceiver.currentIdx = index2;
        }
        index2 === -1 ? $$.unselectRect() : $$.selectRectForSingle(this, index2);
        $$.setOverOut(index2 !== -1, index2);
      }).on("mouseout", function(event) {
        state.event = event;
        if (!config || $$.hasArcType() || eventReceiver.currentIdx === -1) {
          return;
        }
        $$.unselectRect();
        $$.setOverOut(false, eventReceiver.currentIdx);
        eventReceiver.currentIdx = -1;
      });
    }
    return rect;
  },
  clickHandlerForSingleX: function(d, ctx) {
    var $$ = ctx;
    var config = $$.config, state = $$.state, main2 = $$.$el.main;
    if (!d || $$.hasArcType() || state.cancelClick) {
      state.cancelClick && (state.cancelClick = false);
      return;
    }
    var index2 = d.index;
    main2.selectAll(".".concat($SHAPE.shape, "-").concat(index2)).each(function(d2) {
      var _a2;
      if (config.data_selection_grouped || $$.isWithinShape(this, d2)) {
        (_a2 = $$.toggleShape) === null || _a2 === void 0 ? void 0 : _a2.call($$, this, d2, index2);
        config.data_onclick.bind($$.api)(d2, this);
      }
    });
  },
  /**
   * Create an eventRect,
   * Register touch and drag events.
   * @param {object} eventRectEnter d3.select($EVENT.eventRects) object.
   * @private
   */
  generateEventRectsForMultipleXs: function(eventRectEnter) {
    var $$ = this;
    var state = $$.state;
    eventRectEnter.on("click", function(event) {
      state.event = event;
      $$.clickHandlerForMultipleXS.bind(this)($$);
    });
    if (state.inputType === "mouse") {
      eventRectEnter.on("mouseover mousemove", function(event) {
        state.event = event;
        $$.selectRectForMultipleXs(this);
      }).on("mouseout", function(event) {
        state.event = event;
        if (!$$.config || $$.hasArcType()) {
          return;
        }
        $$.unselectRect();
      });
    }
  },
  clickHandlerForMultipleXS: function(ctx) {
    var $$ = ctx;
    var config = $$.config, state = $$.state;
    var targetsToShow = $$.filterTargetsToShow($$.data.targets);
    if ($$.hasArcType(targetsToShow)) {
      return;
    }
    var mouse = getPointer(state.event, this);
    var closest = $$.findClosestFromTargets(targetsToShow, mouse);
    var sensitivity = config.point_sensitivity === "radius" ? closest.r : config.point_sensitivity;
    if (!closest) {
      return;
    }
    if ($$.isBarType(closest.id) || $$.dist(closest, mouse) < sensitivity) {
      $$.$el.main.selectAll(".".concat($SHAPE.shapes).concat($$.getTargetSelectorSuffix(closest.id))).selectAll(".".concat($SHAPE.shape, "-").concat(closest.index)).each(function() {
        var _a2;
        if (config.data_selection_grouped || $$.isWithinShape(this, closest)) {
          (_a2 = $$.toggleShape) === null || _a2 === void 0 ? void 0 : _a2.call($$, this, closest, closest.index);
          config.data_onclick.bind($$.api)(closest, this);
        }
      });
    }
  }
};
var flow = {
  /**
   * Generate flow
   * @param {object} args option object
   * @returns {Function}
   * @private
   */
  generateFlow: function(args) {
    var $$ = this;
    var data2 = $$.data, state = $$.state, $el = $$.$el;
    return function() {
      var flowLength = args.flow.length;
      state.flowing = true;
      data2.targets.forEach(function(d) {
        d.values.splice(0, flowLength);
      });
      if ($$.updateXGrid) {
        $$.updateXGrid(true);
      }
      var elements = {};
      ["axis.x", "grid.x", "gridLines.x", "region.list", "text", "bar", "line", "area", "circle"].forEach(function(v) {
        var name = v.split(".");
        var node = $el[name[0]];
        if (node && name.length > 1) {
          node = node[name[1]];
        }
        if (node === null || node === void 0 ? void 0 : node.size()) {
          elements[v] = node;
        }
      });
      $$.hideGridFocus();
      $$.setFlowList(elements, args);
    };
  },
  /**
   * Set flow list
   * @param {object} elements Target elements
   * @param {object} args option object
   * @private
   */
  setFlowList: function(elements, args) {
    var $$ = this;
    var flow2 = args.flow, targets = args.targets;
    var _a2 = flow2.duration, duration = _a2 === void 0 ? args.duration : _a2, flowIndex = flow2.index, flowLength = flow2.length, orgDataCount = flow2.orgDataCount;
    var transform3 = $$.getFlowTransform(targets, orgDataCount, flowIndex, flowLength);
    var wait = generateWait();
    var n;
    wait.add(Object.keys(elements).map(function(v) {
      n = elements[v].transition().ease(linear2).duration(duration);
      if (v === "axis.x") {
        n = n.call(function(g) {
          $$.axis.x.setTransition(g).create(g);
        });
      } else if (v === "region.list") {
        n = n.filter($$.isRegionOnX).attr("transform", transform3);
      } else {
        n = n.attr("transform", transform3);
      }
      return n;
    }));
    n.call(wait, function() {
      $$.cleanUpFlow(elements, args);
    });
  },
  /**
   * Clean up flow
   * @param {object} elements Target elements
   * @param {object} args option object
   * @private
   */
  cleanUpFlow: function(elements, args) {
    var $$ = this;
    var config = $$.config, state = $$.state, svg = $$.$el.svg;
    var isRotated = config.axis_rotated;
    var flow2 = args.flow, shape2 = args.shape, xv = args.xv;
    var _a2 = shape2.pos, cx = _a2.cx, cy = _a2.cy, xForText = _a2.xForText, yForText = _a2.yForText;
    var _b = flow2.done, done = _b === void 0 ? function() {
    } : _b, flowLength = flow2.length;
    if (flowLength) {
      ["circle", "text", "shape", "eventRect"].forEach(function(v) {
        var target = [];
        for (var i = 0; i < flowLength; i++) {
          target.push(".".concat(CLASS[v], "-").concat(i));
        }
        svg.selectAll(".".concat(CLASS["".concat(v, "s")])).selectAll(target).remove();
      });
      svg.select(".".concat(CLASS.xgrid)).remove();
    }
    Object.keys(elements).forEach(function(v) {
      var n = elements[v];
      if (v !== "axis.x") {
        n.attr("transform", null);
      }
      if (v === "grid.x") {
        n.attr(state.xgridAttr);
      } else if (v === "gridLines.x") {
        n.attr("x1", isRotated ? 0 : xv).attr("x2", isRotated ? state.width : xv);
        n.select("text").attr("x", isRotated ? state.width : 0).attr("y", xv);
      } else if (/^(area|bar|line)$/.test(v)) {
        n.attr("d", shape2.type[v]);
      } else if (v === "text") {
        n.attr("x", xForText).attr("y", yForText).style("fill-opacity", $$.opacityForText.bind($$));
      } else if (v === "circle") {
        if ($$.isCirclePoint()) {
          n.attr("cx", cx).attr("cy", cy);
        } else {
          var xFunc = function(d) {
            return cx(d) - config.point_r;
          };
          var yFunc = function(d) {
            return cy(d) - config.point_r;
          };
          n.attr("x", xFunc).attr("y", yFunc);
        }
      } else if (v === "region.list") {
        n.select("rect").filter($$.isRegionOnX).attr("x", $$.regionX.bind($$)).attr("width", $$.regionWidth.bind($$));
      }
    });
    config.interaction_enabled && $$.redrawEventRect();
    done.call($$.api);
    state.flowing = false;
  },
  /**
   * Get flow transform value
   * @param {object} targets target
   * @param {number} orgDataCount original data count
   * @param {number} flowIndex flow index
   * @param {number} flowLength flow length
   * @returns {string}
   * @private
   */
  getFlowTransform: function(targets, orgDataCount, flowIndex, flowLength) {
    var $$ = this;
    var data2 = $$.data, x3 = $$.scale.x;
    var dataValues = data2.targets[0].values;
    var flowStart = $$.getValueOnIndex(dataValues, flowIndex);
    var flowEnd = $$.getValueOnIndex(dataValues, flowIndex + flowLength);
    var translateX2;
    var orgDomain = x3.domain();
    var domain2 = $$.updateXDomain(targets, true, true);
    if (!orgDataCount) {
      if (dataValues.length !== 1) {
        translateX2 = x3(orgDomain[0]) - x3(domain2[0]);
      } else {
        if ($$.axis.isTimeSeries()) {
          flowStart = $$.getValueOnIndex(dataValues, 0);
          flowEnd = $$.getValueOnIndex(dataValues, dataValues.length - 1);
          translateX2 = x3(flowStart.x) - x3(flowEnd.x);
        } else {
          translateX2 = diffDomain(domain2) / 2;
        }
      }
    } else if (orgDataCount === 1 || (flowStart === null || flowStart === void 0 ? void 0 : flowStart.x) === (flowEnd === null || flowEnd === void 0 ? void 0 : flowEnd.x)) {
      translateX2 = x3(orgDomain[0]) - x3(domain2[0]);
    } else {
      translateX2 = $$.axis.isTimeSeries() ? x3(orgDomain[0]) - x3(domain2[0]) : x3((flowStart === null || flowStart === void 0 ? void 0 : flowStart.x) || 0) - x3(flowEnd.x);
    }
    var scaleX = diffDomain(orgDomain) / diffDomain(domain2);
    return "translate(".concat(translateX2, ",0) scale(").concat(scaleX, ",1)");
  }
};
var clip = {
  initClip: function() {
    var $$ = this;
    var _a2 = $$.state, clip2 = _a2.clip, datetimeId = _a2.datetimeId;
    clip2.id = "".concat(datetimeId, "-clip");
    clip2.idXAxis = "".concat(clip2.id, "-xaxis");
    clip2.idYAxis = "".concat(clip2.id, "-yaxis");
    clip2.idGrid = "".concat(clip2.id, "-grid");
    clip2.path = $$.getClipPath(clip2.id);
    clip2.pathXAxis = $$.getClipPath(clip2.idXAxis);
    clip2.pathYAxis = $$.getClipPath(clip2.idYAxis);
    clip2.pathGrid = $$.getClipPath(clip2.idGrid);
  },
  getClipPath: function(id2) {
    var $$ = this;
    var config = $$.config;
    if (!config.clipPath && /-clip$/.test(id2) || !config.axis_x_clipPath && /-clip-xaxis$/.test(id2) || !config.axis_y_clipPath && /-clip-yaxis$/.test(id2)) {
      return null;
    }
    return "url(#".concat(id2, ")");
  },
  appendClip: function(parent, id2) {
    id2 && parent.append("clipPath").attr("id", id2).append("rect");
  },
  /**
   * Set x Axis clipPath dimension
   * @param {d3Selecton} node clipPath <rect> selection
   * @private
   */
  setXAxisClipPath: function(node) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, margin = _a2.margin, width = _a2.width, height = _a2.height;
    var isRotated = config.axis_rotated;
    var left2 = Math.max(30, margin.left) - (isRotated ? 0 : 20);
    var h = (isRotated ? margin.top + height + 10 : margin.bottom) + 20;
    var x3 = isRotated ? -(1 + left2) : -(left2 - 1);
    var y3 = -15;
    var w = isRotated ? margin.left + 20 : width + 10 + left2;
    node.attr("x", x3).attr("y", y3).attr("width", w).attr("height", h);
  },
  /**
   * Set y Axis clipPath dimension
   * @param {d3Selection} node clipPath <rect> selection
   * @private
   */
  setYAxisClipPath: function(node) {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, margin = _a2.margin, width = _a2.width, height = _a2.height;
    var isRotated = config.axis_rotated;
    var left2 = Math.max(30, margin.left) - (isRotated ? 20 : 0);
    var isInner = config.axis_y_inner;
    var x3 = isInner && !isRotated ? config.axis_y_label.text ? -20 : -1 : isRotated ? -(1 + left2) : -(left2 - 1);
    var y3 = -(isRotated ? 20 : margin.top);
    var w = (isRotated ? width + 15 + left2 : margin.left + 20) + (isInner ? 20 : 0);
    var h = (isRotated ? margin.bottom + 10 : margin.top + height) + 10;
    node.attr("x", x3).attr("y", y3).attr("width", w).attr("height", h);
  },
  updateXAxisTickClip: function() {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, clip2 = _a2.clip, xAxisHeight = _a2.xAxisHeight, defs = $$.$el.defs;
    var newXAxisHeight = $$.getHorizontalAxisHeight("x");
    if (defs && !clip2.idXAxisTickTexts) {
      var clipId = "".concat(clip2.id, "-xaxisticktexts");
      $$.appendClip(defs, clipId);
      clip2.pathXAxisTickTexts = $$.getClipPath(clip2.idXAxisTickTexts);
      clip2.idXAxisTickTexts = clipId;
    }
    if (!config.axis_x_tick_multiline && $$.getAxisTickRotate("x") && newXAxisHeight !== xAxisHeight) {
      $$.setXAxisTickClipWidth();
      $$.setXAxisTickTextClipPathWidth();
    }
    $$.state.xAxisHeight = newXAxisHeight;
  },
  setXAxisTickClipWidth: function() {
    var $$ = this;
    var config = $$.config, maxTickSize = $$.state.current.maxTickSize;
    var xAxisTickRotate = $$.getAxisTickRotate("x");
    if (!config.axis_x_tick_multiline && xAxisTickRotate) {
      var sinRotation = Math.sin(Math.PI / 180 * Math.abs(xAxisTickRotate));
      maxTickSize.x.clipPath = ($$.getHorizontalAxisHeight("x") - 20) / sinRotation;
    } else {
      maxTickSize.x.clipPath = null;
    }
  },
  setXAxisTickTextClipPathWidth: function() {
    var $$ = this;
    var _a2 = $$.state, clip2 = _a2.clip, current = _a2.current, svg = $$.$el.svg;
    if (svg) {
      svg.select("#".concat(clip2.idXAxisTickTexts, " rect")).attr("width", current.maxTickSize.x.clipPath).attr("height", 30);
    }
  }
};
var getGridTextAnchor = function(d) {
  return isValue(d.position) || "end";
};
var getGridTextDx = function(d) {
  return d.position === "start" ? 4 : d.position === "middle" ? 0 : -4;
};
function getGridTextX(isX, width, height) {
  return function(d) {
    var x3 = isX ? 0 : width;
    if (d.position === "start") {
      x3 = isX ? -height : 0;
    } else if (d.position === "middle") {
      x3 = (isX ? -height : width) / 2;
    }
    return x3;
  };
}
function smoothLines(el, type2) {
  if (type2 === "grid") {
    el.each(function() {
      var g = select_default2(this);
      ["x1", "x2", "y1", "y2"].forEach(function(v) {
        return g.attr(v, Math.ceil(+g.attr(v)));
      });
    });
  }
}
var grid = {
  hasGrid: function() {
    var config = this.config;
    return ["x", "y"].some(function(v) {
      return config["grid_".concat(v, "_show")] || config["grid_".concat(v, "_lines")].length;
    });
  },
  initGrid: function() {
    var $$ = this;
    $$.hasGrid() && $$.initGridLines();
    $$.initFocusGrid();
  },
  initGridLines: function() {
    var $$ = this;
    var config = $$.config, clip2 = $$.state.clip, $el = $$.$el;
    if (config.grid_x_lines.length || config.grid_y_lines.length) {
      $el.gridLines.main = $el.main.insert("g", ".".concat($COMMON.chart).concat(config.grid_lines_front ? " + *" : "")).attr("clip-path", clip2.pathGrid).attr("class", "".concat($GRID.grid, " ").concat($GRID.gridLines));
      $el.gridLines.main.append("g").attr("class", $GRID.xgridLines);
      $el.gridLines.main.append("g").attr("class", $GRID.ygridLines);
      $el.gridLines.x = selectAll_default2([]);
    }
  },
  updateXGrid: function(withoutUpdate) {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state, _a2 = $$.$el, main2 = _a2.main, grid2 = _a2.grid;
    var isRotated = config.axis_rotated;
    var xgridData = $$.generateGridData(config.grid_x_type, scale2.x);
    var tickOffset = $$.axis.isCategorized() ? $$.axis.x.tickOffset() : 0;
    var pos = function(d) {
      return (scale2.zoom || scale2.x)(d) + tickOffset * (isRotated ? -1 : 1);
    };
    state.xgridAttr = isRotated ? {
      "x1": 0,
      "x2": state.width,
      "y1": pos,
      "y2": pos
    } : {
      "x1": pos,
      "x2": pos,
      "y1": 0,
      "y2": state.height
    };
    grid2.x = main2.select(".".concat($GRID.xgrids)).selectAll(".".concat($GRID.xgrid)).data(xgridData);
    grid2.x.exit().remove();
    grid2.x = grid2.x.enter().append("line").attr("class", $GRID.xgrid).merge(grid2.x);
    if (!withoutUpdate) {
      grid2.x.each(function() {
        var grid3 = select_default2(this);
        Object.keys(state.xgridAttr).forEach(function(id2) {
          grid3.attr(id2, state.xgridAttr[id2]).style("opacity", function() {
            return grid3.attr(isRotated ? "y1" : "x1") === (isRotated ? state.height : 0) ? "0" : null;
          });
        });
      });
    }
  },
  updateYGrid: function() {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, scale2 = $$.scale, state = $$.state, _a2 = $$.$el, grid2 = _a2.grid, main2 = _a2.main;
    var isRotated = config.axis_rotated;
    var pos = function(d) {
      return Math.ceil(scale2.y(d));
    };
    var gridValues = axis3.y.getGeneratedTicks(config.grid_y_ticks) || $$.scale.y.ticks(config.grid_y_ticks);
    grid2.y = main2.select(".".concat($GRID.ygrids)).selectAll(".".concat($GRID.ygrid)).data(gridValues);
    grid2.y.exit().remove();
    grid2.y = grid2.y.enter().append("line").attr("class", $GRID.ygrid).merge(grid2.y);
    grid2.y.attr("x1", isRotated ? pos : 0).attr("x2", isRotated ? pos : state.width).attr("y1", isRotated ? 0 : pos).attr("y2", isRotated ? state.height : pos);
    smoothLines(grid2.y, "grid");
  },
  updateGrid: function() {
    var $$ = this;
    var _a2 = $$.$el, grid2 = _a2.grid, gridLines = _a2.gridLines;
    !gridLines.main && $$.initGridLines();
    grid2.main.style("visibility", $$.hasArcType() ? "hidden" : null);
    $$.hideGridFocus();
    $$.updateGridLines("x");
    $$.updateGridLines("y");
  },
  /**
   * Update Grid lines
   * @param {string} type x | y
   * @private
   */
  updateGridLines: function(type2) {
    var $$ = this;
    var config = $$.config, _a2 = $$.$el, gridLines = _a2.gridLines, main2 = _a2.main, $T = $$.$T;
    var isRotated = config.axis_rotated;
    var isX = type2 === "x";
    config["grid_".concat(type2, "_show")] && $$["update".concat(type2.toUpperCase(), "Grid")]();
    var lines = main2.select(".".concat($GRID["".concat(type2, "gridLines")])).selectAll(".".concat($GRID["".concat(type2, "gridLine")])).data(config["grid_".concat(type2, "_lines")]);
    $T(lines.exit()).style("opacity", "0").remove();
    var gridLine = lines.enter().append("g");
    gridLine.append("line").style("opacity", "0");
    lines = gridLine.merge(lines);
    lines.each(function(d) {
      var g = select_default2(this);
      if (g.select("text").empty() && d.text) {
        g.append("text").style("opacity", "0");
      }
    });
    $T(lines.attr("class", function(d) {
      return "".concat($GRID["".concat(type2, "gridLine")], " ").concat(d.class || "").trim();
    }).select("text").attr("text-anchor", getGridTextAnchor).attr("transform", function() {
      return isX ? isRotated ? null : "rotate(-90)" : isRotated ? "rotate(-90)" : null;
    }).attr("dx", getGridTextDx).attr("dy", -5)).text(function(d) {
      var _a3;
      return (_a3 = d.text) !== null && _a3 !== void 0 ? _a3 : this.remove();
    });
    gridLines[type2] = lines;
  },
  redrawGrid: function(withTransition) {
    var $$ = this;
    var isRotated = $$.config.axis_rotated, _a2 = $$.state, width = _a2.width, height = _a2.height, gridLines = $$.$el.gridLines, $T = $$.$T;
    var xv = $$.xv.bind($$);
    var yv = $$.yv.bind($$);
    var xLines = gridLines.x.select("line");
    var xTexts = gridLines.x.select("text");
    var yLines = gridLines.y.select("line");
    var yTexts = gridLines.y.select("text");
    xLines = $T(xLines, withTransition).attr("x1", isRotated ? 0 : xv).attr("x2", isRotated ? width : xv).attr("y1", isRotated ? xv : 0).attr("y2", isRotated ? xv : height);
    xTexts = $T(xTexts, withTransition).attr("x", getGridTextX(!isRotated, width, height)).attr("y", xv);
    yLines = $T(yLines, withTransition).attr("x1", isRotated ? yv : 0).attr("x2", isRotated ? yv : width).attr("y1", isRotated ? 0 : yv).attr("y2", isRotated ? height : yv);
    yTexts = $T(yTexts, withTransition).attr("x", getGridTextX(isRotated, width, height)).attr("y", yv);
    return [
      xLines.style("opacity", null),
      xTexts.style("opacity", null),
      yLines.style("opacity", null),
      yTexts.style("opacity", null)
    ];
  },
  initFocusGrid: function() {
    var $$ = this;
    var config = $$.config, clip2 = $$.state.clip, $el = $$.$el;
    var isFront = config.grid_front;
    var className = ".".concat(isFront && $el.gridLines.main ? $GRID.gridLines : $COMMON.chart).concat(isFront ? " + *" : "");
    var grid2 = $el.main.insert("g", className).attr("clip-path", clip2.pathGrid).attr("class", $GRID.grid);
    $el.grid.main = grid2;
    config.grid_x_show && grid2.append("g").attr("class", $GRID.xgrids);
    config.grid_y_show && grid2.append("g").attr("class", $GRID.ygrids);
    if (config.interaction_enabled && config.grid_focus_show) {
      grid2.append("g").attr("class", $FOCUS.xgridFocus).append("line").attr("class", $FOCUS.xgridFocus);
      if (config.grid_focus_y && !config.tooltip_grouped) {
        grid2.append("g").attr("class", $FOCUS.ygridFocus).append("line").attr("class", $FOCUS.ygridFocus);
      }
    }
  },
  /**
   * Show grid focus line
   * @param {Array} data Selected data
   * @private
   */
  showGridFocus: function(data2) {
    var _a2;
    var $$ = this;
    var config = $$.config, _b = $$.state, width = _b.width, height = _b.height;
    var isRotated = config.axis_rotated;
    var focusEl = $$.$el.main.selectAll("line.".concat($FOCUS.xgridFocus, ", line.").concat($FOCUS.ygridFocus));
    var dataToShow = (data2 || [focusEl.datum()]).filter(function(d) {
      return d && isValue($$.getBaseValue(d));
    });
    if (!config.tooltip_show || dataToShow.length === 0 || $$.hasType("bubble") || $$.hasArcType()) {
      return;
    }
    var isEdge = config.grid_focus_edge && !config.tooltip_grouped;
    var xx = $$.xx.bind($$);
    focusEl.style("visibility", null).data(dataToShow.concat(dataToShow)).each(function(d) {
      var el = select_default2(this);
      var pos = {
        x: xx(d),
        y: $$.getYScaleById(d.id)(d.value)
      };
      var xy;
      if (el.classed($FOCUS.xgridFocus)) {
        xy = isRotated ? [
          null,
          // x1
          pos.x,
          // y1
          isEdge ? pos.y : width,
          // x2
          pos.x
          // y2
        ] : [
          pos.x,
          isEdge ? pos.y : null,
          pos.x,
          height
        ];
      } else {
        var isY2 = $$.axis.getId(d.id) === "y2";
        xy = isRotated ? [
          pos.y,
          // x1
          isEdge && !isY2 ? pos.x : null,
          // y1
          pos.y,
          // x2
          isEdge && isY2 ? pos.x : height
          // y2
        ] : [
          isEdge && isY2 ? pos.x : null,
          pos.y,
          isEdge && !isY2 ? pos.x : width,
          pos.y
        ];
      }
      ["x1", "y1", "x2", "y2"].forEach(function(v, i) {
        return el.attr(v, xy[i]);
      });
    });
    smoothLines(focusEl, "grid");
    (_a2 = $$.showCircleFocus) === null || _a2 === void 0 ? void 0 : _a2.call($$, data2);
  },
  hideGridFocus: function() {
    var _a2;
    var $$ = this;
    var _b = $$.state, inputType = _b.inputType, resizing = _b.resizing, main2 = $$.$el.main;
    if (inputType === "mouse" || !resizing) {
      main2.selectAll("line.".concat($FOCUS.xgridFocus, ", line.").concat($FOCUS.ygridFocus)).style("visibility", "hidden");
      (_a2 = $$.hideCircleFocus) === null || _a2 === void 0 ? void 0 : _a2.call($$);
    }
  },
  updateGridFocus: function() {
    var _a2;
    var $$ = this;
    var _b = $$.state, inputType = _b.inputType, width = _b.width, height = _b.height, resizing = _b.resizing, grid2 = $$.$el.grid;
    var xgridFocus = grid2.main.select("line.".concat($FOCUS.xgridFocus));
    if (inputType === "touch") {
      if (xgridFocus.empty()) {
        resizing && ((_a2 = $$.showCircleFocus) === null || _a2 === void 0 ? void 0 : _a2.call($$));
      } else {
        $$.showGridFocus();
      }
    } else {
      var isRotated = $$.config.axis_rotated;
      xgridFocus.attr("x1", isRotated ? 0 : -10).attr("x2", isRotated ? width : -10).attr("y1", isRotated ? -10 : 0).attr("y2", isRotated ? -10 : height);
    }
    return true;
  },
  generateGridData: function(type2, scale2) {
    var $$ = this;
    var tickNum = $$.$el.main.select(".".concat($AXIS.axisX)).selectAll(".tick").size();
    var gridData = [];
    if (type2 === "year") {
      var xDomain = $$.getXDomain();
      var _a2 = xDomain.map(function(v) {
        return v.getFullYear();
      }), firstYear = _a2[0], lastYear = _a2[1];
      for (var i = firstYear; i <= lastYear; i++) {
        gridData.push(new Date("".concat(i, "-01-01 00:00:00")));
      }
    } else {
      gridData = scale2.ticks(10);
      if (gridData.length > tickNum) {
        gridData = gridData.filter(function(d) {
          return String(d).indexOf(".") < 0;
        });
      }
    }
    return gridData;
  },
  getGridFilterToRemove: function(params) {
    return params ? function(line2) {
      var found = false;
      (isArray(params) ? params.concat() : [params]).forEach(function(param) {
        if ("value" in param && line2.value === param.value || "class" in param && line2.class === param.class) {
          found = true;
        }
      });
      return found;
    } : function() {
      return true;
    };
  },
  removeGridLines: function(params, forX) {
    var $$ = this;
    var config = $$.config, $T = $$.$T;
    var toRemove = $$.getGridFilterToRemove(params);
    var toShow = function(line2) {
      return !toRemove(line2);
    };
    var classLines = forX ? $GRID.xgridLines : $GRID.ygridLines;
    var classLine = forX ? $GRID.xgridLine : $GRID.ygridLine;
    $T($$.$el.main.select(".".concat(classLines)).selectAll(".".concat(classLine)).filter(toRemove)).style("opacity", "0").remove();
    var gridLines = "grid_".concat(forX ? "x" : "y", "_lines");
    config[gridLines] = config[gridLines].filter(toShow);
  }
};
var region = {
  initRegion: function() {
    var $$ = this;
    var $el = $$.$el;
    $el.region.main = $el.main.insert("g", ":first-child").attr("clip-path", $$.state.clip.path).attr("class", $REGION.regions);
  },
  updateRegion: function() {
    var $$ = this;
    var config = $$.config, region2 = $$.$el.region, $T = $$.$T;
    if (!region2.main) {
      $$.initRegion();
    }
    region2.main.style("visibility", $$.hasArcType() ? "hidden" : null);
    var regions2 = region2.main.selectAll(".".concat($REGION.region)).data(config.regions);
    $T(regions2.exit()).style("opacity", "0").remove();
    var regionsEnter = regions2.enter().append("g");
    regionsEnter.append("rect").style("fill-opacity", "0");
    region2.list = regionsEnter.merge(regions2).attr("class", $$.classRegion.bind($$));
    region2.list.each(function(d) {
      var _a2;
      var g = select_default2(this);
      if (g.select("text").empty() && ((_a2 = d.label) === null || _a2 === void 0 ? void 0 : _a2.text)) {
        select_default2(this).append("text").style("opacity", "0");
      }
    });
  },
  redrawRegion: function(withTransition) {
    var $$ = this;
    var region2 = $$.$el.region, $T = $$.$T;
    var regions2 = region2.list.select("rect");
    var label = region2.list.selectAll("text");
    regions2 = $T(regions2, withTransition).attr("x", $$.regionX.bind($$)).attr("y", $$.regionY.bind($$)).attr("width", $$.regionWidth.bind($$)).attr("height", $$.regionHeight.bind($$));
    label = $T(label, withTransition).attr("transform", function(d) {
      var _a2;
      var _b = (_a2 = d.label) !== null && _a2 !== void 0 ? _a2 : {}, _c = _b.x, x3 = _c === void 0 ? 0 : _c, _d = _b.y, y3 = _d === void 0 ? 0 : _d, _e = _b.rotated, rotated = _e === void 0 ? false : _e;
      return "translate(".concat($$.regionX.bind($$)(d) + x3, ", ").concat($$.regionY.bind($$)(d) + y3, ")").concat(rotated ? " rotate(-90)" : "");
    }).attr("text-anchor", function(d) {
      var _a2;
      return ((_a2 = d.label) === null || _a2 === void 0 ? void 0 : _a2.rotated) ? "end" : null;
    }).attr("dy", "1em").style("fill", function(d) {
      var _a2, _b;
      return (_b = (_a2 = d.label) === null || _a2 === void 0 ? void 0 : _a2.color) !== null && _b !== void 0 ? _b : null;
    }).text(function(d) {
      var _a2;
      return (_a2 = d.label) === null || _a2 === void 0 ? void 0 : _a2.text;
    });
    return [
      regions2.style("fill-opacity", function(d) {
        return isValue(d.opacity) ? d.opacity : null;
      }).on("end", function() {
        select_default2(this.parentNode).selectAll("rect:not([x])").remove();
      }),
      label.style("opacity", null)
    ];
  },
  getRegionXY: function(type2, d) {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale;
    var isRotated = config.axis_rotated;
    var isX = type2 === "x";
    var key = "start";
    var currScale;
    var pos = 0;
    if (d.axis === "y" || d.axis === "y2") {
      if (!isX) {
        key = "end";
      }
      if ((isX ? isRotated : !isRotated) && key in d) {
        currScale = scale2[d.axis];
        pos = currScale(d[key]);
      }
    } else if ((isX ? !isRotated : isRotated) && key in d) {
      currScale = scale2.zoom || scale2.x;
      pos = currScale($$.axis.isTimeSeries() ? parseDate.call($$, d[key]) : d[key]);
    }
    return pos;
  },
  regionX: function(d) {
    return this.getRegionXY("x", d);
  },
  regionY: function(d) {
    return this.getRegionXY("y", d);
  },
  getRegionSize: function(type2, d) {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state;
    var isRotated = config.axis_rotated;
    var isWidth = type2 === "width";
    var start2 = $$[isWidth ? "regionX" : "regionY"](d);
    var currScale;
    var key = "end";
    var end = state[type2];
    if (d.axis === "y" || d.axis === "y2") {
      if (!isWidth) {
        key = "start";
      }
      if ((isWidth ? isRotated : !isRotated) && key in d) {
        currScale = scale2[d.axis];
        end = currScale(d[key]);
      }
    } else if ((isWidth ? !isRotated : isRotated) && key in d) {
      currScale = scale2.zoom || scale2.x;
      end = currScale($$.axis.isTimeSeries() ? parseDate.call($$, d[key]) : d[key]);
    }
    return end < start2 ? 0 : end - start2;
  },
  regionWidth: function(d) {
    return this.getRegionSize("width", d);
  },
  regionHeight: function(d) {
    return this.getRegionSize("height", d);
  },
  isRegionOnX: function(d) {
    return !d.axis || d.axis === "x";
  }
};
var sizeAxis = {
  /**
   * Get Axis size according its position
   * @param {string} id Axis id value - x, y or y2
   * @returns {number} size Axis size value
   * @private
   */
  getAxisSize: function(id2) {
    var $$ = this;
    var isRotated = $$.config.axis_rotated;
    return isRotated && id2 === "x" || !isRotated && /y2?/.test(id2) ? $$.getAxisWidthByAxisId(id2, true) : $$.getHorizontalAxisHeight(id2);
  },
  getAxisWidthByAxisId: function(id2, withoutRecompute) {
    var _a2, _b;
    var $$ = this;
    if ($$.axis) {
      var position2 = (_a2 = $$.axis) === null || _a2 === void 0 ? void 0 : _a2.getLabelPositionById(id2);
      var width = $$.axis.getMaxTickSize(id2, withoutRecompute).width;
      var gap = width === 0 ? 0.5 : 0;
      return width + (((_b = $$.config.padding) === null || _b === void 0 ? void 0 : _b.mode) === "fit" ? position2.isInner ? 10 + gap : 10 : position2.isInner ? 20 + gap : 40);
    } else {
      return 40;
    }
  },
  getHorizontalAxisHeight: function(id2) {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, state = $$.state;
    var rotatedPadding = state.rotatedPadding, isLegendRight = state.isLegendRight, isLegendInset = state.isLegendInset;
    var isRotated = config.axis_rotated;
    var isFitPadding = ((_a2 = config.padding) === null || _a2 === void 0 ? void 0 : _a2.mode) === "fit";
    var isInner = config["axis_".concat(id2, "_inner")];
    var hasLabelText = config["axis_".concat(id2, "_label")].text;
    var defaultHeight = 13;
    var h = ((_b = config.padding) === null || _b === void 0 ? void 0 : _b.mode) === "fit" ? isInner && !hasLabelText ? id2 === "y" ? 1 : 0 : 20 : 30;
    if (id2 === "x" && !config.axis_x_show) {
      return 8;
    }
    if (id2 === "x" && isNumber(config.axis_x_height)) {
      return config.axis_x_height;
    }
    if (id2 === "y" && !config.axis_y_show) {
      return config.legend_show && !isLegendRight && !isLegendInset ? 10 : 1;
    }
    if (id2 === "y2" && !config.axis_y2_show) {
      return isFitPadding ? 0 : rotatedPadding.top;
    }
    var maxtickSize = $$.axis.getMaxTickSize(id2);
    var isXAxisTickRotated = Math.abs(config.axis_x_tick_rotate) > 0 && (!config.axis_x_tick_autorotate || $$.needToRotateXAxisTickTexts());
    if ((config.axis_x_tick_multiline || isXAxisTickRotated) && maxtickSize.height > defaultHeight) {
      h += maxtickSize.height - defaultHeight;
    }
    return h + ($$.axis.getLabelPositionById(id2).isInner ? 0 : 10) + (id2 === "y2" && !isRotated ? -10 : 0);
  },
  getEventRectWidth: function() {
    var $$ = this;
    var config = $$.config, axis3 = $$.axis;
    var isInverted = config.axis_x_inverted;
    var tickInterval = axis3.x.tickInterval();
    return Math.max(0, isInverted ? Math.abs(tickInterval) : tickInterval);
  },
  /**
   * Get axis tick test rotate value
   * @param {string} id Axis id
   * @returns {number} rotate value
   * @private
   */
  getAxisTickRotate: function(id2) {
    var $$ = this;
    var axis3 = $$.axis, config = $$.config, state = $$.state, $el = $$.$el;
    var rotate = config["axis_".concat(id2, "_tick_rotate")];
    if (id2 === "x") {
      var allowedXAxisTypes = axis3.isCategorized() || axis3.isTimeSeries();
      if (config.axis_x_tick_fit && allowedXAxisTypes) {
        var xTickCount = config.axis_x_tick_count;
        var currentXTicksLength = state.current.maxTickSize.x.ticks.length;
        var tickCount = 0;
        if (xTickCount) {
          tickCount = xTickCount > currentXTicksLength ? currentXTicksLength : xTickCount;
        } else if (currentXTicksLength) {
          tickCount = currentXTicksLength;
        }
        if (tickCount !== state.axis.x.tickCount) {
          var targets = $$.data.targets;
          state.axis.x.padding = $$.getXDomainPadding([
            $$.getXDomainMinMax(targets, "min"),
            $$.getXDomainMinMax(targets, "max")
          ], tickCount);
        }
        state.axis.x.tickCount = tickCount;
      }
      if ($el.svg && config.axis_x_tick_autorotate && config.axis_x_tick_fit && !config.axis_x_tick_multiline && !config.axis_x_tick_culling && allowedXAxisTypes) {
        rotate = $$.needToRotateXAxisTickTexts() ? config.axis_x_tick_rotate : 0;
      }
    }
    return rotate;
  },
  /**
   * Check weather axis tick text needs to be rotated
   * @returns {boolean}
   * @private
   */
  needToRotateXAxisTickTexts: function() {
    var $$ = this;
    var _a2 = $$.state, axis3 = _a2.axis, current = _a2.current, isLegendRight = _a2.isLegendRight, legendItemWidth = _a2.legendItemWidth;
    var legendWidth = isLegendRight && legendItemWidth;
    var xAxisLength = current.width - legendWidth - $$.getCurrentPaddingByDirection("left") - $$.getCurrentPaddingByDirection("right");
    var tickCountWithPadding = axis3.x.tickCount + axis3.x.padding.left + axis3.x.padding.right;
    var width = $$.axis.getMaxTickSize("x").width;
    var tickLength = tickCountWithPadding ? xAxisLength / tickCountWithPadding : 0;
    return width > tickLength;
  }
};
var optDataAxis = {
  /**
   * Specify the keys of the x values for each data.<br><br>
   * This option can be used if we want to show the data that has different x values.
   * @name data․xs
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   xs: {
   *      data1: "x1",
   *      data2: "x2"
   *   }
   * }
   */
  data_xs: {},
  /**
   * Set a format specifier to parse string specifed as x.
   * @name data․xFormat
   * @memberof Options
   * @type {string}
   * @default %Y-%m-%d
   * @example
   * data: {
   *    x: "x",
   *    columns: [
   *        ["x", "01012019", "02012019", "03012019"],
   *        ["data1", 30, 200, 100]
   *    ],
   *    // Format specifier to parse as datetime for given 'x' string value
   *    xFormat: "%m%d%Y"
   * },
   * axis: {
   *    x: {
   *        type: "timeseries"
   *    }
   * }
   * @see [D3's time specifier](https://d3js.org/d3-time-format#locale_format)
   */
  data_xFormat: "%Y-%m-%d",
  /**
   * Set localtime format to parse x axis.
   * @name data․xLocaltime
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * data: {
   *   xLocaltime: false
   * }
   */
  data_xLocaltime: true,
  /**
   * Sort on x axis.
   * - **NOTE:** This option works for lineish(area/line/spline/step) types only.
   * @name data․xSort
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataXSort)
   * @example
   * data: {
   *   xSort: false,
   *   x: "x",
   *   columns: [
   *     // The line graph will start to be drawn following the x axis sequence
   *     // Below data, wil start drawing x=1: 200, x=2: 300, x=3: 100
   *     ["x", 3, 1, 2],
   *     ["data1", 100, 200, 300]
   *   ]
   * }
   */
  data_xSort: true,
  /**
   * Set y axis the data related to. y and y2 can be used.
   * - **NOTE:** If all data is related to one of the axes, the domain of axis without related data will be replaced by the domain from the axis with related data
   * @name data․axes
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   axes: {
   *     data1: "y",
   *     data2: "y2"
   *   }
   * }
   */
  data_axes: {},
  /**
   * Define regions for each data.<br>
   * The values must be an array for each data and it should include an object that has `start`, `end` and `style`.
   * - The object type should be as:
   *   - start {number}: Start data point number. If not set, the start will be the first data point.
   *   - [end] {number}: End data point number. If not set, the end will be the last data point.
   *   - [style.dasharray="2 2"] {object}: The first number specifies a distance for the filled area, and the second a distance for the unfilled area.
   * - **NOTE:** Currently this option supports only line chart and dashed style. If this option specified, the line will be dashed only in the regions.
   * @name data․regions
   * @memberof Options
   * @type {object}
   * @default {}
   * @example
   * data: {
   *   regions: {
   *     data1: [{
   *         start: 1,
   *         end: 2,
   *         style: {
   *             dasharray: "5 2"
   *         }
   *     }, {
   *         start: 3
   *     }],
   *     ...
   *   }
   * }
   */
  data_regions: {},
  /**
   * Set the stacking to be normalized
   * - **NOTE:**
   *   - For stacking, '[data.groups](#.data%25E2%2580%25A4groups)' option should be set
   *   - y Axis will be set in percentage value (0 ~ 100%)
   *   - Must have postive values
   * @name data․stack․normalize
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataStackNormalized)
   * @example
   * data: {
   *   stack: {
   *      normalize: true
   *   }
   * }
   */
  data_stack_normalize: false
};
var x2 = {
  /**
   * Set clip-path attribute for x axis element
   * @name axis․x․clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo]()
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  axis_x_clipPath: true,
  /**
   * Show or hide x axis.
   * @name axis․x․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     show: false
   *   }
   * }
   */
  axis_x_show: true,
  /**
   * Set type of x axis.<br><br>
   * **Available Values:**
   * - category
   * - indexed
   * - log
   * - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the x values specified by [`data.x`](#.data%25E2%2580%25A4x)(or by any equivalent option), must be exclusively-positive.
   *   - x axis min value should be >= 0.
   *   - for 'category' type, `data.xs` option isn't supported.
   *
   * @name axis․x․type
   * @memberof Options
   * @type {string}
   * @default indexed
   * @see [Demo: indexed](https://naver.github.io/billboard.js/demo/#Chart.AreaChart)
   * @see [Demo: timeseries](https://naver.github.io/billboard.js/demo/#Chart.TimeseriesChart)
   * @see [Demo: category](https://naver.github.io/billboard.js/demo/#Data.CategoryData)
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   x: {
   *     type: "timeseries"
   *   }
   * }
   */
  axis_x_type: "indexed",
  /**
   * Set how to treat the timezone of x values.<br>
   * If true, treat x value as localtime. If false, convert to UTC internally.
   * @name axis․x․localtime
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     localtime: false
   *   }
   * }
   */
  axis_x_localtime: true,
  /**
   * Set category names on category axis.
   * This must be an array that includes category names in string. If category names are included in the date by data.x option, this is not required.
   * @name axis․x․categories
   * @memberof Options
   * @type {Array}
   * @default []
   * @example
   * axis: {
   *   x: {
   *     categories: ["Category 1", "Category 2", ...]
   *   }
   * }
   */
  axis_x_categories: [],
  /**
   * centerize ticks on category axis.
   * @name axis․x․tick․centered
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       centered: true
   *     }
   *   }
   * }
   */
  axis_x_tick_centered: false,
  /**
   * A function to format tick value. Format string is also available for timeseries data.
   * @name axis․x․tick․format
   * @memberof Options
   * @type {Function|string}
   * @default undefined
   * @see [D3's time specifier](https://d3js.org/d3-time-format#locale_format)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *        // for timeseries, a 'datetime' object is given as parameter
   *       format: function(x) {
   *           return x.getFullYear();
   *       }
   *
   *       // for category, index(Number) and categoryName(String) are given as parameter
   *       format: function(index, categoryName) {
   *           return categoryName.substr(0, 10);
   *       },
   *
   *        // for timeseries format specifier
   *        format: "%Y-%m-%d %H:%M:%S"
   *     }
   *   }
   * }
   */
  axis_x_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.x.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.x.tick.culling.max`.
   * @name axis․x․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default
   * `true` for indexed axis and timeseries axis, `false` for category axis
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_x_tick_culling: {},
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․x․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 10
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_culling_max: 10,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․x․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_culling_lines: true,
  /**
   * The number of x axis ticks to show.<br><br>
   * This option hides tick lines together with tick text. If this option is used on timeseries axis, the ticks position will be determined precisely and not nicely positioned (e.g. it will have rough second value).
   * @name axis․x․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_x_tick_count: void 0,
  /**
   * Show or hide x axis tick line.
   * @name axis․x․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_x_tick_show: true,
  /**
   * Show or hide x axis tick text.
   * @name axis․x․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_show: true,
  /**
   * Set the first/last axis tick text to be positioned inside of the chart on non-rotated axis.
   * @name axis․x․tick․text․inner
   * @memberof Options
   * @type {boolean|object}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickInner)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *          inner: true,
   *
   *          // or specify each position of the first and last tick text
   *          inner: {
   *       	   first: true,
   *       	   last: true
   *       	}
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_inner: false,
  /**
   * Set the x Axis tick text's position relatively its original position
   * @name axis․x․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_text_position: { x: 0, y: 0 },
  /**
   * Fit x axis ticks.
   * - **true**: ticks will be shown according to x value of the data points.
   * - **false**: ticks will be shown as to have same intervals.
   * @name axis․x․tick․fit
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickFitting)
   * @see [Demo: for timeseries zoom](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickTimeseries)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       fit: false
   *     }
   *   }
   * }
   */
  axis_x_tick_fit: true,
  /**
   * Set the x values of ticks manually.<br><br>
   * If this option is provided, the position of the ticks will be determined based on those values.<br>
   * This option works with `timeseries` data and the x values will be parsed accoding to the type of the value and data.xFormat option.
   * @name axis․x․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       values: [1, 2, 4, 8, 16, 32, ...],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_x_tick_values: null,
  /**
   * Rotate x axis tick text if there is not enough space for 'category' and 'timeseries' type axis.
   * - **NOTE:** The conditions where `autorotate` is enabled are:
   *   - axis.x.type='category' or 'timeseries
   *   - axis.x.tick.multiline=false
   *   - axis.x.tick.culling=false
   *   - axis.x.tick.fit=true
   * - **NOTE:** axis.x.tick.clippath=false is necessary for calculating the overflow padding between the end of x axis and the width of the SVG
   * @name axis․x․tick․autorotate
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickAutorotate)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       rotate: 15,
   *       autorotate: true,
   *       multiline: false,
   *       culling: false,
   *       fit: true
   *     },
   *     clipPath: false
   *   }
   * }
   */
  axis_x_tick_autorotate: false,
  /**
   * Rotate x axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `false`.
   * - As long as `axis_x_tick_fit` is set to `true` it will calculate an overflow for the y2 axis and add this value to the right padding.
   * @name axis․x․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.RotateXAxisTickText)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_x_tick_rotate: 0,
  /**
   * Show x axis outer tick.
   * @name axis․x․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_x_tick_outer: true,
  /**
   * Set tick text to be multiline
   * - **NOTE:**
   *  > When x tick text contains `\n`, it's used as line break and 'axis.x.tick.width' option is ignored.
   * @name axis․x․tick․multiline
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.XAxisTickMultiline)
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       multiline: false
   *     }
   *   }
   * }
   * @example
   * // example of line break with '\n'
   * // In this case, 'axis.x.tick.width' is ignored
   * data: {
   *    x: "x",
   *    columns: [
   *        ["x", "long\ntext", "Another\nLong\nText"],
   *        ...
   *    ],
   * }
   */
  axis_x_tick_multiline: true,
  /**
   * Set tick width
   * - **NOTE:**
   *  > When x tick text contains `\n`, this option is ignored.
   * @name axis․x․tick․width
   * @memberof Options
   * @type {number}
   * @default null
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       width: 50
   *     }
   *   }
   * }
   */
  axis_x_tick_width: null,
  /**
   * Set to display system tooltip(via `<title>` element) for tick text
   * - **NOTE:** Only available for category axis type (`axis.x.type='category'`)
   * @name axis․x․tick․tooltip
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   x: {
   *     tick: {
   *       tooltip: true
   *     }
   *   }
   * }
   */
  axis_x_tick_tooltip: false,
  /**
   * Set max value of x axis range.
   * @name axis․x․max
   * @memberof Options
   * @property {number} max Set the max value
   * @property {boolean} [max.fit=false] When specified `max.value` is greater than the bound data value, setting `true` will make x axis max to be fitted to the bound data max value.
   * - **NOTE:** If the bound data max value is greater than the `max.value`, the x axis max will be limited as the given `max.value`.
   * @property {number} [max.value] Set the max value
   * @example
   * axis: {
   *   x: {
   *     max: 100,
   *
   *     max: {
   *       // 'fit=true' will make x axis max to be limited as the bound data value max when 'max.value' is greater.
   *       // - when bound data max is '10' and max.value: '100' ==>  x axis max will be '10'
   *       // - when bound data max is '1000' and max.value: '100' ==> x axis max will be '100'
   *       fit: true,
   *       value: 100
   *     }
   *   }
   * }
   */
  axis_x_max: void 0,
  /**
   * Set min value of x axis range.
   * @name axis․x․min
   * @memberof Options
   * @property {number} min Set the min value
   * @property {boolean} [min.fit=false] When specified `min.value` is lower than the bound data value, setting `true` will make x axis min to be fitted to the bound data min value.
   * - **NOTE:** If the bound data min value is lower than the `min.value`, the x axis min will be limited as the given `min.value`.
   * @property {number} [min.value] Set the min value
   * @example
   * axis: {
   *   x: {
   *     min: -100,
   *
   *     min: {
   *       // 'fit=true' will make x axis min to be limited as the bound data value min when 'min.value' is lower.
   *       // - when bound data min is '-10' and min.value: '-100' ==>  x axis min will be '-10'
   *       // - when bound data min is '-1000' and min.value: '-100' ==> x axis min will be '-100'
   *       fit: true,
   *       value: -100
   *     }
   *   }
   * }
   */
  axis_x_min: void 0,
  /**
   * Change the direction of x axis.<br><br>
   * If true set, the direction will be `right -> left`.
   * @name axis․x․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   x: {
   *     inverted: true
   *   }
   * }
   */
  axis_x_inverted: false,
  /**
   * Set padding for x axis.<br><br>
   * If this option is set, the range of x axis will increase/decrease according to the values.
   * If no padding is needed in the rage of x axis, 0 should be set.
   * By default, left/right padding are set depending on x axis type or chart types.
   * - **NOTE:**
   *   - The meaning of padding values, differs according axis types:<br>
   *     - **category/indexed:** The unit of tick value
   *       ex. the given value `1`, is same as the width of 1 tick width
   *     - **timeseries:** Numeric time value
   *       ex. the given value `1000*60*60*24`, which is numeric time equivalent of a day, is same as the width of 1 tick width
   *   - If want values to be treated as pixels, specify `unit:"px"`.
   *     - The pixel value will be convered based on the scale values. Hence can not reflect accurate padding result.
   * @name axis․x․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   x: {
   *     padding: {
   *       // when axis type is 'category'
   *       left: 1,  // set left padding width of equivalent value of a tick's width
   *       right: 0.5  // set right padding width as half of equivalent value of tick's width
   *
   *       // when axis type is 'timeseries'
   *       left: 1000*60*60*24,  // set left padding width of equivalent value of a day tick's width
   *       right: 1000*60*60*12   // set right padding width as half of equivalent value of a day tick's width
   *     },
   *
   *     // or set both values at once.
   *     padding: 10,
   *
   *     // or set padding values as pixel unit.
   *     padding: {
   *       left: 100,
   *       right: 50,
   *       unit: "px"
   *     },
   *   }
   * }
   */
  axis_x_padding: {},
  /**
   * Set height of x axis.<br><br>
   * The height of x axis can be set manually by this option. If you need more space for x axis, please use this option for that. The unit is pixel.
   * @name axis․x․height
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     height: 20
   *   }
   * }
   */
  axis_x_height: void 0,
  /**
   * Set default extent for subchart and zoom. This can be an array or function that returns an array.
   * @name axis․x․extent
   * @memberof Options
   * @type {Array|Function}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     // extent range as a pixel value
   *     extent: [0, 200],
   *
   *     // when axis is 'timeseries', parsable datetime string
   *     extent: ["2019-03-01", "2019-03-05"],
   *
   *     // return extent value
   *     extent: function(domain, scale) {
   *    	 var extent = domain.map(function(v) {
   *     	    return scale(v);
   *     	 });
   *
   *   	 // it should return a format of array
   *   	 // ex) [0, 584]
   *     	 return extent;
   *     }
   *   }
   * }
   */
  axis_x_extent: void 0,
  /**
   * Set label on x axis.<br><br>
   * You can set x axis label and change its position by this option.
   * `string` and `object` can be passed and we can change the poisiton by passing object that has position key.<br>
   * Available position differs according to the axis direction (vertical or horizontal).
   * If string set, the position will be the default.
   *
   *  - **If it's horizontal axis:**
   *    - inner-right [default]
   *    - inner-center
   *    - inner-left
   *    - outer-right
   *    - outer-center
   *    - outer-left
   *  - **If it's vertical axis:**
   *    - inner-top [default]
   *    - inner-middle
   *    - inner-bottom
   *    - outer-top
   *    - outer-middle
   *    - outer-bottom
   * @name axis․x․label
   * @memberof Options
   * @type {string|object}
   * @default undefined
   * @example
   * axis: {
   *   x: {
   *     label: "Your X Axis"
   *   }
   * }
   *
   * axis: {
   *   x: {
   *     label: {
   *        text: "Your X Axis",
   *        position: "outer-center"
   *     }
   *   }
   * }
   */
  axis_x_label: {},
  /**
   * Set additional axes for x Axis.
   * - **NOTE:** Axis' scale is based on x Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․x․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * x: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main x Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_x_axes: []
};
var y2 = {
  /**
   * Set clip-path attribute for y axis element
   * - **NOTE**: `clip-path` attribute for y Axis is set only when `axis.y.inner` option is true.
   * @name axis․y․clipPath
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * // don't set 'clip-path' attribute
   * clipPath: false
   */
  axis_y_clipPath: true,
  /**
   * Show or hide y axis.
   * @name axis․y․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     show: false
   *   }
   * }
   */
  axis_y_show: true,
  /**
   * Set type of y axis.<br><br>
   * **Available Values:**
   *  - indexed
   *  - log
   *  - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the bound data values must be exclusively-positive.
   *   - y axis min value should be >= 0.
   *   - [`data.groups`](#.data%25E2%2580%25A4groups)(stacked data) option aren't supported.
   *
   * @name axis․y․type
   * @memberof Options
   * @type {string}
   * @default "indexed"
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   y: {
   *     type: "log"
   *   }
   * }
   */
  axis_y_type: "indexed",
  /**
   * Set max value of y axis.
   * - **NOTE:** Padding will be added based on this value, so if you don't need the padding, please set axis.y.padding to disable it (e.g. axis.y.padding = 0).
   * @name axis․y․max
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     max: 1000
   *   }
   * }
   */
  axis_y_max: void 0,
  /**
   * Set min value of y axis.
   * - **NOTE:**
   *   Padding will be added based on this value, so if you don't need the padding, please set axis.y.padding to disable it (e.g. axis.y.padding = 0).
   * @name axis․y․min
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     min: 1000
   *   }
   * }
   */
  axis_y_min: void 0,
  /**
   * Change the direction of y axis.<br><br>
   * If true set, the direction will be `top -> bottom`.
   * @name axis․y․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   y: {
   *     inverted: true
   *   }
   * }
   */
  axis_y_inverted: false,
  /**
   * Set center value of y axis.
   * @name axis․y․center
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     center: 0
   *   }
   * }
   */
  axis_y_center: void 0,
  /**
   * Show y axis inside of the chart.
   * @name axis․y․inner
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y: {
   *     inner: true
   *   }
   * }
   */
  axis_y_inner: false,
  /**
   * Set label on y axis.<br><br>
   * You can set y axis label and change its position by this option. This option works in the same way as [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label).
   * @name axis․y․label
   * @memberof Options
   * @type {string|object}
   * @default {}
   * @see [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label) for position string value.
   * @example
   * axis: {
   *   y: {
   *     label: "Your Y Axis"
   *   }
   * }
   *
   * axis: {
   *   y: {
   *     label: {
   *        text: "Your Y Axis",
   *        position: "outer-middle"
   *     }
   *   }
   * }
   */
  axis_y_label: {},
  /**
   * Set formatter for y axis tick text.<br><br>
   * This option accepts d3.format object as well as a function you define.
   * @name axis․y․tick․format
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       format: function(x) {
   *           return x.getFullYear();
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.y.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.y.tick.culling.max`.
   * @name axis․y․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_y_tick_culling: false,
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․y․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 5
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_culling_max: 5,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․y․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_culling_lines: true,
  /**
   * Show y axis outer tick.
   * @name axis․y․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_y_tick_outer: true,
  /**
   * Set y axis tick values manually.
   * @name axis․y․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       values: [100, 1000, 10000],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_values: null,
  /**
   * Rotate y axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `true`.
   * @name axis․y․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_y_tick_rotate: 0,
  /**
   * Set the number of y axis ticks.<br><br>
   * - **NOTE:** The position of the ticks will be calculated precisely, so the values on the ticks will not be rounded nicely. In the case, axis.y.tick.format or axis.y.tick.values will be helpful.
   * @name axis․y․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_y_tick_count: void 0,
  /**
   * Show or hide y axis tick line.
   * @name axis․y․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_y_tick_show: true,
  /**
   * Set axis tick step(interval) size.
   * - **NOTE:** Will be ignored if `axis.y.tick.count` or `axis.y.tick.values` options are set.
   * @name axis․y․tick․stepSize
   * @memberof Options
   * @type {number}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.StepSizeForYAxis)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       // tick value will step as indicated interval value.
   *       // ex) 'stepSize=15' ==> [0, 15, 30, 45, 60]
   *       stepSize: 15
   *     }
   *   }
   * }
   */
  axis_y_tick_stepSize: null,
  /**
   * Show or hide y axis tick text.
   * @name axis․y․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_text_show: true,
  /**
   * Set the y Axis tick text's position relatively its original position
   * @name axis․y․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_y_tick_text_position: { x: 0, y: 0 },
  /**
   * Set the number of y axis ticks.<br><br>
   * - **NOTE:** The position of the ticks will be calculated precisely, so the values on the ticks will not be rounded nicely. In the case, axis.y.tick.format or axis.y.tick.values will be helpful.
   * @name axis․y․tick․time
   * @memberof Options
   * @private
   * @type {object}
   * @property {object} time time object
   * @property {Function} [time.value] D3's time interval function (https://github.com/d3/d3-time#intervals)
   * @example
   * axis: {
   *   y: {
   *     tick: {
   *       time: {
   *          // ticks at 15-minute intervals
   *          // https://github.com/d3/d3-scale/blob/master/README.md#time_ticks
   *          value: d3.timeMinute.every(15)
   *       }
   *     }
   *   }
   * }
   */
  // @TODO: not fully implemented yet
  axis_y_tick_time_value: void 0,
  /**
   * Set padding for y axis.<br><br>
   * You can set padding for y axis to create more space on the edge of the axis.
   * This option accepts object and it can include top and bottom. top, bottom will be treated as pixels.
   *
   * - **NOTE:**
   *   - Given values are translated relative to the y Axis domain value for padding
   *   - For area and bar type charts, [area.zerobased](#.area) or [bar.zerobased](#.bar) options should be set to 'false` to get padded bottom.
   * @name axis․y․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   y: {
   *     padding: {
   *       top: 0,
   *       bottom: 0
   *     },
   *
   *     // or set both values at once.
   *     padding: 10
   *   }
   * }
   */
  axis_y_padding: {},
  /**
   * Set default range of y axis.<br><br>
   * This option set the default value for y axis when there is no data on init.
   * @name axis․y․default
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @example
   * axis: {
   *   y: {
   *     default: [0, 1000]
   *   }
   * }
   */
  axis_y_default: void 0,
  /**
   * Set additional axes for y Axis.
   * - **NOTE:** Axis' scale is based on y Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․y․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * y: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main y Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_y_axes: []
};
var y22 = {
  /**
   * Show or hide y2 axis.
   * - **NOTE**:
   *   - When set to `false` will not generate y2 axis node. In this case, all 'y2' axis related functionality won't work properly.
   *   - If need to use 'y2' related options while y2 isn't visible, set the value `true` and control visibility by css display property.
   * @name axis․y2․show
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     show: true
   *   }
   * }
   */
  axis_y2_show: false,
  /**
   * Set type of y2 axis.<br><br>
   * **Available Values:**
   *  - indexed
   *  - log
   *  - timeseries
   *
   * **NOTE:**<br>
   * - **log** type:
   *   - the bound data values must be exclusively-positive.
   *   - y2 axis min value should be >= 0.
   *   - [`data.groups`](#.data%25E2%2580%25A4groups)(stacked data) option aren't supported.
   *
   * @name axis․y2․type
   * @memberof Options
   * @type {string}
   * @default "indexed"
   * @see [Demo: log](https://naver.github.io/billboard.js/demo/#Axis.LogScales)
   * @example
   * axis: {
   *   y2: {
   *     type: "indexed"
   *   }
   * }
   */
  axis_y2_type: "indexed",
  /**
   * Set max value of y2 axis.
   * @name axis․y2․max
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     max: 1000
   *   }
   * }
   */
  axis_y2_max: void 0,
  /**
   * Set min value of y2 axis.
   * @name axis․y2․min
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     min: -1000
   *   }
   * }
   */
  axis_y2_min: void 0,
  /**
   * Change the direction of y2 axis.<br><br>
   * If true set, the direction will be `top -> bottom`.
   * @name axis․y2․inverted
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.InvertedAxis)
   * @example
   * axis: {
   *   y2: {
   *     inverted: true
   *   }
   * }
   */
  axis_y2_inverted: false,
  /**
   * Set center value of y2 axis.
   * @name axis․y2․center
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     center: 0
   *   }
   * }
   */
  axis_y2_center: void 0,
  /**
   * Show y2 axis inside of the chart.
   * @name axis․y2․inner
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     inner: true
   *   }
   * }
   */
  axis_y2_inner: false,
  /**
   * Set label on y2 axis.<br><br>
   * You can set y2 axis label and change its position by this option. This option works in the same way as [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label).
   * @name axis․y2․label
   * @memberof Options
   * @type {string|object}
   * @default {}
   * @see [axis.x.label](#.axis%25E2%2580%25A4x%25E2%2580%25A4label) for position string value.
   * @example
   * axis: {
   *   y2: {
   *     label: "Your Y2 Axis"
   *   }
   * }
   *
   * axis: {
   *   y2: {
   *     label: {
   *        text: "Your Y2 Axis",
   *        position: "outer-middle"
   *     }
   *   }
   * }
   */
  axis_y2_label: {},
  /**
   * Set formatter for y2 axis tick text.<br><br>
   * This option works in the same way as axis.y.format.
   * @name axis․y2․tick․format
   * @memberof Options
   * @type {Function}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       format: d3.format("$,")
   *       //or format: function(d) { return "$" + d; }
   *     }
   *   }
   * }
   */
  axis_y2_tick_format: void 0,
  /**
   * Setting for culling ticks.
   * - `true`: the ticks will be culled, then only limited tick text will be shown.<br>
   *   This option does not hide the tick lines by default, if want to hide tick lines, set `axis.y2.tick.culling.lines=false`.
   * - `false`: all of ticks will be shown.<br><br>
   * The number of ticks to be shown can be chaned by `axis.y2.tick.culling.max`.
   * @name axis․y2․tick․culling
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling: false,
  /**
   * The number of tick texts will be adjusted to less than this value.
   * @name axis․y2․tick․culling․max
   * @memberof Options
   * @type {number}
   * @default 5
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: {
   *           max: 5
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling_max: 5,
  /**
   * Control visibility of tick lines within culling option, along with tick text.
   * @name axis․y2․tick․culling․lines
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       culling: {
   *           lines: false,
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_culling_lines: true,
  /**
   * Show or hide y2 axis outer tick.
   * @name axis․y2․tick․outer
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       outer: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_outer: true,
  /**
   * Set y2 axis tick values manually.
   * @name axis․y2․tick․values
   * @memberof Options
   * @type {Array|Function}
   * @default null
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       values: [100, 1000, 10000],
   *
   *       // an Array value should be returned
   *       values: function() {
   *       	return [ ... ];
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_values: null,
  /**
   * Rotate y2 axis tick text.
   * - If you set negative value, it will rotate to opposite direction.
   * - Applied when [`axis.rotated`](#.axis%25E2%2580%25A4rotated) option is `true`.
   * @name axis․y2․tick․rotate
   * @memberof Options
   * @type {number}
   * @default 0
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       rotate: 60
   *     }
   *   }
   * }
   */
  axis_y2_tick_rotate: 0,
  /**
   * Set the number of y2 axis ticks.
   * - **NOTE:** This works in the same way as axis.y.tick.count.
   * @name axis․y2․tick․count
   * @memberof Options
   * @type {number}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       count: 5
   *     }
   *   }
   * }
   */
  axis_y2_tick_count: void 0,
  /**
   * Show or hide y2 axis tick line.
   * @name axis․y2․tick․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       show: false
   *     }
   *   }
   * }
   */
  axis_y2_tick_show: true,
  /**
   * Set axis tick step(interval) size.
   * - **NOTE:** Will be ignored if `axis.y2.tick.count` or `axis.y2.tick.values` options are set.
   * @name axis․y2․tick․stepSize
   * @memberof Options
   * @type {number}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.StepSizeForYAxis)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       // tick value will step as indicated interval value.
   *       // ex) 'stepSize=15' ==> [0, 15, 30, 45, 60]
   *       stepSize: 15
   *     }
   *   }
   * }
   */
  axis_y2_tick_stepSize: null,
  /**
   * Show or hide y2 axis tick text.
   * @name axis․y2․tick․text․show
   * @memberof Options
   * @type {boolean}
   * @default true
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.HideTickLineText)
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       text: {
   *           show: false
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_text_show: true,
  /**
   * Set the y2 Axis tick text's position relatively its original position
   * @name axis․y2․tick․text․position
   * @memberof Options
   * @type {object}
   * @default {x: 0, y:0}
   * @example
   * axis: {
   *   y2: {
   *     tick: {
   *       text: {
   *         position: {
   *           x: 10,
   *           y: 10
   *         }
   *       }
   *     }
   *   }
   * }
   */
  axis_y2_tick_text_position: { x: 0, y: 0 },
  /**
   * Set padding for y2 axis.<br><br>
   * You can set padding for y2 axis to create more space on the edge of the axis.
   * This option accepts object and it can include top and bottom. top, bottom will be treated as pixels.
   *
   * - **NOTE:**
   *   - Given values are translated relative to the y2 Axis domain value for padding
   *   - For area and bar type charts, [area.zerobased](#.area) or [bar.zerobased](#.bar) options should be set to 'false` to get padded bottom.
   * @name axis․y2․padding
   * @memberof Options
   * @type {object|number}
   * @default {}
   * @example
   * axis: {
   *   y2: {
   *     padding: {
   *       top: 100,
   *       bottom: 100
   *     }
   *
   *     // or set both values at once.
   *     padding: 10
   * }
   */
  axis_y2_padding: {},
  /**
   * Set default range of y2 axis.<br><br>
   * This option set the default value for y2 axis when there is no data on init.
   * @name axis․y2․default
   * @memberof Options
   * @type {Array}
   * @default undefined
   * @example
   * axis: {
   *   y2: {
   *     default: [0, 1000]
   *   }
   * }
   */
  axis_y2_default: void 0,
  /**
   * Set additional axes for y2 Axis.
   * - **NOTE:** Axis' scale is based on y2 Axis value if domain option isn't set.
   *
   * Each axis object should consist with following options:
   *
   * | Name | Type | Default | Description |
   * | --- | --- | --- | --- |
   * | domain | Array | - | Set the domain value |
   * | tick.outer | boolean | true | Show outer tick |
   * | tick.format | Function | - | Set formatter for tick text |
   * | tick.count | Number | - | Set the number of y axis ticks |
   * | tick.values | Array | - | Set tick values manually |
   * @name axis․y2․axes
   * @memberof Options
   * @type {Array}
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Axis.MultiAxes)
   * @see [Demo: Domain](https://naver.github.io/billboard.js/demo/#Axis.MultiAxesDomain)
   * @example
   * y2: {
   *    axes: [
   *      {
   *        // if set, will not be correlated with the main y2 Axis domain value
   *        domain: [0, 1000],
   *        tick: {
   *          outer: false,
   *          format: function(x) {
   *             return x + "%";
   *          },
   *          count: 2,
   *          values: [10, 20, 30]
   *        }
   *      },
   *      ...
   *    ]
   * }
   */
  axis_y2_axes: []
};
var optAxis = _assign(_assign(_assign({
  /**
   * Switch x and y axis position.
   * @name axis․rotated
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * axis: {
   *   rotated: true
   * }
   */
  axis_rotated: false
}, x2), y2), y22);
var optGrid = {
  /**
   * Set related options
   * @name grid
   * @memberof Options
   * @type {object}
   * @property {boolean} [front=false] Set 'grid & focus lines' to be positioned over grid lines and chart elements.
   * @property {object} x Grid x object
   * @property {boolean} [x.show=false] Show grids along x axis.
   * @property {Array} [x.lines=[]] Show additional grid lines along x axis.<br>
   *  This option accepts array including object that has value, text, position and class. text, position and class are optional. For position, start, middle and end (default) are available.
   *  If x axis is category axis, value can be category name. If x axis is timeseries axis, value can be date string, Date object and unixtime integer.
   * @property {object} y Grid y object
   * @property {boolean} [y.show=false] Show grids along x axis.
   * @property {Array} [y.lines=[]] Show additional grid lines along y axis.<br>
   *  This option accepts array including object that has value, text, position and class.
   * @property {number} [y.ticks=undefined] Number of y grids to be shown.
   * @property {object} focus Grid focus object
   * @property {boolean} [focus.edge=false] Show edged focus grid line.<br>**NOTE:** Available when [`tooltip.grouped=false`](#.tooltip) option is set.
   * @property {boolean} [focus.show=true] Show grid line when focus.
   * @property {boolean} [focus.y=false] Show y coordinate focus grid line.<br>**NOTE:** Available when [`tooltip.grouped=false`](#.tooltip) option is set.
   * @property {object} lines Grid lines object
   * @property {boolean} [lines.front=true] Set grid lines to be positioned over chart elements.
   * @default undefined
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Grid.GridLines)
   * @see [Demo: X Grid Lines](https://naver.github.io/billboard.js/demo/#Grid.OptionalXGridLines)
   * @see [Demo: Y Grid Lines](https://naver.github.io/billboard.js/demo/#Grid.OptionalYGridLines)
   * @example
   * grid: {
   *   x: {
   *     show: true,
   *     lines: [
   *       {value: 2, text: "Label on 2"},
   *       {value: 5, text: "Label on 5", class: "label-5"},
   *       {value: 6, text: "Label on 6", position: "start"}
   *     ]
   *   },
   *   y: {
   *     show: true,
   *     lines: [
   *       {value: 100, text: "Label on 100"},
   *       {value: 200, text: "Label on 200", class: "label-200"},
   *       {value: 300, text: "Label on 300", position: 'middle'}
   *     ],
   *     ticks: 5
   *   },
   *   front: true,
   *   focus: {
   *      show: false,
   *
   *      // Below options are available when 'tooltip.grouped=false' option is set
   *      edge: true,
   *      y: true
   *   },
   *   lines: {
   *      front: false
   *   }
   * }
   */
  grid_x_show: false,
  grid_x_type: "tick",
  grid_x_lines: [],
  grid_y_show: false,
  grid_y_lines: [],
  grid_y_ticks: void 0,
  grid_focus_edge: false,
  grid_focus_show: true,
  grid_focus_y: false,
  grid_front: false,
  grid_lines_front: true
};
var api = [
  apiAxis,
  apiCategory,
  apiFlow,
  apiGrid,
  apiGroup,
  apiRegion,
  apiX
];
var internal = {
  axis: axis2,
  clip,
  eventrect,
  flow,
  grid,
  region,
  sizeAxis
};
var options = {
  optDataAxis,
  optAxis,
  optGrid
};
function getRadiusFn(expandRate) {
  if (expandRate === void 0) {
    expandRate = 0;
  }
  var $$ = this;
  var config = $$.config, state = $$.state;
  var hasMultiArcGauge = $$.hasMultiArcGauge();
  var singleArcWidth = state.gaugeArcWidth / $$.filterTargetsToShow($$.data.targets).length;
  var expandWidth = expandRate ? Math.min(state.radiusExpanded * expandRate - state.radius, singleArcWidth * 0.8 - (1 - expandRate) * 100) : 0;
  return {
    /**
     * Getter of arc innerRadius value
     * @param {IArcData} d Data object
     * @returns {number} innerRadius value
     * @private
     */
    inner: function(d) {
      var innerRadius = $$.getRadius(d).innerRadius;
      return hasMultiArcGauge ? state.radius - singleArcWidth * (d.index + 1) : isNumber(innerRadius) ? innerRadius : 0;
    },
    /**
     * Getter of arc outerRadius value
     * @param {IArcData} d Data object
     * @returns {number} outerRadius value
     * @private
     */
    outer: function(d) {
      var outerRadius = $$.getRadius(d).outerRadius;
      var radius;
      if (hasMultiArcGauge) {
        radius = state.radius - singleArcWidth * d.index + expandWidth;
      } else if ($$.hasType("polar") && !expandRate) {
        radius = $$.getPolarOuterRadius(d, outerRadius);
      } else {
        radius = outerRadius;
        if (expandRate) {
          var radiusExpanded = state.radiusExpanded;
          if (state.radius !== outerRadius) {
            radiusExpanded -= Math.abs(state.radius - outerRadius);
          }
          radius = radiusExpanded * expandRate;
        }
      }
      return radius;
    },
    /**
     * Getter of arc cornerRadius value
     * @param {IArcData} d Data object
     * @param {number} outerRadius outer radius value
     * @returns {number} cornerRadius value
     * @private
     */
    corner: function(d, outerRadius) {
      var _a2 = config.arc_cornerRadius_ratio, ratio = _a2 === void 0 ? 0 : _a2, _b = config.arc_cornerRadius, cornerRadius = _b === void 0 ? 0 : _b;
      var id2 = d.data.id, value = d.value;
      var corner = 0;
      if (ratio) {
        corner = ratio * outerRadius;
      } else {
        corner = isNumber(cornerRadius) ? cornerRadius : cornerRadius.call($$.api, id2, value, outerRadius);
      }
      return corner;
    }
  };
}
function getAttrTweenFn(fn) {
  return function(d) {
    var getAngleKeyValue = function(_a2) {
      var _b = _a2.startAngle, startAngle = _b === void 0 ? 0 : _b, _c = _a2.endAngle, endAngle = _c === void 0 ? 0 : _c, _d = _a2.padAngle, padAngle = _d === void 0 ? 0 : _d;
      return {
        startAngle,
        endAngle,
        padAngle
      };
    };
    var interpolate$1 = value_default(getAngleKeyValue(this._current), getAngleKeyValue(d));
    this._current = d;
    return function(t) {
      var interpolated = interpolate$1(t);
      var data2 = d.data, index2 = d.index, value = d.value;
      return fn(_assign(_assign({}, interpolated), { data: data2, index: index2, value }));
    };
  };
}
var shapeArc = {
  initPie: function() {
    var $$ = this;
    var config = $$.config;
    var dataType = config.data_type;
    var padding = config["".concat(dataType, "_padding")];
    var startingAngle = config["".concat(dataType, "_startingAngle")] || 0;
    var padAngle = (padding ? padding * 0.01 : config["".concat(dataType, "_padAngle")]) || 0;
    $$.pie = pie_default().startAngle(startingAngle).endAngle(startingAngle + 2 * Math.PI).padAngle(padAngle).value(function(d) {
      var _a2, _b;
      return (_b = (_a2 = d.values) === null || _a2 === void 0 ? void 0 : _a2.reduce(function(a2, b) {
        return a2 + b.value;
      }, 0)) !== null && _b !== void 0 ? _b : d;
    }).sort($$.getSortCompareFn.bind($$)(true));
  },
  updateRadius: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var dataType = config.data_type;
    var padding = config["".concat(dataType, "_padding")];
    var w = config.gauge_width || config.donut_width;
    var gaugeArcWidth = $$.filterTargetsToShow($$.data.targets).length * config.gauge_arcs_minWidth;
    state.radiusExpanded = Math.min(state.arcWidth, state.arcHeight) / 2 * ($$.hasMultiArcGauge() && config.gauge_label_show ? 0.85 : 1);
    state.radius = state.radiusExpanded * 0.95;
    state.innerRadiusRatio = w ? (state.radius - w) / state.radius : 0.6;
    state.gaugeArcWidth = w || (gaugeArcWidth <= state.radius - state.innerRadius ? state.radius - state.innerRadius : gaugeArcWidth <= state.radius ? gaugeArcWidth : state.radius);
    var innerRadius = config.pie_innerRadius || (padding ? padding * (state.innerRadiusRatio + 0.1) : 0);
    state.outerRadius = config.pie_outerRadius;
    state.innerRadius = $$.hasType("donut") || $$.hasType("gauge") ? state.radius * state.innerRadiusRatio : innerRadius;
  },
  /**
   * Get pie's inner & outer radius value
   * @param {object|undefined} d Data object
   * @returns {object}
   * @private
   */
  getRadius: function(d) {
    var $$ = this;
    var data2 = d === null || d === void 0 ? void 0 : d.data;
    var _a2 = $$.state, innerRadius = _a2.innerRadius, outerRadius = _a2.outerRadius;
    if (!isNumber(innerRadius) && data2) {
      innerRadius = innerRadius[data2.id] || 0;
    }
    if (isObject(outerRadius) && data2 && data2.id in outerRadius) {
      outerRadius = outerRadius[data2.id];
    } else if (!isNumber(outerRadius)) {
      outerRadius = $$.state.radius;
    }
    return { innerRadius, outerRadius };
  },
  updateArc: function() {
    var $$ = this;
    $$.updateRadius();
    $$.svgArc = $$.getSvgArc();
    $$.svgArcExpanded = $$.getSvgArcExpanded();
  },
  getArcLength: function() {
    var $$ = this;
    var config = $$.config;
    var arcLengthInPercent = config.gauge_arcLength * 3.6;
    var len = 2 * (arcLengthInPercent / 360);
    if (arcLengthInPercent < -360) {
      len = -2;
    } else if (arcLengthInPercent > 360) {
      len = 2;
    }
    return len * Math.PI;
  },
  getStartingAngle: function() {
    var $$ = this;
    var config = $$.config;
    var dataType = config.data_type;
    var isFullCircle = $$.hasType("gauge") ? config.gauge_fullCircle : false;
    var defaultStartAngle = -1 * Math.PI / 2;
    var defaultEndAngle = Math.PI / 2;
    var startAngle = config["".concat(dataType, "_startingAngle")] || 0;
    if (!isFullCircle && startAngle <= defaultStartAngle) {
      startAngle = defaultStartAngle;
    } else if (!isFullCircle && startAngle >= defaultEndAngle) {
      startAngle = defaultEndAngle;
    } else if (startAngle > Math.PI || startAngle < -1 * Math.PI) {
      startAngle = Math.PI;
    }
    return startAngle;
  },
  /**
   * Update angle data
   * @param {object} dValue Data object
   * @param {boolean} forRange Weather is for ranged text option(arc.rangeText.values)
   * @returns {object|null} Updated angle data
   * @private
   */
  updateAngle: function(dValue, forRange) {
    var _a2;
    if (forRange === void 0) {
      forRange = false;
    }
    var $$ = this;
    var config = $$.config, state = $$.state;
    var hasGauge = forRange && $$.hasType("gauge");
    var pie2 = $$.pie;
    var d = dValue;
    var found = false;
    if (!config) {
      return null;
    }
    var gStart = $$.getStartingAngle();
    var radius = config.gauge_fullCircle || forRange && !hasGauge ? $$.getArcLength() : gStart * -2;
    if (d.data && $$.isGaugeType(d.data) && !$$.hasMultiArcGauge()) {
      var gMin = config.gauge_min, gMax = config.gauge_max;
      var totalSum = $$.getTotalDataSum(state.rendered);
      var gEnd = radius * ((totalSum - gMin) / (gMax - gMin));
      pie2 = pie2.startAngle(gStart).endAngle(gEnd + gStart);
    }
    if (forRange === false) {
      pie2($$.filterTargetsToShow()).forEach(function(t, i) {
        var _a3;
        if (!found && t.data.id === ((_a3 = d.data) === null || _a3 === void 0 ? void 0 : _a3.id)) {
          found = true;
          d = t;
          d.index = i;
        }
      });
    }
    if (isNaN(d.startAngle)) {
      d.startAngle = 0;
    }
    if (isNaN(d.endAngle)) {
      d.endAngle = d.startAngle;
    }
    if (forRange || d.data && (config.gauge_enforceMinMax || $$.hasMultiArcGauge())) {
      var gMin = config.gauge_min, gMax = config.gauge_max;
      var max4 = forRange && !hasGauge ? $$.getTotalDataSum(state.rendered) : gMax;
      var gTic = radius / (max4 - gMin);
      var value = (_a2 = d.value) !== null && _a2 !== void 0 ? _a2 : 0;
      var gValue = value < gMin ? 0 : value < max4 ? value - gMin : max4 - gMin;
      d.startAngle = gStart;
      d.endAngle = gStart + gTic * gValue;
    }
    return found || forRange ? d : null;
  },
  getSvgArc: function() {
    var $$ = this;
    var _a2 = getRadiusFn.call($$), inner = _a2.inner, outer = _a2.outer, corner = _a2.corner;
    var arc$1 = arc_default().innerRadius(inner).outerRadius(outer);
    var newArc = function(d, withoutUpdate) {
      var _a3;
      var path2 = "M 0 0";
      if (d.value || d.data) {
        var data2 = withoutUpdate ? d : (_a3 = $$.updateAngle(d)) !== null && _a3 !== void 0 ? _a3 : null;
        if (data2) {
          path2 = arc$1.cornerRadius(corner(data2, outer(data2)))(data2);
        }
      }
      return path2;
    };
    newArc.centroid = arc$1.centroid;
    return newArc;
  },
  /**
   * Get expanded arc path function
   * @param {number} rate Expand rate
   * @returns {Function} Expanded arc path getter function
   * @private
   */
  getSvgArcExpanded: function(rate) {
    if (rate === void 0) {
      rate = 1;
    }
    var $$ = this;
    var _a2 = getRadiusFn.call($$, rate), inner = _a2.inner, outer = _a2.outer, corner = _a2.corner;
    var arc$1 = arc_default().innerRadius(inner).outerRadius(outer);
    return function(d) {
      var updated = $$.updateAngle(d);
      var outerR = outer(updated);
      var cornerR = 0;
      if (updated) {
        cornerR = corner(updated, outerR);
      }
      return updated ? arc$1.cornerRadius(cornerR)(updated) : "M 0 0";
    };
  },
  getArc: function(d, withoutUpdate, force) {
    return force || this.isArcType(d.data) ? this.svgArc(d, withoutUpdate) : "M 0 0";
  },
  /**
   * Render range value text
   * @private
   */
  redrawArcRangeText: function() {
    var $$ = this;
    var config = $$.config, arcs = $$.$el.arcs, state = $$.state, $T = $$.$T;
    var format3 = config.arc_rangeText_format;
    var fixed = $$.hasType("gauge") && config.arc_rangeText_fixed;
    var values = config.arc_rangeText_values;
    if (values === null || values === void 0 ? void 0 : values.length) {
      var isPercent_1 = config.arc_rangeText_unit === "%";
      var totalSum_1 = $$.getTotalDataSum(state.rendered);
      if (isPercent_1) {
        values = values.map(function(v) {
          return totalSum_1 / 100 * v;
        });
      }
      var pieData_1 = $$.pie(values).map(function(d, i) {
        return d.index = i, d;
      });
      var rangeText = arcs.selectAll(".".concat($ARC.arcRange)).data(values);
      rangeText.exit();
      rangeText = $T(rangeText.enter().append("text").attr("class", $ARC.arcRange).style("text-anchor", "middle").style("pointer-events", "none").style("opacity", "0").text(function(v) {
        var range2 = isPercent_1 ? v / totalSum_1 * 100 : v;
        return isFunction(format3) ? format3(range2) : "".concat(range2).concat(isPercent_1 ? "%" : "");
      }).merge(rangeText));
      if ((!state.rendered || state.rendered && !fixed) && totalSum_1 > 0) {
        rangeText.attr("transform", function(d, i) {
          return $$.transformForArcLabel(pieData_1[i], true);
        });
      }
      rangeText.style("opacity", function(d) {
        return !fixed && (d > totalSum_1 || totalSum_1 === 0) ? "0" : null;
      });
    }
  },
  /**
   * Set transform attributes to arc label text
   * @param {object} d Data object
   * @param {boolean} forRange Weather is for ranged text option(arc.rangeText.values)
   * @returns {string} Translate attribute string
   * @private
   */
  transformForArcLabel: function(d, forRange) {
    var _a2, _b, _c;
    if (forRange === void 0) {
      forRange = false;
    }
    var $$ = this;
    var config = $$.config, radiusExpanded = $$.state.radiusExpanded;
    var updated = $$.updateAngle(d, forRange);
    var translate = "";
    if (updated) {
      if (forRange || $$.hasMultiArcGauge()) {
        var y1 = Math.sin(updated.endAngle - Math.PI / 2);
        var rangeTextPosition = config.arc_rangeText_position;
        var x3 = Math.cos(updated.endAngle - Math.PI / 2) * (radiusExpanded + (forRange ? 5 : 25));
        var y3 = y1 * (radiusExpanded + 15 - Math.abs(y1 * 10)) + 3;
        if (forRange && rangeTextPosition) {
          var rangeValues = config.arc_rangeText_values;
          var pos = isFunction(rangeTextPosition) ? rangeTextPosition(rangeValues[d.index]) : rangeTextPosition;
          x3 += (_a2 = pos === null || pos === void 0 ? void 0 : pos.x) !== null && _a2 !== void 0 ? _a2 : 0;
          y3 += (_b = pos === null || pos === void 0 ? void 0 : pos.y) !== null && _b !== void 0 ? _b : 0;
        }
        translate = "translate(".concat(x3, ",").concat(y3, ")");
      } else if (!$$.hasType("gauge") || $$.data.targets.length > 1) {
        var outerRadius = $$.getRadius(d).outerRadius;
        if ($$.hasType("polar")) {
          outerRadius = $$.getPolarOuterRadius(d, outerRadius);
        }
        var c = this.svgArc.centroid(updated);
        var _d = c.map(function(v) {
          return isNaN(v) ? 0 : v;
        }), x3 = _d[0], y3 = _d[1];
        var h = Math.sqrt(x3 * x3 + y3 * y3);
        var ratio = (_c = ["donut", "gauge", "pie", "polar"].filter($$.hasType.bind($$)).map(function(v) {
          return config["".concat(v, "_label_ratio")];
        })) === null || _c === void 0 ? void 0 : _c[0];
        if (ratio) {
          ratio = isFunction(ratio) ? ratio.bind($$.api)(d, outerRadius, h) : ratio;
        } else {
          ratio = outerRadius && (h ? (36 / outerRadius > 0.375 ? 1.175 - 36 / outerRadius : 0.8) * outerRadius / h : 0);
        }
        translate = "translate(".concat(x3 * ratio, ",").concat(y3 * ratio, ")");
      }
    }
    return translate;
  },
  convertToArcData: function(d) {
    return this.addName({
      id: "data" in d ? d.data.id : d.id,
      value: d.value,
      ratio: this.getRatio("arc", d),
      index: d.index
    });
  },
  textForArcLabel: function(selection3) {
    var $$ = this;
    var hasGauge = $$.hasType("gauge");
    if ($$.shouldShowArcLabel()) {
      selection3.style("fill", $$.updateTextColor.bind($$)).attr("filter", $$.updateTextBackgroundColor.bind($$)).each(function(d) {
        var _a2;
        var node = select_default2(this);
        var updated = $$.updateAngle(d);
        var ratio = $$.getRatio("arc", updated);
        var isUnderThreshold = $$.meetsLabelThreshold(ratio, (_a2 = ["donut", "gauge", "pie", "polar"].filter($$.hasType.bind($$))) === null || _a2 === void 0 ? void 0 : _a2[0]);
        if (isUnderThreshold) {
          var value = (updated || d).value;
          var text2 = ($$.getArcLabelFormat() || $$.defaultArcValueFormat)(value, ratio, d.data.id).toString();
          setTextValue(node, text2, [-1, 1], hasGauge);
        } else {
          node.text("");
        }
      });
    }
  },
  expandArc: function(targetIds) {
    var $$ = this;
    var transiting = $$.state.transiting, $el = $$.$el;
    if (transiting) {
      var interval_1 = setInterval(function() {
        if (!transiting) {
          clearInterval(interval_1);
          $el.legend.selectAll(".".concat($FOCUS.legendItemFocused)).size() > 0 && $$.expandArc(targetIds);
        }
      }, 10);
      return;
    }
    var newTargetIds = $$.mapToTargetIds(targetIds);
    $el.svg.selectAll($$.selectorTargets(newTargetIds, ".".concat($ARC.chartArc))).each(function(d) {
      if (!$$.shouldExpand(d.data.id)) {
        return;
      }
      var expandDuration = $$.getExpandConfig(d.data.id, "duration");
      var svgArcExpandedSub = $$.getSvgArcExpanded($$.getExpandConfig(d.data.id, "rate"));
      select_default2(this).selectAll("path").transition().duration(expandDuration).attrTween("d", getAttrTweenFn($$.svgArcExpanded.bind($$))).transition().duration(expandDuration * 2).attrTween("d", getAttrTweenFn(svgArcExpandedSub.bind($$)));
    });
  },
  unexpandArc: function(targetIds) {
    var $$ = this;
    var transiting = $$.state.transiting, svg = $$.$el.svg;
    if (transiting) {
      return;
    }
    var newTargetIds = $$.mapToTargetIds(targetIds);
    svg.selectAll($$.selectorTargets(newTargetIds, ".".concat($ARC.chartArc))).selectAll("path").transition().duration(function(d) {
      return $$.getExpandConfig(d.data.id, "duration");
    }).attrTween("d", getAttrTweenFn($$.svgArc.bind($$)));
    svg.selectAll("".concat($ARC.arc)).style("opacity", null);
  },
  /**
   * Get expand config value
   * @param {string} id data ID
   * @param {string} key config key: 'duration | rate'
   * @returns {number}
   * @private
   */
  getExpandConfig: function(id2, key) {
    var $$ = this;
    var config = $$.config;
    var def = {
      duration: 50,
      rate: 0.98
    };
    var type2;
    if ($$.isDonutType(id2)) {
      type2 = "donut";
    } else if ($$.isGaugeType(id2)) {
      type2 = "gauge";
    } else if ($$.isPieType(id2)) {
      type2 = "pie";
    }
    return type2 ? config["".concat(type2, "_expand_").concat(key)] : def[key];
  },
  shouldExpand: function(id2) {
    var $$ = this;
    var config = $$.config;
    return $$.isDonutType(id2) && config.donut_expand || $$.isGaugeType(id2) && config.gauge_expand || $$.isPieType(id2) && config.pie_expand;
  },
  shouldShowArcLabel: function() {
    var $$ = this;
    var config = $$.config;
    return ["donut", "gauge", "pie", "polar"].some(function(v) {
      return $$.hasType(v) && config["".concat(v, "_label_show")];
    });
  },
  getArcLabelFormat: function() {
    var $$ = this;
    var config = $$.config;
    var format3 = function(v) {
      return v;
    };
    ["donut", "gauge", "pie", "polar"].filter($$.hasType.bind($$)).forEach(function(v) {
      format3 = config["".concat(v, "_label_format")];
    });
    return isFunction(format3) ? format3.bind($$.api) : format3;
  },
  updateTargetsForArc: function(targets) {
    var $$ = this;
    var $el = $$.$el;
    var hasGauge = $$.hasType("gauge");
    var classChartArc = $$.getChartClass("Arc");
    var classArcs = $$.getClass("arcs", true);
    var classFocus = $$.classFocus.bind($$);
    var chartArcs = $el.main.select(".".concat($ARC.chartArcs));
    var mainPieUpdate = chartArcs.selectAll(".".concat($ARC.chartArc)).data($$.pie(targets)).attr("class", function(d) {
      return classChartArc(d) + classFocus(d.data);
    });
    var mainPieEnter = mainPieUpdate.enter().append("g").attr("class", classChartArc).call(this.setCssRule(false, ".".concat($ARC.chartArcs, " text"), ["pointer-events:none", "text-anchor:middle"]));
    mainPieEnter.append("g").attr("class", classArcs).merge(mainPieUpdate);
    mainPieEnter.append("text").attr("dy", hasGauge && !$$.hasMultiTargets() ? "-.1em" : ".35em").style("opacity", "0").style("text-anchor", $$.getStylePropValue("middle")).style("pointer-events", $$.getStylePropValue("none"));
    $el.text = chartArcs.selectAll(".".concat($COMMON.target, " text"));
  },
  initArc: function() {
    var $$ = this;
    var $el = $$.$el;
    $el.arcs = $el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $ARC.chartArcs).attr("transform", $$.getTranslate("arc"));
    $$.setArcTitle();
  },
  /**
   * Set arc title text
   * @param {string} str Title text
   * @private
   */
  setArcTitle: function(str) {
    var $$ = this;
    var title2 = str || $$.getArcTitle();
    var hasGauge = $$.hasType("gauge");
    if (title2) {
      var className = hasGauge ? $GAUGE.chartArcsGaugeTitle : $ARC.chartArcsTitle;
      var text2 = $$.$el.arcs.select(".".concat(className));
      if (text2.empty()) {
        text2 = $$.$el.arcs.append("text").attr("class", className).style("text-anchor", "middle");
      }
      hasGauge && text2.attr("dy", "-0.3em");
      setTextValue(text2, title2, hasGauge ? void 0 : [-0.6, 1.35], true);
    }
  },
  /**
   * Return arc title text
   * @returns {string} Arc title text
   * @private
   */
  getArcTitle: function() {
    var $$ = this;
    var type2 = $$.hasType("donut") && "donut" || $$.hasType("gauge") && "gauge";
    return type2 ? $$.config["".concat(type2, "_title")] : "";
  },
  /**
   * Get arc title text with needle value
   * @returns {string|boolean} When title contains needle template string will return processed string, otherwise false
   * @private
   */
  getArcTitleWithNeedleValue: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var title2 = $$.getArcTitle();
    if (title2 && $$.config.arc_needle_show && /{=[A-Z_]+}/.test(title2)) {
      var value = state.current.needle;
      if (!isNumber(value)) {
        value = config.arc_needle_value;
      }
      return tplProcess(title2, {
        NEEDLE_VALUE: isNumber(value) ? value : 0
      });
    }
    return false;
  },
  redrawArc: function(duration, durationForExit, withTransform) {
    var $$ = this;
    var config = $$.config, state = $$.state, main2 = $$.$el.main;
    var hasInteraction = config.interaction_enabled;
    var isSelectable = hasInteraction && config.data_selection_isselectable;
    var mainArc = main2.selectAll(".".concat($ARC.arcs)).selectAll(".".concat($ARC.arc)).data($$.arcData.bind($$));
    mainArc.exit().transition().duration(durationForExit).style("opacity", "0").remove();
    mainArc = mainArc.enter().append("path").attr("class", $$.getClass("arc", true)).style("fill", function(d) {
      return $$.color(d.data);
    }).style("cursor", function(d) {
      var _a2;
      return ((_a2 = isSelectable === null || isSelectable === void 0 ? void 0 : isSelectable.bind) === null || _a2 === void 0 ? void 0 : _a2.call(isSelectable, $$.api)(d)) ? "pointer" : null;
    }).style("opacity", "0").each(function(d) {
      if ($$.isGaugeType(d.data)) {
        d.startAngle = config.gauge_startingAngle;
        d.endAngle = config.gauge_startingAngle;
      }
      this._current = d;
    }).merge(mainArc);
    if ($$.hasType("gauge")) {
      $$.updateGaugeMax();
      $$.hasMultiArcGauge() && $$.redrawArcGaugeLine();
    }
    mainArc.attr("transform", function(d) {
      return !$$.isGaugeType(d.data) && withTransform ? "scale(0)" : "";
    }).style("opacity", function(d) {
      return d === this._current ? "0" : null;
    }).each(function() {
      state.transiting = true;
    }).transition().duration(duration).attrTween("d", function(d) {
      var updated = $$.updateAngle(d);
      if (!updated) {
        return function() {
          return "M 0 0";
        };
      }
      if (isNaN(this._current.startAngle)) {
        this._current.startAngle = 0;
      }
      if (isNaN(this._current.endAngle)) {
        this._current.endAngle = this._current.startAngle;
      }
      var interpolate$1 = value_default(this._current, updated);
      this._current = interpolate$1(0);
      return function(t) {
        var interpolated = interpolate$1(t);
        interpolated.data = d.data;
        return $$.getArc(interpolated, true);
      };
    }).attr("transform", withTransform ? "scale(1)" : "").style("fill", function(d) {
      var color3;
      if ($$.levelColor) {
        color3 = $$.levelColor(d.data.values[0].value);
        config.data_colors[d.data.id] = color3;
      } else {
        color3 = $$.color(d.data);
      }
      return color3;
    }).style("opacity", null).call(endall, function() {
      if ($$.levelColor) {
        var path2 = select_default2(this);
        var d = path2.datum(this._current);
        $$.updateLegendItemColor(d.data.id, path2.style("fill"));
      }
      state.transiting = false;
      callFn(config.onrendered, $$.api);
    });
    hasInteraction && $$.bindArcEvent(mainArc);
    $$.hasType("polar") && $$.redrawPolar();
    $$.hasType("gauge") && $$.redrawBackgroundArcs();
    config.arc_needle_show && $$.redrawNeedle();
    $$.redrawArcText(duration);
    $$.redrawArcRangeText();
  },
  /**
   * Update needle element
   * @private
   */
  redrawNeedle: function() {
    var $$ = this;
    var $el = $$.$el, config = $$.config, _a2 = $$.state, hiddenTargetIds = _a2.hiddenTargetIds, radius = _a2.radius;
    var length = (radius - 1) / 100 * config.arc_needle_length;
    var hasDataToShow = hiddenTargetIds.length !== $$.data.targets.length;
    var needle = $$.$el.arcs.select(".".concat($ARC.needle));
    var pathFn = config.arc_needle_path;
    var baseWidth = config.arc_needle_bottom_width / 2;
    var topWidth = config.arc_needle_top_width / 2;
    var topRx = config.arc_needle_top_rx;
    var topRy = config.arc_needle_top_ry;
    var bottomLen = config.arc_needle_bottom_len;
    var bottomRx = config.arc_needle_bottom_rx;
    var bottomRy = config.arc_needle_bottom_ry;
    var needleAngle = $$.getNeedleAngle();
    var updateNeedleValue = function() {
      var title2 = $$.getArcTitleWithNeedleValue();
      title2 && $$.setArcTitle(title2);
    };
    updateNeedleValue();
    if (needle.empty()) {
      needle = $el.arcs.append("path").classed($ARC.needle, true);
      $el.needle = needle;
      $el.needle.updateHelper = function(v, updateConfig) {
        if (updateConfig === void 0) {
          updateConfig = false;
        }
        if ($el.needle.style("display") !== "none") {
          $$.$T($el.needle).style("transform", "rotate(".concat($$.getNeedleAngle(v), "deg)")).call(endall, function() {
            updateConfig && (config.arc_needle_value = v);
            updateNeedleValue();
          });
        }
      };
    }
    if (hasDataToShow) {
      var path2 = isFunction(pathFn) ? pathFn.call($$, length) : "M-".concat(baseWidth, " ").concat(bottomLen, " A").concat(bottomRx, " ").concat(bottomRy, " 0 0 0 ").concat(baseWidth, " ").concat(bottomLen, " L").concat(topWidth, " -").concat(length, " A").concat(topRx, " ").concat(topRy, " 0 0 0 -").concat(topWidth, " -").concat(length, " L-").concat(baseWidth, " ").concat(bottomLen, " Z");
      $$.$T(needle).attr("d", path2).style("fill", config.arc_needle_color).style("display", null).style("transform", "rotate(".concat(needleAngle, "deg)"));
    } else {
      needle.style("display", "none");
    }
  },
  /**
   * Get needle angle value relative given value
   * @param {number} v Value to be calculated angle
   * @returns {number} angle value
   * @private
   */
  getNeedleAngle: function(v) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var arcLength = $$.getArcLength();
    var hasGauge = $$.hasType("gauge");
    var total = $$.getTotalDataSum(true);
    var value = isDefined(v) ? v : config.arc_needle_value;
    var startingAngle = config["".concat(config.data_type, "_startingAngle")] || 0;
    var radian = 0;
    if (!isNumber(value)) {
      value = hasGauge && $$.data.targets.length === 1 ? total : 0;
    }
    state.current.needle = value;
    if (hasGauge) {
      startingAngle = $$.getStartingAngle();
      var radius = config.gauge_fullCircle ? arcLength : startingAngle * -2;
      var min4 = config.gauge_min, max4 = config.gauge_max;
      radian = radius * ((value - min4) / (max4 - min4));
    } else {
      radian = arcLength * (value / total);
    }
    return (startingAngle + radian) * (180 / Math.PI);
  },
  redrawBackgroundArcs: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var hasMultiArcGauge = $$.hasMultiArcGauge();
    var isFullCircle = config.gauge_fullCircle;
    var showEmptyTextLabel = $$.filterTargetsToShow($$.data.targets).length === 0 && !!config.data_empty_label_text;
    var startAngle = $$.getStartingAngle();
    var endAngle = isFullCircle ? startAngle + $$.getArcLength() : startAngle * -1;
    var backgroundArc = $$.$el.arcs.select("".concat(hasMultiArcGauge ? "g" : "", ".").concat($ARC.chartArcsBackground));
    if (hasMultiArcGauge) {
      var index_1 = 0;
      backgroundArc = backgroundArc.selectAll("path.".concat($ARC.chartArcsBackground)).data($$.data.targets);
      backgroundArc.enter().append("path").attr("class", function(d, i) {
        return "".concat($ARC.chartArcsBackground, " ").concat($ARC.chartArcsBackground, "-").concat(i);
      }).merge(backgroundArc).style("fill", config.gauge_background || null).attr("d", function(_a2) {
        var id2 = _a2.id;
        if (showEmptyTextLabel || state.hiddenTargetIds.indexOf(id2) >= 0) {
          return "M 0 0";
        }
        var d = {
          data: [{ value: config.gauge_max }],
          startAngle,
          endAngle,
          index: index_1++
        };
        return $$.getArc(d, true, true);
      });
      backgroundArc.exit().remove();
    } else {
      backgroundArc.attr("d", showEmptyTextLabel ? "M 0 0" : function() {
        var d = {
          data: [{ value: config.gauge_max }],
          startAngle,
          endAngle
        };
        return $$.getArc(d, true, true);
      });
    }
  },
  bindArcEvent: function(arc) {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var isTouch = state.inputType === "touch";
    var isMouse = state.inputType === "mouse";
    function selectArc(_this, arcData, id2) {
      $$.expandArc(id2);
      $$.api.focus(id2);
      $$.toggleFocusLegend(id2, true);
      $$.showTooltip([arcData], _this);
    }
    function unselectArc(arcData) {
      var id2 = (arcData === null || arcData === void 0 ? void 0 : arcData.id) || void 0;
      $$.unexpandArc(id2);
      $$.api.revert();
      $$.revertLegend();
      $$.hideTooltip();
    }
    arc.on("click", function(event, d, i) {
      var _a2;
      var updated = $$.updateAngle(d);
      var arcData;
      if (updated) {
        arcData = $$.convertToArcData(updated);
        (_a2 = $$.toggleShape) === null || _a2 === void 0 ? void 0 : _a2.call($$, this, arcData, i);
        config.data_onclick.bind($$.api)(arcData, this);
      }
    });
    if (isMouse) {
      arc.on("mouseover", function(event, d) {
        if (state.transiting) {
          return;
        }
        state.event = event;
        var updated = $$.updateAngle(d);
        var arcData = updated ? $$.convertToArcData(updated) : null;
        var id2 = (arcData === null || arcData === void 0 ? void 0 : arcData.id) || void 0;
        selectArc(this, arcData, id2);
        $$.setOverOut(true, arcData);
      }).on("mouseout", function(event, d) {
        if (state.transiting) {
          return;
        }
        state.event = event;
        var updated = $$.updateAngle(d);
        var arcData = updated ? $$.convertToArcData(updated) : null;
        unselectArc();
        $$.setOverOut(false, arcData);
      }).on("mousemove", function(event, d) {
        var updated = $$.updateAngle(d);
        var arcData = updated ? $$.convertToArcData(updated) : null;
        state.event = event;
        $$.showTooltip([arcData], this);
      });
    }
    if (isTouch && $$.hasArcType() && !$$.radars) {
      var getEventArc_1 = function(event) {
        var _a2, _b;
        var _c = (_b = (_a2 = event.changedTouches) === null || _a2 === void 0 ? void 0 : _a2[0]) !== null && _b !== void 0 ? _b : { clientX: 0, clientY: 0 }, clientX = _c.clientX, clientY = _c.clientY;
        var eventArc = select_default2(doc.elementFromPoint(clientX, clientY));
        return eventArc;
      };
      $$.$el.svg.on("touchstart touchmove", function(event) {
        if (state.transiting) {
          return;
        }
        state.event = event;
        var eventArc = getEventArc_1(event);
        var datum2 = eventArc.datum();
        var updated = (datum2 === null || datum2 === void 0 ? void 0 : datum2.data) && datum2.data.id ? $$.updateAngle(datum2) : null;
        var arcData = updated ? $$.convertToArcData(updated) : null;
        var id2 = (arcData === null || arcData === void 0 ? void 0 : arcData.id) || void 0;
        $$.callOverOutForTouch(arcData);
        isUndefined(id2) ? unselectArc() : selectArc(this, arcData, id2);
      });
    }
  },
  redrawArcText: function(duration) {
    var $$ = this;
    var config = $$.config, state = $$.state, _a2 = $$.$el, main2 = _a2.main, arcs = _a2.arcs;
    var hasGauge = $$.hasType("gauge");
    var hasMultiArcGauge = $$.hasMultiArcGauge();
    var text2;
    if (!(hasGauge && $$.data.targets.length === 1 && config.gauge_title)) {
      text2 = main2.selectAll(".".concat($ARC.chartArc)).select("text").style("opacity", "0").attr("class", function(d) {
        return $$.isGaugeType(d.data) ? $GAUGE.gaugeValue : null;
      }).call($$.textForArcLabel.bind($$)).attr("transform", function(d) {
        return $$.transformForArcLabel.bind($$)(d);
      }).style("font-size", function(d) {
        return $$.isGaugeType(d.data) && $$.data.targets.length === 1 && !hasMultiArcGauge ? "".concat(Math.round(state.radius / 5), "px") : null;
      }).transition().duration(duration).style("opacity", function(d) {
        return $$.isTargetToShow(d.data.id) && $$.isArcType(d.data) ? null : "0";
      });
      hasMultiArcGauge && text2.attr("dy", "-.1em");
    }
    main2.select(".".concat($ARC.chartArcsTitle)).style("opacity", $$.hasType("donut") || hasGauge ? null : "0");
    if (hasGauge) {
      var isFullCircle = config.gauge_fullCircle;
      isFullCircle && (text2 === null || text2 === void 0 ? void 0 : text2.attr("dy", "".concat(hasMultiArcGauge ? 0 : Math.round(state.radius / 14))));
      if (config.gauge_label_show) {
        arcs.select(".".concat($GAUGE.chartArcsGaugeUnit)).attr("dy", "".concat(isFullCircle ? 1.5 : 0.75, "em")).text(config.gauge_units);
        arcs.select(".".concat($GAUGE.chartArcsGaugeMin)).attr("dx", "".concat(-1 * (state.innerRadius + (state.radius - state.innerRadius) / (isFullCircle ? 1 : 2)), "px")).attr("dy", "1.2em").text($$.textForGaugeMinMax(config.gauge_min, false));
        !isFullCircle && arcs.select(".".concat($GAUGE.chartArcsGaugeMax)).attr("dx", "".concat(state.innerRadius + (state.radius - state.innerRadius) / 2, "px")).attr("dy", "1.2em").text($$.textForGaugeMinMax(config.gauge_max, true));
      }
    }
  },
  /**
   * Get Arc element by id or index
   * @param {string|number} value id or index of Arc
   * @returns {d3Selection} Arc path element
   * @private
   */
  getArcElementByIdOrIndex: function(value) {
    var $$ = this;
    var arcs = $$.$el.arcs;
    var filterFn = isNumber(value) ? function(d) {
      return d.index === value;
    } : function(d) {
      return d.data.id === value;
    };
    return arcs === null || arcs === void 0 ? void 0 : arcs.selectAll(".".concat($COMMON.target, " path")).filter(filterFn);
  }
};
var shapeArea = {
  initArea: function(mainLine) {
    var $$ = this;
    var config = $$.config;
    mainLine.insert("g", ".".concat(config.area_front ? $CIRCLE.circles : $LINE.lines)).attr("class", $$.getClass("areas", true));
  },
  /**
   * Update area color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateAreaColor: function(d) {
    var $$ = this;
    return $$.config.area_linearGradient ? $$.getGradienColortUrl(d.id) : $$.color(d);
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateArea: function(withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var config = $$.config, state = $$.state, $el = $$.$el, $T = $$.$T;
    var $root = isSub ? $el.subchart : $el;
    config.area_linearGradient && $$.updateLinearGradient();
    var area2 = $root.main.selectAll(".".concat($AREA.areas)).selectAll(".".concat($AREA.area)).data($$.lineData.bind($$));
    $T(area2.exit(), withTransition).style("opacity", "0").remove();
    $root.area = area2.enter().append("path").attr("class", $$.getClass("area", true)).style("fill", $$.updateAreaColor.bind($$)).style("opacity", function() {
      state.orgAreaOpacity = select_default2(this).style("opacity");
      return "0";
    }).merge(area2);
    area2.style("opacity", state.orgAreaOpacity);
    $$.setRatioForGroupedData($root.area.data());
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned functino from .generateDrawCandlestick()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   */
  redrawArea: function(drawFn, withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var area2 = (isSub ? this.$el.subchart : this.$el).area;
    var orgAreaOpacity = $$.state.orgAreaOpacity;
    return [
      $$.$T(area2, withTransition, getRandom()).attr("d", drawFn).style("fill", $$.updateAreaColor.bind($$)).style("opacity", function(d) {
        return String($$.isAreaRangeType(d) ? orgAreaOpacity / 1.75 : orgAreaOpacity);
      })
    ];
  },
  /**
   * Generate area path data
   * @param {object} areaIndices Indices
   * @param {boolean} isSub Weather is sub axis
   * @returns {Function}
   * @private
   */
  generateDrawArea: function(areaIndices, isSub) {
    var $$ = this;
    var config = $$.config;
    var lineConnectNull = config.line_connectNull;
    var isRotated = config.axis_rotated;
    var getPoints = $$.generateGetAreaPoints(areaIndices, isSub);
    var yScale = $$.getYScaleById.bind($$);
    var xValue = function(d) {
      return (isSub ? $$.subxx : $$.xx).call($$, d);
    };
    var value0 = function(d, i) {
      return $$.isGrouped(d.id) ? getPoints(d, i)[0][1] : yScale(d.id, isSub)($$.isAreaRangeType(d) ? $$.getRangedData(d, "high") : $$.getShapeYMin(d.id));
    };
    var value1 = function(d, i) {
      return $$.isGrouped(d.id) ? getPoints(d, i)[1][1] : yScale(d.id, isSub)($$.isAreaRangeType(d) ? $$.getRangedData(d, "low") : d.value);
    };
    return function(d) {
      var values = lineConnectNull ? $$.filterRemoveNull(d.values) : d.values;
      var x0 = 0;
      var y0 = 0;
      var path2;
      if ($$.isAreaType(d)) {
        var area2 = area_default();
        area2 = isRotated ? area2.y(xValue).x0(value0).x1(value1) : area2.x(xValue).y0(config.area_above ? 0 : config.area_below ? $$.state.height : value0).y1(value1);
        if (!lineConnectNull) {
          area2 = area2.defined(function(d2) {
            return $$.getBaseValue(d2) !== null;
          });
        }
        if ($$.isStepType(d)) {
          values = $$.convertValuesToStep(values);
        }
        path2 = area2.curve($$.getCurve(d))(values);
      } else {
        if (values[0]) {
          x0 = $$.scale.x(values[0].x);
          y0 = $$.getYScaleById(d.id)(values[0].value);
        }
        path2 = isRotated ? "M ".concat(y0, " ").concat(x0) : "M ".concat(x0, " ").concat(y0);
      }
      return path2 || "M 0 0";
    };
  },
  generateGetAreaPoints: function(areaIndices, isSub) {
    var $$ = this;
    var config = $$.config;
    var x3 = $$.getShapeX(0, areaIndices, isSub);
    var y3 = $$.getShapeY(!!isSub);
    var areaOffset = $$.getShapeOffset($$.isAreaType, areaIndices, isSub);
    var yScale = $$.getYScaleById.bind($$);
    return function(d, i) {
      var y0 = yScale.call($$, d.id, isSub)($$.getShapeYMin(d.id));
      var offset = areaOffset(d, i) || y0;
      var posX = x3(d);
      var value = d.value;
      var posY = y3(d);
      if (config.axis_rotated && (value > 0 && posY < y0 || value < 0 && y0 < posY)) {
        posY = y0;
      }
      return [
        [posX, offset],
        [posX, posY - (y0 - offset)],
        [posX, posY - (y0 - offset)],
        // needed for compatibility
        [posX, offset]
        // needed for compatibility
      ];
    };
  }
};
var shapeBar = {
  initBar: function() {
    var _a2 = this, $el = _a2.$el, config = _a2.config, clip2 = _a2.state.clip;
    $el.bar = $el.main.select(".".concat($COMMON.chart));
    $el.bar = config.bar_front ? $el.bar.append("g") : $el.bar.insert("g", ":first-child");
    $el.bar.attr("class", $BAR.chartBars).call(this.setCssRule(false, ".".concat($BAR.chartBars), ["pointer-events:none"]));
    if (config.clipPath === false && (config.bar_radius || config.bar_radius_ratio)) {
      $el.bar.attr("clip-path", clip2.pathXAxis.replace(/#[^)]*/, "#".concat(clip2.id)));
    }
  },
  updateTargetsForBar: function(targets) {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    var classChartBar = $$.getChartClass("Bar");
    var classBars = $$.getClass("bars", true);
    var classFocus = $$.classFocus.bind($$);
    var isSelectable = config.interaction_enabled && config.data_selection_isselectable;
    if (!$el.bar) {
      $$.initBar();
    }
    var mainBarUpdate = $el.main.select(".".concat($BAR.chartBars)).selectAll(".".concat($BAR.chartBar)).data(
      // remove
      targets.filter(function(v) {
        return v.values.some(function(d) {
          return isNumber(d.value) || $$.isBarRangeType(d);
        });
      })
    ).attr("class", function(d) {
      return classChartBar(d) + classFocus(d);
    });
    var mainBarEnter = mainBarUpdate.enter().append("g").attr("class", classChartBar).style("opacity", "0").style("pointer-events", $$.getStylePropValue("none"));
    mainBarEnter.append("g").attr("class", classBars).style("cursor", function(d) {
      var _a2;
      return ((_a2 = isSelectable === null || isSelectable === void 0 ? void 0 : isSelectable.bind) === null || _a2 === void 0 ? void 0 : _a2.call(isSelectable, $$.api)(d)) ? "pointer" : null;
    }).call($$.setCssRule(true, " .".concat($BAR.bar), ["fill"], $$.color));
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateBar: function(withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var config = $$.config, $el = $$.$el, $T = $$.$T;
    var $root = isSub ? $el.subchart : $el;
    var classBar = $$.getClass("bar", true);
    var initialOpacity = $$.initialOpacity.bind($$);
    config.bar_linearGradient && $$.updateLinearGradient();
    var bar2 = $root.main.selectAll(".".concat($BAR.bars)).selectAll(".".concat($BAR.bar)).data($$.labelishData.bind($$));
    $T(bar2.exit(), withTransition).style("opacity", "0").remove();
    $root.bar = bar2.enter().append("path").attr("class", classBar).style("fill", $$.updateBarColor.bind($$)).merge(bar2).style("opacity", initialOpacity);
    $$.setRatioForGroupedData($root.bar.data());
  },
  /**
   * Update bar color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateBarColor: function(d) {
    var $$ = this;
    var fn = $$.getStylePropValue($$.color);
    return $$.config.bar_linearGradient ? $$.getGradienColortUrl(d.id) : fn ? fn(d) : null;
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned function from .getDrawShape() => .generateDrawBar()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   * @private
   */
  redrawBar: function(drawFn, withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var bar2 = (isSub ? $$.$el.subchart : $$.$el).bar;
    return [
      $$.$T(bar2, withTransition, getRandom()).attr("d", function(d) {
        return (isNumber(d.value) || $$.isBarRangeType(d)) && drawFn(d);
      }).style("fill", $$.updateBarColor.bind($$)).style("opacity", null)
    ];
  },
  /**
   * Generate draw function
   * @param {object} barIndices data order within x axis.
   * barIndices ==> {data1: 0, data2: 0, data3: 1, data4: 1, __max__: 1}
   *
   * When gropus given as:
   *  groups: [
   *		["data1", "data2"],
   *		["data3", "data4"]
   *	],
   *
   * Will be rendered as:
   * 		data1 data3   data1 data3
   *		data2 data4   data2 data4
   *		-------------------------
   *			 0             1
   * @param {boolean} isSub If is for subchart
   * @returns {Function}
   * @private
   */
  generateDrawBar: function(barIndices, isSub) {
    var $$ = this;
    var config = $$.config;
    var getPoints = $$.generateGetBarPoints(barIndices, isSub);
    var isRotated = config.axis_rotated;
    var barRadius = config.bar_radius;
    var barRadiusRatio = config.bar_radius_ratio;
    var getRadius = isNumber(barRadius) && barRadius > 0 ? function() {
      return barRadius;
    } : isNumber(barRadiusRatio) ? function(w) {
      return w * barRadiusRatio;
    } : null;
    return function(d, i) {
      var points = getPoints(d, i);
      var indexX = +isRotated;
      var indexY = +!indexX;
      var isUnderZero = d.value < 0;
      var isInverted = config["axis_".concat($$.axis.getId(d.id), "_inverted")];
      var isNegative = !isInverted && isUnderZero || isInverted && !isUnderZero;
      var pathRadius = ["", ""];
      var radius = 0;
      var isGrouped = $$.isGrouped(d.id);
      var isRadiusData = getRadius && isGrouped ? $$.isStackingRadiusData(d) : false;
      if (getRadius) {
        var index2 = isRotated ? indexY : indexX;
        var barW = points[2][index2] - points[0][index2];
        radius = !isGrouped || isRadiusData ? getRadius(barW) : 0;
        var arc = "a".concat(radius, ",").concat(radius, " ").concat(isNegative ? "1 0 0" : "0 0 1", " ");
        pathRadius[+!isRotated] = "".concat(arc).concat(radius, ",").concat(radius);
        pathRadius[+isRotated] = "".concat(arc).concat([-radius, radius][isRotated ? "sort" : "reverse"]());
        isNegative && pathRadius.reverse();
      }
      var path2 = isRotated ? "H".concat(points[1][indexX] + (isNegative ? radius : -radius), " ").concat(pathRadius[0], "V").concat(points[2][indexY] - radius, " ").concat(pathRadius[1], "H").concat(points[3][indexX]) : "V".concat(points[1][indexY] + (isNegative ? -radius : radius), " ").concat(pathRadius[0], "H").concat(points[2][indexX] - radius, " ").concat(pathRadius[1], "V").concat(points[3][indexY]);
      return "M".concat(points[0][indexX], ",").concat(points[0][indexY]).concat(path2, "z");
    };
  },
  /**
   * Determine if given stacking bar data is radius type
   * @param {object} d Data row
   * @returns {boolean}
   */
  isStackingRadiusData: function(d) {
    var $$ = this;
    var $el = $$.$el, config = $$.config, data2 = $$.data, state = $$.state;
    var id2 = d.id, index2 = d.index, value = d.value;
    if (state.hiddenTargetIds.indexOf(id2) > -1) {
      var target = $el.bar.filter(function(d2) {
        return d2.id === id2 && d2.value === value;
      });
      return !target.empty() && /a\d+/i.test(target.attr("d"));
    }
    var keys = config.data_groups.find(function(v) {
      return v.indexOf(id2) > -1;
    });
    var sortedList = $$.orderTargets($$.filterTargetsToShow(data2.targets.filter($$.isBarType, $$))).filter(function(v) {
      return keys.indexOf(v.id) > -1;
    });
    var sortedIds = sortedList.map(function(v) {
      return v.values.filter(function(v2) {
        return v2.index === index2 && (isNumber(value) && value > 0 ? v2.value > 0 : v2.value < 0);
      })[0];
    }).filter(Boolean).map(function(v) {
      return v.id;
    });
    return value !== 0 && sortedIds.indexOf(id2) === sortedIds.length - 1;
  },
  /**
   * Generate bar coordinate points data
   * @param {object} barIndices Data order within x axis.
   * @param {boolean} isSub If is for subchart
   * @returns {Array} Array of coordinate points
   * @private
   */
  generateGetBarPoints: function(barIndices, isSub) {
    var $$ = this;
    var config = $$.config;
    var axis3 = isSub ? $$.axis.subX : $$.axis.x;
    var barTargetsNum = $$.getIndicesMax(barIndices) + 1;
    var barW = $$.getBarW("bar", axis3, barTargetsNum);
    var barX = $$.getShapeX(barW, barIndices, !!isSub);
    var barY = $$.getShapeY(!!isSub);
    var barOffset = $$.getShapeOffset($$.isBarType, barIndices, !!isSub);
    var yScale = $$.getYScaleById.bind($$);
    return function(d, i) {
      var id2 = d.id;
      var y0 = yScale.call($$, id2, isSub)($$.getShapeYMin(id2));
      var offset = barOffset(d, i) || y0;
      var width = isNumber(barW) ? barW : barW[d.id] || barW._$width;
      var isInverted = config["axis_".concat($$.axis.getId(id2), "_inverted")];
      var value = d.value;
      var posX = barX(d);
      var posY = barY(d);
      if (config.axis_rotated && !isInverted && (value > 0 && posY < y0 || value < 0 && y0 < posY)) {
        posY = y0;
      }
      if (!$$.isBarRangeType(d)) {
        posY -= y0 - offset;
      }
      var startPosX = posX + width;
      return [
        [posX, offset],
        [posX, posY],
        [startPosX, posY],
        [startPosX, offset]
      ];
    };
  }
};
var shapeCandlestick = {
  initCandlestick: function() {
    var $el = this.$el;
    $el.candlestick = $el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $CANDLESTICK.chartCandlesticks);
  },
  /**
   * Update targets by its data
   * called from: ChartInternal.updateTargets()
   * @param {Array} targets Filtered target by type
   * @private
   */
  updateTargetsForCandlestick: function(targets) {
    var $$ = this;
    var $el = $$.$el;
    var classChart = $$.getChartClass("Candlestick");
    if (!$el.candlestick) {
      $$.initCandlestick();
    }
    var mainUpdate = $$.$el.main.select(".".concat($CANDLESTICK.chartCandlesticks)).selectAll(".".concat($CANDLESTICK.chartCandlestick)).data(targets);
    mainUpdate.enter().append("g").attr("class", classChart).style("pointer-events", "none");
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateCandlestick: function(withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var $el = $$.$el, $T = $$.$T;
    var $root = isSub ? $el.subchart : $el;
    var classSetter = $$.getClass("candlestick", true);
    var initialOpacity = $$.initialOpacity.bind($$);
    var candlestick2 = $root.main.selectAll(".".concat($CANDLESTICK.chartCandlestick)).selectAll(".".concat($CANDLESTICK.candlestick)).data($$.labelishData.bind($$));
    $T(candlestick2.exit(), withTransition).style("opacity", "0").remove();
    var candlestickEnter = candlestick2.enter().filter(function(d) {
      return d.value;
    }).append("g").attr("class", classSetter);
    candlestickEnter.append("line");
    candlestickEnter.append("path");
    $root.candlestick = candlestick2.merge(candlestickEnter).style("opacity", initialOpacity);
  },
  /**
   * Get draw function
   * @param {object} indices Indice data
   * @param {boolean} isSub Subchart draw
   * @returns {Function}
   * @private
   */
  generateDrawCandlestick: function(indices, isSub) {
    var $$ = this;
    var config = $$.config;
    var getPoints = $$.generateGetCandlestickPoints(indices, isSub);
    var isRotated = config.axis_rotated;
    var downColor = config.candlestick_color_down;
    return function(d, i, g) {
      var points = getPoints(d, i);
      var value = $$.getCandlestickData(d);
      var isUp = value === null || value === void 0 ? void 0 : value._isUp;
      var indexX = +isRotated;
      var indexY = +!indexX;
      if (g.classed) {
        g.classed($CANDLESTICK[isUp ? "valueUp" : "valueDown"], true);
      }
      var path2 = isRotated ? "H".concat(points[1][1], " V").concat(points[1][0], " H").concat(points[0][1]) : "V".concat(points[1][1], " H").concat(points[1][0], " V").concat(points[0][1]);
      g.select("path").attr("d", "M".concat(points[0][indexX], ",").concat(points[0][indexY]).concat(path2, "z")).style("fill", function(d2) {
        var color3 = isUp ? $$.color(d2) : isObject(downColor) ? downColor[d2.id] : downColor;
        return color3 || $$.color(d2);
      });
      var line2 = g.select("line");
      var pos = isRotated ? {
        x1: points[2][1],
        x2: points[2][2],
        y1: points[2][0],
        y2: points[2][0]
      } : {
        x1: points[2][0],
        x2: points[2][0],
        y1: points[2][1],
        y2: points[2][2]
      };
      for (var x3 in pos) {
        line2.attr(x3, pos[x3]);
      }
    };
  },
  /**
   * Generate shape drawing points
   * @param {object} indices Indice data
   * @param {boolean} isSub Subchart draw
   * @returns {Function}
   */
  generateGetCandlestickPoints: function(indices, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var axis3 = isSub ? $$.axis.subX : $$.axis.x;
    var targetsNum = $$.getIndicesMax(indices) + 1;
    var barW = $$.getBarW("candlestick", axis3, targetsNum);
    var x3 = $$.getShapeX(barW, indices, !!isSub);
    var y3 = $$.getShapeY(!!isSub);
    var shapeOffset = $$.getShapeOffset($$.isBarType, indices, !!isSub);
    var yScale = $$.getYScaleById.bind($$);
    return function(d, i) {
      var y0 = yScale.call($$, d.id, isSub)($$.getShapeYMin(d.id));
      var offset = shapeOffset(d, i) || y0;
      var width = isNumber(barW) ? barW : barW[d.id] || barW._$width;
      var value = $$.getCandlestickData(d);
      var points;
      if (value && isNumber(value.open) && isNumber(value.close)) {
        var posX = {
          start: x3(d),
          end: 0
        };
        posX.end = posX.start + width;
        var posY = {
          start: y3(value.open),
          end: y3(value.close)
        };
        var posLine = {
          x: posX.start + width / 2,
          high: y3(value.high),
          low: y3(value.low)
        };
        posY.start -= y0 - offset;
        points = [
          [posX.start, posY.start],
          [posX.end, posY.end],
          [posLine.x, posLine.low, posLine.high]
        ];
      } else {
        points = [[0, 0], [0, 0], [0, 0, 0]];
      }
      return points;
    };
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned functino from .generateDrawCandlestick()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   */
  redrawCandlestick: function(drawFn, withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var $el = $$.$el, $T = $$.$T;
    var candlestick2 = (isSub ? $el.subchart : $el).candlestick;
    var rand = getRandom(true);
    return [
      candlestick2.each(function(d, i) {
        var g = $T(select_default2(this), withTransition, rand);
        drawFn(d, i, g);
      }).style("opacity", null)
    ];
  },
  /**
   * Get candlestick data as object
   * @param {object} param Data object
   * @param {Array|object} param.value Data value
   * @returns {object|null} Converted data object
   * @private
   */
  getCandlestickData: function(_a2) {
    var value = _a2.value;
    var d;
    if (isArray(value)) {
      var open_1 = value[0], high = value[1], low = value[2], close_1 = value[3], _b = value[4], volume = _b === void 0 ? false : _b;
      d = { open: open_1, high, low, close: close_1 };
      if (volume !== false) {
        d.volume = volume;
      }
    } else if (isObject(value)) {
      d = _assign({}, value);
    }
    if (d) {
      d._isUp = d.close >= d.open;
    }
    return d || null;
  }
};
var shapeGauge = {
  initGauge: function() {
    var $$ = this;
    var config = $$.config, arcs = $$.$el.arcs;
    var appendText = function(className, value) {
      if (className === void 0) {
        className = null;
      }
      if (value === void 0) {
        value = "";
      }
      arcs.append("text").attr("class", className).style("text-anchor", "middle").style("pointer-events", "none").text(value);
    };
    if ($$.hasType("gauge")) {
      var hasMulti = $$.hasMultiArcGauge();
      arcs.append(hasMulti ? "g" : "path").attr("class", $ARC.chartArcsBackground).style("fill", !hasMulti && config.gauge_background || null);
      config.gauge_units && appendText($GAUGE.chartArcsGaugeUnit);
      if (config.gauge_label_show) {
        appendText($GAUGE.chartArcsGaugeMin);
        !config.gauge_fullCircle && appendText($GAUGE.chartArcsGaugeMax);
      }
    }
  },
  updateGaugeMax: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var hasMultiGauge = $$.hasMultiArcGauge();
    var max4 = hasMultiGauge ? $$.getMinMaxData().max[0].value : $$.getTotalDataSum(state.rendered);
    if (!config.gauge_enforceMinMax && max4 + config.gauge_min * (config.gauge_min > 0 ? -1 : 1) > config.gauge_max) {
      config.gauge_max = max4 - config.gauge_min;
    }
  },
  redrawArcGaugeLine: function() {
    var $$ = this;
    var config = $$.config, state = $$.state, $el = $$.$el;
    var hiddenTargetIds = $$.state.hiddenTargetIds;
    var arcLabelLines = $el.main.selectAll(".".concat($ARC.arcs)).selectAll(".".concat($ARC.arcLabelLine)).data($$.arcData.bind($$));
    var mainArcLabelLine = arcLabelLines.enter().append("rect").attr("class", function(d) {
      return "".concat($ARC.arcLabelLine, " ").concat($COMMON.target, " ").concat($COMMON.target, "-").concat(d.data.id);
    }).merge(arcLabelLines);
    mainArcLabelLine.style("fill", function(d) {
      return $$.levelColor ? $$.levelColor(d.data.values[0].value) : $$.color(d.data);
    }).style("display", config.gauge_label_show ? null : "none").each(function(d) {
      var lineLength = 0;
      var lineThickness = 2;
      var x3 = 0;
      var y3 = 0;
      var transform3 = "";
      if (hiddenTargetIds.indexOf(d.data.id) < 0) {
        var updated = $$.updateAngle(d);
        var innerLineLength = state.gaugeArcWidth / $$.filterTargetsToShow($$.data.targets).length * (updated.index + 1);
        var lineAngle = updated.endAngle - Math.PI / 2;
        var arcInnerRadius2 = state.radius - innerLineLength;
        var linePositioningAngle = lineAngle - (arcInnerRadius2 === 0 ? 0 : 1 / arcInnerRadius2);
        lineLength = state.radiusExpanded - state.radius + innerLineLength;
        x3 = Math.cos(linePositioningAngle) * arcInnerRadius2;
        y3 = Math.sin(linePositioningAngle) * arcInnerRadius2;
        transform3 = "rotate(".concat(lineAngle * 180 / Math.PI, ", ").concat(x3, ", ").concat(y3, ")");
      }
      select_default2(this).attr("x", x3).attr("y", y3).attr("width", lineLength).attr("height", lineThickness).attr("transform", transform3).style("stroke-dasharray", "0, ".concat(lineLength + lineThickness, ", 0"));
    });
  },
  textForGaugeMinMax: function(value, isMax) {
    var $$ = this;
    var config = $$.config;
    var format3 = config.gauge_label_extents;
    return isFunction(format3) ? format3.bind($$.api)(value, isMax) : value;
  },
  getGaugeLabelHeight: function() {
    var config = this.config;
    return this.config.gauge_label_show && !config.gauge_fullCircle ? 20 : 0;
  },
  getPaddingBottomForGauge: function() {
    var $$ = this;
    return $$.getGaugeLabelHeight() * ($$.config.gauge_label_show ? 2 : 2.5);
  }
};
var shapeBubble = {
  /**
   * Initializer
   * @private
   */
  initBubble: function() {
    var $$ = this;
    var config = $$.config;
    if ($$.hasType("bubble")) {
      config.point_show = true;
      config.point_type = "circle";
    }
  },
  /**
   * Get user agent's computed value
   * @returns {number}
   * @private
   */
  getBaseLength: function() {
    var $$ = this;
    var _a2 = $$.state, width = _a2.width, height = _a2.height;
    var cacheKey = KEY.bubbleBaseLength;
    var baseLength = $$.cache.get(cacheKey);
    if (!baseLength) {
      $$.cache.add(cacheKey, baseLength = getMinMax$1("min", [width, height]));
    }
    return baseLength;
  },
  /**
   * Get the radius value for bubble circle
   * @param {object} d Data object
   * @returns {number}
   * @private
   */
  getBubbleR: function(d) {
    var $$ = this;
    var maxR = $$.config.bubble_maxR;
    if (isFunction(maxR)) {
      maxR = maxR.bind($$.api)(d);
    } else if (!isNumber(maxR)) {
      maxR = $$.getBaseLength() / ($$.getMaxDataCount() * 2) + 12;
    }
    var max4 = getMinMax$1("max", $$.getMinMaxData().max.map(function(d2) {
      return $$.isBubbleZType(d2) ? $$.getBubbleZData(d2.value, "y") : isObject(d2.value) ? d2.value.mid : d2.value;
    }));
    var maxArea = maxR * maxR * Math.PI;
    var area2 = ($$.isBubbleZType(d) ? $$.getBubbleZData(d.value, "z") : d.value) * (maxArea / max4);
    return Math.sqrt(area2 / Math.PI);
  },
  /**
   * Get bubble dimension data
   * @param {object|Array} d data value
   * @param {string} type - y or z
   * @returns {number}
   * @private
   */
  getBubbleZData: function(d, type2) {
    return isObject(d) ? d[type2] : d[type2 === "y" ? 0 : 1];
  }
};
var shapeLine = {
  initLine: function() {
    var $el = this.$el;
    $el.line = $el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $LINE.chartLines).call(this.setCssRule(false, ".".concat($LINE.chartLines), ["pointer-events:none"]));
  },
  updateTargetsForLine: function(t) {
    var $$ = this;
    var _a2 = $$.$el, area2 = _a2.area, line2 = _a2.line, main2 = _a2.main;
    var classChartLine = $$.getChartClass("Line");
    var classLines = $$.getClass("lines", true);
    var classFocus = $$.classFocus.bind($$);
    if (!line2) {
      $$.initLine();
    }
    var targets = t.filter(function(d) {
      return !($$.isScatterType(d) || $$.isBubbleType(d));
    });
    var mainLineUpdate = main2.select(".".concat($LINE.chartLines)).selectAll(".".concat($LINE.chartLine)).data(targets).attr("class", function(d) {
      return classChartLine(d) + classFocus(d);
    });
    var mainLineEnter = mainLineUpdate.enter().append("g").attr("class", classChartLine).style("opacity", "0").style("pointer-events", $$.getStylePropValue("none"));
    mainLineEnter.append("g").attr("class", classLines);
    if ($$.hasTypeOf("Area")) {
      var mainLine = (!area2 && mainLineEnter.empty() ? mainLineUpdate : mainLineEnter).filter($$.isAreaType.bind($$));
      $$.initArea(mainLine);
    }
    $$.updateTargetForCircle(targets, mainLineEnter);
  },
  /**
   * Generate/Update elements
   * @param {boolean} withTransition Transition for exit elements
   * @param {boolean} isSub Subchart draw
   * @private
   */
  updateLine: function(withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var extraLineClasses = $$.format.extraLineClasses, $el = $$.$el, $T = $$.$T;
    var $root = isSub ? $el.subchart : $el;
    var line2 = $root.main.selectAll(".".concat($LINE.lines)).selectAll(".".concat($LINE.line)).data($$.lineData.bind($$));
    $T(line2.exit(), withTransition).style("opacity", "0").remove();
    $root.line = line2.enter().append("path").attr("class", function(d) {
      return "".concat($$.getClass("line", true)(d), " ").concat(extraLineClasses(d) || "");
    }).style("stroke", $$.color).merge(line2).style("opacity", $$.initialOpacity.bind($$)).attr("transform", null);
  },
  /**
   * Redraw function
   * @param {Function} drawFn Retuned functino from .generateDrawCandlestick()
   * @param {boolean} withTransition With or without transition
   * @param {boolean} isSub Subchart draw
   * @returns {Array}
   */
  redrawLine: function(drawFn, withTransition, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var $el = $$.$el, $T = $$.$T;
    var line2 = (isSub ? $el.subchart : $el).line;
    return [
      $T(line2, withTransition, getRandom()).attr("d", drawFn).style("stroke", this.color).style("opacity", null)
    ];
  },
  /**
   * Get the curve interpolate
   * @param {Array} d Data object
   * @returns {Function}
   * @private
   */
  getCurve: function(d) {
    var $$ = this;
    var isRotatedStepType = $$.config.axis_rotated && $$.isStepType(d);
    return isRotatedStepType ? function(context) {
      var step2 = $$.getInterpolate(d)(context);
      step2.orgPoint = step2.point;
      step2.pointRotated = function(x3, y3) {
        this._point === 1 && (this._point = 2);
        var y1 = this._y * (1 - this._t) + y3 * this._t;
        this._context.lineTo(this._x, y1);
        this._context.lineTo(x3, y1);
        this._x = x3;
        this._y = y3;
      };
      step2.point = function(x3, y3) {
        this._point === 0 ? this.orgPoint(x3, y3) : this.pointRotated(x3, y3);
      };
      return step2;
    } : $$.getInterpolate(d);
  },
  generateDrawLine: function(lineIndices, isSub) {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale;
    var lineConnectNull = config.line_connectNull;
    var isRotated = config.axis_rotated;
    var getPoints = $$.generateGetLinePoints(lineIndices, isSub);
    var yScale = $$.getYScaleById.bind($$);
    var xValue = function(d) {
      return (isSub ? $$.subxx : $$.xx).call($$, d);
    };
    var yValue = function(d, i) {
      return $$.isGrouped(d.id) ? getPoints(d, i)[0][1] : yScale(d.id, isSub)($$.getBaseValue(d));
    };
    var line2 = line_default();
    line2 = isRotated ? line2.x(yValue).y(xValue) : line2.x(xValue).y(yValue);
    if (!lineConnectNull) {
      line2 = line2.defined(function(d) {
        return $$.getBaseValue(d) !== null;
      });
    }
    var x3 = isSub ? scale2.subX : scale2.x;
    return function(d) {
      var y3 = yScale(d.id, isSub);
      var values = lineConnectNull ? $$.filterRemoveNull(d.values) : d.values;
      var x0 = 0;
      var y0 = 0;
      var path2;
      if ($$.isLineType(d)) {
        var regions2 = config.data_regions[d.id];
        if (regions2) {
          path2 = $$.lineWithRegions(values, scale2.zoom || x3, y3, regions2);
        } else {
          if ($$.isStepType(d)) {
            values = $$.convertValuesToStep(values);
          }
          path2 = line2.curve($$.getCurve(d))(values);
        }
      } else {
        if (values[0]) {
          x0 = x3(values[0].x);
          y0 = y3(values[0].value);
        }
        path2 = isRotated ? "M ".concat(y0, " ").concat(x0) : "M ".concat(x0, " ").concat(y0);
      }
      return path2 || "M 0 0";
    };
  },
  lineWithRegions: function(d, x3, y3, _regions) {
    var $$ = this;
    var config = $$.config;
    var isRotated = config.axis_rotated;
    var isTimeSeries = $$.axis.isTimeSeries();
    var regions2 = [];
    var dasharray = "2 2";
    var xp;
    var yp;
    var diff;
    var diffx2;
    if (isDefined(_regions)) {
      var getValue = function(v, def) {
        return isUndefined(v) ? def : isTimeSeries ? parseDate.call($$, v) : v;
      };
      for (var i = 0, reg = void 0; reg = _regions[i]; i++) {
        var start2 = getValue(reg.start, d[0].x);
        var end = getValue(reg.end, d[d.length - 1].x);
        var style2 = reg.style || { dasharray };
        regions2[i] = { start: start2, end, style: style2 };
      }
    }
    var xValue = isRotated ? function(dt) {
      return y3(dt.value);
    } : function(dt) {
      return x3(dt.x);
    };
    var yValue = isRotated ? function(dt) {
      return x3(dt.x);
    } : function(dt) {
      return y3(dt.value);
    };
    var generateM = function(points) {
      return "M".concat(points[0][0], ",").concat(points[0][1], "L").concat(points[1][0], ",").concat(points[1][1]);
    };
    var sWithRegion = isTimeSeries ? function(d0, d1, k2, timeseriesDiff) {
      var x0 = d0.x.getTime();
      var xDiff = d1.x - d0.x;
      var xv0 = new Date(x0 + xDiff * k2);
      var xv1 = new Date(x0 + xDiff * (k2 + timeseriesDiff));
      var points = isRotated ? [[y3(yp(k2)), x3(xv0)], [y3(yp(k2 + diff)), x3(xv1)]] : [[x3(xv0), y3(yp(k2))], [x3(xv1), y3(yp(k2 + diff))]];
      return generateM(points);
    } : function(d0, d1, k2, otherDiff) {
      var points = isRotated ? [[y3(yp(k2), true), x3(xp(k2))], [y3(yp(k2 + otherDiff), true), x3(xp(k2 + otherDiff))]] : [[x3(xp(k2), true), y3(yp(k2))], [x3(xp(k2 + otherDiff), true), y3(yp(k2 + otherDiff))]];
      return generateM(points);
    };
    var axisType = { x: $$.axis.getAxisType("x"), y: $$.axis.getAxisType("y") };
    var path2 = "";
    for (var i = 0, data2 = void 0; data2 = d[i]; i++) {
      var prevData = d[i - 1];
      var hasPrevData = prevData && isValue(prevData.value);
      var style2 = $$.isWithinRegions(data2.x, regions2);
      if (!isValue(data2.value)) {
        continue;
      }
      if (isUndefined(regions2) || !style2 || !hasPrevData) {
        path2 += "".concat(i && hasPrevData ? "L" : "M").concat(xValue(data2), ",").concat(yValue(data2));
      } else if (hasPrevData) {
        try {
          style2 = style2.dasharray.split(" ");
        } catch (e) {
          style2 = dasharray.split(" ");
        }
        xp = getScale(axisType.x, prevData.x, data2.x);
        yp = getScale(axisType.y, prevData.value, data2.value);
        var dx = x3(data2.x) - x3(prevData.x);
        var dy = y3(data2.value) - y3(prevData.value);
        var dd = Math.sqrt(Math.pow(dx, 2) + Math.pow(dy, 2));
        diff = style2[0] / dd;
        diffx2 = diff * style2[1];
        for (var j = diff; j <= 1; j += diffx2) {
          path2 += sWithRegion(prevData, data2, j, diff);
          if (j + diffx2 >= 1) {
            path2 += sWithRegion(prevData, data2, 1, 0);
          }
        }
      }
    }
    return path2;
  },
  isWithinRegions: function(withinX, withinRegions) {
    for (var i = 0, reg = void 0; reg = withinRegions[i]; i++) {
      if (reg.start < withinX && withinX <= reg.end) {
        return reg.style;
      }
    }
    return false;
  },
  isWithinStep: function(that, y3) {
    return Math.abs(y3 - getPointer(this.state.event, that)[1]) < 30;
  },
  shouldDrawPointsForLine: function(d) {
    var linePoint = this.config.line_point;
    return linePoint === true || isArray(linePoint) && linePoint.indexOf(d.id) !== -1;
  }
};
function hasValidPointDrawMethods(point6) {
  return isObjectType(point6) && isFunction(point6.create) && isFunction(point6.update);
}
function insertPointInfoDefs(point6, id2) {
  var _a2;
  var $$ = this;
  var copyAttr = function(from, target) {
    var attribs = from.attributes;
    for (var i = 0, name_1; name_1 = attribs[i]; i++) {
      name_1 = name_1.name;
      target.setAttribute(name_1, from.getAttribute(name_1));
    }
  };
  var doc$1 = new DOMParser().parseFromString(point6, "image/svg+xml");
  var node = doc$1.documentElement;
  var clone = doc.createElementNS(namespaces_default.svg, node.nodeName.toLowerCase());
  clone.id = id2;
  clone.style.fill = "inherit";
  clone.style.stroke = "inherit";
  copyAttr(node, clone);
  if ((_a2 = node.childNodes) === null || _a2 === void 0 ? void 0 : _a2.length) {
    var parent_1 = select_default2(clone);
    if ("innerHTML" in clone) {
      parent_1.html(node.innerHTML);
    } else {
      toArray(node.childNodes).forEach(function(v) {
        copyAttr(v, parent_1.append(v.tagName).node());
      });
    }
  }
  $$.$el.defs.node().appendChild(clone);
}
var shapePointCommon = {
  /**
   * Check if point type option is valid
   * @param {string} type point type
   * @returns {boolean}
   * @private
   */
  hasValidPointType: function(type2) {
    return /^(circle|rect(angle)?|polygon|ellipse|use)$/i.test(type2 || this.config.point_type);
  },
  /**
   * Check if pattern point is set to be used on legend
   * @returns {boolean}
   * @private
   */
  hasLegendDefsPoint: function() {
    var _a2;
    var config = this.config;
    return config.legend_show && ((_a2 = config.point_pattern) === null || _a2 === void 0 ? void 0 : _a2.length) && config.legend_usePoint;
  },
  getDefsPointId: function(id2) {
    var datetimeId = this.state.datetimeId;
    return "".concat(datetimeId, "-point").concat(id2);
  },
  /**
   * Get generate point function
   * @returns {Function}
   * @private
   */
  generatePoint: function() {
    var $$ = this;
    var $el = $$.$el, config = $$.config;
    var ids = [];
    var pattern = notEmpty(config.point_pattern) ? config.point_pattern : [config.point_type];
    return function(method, context) {
      var args = [];
      for (var _i = 2; _i < arguments.length; _i++) {
        args[_i - 2] = arguments[_i];
      }
      return function(d) {
        var _a2, _b, _c, _d;
        var id2 = $$.getTargetSelectorSuffix(d.id || ((_a2 = d.data) === null || _a2 === void 0 ? void 0 : _a2.id) || d);
        var element = select_default2(this);
        ids.indexOf(id2) < 0 && ids.push(id2);
        var point6 = pattern[ids.indexOf(id2) % pattern.length];
        if ($$.hasValidPointType(point6)) {
          point6 = $$[point6];
        } else if (!hasValidPointDrawMethods(point6 || config.point_type)) {
          var pointId = $$.getDefsPointId(id2);
          var defsPoint = $el.defs.select("#".concat(pointId));
          if (defsPoint.size() < 1) {
            insertPointInfoDefs.bind($$)(point6, pointId);
          }
          if (method === "create") {
            return (_b = $$.custom) === null || _b === void 0 ? void 0 : _b.create.bind(context).apply(void 0, __spreadArray([element, pointId], args, false));
          } else if (method === "update") {
            return (_c = $$.custom) === null || _c === void 0 ? void 0 : _c.update.bind(context).apply(void 0, __spreadArray([element], args, false));
          }
        }
        return (_d = point6[method]) === null || _d === void 0 ? void 0 : _d.bind(context).apply(void 0, __spreadArray([element], args, false));
      };
    };
  }
};
var getTransitionName = function() {
  return getRandom();
};
var shapePoint = {
  initialOpacityForCircle: function(d) {
    var _a2 = this, config = _a2.config, withoutFadeIn = _a2.state.withoutFadeIn;
    var opacity = config.point_opacity;
    if (isUndefined(opacity)) {
      opacity = this.getBaseValue(d) !== null && withoutFadeIn[d.id] ? this.opacityForCircle(d) : "0";
    }
    return opacity;
  },
  opacityForCircle: function(d) {
    var _a2;
    var config = this.config;
    var opacity = config.point_opacity;
    if (isUndefined(opacity)) {
      opacity = config.point_show && !((_a2 = this.isPointFocusOnly) === null || _a2 === void 0 ? void 0 : _a2.call(this)) ? null : "0";
      opacity = isValue(this.getBaseValue(d)) ? this.isBubbleType(d) || this.isScatterType(d) ? "0.5" : opacity : "0";
    }
    return opacity;
  },
  initCircle: function() {
    var $$ = this;
    var main2 = $$.$el.main;
    $$.point = $$.generatePoint();
    if (($$.hasType("bubble") || $$.hasType("scatter")) && main2.select(".".concat($CIRCLE.chartCircles)).empty()) {
      main2.select(".".concat($COMMON.chart)).append("g").attr("class", $CIRCLE.chartCircles);
    }
  },
  updateTargetForCircle: function(targetsValue, enterNodeValue) {
    var _this = this;
    var $$ = this;
    var config = $$.config, data2 = $$.data, $el = $$.$el;
    var selectionEnabled = config.interaction_enabled && config.data_selection_enabled;
    var isSelectable = selectionEnabled && config.data_selection_isselectable;
    var classCircles = $$.getClass("circles", true);
    if (!config.point_show) {
      return;
    }
    !$el.circle && $$.initCircle();
    var targets = targetsValue;
    var enterNode = enterNodeValue;
    if (!targets) {
      targets = data2.targets.filter(function(d) {
        return _this.isScatterType(d) || _this.isBubbleType(d);
      });
      var mainCircle = $el.main.select(".".concat($CIRCLE.chartCircles)).style("pointer-events", "none").selectAll(".".concat($CIRCLE.circles)).data(targets);
      mainCircle.exit().remove();
      enterNode = mainCircle.enter();
    }
    selectionEnabled && enterNode.append("g").attr("class", function(d) {
      return $$.generateClass($SELECT.selectedCircles, d.id);
    });
    enterNode.append("g").attr("class", classCircles).call(function(selection3) {
      $$.setCssRule(true, ".".concat($CIRCLE.circles), ["cursor:pointer"], isSelectable)(selection3);
      $$.setCssRule(true, " .".concat($CIRCLE.circle), ["fill", "stroke"], $$.color)(selection3);
    }).style("opacity", function() {
      var parent = select_default2(this.parentNode);
      return parent.attr("class").indexOf($CIRCLE.chartCircles) > -1 ? "0" : null;
    });
    selectionEnabled && targets.forEach(function(t) {
      $el.main.selectAll(".".concat($SELECT.selectedCircles).concat($$.getTargetSelectorSuffix(t.id))).selectAll("".concat($SELECT.selectedCircle)).each(function(d) {
        d.value = t.values[d.index].value;
      });
    });
  },
  updateCircle: function(isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var config = $$.config, state = $$.state, $el = $$.$el;
    var focusOnly = $$.isPointFocusOnly();
    var $root = isSub ? $el.subchart : $el;
    if (config.point_show && !state.toggling) {
      config.point_radialGradient && $$.updateLinearGradient();
      var circles = $root.main.selectAll(".".concat($CIRCLE.circles)).selectAll(".".concat($CIRCLE.circle)).data(function(d) {
        return $$.isLineType(d) && $$.shouldDrawPointsForLine(d) || $$.isBubbleType(d) || $$.isRadarType(d) || $$.isScatterType(d) ? focusOnly ? [d.values[0]] : d.values : [];
      });
      circles.exit().remove();
      circles.enter().filter(Boolean).append($$.point("create", this, $$.pointR.bind($$), $$.updateCircleColor.bind($$)));
      $root.circle = $root.main.selectAll(".".concat($CIRCLE.circles, " .").concat($CIRCLE.circle)).style("stroke", $$.getStylePropValue($$.color)).style("opacity", $$.initialOpacityForCircle.bind($$));
    }
  },
  /**
   * Update circle color
   * @param {object} d Data object
   * @returns {string} Color string
   * @private
   */
  updateCircleColor: function(d) {
    var $$ = this;
    var fn = $$.getStylePropValue($$.color);
    return $$.config.point_radialGradient ? $$.getGradienColortUrl(d.id) : fn ? fn(d) : null;
  },
  redrawCircle: function(cx, cy, withTransition, flow2, isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var rendered = $$.state.rendered, $el = $$.$el, $T = $$.$T;
    var $root = isSub ? $el.subchart : $el;
    var selectedCircles = $root.main.selectAll(".".concat($SELECT.selectedCircle));
    if (!$$.config.point_show) {
      return [];
    }
    var fn = $$.point("update", $$, cx, cy, $$.updateCircleColor.bind($$), withTransition, flow2, selectedCircles);
    var posAttr = $$.isCirclePoint() ? "c" : "";
    var t = getRandom();
    var opacityStyleFn = $$.opacityForCircle.bind($$);
    var mainCircles = [];
    $root.circle.each(function(d) {
      var result = fn.bind(this)(d);
      result = $T(result, withTransition || !rendered, t).style("opacity", opacityStyleFn);
      mainCircles.push(result);
    });
    return [
      mainCircles,
      $T(selectedCircles, withTransition).attr("".concat(posAttr, "x"), cx).attr("".concat(posAttr, "y"), cy)
    ];
  },
  /**
   * Show focused data point circle
   * @param {object} d Selected data
   * @private
   */
  showCircleFocus: function(d) {
    var $$ = this;
    var _a2 = $$.state, hasRadar = _a2.hasRadar, resizing = _a2.resizing, toggling = _a2.toggling, transiting = _a2.transiting, $el = $$.$el;
    var circle = $el.circle;
    if (transiting === false && circle && $$.isPointFocusOnly()) {
      var cx = (hasRadar ? $$.radarCircleX : $$.circleX).bind($$);
      var cy = (hasRadar ? $$.radarCircleY : $$.circleY).bind($$);
      var withTransition = toggling || isUndefined(d);
      var fn_1 = $$.point("update", $$, cx, cy, $$.getStylePropValue($$.color), resizing ? false : withTransition);
      if (d) {
        circle = circle.filter(function(t) {
          var _a3;
          var data2 = (_a3 = d.filter) === null || _a3 === void 0 ? void 0 : _a3.call(d, function(v) {
            return v.id === t.id;
          });
          return data2.length ? select_default2(this).datum(data2[0]) : false;
        });
      }
      circle.attr("class", this.updatePointClass.bind(this)).style("opacity", null).each(function(d2) {
        var id2 = d2.id, index2 = d2.index, value = d2.value;
        var visibility = "hidden";
        if (isValue(value)) {
          fn_1.bind(this)(d2);
          $$.expandCircles(index2, id2);
          visibility = "";
        }
        this.style.visibility = visibility;
      });
    }
  },
  /**
   * Hide focused data point circle
   * @private
   */
  hideCircleFocus: function() {
    var $$ = this;
    var circle = $$.$el.circle;
    if ($$.isPointFocusOnly() && circle) {
      $$.unexpandCircles();
      circle.style("visibility", "hidden");
    }
  },
  circleX: function(d) {
    return this.xx(d);
  },
  updateCircleY: function(isSub) {
    if (isSub === void 0) {
      isSub = false;
    }
    var $$ = this;
    var getPoints = $$.generateGetLinePoints($$.getShapeIndices($$.isLineType), isSub);
    return function(d, i) {
      var id2 = d.id;
      return $$.isGrouped(id2) ? getPoints(d, i)[0][1] : $$.getYScaleById(id2, isSub)($$.getBaseValue(d));
    };
  },
  expandCircles: function(i, id2, reset) {
    var $$ = this;
    var r = $$.pointExpandedR.bind($$);
    reset && $$.unexpandCircles();
    var circles = $$.getShapeByIndex("circle", i, id2).classed($COMMON.EXPANDED, true);
    var scale2 = r(circles) / $$.config.point_r;
    var ratio = 1 - scale2;
    if ($$.isCirclePoint()) {
      circles.attr("r", r);
    } else {
      circles.each(function() {
        var point6 = select_default2(this);
        if (this.tagName === "circle") {
          point6.attr("r", r);
        } else {
          var _a2 = this.getBBox(), width = _a2.width, height = _a2.height;
          var x3 = ratio * (+point6.attr("x") + width / 2);
          var y3 = ratio * (+point6.attr("y") + height / 2);
          point6.attr("transform", "translate(".concat(x3, " ").concat(y3, ") scale(").concat(scale2, ")"));
        }
      });
    }
  },
  unexpandCircles: function(i) {
    var $$ = this;
    var r = $$.pointR.bind($$);
    var circles = $$.getShapeByIndex("circle", i).filter(function() {
      return select_default2(this).classed($COMMON.EXPANDED);
    }).classed($COMMON.EXPANDED, false);
    circles.attr("r", r);
    if (!$$.isCirclePoint()) {
      var scale2 = r(circles) / $$.config.point_r;
      circles.attr("transform", scale2 !== 1 ? "scale(".concat(scale2, ")") : null);
    }
  },
  pointR: function(d) {
    var $$ = this;
    var config = $$.config;
    var pointR = config.point_r;
    var r = pointR;
    if ($$.isBubbleType(d)) {
      r = $$.getBubbleR(d);
    } else if (isFunction(pointR)) {
      r = pointR.bind($$.api)(d);
    }
    d.r = r;
    return r;
  },
  pointExpandedR: function(d) {
    var $$ = this;
    var config = $$.config;
    var scale2 = $$.isBubbleType(d) ? 1.15 : 1.75;
    return config.point_focus_expand_enabled ? config.point_focus_expand_r || $$.pointR(d) * scale2 : $$.pointR(d);
  },
  pointSelectR: function(d) {
    var $$ = this;
    var selectR = $$.config.point_select_r;
    return isFunction(selectR) ? selectR(d) : selectR || $$.pointR(d) * 4;
  },
  /**
   * Check if point.focus.only option can be applied.
   * @returns {boolean}
   * @private
   */
  isPointFocusOnly: function() {
    var $$ = this;
    return $$.config.point_focus_only && !$$.hasType("bubble") && !$$.hasType("scatter") && !$$.hasArcType(null, ["radar"]);
  },
  isWithinCircle: function(node, r) {
    var _a2 = this, config = _a2.config, state = _a2.state;
    var mouse = getPointer(state.event, node);
    var element = select_default2(node);
    var prefix = this.isCirclePoint(node) ? "c" : "";
    var sensitivity = config.point_sensitivity === "radius" ? node.getAttribute("r") : config.point_sensitivity;
    var cx = +element.attr("".concat(prefix, "x"));
    var cy = +element.attr("".concat(prefix, "y"));
    if (!(cx || cy) && node.nodeType === 1) {
      var _b = getBoundingRect(node), x3 = _b.x, y3 = _b.y;
      cx = x3;
      cy = y3;
    }
    return Math.sqrt(Math.pow(cx - mouse[0], 2) + Math.pow(cy - mouse[1], 2)) < (r || sensitivity);
  },
  /**
   * Get data point sensitivity radius
   * @param {object} d Data point object
   * @returns {number} return the sensitivity value
   */
  getPointSensitivity: function(d) {
    var $$ = this;
    var sensitivity = $$.config.point_sensitivity;
    if (isFunction(sensitivity)) {
      sensitivity = sensitivity.call($$.api, d);
    } else if (sensitivity === "radius") {
      sensitivity = d.r;
    }
    return sensitivity;
  },
  updatePointClass: function(d) {
    var $$ = this;
    var circle = $$.$el.circle;
    var pointClass = false;
    if (isObject(d) || circle) {
      pointClass = d === true ? circle.each(function(d2) {
        var className = $$.getClass("circle", true)(d2);
        if (this.getAttribute("class").indexOf($COMMON.EXPANDED) > -1) {
          className += " ".concat($COMMON.EXPANDED);
        }
        this.setAttribute("class", className);
      }) : $$.getClass("circle", true)(d);
    }
    return pointClass;
  },
  generateGetLinePoints: function(lineIndices, isSub) {
    var $$ = this;
    var config = $$.config;
    var x3 = $$.getShapeX(0, lineIndices, isSub);
    var y3 = $$.getShapeY(isSub);
    var lineOffset = $$.getShapeOffset($$.isLineType, lineIndices, isSub);
    var yScale = $$.getYScaleById.bind($$);
    return function(d, i) {
      var y0 = yScale.call($$, d.id, isSub)($$.getShapeYMin(d.id));
      var offset = lineOffset(d, i) || y0;
      var posX = x3(d);
      var posY = y3(d);
      if (config.axis_rotated && (d.value > 0 && posY < y0 || d.value < 0 && y0 < posY)) {
        posY = y0;
      }
      var point6 = [posX, posY - (y0 - offset)];
      return [
        point6,
        point6,
        // from here and below, needed for compatibility
        point6,
        point6
      ];
    };
  },
  custom: {
    create: function(element, id2, fillStyleFn) {
      return element.append("use").attr("xlink:href", "#".concat(id2)).attr("class", this.updatePointClass.bind(this)).style("fill", fillStyleFn).node();
    },
    update: function(element, xPosFn, yPosFn, fillStyleFn, withTransition, flow2, selectedCircles) {
      var $$ = this;
      var _a2 = element.node().getBBox(), width = _a2.width, height = _a2.height;
      var xPosFn2 = function(d) {
        return isValue(d.value) ? xPosFn(d) - width / 2 : 0;
      };
      var yPosFn2 = function(d) {
        return isValue(d.value) ? yPosFn(d) - height / 2 : 0;
      };
      var mainCircles = element;
      if (withTransition) {
        flow2 && mainCircles.attr("x", xPosFn2);
        mainCircles = $$.$T(mainCircles, withTransition, getTransitionName());
        selectedCircles && $$.$T(selectedCircles, withTransition, getTransitionName());
      }
      return mainCircles.attr("x", xPosFn2).attr("y", yPosFn2).style("fill", fillStyleFn);
    }
  },
  // 'circle' data point
  circle: {
    create: function(element, sizeFn, fillStyleFn) {
      return element.append("circle").attr("class", this.updatePointClass.bind(this)).attr("r", sizeFn).style("fill", fillStyleFn).node();
    },
    update: function(element, xPosFn, yPosFn, fillStyleFn, withTransition, flow2, selectedCircles) {
      var $$ = this;
      var mainCircles = element;
      if ($$.hasType("bubble")) {
        mainCircles.attr("r", $$.pointR.bind($$));
      }
      if (withTransition) {
        flow2 && mainCircles.attr("cx", xPosFn);
        if (mainCircles.attr("cx")) {
          mainCircles = $$.$T(mainCircles, withTransition, getTransitionName());
        }
        selectedCircles && $$.$T(mainCircles, withTransition, getTransitionName());
      }
      return mainCircles.attr("cx", xPosFn).attr("cy", yPosFn).style("fill", fillStyleFn);
    }
  },
  // 'rectangle' data point
  rectangle: {
    create: function(element, sizeFn, fillStyleFn) {
      var rectSizeFn = function(d) {
        return sizeFn(d) * 2;
      };
      return element.append("rect").attr("class", this.updatePointClass.bind(this)).attr("width", rectSizeFn).attr("height", rectSizeFn).style("fill", fillStyleFn).node();
    },
    update: function(element, xPosFn, yPosFn, fillStyleFn, withTransition, flow2, selectedCircles) {
      var $$ = this;
      var r = $$.config.point_r;
      var rectXPosFn = function(d) {
        return xPosFn(d) - r;
      };
      var rectYPosFn = function(d) {
        return yPosFn(d) - r;
      };
      var mainCircles = element;
      if (withTransition) {
        flow2 && mainCircles.attr("x", rectXPosFn);
        mainCircles = $$.$T(mainCircles, withTransition, getTransitionName());
        selectedCircles && $$.$T(selectedCircles, withTransition, getTransitionName());
      }
      return mainCircles.attr("x", rectXPosFn).attr("y", rectYPosFn).style("fill", fillStyleFn);
    }
  }
};
function getDataMax($$) {
  var levelMax = $$.config.polar_level_max;
  var dataMax = $$.getMinMaxData().max[0].value;
  if (levelMax && levelMax > dataMax) {
    dataMax = levelMax;
  }
  return dataMax;
}
var shapePolar = {
  /**
   * Initialize polar
   * @private
   */
  initPolar: function() {
    var $$ = this;
    var arcs = $$.$el.arcs, config = $$.config;
    var levelTextShow = config.polar_level_text_show;
    var levelTextBgColor = config.polar_level_text_backgroundColor;
    arcs.levels = arcs.append("g").attr("class", $LEVEL.levels);
    if (levelTextShow && levelTextBgColor) {
      $$.generateDataLabelBackgroundColorFilter(levelTextBgColor);
    }
  },
  /**
   * Get polar outer radius according to the data value
   * @param {object} d Data object
   * @param {numbet} outerRadius Outer radius
   * @returns {number} outer radius
   * @private
   */
  getPolarOuterRadius: function(d, outerRadius) {
    var _a2;
    var dataMax = getDataMax(this);
    return ((_a2 = d === null || d === void 0 ? void 0 : d.data.values[0].value) !== null && _a2 !== void 0 ? _a2 : 0) / dataMax * outerRadius;
  },
  /**
   * Update polar based on given data array
   * @param {object} targets Data object
   * @private
   */
  updateTargetsForPolar: function(targets) {
    this.updateTargetsForArc(targets);
  },
  /**
   * Called whenever redraw happens
   * @private
   */
  redrawPolar: function() {
    var $$ = this;
    var config = $$.config;
    config.polar_level_show && $$.updatePolarLevel();
  },
  /**
   * Update polar level circle
   * @private
   */
  updatePolarLevel: function() {
    var $$ = this;
    var config = $$.config, state = $$.state, levels = $$.$el.arcs.levels;
    var depth = config.polar_level_depth;
    var dataMax = getDataMax($$);
    var levelData = getRange(0, depth);
    var outerRadius = state.radius;
    var levelRatio = levelData.map(function(l) {
      return outerRadius * ((l + 1) / depth);
    });
    var levelTextFormat = (config.polar_level_text_format || function() {
    }).bind($$.api);
    var level = levels.selectAll(".".concat($LEVEL.level)).data(levelData);
    level.exit().remove();
    var levelEnter = level.enter().append("g").attr("class", function(d, i) {
      return "".concat($LEVEL.level, " ").concat($LEVEL.level, "-").concat(i);
    });
    levelEnter.append("circle");
    levelEnter.merge(level).selectAll("circle").style("visibility", config.polar_level_show ? null : "hidden").attr("cx", 0).attr("cy", 0).attr("r", function(d) {
      return levelRatio[d];
    });
    if (config.polar_level_text_show) {
      var levelTextBackgroundColor = config.polar_level_text_backgroundColor;
      var defsId = "#".concat(state.datetimeId, "-labels-bg").concat($$.getTargetSelectorSuffix(levelTextBackgroundColor));
      levelEnter.append("text").style("text-anchor", "middle");
      levelEnter.merge(level).selectAll("text").attr("dy", function(d) {
        return -levelRatio[d] + 5;
      }).attr("filter", levelTextBackgroundColor ? "url(".concat(defsId, ")") : null).text(function(d) {
        return levelTextFormat(dataMax / levelData.length * (d + 1));
      });
    }
  }
};
function getPosition(isClockwise, type2, edge, pos, range2, ratio) {
  var index2 = isClockwise && pos > 0 ? edge - pos : pos;
  var r = 2 * Math.PI;
  var func = type2 === "x" ? Math.sin : Math.cos;
  return range2 * (1 - ratio * func(index2 * r / edge));
}
var cacheKeyPoints = KEY.radarPoints;
var cacheKeyTextWidth = KEY.radarTextWidth;
var shapeRadar = {
  initRadar: function() {
    var $$ = this;
    var config = $$.config, current = $$.state.current, $el = $$.$el;
    if ($$.hasType("radar")) {
      $el.radar = $el.main.select(".".concat($COMMON.chart)).append("g").attr("class", $RADAR.chartRadars);
      $el.radar.levels = $el.radar.append("g").attr("class", $LEVEL.levels);
      $el.radar.axes = $el.radar.append("g").attr("class", $AXIS.axis);
      $el.radar.shapes = $el.radar.append("g").attr("class", $SHAPE.shapes);
      current.dataMax = config.radar_axis_max || $$.getMinMaxData().max[0].value;
      if (config.radar_axis_text_show) {
        config.interaction_enabled && $$.bindRadarEvent();
        $$.updateRadarLevel();
        $$.updateRadarAxes();
      }
    }
  },
  getRadarSize: function() {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, arcWidth = _a2.arcWidth, arcHeight = _a2.arcHeight;
    var padding = config.axis_x_categories.length < 4 ? -20 : 10;
    var size2 = (Math.min(arcWidth, arcHeight) - padding) / 2;
    return [size2, size2];
  },
  updateTargetsForRadar: function(targets) {
    var $$ = this;
    var config = $$.config;
    if (isEmpty(config.axis_x_categories)) {
      config.axis_x_categories = getRange(0, getMinMax$1("max", targets.map(function(v) {
        return v.values.length;
      })));
    }
    $$.generateRadarPoints();
  },
  getRadarPosition: function(type2, index2, range2, ratio) {
    var $$ = this;
    var config = $$.config;
    var _a2 = $$.getRadarSize(), width = _a2[0], height = _a2[1];
    var edge = config.axis_x_categories.length;
    var isClockwise = config.radar_direction_clockwise;
    var pos = toArray(type2).map(function(v) {
      return getPosition(isClockwise, v, edge, index2, isDefined(range2) ? range2 : type2 === "x" ? width : height, isNumber(ratio) ? ratio : config.radar_size_ratio);
    });
    return pos.length === 1 ? pos[0] : pos;
  },
  /**
   * Generate data points
   * @private
   */
  generateRadarPoints: function() {
    var $$ = this;
    var targets = $$.data.targets;
    var _a2 = $$.getRadarSize(), width = _a2[0], height = _a2[1];
    var points = $$.cache.get(cacheKeyPoints) || {};
    var size2 = points._size;
    if (!size2 || size2.width !== width && size2.height !== height) {
      targets.forEach(function(d) {
        points[d.id] = d.values.map(function(v, i) {
          return $$.getRadarPosition(["x", "y"], i, void 0, $$.getRatio("radar", v));
        });
      });
      points._size = { width, height };
      $$.cache.add(cacheKeyPoints, points);
    }
  },
  redrawRadar: function() {
    var $$ = this;
    var _a2 = $$.$el, radar2 = _a2.radar, main2 = _a2.main;
    var translate = $$.getTranslate("radar");
    if (translate) {
      radar2.attr("transform", translate);
      main2.select(".".concat($TEXT.chartTexts)).attr("transform", translate);
      $$.generateRadarPoints();
      $$.updateRadarLevel();
      $$.updateRadarAxes();
      $$.updateRadarShape();
    }
  },
  generateGetRadarPoints: function() {
    var points = this.cache.get(cacheKeyPoints);
    return function(d, i) {
      var point6 = points[d.id][i];
      return [
        point6,
        point6,
        point6,
        point6
      ];
    };
  },
  updateRadarLevel: function() {
    var $$ = this;
    var config = $$.config, state = $$.state, radar2 = $$.$el.radar;
    var _a2 = $$.getRadarSize(), width = _a2[0], height = _a2[1];
    var depth = config.radar_level_depth;
    var edge = config.axis_x_categories.length;
    var showText = config.radar_level_text_show;
    var radarLevels = radar2.levels;
    var levelData = getRange(0, depth);
    var radius = config.radar_size_ratio * Math.min(width, height);
    var levelRatio = levelData.map(function(l) {
      return radius * ((l + 1) / depth);
    });
    var levelTextFormat = (config.radar_level_text_format || function() {
    }).bind($$.api);
    var points = levelData.map(function(v) {
      var range2 = levelRatio[v];
      var pos = getRange(0, edge).map(function(i) {
        return $$.getRadarPosition(["x", "y"], i, range2, 1).join(",");
      });
      return pos.join(" ");
    });
    var level = radarLevels.selectAll(".".concat($LEVEL.level)).data(levelData);
    level.exit().remove();
    var levelEnter = level.enter().append("g").attr("class", function(d, i) {
      return "".concat($LEVEL.level, " ").concat($LEVEL.level, "-").concat(i);
    });
    levelEnter.append("polygon").style("visibility", config.radar_level_show ? null : "hidden");
    if (showText) {
      if (radarLevels.select("text").empty()) {
        radarLevels.append("text").attr("dx", "-.5em").attr("dy", "-.7em").style("text-anchor", "end").text(function() {
          return levelTextFormat(0);
        });
      }
      levelEnter.append("text").attr("dx", "-.5em").style("text-anchor", "end").text(function(d) {
        return levelTextFormat(state.current.dataMax / levelData.length * (d + 1));
      });
    }
    levelEnter.merge(level).attr("transform", function(d) {
      return "translate(".concat(width - levelRatio[d], ", ").concat(height - levelRatio[d], ")");
    }).selectAll("polygon").attr("points", function(d) {
      return points[d];
    });
    if (showText) {
      radarLevels.selectAll("text").attr("x", function(d) {
        return isUndefined(d) ? width : points[d].split(",")[0];
      }).attr("y", function(d) {
        return isUndefined(d) ? height : 0;
      });
    }
  },
  updateRadarAxes: function() {
    var $$ = this;
    var config = $$.config, radar2 = $$.$el.radar;
    var _a2 = $$.getRadarSize(), width = _a2[0], height = _a2[1];
    var categories = config.axis_x_categories;
    var axis3 = radar2.axes.selectAll("g").data(categories);
    axis3.exit().remove();
    var axisEnter = axis3.enter().append("g").attr("class", function(d, i) {
      return "".concat($AXIS.axis, "-").concat(i);
    });
    config.radar_axis_line_show && axisEnter.append("line");
    config.radar_axis_text_show && axisEnter.append("text");
    axis3 = axisEnter.merge(axis3);
    if (config.radar_axis_line_show) {
      axis3.select("line").attr("x1", width).attr("y1", height).attr("x2", function(d, i) {
        return $$.getRadarPosition("x", i);
      }).attr("y2", function(d, i) {
        return $$.getRadarPosition("y", i);
      });
    }
    if (config.radar_axis_text_show) {
      var _b = config.radar_axis_text_position, _c = _b.x, x_1 = _c === void 0 ? 0 : _c, _d = _b.y, y_1 = _d === void 0 ? 0 : _d;
      var textWidth = $$.cache.get(cacheKeyTextWidth) || 0;
      axis3.select("text").style("text-anchor", "middle").attr("dy", ".5em").call(function(selection3) {
        selection3.each(function(d) {
          setTextValue(select_default2(this), String(d), [-0.6, 1.2]);
        });
      }).datum(function(d, i) {
        return { index: i };
      }).attr("transform", function(d) {
        if (isUndefined(this.width)) {
          this.width = this.getBoundingClientRect().width / 2;
        }
        var posX = $$.getRadarPosition("x", d.index, void 0, 1);
        var posY = Math.round($$.getRadarPosition("y", d.index, void 0, 1));
        if (posX > width) {
          posX += this.width + x_1;
        } else if (Math.round(posX) < width) {
          posX -= this.width + x_1;
        }
        if (posY > height) {
          if (posY / 2 === height && this.firstChild.tagName === "tspan") {
            this.firstChild.setAttribute("dy", "0em");
          }
          posY += y_1;
        } else if (posY < height) {
          posY -= y_1;
        }
        return "translate(".concat(posX, " ").concat(posY, ")");
      });
      if (!textWidth) {
        var widths = [radar2.axes, radar2.levels].map(function(v) {
          return getPathBox(v.node()).width;
        });
        if (widths.every(function(v) {
          return v > 0;
        })) {
          $$.cache.add(cacheKeyTextWidth, widths[0] - widths[1]);
        }
      }
    }
  },
  bindRadarEvent: function() {
    var $$ = this;
    var state = $$.state, _a2 = $$.$el, radar2 = _a2.radar, svg = _a2.svg;
    var focusOnly = $$.isPointFocusOnly();
    var inputType = state.inputType, transiting = state.transiting;
    var isMouse = inputType === "mouse";
    var hide = function(event) {
      state.event = event;
      var index2 = $$.getDataIndexFromEvent(event);
      var noIndex = isUndefined(index2);
      if (isMouse || noIndex) {
        $$.hideTooltip();
        focusOnly ? $$.hideCircleFocus() : $$.unexpandCircles();
        if (isMouse) {
          $$.setOverOut(false, index2);
        } else if (noIndex) {
          $$.callOverOutForTouch();
        }
      }
    };
    radar2.axes.on(isMouse ? "mouseover " : "touchstart", function(event) {
      if (transiting) {
        return;
      }
      state.event = event;
      var index2 = $$.getDataIndexFromEvent(event);
      $$.selectRectForSingle(svg.node(), index2);
      isMouse ? $$.setOverOut(true, index2) : $$.callOverOutForTouch(index2);
    }).on("mouseout", isMouse ? hide : null);
    if (!isMouse) {
      svg.on("touchstart", hide);
    }
  },
  updateRadarShape: function() {
    var $$ = this;
    var targets = $$.data.targets.filter(function(d) {
      return $$.isRadarType(d);
    });
    var points = $$.cache.get(cacheKeyPoints);
    var areas = $$.$el.radar.shapes.selectAll("polygon").data(targets);
    var areasEnter = areas.enter().append("g").attr("class", $$.getChartClass("Radar"));
    $$.$T(areas.exit()).remove();
    areasEnter.append("polygon").merge(areas).style("fill", $$.color).style("stroke", $$.color).attr("points", function(d) {
      return points[d.id].join(" ");
    });
    $$.updateTargetForCircle(targets, areasEnter);
  },
  /**
   * Get data point x coordinate
   * @param {object} d Data object
   * @returns {number}
   * @private
   */
  radarCircleX: function(d) {
    return this.cache.get(cacheKeyPoints)[d.id][d.index][0];
  },
  /**
   * Get data point y coordinate
   * @param {object} d Data object
   * @returns {number}
   * @private
   */
  radarCircleY: function(d) {
    return this.cache.get(cacheKeyPoints)[d.id][d.index][1];
  }
};
function position(group2, root2) {
  var $$ = this;
  var _a2 = $$.scale, x3 = _a2.x, y3 = _a2.y, width = $$.state.width;
  group2.selectAll("g").attr("transform", function(d) {
    return "translate(".concat(d === root2 ? "0,0" : "".concat(x3(d.x0), ",").concat(y3(d.y0)), ")");
  }).select("rect").attr("width", function(d) {
    return d === root2 ? width : x3(d.x1) - x3(d.x0);
  }).attr("height", function(d) {
    return d === root2 ? 0 : y3(d.y1) - y3(d.y0);
  });
}
function convertDataToTreemapData(data2) {
  var $$ = this;
  return data2.map(function(d) {
    var id2 = d.id, values = d.values;
    var value = values[0].value;
    return {
      name: id2,
      id: id2,
      // needed to keep compatibility on whole code logic
      value,
      ratio: $$.getRatio("treemap", values[0])
    };
  });
}
var shapeTreemap = {
  initTreemap: function() {
    var $$ = this;
    var $el = $$.$el, _a2 = $$.state, _b = _a2.current, width = _b.width, height = _b.height, clip2 = _a2.clip, datetimeId = _a2.datetimeId;
    clip2.id = "".concat(datetimeId, "-clip");
    $$.treemap = treemap_default().tile($$.getTreemapTile());
    $$.treemapFn = function(data2) {
      var hierarchyData = hierarchy(data2).sum(function(d) {
        return d.value;
      });
      var sortFn = $$.getSortCompareFn(true);
      return $$.treemap(sortFn ? hierarchyData.sort(sortFn) : hierarchyData);
    };
    $el.defs.append("clipPath").attr("id", clip2.id).append("rect").attr("width", width).attr("height", height);
    $el.treemap = $el.main.select(".".concat($COMMON.chart)).attr("clip-path", "url(#".concat(clip2.id, ")")).append("g").classed($TREEMAP.chartTreemaps, true);
    $$.bindTreemapEvent();
  },
  /**
   * Bind events
   * @private
   */
  bindTreemapEvent: function() {
    var $$ = this;
    var $el = $$.$el, config = $$.config, state = $$.state;
    var getTarget = function(event) {
      var _a2;
      var target = event.isTrusted ? event.target : (_a2 = state.eventReceiver.rect) === null || _a2 === void 0 ? void 0 : _a2.node();
      var data2;
      if (/^rect$/i.test(target.tagName)) {
        state.event = event;
        data2 = select_default2(target).datum();
      }
      return data2 === null || data2 === void 0 ? void 0 : data2.data;
    };
    if (config.interaction_enabled) {
      var isTouch = state.inputType === "touch";
      $el.treemap.on(isTouch ? "touchstart" : "mouseover mousemove", function(event) {
        var data2 = getTarget(event);
        if (data2) {
          $$.showTooltip([data2], event.currentTarget);
          /^(touchstart|mouseover)$/.test(event.type) && $$.setOverOut(true, data2);
        }
      }).on(isTouch ? "touchend" : "mouseout", function(event) {
        var data2 = getTarget(event);
        $$.hideTooltip();
        $$.setOverOut(false, data2);
      });
    }
  },
  /**
   * Get tiling function
   * @returns {Function}
   * @private
   */
  getTreemapTile: function() {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, _c = $$.state.current, width = _c.width, height = _c.height;
    var tile = (_b = {
      "binary": binary_default,
      "dice": dice_default,
      "slice": slice_default,
      "sliceDice": sliceDice_default,
      "squarify": squarify_default,
      "resquarify": resquarify_default
    }[(_a2 = config.treemap_tile) !== null && _a2 !== void 0 ? _a2 : "binary"]) !== null && _b !== void 0 ? _b : binary_default;
    return function(node, x0, y0, x1, y1) {
      tile(node, 0, 0, width, height);
      for (var _i = 0, _a3 = node.children; _i < _a3.length; _i++) {
        var child = _a3[_i];
        child.x0 = x0 + child.x0 / width * (x1 - x0);
        child.x1 = x0 + child.x1 / width * (x1 - x0);
        child.y0 = y0 + child.y0 / height * (y1 - y0);
        child.y1 = y0 + child.y1 / height * (y1 - y0);
      }
    };
  },
  /**
   * Get treemap hierarchy data
   * @param {Array} targets Data targets
   * @returns {object}
   * @private
   */
  getTreemapData: function(targets) {
    var $$ = this;
    return {
      name: "root",
      children: convertDataToTreemapData.bind($$)($$.filterTargetsToShow(targets.filter($$.isTreemapType, $$)))
    };
  },
  /**
   * Update treemap data
   * @param {Array} targets Data targets
   * @private
   */
  updateTargetsForTreemap: function(targets) {
    var $$ = this;
    var treemap2 = $$.$el.treemap;
    var treemapData = $$.treemapFn($$.getTreemapData(targets !== null && targets !== void 0 ? targets : $$.data.targets));
    treemap2.data(treemapData);
  },
  /**
   * Render treemap
   * @param {number} durationForExit Duration for exit transition
   * @private
   */
  updateTreemap: function(durationForExit) {
    var $$ = this;
    var $el = $$.$el, $T = $$.$T;
    var data2 = $el.treemap.datum();
    var classChartTreemap = $$.getChartClass("Treemap");
    var treemap2 = $el.treemap.selectAll("g").data(data2.children);
    $T(treemap2.exit(), durationForExit).style("opacity", "0").remove();
    treemap2.enter().append("g").append("rect");
    $el.treemap.selectAll("g").attr("class", classChartTreemap).select("rect").attr("fill", function(d) {
      return $$.color(d.data.name);
    });
  },
  /**
   * Generate treemap coordinate points data
   * @returns {Array} Array of coordinate points
   * @private
   */
  generateGetTreemapPoints: function() {
    var $$ = this;
    var $el = $$.$el, _a2 = $$.scale, x3 = _a2.x, y3 = _a2.y;
    var points = {};
    $el.treemap.selectAll("g").each(function(d) {
      points[d.data.name] = [
        [x3(d.x0), y3(d.y0)],
        [x3(d.x1), y3(d.y1)]
      ];
    });
    return function(d) {
      return points[d.id];
    };
  },
  /**
   * Redraw treemap
   * @param {boolean} withTransition With or without transition
   * @returns {Array} Selections
   * @private
   */
  redrawTreemap: function(withTransition) {
    var $$ = this;
    var $el = $$.$el, _a2 = $$.state.current, width = _a2.width, height = _a2.height;
    $el.defs.select("rect").attr("width", width).attr("height", height);
    return [
      $$.$T($el.treemap, withTransition, getRandom()).call(position.bind($$), $el.treemap.datum())
    ];
  },
  /**
   * Get treemap data label format function
   * @param {object} d Data object
   * @returns {Function}
   * @private
   */
  treemapDataLabelFormat: function(d) {
    var $$ = this;
    var config = $$.config;
    var id2 = d.id, value = d.value;
    var format3 = config.treemap_label_format;
    var ratio = $$.getRatio("treemap", d);
    var percentValue = (ratio * 100).toFixed(2);
    var meetLabelThreshold = config.treemap_label_show && $$.meetsLabelThreshold(ratio, "treemap") ? null : "0";
    return function(node) {
      node.style("opacity", meetLabelThreshold);
      return isFunction(format3) ? format3.bind($$.api)(value, ratio, id2) : "".concat(id2, "\n").concat(percentValue, "%");
    };
  }
};
var optPoint = {
  /**
   * Set point options
   * @name point
   * @memberof Options
   * @type {object}
   * @property {object} point Point object
   * @property {boolean} [point.show=true] Whether to show each point in line.
   * @property {number|Function} [point.r=2.5] The radius size of each point.
   *  - **NOTE:** Disabled for 'bubble' type
   * @property {boolean|object} [point.radialGradient=false] Set the radial gradient on point.<br><br>
   * Or customize by giving below object value:
   *  - cx {number}: `cx` value (default: `0.3`)
   *  - cy {number}: `cy` value (default: `0.3`)
   *  - r {number}: `r` value (default: `0.7`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0.1, $DATA_COLOR, 1], [0.9, $DATA_COLOR, 0]]`)
   * @property {boolean} [point.focus.expand.enabled=true] Whether to expand each point on focus.
   * @property {number} [point.focus.expand.r=point.r*1.75] The radius size of each point on focus.
   *  - **NOTE:** For 'bubble' type, the default is `bubbleSize*1.15`
   * @property {boolean} [point.focus.only=false] Show point only when is focused.
   * @property {number|null} [point.opacity=undefined] Set point opacity value.
   * - **NOTE:**
   *	- `null` will make to not set inline 'opacity' css prop.
   *	- when no value(or undefined) is set, it defaults to set opacity value according its chart types.
   * @property {number|string|Function} [point.sensitivity=10] The senstivity value for interaction boundary.
   * - **Available Values:**
   *   - {number}: Absolute sensitivity value which is the distance from the data point in pixel.
   *   - "radius": sensitivity based on point's radius
   *   - Function: callback for each point to determine the sensitivity<br>
   *    	```js
   *   	sensitivity: function(d) {
   * 	  // ex. of argument d:
   * 	  // ==> {x: 2, value: 55, id: 'data3', index: 2, r: 19.820624179302296}
   *
   * 	  // returning d.r, will make sensitivity same as point's radius value.
   *  	  return d.r;
   * 	}
   * 	```
   * @property {number} [point.select.r=point.r*4] The radius size of each point on selected.
   * @property {string} [point.type="circle"] The type of point to be drawn
   * - **NOTE:**
   *   - If chart has 'bubble' type, only circle can be used.
   *   - For IE, non circle point expansions are not supported due to lack of transform support.
   * - **Available Values:**
   *   - circle
   *   - rectangle
   * @property {Array} [point.pattern=[]] The type of point or svg shape as string, to be drawn for each line
   * - **NOTE:**
   *   - This is an `experimental` feature and can have some unexpected behaviors.
   *   - If chart has 'bubble' type, only circle can be used.
   *   - For IE, non circle point expansions are not supported due to lack of transform support.
   * - **Available Values:**
   *   - circle
   *   - rectangle
   *   - svg shape tag interpreted as string<br>
   *     (ex. `<polygon points='2.5 0 0 5 5 5'></polygon>`)
   * @see [Demo: point type](https://naver.github.io/billboard.js/demo/#Point.RectanglePoints)
   * @see [Demo: point focus only](https://naver.github.io/billboard.js/demo/#Point.FocusOnly)
   * @see [Demo: point radialGradient](https://naver.github.io/billboard.js/demo/#Point.RadialGradientPoint)
   * @see [Demo: point sensitivity](https://naver.github.io/billboard.js/demo/#Point.PointSensitivity)
   * @example
   *  point: {
   *      show: false,
   *      r: 5,
   *
   *      // or customize the radius
   *      r: function(d) {
   *          ...
   *          return r;
   *      },
   *
   *      // will generate follwing radialGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/radialGradient
   *      // <radualGradient cx="0.3" cy="0.3" r="0.7">
   *      //    <stop offset="0.1" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="0.9" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </radialrGradient>
   *      radialGradient: true,
   *
   *      // Or customized gradient
   *      radialGradient: {
   *      	cx: 0.3,  // cx attributes
   *      	cy: 0.5,  // cy attributes
   *      	r: 0.7,  // r attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      },
   *
   *      focus: {
   *          expand: {
   *              enabled: true,
   *              r: 1
   *          },
   *          only: true
   *      },
   *
   *      // do not set inline 'opacity' css prop setting
   *      opacity: null,
   *
   *      // set every data point's opacity value
   *      opacity: 0.7,
   *
   *      select: {
   *          r: 3
   *      },
   *
   *      // having lower value, means how closer to be for interaction
   *      sensitivity: 3,
   *
   *      // sensitivity based on point's radius
   *      sensitivity: "radius",
   *
   *      // callback for each point to determine the sensitivity
   *      sensitivity: function(d) {
   *	// ex. of argument d:
   *	// ==> {x: 2, value: 55, id: 'data3', index: 2, r: 19.820624179302296}
   *
   *	// returning d.r, will make sensitivity same as point's radius value.
   *	return d.r;
   *      }
   *
   *      // valid values are "circle" or "rectangle"
   *      type: "rectangle",
   *
   *      // or indicate as pattern
   *      pattern: [
   *        "circle",
   *        "rectangle",
   *        "<polygon points='0 6 4 0 -4 0'></polygon>"
   *     ],
   *  }
   */
  point_show: true,
  point_r: 2.5,
  point_radialGradient: false,
  point_sensitivity: 10,
  point_focus_expand_enabled: true,
  point_focus_expand_r: void 0,
  point_focus_only: false,
  point_opacity: void 0,
  point_pattern: [],
  point_select_r: void 0,
  point_type: "circle"
};
var optArea = {
  /**
   * Set area options
   * @name area
   * @memberof Options
   * @type {object}
   * @property {object} area Area object
   * @property {boolean} [area.above=false] Set background area `above` the data chart line.
   * @property {boolean} [area.below=false] Set background area `below` the data chart line.
   *  - **NOTE**: Can't be used along with `above` option. When above & below options are set to true, `above` will be prioritized.
   * @property {boolean} [area.front=true] Set area node to be positioned over line node.
   * @property {boolean|object} [area.linearGradient=false] Set the linear gradient on area.<br><br>
   * Or customize by giving below object value:
   *  - x {Array}: `x1`, `x2` value (default: `[0, 0]`)
   *  - y {Array}: `y1`, `y2` value (default: `[0, 1]`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0, $DATA_COLOR, 1], [1, $DATA_COLOR, 0]]`)
   * @property {boolean} [area.zerobased=true] Set if min or max value will be 0 on area chart.
   * @see [MDN's &lt;linearGradient>](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient), [&lt;stop>](https://developer.mozilla.org/en-US/docs/Web/SVG/Element/stop)
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Chart.AreaChart)
   * @see [Demo: above](https://naver.github.io/billboard.js/demo/#AreaChartOptions.Above)
   * @see [Demo: below](https://naver.github.io/billboard.js/demo/#AreaChartOptions.Below)
   * @see [Demo: linearGradient](https://naver.github.io/billboard.js/demo/#AreaChartOptions.LinearGradient)
   * @example
   *  area: {
   *      above: true,
   *      below: false,
   *      zerobased: false,
   *
   *      // <g class='bb-areas'> will be positioned behind the line <g class='bb-lines'> in stacking order
   *      front: false,
   *
   *      // will generate follwing linearGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient
   *      // <linearGradient x1="0" x2="0" y1="0" y2="1">
   *      //    <stop offset="0" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="1" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </linearGradient>
   *      linearGradient: true,
   *
   *      // Or customized gradient
   *      linearGradient: {
   *      	x: [0, 0],  // x1, x2 attributes
   *      	y: [0, 0],  // y1, y2 attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      }
   *  }
   */
  area_above: false,
  area_below: false,
  area_front: true,
  area_linearGradient: false,
  area_zerobased: true
};
var optBar = {
  /**
   * Set bar options
   * @name bar
   * @memberof Options
   * @type {object}
   * @property {object} bar Bar object
   * @property {boolean} [bar.front=false] Set 'bar' to be positioned over(on the top) other shapes elements.
   * @property {number} [bar.indices.removeNull=false] Remove nullish data on bar indices positions.
   * @property {number} [bar.label.threshold=0] Set threshold ratio to show/hide labels.
   * @property {boolean|object} [bar.linearGradient=false] Set the linear gradient on bar.<br><br>
   * Or customize by giving below object value:
   *  - x {Array}: `x1`, `x2` value (default: `[0, 0]`)
   *  - y {Array}: `y1`, `y2` value (default: `[0, 1]`)
   *  - stops {Array}: Each item should be having `[offset, stop-color, stop-opacity]` values.
   *    - (default: `[[0, $DATA_COLOR, 1], [1, $DATA_COLOR, 0]]`)
   * @property {boolean} [bar.overlap=false] Bars will be rendered at same position, which will be overlapped each other. (for non-grouped bars only)
   * @property {number} [bar.padding=0] The padding pixel value between each bar.
   * @property {number} [bar.radius] Set the radius of bar edge in pixel.
   * @property {number} [bar.radius.ratio] Set the radius ratio of bar edge in relative the bar's width.
   * @property {number} [bar.sensitivity=2] The senstivity offset value for interaction boundary.
   * @property {number} [bar.width] Change the width of bar chart.
   * @property {number} [bar.width.ratio=0.6] Change the width of bar chart by ratio.
   * @property {number} [bar.width.max] The maximum width value for ratio.
   * @property {number} [bar.width.dataname] Change the width of bar for indicated dataset only.
   * - **NOTE:**
   *   - Works only for non-stacked bar
   *   - Bars are centered accoding its total width value
   * @property {number} [bar.width.dataname.ratio=0.6] Change the width of bar chart by ratio.
   * @property {number} [bar.width.dataname.max] The maximum width value for ratio.
   * @property {boolean} [bar.zerobased=true] Set if min or max value will be 0 on bar chart.
   * @see [Demo: bar front](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarFront)
   * @see [Demo: bar indices](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarIndices)
   * @see [Demo: bar overlap](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarOverlap)
   * @see [Demo: bar padding](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarPadding)
   * @see [Demo: bar radius](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarRadius)
   * @see [Demo: bar width](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarWidth)
   * @see [Demo: bar width variant](https://naver.github.io/billboard.js/demo/#BarChartOptions.BarWidthVariant)
   * @example
   *  bar: {
   *      // make bar shape to be positioned over the other shape elements
   *      front: true,
   *
   *      // remove nullish data on bar indices postions
   *      indices: {
   *          removeNull: true
   *      },
   *
   *      // will generate follwing linearGradient:
   *      // for more info: https://developer.mozilla.org/en-US/docs/Web/SVG/Element/linearGradient
   *      // <linearGradient x1="0" x2="0" y1="0" y2="1">
   *      //    <stop offset="0" stop-color="$DATA_COLOR" stop-opacity="1"></stop>
   *      //    <stop offset="1" stop-color="$DATA_COLOR" stop-opacity="0"></stop>
   *      // </linearGradient>
   *      linearGradient: true,
   *
   *      // Or customized gradient
   *      linearGradient: {
   *      	x: [0, 0],  // x1, x2 attributes
   *      	y: [0, 0],  // y1, y2 attributes
   *      	stops: [
   *      	  // offset, stop-color, stop-opacity
   *      	  [0, "#7cb5ec", 1],
   *
   *      	  // setting 'null' for stop-color, will set its original data color
   *      	  [0.5, null, 0],
   *
   *      	  // setting 'function' for stop-color, will pass data id as argument.
   *      	  // It should return color string or null value
   *      	  [1, function(id) { return id === "data1" ? "red" : "blue"; }, 0],
   *      	]
   *      },
   *
   *      // remove nullish da
   *      overlap: true,
   *
   *      padding: 1,
   *
   *      // bar radius
   *      radius: 10,
   *      // or
   *      radius: {
   *          ratio: 0.5
   *      }
   *
   *      label: {
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the y Axis domain range value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *      },
   *
   *      // will not have offset between each bar elements for interaction
   *      sensitivity: 0,
   *
   *      width: 10,
   *
   *      // or
   *      width: {
   *          ratio: 0.2,
   *          max: 20
   *      },
   *
   *      // or specify width per dataset
   *      width: {
   *          data1: 20,
   *          data2: {
   *              ratio: 0.2,
   *              max: 20
   *          }
   *      },
   *
   *      zerobased: false
   *  }
   */
  bar_front: false,
  bar_indices_removeNull: false,
  bar_label_threshold: 0,
  bar_linearGradient: false,
  bar_overlap: false,
  bar_padding: 0,
  bar_radius: void 0,
  bar_radius_ratio: void 0,
  bar_sensitivity: 2,
  bar_width: void 0,
  bar_width_ratio: 0.6,
  bar_width_max: void 0,
  bar_zerobased: true
};
var optBubble = {
  /**
   * Set bubble options
   * @name bubble
   * @memberof Options
   * @type {object}
   * @property {object} bubble bubble object
   * @property {number|Function} [bubble.maxR=35] Set the max bubble radius value
   * @property {boolean} [bubble.zerobased=false] Set if min or max value will be 0 on bubble chart.
   * @example
   *  bubble: {
   *      // ex) If 100 is the highest value among data bound, the representation bubble of 100 will have radius of 50.
   *      // And the lesser will have radius relatively from tha max value.
   *      maxR: 50,
   *
   *      // or set radius callback
   *      maxR: function(d) {
   *          // ex. of d param - {x: Fri Oct 06 2017 00:00:00 GMT+0900, value: 80, id: "data2", index: 5}
   *          ...
   *          return Math.sqrt(d.value * 2);
   *      },
   *      zerobased: false
   *  }
   */
  bubble_maxR: 35,
  bubble_zerobased: false
};
var optCandlestick = {
  /**
   * Set candlestick options
   * @name candlestick
   * @memberof Options
   * @type {object}
   * @property {object} candlestick Candlestick object
   * @property {number} [candlestick.width] Change the width.
   * @property {number} [candlestick.width.ratio=0.6] Change the width by ratio.
   * @property {number} [candlestick.width.max] The maximum width value for ratio.
   * @property {number} [candlestick.width.dataname] Change the width for indicated dataset only.
   * @property {number} [candlestick.width.dataname.ratio=0.6] Change the width of bar chart by ratio.
   * @property {number} [candlestick.width.dataname.max] The maximum width value for ratio.
   * @property {object} [candlestick.color] Color setting.
   * @property {string|object} [candlestick.color.down] Change down(bearish) value color.
   * @property {string} [candlestick.color.down.dataname] Change down value color for indicated dataset only.
   *
   * @see [Demo](https://naver.github.io/billboard.js/demo/##Chart.CandlestickChart)
   * @example
   *  candlestick: {
   *      width: 10,
   *
   *      // or
   *      width: {
   *         	ratio: 0.2,
   *         	max: 20
   *      },
   *
   *      // or specify width per dataset
   *      width: {
   *         	data1: 20,
   *         	data2: {
   *         	    ratio: 0.2,
   *         		max: 20
   *         	}
   *      },
   *      color: {
   *  	  	// spcify bearish color
   *  	  	down: "red",
   *
   *  	  	// or specify color per dataset
   *  	  	down: {
   *  	  		data1: "red",
   *  	  		data2: "blue",
   *  	  	}
   *      }
   *  }
   */
  candlestick_width: void 0,
  candlestick_width_ratio: 0.6,
  candlestick_width_max: void 0,
  candlestick_color_down: "red"
};
var optLine = {
  /**
   * Set line options
   * @name line
   * @memberof Options
   * @type {object}
   * @property {object} line Line object
   * @property {boolean} [line.connectNull=false] Set if null data point will be connected or not.<br>
   *  If true set, the region of null data will be connected without any data point. If false set, the region of null data will not be connected and get empty.
   * @property {Array}   [line.classes=undefined] If set, used to set a css class on each line.
   * @property {boolean} [line.step.type=step] Change step type for step chart.<br>
   * **Available values:**
   * - step
   * - step-before
   * - step-after
   * @property {boolean} [line.step.tooltipMatch=false] Set to `true` for `step-before` and `step-after` types to have cursor/tooltip match to hovered step's point instead of nearest point.
   * @property {boolean|Array} [line.point=true] Set to false to not draw points on linecharts. Or pass an array of line ids to draw points for.
   * @property {boolean} [line.zerobased=false] Set if min or max value will be 0 on line chart.
   * @example
   *  line: {
   *      connectNull: true,
   *      classes: [
   *          "line-class1",
   *          "line-class2"
   *      ],
   *      step: {
   *          type: "step-after",
   *
   *          // to have cursor/tooltip match to hovered step's point instead of nearest point.
   *          tooltipMatch: true
   *      },
   *
   *      // hide all data points ('point.show=false' also has similar effect)
   *      point: false,
   *
   *      // show data points for only indicated datas
   *      point: [
   *          "data1", "data3"
   *      ],
   *
   *      zerobased: false
   *  }
   */
  line_connectNull: false,
  line_step_type: "step",
  line_step_tooltipMatch: false,
  line_zerobased: false,
  line_classes: void 0,
  line_point: true
};
var optScatter = {
  /**
   * Set scatter options
   * @name scatter
   * @memberof Options
   * @type {object}
   * @property {object} [scatter] scatter object
   * @property {boolean} [scatter.zerobased=false] Set if min or max value will be 0 on scatter chart.
   * @example
   *  scatter: {
   *      connectNull: true,
   *      step: {
   *          type: "step-after"
   *      },
   *
   *      // hide all data points ('point.show=false' also has similar effect)
   *      point: false,
   *
   *      // show data points for only indicated datas
   *      point: [
   *          "data1", "data3"
   *      ],
   *
   *      zerobased: false
   *  }
   */
  scatter_zerobased: false
};
var optSpline = {
  /**
   * Set spline options
   * - **Available interpolation type values:**
   *  - basis (d3.curveBasis)
   *  - basis-closed (d3.curveBasisClosed)
   *  - basis-open (d3.curveBasisOpen)
   *  - bundle (d3.curveBundle)
   *  - cardinal (d3.curveCardinal)
   *  - cardinal-closed (d3.curveCardinalClosed)
   *  - cardinal-open (d3.curveCardinalOpen)
   *  - catmull-rom (d3.curveCatmullRom)
   *  - catmull-rom-closed (d3.curveCatmullRomClosed)
   *  - catmull-rom-open (d3.curveCatmullRomOpen)
   *  - monotone-x (d3.curveMonotoneX)
   *  - monotone-y (d3.curveMonotoneY)
   *  - natural (d3.curveNatural)
   *  - linear-closed (d3.curveLinearClosed)
   *  - linear (d3.curveLinear)
   *  - step (d3.curveStep)
   *  - step-after (d3.curveStepAfter)
   *  - step-before (d3.curveStepBefore)
   * @name spline
   * @memberof Options
   * @type {object}
   * @property {object} spline Spline object
   * @property {object} spline.interpolation Spline interpolation object
   * @property {string} [spline.interpolation.type="cardinal"] Interpolation type
   * @see [Interpolation (d3 v4)](http://bl.ocks.org/emmasaunders/c25a147970def2b02d8c7c2719dc7502)
   * @example
   *  spline: {
   *      interpolation: {
   *          type: "cardinal"
   *      }
   *  }
   */
  spline_interpolation_type: "cardinal"
};
var optArc = {
  /**
   * Set arc options
   * @name arc
   * @memberof Options
   * @type {object}
   * @property {object} arc Arc object
   * @property {number|Function} [arc.cornerRadius=0] Set corner radius of Arc(donut/gauge/pie/polar) shape.
   *  - **NOTE:**
   * 	  - Corner radius can't surpass the `(outerRadius - innerRadius) /2` of indicated shape.
   * @property {number} [arc.cornerRadius.ratio=0] Set ratio relative of outer radius.
   * @property {object} [arc.needle] Set needle options.
   * @property {boolean} [arc.needle.show=false] Show or hide needle.
   * @property {string} [arc.needle.color] Set needle filled color.
   * @property {Function} [arc.needle.path] Set custom needle path function.
   *  - **NOTE:**
   *   - The path should be starting from 0,0 (which is center) to top center coordinate.
   *   - The function will receive, `length`{number} parameter which indicating the needle length in pixel relative to radius.
   * @property {number} [arc.needle.value] Set needle value.
   *  - **NOTE:**
   *   - For single gauge chart, needle will point the data value by default, otherwise will point 0(zero).
   * @property {number} [arc.needle.length=100] Set needle length in percentages relative to radius.
   * @property {object} [arc.needle.top] Set needle top options.
   * @property {number} [arc.needle.top.rx=0] Set needle top [rx radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.top.ry=0] Set needle top [ry radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.top.width=0] Set needle top width in pixel.
   * @property {object} [arc.needle.bottom] Set needle bottom options.
   * @property {number} [arc.needle.bottom.rx=1] Set needle bottom [rx radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.bottom.ry=1] Set needle bottom [ry radius value](https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve).
   * @property {number} [arc.needle.bottom.width=15] Set needle bottom width in pixel.
   * @property {number} [arc.needle.bottom.len=0] Set needle bottom length in pixel. Setting this value, will make bottom larger starting from center.
   * @property {object} [arc.rangeText] Set rangeText options.
   * @property {Array} [arc.rangeText.values] Set range text values to be shown around Arc.
   * - When `unit: 'absolute'`: Given values are treated as absolute values.
   * - When `unit: '%'`: Given values are treated as percentages.
   * @property {string} [arc.rangeText.unit="absolute"] Specify the range text unit.
   * - "absolute": Show absolute value
   * - "%": Show percentage value
   * @property {boolean} [arc.rangeText.fiexed=false] Set if range text shown will be fixed w/o data toggle update. Only available for gauge chart.
   * @property {Function} [arc.rangeText.format] Set format function for the range text.
   * @property {number} [arc.rangeText.position] Set position function or object for the range text.
   * @see [Demo: Donut corner radius](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutCornerRadius)
   * @see [Demo: Donut corner radius](https://naver.github.io/billboard.js/demo/#PieChartOptions.CornerRadius)
   * @see [Demo: Donut needle](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutNeedle)
   * @see [Demo: Donut RangeText](https://naver.github.io/billboard.js/demo/#DonutChartOptions.DonutRangeText)
   * @see [Demo: Gauge corner radius](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeCornerRadius)
   * @see [Demo: Gauge needle](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeNeedle)
   * @see [Demo: Gauge RangeText](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeRangeText)
   * @example
   *  arc: {
   *      cornerRadius: 12,
   *
   *      // can customize corner radius for each data with function callback
   *      //
   *      // The function will receive:
   *      // - id {string}: Data id
   *      // - value {number}: Data value
   *      // - outerRadius {number}: Outer radius value
   *      cornerRadius: function(id, value, outerRadius) {
   *          return (id === "data1" && value > 10) ?
   *          	50 : outerRadius * 1.2;
   *      },
   *
   *      // set ratio relative of outer radius
   *      cornerRadius: {
   *          ratio: 0.5
   *      },
   *
   *      needle: {
   *       	show: true,
   *       	color: "red", // any valid CSS color
   *       	path: function(length) {
   *       	  const len = length - 20;
   *
   *       	  // will return upper arrow shape path
   *       	  // Note: The path should begun from '0,0' coordinate to top center.
   *       	  const path = `M 0 -${len + 20}
   *       		L -12 -${len}
   *       		L -5 -${len}
   *       		L -5 0
   *       		A 1 1 0 0 0 5 0
   *       		L 5 -${len}
   *       		L 12 -${len} Z`;
   *
   *       	  return path;
   *       	},
   *       	value: 40,  // will make needle to point value 40.
   *       	length: 80, // needle length in percentages relative to radius.
   *
   *       	top: {
   *       	  // rx and ry are the two radii of the ellipse;
   *       	  // https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve
   *       	  rx: 1,
   *       	  ry: 1,
   *       	  width: 5
   *       	},
   *       	bottom: {
   *       	  // rx and ry are the two radii of the ellipse;
   *       	  // https://developer.mozilla.org/en-US/docs/Web/SVG/Attribute/d#elliptical_arc_curve
   *       	  rx: 1,
   *       	  ry: 1,
   *       	  width: 10
   *       	  len: 10
   *       	}
   *      },
   *
   *      rangeText: {
   *       	values: [15, 30, 50, 75, 95],
   *       	unit: "%",
   *       	fixed: false, // only available for gauge chart
   *       	format: function(v) {
   *       	  return v === 15 ? "Fifteen" : v;
   *       	},
   *
   *       	position: function(v) {
   *       	  return v === 15 ? {x: 20, y: 10} : null; // can return one props value also.
   *       	},
   *       	position: {x: 10, y: 15},
   *       	position: {x: 10}
   *      }
   *  }
   */
  arc_cornerRadius: 0,
  arc_cornerRadius_ratio: 0,
  arc_needle_show: false,
  arc_needle_color: void 0,
  arc_needle_value: void 0,
  arc_needle_path: void 0,
  arc_needle_length: 100,
  arc_needle_top_rx: 0,
  arc_needle_top_ry: 0,
  arc_needle_top_width: 0,
  arc_needle_bottom_rx: 1,
  arc_needle_bottom_ry: 1,
  arc_needle_bottom_width: 15,
  arc_needle_bottom_len: 0,
  arc_rangeText_values: void 0,
  arc_rangeText_unit: "absolute",
  arc_rangeText_fixed: false,
  arc_rangeText_format: void 0,
  arc_rangeText_position: void 0
};
var optDonut = {
  /**
   * Set donut options
   * @name donut
   * @memberof Options
   * @type {object}
   * @property {object} donut Donut object
   * @property {boolean} [donut.label.show=true] Show or hide label on each donut piece.
   * @property {Function} [donut.label.format] Set formatter for the label on each donut piece.
   * @property {number} [donut.label.threshold=0.05] Set threshold ratio to show/hide labels.
   * @property {number|Function} [donut.label.ratio=undefined] Set ratio of labels position.
   * @property {boolean} [donut.expand=true] Enable or disable expanding donut pieces.
   * @property {number} [donut.expand.rate=0.98] Set expand rate.
   * @property {number} [donut.expand.duration=50] Set expand transition time in ms.
   * @property {number} [donut.width] Set width of donut chart.
   * @property {string} [donut.title=""] Set title of donut chart. Use `\n` character for line break.
   *  - **NOTE:**
   *    - When `arc.needle.show=true` is set, special template `{=NEEDLE_VALUE}` can be used inside the title text to show current needle value.
   * @property {number} [donut.padAngle=0] Set padding between data.
   * @property {number} [donut.startingAngle=0] Set starting angle where data draws.
   * @example
   *  donut: {
   *      label: {
   *          show: false,
   *          format: function(value, ratio, id) {
   *              return d3.format("$")(value);
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the total value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *
   *          // set ratio callback. Should return ratio value
   *          ratio: function(d, radius, h) {
   *          	...
   *          	return ratio;
   *          },
   *          // or set ratio number
   *          ratio: 0.5
   *      },
   *
   *      // disable expand transition for interaction
   *      expand: false,
   *
   *      expand: {
   *      	// set duration of expand transition to 500ms.
   *          duration: 500,
   *
   *      	// set expand area rate
   *          rate: 1
   *      },
   *
   *      width: 10,
   *      padAngle: 0.2,
   *      startingAngle: 1,
   *      title: "Donut Title"
   *
   *      // when 'arc.needle.show=true' is set, can show current needle value.
   *      title: "Needle value:\n{=NEEDLE_VALUE}",
   *
   *      // title with line break
   *      title: "Title1\nTitle2"
   *  }
   */
  donut_label_show: true,
  donut_label_format: void 0,
  donut_label_threshold: 0.05,
  donut_label_ratio: void 0,
  donut_width: void 0,
  donut_title: "",
  donut_expand: {},
  donut_expand_rate: 0.98,
  donut_expand_duration: 50,
  donut_padAngle: 0,
  donut_startingAngle: 0
};
var optGauge = {
  /**
   * Set gauge options
   * @name gauge
   * @memberof Options
   * @type {object}
   * @property {object} gauge Gauge object
   * @property {boolean} [gauge.background=""] Set background color. (The `.bb-chart-arcs-background` element)
   * @property {boolean} [gauge.fullCircle=false] Show full circle as donut. When set to 'true', the max label will not be showed due to start and end points are same location.
   * @property {boolean} [gauge.label.show=true] Show or hide label on gauge.
   * @property {Function} [gauge.label.extents] Set customized min/max label text.
   * @property {Function} [gauge.label.format] Set formatter for the label on gauge. Label text can be multilined with `\n` character.<br>
   * Will pass following arguments to the given function:
   * - value {number}: absolute value
   * - ratio {number}: value's ratio
   * - id {string}: data's id value
   * @property {number|Function} [gauge.label.ratio=undefined] Set ratio of labels position.
   * @property {number} [gauge.label.threshold=0] Set threshold ratio to show/hide labels.
   * @property {boolean} [gauge.expand=true] Enable or disable expanding gauge.
   * @property {number} [gauge.expand.rate=0.98] Set expand rate.
   * @property {number} [gauge.expand.duration=50] Set the expand transition time in milliseconds.
   * @property {boolean} [gauge.enforceMinMax=false] Enforce to given min/max value.
   * - When `gauge.min=50` and given value is `30`, gauge will render as empty value.
   * - When `gauge.max=100` and given value is `120`, gauge will render till 100, not surpassing max value.
   * @property {number} [gauge.min=0] Set min value of the gauge.
   * @property {number} [gauge.max=100] Set max value of the gauge.
   * @property {number} [gauge.startingAngle=-1 * Math.PI / 2] Set starting angle where data draws.
   *
   * **Limitations:**
   * - when `gauge.fullCircle=false`:
   *   - -1 * Math.PI / 2 <= startingAngle <= Math.PI / 2
   *   - `startingAngle <= -1 * Math.PI / 2` defaults to `-1 * Math.PI / 2`
   *   - `startingAngle >= Math.PI / 2` defaults to `Math.PI / 2`
   * - when `gauge.fullCircle=true`:
   *   - -1 * Math.PI < startingAngle < Math.PI
   *   - `startingAngle < -1 * Math.PI` defaults to `Math.PI`
   *   - `startingAngle >  Math.PI` defaults to `Math.PI`
   * @property {number} [gauge.arcLength=100] Set the length of the arc to be drawn in percent from -100 to 100.<br>
   * Negative value will draw the arc **counterclockwise**. Need to be used in conjunction with `gauge.fullCircle=true`.
   *
   * **Limitations:**
   * - -100 <= arcLength (in percent) <= 100
   * - 'arcLength < -100' defaults to -100
   * - 'arcLength > 100' defaults to 100
   * @property {string} [gauge.title=""] Set title of gauge chart. Use `\n` character for line break.
   *  - **NOTE:**
   *    - When `arc.needle.show=true` is set, special template `{=NEEDLE_VALUE}` can be used inside the title text to show current needle value.
   * @property {string} [gauge.units] Set units of the gauge.
   * @property {number} [gauge.width] Set width of gauge chart.
   * @property {string} [gauge.type="single"] Set type of gauge to be displayed.<br><br>
   * **Available Values:**
   * - single
   * - multi
   * @property {number} [gauge.arcs.minWidth=5] Set minimal width of gauge arcs until the innerRadius disappears.
   * @see [Demo: enforceMinMax, min/max](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeMinMax)
   * @see [Demo: archLength](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeArcLength)
   * @see [Demo: startingAngle](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeStartingAngle)
   * @see [Demo: labelRatio](https://naver.github.io/billboard.js/demo/#GaugeChartOptions.GaugeLabelRatio)
   * @example
   *  gauge: {
   *      background: "#eee", // will set 'fill' css prop for '.bb-chart-arcs-background' classed element.
   *      fullCircle: false,
   *      label: {
   *          show: false,
   *          format: function(value, ratio, id) {
   *              return value;
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *           extents: function(value, isMax) {
   *              return (isMax ? "Max:" : "Min:") + value;
   *          },
   *
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the total value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *
   *          // set ratio callback. Should return ratio value
   *          ratio: function(d, radius, h) {
   *              ...
   *              return ratio;
   *          },
   *          // or set ratio number
   *          ratio: 0.5
   *      },
   *
   *      // disable expand transition for interaction
   *      expand: false,
   *
   *      expand: {
   *      	// set duration of expand transition to 500ms.
   *          duration: 500,
   *
   *      	// set expand area rate
   *          rate: 1
   *      },
   *
   *      // enforce min/max value.
   * 		// when given value < min, will render as empty value.
   * 		// when value > max, will render to given max value not surpassing it.
   *      enforceMinMax: true,
   *
   *      min: -100,
   *      max: 200,
   *      type: "single"  // or 'multi'
   *      title: "Title Text",
   *
   *      // when 'arc.needle.show=true' is set, can show current needle value.
   *      title: "Needle value:\n{=NEEDLE_VALUE}",
   *
   *      units: "%",
   *      width: 10,
   *      startingAngle: -1 * Math.PI / 2,
   *      arcLength: 100,
   *      arcs: {
   *          minWidth: 5
   *      }
   *  }
   */
  gauge_background: "",
  gauge_fullCircle: false,
  gauge_label_show: true,
  gauge_label_extents: void 0,
  gauge_label_format: void 0,
  gauge_label_ratio: void 0,
  gauge_label_threshold: 0,
  gauge_enforceMinMax: false,
  gauge_min: 0,
  gauge_max: 100,
  gauge_type: "single",
  gauge_startingAngle: -1 * Math.PI / 2,
  gauge_arcLength: 100,
  gauge_title: "",
  gauge_units: void 0,
  gauge_width: void 0,
  gauge_arcs_minWidth: 5,
  gauge_expand: {},
  gauge_expand_rate: 0.98,
  gauge_expand_duration: 50
};
var optPie = {
  /**
   * Set pie options
   * @name pie
   * @memberof Options
   * @type {object}
   * @property {object} pie Pie object
   * @property {boolean} [pie.label.show=true] Show or hide label on each pie piece.
   * @property {Function} [pie.label.format] Set formatter for the label on each pie piece.
   * @property {number|Function} [pie.label.ratio=undefined] Set ratio of labels position.
   * @property {number} [pie.label.threshold=0.05] Set threshold ratio to show/hide labels.
   * @property {boolean|object} [pie.expand=true] Enable or disable expanding pie pieces.
   * @property {number} [pie.expand.rate=0.98] Set expand rate.
   * @property {number} [pie.expand.duration=50] Set expand transition time in ms.
   * @property {number|object} [pie.innerRadius=0] Sets the inner radius of pie arc.
   * @property {number|object|undefined} [pie.outerRadius=undefined] Sets the outer radius of pie arc.
   * @property {number} [pie.padAngle=0] Set padding between data.
   * @property {number} [pie.padding=0] Sets the gap between pie arcs.
   * @property {number} [pie.startingAngle=0] Set starting angle where data draws.
   * @see [Demo: expand.rate](https://naver.github.io/billboard.js/demo/#PieChartOptions.ExpandRate)
   * @see [Demo: innerRadius](https://naver.github.io/billboard.js/demo/#PieChartOptions.InnerRadius)
   * @see [Demo: outerRadius](https://naver.github.io/billboard.js/demo/#PieChartOptions.OuterRadius)
   * @see [Demo: startingAngle](https://naver.github.io/billboard.js/demo/#PieChartOptions.StartingAngle)
   * @example
   *  pie: {
   *      label: {
   *          show: false,
   *          format: function(value, ratio, id) {
   *              return d3.format("$")(value);
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the total value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *
   *          // set ratio callback. Should return ratio value
   *          ratio: function(d, radius, h) {
   *              ...
   *              return ratio;
   *          },
   *          // or set ratio number
   *          ratio: 0.5
   *      },
   *
   *      // disable expand transition for interaction
   *      expand: false,
   *
   *      expand: {
   *      	// set duration of expand transition to 500ms.
   *          duration: 500,
   *
   *      	// set expand area rate
   *          rate: 1
   *      },
   *
   *      innerRadius: 0,
   *
   *      // set different innerRadius for each data
   *      innerRadius: {
   *      	data1: 10,
   *      	data2: 0
   *      },
   *
   *      outerRadius: 100,
   *
   *      // set different outerRadius for each data
   *      outerRadius: {
   *      	data1: 50,
   *      	data2: 100
   *      }
   *
   *      padAngle: 0.1,
   *      padding: 0,
   *      startingAngle: 1
   *  }
   */
  pie_label_show: true,
  pie_label_format: void 0,
  pie_label_ratio: void 0,
  pie_label_threshold: 0.05,
  pie_expand: {},
  pie_expand_rate: 0.98,
  pie_expand_duration: 50,
  pie_innerRadius: 0,
  pie_outerRadius: void 0,
  pie_padAngle: 0,
  pie_padding: 0,
  pie_startingAngle: 0
};
var optPolar = {
  /**
   * Set polar options
   * @name polar
   * @memberof Options
   * @type {object}
   * @property {object} polar Polar object
   * @property {boolean} [polar.label.show=true] Show or hide label on each polar piece.
   * @property {Function} [polar.label.format] Set formatter for the label on each polar piece.
   * @property {number} [polar.label.threshold=0.05] Set threshold ratio to show/hide labels.
   * @property {number|Function} [polar.label.ratio=undefined] Set ratio of labels position.
   * @property {number} [polar.level.depth=3] Set the level depth.
   * @property {boolean} [polar.level.show=true] Show or hide level.
   * @property {string} [polar.level.text.backgroundColor="#fff"] Set label text's background color.
   * @property {Function} [polar.level.text.format] Set format function for the level value.<br>- Default value: `(x) => x % 1 === 0 ? x : x.toFixed(2)`
   * @property {boolean} [polar.level.text.show=true] Show or hide level text.
   * @property {number} [polar.padAngle=0] Set padding between data.
   * @property {number} [polar.padding=0] Sets the gap between pie arcs.
   * @property {number} [polar.startingAngle=0] Set starting angle where data draws.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Chart.PolarChart)
   * @example
   *  polar: {
   *      label: {
   *          show: false,
   *          format: function(value, ratio, id) {
   *              return d3.format("$")(value);
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *          // 0.1(10%) ratio value means, the minimum ratio to show text label relative to the total value.
   *          // if data value is below than 0.1, text label will be hidden.
   *          threshold: 0.1,
   *
   *          // set ratio callback. Should return ratio value
   *          ratio: function(d, radius, h) {
   *              ...
   *              return ratio;
   *          },
   *          // or set ratio number
   *          ratio: 0.5
   *      },
   *      level: {
   *          depth: 3,
   *          max: 500,
   *          show: true,
   *          text: {
   *              format: function(x) {
   *                  return x + "%";
   *              },
   *              show: true,
   *              backgroundColor: "red"
   *          }
   *      },
   *      padAngle: 0.1,
   *      padding: 0,
   *      startingAngle: 1
   *  }
   */
  polar_label_show: true,
  polar_label_format: void 0,
  polar_label_threshold: 0.05,
  polar_label_ratio: void 0,
  polar_level_depth: 3,
  polar_level_max: void 0,
  polar_level_show: true,
  polar_level_text_backgroundColor: "#fff",
  polar_level_text_format: function(x3) {
    return x3 % 1 === 0 ? x3 : x3.toFixed(2);
  },
  polar_level_text_show: true,
  polar_padAngle: 0,
  polar_padding: 0,
  polar_startingAngle: 0
};
var optRadar = {
  /**
   * Set radar options
   * - **NOTE:**
   *  > When x tick text contains `\n`, it's used as line break.
   * @name radar
   * @memberof Options
   * @type {object}
   * @property {object} radar Radar object
   * @property {number} [radar.axis.max=undefined] The max value of axis. If not given, it'll take the max value from the given data.
   * @property {boolean} [radar.axis.line.show=true] Show or hide axis line.
   * @property {number} [radar.axis.text.position.x=0] x coordinate position, relative the original.
   * @property {number} [radar.axis.text.position.y=0] y coordinate position, relative the original.
   * @property {boolean} [radar.axis.text.show=true] Show or hide axis text.
   * @property {boolean} [radar.direction.clockwise=false] Set the direction to be drawn.
   * @property {number} [radar.level.depth=3] Set the level depth.
   * @property {boolean} [radar.level.show=true] Show or hide level.
   * @property {Function} [radar.level.text.format] Set format function for the level value.<br>- Default value: `(x) => x % 1 === 0 ? x : x.toFixed(2)`
   * @property {boolean} [radar.level.text.show=true] Show or hide level text.
   * @property {number} [radar.size.ratio=0.87] Set size ratio.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Chart.RadarChart)
   * @see [Demo: radar axis](https://naver.github.io/billboard.js/demo/#RadarChartOptions.RadarAxis)
   * @see [Demo: radar level](https://naver.github.io/billboard.js/demo/#RadarChartOptions.RadarLevel)
   * @see [Demo: radar size](https://naver.github.io/billboard.js/demo/#RadarChartOptions.RadarSize)
   * @see [Demo: radar axis multiline](https://naver.github.io/billboard.js/demo/#RadarChartOptions.RadarAxisMultiline)
   * @example
   *  radar: {
   *      axis: {
   *          max: 50,
   *          line: {
   *              show: false
   *          },
   *          text: {
   *              position: {
   *              	x: 0,
   *              	y: 0
   *              },
   *              show: false
   *          }
   *      },
   *      direction: {
   *          clockwise: true
   *      },
   *      level: {
   *          show: false,
   *          text: {
   *              format: function(x) {
   *                  return x + "%";
   *              },
   *              show: true
   *          }
   *      },
   *      size: {
   *          ratio: 0.7
   *      }
   *  }
   */
  radar_axis_max: void 0,
  radar_axis_line_show: true,
  radar_axis_text_show: true,
  radar_axis_text_position: {},
  radar_level_depth: 3,
  radar_level_show: true,
  radar_level_text_format: function(x3) {
    return x3 % 1 === 0 ? x3 : x3.toFixed(2);
  },
  radar_level_text_show: true,
  radar_size_ratio: 0.87,
  radar_direction_clockwise: false
};
var optTreemap = {
  /**
   * Set treemap options
   * @name treemap
   * @memberof Options
   * @type {object}
   * @property {object} treemap Treemap object
   * @property {string} [treemap.tile="binary"] Treemap tile type
   * - **Available tile type values:**
   * 	- binary ([d3.treemapBinary](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapBinary))
   * 	- dice ([d3.treemapDice](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapDice))
   * 	- slice ([d3.treemapSlice](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapSlice))
   * 	- sliceDice ([d3.treemapSliceDice](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapSliceDice))
   * 	- squrify ([d3.treemapSquarify](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapSquarify))
   * 	- resquarify ([d3.treemapResquarify](https://github.com/d3/d3-hierarchy/blob/main/README.md#treemapResquarify))
   * @property {Function} [treemap.label.format] Set formatter for the label text.
   * @property {number} [treemap.label.threshold=0.05] Set threshold ratio to show/hide labels text.
   * @property {number} [treemap.label.show=true] Show or hide label text.
   * @see [Demo: treemap](https://naver.github.io/billboard.js/demo/#Chart.TreemapChart)
   * @example
   *  treemap: {
   *      // "binary", "dice", "slice", "sliceDice", "squrify", "resquarify"
   *      tile: "dice",
   *
   *      label: {
   *          // show or hide label text
   *          show: false,
   *
   *          // set label text formatter
   *          format: function(value, ratio, id) {
   *              return d3.format("$")(value);
   *
   *              // to multiline, return with '\n' character
   *              // return value +"%\nLine1\n2Line2";
   *          },
   *
   *          // set ratio number
   *          ratio: 0.05
   *      }
   *  }
   */
  treemap_tile: "binary",
  treemap_label_format: void 0,
  treemap_label_threshold: 0.05,
  treemap_label_show: true
};
function extendAxis(module, option) {
  extend2(ChartInternal.prototype, Object.values(internal).concat(module));
  extend2(Chart.prototype, api);
  Options.setOptions(Object.values(options).concat(option || []));
}
function extendLine(module, option) {
  extendAxis([shapePointCommon, shapePoint, shapeLine].concat(module || []));
  Options.setOptions([optPoint, optLine].concat(option || []));
}
function extendArc(module, option) {
  extend2(ChartInternal.prototype, [shapeArc, shapePointCommon].concat(module || []));
  Options.setOptions([optPoint].concat(option || []));
}
var area = function() {
  return extendLine(shapeArea, [optArea]), (area = function() {
    return TYPE.AREA;
  })();
};
var areaLineRange = function() {
  return extendLine(shapeArea, [optArea]), (areaLineRange = function() {
    return TYPE.AREA_LINE_RANGE;
  })();
};
var areaSpline = function() {
  return extendLine(shapeArea, [optArea, optSpline]), (areaSpline = function() {
    return TYPE.AREA_SPLINE;
  })();
};
var areaSplineRange = function() {
  return extendLine(shapeArea, [optArea, optSpline]), (areaSplineRange = function() {
    return TYPE.AREA_SPLINE_RANGE;
  })();
};
var areaStep = function() {
  return extendLine(shapeArea, [optArea]), (areaStep = function() {
    return TYPE.AREA_STEP;
  })();
};
var line = function() {
  return extendLine(), (line = function() {
    return TYPE.LINE;
  })();
};
var spline = function() {
  return extendLine(void 0, [optSpline]), (spline = function() {
    return TYPE.SPLINE;
  })();
};
var step = function() {
  return extendLine(), (step = function() {
    return TYPE.STEP;
  })();
};
var donut = function() {
  return extendArc(void 0, [optArc, optDonut]), (donut = function() {
    return TYPE.DONUT;
  })();
};
var gauge = function() {
  return extendArc([shapeGauge], [optArc, optGauge]), (gauge = function() {
    return TYPE.GAUGE;
  })();
};
var pie = function() {
  return extendArc(void 0, [optArc, optPie]), (pie = function() {
    return TYPE.PIE;
  })();
};
var polar = function() {
  return extendArc([shapePolar], [optArc, optPolar]), (polar = function() {
    return TYPE.POLAR;
  })();
};
var radar = function() {
  return extendArc([internal.eventrect, shapePoint, shapeRadar], [optPoint, optRadar, { axis_x_categories: options.optAxis.axis_x_categories }]), (radar = function() {
    return TYPE.RADAR;
  })();
};
var bar = function() {
  return extendAxis([shapeBar, shapePointCommon], [optBar, optPoint]), (bar = function() {
    return TYPE.BAR;
  })();
};
var bubble = function() {
  return extendAxis([shapePointCommon, shapePoint, shapeBubble], [optBubble, optPoint]), (bubble = function() {
    return TYPE.BUBBLE;
  })();
};
var candlestick = function() {
  return extendAxis([shapeCandlestick, shapePointCommon], [optCandlestick, optPoint]), (candlestick = function() {
    return TYPE.CANDLESTICK;
  })();
};
var scatter = function() {
  return extendAxis([shapePointCommon, shapePoint], [optPoint, optScatter]), (scatter = function() {
    return TYPE.SCATTER;
  })();
};
var treemap = function() {
  return extendAxis([shapeTreemap], [optTreemap]), (treemap = function() {
    return TYPE.TREEMAP;
  })();
};
function setSelection(isSelection, ids, indices, resetOther) {
  if (isSelection === void 0) {
    isSelection = false;
  }
  var $$ = this;
  var config = $$.config, main2 = $$.$el.main;
  var selectionGrouped = config.data_selection_grouped;
  var isSelectable = config.data_selection_isselectable.bind($$.api);
  if (!config.data_selection_enabled) {
    return;
  }
  main2.selectAll(".".concat($SHAPE.shapes)).selectAll(".".concat($SHAPE.shape)).each(function(d) {
    var shape2 = select_default2(this);
    var _a2 = d.data ? d.data : d, id2 = _a2.id, index2 = _a2.index;
    var toggle = $$.getToggle(this, d).bind($$);
    var isTargetId = selectionGrouped || !ids || ids.indexOf(id2) >= 0;
    var isTargetIndex = !indices || indices.indexOf(index2) >= 0;
    var isSelected = shape2.classed($SELECT.SELECTED);
    if (shape2.classed($LINE.line) || shape2.classed($AREA.area)) {
      return;
    }
    if (isSelection) {
      if (isTargetId && isTargetIndex && isSelectable(d) && !isSelected) {
        toggle(true, shape2.classed($SELECT.SELECTED, true), d, index2);
      } else if (isDefined(resetOther) && resetOther && isSelected) {
        toggle(false, shape2.classed($SELECT.SELECTED, false), d, index2);
      }
    } else {
      if (isTargetId && isTargetIndex && isSelectable(d) && isSelected) {
        toggle(false, shape2.classed($SELECT.SELECTED, false), d, index2);
      }
    }
  });
}
var apiSelection = {
  /**
   * Get selected data points.<br><br>
   * By this API, you can get selected data points information. To use this API, data.selection.enabled needs to be set true.
   * @function selected
   * @instance
   * @memberof Chart
   * @param {string} [targetId] You can filter the result by giving target id that you want to get. If not given, all of data points will be returned.
   * @returns {Array} dataPoint Array of the data points.<br>ex.) `[{x: 1, value: 200, id: "data1", index: 1, name: "data1"}, ...]`
   * @example
   *  // all selected data points will be returned.
   *  chart.selected();
   *  // --> ex.) [{x: 1, value: 200, id: "data1", index: 1, name: "data1"}, ... ]
   *
   *  // all selected data points of data1 will be returned.
   *  chart.selected("data1");
   */
  selected: function(targetId) {
    var $$ = this.internal;
    var dataPoint = [];
    $$.$el.main.selectAll(".".concat($SHAPE.shapes + $$.getTargetSelectorSuffix(targetId))).selectAll(".".concat($SHAPE.shape)).filter(function() {
      return select_default2(this).classed($SELECT.SELECTED);
    }).each(function(d) {
      return dataPoint.push(d);
    });
    return dataPoint;
  },
  /**
   * Set data points to be selected. ([`data.selection.enabled`](Options.html#.data%25E2%2580%25A4selection%25E2%2580%25A4enabled) option should be set true to use this method)
   * @function select
   * @instance
   * @memberof Chart
   * @param {string|Array} [ids] id value to get selected.
   * @param {Array} [indices] The index array of data points. If falsy value given, will select all data points.
   * @param {boolean} [resetOther] Unselect already selected.
   * @example
   *  // select all data points
   *  chart.select();
   *
   *  // select all from 'data2'
   *  chart.select("data2");
   *
   *  // select all from 'data1' and 'data2'
   *  chart.select(["data1", "data2"]);
   *
   *  // select from 'data1', indices 2 and unselect others selected
   *  chart.select("data1", [2], true);
   *
   *  // select from 'data1', indices 0, 3 and 5
   *  chart.select("data1", [0, 3, 5]);
   */
  select: function(ids, indices, resetOther) {
    var $$ = this.internal;
    setSelection.bind($$)(true, ids, indices, resetOther);
  },
  /**
   * Set data points to be un-selected.
   * @function unselect
   * @instance
   * @memberof Chart
   * @param {string|Array} [ids] id value to be unselected.
   * @param {Array} [indices] The index array of data points. If falsy value given, will select all data points.
   * @example
   *  // unselect all data points
   *  chart.unselect();
   *
   *  // unselect all from 'data1'
   *  chart.unselect("data1");
   *
   *  // unselect from 'data1', indices 2
   *  chart.unselect("data1", [2]);
   */
  unselect: function(ids, indices) {
    var $$ = this.internal;
    setSelection.bind($$)(false, ids, indices);
  }
};
var subchart$1 = function(domainValue) {
  var _a2;
  var $$ = this.internal;
  var axis3 = $$.axis, brush2 = $$.brush, config = $$.config, _b = $$.scale, x3 = _b.x, subX = _b.subX, state = $$.state;
  var domain2;
  if (config.subchart_show) {
    domain2 = domainValue;
    if (Array.isArray(domain2)) {
      if (axis3.isTimeSeries()) {
        domain2 = domain2.map(function(x4) {
          return parseDate.bind($$)(x4);
        });
      }
      var isWithinRange = $$.withinRange(domain2, $$.getZoomDomain("subX", true), $$.getZoomDomain("subX"));
      if (isWithinRange) {
        state.domain = domain2;
        brush2.move(brush2.getSelection(), domain2.map(subX));
      }
    } else {
      domain2 = (_a2 = state.domain) !== null && _a2 !== void 0 ? _a2 : x3.orgDomain();
    }
  }
  return domain2;
};
extend2(subchart$1, {
  /**
   * Show subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․show
   * @instance
   * @memberof Chart
   * @example
   * // for ESM imports, needs to import 'subchart' and must be instantiated first to enable subchart's API.
   * import {subchart} from "billboard.js";
   *
   * const chart = bb.generate({
   *   ...
   *   subchart: {
   *      // need to be instantiated by calling 'subchart()'
   *      enabled: subchart()
   *
   *      // in case don't want subchart to be shown at initialization, instantiate with '!subchart()'
   *      enabled: !subchart()
   *   }
   * });
   *
   * chart.subchart.show();
   */
  show: function() {
    var _a2, _b;
    var $$ = this.internal;
    var subchart2 = $$.$el.subchart, config = $$.config;
    var show = config.subchart_show;
    if (!show) {
      $$.unbindZoomEvent();
      config.subchart_show = !show;
      !subchart2.main && $$.initSubchart();
      var $target = subchart2.main.selectAll(".".concat($COMMON.target));
      if ($$.data.targets.length !== $target.size()) {
        $$.updateSizes();
        $$.updateTargetsForSubchart($$.data.targets);
        $target = (_a2 = subchart2.main) === null || _a2 === void 0 ? void 0 : _a2.selectAll(".".concat($COMMON.target));
      }
      $target === null || $target === void 0 ? void 0 : $target.style("opacity", null);
      (_b = subchart2.main) === null || _b === void 0 ? void 0 : _b.style("display", null);
      this.resize();
    }
  },
  /**
   * Hide generated subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․hide
   * @instance
   * @memberof Chart
   * @example
   *  chart.subchart.hide();
   */
  hide: function() {
    var $$ = this.internal;
    var main2 = $$.$el.subchart.main, config = $$.config;
    if (config.subchart_show && (main2 === null || main2 === void 0 ? void 0 : main2.style("display")) !== "none") {
      config.subchart_show = false;
      main2.style("display", "none");
      this.resize();
    }
  },
  /**
   * Toggle the visiblity of subchart
   * - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   * @function subchart․toggle
   * @instance
   * @memberof Chart
   * @example
   * // When subchart is hidden, will be shown
   * // When subchart is shown, will be hidden
   * chart.subchart.toggle();
   */
  toggle: function() {
    var $$ = this.internal;
    var config = $$.config;
    this.subchart[config.subchart_show ? "hide" : "show"]();
  },
  /**
   * Reset subchart selection
   * @function subchart․reset
   * @instance
   * @memberof Chart
   * @example
   * // Reset subchart selection
   * chart.subchart.reset();
   */
  reset: function() {
    var $$ = this.internal;
    var brush2 = $$.brush;
    brush2.clear(brush2.getSelection());
  }
});
var apiSubchart = {
  subchart: subchart$1
};
var zoom$1 = function(domainValue) {
  var _a2;
  var $$ = this.internal;
  var axis3 = $$.axis, config = $$.config, org = $$.org, scale2 = $$.scale, state = $$.state;
  var isCategorized = axis3.isCategorized();
  var domain2;
  if (config.zoom_enabled) {
    domain2 = domainValue;
    if (Array.isArray(domain2)) {
      if (axis3.isTimeSeries()) {
        domain2 = domain2.map(function(x4) {
          return parseDate.bind($$)(x4);
        });
      }
      var isWithinRange = $$.withinRange(domain2, $$.getZoomDomain("zoom", true), $$.getZoomDomain("zoom"));
      if (isWithinRange) {
        state.domain = domain2;
        domain2 = $$.getZoomDomainValue(domain2);
        $$.api.tooltip.hide();
        if (config.subchart_show) {
          var x3 = scale2.zoom || scale2.x;
          $$.brush.getSelection().call($$.brush.move, domain2.map(x3));
        } else {
          var x3 = isCategorized ? scale2.x.orgScale() : org.xScale || scale2.x;
          $$.updateCurrentZoomTransform(x3, domain2);
        }
        $$.setZoomResetButton();
      }
    } else {
      domain2 = $$.zoom.getDomain();
    }
  }
  return (_a2 = state.domain) !== null && _a2 !== void 0 ? _a2 : domain2;
};
extend2(zoom$1, {
  /**
   * Enable and disable zooming.
   * @function zoom․enable
   * @instance
   * @memberof Chart
   * @param {string|boolean} enabled Possible string values are "wheel" or "drag". If enabled is true, "wheel" will be used. If false is given, zooming will be disabled.<br>When set to false, the current zooming status will be reset.
   * @example
   *  // Enable zooming using the mouse wheel
   *  chart.zoom.enable(true);
   *  // Or
   *  chart.zoom.enable("wheel");
   *
   *  // Enable zooming by dragging
   *  chart.zoom.enable("drag");
   *
   *  // Disable zooming
   *  chart.zoom.enable(false);
   */
  enable: function(enabled) {
    var $$ = this.internal;
    var config = $$.config;
    if (/^(drag|wheel)$/.test(enabled)) {
      config.zoom_type = enabled;
    }
    config.zoom_enabled = !!enabled;
    if (!$$.zoom) {
      $$.initZoom();
      $$.bindZoomEvent();
    } else if (enabled === false) {
      $$.bindZoomEvent(false);
    }
    $$.updateAndRedraw();
  },
  /**
   * Set or get x Axis maximum zoom range value
   * @function zoom․max
   * @instance
   * @memberof Chart
   * @param {number} [max] maximum value to set for zoom
   * @returns {number} zoom max value
   * @example
   *  // Set maximum range value
   *  chart.zoom.max(20);
   */
  max: function(max4) {
    var $$ = this.internal;
    var config = $$.config, xDomain = $$.org.xDomain;
    if (max4 === 0 || max4) {
      config.zoom_x_max = getMinMax$1("max", [xDomain[1], max4]);
    }
    return config.zoom_x_max;
  },
  /**
   * Set or get x Axis minimum zoom range value
   * @function zoom․min
   * @instance
   * @memberof Chart
   * @param {number} [min] minimum value to set for zoom
   * @returns {number} zoom min value
   * @example
   *  // Set minimum range value
   *  chart.zoom.min(-1);
   */
  min: function(min4) {
    var $$ = this.internal;
    var config = $$.config, xDomain = $$.org.xDomain;
    if (min4 === 0 || min4) {
      config.zoom_x_min = getMinMax$1("min", [xDomain[0], min4]);
    }
    return config.zoom_x_min;
  },
  /**
   * Set zoom range
   * @function zoom․range
   * @instance
   * @memberof Chart
   * @param {object} [range] zoom range
   * @returns {object} zoom range value
   * {
   *   min: 0,
   *   max: 100
   * }
   * @example
   *  chart.zoom.range({
   *      min: 10,
   *      max: 100
   *  });
   */
  range: function(range2) {
    var zoom2 = this.zoom;
    if (isObject(range2)) {
      var min4 = range2.min, max4 = range2.max;
      isDefined(min4) && zoom2.min(min4);
      isDefined(max4) && zoom2.max(max4);
    }
    return {
      min: zoom2.min(),
      max: zoom2.max()
    };
  }
});
var apiZoom = {
  zoom: zoom$1,
  /**
   * Unzoom zoomed area
   * - **NOTE:** Calling .unzoom() will not trigger zoom events.
   * @function unzoom
   * @instance
   * @memberof Chart
   * @example
   *  chart.unzoom();
   */
  unzoom: function() {
    var $$ = this.internal;
    var config = $$.config, _a2 = $$.$el, eventRect = _a2.eventRect, zoomResetBtn = _a2.zoomResetBtn, zoom2 = $$.scale.zoom, state = $$.state;
    if (zoom2) {
      config.subchart_show ? $$.brush.getSelection().call($$.brush.move, null) : $$.zoom.updateTransformScale(identity5);
      $$.updateZoom(true);
      zoomResetBtn === null || zoomResetBtn === void 0 ? void 0 : zoomResetBtn.style("display", "none");
      if (transform(eventRect.node()) !== identity5) {
        $$.zoom.transform(eventRect, identity5);
      }
      state.domain = void 0;
    }
  }
};
var drag = {
  /**
   * Called when dragging.
   * Data points can be selected.
   * @private
   * @param {object} mouse Object
   */
  drag: function(mouse) {
    var $$ = this;
    var config = $$.config, state = $$.state, main2 = $$.$el.main;
    var isSelectionGrouped = config.data_selection_grouped;
    var isSelectable = config.interaction_enabled && config.data_selection_isselectable;
    if ($$.hasArcType() || !config.data_selection_enabled || // do nothing if not selectable
    config.zoom_enabled && !$$.zoom.altDomain || // skip if zoomable because of conflict drag behavior
    !config.data_selection_multiple) {
      return;
    }
    var _a2 = state.dragStart || [0, 0], sx = _a2[0], sy = _a2[1];
    var mx = mouse[0], my = mouse[1];
    var minX = Math.min(sx, mx);
    var maxX = Math.max(sx, mx);
    var minY = isSelectionGrouped ? state.margin.top : Math.min(sy, my);
    var maxY = isSelectionGrouped ? state.height : Math.max(sy, my);
    main2.select(".".concat($DRAG.dragarea)).attr("x", minX).attr("y", minY).attr("width", maxX - minX).attr("height", maxY - minY);
    main2.selectAll(".".concat($SHAPE.shapes)).selectAll(".".concat($SHAPE.shape)).filter(function(d) {
      return isSelectable === null || isSelectable === void 0 ? void 0 : isSelectable.bind($$.api)(d);
    }).each(function(d, i) {
      var shape2 = select_default2(this);
      var isSelected = shape2.classed($SELECT.SELECTED);
      var isIncluded = shape2.classed($DRAG.INCLUDED);
      var isWithin = false;
      var toggle;
      if (shape2.classed($CIRCLE.circle)) {
        var x3 = +shape2.attr("cx") * 1;
        var y3 = +shape2.attr("cy") * 1;
        toggle = $$.togglePoint;
        isWithin = minX < x3 && x3 < maxX && minY < y3 && y3 < maxY;
      } else if (shape2.classed($BAR.bar)) {
        var _a3 = getPathBox(this), x3 = _a3.x, y3 = _a3.y, width = _a3.width, height = _a3.height;
        toggle = $$.togglePath;
        isWithin = !(maxX < x3 || x3 + width < minX) && !(maxY < y3 || y3 + height < minY);
      } else {
        return;
      }
      if (isWithin ^ isIncluded) {
        shape2.classed($DRAG.INCLUDED, !isIncluded);
        shape2.classed($SELECT.SELECTED, !isSelected);
        toggle.call($$, !isSelected, shape2, d, i);
      }
    });
  },
  /**
   * Called when the drag starts.
   * Adds and Shows the drag area.
   * @private
   * @param {object} mouse Object
   */
  dragstart: function(mouse) {
    var $$ = this;
    var config = $$.config, state = $$.state, main2 = $$.$el.main;
    if ($$.hasArcType() || !config.data_selection_enabled) {
      return;
    }
    state.dragStart = mouse;
    main2.select(".".concat($COMMON.chart)).append("rect").attr("class", $DRAG.dragarea).style("opacity", "0.1");
    $$.setDragStatus(true);
  },
  /**
   * Called when the drag finishes.
   * Removes the drag area.
   * @private
   */
  dragend: function() {
    var $$ = this;
    var config = $$.config, main2 = $$.$el.main, $T = $$.$T;
    if ($$.hasArcType() || !config.data_selection_enabled) {
      return;
    }
    $T(main2.select(".".concat($DRAG.dragarea))).style("opacity", "0").remove();
    main2.selectAll(".".concat($SHAPE.shape)).classed($DRAG.INCLUDED, false);
    $$.setDragStatus(false);
  }
};
var selection2 = _assign(_assign({}, drag), {
  /**
   * Select a point
   * @param {object} target Target point
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  selectPoint: function(target, d, i) {
    var $$ = this;
    var config = $$.config, main2 = $$.$el.main, $T = $$.$T;
    var isRotated = config.axis_rotated;
    var cx = (isRotated ? $$.circleY : $$.circleX).bind($$);
    var cy = (isRotated ? $$.circleX : $$.circleY).bind($$);
    var r = $$.pointSelectR.bind($$);
    callFn(config.data_onselected, $$.api, d, target.node());
    $T(main2.select(".".concat($SELECT.selectedCircles).concat($$.getTargetSelectorSuffix(d.id))).selectAll(".".concat($SELECT.selectedCircle, "-").concat(i)).data([d]).enter().append("circle").attr("class", function() {
      return $$.generateClass($SELECT.selectedCircle, i);
    }).attr("cx", cx).attr("cy", cy).attr("stroke", $$.color).attr("r", function(d2) {
      return $$.pointSelectR(d2) * 1.4;
    })).attr("r", r);
  },
  /**
   * Unelect a point
   * @param {object} target Target point
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  unselectPoint: function(target, d, i) {
    var $$ = this;
    var config = $$.config, main2 = $$.$el.main, $T = $$.$T;
    callFn(config.data_onunselected, $$.api, d, target === null || target === void 0 ? void 0 : target.node());
    $T(main2.select(".".concat($SELECT.selectedCircles).concat($$.getTargetSelectorSuffix(d.id))).selectAll(".".concat($SELECT.selectedCircle, "-").concat(i))).attr("r", 0).remove();
  },
  /**
   * Toggles the selection of points
   * @param {boolean} selected whether or not to select.
   * @param {object} target Target object
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  togglePoint: function(selected, target, d, i) {
    var method = "".concat(selected ? "" : "un", "selectPoint");
    this[method](target, d, i);
  },
  /**
   * Select a path
   * @param {object} target Target path
   * @param {object} d Data object
   * @private
   */
  selectPath: function(target, d) {
    var $$ = this;
    var config = $$.config;
    callFn(config.data_onselected, $$.api, d, target.node());
    if (config.interaction_brighten) {
      target.style("filter", "brightness(1.25)");
    }
  },
  /**
   * Unelect a path
   * @private
   * @param {object} target Target path
   * @param {object} d Data object
   */
  unselectPath: function(target, d) {
    var $$ = this;
    var config = $$.config;
    callFn(config.data_onunselected, $$.api, d, target.node());
    if (config.interaction_brighten) {
      target.style("filter", null);
    }
  },
  /**
   * Toggles the selection of lines
   * @param {boolean} selected whether or not to select.
   * @param {object} target Target object
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  togglePath: function(selected, target, d, i) {
    this["".concat(selected ? "" : "un", "selectPath")](target, d, i);
  },
  /**
   * Returns the toggle method of the target
   * @param {object} that shape
   * @param {object} d Data object
   * @returns {Function} toggle method
   * @private
   */
  getToggle: function(that, d) {
    var $$ = this;
    return that.nodeName === "path" ? $$.togglePath : $$.isStepType(d) ? function() {
    } : (
      // circle is hidden in step chart, so treat as within the click area
      $$.togglePoint
    );
  },
  /**
   * Toggles the selection of shapes
   * @param {object} that shape
   * @param {object} d Data object
   * @param {number} i Index number
   * @private
   */
  toggleShape: function(that, d, i) {
    var _a2;
    var $$ = this;
    var config = $$.config, main2 = $$.$el.main;
    if (config.data_selection_enabled && config.data_selection_isselectable.bind($$.api)(d)) {
      var shape2 = select_default2(that);
      var isSelected = shape2.classed($SELECT.SELECTED);
      var toggle_1 = $$.getToggle(that, d).bind($$);
      var toggledShape_1;
      if (!config.data_selection_multiple) {
        var focusOnly = (_a2 = $$.isPointFocusOnly) === null || _a2 === void 0 ? void 0 : _a2.call($$);
        var selector = ".".concat(focusOnly ? $SELECT.selectedCircles : $SHAPE.shapes);
        if (config.data_selection_grouped) {
          selector += $$.getTargetSelectorSuffix(d.id);
        }
        main2.selectAll(selector).selectAll(focusOnly ? ".".concat($SELECT.selectedCircle) : ".".concat($SHAPE.shape, ".").concat($SELECT.SELECTED)).classed($SELECT.SELECTED, false).each(function(d2) {
          var shape3 = select_default2(this);
          toggledShape_1 = shape3;
          toggle_1(false, shape3, d2, d2.index);
        });
      }
      if (!toggledShape_1 || toggledShape_1.node() !== shape2.node()) {
        shape2.classed($SELECT.SELECTED, !isSelected);
        toggle_1(!isSelected, shape2, d, i);
      }
    }
  }
});
var subchart = {
  /**
   * Initialize the brush.
   * @private
   */
  initBrush: function() {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, subchart2 = $$.$el.subchart, state = $$.state;
    var isRotated = config.axis_rotated;
    var height = config.subchart_size_height;
    var lastDomain;
    var lastSelection;
    var timeout2;
    $$.brush = (isRotated ? brushY() : brushX()).handleSize(5);
    $$.brush.on("start brush end", function(event) {
      var selection3 = event.selection, sourceEvent = event.sourceEvent, target = event.target, type2 = event.type;
      if (type2 === "start") {
        $$.state.inputType === "touch" && $$.hideTooltip();
        lastSelection = sourceEvent ? selection3 : null;
      }
      if (/(start|brush)/.test(type2)) {
        type2 === "brush" && sourceEvent && state.domain && (lastSelection === null || lastSelection === void 0 ? void 0 : lastSelection.forEach(function(v, i) {
          if (v !== selection3[i]) {
            state.domain[i] = scale2.x.orgDomain()[i];
          }
        }));
        $$.redrawForBrush(type2 !== "start");
      }
      if (type2 === "end") {
        lastDomain = scale2.x.orgDomain();
      }
      if (target === null || target === void 0 ? void 0 : target.handle) {
        if (selection3 === null) {
          $$.brush.handle.attr("display", "none");
        } else {
          $$.brush.handle.attr("display", null).attr("transform", function(d, i) {
            var pos = [selection3[i], height / 2];
            return "translate(".concat(isRotated ? pos.reverse() : pos, ")");
          });
        }
      }
    });
    $$.brush.updateResize = function() {
      var _this = this;
      timeout2 && clearTimeout(timeout2);
      timeout2 = setTimeout(function() {
        var selection3 = _this.getSelection();
        lastDomain && brushSelection(selection3.node()) && _this.move(selection3, lastDomain.map(scale2.subX.orgScale()));
      }, 0);
    };
    $$.brush.update = function() {
      var _a2;
      var extent2 = this.extent()();
      if (extent2[1].filter(function(v) {
        return isNaN(v);
      }).length === 0) {
        (_a2 = subchart2.main) === null || _a2 === void 0 ? void 0 : _a2.select(".".concat(CLASS.brush)).call(this);
      }
      return this;
    };
    $$.brush.scale = function(scale3) {
      var h = config.subchart_size_height;
      var extent2 = $$.getExtent();
      if (!extent2 && scale3.range) {
        extent2 = [[0, 0], [scale3.range()[1], h]];
      } else if (isArray(extent2)) {
        extent2 = extent2.map(function(v, i) {
          return [v, i > 0 ? h : i];
        });
      }
      isRotated && extent2[1].reverse();
      this.extent(extent2);
      this.update();
    };
    $$.brush.getSelection = function() {
      return (
        // @ts-ignore
        subchart2.main ? subchart2.main.select(".".concat(CLASS.brush)) : select_default2([])
      );
    };
  },
  /**
   * Initialize the subchart.
   * @private
   */
  initSubchart: function() {
    var $$ = this;
    var config = $$.config, _a2 = $$.state, clip2 = _a2.clip, hasAxis = _a2.hasAxis, _b = $$.$el, defs = _b.defs, svg = _b.svg, subchart2 = _b.subchart, axis3 = _b.axis;
    if (!hasAxis) {
      return;
    }
    var visibility = config.subchart_show ? null : "hidden";
    var clipId = "".concat(clip2.id, "-subchart");
    var clipPath = $$.getClipPath(clipId);
    clip2.idSubchart = clipId;
    $$.appendClip(defs, clipId);
    $$.initBrush();
    subchart2.main = svg.append("g").classed(CLASS.subchart, true).attr("transform", $$.getTranslate("context"));
    var main2 = subchart2.main;
    main2.style("visibility", visibility);
    main2.append("g").attr("clip-path", clipPath).attr("class", CLASS.chart);
    ["bar", "line", "bubble", "candlestick", "scatter"].forEach(function(v) {
      var type2 = capitalize(/^(bubble|scatter)$/.test(v) ? "circle" : v);
      if ($$.hasType(v) || $$.hasTypeOf(type2)) {
        var chart = main2.select(".".concat(CLASS.chart));
        var chartClassName = CLASS["chart".concat(type2, "s")];
        if (chart.select(".".concat(chartClassName)).empty()) {
          chart.append("g").attr("class", chartClassName);
        }
      }
    });
    var brush2 = main2.append("g").attr("clip-path", clipPath).attr("class", CLASS.brush).call($$.brush);
    config.subchart_showHandle && $$.addBrushHandle(brush2);
    axis3.subX = main2.append("g").attr("class", CLASS.axisX).attr("transform", $$.getTranslate("subX")).attr("clip-path", config.axis_rotated ? "" : clip2.pathXAxis).style("visibility", config.subchart_axis_x_show ? visibility : "hidden");
  },
  /**
   * Add brush handle
   * Enabled when: subchart.showHandle=true
   * @param {d3Selection} brush Brush selection
   * @private
   */
  addBrushHandle: function(brush2) {
    var $$ = this;
    var config = $$.config;
    var isRotated = config.axis_rotated;
    var initRange2 = config.subchart_init_range;
    var customHandleClass = "handle--custom";
    var path2 = isRotated ? [
      "M8.5 0 a6 6 0 0 0 -6 -6.5 H-2.5 a 6 6 0 0 0 -6 6.5 z m-5 -2 H-3.5 m7 -2 H-3.5z",
      "M8.5 0 a6 -6 0 0 1 -6 6.5 H-2.5 a 6 -6 0 0 1 -6 -6.5z m-5 2 H-3.5 m7 2 H-3.5z"
    ] : [
      "M0 -8.5 A6 6 0 0 0 -6.5 -3.5 V2.5 A6 6 0 0 0 0 8.5 Z M-2 -3.5 V3.5 M-4 -3.5 V3.5z",
      "M0 -8.5 A6 6 0 0 1 6.5 -3.5 V2.5 A6 6 0 0 1 0 8.5 Z M2 -3.5 V3.5 M4 -3.5 V3.5z"
    ];
    $$.brush.handle = brush2.selectAll(".".concat(customHandleClass)).data(isRotated ? [{ type: "n" }, { type: "s" }] : [{ type: "w" }, { type: "e" }]).enter().append("path").attr("class", customHandleClass).attr("cursor", "".concat(isRotated ? "ns" : "ew", "-resize")).attr("d", function(d) {
      return path2[+/[se]/.test(d.type)];
    }).attr("display", initRange2 ? null : "none");
  },
  /**
   * Update sub chart
   * @param {object} targets $$.data.targets
   * @private
   */
  updateTargetsForSubchart: function(targets) {
    var $$ = this;
    var config = $$.config, state = $$.state, main2 = $$.$el.subchart.main;
    if (config.subchart_show) {
      ["bar", "line", "bubble", "candlestick", "scatter"].filter(function(v) {
        return $$.hasType(v) || $$.hasTypeOf(capitalize(v));
      }).forEach(function(v) {
        var isPointType = /^(bubble|scatter)$/.test(v);
        var name = capitalize(isPointType ? "circle" : v);
        var chartClass = $$.getChartClass(name, true);
        var shapeClass = $$.getClass(isPointType ? "circles" : "".concat(v, "s"), true);
        var shapeChart = main2.select(".".concat(CLASS["chart".concat("".concat(name, "s"))]));
        if (isPointType) {
          var circle = shapeChart.selectAll(".".concat(CLASS.circles)).data(targets.filter($$["is".concat(capitalize(v), "Type")].bind($$))).attr("class", shapeClass);
          circle.exit().remove();
          circle.enter().append("g").attr("class", shapeClass);
        } else {
          var shapeUpdate = shapeChart.selectAll(".".concat(CLASS["chart".concat(name)])).attr("class", chartClass).data(targets.filter($$["is".concat(name, "Type")].bind($$)));
          var shapeEnter = shapeUpdate.enter().append("g").style("opacity", "0").attr("class", chartClass).append("g").attr("class", shapeClass);
          shapeUpdate.exit().remove();
          v === "line" && $$.hasTypeOf("Area") && shapeEnter.append("g").attr("class", $$.getClass("areas", true));
        }
      });
      main2.selectAll(".".concat(CLASS.brush, " rect")).attr(config.axis_rotated ? "width" : "height", config.axis_rotated ? state.width2 : state.height2);
    }
  },
  /**
   * Redraw subchart.
   * @private
   * @param {boolean} withSubchart whether or not to show subchart
   * @param {number} duration duration
   * @param {object} shape Shape's info
   */
  redrawSubchart: function(withSubchart, duration, shape2) {
    var _a2;
    var $$ = this;
    var config = $$.config, main2 = $$.$el.subchart.main, state = $$.state;
    var withTransition = !!duration;
    main2.style("visibility", config.subchart_show ? null : "hidden");
    if (config.subchart_show) {
      if (((_a2 = state.event) === null || _a2 === void 0 ? void 0 : _a2.type) === "zoom") {
        $$.brush.update();
      }
      if (withSubchart) {
        var initRange2 = config.subchart_init_range;
        !brushEmpty($$) && $$.brush.update();
        Object.keys(shape2.type).forEach(function(v) {
          var name = capitalize(v);
          var drawFn = $$["generateDraw".concat(name)](shape2.indices[v], true);
          $$["update".concat(name)](withTransition, true);
          $$["redraw".concat(name)](drawFn, withTransition, true);
        });
        if ($$.hasType("bubble") || $$.hasType("scatter")) {
          var cx = shape2.pos.cx;
          var cy = $$.updateCircleY(true);
          $$.updateCircle(true);
          $$.redrawCircle(cx, cy, withTransition, void 0, true);
        }
        if (!state.rendered && initRange2) {
          state.domain = initRange2;
          $$.brush.move($$.brush.getSelection(), initRange2.map($$.scale.x));
        }
      }
    }
  },
  /**
   * Redraw the brush.
   * @param {boolean} [callCallbck=true] Call 'onbrush' callback or not.
   * @private
   */
  redrawForBrush: function(callCallbck) {
    var _a2;
    if (callCallbck === void 0) {
      callCallbck = true;
    }
    var $$ = this;
    var _b = $$.config, onBrush = _b.subchart_onbrush, withY = _b.zoom_rescale, scale2 = $$.scale, state = $$.state;
    $$.redraw({
      withTransition: false,
      withY,
      withSubchart: false,
      withUpdateXDomain: true,
      withDimension: false
    });
    callCallbck && state.rendered && onBrush.bind($$.api)((_a2 = state.domain) !== null && _a2 !== void 0 ? _a2 : scale2.x.orgDomain());
  },
  /**
   * Transform context
   * @param {boolean} withTransition indicates transition is enabled
   * @param {object} transitions The return value of the generateTransitions method of Axis.
   * @private
   */
  transformContext: function(withTransition, transitions) {
    var $$ = this;
    var subchart2 = $$.$el.subchart, $T = $$.$T;
    var subXAxis = (transitions === null || transitions === void 0 ? void 0 : transitions.axisSubX) ? transitions.axisSubX : $T(subchart2.main.select(".".concat(CLASS.axisX)), withTransition);
    subchart2.main.attr("transform", $$.getTranslate("context"));
    subXAxis.attr("transform", $$.getTranslate("subX"));
  },
  /**
   * Get extent value
   * @returns {Array} default extent
   * @private
   */
  getExtent: function() {
    var $$ = this;
    var config = $$.config, scale2 = $$.scale;
    var extent2 = config.axis_x_extent;
    if (extent2) {
      if (isFunction(extent2)) {
        extent2 = extent2.bind($$.api)($$.getXDomain($$.data.targets), scale2.subX);
      } else if ($$.axis.isTimeSeries() && extent2.every(isNaN)) {
        var fn_1 = parseDate.bind($$);
        extent2 = extent2.map(function(v) {
          return scale2.subX(fn_1(v));
        });
      }
    }
    return extent2;
  }
};
var zoom = {
  /**
   * Initialize zoom.
   * @private
   */
  initZoom: function() {
    var $$ = this;
    $$.scale.zoom = null;
    $$.generateZoom();
    $$.initZoomBehaviour();
  },
  /**
   * Bind zoom event
   * @param {boolean} bind Weather bind or unbound
   * @private
   */
  bindZoomEvent: function(bind) {
    if (bind === void 0) {
      bind = true;
    }
    var $$ = this;
    var config = $$.config;
    var zoomEnabled = config.zoom_enabled;
    if (zoomEnabled && bind) {
      !config.subchart_show && $$.bindZoomOnEventRect();
    } else if (bind === false) {
      $$.api.unzoom();
      $$.unbindZoomEvent();
    }
  },
  /**
   * Generate zoom
   * @private
   */
  generateZoom: function() {
    var $$ = this;
    var config = $$.config, org = $$.org, scale2 = $$.scale;
    var zoom2 = zoom_default2().duration(0).on("start", $$.onZoomStart.bind($$)).on("zoom", $$.onZoom.bind($$)).on("end", $$.onZoomEnd.bind($$));
    zoom2.orgScaleExtent = function() {
      var extent2 = config.zoom_extent || [1, 10];
      return [extent2[0], Math.max($$.getMaxDataCount() / extent2[1], extent2[1])];
    };
    zoom2.updateScaleExtent = function() {
      var ratio = diffDomain($$.scale.x.orgDomain()) / diffDomain($$.getZoomDomain());
      var extent2 = this.orgScaleExtent();
      this.scaleExtent([extent2[0] * ratio, extent2[1] * ratio]);
      return this;
    };
    zoom2.updateTransformScale = function(transform3, correctTransform) {
      var _a2;
      var isRotated = config.axis_rotated;
      (_a2 = org.xScale) === null || _a2 === void 0 ? void 0 : _a2.range(scale2.x.range());
      var newScale = transform3[isRotated ? "rescaleY" : "rescaleX"](org.xScale || scale2.x);
      var domain2 = $$.trimXDomain(newScale.domain());
      var rescale = config.zoom_rescale;
      newScale.domain(domain2, org.xDomain);
      if (correctTransform) {
        var t = newScale(scale2.x.domain()[0]);
        var tX = isRotated ? transform3.x : t;
        var tY = isRotated ? t : transform3.y;
        $$.$el.eventRect.property("__zoom", identity5.translate(tX, tY).scale(transform3.k));
      }
      if (!$$.state.xTickOffset) {
        $$.state.xTickOffset = $$.axis.x.tickOffset();
      }
      scale2.zoom = $$.getCustomizedXScale(newScale);
      $$.axis.x.scale(scale2.zoom);
      if (rescale) {
        !org.xScale && (org.xScale = scale2.x.copy());
        scale2.x.domain(domain2);
      } else if (org.xScale) {
        scale2.x.domain(org.xScale.domain());
        org.xScale = null;
      }
    };
    zoom2.getDomain = function() {
      var domain2 = scale2[scale2.zoom ? "zoom" : "subX"].domain();
      var isCategorized = $$.axis.isCategorized();
      if (isCategorized) {
        domain2[1] -= 2;
      }
      return domain2;
    };
    $$.zoom = zoom2;
  },
  /**
   * 'start' event listener
   * @param {object} event Event object
   * @private
   */
  onZoomStart: function(event) {
    var $$ = this;
    var sourceEvent = event.sourceEvent;
    if (sourceEvent) {
      $$.zoom.startEvent = sourceEvent;
      $$.state.zooming = true;
      callFn($$.config.zoom_onzoomstart, $$.api, event);
    }
  },
  /**
   * 'zoom' event listener
   * @param {object} event Event object
   * @private
   */
  onZoom: function(event) {
    var _a2;
    var $$ = this;
    var config = $$.config, scale2 = $$.scale, state = $$.state, org = $$.org;
    var sourceEvent = event.sourceEvent;
    var isUnZoom = (event === null || event === void 0 ? void 0 : event.transform) === identity5;
    if (!config.zoom_enabled || $$.filterTargetsToShow($$.data.targets).length === 0 || !scale2.zoom && (sourceEvent === null || sourceEvent === void 0 ? void 0 : sourceEvent.type.indexOf("touch")) > -1 && (sourceEvent === null || sourceEvent === void 0 ? void 0 : sourceEvent.touches.length) === 1) {
      return;
    }
    if (event.sourceEvent) {
      state.zooming = true;
      state.domain = void 0;
    }
    var isMousemove = (sourceEvent === null || sourceEvent === void 0 ? void 0 : sourceEvent.type) === "mousemove";
    var isZoomOut = (sourceEvent === null || sourceEvent === void 0 ? void 0 : sourceEvent.wheelDelta) < 0;
    var transform3 = event.transform;
    if (!isMousemove && isZoomOut && scale2.x.domain().every(function(v, i) {
      return v !== org.xDomain[i];
    })) {
      scale2.x.domain(org.xDomain);
    }
    $$.zoom.updateTransformScale(transform3, config.zoom_type === "wheel" && sourceEvent);
    var doTransition = config.transition_duration > 0 && !config.subchart_show && (state.dragging || isUnZoom || !event.sourceEvent);
    $$.redraw({
      withTransition: doTransition,
      withY: config.zoom_rescale,
      withSubchart: false,
      withEventRect: false,
      withDimension: false
    });
    $$.state.cancelClick = isMousemove;
    !isUnZoom && callFn(config.zoom_onzoom, $$.api, (_a2 = $$.state.domain) !== null && _a2 !== void 0 ? _a2 : $$.zoom.getDomain());
  },
  /**
   * 'end' event listener
   * @param {object} event Event object
   * @private
   */
  onZoomEnd: function(event) {
    var _a2, _b;
    var $$ = this;
    var config = $$.config, state = $$.state;
    var startEvent = $$.zoom.startEvent;
    var e = event === null || event === void 0 ? void 0 : event.sourceEvent;
    var isUnZoom = (event === null || event === void 0 ? void 0 : event.transform) === identity5;
    if ((startEvent === null || startEvent === void 0 ? void 0 : startEvent.type.indexOf("touch")) > -1) {
      startEvent = startEvent.changedTouches[0];
      e = (_a2 = e === null || e === void 0 ? void 0 : e.changedTouches) === null || _a2 === void 0 ? void 0 : _a2[0];
    }
    if (config.zoom_type === "drag" && (e && startEvent.clientX === e.clientX && startEvent.clientY === e.clientY)) {
      return;
    }
    state.zooming = false;
    $$.redrawEventRect();
    $$.updateZoom();
    !isUnZoom && (e || state.dragging) && callFn(config.zoom_onzoomend, $$.api, (_b = $$.state.domain) !== null && _b !== void 0 ? _b : $$.zoom.getDomain());
  },
  /**
   * Update zoom
   * @param {boolean} force Force unzoom
   * @private
   */
  updateZoom: function(force) {
    var $$ = this;
    var _a2 = $$.scale, subX = _a2.subX, x3 = _a2.x, zoom2 = _a2.zoom;
    if (zoom2) {
      var zoomDomain = zoom2.domain();
      var xDomain = subX.domain();
      var delta = 0.015;
      var isfullyShown = $$.config.axis_x_inverted ? (zoomDomain[0] >= xDomain[0] || zoomDomain[0] + delta >= xDomain[0]) && (xDomain[1] >= zoomDomain[1] || xDomain[1] >= zoomDomain[1] + delta) : (zoomDomain[0] <= xDomain[0] || zoomDomain[0] - delta <= xDomain[0]) && (xDomain[1] <= zoomDomain[1] || xDomain[1] <= zoomDomain[1] - delta);
      if (force || isfullyShown) {
        $$.axis.x.scale(subX);
        x3.domain(subX.orgDomain());
        $$.scale.zoom = null;
      }
    }
  },
  /**
   * Set zoom transform to event rect
   * @param {Function} x x Axis scale function
   * @param {Array} domain Domain value to be set
   * @private
   */
  updateCurrentZoomTransform: function(x3, domain2) {
    var _a2;
    var $$ = this;
    var eventRect = $$.$el.eventRect, config = $$.config;
    var isRotated = config.axis_rotated;
    var translate = [-x3(domain2[0]), 0];
    var transform3 = (_a2 = identity5.scale(x3.range()[1] / (x3(domain2[1]) - x3(domain2[0])))).translate.apply(_a2, isRotated ? translate.reverse() : translate);
    eventRect.call($$.zoom.transform, transform3);
  },
  /**
   * Attach zoom event on <rect>
   * @private
   */
  bindZoomOnEventRect: function() {
    var $$ = this;
    var config = $$.config, eventRect = $$.$el.eventRect;
    var behaviour = config.zoom_type === "drag" ? $$.zoomBehaviour : $$.zoom;
    $$.$el.svg.on("wheel", function() {
    });
    eventRect === null || eventRect === void 0 ? void 0 : eventRect.call(behaviour).on("dblclick.zoom", null);
  },
  /**
   * Initialize the drag behaviour used for zooming.
   * @private
   */
  initZoomBehaviour: function() {
    var $$ = this;
    var config = $$.config, state = $$.state;
    var isRotated = config.axis_rotated;
    var start2 = 0;
    var end = 0;
    var zoomRect;
    var prop = {
      axis: isRotated ? "y" : "x",
      attr: isRotated ? "height" : "width",
      index: isRotated ? 1 : 0
    };
    $$.zoomBehaviour = drag_default().clickDistance(4).on("start", function(event) {
      state.event = event;
      $$.setDragStatus(true);
      $$.unselectRect();
      if (!zoomRect) {
        zoomRect = $$.$el.main.append("rect").attr("clip-path", state.clip.path).attr("class", $ZOOM.zoomBrush).attr("width", isRotated ? state.width : 0).attr("height", isRotated ? 0 : state.height);
      }
      start2 = getPointer(event, this)[prop.index];
      end = start2;
      zoomRect.attr(prop.axis, start2).attr(prop.attr, 0);
      $$.onZoomStart(event);
    }).on("drag", function(event) {
      end = getPointer(event, this)[prop.index];
      zoomRect.attr(prop.axis, Math.min(start2, end)).attr(prop.attr, Math.abs(end - start2));
    }).on("end", function(event) {
      var _a2;
      var scale2 = $$.scale.zoom || $$.scale.x;
      state.event = event;
      zoomRect.attr(prop.axis, 0).attr(prop.attr, 0);
      if (start2 > end) {
        _a2 = [end, start2], start2 = _a2[0], end = _a2[1];
      }
      if (start2 < 0) {
        end += Math.abs(start2);
        start2 = 0;
      }
      if (start2 !== end) {
        $$.api.zoom([start2, end].map(function(v) {
          return scale2.invert(v);
        }));
      }
      $$.setDragStatus(false);
    });
  },
  setZoomResetButton: function() {
    var $$ = this;
    var config = $$.config, $el = $$.$el;
    var resetButton = config.zoom_resetButton;
    if (resetButton && config.zoom_type === "drag") {
      if (!$el.zoomResetBtn) {
        $el.zoomResetBtn = $$.$el.chart.append("div").classed($COMMON.button, true).append("span").on("click", function() {
          isFunction(resetButton.onclick) && resetButton.onclick.bind($$.api)(this);
          $$.api.unzoom();
        }).classed($ZOOM.buttonZoomReset, true).text(resetButton.text || "Reset Zoom");
      } else {
        $el.zoomResetBtn.style("display", null);
      }
    }
  },
  getZoomTransform: function() {
    var $$ = this;
    var eventRect = $$.$el.eventRect;
    return (eventRect === null || eventRect === void 0 ? void 0 : eventRect.node()) ? transform(eventRect.node()) : { k: 1 };
  }
};
var optDataSelection = {
  /**
   * Set data selection enabled<br><br>
   * If this option is set true, we can select the data points and get/set its state of selection by API (e.g. select, unselect, selected).
   *  - **NOTE:** for ESM imports, needs to import 'selection' exports and instantiate it by calling `selection()`.
   *    - `enabled: selection()`
   * @name data․selection․enabled
   * @memberof Options
   * @type {boolean}
   * @default false
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Data.DataSelection)
   * @example
   * data: {
   *    selection: {
   *       enabled: true
   *    }
   * }
   * @example
   * // importing ESM
   * import bb, {selection} from "billboard.js";
   *
   * data: {
   *    selection: {
   *       enabled: selection(),
   *       ...
   *    }
   * }
   */
  data_selection_enabled: false,
  /**
   * Set grouped selection enabled.<br><br>
   * If this option set true, multiple data points that have same x value will be selected by one selection.
   * @name data․selection․grouped
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * data: {
   *    selection: {
   *       grouped: true
   *    }
   * }
   */
  data_selection_grouped: false,
  /**
   * Set a callback for each data point to determine if it's selectable or not.<br><br>
   * The callback will receive d as an argument and it has some parameters like id, value, index. This callback should return boolean.
   * @name data․selection․isselectable
   * @memberof Options
   * @type {Function}
   * @default function() { return true; }
   * @example
   * data: {
   *    selection: {
   *       isselectable: function(d) { ... }
   *    }
   * }
   */
  data_selection_isselectable: function() {
    return true;
  },
  /**
   * Set multiple data points selection enabled.<br><br>
   * If this option set true, multile data points can have the selected state at the same time. If false set, only one data point can have the selected state and the others will be unselected when the new data point is selected.
   * @name data․selection․multiple
   * @memberof Options
   * @type {boolean}
   * @default true
   * @example
   * data: {
   *    selection: {
   *       multiple: false
   *    }
   * }
   */
  data_selection_multiple: true,
  /**
   * Enable to select data points by dragging.
   * If this option set true, data points can be selected by dragging.
   * - **NOTE:** If this option set true, scrolling on the chart will be disabled because dragging event will handle the event.
   * @name data․selection․draggable
   * @memberof Options
   * @type {boolean}
   * @default false
   * @example
   * data: {
   *    selection: {
   *       draggable: true
   *   }
   * }
   */
  data_selection_draggable: false,
  /**
   * Set a callback for on data selection.
   * @name data․onselected
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onselected: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4, name: "data1"}
   *        // element - <circle>
   *        ...
   *    }
   * }
   */
  data_onselected: function() {
  },
  /**
   * Set a callback for on data un-selection.
   * @name data․onunselected
   * @memberof Options
   * @type {Function}
   * @default function() {}
   * @example
   * data: {
   *     onunselected: function(d, element) {
   *        // d - ex) {x: 4, value: 150, id: "data1", index: 4, name: "data1"}
   *        // element - <circle>
   *        ...
   *    }
   * }
   */
  data_onunselected: function() {
  }
};
var optSubchart = {
  /**
   * Set subchart options.
   * - **NOTE:** Not supported for `bubble`, `scatter` and non-Axis based(pie, donut, gauge, radar) types.
   * @name subchart
   * @memberof Options
   * @type {object}
   * @property {object} subchart Subchart object
   * @property {boolean} [subchart.show=false] Show sub chart on the bottom of the chart.
   *  - **NOTE:** for ESM imports, needs to import 'subchart' exports and instantiate it by calling `subchart()`.
   *    - `show: subchart()`
   * @property {boolean} [subchart.showHandle=false] Show sub chart's handle.
   * @property {boolean} [subchart.axis.x.show=true] Show or hide x axis.
   * @property {boolean} [subchart.axis.x.tick.show=true] Show or hide x axis tick line.
   * @property {Function|string} [subchart.axis.x.tick.format] Use custom format for x axis ticks - see [axis.x.tick.format](#.axis․x․tick․format) for details.
   * @property {boolean} [subchart.axis.x.tick.text.show=true] Show or hide x axis tick text.
   * @property {Array} [subchart.init.range] Set initial selection domain range.
   * @property {number} [subchart.size.height] Change the height of the subchart.
   * @property {Function} [subchart.onbrush] Set callback for brush event.<br>
   *  Specified function receives the current zoomed x domain.
   * @see [Demo](https://naver.github.io/billboard.js/demo/#Interaction.SubChart)
   * @example
   *  subchart: {
   *      show: true,
   *      showHandle: true,
   *      size: {
   *          height: 20
   *      },
   *      init: {
   *          // specify initial range domain selection
   *          range: [1, 2]
   *      },
   *      axis: {
   *      	x: {
   *      	  show: true,
   *      	    tick: {
   *      	      show: true,
   *      	      format: (x) => d3Format(".1f")(x)
   *      	      text: {
   *      	        show: false
   *      	      }
   *      	    }
   *      	}
   *      },
   *      onbrush: function(domain) { ... }
   *  }
   * @example
   * // importing ESM
   * import bb, {subchart} from "billboard.js";
   *
   * subchart: {
   *      show: subchart(),
   *      ...
   * }
   */
  subchart_show: false,
  subchart_showHandle: false,
  subchart_size_height: 60,
  subchart_axis_x_show: true,
  subchart_axis_x_tick_show: true,
  subchart_axis_x_tick_format: void 0,
  subchart_axis_x_tick_text_show: true,
  subchart_init_range: void 0,
  subchart_onbrush: function() {
  }
};
var optZoom = {
  /**
   * Set zoom options
   * @name zoom
   * @memberof Options
   * @type {object}
   * @property {object} zoom Zoom object
   * @property {boolean} [zoom.enabled=false] Enable zooming.
   *  - **NOTE:** for ESM imports, needs to import 'zoom' exports and instantiate it by calling `zoom()`.
   *    - `enabled: zoom()`
   * @property {string} [zoom.type='wheel'] Set zoom interaction type.
   *  - **Available types:**
   *    - wheel
   *    - drag
   * @property {boolean} [zoom.rescale=false] Enable to rescale after zooming.<br>
   *  If true set, y domain will be updated according to the zoomed region.
   * @property {Array} [zoom.extent=[1, 10]] Change zoom extent.
   * @property {number|Date} [zoom.x.min] Set x Axis minimum zoom range
   * @property {number|Date} [zoom.x.max] Set x Axis maximum zoom range
   * @property {Function} [zoom.onzoomstart=undefined] Set callback that is called when zooming starts.<br>
   *  Specified function receives the zoom event.
   * @property {Function} [zoom.onzoom=undefined] Set callback that is called when the chart is zooming.<br>
   *  Specified function receives the zoomed domain.
   * @property {Function} [zoom.onzoomend=undefined] Set callback that is called when zooming ends.<br>
   *  Specified function receives the zoomed domain.
   * @property {boolean|object} [zoom.resetButton=true] Set to display zoom reset button for 'drag' type zoom
   * @property {Function} [zoom.resetButton.onclick] Set callback when clicks the reset button. The callback will receive reset button element reference as argument.
   * @property {string} [zoom.resetButton.text='Reset Zoom'] Text value for zoom reset button.
   * @see [Demo:zoom](https://naver.github.io/billboard.js/demo/#Interaction.Zoom)
   * @see [Demo:drag zoom](https://naver.github.io/billboard.js/demo/#Interaction.DragZoom)
   * @example
   *  zoom: {
   *      enabled: true,
   *      type: "drag",
   *      rescale: true,
   *      extent: [1, 100]  // enable more zooming
   *      x: {
   *          min: -1,  // set min range
   *          max: 10  // set max range
   *      },
   *      onzoomstart: function(event) { ... },
   *      onzoom: function(domain) { ... },
   *      onzoomend: function(domain) { ... },
   *
   *      // show reset button when is zoomed-in
   *      resetButton: true,
   *
   *      resetButton: {
   *          // onclick callback when reset button is clicked
   *          onclick: function(button) {
   *            button; // Reset button element reference
   *            ...
   *          },
   *
   *          // customized text value for reset zoom button
   *          text: "Unzoom"
   *      }
   *  }
   * @example
   * // importing ESM
   * import bb, {zoom} from "billboard.js";
   *
   * zoom: {
   *      enabled: zoom(),
   *      ...
   * }
   */
  zoom_enabled: false,
  zoom_type: "wheel",
  zoom_extent: void 0,
  zoom_privileged: false,
  zoom_rescale: false,
  zoom_onzoom: void 0,
  zoom_onzoomstart: void 0,
  zoom_onzoomend: void 0,
  zoom_resetButton: true,
  zoom_x_min: void 0,
  zoom_x_max: void 0
};
var selectionModule = function() {
  extend2(ChartInternal.prototype, selection2);
  extend2(Chart.prototype, apiSelection);
  Options.setOptions([optDataSelection]);
  return (selectionModule = function() {
    return true;
  })();
};
var subchartModule = function() {
  extend2(ChartInternal.prototype, subchart);
  extend2(Chart.prototype, apiSubchart);
  Options.setOptions([optSubchart]);
  return (subchartModule = function() {
    return true;
  })();
};
var zoomModule = function() {
  extend2(ChartInternal.prototype, zoom);
  extend2(Chart.prototype, apiZoom);
  Options.setOptions([optZoom]);
  return (zoomModule = function() {
    return true;
  })();
};
var defaults = {};
var bb = {
  /**
   * Version information
   * @property {string} version version
   * @example
   *    bb.version;  // "1.0.0"
   * @memberof bb
   */
  version: "3.11.3",
  /**
   * Generate chart
   * - **NOTE:** Bear in mind for the possiblity of ***throwing an error***, during the generation when:
   *   - Unused option value is given.
   *     - ex) For `data.type="pie"` option, setting 'axis' option can cause unexpected generation error.
   *   - Insufficient value is given for certain option used.
   *     - ex) `data: { x: "x", columns: [["x"], ["data1", 30, 200, 100]] }`
   * @param {Options} config chart options
   * @memberof bb
   * @returns {Chart}
   * @see {@link Options} for different generation options
   * @see {@link Chart} for different methods API
   * @example
   *  <!-- chart holder -->
   * <div id="LineChart"></div>
   * @example
   *  // Generate chart with options
   *  var chart = bb.generate({
   *      "bindto": "#LineChart"
   *      "data": {
   *          "columns": [
   *              ["data1", 30, 200, 100, 400, 150, 250],
   *              ["data2", 50, 20, 10, 40, 15, 25]
   *           ],
   *          "type": "line"
   *      }
   *  });
   *
   *  // call some API
   *  // ex) get the data of 'data1'
   *  chart.data("data1");
   * @example
   * // Generate chart by importing ESM
   * // Import types to be used only, where this will make smaller bundle size.
   * import bb, {
   *   area,
   *   areaLineRange,
   *   areaSpline,
   *   areaSplineRange,
   *   areaStep,
   *   bar,
   *   bubble,
   *   donut,
   *   gauge,
   *   line,
   *   pie,
   *   polar,
   *   radar,
   *   scatter,
   *   spline,
   *   step
   * }
   *
   * bb.generate({
   *      "bindto": "#LineChart"
   *      "data": {
   *          "columns": [
   *              ["data1", 30, 200, 100, 400, 150, 250],
   *              ["data2", 50, 20, 10, 40, 15, 25]
   *           ]
   *      },
   *      type: line(),
   *
   *      // or
   *      types: {
   *        data1: bar(),
   *        data2: step()
   *      }
   * });
   */
  generate: function(config) {
    var options2 = mergeObj({}, defaults, config);
    var inst = new Chart(options2);
    inst.internal.charts = this.instance;
    this.instance.push(inst);
    return inst;
  },
  /**
   * Set or get global default options.
   * - **NOTE:**
   *   - The options values settings are valid within page context only.
   *   - If is called multiple times, will override the last value.
   * @param {Options} options chart options
   * @memberof bb
   * @returns {Options}
   * @see {@link Options}
   * @example
   * // Set same option value as for `.generate()`
   * bb.defaults({
   *   data: {
   *     type: "bar"
   *   }
   * });
   *
   * bb.defaults();  // {data:{type: "bar"}}
   *
   * // data.type defaults to 'bar'
   * var chart = bb.generate({ ... });
   */
  defaults: function(options2) {
    if (isObject(options2)) {
      defaults = options2;
    }
    return defaults;
  },
  /**
   * An array containing instance created
   * @property {Array} instance instance array
   * @example
   *  // generate charts
   *  var chart1 = bb.generate(...);
   *  var chart2 = bb.generate(...);
   *
   *  bb.instance;  // [ chart1, chart2, ... ]
   * @memberof bb
   */
  instance: [],
  /**
   * Namespace for plugins
   * @property {object} plugin plugin namespace
   * @example
   *  // Stanford diagram plugin
   *  bb.plugin.stanford;
   * @memberof bb
   */
  plugin: {}
};
export {
  area,
  areaLineRange,
  areaSpline,
  areaSplineRange,
  areaStep,
  bar,
  bb,
  bubble,
  candlestick,
  bb as default,
  donut,
  gauge,
  line,
  pie,
  polar,
  radar,
  scatter,
  selectionModule as selection,
  spline,
  step,
  subchartModule as subchart,
  treemap,
  zoomModule as zoom
};
/*! Bundled license information:

billboard.js/dist-esm/billboard.js:
  (*!
  * Copyright (c) 2017 ~ present NAVER Corp.
   * billboard.js project is licensed under the MIT license
   * 
   * billboard.js, JavaScript chart library
   * https://naver.github.io/billboard.js/
   * 
   * @version 3.11.3
  *)
*/
//# sourceMappingURL=billboard__js.js.map
