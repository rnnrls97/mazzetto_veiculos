import { Extension } from './types';
/**
 * Handle the paste event, if the pasted text is a URL and something is selected, it will be converted to link/image markup.
 */
export declare const linkPasteExtension: Extension;
/**
 * Handle `tab`/`shift+tab` combination. Will insert or remove an intend depends on selection
 */
export declare const indentExtension: Extension;
/**
 * Handle next-line event. Will wrap current list sequence if needed
 */
export declare const prefixWrappingExtension: Extension;
export declare const properLineRemoveBehaviorExtension: Extension;
export declare const orderedListAutoCorrectExtension: Extension;
export declare const fixUndoBehaviorExtension: Extension;
