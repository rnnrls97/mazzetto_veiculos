import { Cursor } from './Cursor.new';
import { Command, CommandTrigger, TextareaMarkdownOptions } from './types';
declare type BootstrapConfig = {
    commands?: Command[];
    options?: Partial<TextareaMarkdownOptions>;
};
export declare const bootstrapTextareaMarkdown: (textarea: HTMLTextAreaElement, config?: BootstrapConfig) => {
    trigger: CommandTrigger;
    dispose: () => void;
    cursor: Cursor;
};
export {};
